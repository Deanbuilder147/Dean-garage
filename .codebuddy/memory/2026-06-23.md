# 2026-06-23

## Phase 29-I 鹦鹉螺号定向置换 (11:19-11:30)

### 三大工序全部完成并真机验证通过

---

### 工序一：绝杀登录死锁 — 注入纯净验证管道

**修改文件**：
- `frontend/src/api/client.js` — 响应拦截器白名单重构
  - 401 拦截器从「无差别踢回 /login」改为「仅 Token 真过期 + 非公开路径」时重定向
  - 公开路径白名单：`/auth/login`, `/auth/register`, `/auth/health`, `/comm/health`, `/combat/glossary-config`, `/map/list`, `/comm/watch-feed`, `/comm/watch-buffer-status`
  - 无 Token 的游客/观战流请求被 `401` 时不再跳转登录页
- `frontend/src/views/NewLoginView.vue` — 表单 ID 大一统规范
  - 登录：`universe-username`, `universe-password`
  - 注册：`universe-reg-username`, `universe-reg-email`, `universe-reg-password`, `universe-reg-verify`
- `frontend/src/views/TerminalView.vue` — 隐藏 autofill 字段 ID 更新为 `universe-username`, `universe-password`

### 工序二：拔除房间恶疮 — 3006 持久化独裁

**修改文件**：
- `frontend/src/api/client.js` — 房间 API 主权移交
  - `commAPI` 仅保留 `sendMessage` (Socket.io) 和 `getWatchBufferStatus`，其余注释废弃
  - `onlineBattleAPI` 新增 `setReady`, `startBattle`, `updateSettings`, `deleteRoom`, `getChat`, `sendChat`
  - `joinRoom` 从 PUT 修复为 POST（对齐 3006 真实端点）
- `frontend/src/views/NewBattlefieldSelector.vue` — `commAPI` → `onlineBattleAPI` 创建房间
- `frontend/src/views/NewPreparationRoom.vue` — `commAPI` → `onlineBattleAPI` 获取房间
- `services/comm-service/src/index.js` — 废弃所有房间 HTTP 写入端点（创建/加入/准备/开始/离开/AI），全部包裹注释
  - 新增观战缓冲队列 `watchBufferQueue`（FIFO 容量 200，5 秒 setTimeout 延迟阻尼 `io.to(battle-XXX).emit('watch-stream')`）
  - 新增 `/api/comm/watch-feed` 馈入端点和 `/api/comm/watch-buffer-status` 状态查询

### 工序三：冷打包冲刷与真机对账

- npm run build → 新 JS hash `DBMzvX9L`（旧 `BpD71Uo3`）
- `docker compose build --no-cache frontend` → 镜像 `fc9f2ab39`
- `docker compose up -d --force-recreate frontend` / comm-service
- comm-service 重建时依赖链导致 map/combat 也级联重创建

### 验证结果（全通过）

| 验证项 | 结果 |
|--------|------|
| 前端 /login 页 | HTTP 200 ✅ |
| 401 白名单逻辑 | 打包进 JS: `!!token && !publicPath` ✅ |
| universe-username 表单 ID | 登录+注册双表单 2 处 ✅ |
| 3005 房间创建端点 | HTTP 404（已废弃） ✅ |
| 3006 房间端点 | HTTP 401（需鉴权，端点存活） ✅ |
| 观战缓冲队列 | `{"queueSize":0,"hasPendingFlush":false,"maxSize":200}` ✅ |
| 前端容器健康 | healthy ✅ |
| comm容器健康 | healthy ✅ |

---

## Phase 29-J mecha-gateway Docker 构建修复 (12:44-13:48)

### 问题链
1. `apk add` 卡死在 Alpine CDN 下载（国内网络慢，285+秒无进展）
2. `file:` 本地依赖 `@mecha/shared-kernel` 符号链接指向 `.ts` 源文件，Node.js 无法执行
3. shared-kernel 缺 `typescript` 依赖，`npx tsc` 失败

### Dockerfile 最终状态

**服务器**：106.54.197.69，项目路径 `/root/mecha-universe-engine_20260623124438`

**关键修复**：
- Alpine 包源：`dl-cdn.alpinelinux.org` → `mirrors.tuna.tsinghua.edu.cn`（apk 从 285s 降至 1.5s）
- npm 镜像：`registry.npmmirror.com`
- Builder 编译 shared-kernel：`npm install && npm install typescript --no-save && npx tsc`
- 运行时拷贝编译产物 `shared-kernel/dist/` 并 sed 修改 package.json 入口 `./src/*.ts` → `./dist/*.js`
- 运行时安装生产依赖：sed 删除 @mecha/shared-kernel 行 → npm install → ln -s 手动创建符号链接

### 最终结果
- 镜像：`mecha-universe-engine_20260623124438-mecha-gateway:latest` (SHA: `4c9089679fc4`)
- 容器运行正常，健康检查：`{"status":"ok","service":"mecha-universe-gateway","version":"29.0.0"}`
- 端口：3006

---

## Phase 29-P0: 熔铸无状态大一统画布内核 (15:28-16:16)

### Step 1: 构建引擎
- 创建 `frontend/src/components/HexGridCanvasEngine.vue` (~955行)
- 无状态：纯 props 输入 / emit 输出，无 vuex/pinia 依赖
- Props: gridData, highlightCells, drawFn, isoConfig, showCoords, showHover, useTerrainCache
- 唯一 Emit: cell-clicked({q, r})
- 地形缓存: 离屏 Canvas, invalidateTerrain() 触发重绘
- 视口裁剪: isInViewport(q, r) 过滤不可见格子

### Step 2: 地图编辑器改造
- `NewBattlefieldView.vue` 重构，mount HexGridCanvasEngine
- 移除所有内置 Canvas 绘制，画笔通过 @cell-clicked 处理
- 右键擦除通过 contextmenu 事件监听引擎 canvas
- 反查验证通过：Canvas 正确渲染，画笔即时生效，无控制台错误

### Step 3: 战场视图改造
- `NewBattleView.vue` 重构，mount 同一引擎
- drawBattleScene 移除地形/坐标/悬停绘制（引擎负责）
- 仅保留战斗专用叠加层：移动/攻击/技能范围高亮 + 单位 Billboard 渲染
- 悬停坐标通过 engineContainer canvas mousemove + getHexAtEvent 手动接入
- 图例 Legend 从引擎 slot 移出为绝对定位浮动元素
- .mapCanvas → .mainCanvas 引用更新

### 关键 Bug 修复
- **isCellInViewport/isInViewport 命名不一致**：引擎定义函数 `isCellInViewport` 但 drawFn 传入 `isInViewport`，导致 ReferenceError
- 修复：统一重命名为 `isInViewport`（引擎内 3 处：函数定义、渲染循环引用、defineExpose）

### 反查验证
- 编辑器：Canvas 1022×278，画笔点击即时变色，无错误
- 战场：Canvas 1020×626，isInViewport ReferenceError 已消除，控件完整
- 剩余错误均为预存在 API 404（combat/hangar/map 服务未部署），与此次改动无关

---

## Phase 29-P1: 大一统权限网关部署 (16:42-17:17)

### 部署目标
- 服务器: 106.54.197.69，容器名 `mecha-online-battle`，端口 3006
- 镜像: `mecha-gateway:latest` (含 UserRole, docker pull 当日 16:41)
- 项目路径: `/root/mecha-universe-engine_20260623170229`

### 关键修复（2个线上 Bug）
1. **shared-kernel .ts 加载崩溃**: 容器内 `package.json` 仍指向 `./src/index.ts`，Node.js 无法执行 TypeScript → 编译 shared-kernel 生成 `dist/`，sed 修改 package.json main → `./dist/index.js`
2. **房间列表 401 误判**: `router.use('/api/rooms', authenticate, requireAuth)` 会**剥离路径前缀**，导致 `req.path` 从 `/api/rooms` 变成 `/`，`isPublicPath('/')` 无法匹配 → 修复 middleware 使用 `req.originalUrl` 替代 `req.path`

### 服务器端改动
- `backend-gateway/src/middleware/auth.ts`: `req.path` → `req.originalUrl || req.path`
- `backend-gateway/src/routes/rooms.ts`: 删除冗余 GET 穿透 handler，router.use 统一处理（GET/HEAD 放行）
- shared-kernel 编译出 dist/（tokens.js, types.js, index.js）
- 前端 dist 更新: nginx.conf 已含 P1 路由，SPA hash `FJgWwzMP`

### 验证结果（全通过）
| # | 验证项 | 结果 |
|---|--------|------|
| 1 | Health (3006) | ✅ 200 |
| 2 | Campaign trial (guest) | ✅ 200 |
| 3 | Rooms list (guest) | ✅ 200 |
| 4 | Login | ✅ 200 (role=user, credits=9) |
| 5 | Units (no auth) | ✅ 401 |
| 6 | Admin (no auth) | ✅ 401 |
| 7 | Frontend (8081) | ✅ 200 |
| 8 | JWT 荷载含 role+credits | ✅ |
| 9 | AI 生成扣积分 (10→9) | ✅ |
| 10 | 管理员端点 403 (role=user) | ✅ ROLE_FORBIDDEN |
| 11 | Nginx 代理全链路 | ✅ 全部通过 |

### 容器状态
- mecha-online-battle: Up (healthy), 端口 3006
- mecha-frontend: Up, 端口 8081
- 旧 mecha-gateway 已清理

---

## 任务列表清理 (17:18)
- 删除 `.codebuddy/plans/` 下两个过时计划文件：
  - 海豹骰子核心集成到机甲战棋_e359d871.md（全部 done）
  - hex-editor-integration_bc86816.md（已过期）
- 任务列表现已清空

---

## TerminalView.vue 全量清理 (17:36)
- **本地**: 删除 `frontend/src/views/TerminalView.vue`, `frontend-files/src/views/TerminalView.vue`
- **main.js**: 移除 import、`/terminal` 路由、路由守卫白名单条目
- **服务器**: 清理 `original-project`、`mecha-universe-engine`、所有备份目录中的 TerminalView.vue（共 20+ 处残留）
- **Git**: `git rm --cached` 移除追踪，下次提交生效
- **生产容器**: 已无残留（先验证过）

---

## Phase 29-P2: 大一统词元AST语法树 + 定状补状态机 + dicescript管道 (17:23-17:54)

### Step 1: 共享内核 OrderClause 句式结构
- `tokens.ts` 新增: `Qualifier`, `Adverbial`, `Complement`, `OrderClause`, `OrderClauseError` interface
- 定状补三层嵌套语法树骨架固化为标准 TypeScript 类型
- 编译产物 dist 类型声明 100% 无损

### Step 2: skillExecutor.ts v2.0 动态断言状态机
- 全量改写为 Rule JSON 遍历（无硬编码技能名）
- MAX_LOOP_STEP=10 死循环熔断（loop + predicate 双重拦截）
- 外层刚性 try...catch + `[CRITICAL FAILED]` 详细日志
- 支持 OrderClause 规则 JSON 和 DSL 字符串双模式

### Step 3: damagePipe.ts 层叠管理器
- LayeredExecutor: 定→状→补 严格顺序执行
- 状语按 weight 排序, 补语按 mode 层级排序 (flat→percent→dice_bonus)
- ExclusionPool 互斥池支持 BUFF/DEBUFF/DAMAGE_BOOST 标签
- 内置 STABLE_SHOT_CLAUSE 稳定光束狙击测试案例

### P2 反查验证（真机 docker exec 端到端）
| # | 测试 | 结果 |
|---|------|:--:|
| 2 | 垃圾词元 BUEF 安全沙盒 | ✅ 沙盒存活，最终伤害=19 |
| 3 | 稳定光束 定语熔断 hasMoved=true | ✅ blocked=true，伤害=0 |
| 3B | 稳定光束 hasMoved=false + 固定骰=6 | ✅ 伤害=36 (26基础+5flat+5dice_bonus) |
| GW | 网关健康/登录/房间 | ✅ 全部正常 |

### 容器状态
- mecha-online-battle: Up (healthy), 端口3006
- 新文件: skillExecutor.js, damagePipe.js
- shared-kernel dist 已同步
