# 2026-06-25

## Excel 解析器旧版兼容改造 (v2.0 → v2.1)

将新版 backend-gateway 的 Excel 解析器全面对齐旧版 hangar-service `/import-excel` 直导模式：

**修改文件：**
- `mecha-universe-engine/backend-gateway/src/services/excel-template.ts` — 坐标重写 (C2/F2/I2, 行4-8, 行12-22)
- `mecha-universe-engine/backend-gateway/src/services/excel-parser.ts` — 工作表选"设定器", 技能按行号范围分配, 阵营中文映射
- `mecha-universe-engine/backend-gateway/src/services/excel-validator.ts` — 基于 owner(非 slot) 验证

**核心对齐点：**
- 基本信息: name=C2, codename=F2, faction=I2 (原 B2/B3/B4)
- 单位属性: 行4-8 (原 行7-11)
- 技能: 行12-22 按行号范围分配归属 (原 行14+slot列数值)
- 工作表: 优先"设定器" (原 取第一个)

## 部署完成

**已成功部署到服务器 106.54.197.69:3006**

额外修复：
- `sqlite.ts` 迁移函数加了顶层 try-catch，解决 sql.js 对已持久化 DB 的 `duplicate column name: is_public` 崩溃问题
- CREATE TABLE units DDL 新增 is_public/review_status 列（对新库生效）
- Docker build 必须加 --no-cache 否则 dist 缓存不会刷新

## 登录 502 Bug 修复

**现象**: 前端 POST `/api/auth/login` 返回 502 Bad Gateway (nginx/1.31.2)
**根因**: `mecha-gateway` 容器用 docker run 单独创建在 bridge 网络；前端在 `mecha-universe-engine_mecha-net` 网络。DNS 不通。
**修复**: 已通过 Compose 永久解决（见下文 Phase 29-TrueFinal）

---

## ★ Phase 29-TrueFinal: 大一统 Compose 网络强并线 (最高通车令)

### 工序一：docker-compose.yml 重构 ✅
- **永久焊入 mecha-gateway 服务**: build context=`.`, Dockerfile=`backend-gateway/Dockerfile`
- **精简为核心三件套**: postgres(:5432) + mecha-gateway(:3006) + frontend(:8081→80)
- **Legacy 微服务全部移除**: auth/hangar/map/combat/comm/online-battle 源码目录不在服务器上
- **网络统一**: `mecha-universe-engine_mecha-net`，所有容器同网格互通
- **修复的 Bug**:
  - env_file 路径双重嵌套 (`./mecha-universe-engine/.env` → `.env`)
  - build context 双重嵌套 (`./mecha-universe-engine` → `.`)
  - online-battle-service 端口冲突 3006→3007（最终移除）

### 工序二：表现层修复 ✅
- **client.js Token 拼字**: 第21行 `Authorization = Bearer ${token}` → **已正确，无需修改**
- **GlossaryView.vue 词条解包**: 第741行 `data.glossary || data` → **已兼容包裹层，无需修改**
- **frontend/Dockerfile**: EXPOSE 8081→**80**, HEALTHCHECK localhost:8081→**localhost/** （nginx 内部 listen 80）
- **frontend/nginx.conf**: 两处 `mecha-comm:3005` 引用→**mecha-gateway:3006**（/api/comm/ + /socket.io/）

### 工序三：全冷清洗部署 ✅
```
docker compose down --remove-orphans  # 清理旧 docker run 残留
docker compose build --no-cache        # 无缓存重建 gateway+frontend
docker compose up -d                    # 三件套统一拉起
```

### 终极验房结果
| 验证项 | 结果 |
|--------|------|
| Gateway Health `/health` | `{"status":"ok"}` ✅ |
| 登录接口 `POST /api/auth/login` | 正常业务响应 (502 彻底消失) ✅ |
| mecha-frontend | Up, **healthy**, 8081→80 ✅ |
| mecha-gateway | Up, :3006 ✅ |
| mecha-battle-db | Up, healthy, :5432 ✅ |

### 修改文件清单
| 文件 | 操作 |
|------|------|
| `docker-compose.yml` | 🆕 重写：核心三件套 + 统一网络 |
| `frontend/Dockerfile` | 🔧 EXPOSE+HEALTHCHECK 修正为端口 80 |
| `frontend/nginx.conf` | 🔧 mecha-comm→mecha-gateway 两处 |
| `code-index.md` | 🆕 更新部署架构说明 |
| `MEMORY.md` | 🔧 容器架构更新为核心三件套 |
