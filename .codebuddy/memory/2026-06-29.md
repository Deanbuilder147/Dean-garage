# 2026-06-29 工作日志

## Phase 29-CanvasTrueCenter 绝杀令执行记录

### 问题
NewBattleView的Canvas六角格棋盘左上角沿线变平、显示不完整，地图编辑器正常。

### 前期修复（前2轮，未解决）
1. centerGrid() 居中算法重构（ISO世界尺寸）
2. initTerrainCache() 缓存尺寸扩展
3. spacingH/V 改为ref响应式
4. loadViewConfig() 全量7字段读取

### 绝杀令三大工序（第3轮，已部署）
**工序一 — CTM 补齐 Rotation 旋转变换**:
- `draw()` CTM链新增 `ctx.rotate(ISO.rotation * Math.PI / 180)` (基准-24°)
- `centerGrid()` 重构为完整 ISO+rotation 变换链，4角点AABB边界检测
- `defineExpose` 新增暴露 `centerGrid` 方法

**工序二 — 双Tick物理绝杀 + resize防护**:
- onMounted末端注入 `await nextTick(); await nextTick()` + centerGrid/invalidateTerrain/redraw连招
- resize事件处理器加入centerGrid()+redraw()触发链（100ms延迟）
- onUnmounted同步更新清理

**工序三 — 全冷清洗部署**:
- docker compose down + build --no-cache + up -d
- 部署成功: mecha-frontend Running (healthy), HTTP 200

**状态**: ⏳ 待用户验证

## Phase 29-TrueFinal 终极纠偏令（第4轮修复 — 已部署）

### 核心变更（相对上一轮）
**工序一 — 撤销 ctx.rotate() 魔改**:
- `draw()` CTM链：**物理删除** `ctx.rotate(ISO.rotation)` 代码，恢复纯净 `translate→scale→transform(shearX,scaleY)`
- `centerGrid()`：移除 rotation 旋转变换计算，恢复纯 ISO `isoTransform(p)` (worldX=x*scaleX+y*shearX, worldY=y*scaleY)
- 原因：rotate破坏了2D屏幕像素到Hex空间坐标的逆矩阵转换，导致鼠标错位
- **坐标系原则**：rotation 由 hexUtils.js 公式完美接管，Canvas CTM 保持100%纯净！

**工序二 — 双Tick相机死锁（保留不变）**:
- onMounted末端双 nextTick + centerGrid/invalidateTerrain/redraw ✅ 已就位
- resize防护 _resizeHandler + centerGrid/redraw ✅ 已就位

**工序三 — 全冷清洗部署**:
- docker compose down + build --no-cache + up -d ✅ 成功
- mecha-frontend Running (healthy), HTTP 200 OK

**待验证**: 鼠标高亮是否像素级精准、左上角切边是否消失

---

## Phase 29-Diagnostic 探针植入 (2026-06-29 17:44)

### 三大探针注入 HexGridCanvasEngine.vue
- **探针一**: `window._debugCanvasStatus()` — 14项全量体检报告（draw()首次+2秒节流自动打印）
- **探针二**: `window._debugMouseProbe = true` — 鼠标坐标标尺追踪（mousemove handler）
- **探针三**: 静态CSS样式污染分析 → 生成《双盲对照诊断表》

### 探针三静态分析关键发现（战场 vs 编辑器）
| 属性 | 战场(异常) | 编辑器(正常) | 风险 |
|------|-----------|-------------|------|
| 外层wrapper | `.game-canvas-sandbox` | ❌ 无 | 🔴 |
| 外层border | `border:1px` | ❌ 无 | 🔴 |
| 双层border | wrapper 1px + engine 1px | 仅 engine 1px | 🔴 |
| contain:layout | ✅ | ❌ | 🟡 |
| overflow层级 | 4层 | 2层 | 🟡 |
| engine定位 | absolute inset-0 | flex:1 | 🟡 |
| CSS transform污染 | ❌ 无 | ❌ 无 | ✅ |

### 部署
- 服务器: lhins-2fs1rzs8, 项目路径 /root/mecha-universe-engine
- 前端已构建并重新部署, mecha-frontend healthy, HTTP 200
- 诊断报告: `/Users/dingxuyang/Desktop/Phase29-Diagnostic-双盲对照诊断表.md`

---

## Phase 29-DOM_Purge 物理拆违 (2026-06-29 20:45)

### 问题根因
探针三诊断确认：`.game-canvas-sandbox` 外层 wrapper 的 `border:1px` + `overflow:hidden` + `contain:layout` 导致 Canvas 坐标系污染（左上角切边 + 鼠标错位）。

### 手术内容
**NewBattleView.vue 模板 (L40-60)**:
- ✅ 物理删除 `.game-canvas-sandbox` 外层 wrapper div
- ✅ 移除 HexGridCanvasEngine 上的 `class="absolute inset-0 w-full h-full"` 脱离文档流定位
- ✅ `<HexGridCanvasEngine>` 直接作为 `.dm-main` flex 子节点（与编辑器 DOM 结构一致）
- ✅ Legend 移到外部作为兄弟节点，锚定 `.dm-main` 的 `position:relative`

**NewBattleView.vue CSS (L2969-2986)**:
- ✅ 删除 `.game-canvas-sandbox` CSS 规则（border, overflow, contain 全部铲除）
- ✅ 删除 `.canvas-container` 死代码

### 最终 DOM 结构对比
| 层级 | 战场 (修复后) | 编辑器 | 
|------|-------------|--------|
| 外层 | `.dm-main` (flex-col) | `.page-container` (flex-col) |
| 引擎 | `<HexGridCanvasEngine>` 直接子节点 | `<HexGridCanvasEngine>` 直接子节点 ✅ |
| 引擎定位 | 自然 flex:1 (引擎自身 `.hex-engine-sandbox`) | 自然 flex:1 ✅ |
| wrapper border | ❌ 无 (仅引擎自身 1px) | ❌ 无 (仅引擎自身 1px) ✅ |

### 部署
- npm run build ✅ → docker compose down + build --no-cache + up -d ✅
- mecha-frontend Running (healthy), HTTP 200

---

## Phase 29-ParitySync 基因克隆 (2026-06-29 20:59)

### 工序一：探针全切除 ✅
- HexGridCanvasEngine.vue: 删除 `_emitCanvasDebugReport()` (48行)
- 删除 `draw()` 中的节流日志调用 (8行)
- 删除 mousemove 中的鼠标标尺探针 (16行)
- 删除 onMounted 中的 `window._debugCanvasStatus`/`_debugMouseProbe` 暴露

### 工序二：关闭离屏缓冲 ✅
- `:use-terrain-cache="true"` → `:use-terrain-cache="false"` (与编辑器一致)
- 原因: 离屏 Canvas 在 Retina 高分屏下产生负坐标裁剪

### 工序三：1:1 基因克隆 ✅
**isoConfig 强制回退 ISO_DEFAULTS**:
```js
// 变更前 (污染): computed(() => ({ ...ISO }))  // reactive 可变
// 变更后 (纯净): computed(() => ({
//   shearX: ISO.shearX, shearY: ISO.shearY,   // 仅 shear 允后端覆写
//   scaleX: ISO_DEFAULTS.scaleX, scaleY: ISO_DEFAULTS.scaleY,
//   rotation: ISO_DEFAULTS.rotation, topFlat/bottomFlat: ISO_DEFAULTS
// }))
```
**DPR 鼠标校准**: getHexAtEvent() 增加 DPR 安全注释，确认 `canvas.width/rect.width` 比例尺在任意 DPR 下正确

### 战场 vs 编辑器最终参数对照
| 参数 | 战场 | 编辑器 | 一致? |
|------|------|--------|-------|
| useTerrainCache | false | false | ✅ |
| isoConfig.scaleX | ISO_DEFAULTS | ISO_DEFAULTS | ✅ |
| isoConfig.rotation | ISO_DEFAULTS | ISO_DEFAULTS | ✅ |
| topologyParam | {spacingH,spacingV} ref | {spacingH,spacingV} ref | ✅ |
| DOM wrapper | ❌ 无 | ❌ 无 | ✅ |

### 部署
- npm run build ✅ → docker compose down + build --no-cache + up -d ✅
- mecha-frontend Running (healthy), HTTP 200
