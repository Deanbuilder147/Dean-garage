# 2026-07-11 工作日志

## CSP 动态放行与表单语义化全栈修复

### P0-动作1: Nginx CSP 加强
- `frontend/nginx.conf`: 16 个 location 块 CSP 全部追加 `worker-src 'self' blob:` (OffscreenCanvas) + `frame-ancestors 'self'` (防点击劫持)
- `mecha-universe-engine/frontend/nginx.conf`: 全局 CSP 追加 `worker-src 'self' blob:`

### P0-动作2: HexGridCanvasEngine.vue 代码审计
- 添加审计注释：确认 0 处 eval()/new Function() 调用
- 'unsafe-eval' 仅服务于 Vue 模板编译器/DiceScript/HMR

### P2-动作3&4: 表单语义化升级
- `SkillsEditor.vue`: 5 input + 3 select 补全 id，sr-only label 补全 for
- `NewBattlefieldView.vue`: 批量坐标、3D 滑块、新建地图、地形管理器全部 input/select/label 补全 id/for
- `NewPreparationRoom.vue`: 胜利条件、ACE 选择、聊天输入补全 id/for

### 构建结果
- npm run build 成功 (116 modules, 956ms)
- 文件已上传到服务器 `/root/20260604120036_20260629174435/frontend/`
- 待用户手动执行 docker compose rebuild
