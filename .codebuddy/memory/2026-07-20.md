# 2026-07-20

## 修复：createBattle 404 BATTLEFIELD_NOT_FOUND
- 现象：浏览器 `POST /api/combat` → 404（`createBattle failed: 404`）
- 排查关键：无 token 的 curl 被 authenticate 拦在 401，误以为路径/代理问题；实际带 token 后网关走到 DB 查询 `maps WHERE id = ?`，因前端硬编码 `battlefield_id: 1` 而数据库地图用 UUID（唯一战场 `a4eba9f1-ff74-4536-baf7-710801bac871`）→ 404。
- 根因：`frontend/src/views/NewPreparationRoom.vue:270` 硬编码 `battlefield_id: 1`。
- 修复：改为房间真实 mapId `room.value?.room?.mapId || room.value?.mapId || 1`。
- 部署：SCP 到服务器 `/root/mecha-universe-engine/frontend/src/views/`，`cd frontend && npm run build`，`docker compose build frontend && docker compose up -d frontend`。新 bundle `index-B5OxQzld.js` 已含 mapId 逻辑，容器已重建启动。
- 经验：401 是 authenticate 拦截，真正 404 在带 token 的 DB 查询阶段，勿被 401 误导。

## 推送整个项目到 GitHub
- 远程 `git@github.com:Deanbuilder147/Dean-garage.git`（SSH，已认证通过）
- `git add -A` → 105 新增 / 21 删除 / 34 修改；确认无 .env/密钥被追踪，无 >1MB 文件
- 提交信息："chore: 同步整个项目至 GitHub"（含 createBattle 修复）
- `git push origin main` 成功：`850a434..37b5b95 main -> main`

## 项目结构收敛与资产归档
- **唯一单体架构入口确立**：`mecha-universe-engine/` 为唯一可构建/部署根（其 `docker-compose.yml` 引用 `./backend-gateway`、`../frontend`、`../services/comm-service` 全部成立）。
- 收敛动作（git 识别 194 个 rename，已 push `37b5b95..b95b5dc`）：
  - 真实 `frontend/` 与 `services/` 整体迁入 `mecha-universe-engine/`
  - 废弃根目录断头 `docker-compose.yml`（引用不存在的 root/backend-gateway 且缺 .env）
  - 废弃 `mecha-universe-engine/frontend` 骨架(17文件) 与 `mecha-universe-engine/services` 占位(仅 comm-service)
  - 废弃第三份前端副本 `frontend-files/`
  - `run-services.sh`/`stop-services.sh`/`README.md`/`.env.example` 随单体入口迁入
  - 资产归档：`*.png`(7)+`step3-battle.yaml` → `docs/assets/debug-snapshots/`；诊断报告(3) → `docs/reports/`
- 保留为独立工具目录（未删，建议后续归档）：`apps/`、`scripts/`、`fix_scripts/`、`combat-patches/`、`dicescript/`
- 注意：`mecha-universe-engine/.gitignore` 有 `*.db`，`services/map-service/data/map.db` 被忽略，已 `git add -f` 强制加回保持跟踪。
- 结论：服务器部署路径 `/root/mecha-universe-engine/` 与本地单体入口一致，无需改部署模型。

## 词条库中枢 v5.0 战斗管道 vs Excel(作品设定器beta1.xlsx) 对照
- 交付物：`/Users/dingxuyang/Desktop/词条库中枢_万能语法战斗中枢v5.0_梳理与Excel对照.md`
- 按 5 场景划分：A单位编辑器(设计期)/B词条库中枢(配置期)/C战斗管道(结算期)/D地形系统(环境期)/E-Excel导入(接入期)。
- **一致项（v5.0 项目 vs 桌面战斗规则.xlsx）**：点数预算(40/25/15/15/15/10)、技能槽(3/2/1/1/2/0)、伤害公式(格斗|射击+机动差)、**主机体 HP=结构×10**、Royroy HP=结构×3、装备 DKM 维度。
- **关键偏差**（含 2026-07-20 二次复核修正）：
  1. 主机体 HP：项目=结构×10，与桌面 `战斗规则.xlsx`(×10) 一致；但 `机甲战棋/设计资料/作品设定器beta1.xlsx` 写×5，**两份 Excel 互相矛盾**（非项目问题）。
  2. 伤害种类：Excel 实体/光束(2) → 项目 kinetic/beam/explosive/corrosive/thermal(5)；**beam/energy 术语错配**（装备系统用 `energy` 表示光束，全局用 `beam`）→ 抗性涂层对光束武器实际不触发（真 bug，见文档 §十·10.6）。
  3. 技能类别：Excel 近战/远程/**自动化** → 项目 melee/ranged/**special**，但自动化特性已通过 unitConverter+skillExecutor 落地。
  4. **技能落地度远高于初判**：设定器 15 特性 + 特殊词条 10 条中约 20+ 已映射执行。偏差在：掷骰→确定性(格挡/斩杀/决斗/抢夺/专注射击/幸运)、数值微调(全覆式/涂层 -2 vs -3、专注射击 +4 vs +3/+5)、缺失分支(投掷移动-5/双槽/决斗同归/幸运1-2·3-4)、长柄/变形已映射但效果疑似未执行。
  5. 阵营特性：火力覆盖/增援/补给(月面+4)/隐匿 未实现；奇袭全局70%先制 vs 马克西翁阵营限定(语义不同)；迷雾系统占位无作用；暴击为项目新增 Excel 无。
  6. 地形不同构：Excel 母舰/太空/基地/防御层/空投，项目 10 通用地形(forest/mountain/water…)。月面 Excel 有 HP+4 buff，项目无。
  7. 战术规则：再动/抢夺/扫射/稳定/狙击/助攻/守护/阻碍/侦察 已落地；专注射击固定+4 vs 掷骰+3/+5；协同/联防/空投生成 缺失。

## 战斗规则.xlsx 词条"是否要重写"的结论（用户后续提问）
- 结论：**有必要重写 Excel，但方向是"双列+状态列+术语映射"让设计意图与实现现状同时钉死，不是无脑照抄代码 bug 值**。
- 三类偏差处理：A代码bug(beam/energy、-2vs-3)→改代码、Excel保留设计值；B有意简化(掷骰→固定)→Excel加"当前实现值"列；C未实现(阵营/地形/协同/空投/迷雾)→标❌或移入规划sheet。
- 代码配套 P0：统一 beam↔energy + 全覆式/涂层改回 -3（真bug）；P1：补 长柄/变形/投掷移动-5/双槽/决斗同归/幸运分支；P2：定掷骰口径、奇袭阵营限定；P3：排期阵营/地形/协同/防空投。
- 文档已补 §十一「战斗规则.xlsx 词条修改建议」。

## 词条编辑器(GlossaryView.vue)属性编辑逻辑报告
- 交付物：`/Users/dingxuyang/Desktop/词条编辑器_属性编辑逻辑报告.md`（无现成专项报告，新生成）。
- 编辑器数据流：GET /combat-glossary/config → editableConfig{_meta,skills,systems} → v-model 编辑 → POST 合并落盘 → reload。后端 CORE_SKILLS(6个:melee_strike/ranged_shot/shield_wall/repair/overdrive/beam_cannon) 不可覆盖/删除。
- **🔴 真 bug**：`importAiSkills()` 引用未声明的 `skillsData`（L962/L1012 ReferenceError），且 `saveConfig(config)` 传参被忽略（函数固定读 editableConfig）→ AI 导入完全失效，导入技能不入库存。修复：直接写 editableConfig.skills 再调无参 saveConfig。
- **🟡 缺口**：`terrains`/`damage_kinds`/`action_types` 在 GlossaryView 全文 0 引用 → 三类共20项配置 UI 完全不可见不可编辑（靠后端读旧文件合并存活，不丢但不可改）。
- 其他：向导创建技能缺 `type` 字段；category(含passive)与 type(active/passive)语义重叠；核心技能 UI 假删除误导；beam/energy 错配在编辑器侧体现为 damage_kind=beam 与装备 energy 不匹配（同 Excel 报告 §10.6）；专属字段(reduction/trigger/sector_angle/requires...)靠"整对象回传"才不丢。

## 战斗核心"能否按词条内容计算"核查（用户追问）
- 结论：**否**——能读到配置，但不能完整/准确按词条计算。报告：`/Users/dingxuyang/Desktop/战斗核心_词条计算问题报告.md`
- P1(🔴): `damagePipe.cjs` 全程以 `attacker.weaponType` 作伤害种类键；`combatResolver.js` L255-291 调 DamagePipe 时未把技能 `damage_kind` 传入 → 编辑器设 beam/kinetic 对普通攻击无效（死字段）。
- P2(🔴): beam↔energy 错配：equipmentDurability 涂层判定 `weaponType==='energy'`，管线用 `beam` → 涂层对光束永不触发。
- P3(🟡): combatResolver.js L240 sniper 机动削减硬编码 2，不读配置。
- P4(🟡): counter 双路径：`_extractSkillBonuses` L346 写死 value:2，而 executeCounter 读配置 → 不一致。
- P5(🟡): 具名处理器读 reduction/bonus/damage_multiplier/mobility_reduction 等专属字段，编辑器无控件 → 改 base_damage 常不生效。
- 确认正常（避免误报）：executeFocusedFire/executeSnatch/executeDuel 等均读配置；通用字段在 skillExecutor 万能路径确实参与计算。

## 战斗核心「词条解析管道 Parsing Pipeline」深度审查（用户专项请求）
- 交付物：`/Users/dingxuyang/Desktop/战斗核心_词条解析管道技术现状审查.md`
- 读取层：configLoader.cjs 每次 fs.readFileSync 静态 JSON，单源干净、无缓存问题、无 Schema 校验。
- 解析层三轨并行：combatResolver 按 skillId 大 if/else 具名分发 + skillExecutor 具名 EXECUTORS + 万能路径。配置只在最简单一轨真正驱动。
- 🔴 P0-1：getDamageType 用 attacker.weaponType 覆盖 skillData.damage_kind（配置无 attack_type 字段→恒 undefined）；combatResolver 调 DamagePipe 不传 damage_kind → 编辑器光束/动能设置无效（死字段）。
- 🔴 P0-2：beam↔energy 错配——配置 damage_kind=beam，equipmentDurability 涂层判定 weaponType==='energy'，且全覆式/涂层减伤写死 -2（设计 -3）→ 涂层对光束永不触发。
- 🟡 P1：sniper 机动削减写死2、_extractSkillBonuses counter 写死 value:2（双份真相）；height_bonus 仅在具名路径注入。
- 🟠 P2：无分支 modifiers 机制（区间判定如「1-4伤+3」只能硬编码）；ConditionEvaluator 仅用于触发门控，未复用到结算分支选择；无通用 effects[] 操作序列。新增机制必改代码（id 分发硬编码）。
- 契约建议：引入 mechanic 字段替代 id 分发、damage_kind 唯一真相、effects[] 有序操作、branches[{when,then}] 复用 ConditionEvaluator、trigger 通用化、configLoader 加 Schema 校验。

## 词条解析对齐方案重构（渐进式，决策 C + 核心6项动作词）
- 用户确认：迁移策略=C.渐进式；branches.effects[].action=核心6项(damage/damage_bonus/heal/apply_status/mobility_mod/accuracy_mod)。每步需确认再执行。
- **Step 1 完成**：新增契约模块 `services/combat-service/src/services/combatCore/skillContract.cjs`（常量+normalizeSkill/validateSkill/toContract + beam↔energy 别名归一 normalizeDamageKind）；新增规范 `docs/skill-contract-schema.md`。纯新增零侵入。冒烟测试通过（旧9技能兼容、非法action被校验拦截）。
- 真实配置枚举校准：damage_kinds=kinetic/beam/explosive/corrosive/thermal（无 energy）；skills 实为 9 个（block/sweep/throw/execute/duel/snatch/focused_fire/lucky/reactivate）。
- 后续 Step：Step3 动态注册表(废弃 combatResolver skillId if/else)、Step4 通用 Branch Evaluator、Step5 修 P0-1 damage_kind 漏传、Step6 修 P0-2 beam/energy+涂层-3、Step7 配置迁移、Step8 前端编辑器表单对齐、Step9 所见即所得保存、Step10 端到端验证。

- **Step 3 完成**：新增 `skillRegistry.cjs`（REGISTRY + registerSkill/getBonusExtractor/hasSkill；内置注册 assist/blockade/counter/focused_fire/guard，与旧 _extractSkillBonuses 1:1）。`combatResolver.js` 的 `_extractSkillBonuses` 由 5 段 `if (skill.type===...)` 改为 `SkillRegistry.getBonusExtractor(skill.type)` 动态查找（删死代码 `_getUniversalFields` 调用）。行为不变。语法/ lint 通过。注意：文件首次 write 曾空写入（瞬态），已重写确认内容持久。
- 已知残留硬编码（非本次范围，低优先）：combatResolver L235 的 `skill.type==='sniper'` 机动削减、L131 `initSkillCounters` 的 switch。Step 4 聚焦 Branch Evaluator。

- **Step 1 契约细化（随 Step 4 重做）**：用户给出更精细规范 → 分支字段顶层化 `has_dice/dice_type/dice_branches`，每分支 `points` 支持「离散点数 number + 区间 [min,max] 并存」（取代旧单个 condition_range）。`cast_range` 支持数值或 {min,max}。`skillContract.cjs` 已重写对齐（normalizeSkill 暴露 `dice` 命名空间 + 顶层镜像仅在新模型启用时覆盖，避免污染旧 `dice_type` 字符串）。文档 `docs/skill-contract-schema.md` 同步更新。
- **Step 4 完成**：新增 `branchEvaluator.cjs`（rollDice/pointMatches/branchMatches/evaluateBranches/newEffectContext/applyBranchEffects，6 动作词解释，多分支可同时命中）。`skillExecutor.cjs` 加入新模型路由（`uf.dice.has_dice` 时绕过旧 `_evaluateDice`，改走 `_executeBranchModelSkill`）；新执行器投骰→命中分支→顺序执行 effects，命中/机动修正回写 context。端到端验证：roll 1-6 均正确命中，roll=5 同时命中两分支（追加+3+6+stun）。lint 0 错。
- 后端解析管道已具备：动态注册表(Step3)+通用分支解析器(Step4)+契约模块(Step1)。下一步 Step5 修 P0-1 damage_kind 漏传、Step6 修 P0-2 beam/energy 错配+涂层-3。

- **Step 5 完成（修 P0-1 damage_kind 漏传）**：切断 `weaponType` 对伤害种类的历史覆盖。
  - `skillContract.cjs` 新增 `getDamageType(skillData, fallbackWeaponType)`：优先 `skillData.damage_kind`(经 normalizeDamageKind 别名归一)，缺失回退 `fallbackWeaponType`，再缺失默认 `kinetic`；已加入 module.exports。
  - `combatResolver.js` 引入 `getDamageType`，在 `executeTurn` 计算 `damageKind = getDamageType(skillUf, attacker.weaponType)`，并在 `DamagePipe.calculate` 配置对象显式注入 `damage_kind: damageKind`。
  - `damagePipe.cjs`：`calculate` 入口 `damageKind = config.damage_kind || attacker.weaponType || 'kinetic'`；地形修正/防御装备减免/武器克制/装备减免四阶段内部方法签名新增 `damageKind` 参数并以其为准（不再读 attacker.weaponType）；`calculateTerrainDamage` 也优先 `config.damage_kind`。`attacker.weaponType` 仅作为兜底保留（combatIntegrator 旧路径无感回归）。
  - 验证：临时脚本 10 项断言全 PASS（词条 beam 优先于 weaponType=kinetic；地形/装备/克制三类修正均按权威 damage_kind 生效）；lint 0 错。
  - 关联 P0-2（beam↔energy 错配+涂层-3）仍待修，是下一步。

- **Step 6 完成（修 P0-2 beam↔energy 错配 + 涂层 -3）**：修复 `equipmentDurability.cjs` 装备减伤权威逻辑。
  - 头部注释与 `applyDamage` JSDoc 更新：-2→-3；`energy`→`beam(含 energy/laser/em 别名)`。
  - 新增依赖 `const { normalizeDamageKind } = require('./skillContract.cjs');`（`skillContract` 不反向依赖，无环）。
  - `applyDamage` 入口 `const dk = normalizeDamageKind(weaponType);` 归一武器类型为权威伤害种类。
  - 全覆式装甲：`weaponType === 'kinetic'` → `dk === 'kinetic'`，吸收 `Math.min(remaining, 2)` → `3`。
  - 抗性涂层：`weaponType === 'energy'` → `dk === 'beam'`（涂层对光束现可触发，原 'energy' 永不命中 beam 的 bug 修复），吸收 `2` → `3`。
  - 验证：临时脚本 5 项断言全 PASS（beam/energy/laser 均触发涂层减伤3；kinetic 触发全覆式减伤3；beam 不触发全覆式）；lint 0 错。
  - 备注：combatIntegrator.cjs:261 `damageType: attackType==='melee'?'kinetic':'energy'` 的 'energy' 仍是潜在别名错配点（走 DamagePipe 时不经此字段，影响 faction skill 的 context.damageType），留作后续清理，不在 P0-2 范围。
  - 词条契约/解析重构链条至此（Step1-6）后端核心已闭环：契约+注册表+多分支解析器+damage_kind 权威流转+装备减伤校正。

- **运行时崩溃修复（用户报障 + 顺带挖出的更致命 bug）**：`combatResolver.js` + `equipmentDurability.cjs`
  - 报障 #1 jsonwebtoken：**误报**。已核实 `services/combat-service/package.json:17` 已声明 `"jsonwebtoken": "^9.0.0"`，无需改动。
  - 报障 #2 resolveTurn：真实存在。`executeTurn` 第 304 行调用 `this.durability.resolveTurn(...)`，但 `EquipmentDurability` 无此方法 → TypeError 中断主攻击链。已对齐为真实的 `applyDamage(defender, damageResult.final_damage, damageKind)` + `consumeWeaponDurability(attacker)`。
  - **额外挖出（更致命）**：`executeTurn` 默认路径（`options.enableAmbush !== false`）会调用 `resolveAmbush`/`resolveFogEffect`，二者都用 `getSystemConfig` 却**从未 import** → 任何一次战斗结算都直接崩溃。已在 `combatResolver.js:12` 补 `import { getGlossaryConfig, getSystemConfig }`。
  - **第三个崩溃点**：`combatResolver.js` 第 109/121 行调用 `this.durability.consumeDurability(...)`，而 `EquipmentDurability` 也无此方法 → TypeError。已在 `equipmentDurability.cjs` 新增 `consumeDurability(unit, slot, amount=1)`（通用槽位消耗，归零同步关 equipment 开关）。
  - **耐久系统原本完全失效**：`EquipmentDurability.register` 从未被调用，导致所有耐久操作为静默 no-op。已在 `combatResolver.init(battlefield, allUnits)` 中遍历 `allUnits` 调用 `this.durability.register(unit)`。
  - 另：`register` 补齐 `special_shield_gen`/`special_reactive_armor` 两槽位，使火力覆盖阻断消耗路径可真正生效。
  - 验证：临时 .mjs 端到端 8 项断言全 PASS（executeTurn 不崩、durabilityChanges 含 coating_absorb(beam,3)、consumeDurability 四种特殊装备耗尽、applyDamage/consumeWeaponDurability 正常）；lint 0 错。
  - 结论：战斗结算链路此前因 getSystemConfig 未导入 + resolveTurn/consumeDurability 不存在 + 单位未 register 而**根本无法端到端运行**，本次一并修通。

- **GlossaryView 结构化重构（Step1-4 前后端对齐）**：按《Mecha Universe 词条规范与核心解析对齐方案》重构前端词条编辑器，严格 Step1→Step4。
  - Step1 新建前端契约模块 `frontend/src/contracts/skillContract.js`（ESM，镜像后端 skillContract.cjs 的常量/normalize/validate/toContract/hydrateSkill/serializeSkillToContract）+ `skillContract.d.ts`（TS 类型）。枚举：SKILL_CATEGORIES/melee|ranged|automation|support、TARGET_SCOPES、SKILL_SHAPES、DAMAGE_KINDS(+energy/laser/em→beam 别名)、ACTION_TYPES、DICE_TYPES[4..20]、BRANCH_ACTIONS[damage|damage_bonus|heal|apply_status|mobility_mod|accuracy_mod]。
  - Step2/3 重写 `frontend/src/views/GlossaryView.vue` 词条卡片为三大区块：①名称分类(key/name/category) ②基础属性与伤害分流(target_scope/cast_range{min,max}/skill_shape/权威 damage_kind/action_type，旧字段收进「兼容插槽」details) ③投骰属性与多重判定(has_dice 主开关 → dice_type 下拉 + 动态《判定N》分支：点数离散/区间文本 `2,5`/`[1,4]`、每条分支动态 6 选 1 效果)。新增 addBranch/removeBranch/addEffect/removeEffect/parsePointText/pointText/setPointText 助手。
  - Step4 序列化闭环：`loadConfig` 用 `hydrateSkill(sk)` 把任意格式回显为编辑器树；`saveConfig` 用 `serializeSkillToContract(s)` 产出标准契约 + 旧镜像(target_filter/min_cast_range/cast_range/range_type) 经 POST /api/combat-glossary/config 提交；addNewSkill/commitWizardSkill 均产出新形状。
- **🔴 重大架构事实（部署核查发现，2026-07-20 晚）**：运行中的 `mecha-gateway` 容器**自带一份 TypeScript 战斗实现**（`backend-gateway/src/skillExecutor.ts` → 编译进 `dist/skillExecutor.js`），与 `services/combat-service/src/services/combatCore/*.cjs` 是**两套并行、互不影响的实现**。gateway Dockerfile 只 COPY `shared-kernel` + `backend-gateway`，**完全不打包 combat-service**，容器内也无 `combatCore` 目录，docker ps 也无独立 combat-service 容器。
  - 含义：此前 Step1-6 对 `.cjs`（skillContract/branchEvaluator/skillExecutor/skillRegistry/combatResolver/damagePipe/equipmentDurability）的全部改动**不会**影响运行中的网关战斗逻辑！只有 `backend-gateway` 里的 TS 版才真正生效。
  - glossary 路由（`dist/routes/glossary.js`）仅读写 `data/glossary-skill-config.json`（相对 dist 的 `../../data/`），与 combat-core 解耦。前端 GlossaryView 改造只依赖此 JSON + 自身 bundle，故纯前端部署即可显示新界面。
  - 后果：投骰多分支(dice_branches)等"战斗功能"在生产环境尚未真正可用——除非把 .cjs 的改动移植到 `backend-gateway/src/skillExecutor.ts`（或改变部署让 gateway 引用 combat-service）。**这是后续待办，非本次前端显示问题范围**。

- **前端部署流程实测（2026-07-20 晚，修"看不到改动"）**：根因=本地 7/20 的 GlossaryView.vue + contracts/ 改动从未同步服务器（服务器文件仍是 7/3 旧版、无 contracts 目录）。修复步骤：①`scp` GlossaryView.vue 与 contracts/ 到 `/root/mecha-universe-engine/frontend/src/`；②服务器 `cd frontend && npm run build`（新 bundle `index-Bhd4g2xZ.js`，含 has_dice）；③`docker compose build frontend && docker compose up -d frontend` 重建镜像（Dockerfile 仅 COPY dist）。验证：curl 8081 返回新 bundle 且 `/glossary`=200。注意前端容器无 curl 时 health 显示 starting 但服务正常（cosmetic）。
  - 注意：服务器 `/root/mecha-universe-engine` **不是 git 仓库**（git pull 不可用），只能 SCP 同步。

  - **Step4 后端接线缺口修复（关键）**：`skillExecutor._getUniversalFields` 返回对象原本**没有 dice/name 字段**，而 `executeUniversalSkill` 用 `uf.dice.has_dice` 判断分支模型 → 新投骰管道在 executeUniversalSkill 路径**从未被触发**。已补 `name` 与 `dice`(cfg.dice 命名空间 或 顶层 has_dice/dice_type/dice_branches) 透传，接通 `_executeBranchModelSkill`。
  - 验证：新增集成测试 `services/combat-service/src/services/combatCore/tests/glossary-branch-integration.mjs`（.mjs，因需 import 前端 ESM 契约）。结果全部通过：前端序列化含 dice 命名空间、hydrateSkill 无损回显、后端 getSkillConfig 识别 has_dice/dice_branches、executeUniversalSkill 10 次投骰全命中分支并打出战斗日志（如 `直伤=10; 施加状态=burn`）、_getUniversalFields 透传 dice+name 生效。测试结束已还原真实 glossary 配置（留 .bak 已删）。
  - 注意：此任务**取代**了之前未执行的 BattlefieldView 计划。该旧计划文件 `.codebuddy/plans/hex-editor-integration_9f1bfcf0(未完成).md`（激活 BattlefieldView.vue 取代 NewBattlefieldView 的六角格编辑器方案）已于 2026-07-20 删除，确认为过时版本内容（BattlefieldView 若需后续再做，应从当前 NewBattlefieldView.vue 现状出发重新规划）。

- **战斗桥接落地：combat-service .cjs 引擎正式在生产网关生效（2026-07-20 深夜）**：解决"两套并行实现、.cjs 改动不生效"的架构事实（见上条）。
  - **改动文件（本地仓库已改，并 SCP 服务器 `/root/mecha-universe-engine/`）**：
    1. `backend-gateway/Dockerfile`：新增 `COPY services/combat-service ./services/combat-service/`；并在运行阶段 `ln -s /app/data/glossary-skill-config.json /app/services/combat-service/src/config/glossary-skill-config.json` 统一配置路径（单点真相 = 前端 glossary 的 9 技能：block/sweep/throw/execute/duel/snatch/focused_fire/lucky/reactivate）。
    2. `backend-gateway/src/combatBridge.ts`（新增）：用 `createRequire(import.meta.url)` **分别** require `skillExecutor.cjs`/`skillContract.cjs`/`equipmentDurability.cjs`（index.cjs 不导出这些）；懒加载单例，combat-service 缺失时网关仍能启动。
    3. `backend-gateway/src/routes/combat.ts`：新增 `POST /api/combat/:battleId/skill`（无 authenticate，沿用 `/damage` 直传单位对象约定）→ 调桥接 `SkillExecutor.executeUniversalSkill` 并回写伤害/治疗。
    4. `services/combat-service/src/services/combatCore/skillExecutor.cjs`：`executeUniversalSkill` 增加可选第 5 参 `inlineSkillDef`，请求级注入技能定义（如 has_dice 掷骰分支）而不污染持久配置。
  - **部署注意**：服务器 `/root/mecha-universe-engine/services/` 原先**只有 comm-service，无 combat-service**（印证从未打包）。需先 `scp -r` 本地 `services/combat-service/{src,vendor}`（**不要**带 node_modules，30MB 冗余；核心 .cjs 零 npm 依赖，vendor/dicescript.cjs 3.1MB 随 COPY 自动带入）。
  - **部署命令**：`docker compose build --no-cache mecha-gateway && docker compose up -d mecha-gateway`（已执行，镜像 sha256:61093c…）。
  - **冒烟测试全 PASS**（容器内 curl localhost:3006）：
    - 内联 has_dice 技能（6面骰 + 两分支）→ `engine:"combat-service/.cjs"`、掷骰 roll 4/4/6、`dice_type:6`、命中分支并施加 `stun`、`final_damage:20`。
    - 配置技能 focused_fire/sweep/throw（读 /app/data 单点真相）→ 同样走 `.cjs` 且真实掷骰（roll 2/2/6、`dice:{diceType:"1d6",successLine:4}`）。
  - **结论**：生产战斗路径现真正经 .cjs 引擎（含掷骰+多分支），原 TS 死代码 `backend-gateway/src/skillExecutor.ts` 仍未被引用（保留未删）。配置统一为前端 glossary 的 9 技能；combat-service 原 14 遗留技能（precision_shot 等）因 symlink 指向 /app/data 已不再被 .cjs 读取，如需可在前端 glossary 中加回。

## 前端五项改动（2026-07-20 收尾，用户需求）
1. **地图编辑器新增「删除地图」**：`NewBattlefieldView.vue` 在「新建地图」按钮旁加红色 `.btn-danger`「删除地图」按钮 + `deleteCurrentMap()`（复用后端既有 `DELETE /api/map/battlefields/:id`）。按钮仅在 `battlefield.id` 存在（已保存）时可用；未保存新图点击提示先保存。删除后刷新列表并重置编辑器。
2. **词条库区块一同排**：GlossaryView 区块一（区域一）的 KEY/名称/分类包进新 `.block-row`（flex 不换行），描述保持整行；新增 `.block-row` CSS。
3. **词条库页面滚动**：根 `.page-container` 加 `overflow-y-auto`（此前无滚动，长列表被截断）。
4. **分步向导适配新内容**：检查结论=向导旧字段（target_filter/cast_range 单值/range_type + dice_type/success_line）经 `hydrateSkill` 已归一化为新形状（target_scope/cast_range{min,max}/skill_shape），基本适配；唯一缺口=无法产出新多分支 `has_dice` 模型。已实施修复：`wizardForm` 增 `has_dice`/`dice_branches`；步骤4（状语）加 has_dice 开关 + 动态分支编辑器（points 支持逗号/区间，effects 可增删）；`commitWizardSkill` 在 has_dice 时构建 dice_branches（否则旧单骰，向后兼容）；新增 `addWizardBranch`/`parseWizardPoints` 及样式。
5. **词条中枢首页=全部词条列表**：在技能编辑面板前新增「词条中枢首页·全部词条」总览面板，列出所有词条（KEY/名称/分类徽章/🎲dN），点击 `scrollToSkill` 平滑滚动到对应卡片（卡片加 `:id` 锚点）；明确首页=列表总览语义。

- **部署**：SCP 两文件 → 服务器 `cd frontend && npm run build`（成功，117 模块）→ `docker compose build frontend && up -d`（mecha-frontend running）。lint 0 错误。报告已存桌面 `项目修复报告_2026-07-20.md`。
