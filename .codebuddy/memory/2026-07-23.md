# 2026-07-23 工作日志

## 编辑器三项需求落地（已部署 12:39）
- 需求：①画布改 50×50；②grid 中高亮中心点 y25:z26 这 2×2 格；③移动轴右角落(纵轴立直/横轴横向)；④所有旧地图中心平移到 y25:z26 区。
- **坐标解析**：y25 = (q=24, r=24)→formatCoord="Y25"；z26=(q=25,r=25)→"Z26"。50×50 画布几何中心 floor((50-1)/2)=24 起 2×2 块 (q24-25,r24-25) = Y25/Z25/Y26/Z26 = "y25:z26"。
- **改动引擎 HexGridCanvasEngine.vue**：新增 prop `showCenterMarker`(默认false)；新增 `renderCenterMarker()` 在 draw() 第二层半绘制金边+淡填充+金色十字+“中心 CENTER”文字(planar 走 drawHexPath)；双轴滑槽模板改 `.pan-axis` 竖排——纵轴=`slider-v`(writing-mode:vertical-lr;direction:rtl;立直)，横轴=`slider-h`(横向)，`.slider-panel` 定位 `right:14px;bottom:14px`(原 bottom 居中)。
- **改动 NewBattlefieldView.vue**：`gridW/gridH` 强制 computed 50；`createNewMap` 宽高 50；`<HexGridCanvasEngine>` 传 `:show-center-marker="true"`；`loadMapData` 中旧地图(attributes.legacy_type==='standard'||legacy_id)加载后平移：算 bbox 中心→偏移 dq=round(24.5-cq),dr=round(24.5-cr) 应用到 terrainMap 所有键与 spawnPoints。**幂等**：已居中地图再次加载 dq=dr=0。
- **验证**：`migrateCells` 非空数组返回原稀疏数组(不补满)，平移安全不出界；后端 `/battlefields/:id` 与 `/list?id=` 均返回 attributes。lint 0 errors；build 120 模块；`mecha-frontend` Recreated/Started(healthy)；产物含 pan-axis + showCenterMarker。
- 观察提示：强制刷新(Cmd+Shift+R)；新建空图可见淡网格+中心金框；旧地图自动居中到 y25:z26 并高亮。

## 前端改造 Plan v2 计划报告（融合改造方案 + 影响评估报告）
- 用户要求按《前端视图改造与架构变更综合方案.md》+《固定画布与平顶六边形架构变更影响评估报告.md》重新生成 plan 报告，重点排查改造后核心距离/邻居/寻路等关联问题。报告已写入桌面 `前端改造与平顶六边形_实施计划报告_v2.md`。
- **关键发现 1（固定画布未真正执行）**：评估报告的「固定画布 100×100 + cells 边界迁移」在已落地的阶段1-5 中**被跳过**——仅删了编辑器 3D 面板+saveViewConfig，width/height 输入框(`NewBattlefieldView.vue` L121-130/237-238/626-650)与后端 `maps.ts` 矩形边界(L155/197/205-212)仍在。列为 plan 阶段 A 必做。
- **关键发现 2（邻居方向前后端语义分歧，高危）**：前端 `getHexNeighbors`(`hexUtils.js:256`) 是 **Even-R offset 奇偶行方向**；后端 `tsFindPath` dirs(`combat.ts:67`) 是 **axial 固定方向** `[1,0],[1,-1],[0,-1],[-1,0],[-1,1],[0,1]`。对 (2,3)：前端产出 `{1,2}` 邻居，后端产出 `{3,2}` 邻居——**集合不一致**，说明系统 (q,r) 的 axial/offset 语义前后端未统一。列 plan 阶段 B 必做（单测断言两边邻居集合相等后统一）。
- **关键发现 3（移动范围公式已存在分歧）**：网关 `combat.ts` 用 `max(1, round(rawMob/10))`，combat-service 用 `Math.floor(mobility/2)`（评估报告§2.4）。需确认 combat-service 路径是否仍在实时链路，统一或显式分流。
- **风险澄清（用户问「3.3 顶点基准+倒角轴随FLAT_TOP切换防横扁」）**：该任务属方案§3.1b「全局FLAT_TOP朝向切换」的配套；因阶段3采用保守平顶(保持尖顶Even-R+topFlat压扁)，未启用全局FLAT_TOP切换，故「倒角轴随FLAT_TOP切换」前提不成立、未执行——保守策略下倒角轴恒为Y轴压扁，不会横扁。若需真正平顶朝向(Even-Q)列为阶段C(高风险,需浏览器校准)。
- 距离计算 `hexDistance`(battleState.js:147 / map-service:85 / combat.ts:315 / skillExecutor:965 / DamagePipe) 全部基于(q,r)，与渲染层正交，改造不受影响，但多处重复实现建议统一。

## 用户补充排查三项决策（2026-07-23 复核 Plan v2）
- **① 邻居统一方向：原 v2「推荐前端 getHexNeighbors 改 axial 固定6方向」被判定为错误**。前端(q,r)是 Even-R offset（渲染 pointyTopCenter / 点击 pointyTopToHex 均走 offset）。若只把前端改 axial 向量，高亮会与前端自身渲染错位（如(2,3) axial 含(3,2)、offset 含(1,2)，屏幕不相邻→跨格跳）。**正确做法：后端迁就前端——tsFindPath 的 dirs 改用 Even-R offset 方向表，同步 Royroy 重定位 dirs(combat.ts:1130) 与 combat-service getReachableHexes**。真正"全 axial"需连渲染管道+DB 序列化大迁移，不在此 plan。已写入报告 §2.2 决策块 + §2.9。
- **② 旧地图 cells 平滑迁移：方向采纳但格式修正**。`cells` 是 `{q,r,terrain}` 对象数组（combat.ts 用 `cells.map(c=>`${c.q},${c.r}`)` 建 cellSet），非 dict/下划线 key。旧补丁 `${q}_${r}:{active:true}` 会让 cellSet 崩溃。`maps` 表 cells 永远存在(默认'[]')，条件应判"空数组"而非"无字段"，补丁放 maps.ts 各 GET 端点，默认 terrain 填 'moon'。已写入阶段 A·7。
- **③ 跳过阶段 C（全局 FLAT_TOP 平顶切换）：完全采纳**，理由见阶段 C 决策块。

## 阶段 A/B/D 全量执行完成（2026-07-23，用户授权直接执行不打断）
已按 Plan v2 落地阶段 A/B/D（跳过 C），并部署到服务器。关键改动与验证：
- **B1 邻居统一**：后端迁就前端 Even-R offset（新增 getEvenROffsetDirs，应用于 combat.ts tsFindPath/Royroy 重定位、terrainMovement.getReachableHexes、battleState.js、map-service getNeighbors）。前端渲染为 offset 真理源，零渲染回归。
- **B3 距离修正（超出 v2 原描述的重要修复）**：原 5 处 hexDistance 用 axial 公式在 offset 坐标上系统性高估距离（offset 邻居如 (q+1,r-1) 算出 2 而非 1），导致 Royroy 相邻判定误拒、技能范围方向性偏差。统一为 `offset→axial` cube 距离：换算 `axial = {q: q-(r+(r&1))/2, r}`，经 node 暴力校验所有 6 邻居 cube 距离=1。应用点：combat.ts 顶层/局部 hexDist、skillExecutor._hexDistance、battleState.js、map-service。
- **A 固定画布 100×100**：sqlite 默认 100；maps.ts 3 GET 端点强制 100 + migrateCells 旧地图补全（数组格式 `{q,r,terrain}`，非 dict）；combat.ts createBattle 100；前端 NewBattlefieldView 删宽高输入固定 100；引擎点击/悬停边界兜底 (data.width||100)；NewBattleView gridData+cellMap 边界。
- **B2 移动公式**：battleState.js 遗留 getMoveRange/moveUnit 统一为 max(1,round(rawMob/10))（修复 ?? 不捕获 0）。
- **单测**：scripts/_test_coord_consistency.mjs → 点击逆推 round-trip PASS、距离公式==BFS PASS。
- **校验**：gateway `tsc --noEmit` exit 0；前端 lint 0 errors；rsync→服务器→`npm run build`→`docker compose build --no-cache mecha-gateway mecha-frontend`→`up -d --no-deps` 成功，gateway Healthy，前端/health 200。
- 服务器 SQLite battlefields 当前为空，cells 迁移补丁无现成旧数据触发（逻辑经编译+审查+节点校验确认正确）。
- 完成报告：`前端改造与平顶六边形_计划完成报告.md`（工作区+桌面）。临时校验脚本 scripts/_verify_offset_dist.mjs 已删，保留 _test_coord_consistency.mjs 作为 B5 交付单测。

## 移动范围 Bug 修复（2026-07-23，用户确认后执行）
用户确认语义：移动值=移动力总预算（移动点），移动范围=地形加权累计消耗 ≤ 预算的所有可达格（普通 1 点/格，特殊地形更多）。据此修复四处：
- **Bug1 /10 折算**：`combat.ts` `/move` 与 `NewBattleView.vue` 高亮均 `round(rawMob/10)`，错误将移动值缩小 10 倍。已删除 `/10`。
- **Bug2 优先级反了**：`rawMob` 原 `mobility`(仅机体) 优先，吞掉载具/背包机动。`combat.ts` 与前端均改为 `moveRange`(机体+载具+背包=`currentStats.speed`) 优先，否则 `mobility`，否则 3。
- **Bug3 fallback 缺 speed**：`combat.ts` 兜底 `currentStats` 仅 `mobility:5` 无 `speed` → `moveRange=0` 锁死。已补 `speed:5`。
- **Bug4 后端地形无视成本（新发现）**：`tsFindPath` 每步 `dist+1` 无视地形。已重写为**地形加权 Dijkstra**，新建 `backend-gateway/src/routes/terrainCosts.ts`（`TERRAIN_COST`/`terrainCost`，与前端 `UNIVERSAL_TERRAIN_MAP.cost` 对齐；wall=99 不可通行）。combat-service `battleState.js` 两处 `movementRange` 同改（moveRange 优先、去 /10）。
- **验证**：node 临时脚本 8/8 PASS（移动值10→达距10非1；mountain 加权预算4可达/3不可；wall 阻挡踏入可绕行）。gateway `tsc --noEmit` exit 0。rsync→`npm run build`→`docker compose build --no-cache mecha-gateway mecha-frontend`→`up -d --no-deps` 成功，两容器 Healthy，前端/health 200。
- ⚠️ MEMORY.md 移动范围公式记忆已更新：旧「/10 + mobility 优先」废弃。

## 留白地形 moon→void（透明 + 消耗999）改造（2026-07-23）
用户心智：100×100=画布，留白=透明不可通行。落地方案：
- 新增 `void` 地形：前端 `hexUtils.js` 的 `TERRAIN_COLORS`+`UNIVERSAL_TERRAIN_MAP` 加入 `void`（name 留白、color `rgba(120,140,180,0.06)`、cost 999、height 0）；保留 `moon` 作为**可通行灰色真实地形**（cost1）。
- 后端 `terrainCosts.ts` 的 `TERRAIN_COST` 加 `void:999`；`maps.ts` `migrateCells` 填充物由 `'moon'` 改为 `'void'`（旧图加载即获透明背景）。
- 引擎 `HexGridCanvasEngine.vue`：缓存渲染+实时绘制两处默认地形 `'moon'→'void'`、fallback `UNIVERSAL_TERRAIN_MAP.void`；**void 跳过实体绘制**（战场 iso 完全透明露背景，编辑器 planar 画 `rgba(255,255,255,0.05)` 极淡描边保留网格感）。`centerGrid` 居中锚点判定由 `=== 'moon'` 改为 `=== 'void'`（moon 现计入"画"）。
- 战斗 `NewBattleView.vue` 8 处 `|| 'moon'` 兜底全部改 `|| 'void'`（缺失地形=留白=不可通行）。
- 编辑器 `NewBattlefieldView.vue`：`saveMap`/`nonEmptyCellCount`/`terrainCount` 排除 `void`（与 moon 同为非画背景，不入库）。注意 `extractTerrainId` 对字符串返回 'moon' 兜底，故排除判断直接用原始值而非 extractTerrainId。void 自动出现在调色板（UNIVERSAL_TERRAIN_MAP 派生），可作橡皮/留白笔刷。
- **坐标以"画"为准(#2)**：void cost 999 使 `tsFindPath`(`enterCost>=99`continue) 与前端 `moveRangeHexes` BFS 自然排除留白；居中用画包围盒。无需改坐标系统。
- **不规则地图(#3)**：100×100 纸 + 逐格可涂 + void 填充已天然支持任意不规则形状（含镂空），无需代码改动。
- **B(导出裁剪到画边界) 已做**：`NewBattlefieldView.vue exportJSON` 改为只导出非 moon/void 的格子(paintedMap=真正画)，并计算 bounds{minQ,maxQ,minR,maxR,width,height} 写入导出 JSON 顶层；坐标保持绝对(q,r)不动(兼容后端 100×100 纸模型)。与 saveMap(排除 moon/void) 行为一致。截图(PNG)裁剪未做——编辑器当前无截图导出功能，按需再加。
- **战场默认视角=地图正中10×10 (focusCentralGrid) 已做**：`HexGridCanvasEngine.vue` 新增 `focusCentralGrid(sideN=10)`——求非void"画"包围盒中心→取 N×N 区域 ISO 世界 AABB→按视图 0.9 边距算 scale(夹 0.2~4)并居中。`initCanvas` 战场(iso)模式改调 `focusCentralGrid(10)`（编辑器 planar 仍 0.5 整图）；修 `onMounted` setTimeout 原为 `centerGrid()` 会覆盖战场视角→改为 iso 仍 `focusCentralGrid(10)`。已 `defineExpose` 暴露 `focusCentralGrid` 供父层（如"回到中心10×10"按钮）调用。`centerGrid`/`zoomIn`/`zoomOut`/`zoomReset` 不变，玩家照旧缩放平移。`zoomReset` 仍是整图适配（可作为"看全图"）。
- 状态：**已部署**（2026-07-23 03:02）。rsync 同步 → `frontend npm run build`(3.5s,120 模块) → `docker compose build --no-cache mecha-gateway mecha-frontend` → `up -d --no-deps`（db 孤儿按预期避开）。gateway Healthy、frontend Started。本次打包：#1 void 留白(moon→void)、#2 坐标以画为准、B 导出裁剪到画边界、战场默认聚焦中央10×10(focusCentralGrid)。

## 编辑器去伪3D + 旧地图加载修复（2026-07-23 第二轮，已部署 03:15）
- **编辑器去伪3D**：`HexGridCanvasEngine.vue` 新增 `worldOf(flatX,flatY)`（planar→恒等、iso→isoTransformPoint）。所有绘制路径在 planar 改用标准正六边形 `drawHexPath(flatX,flatY)`：缓存渲染 renderTerrainToCache(void/普通/文本)、实时 renderTerrainInline、renderHighlights、hover、showCoords、canvas 尺寸估算 computeCanvasSize/getGridPixelSize、centerGrid/centerOn 全部 planar→恒等+drawHexPath。删 renderHighlights 里残留的旧 drawIsoHexPath 重复调用。planar 下 ISO 参数完全忽略。
- **旧地图空白 bug 修复**：`NewBattlefieldView.vue loadMapData` 原用 `Object.entries(rawTerrain)` 遍历——旧地图 `terrain` 是 JSON 字符串(对象数组 `[{q,r,terrain}]`)，Object.entries 把数组当索引键 `"0","1"...`，导致 terrainMap 键非 `"q,r"`，gridData 派生 `key.split(',')` 坐标错乱→完全空白。改为：`Array.isArray` 时遍历元素写 `terrainMap[`${c.q},${c.r}`]=terrainId`；dict 时兼容 `{id,...}` 提取 id。
- **验证要点**：进编辑器打开旧地图，应显示满屏月面(moon 是 #1 后"可通行真实地形"，非 void 透明)；有内容的旧地图恢复显示，空 cells 的旧图仍空白(migrateCells 补 void 属真实空数据)。编辑器现为正六边形网格、无等距压扁。
- 部署：rsync → `frontend npm run build`(3.59s,120模块) → `docker compose build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`（仅前端改动，db 孤儿避开）。frontend Recreated/Started。两文件 lint 0 errors。

## 编辑器整张空白 bug（2026-07-23 第三轮，已部署 03:21）
- **根因**：`HexGridCanvasEngine.vue initTerrainCache` 缓存画布尺寸用 `ISO.scaleX/scaleY/shear` 计算（`cacheH = worldH2D*ISO.scaleY + ...`）。planar 模式世界坐标是纯净2D，格子实际 y 范围 = `worldH2D`，但缓存高度被 `ISO.scaleY=0.39` 压到约 0.39×真实高度→地图大半被裁出缓存；而 `centerGrid`(planar→worldOf 恒等) 居中用的是完整 planar 尺寸，于是视口中心对应的世界坐标在缓存里是空白→编辑器整张看不到格子。
- **修复**：`initTerrainCache` 按 `props.mode` 分支——planar 用纯净2D尺寸(`worldW2D+HEX_WIDTH*4` / `worldH2D+HEX_HEIGHT*4`)，iso 保留原 ISO 扩展公式。缓存原点对齐世界(0,0)，planar flat 坐标从0起正向，完美对齐。
- **附**：planar void 描边 `0.05`→`0.12`（cache+inline 两处），让空(全 void)地图也能看到网格便于绘制。
- **验证**：进任意旧地图应满屏月面/地形(正六边形无压扁)；新建空图可见淡网格线。
- 部署：rsync → `npm run build`(3.52s,120模块) → `build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`。lint 0 errors。

## 编辑器看不到格子 根因深挖（2026-07-23 第四轮，已部署 03:35）
- **实查数据库**：`docker exec mecha-gateway node` 读 SQLite `maps` 表，旧地图 cells 有内容（如 `test`=1157 格全 fortress、`test-map`=forest、`test(lunar)`=lunar），地形 id(fortress/forest/lunar/base/desert...) 都在 `UNIVERSAL_TERRAIN_MAP` 内→**非地形表缺失导致兜底 void**。前端 `getBattlefield` 走 `/api/map/battlefields/:id`，响应无 `terrain` 字段，loadMapData 回退到 `mapData.cells`(migrateCells 数组)→填充 terrainMap 正确（数组分支 OK）。
- **真 bug（第四轮挖出）**：`canvasPosToHex`/`canvasPosToWorld` 在算完 `(screen-offset)/scale` 后又对**所有模式**套了 `isoInverseTransformPoint`。planar 模式世界坐标即纯净2D(worldOf 恒等)，不该逆 ISO。结果 `updateVisibleRange` 用错乱 q,r 算视野、鼠标拾取(hover/绘制/点击)落错格。
- **修复**：planar 下 `canvasPosToHex/world` 直接 `{x:worldX,y:worldY}`（恒等逆），iso 下才 `isoInverseTransformPoint`。
- **附**：planar void 网格描边 `0.12`→`0.25`（cache+inline 两处），空地图也清晰可见六边形网格。
- **渲染路径澄清**：`renderTerrainCache` 末尾已有 `terrainDirty=false`，draw 实际走 `drawImage(terrainCache)`（缓存生效），不依赖 visibleRange；但 canvasPosToHex bug 仍导致鼠标拾取/绘制错位，已修。
- 部署：rsync → `npm run build`(3.56s,120模块) → `build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`。lint 0 errors。
- ⚠️ 观察提示：请**强制刷新**(Ctrl+Shift+R)再测，旧 bundle 可能被 nginx/浏览器缓存；若仍空白，请提供具体地图名+控制台报错。

## 战场渲染「画未居中」+ 行动面板四维修复（2026-07-23）
用户心智模型澄清：**100×100 = 画布（固定），实际地图 = 画，应居中，留白=moon**。
- **根因（画未居中）**：编辑器 `saveMap` 只存非 moon 的格子（绝对 q,r），用户从 (0,0) 涂起则"画"贴在画布角落。后端 `maps.ts` 强制 100×100 + `migrateCells` 补 moon（留白）是对的；引擎也遍历全 100×100 渲染。但引擎原有 `originQ/originR` 偏移机制**未接上**（`hexToPixel` 调用没传 origin，`canvasPosToHex` 内联逆运算也没减 offset）。
- **修复（最小单点）**：只改 `HexGridCanvasEngine.vue` 的 `centerGrid()`——战场 `mode==='iso'` 时，把相机居中锚点从「画布几何中心」改为「非 moon 地形的包围盒中心」，两分支统一以该中心为锚点。`offsetX/offsetY` 是地形/高亮/单位共享的同一相机平移，点击逆运算也用同一 offset，故**单位与地形永远对齐、后端坐标零影响**；编辑器(planar)保持原行为。
- **问题1 行动面板四维**：`normalizeBattleState` 仅把 `hp` 提顶层，面板读顶层 `attack/defense/range/shield` 全取不到（藏在后端 `currentStats`）。已在 `normalizeBattleState` 补提 attack/defense/range/shield/armor/maxHp（仅顶层缺失时）；并把战斗面板 `攻击/防御/射程` 兜底 `||` 改为 `??`（避免合法 0 被显示成 ?）。部署面板走 `getDeployPool`/hangar，单位自带四维，不受此影响。
- **状态**：两文件 lint 0 errors。尚未部署（前端改动需 rsync→build→docker compose build --no-cache mecha-gateway mecha-frontend→up -d --no-deps）。

## 距离/射程计算全系统审计（2026-07-23）
审计报告：`docs/距离射程计算审计报告.html`（含 SVG 图例）。审计范围覆盖 4 层距离计算系统（前端 UI BFS、网关 tsFindPath+skill路由、combat-service battleState、共享数学层 terrainCosts+hexUtils）。

### 已验证正确（5/5 Pass）
- 前端 `getHexNeighbors` Even-R 奇偶双表 ✅
- 网关 `tsFindPath` 地形加权 Dijkstra + Even-R 方向 ✅
- 网关 `hexDistanceOffset` offset→axial→cube 标准化 ✅
- 前端 `moveRangeHexes` BFS（moveRange优先、去/10、地形加权） ✅
- combat-service `getEvenROffsetDirs`/`hexDistanceOffset` 结构正确 ✅

### 发现 4 个问题
1. **P0 严重：combat-service `TERRAIN_COST_MAP`(battleState.js:9-14) 缺 6 种地形**
   - 缺 void/plain/ruins/crystal/rubble/city_building
   - void(留白) 误为 1→可穿越，ruins/crystal/rubble 误为 1→移动消耗减半
   - 修复：追加 6 行 key-value（与前端 `UNIVERSAL_TERRAIN_MAP` 对齐）

2. **P0 严重：`/skill` 路由无距离校验 (combat.ts:616-698)**
   - caster/target 解析后直接 `executeUniversalSkill`，无 hex 距离判断
   - 修复：在 execute 前插入 `hexDistanceOffset` 校验（dist ≤ range_max）

3. **P1 中等：`min_cast_range` 仅前端展示不服务端校验**
   - 炫技标签显示 `≥N格` 但 /skill 路由完全忽视最小距离
   - 修复：在上条 skill 距离校验中同步检查 `dist ≥ min_cast_range`

4. **P1 中等：前端技能范围预览不排除 min_range 格 (NewBattleView.vue:1686-1692)**
   - `getSkillRange` 只返回 max 值，BFS 把 dist=1,2 也加入 skillRangeHexes
   - 修复：加 `newDist >= skillRangeMin` 条件过滤

### 不修改项
- 前端 `getHexNeighbors` 不改为 axial（会破坏 Even-R 渲染管道）
- 网关 `tsFindPath` 不改（已是 Even-R Dijkstra，完美）
- 不将范围校验推入 combat-service .cjs（应保留在掌握全部上下文的网关层）

### 留白/月面概念最终澄清（用户二次纠正，覆盖第44-55/79-84行旧模型）
- **概念**：50×50 六边形框线 = 画布/坐标空间（仅视觉参照）；地图 = 真实地形数据居中放置；**留白(void) = 画板外/非地图区域 = 画布背景，不入库、不可进入**；**月面(moon) = 真实地形，必须入库**（cost1，可通行）。
- **代码修正**（`NewBattlefieldView.vue`，当前轮）：`saveMap`(原707)、`exportJSON`(原766)、`nonEmptyCellCount`(原246)、加载日志(原621) 原把 `void` 和 `moon` 一并排除，现改为**仅排除 `void`，保留 `moon`**。注释同步更新（"留白(void)=画板背景不入库；月面(moon)是真实地形入库"）。
- **一致性核验**：三层地形表均含 `moon:1`（前端 `UNIVERSAL_TERRAIN_MAP:349`、网关 `terrainCosts.ts:13`、combat-service `battleState.js:10`），moon 入库后战斗消耗正确；`void` 不在 combat-service 表无碍——本就不入库，BFS `if(!cell) continue` 拦截背景区。
- **反向纠正早前记忆**：第80行"留白=moon"与第50行"void与moon同为非画背景不入库"为旧错误模型，以此轮为准。第44-55行的 moon→void 视觉改造仍有效（void 透明不可通行；moon 真实灰色地形），仅入库规则需按此轮修正。
- **已部署**（2026-07-23 16:55）：rsync → `frontend npm run build`(3.57s,120模块) → `docker compose build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`。容器 Recreated/Started，`healthy`，8081 返回 200。仅前端改动，未动 gateway/db。

### Bug 修复：地图编辑器"保存不记录编辑" + 战场空白边线（2026-07-23 17:00+）
- **根因**：前端 `NewBattlefieldView.saveMap` 发送 `{terrain: dict}`（"q,r":id），但后端 `maps.ts` 的 `updateBattlefield`/`createBattlefield` 仅解构 `cells` 字段；`cells` 为 undefined → 回退 `JSON.parse(map.cells)`（旧数据），**编辑从未入库**。
- **修复**（`backend-gateway/src/routes/maps.ts`）：新增 `resolveTerrainId`/`terrainDictToCells`/`resolveCellsFromBody` 三个辅助函数，update/create 均改用 `resolveCellsFromBody(req.body, fallback)`，优先 `body.cells` → `body.terrain`(dict 转 cells 数组) → 旧数据。同时兼容直接传 cells 的旧调用方。
- **E2E 验证**（临时账号注册→创建(terrain dict)→更新(不同 terrain)→回查）：GET 返回 `cells=[{q:5,r:5,terrain:'desert'},{q:10,r:10,terrain:'water'}]` = 更新后的新值，证明持久化成功；`void` 格正确排除入库。已 DELETE 清理临时地图。
- **战场空白边线**（`frontend/src/components/HexGridCanvasEngine.vue`）：`renderTerrainCache` 与 `renderTerrainInline` 原在 iso(战场)模式对 `void` 直接 `continue` 不画边线。改为 planar 与 iso 均画极淡边线（iso `rgba(255,255,255,0.10)`、planar `0.25`），保留网格结构可见性。
- **已部署**（2026-07-23 17:20）：rsync → `frontend npm run build`(3.56s) → `docker compose build --no-cache mecha-gateway mecha-frontend` → `up -d --no-deps`。gateway `Healthy`、frontend `Started`，8081 返回 200。后端+前端双改，已重建两镜像。

### 战场相机修复：最小缩放 + 选中棋子画布消失（2026-07-23 17:50+）
- **问题2 根因**：`NewBattleView.selectUnitById` 手动算相机偏移用了 `hexToPixel(q,r)` 的**平面坐标**，但战场是 iso 模式须经 `worldOf/isoTransformPoint` 等距变换。错误偏移把相机甩到空白区 → 地形(随相机变换)被推出屏幕、棋子(drawFn 用正确等距变换)仍居中 = "画布消失只剩棋子"。
- **问题2 修复**：`selectUnitById` 改用引擎自带的 `hexGrid.value.centerOn(unit.q, unit.r)`（内部 `worldOf` 已正确处理 iso）。删除了错误的 `hg.offsetX/Y = rect.width/2 - (x+HEX_APOTHEM)*scale` 平面公式。
- **问题1 根因**：缩放下限写死 `0.2`（`focusCentralGrid`/`zoomOut`/wheel），可缩到比整图可见更小。
- **问题1 修复**：`HexGridCanvasEngine` 新增 `computeFitScale()`（整张 gridData 100×100 世界 AABB 落入视口所需最小 scale，margin 0.95），wheel/zoomOut/focusCentralGrid 的缩放下限由 `0.2` 改为 `computeFitScale()` → 缩到最小即整图完整显示、不再继续缩小。
- **已部署**（2026-07-23 17:55）：rsync → `frontend npm run build`(3.66s) → `docker compose build --no-cache mecha-frontend` → `up -d --no-deps`。frontend Recreated/Started，8081 返回 200。**仅前端改动，未动 gateway/db**。

### 用户图例核验（标准距离环图 0→1→2→3，结论：无冲突）
- 用户提供尖顶六边形距离环示意图（中心0、环1=6格绿、环2=12格米、环3=18格黄；环n恰6n格，距离n内总格数=1+3n(n+1)，n=3→37格）。
- 数学验证：`hexDistanceOffset`（combat.ts）对 2 种行奇偶 × 6 邻居共 12 组逐一代入 cube 公式，距离全=1 ✓；BFS 环扩散天然复现 6/12/18。前端 `skillRangeHexes`/`moveRangeHexes` 即此 BFS。
- 唯一订正：报告 #2 示例 A(5,10)→B(9,12) 距离由「≈5」改为「恰好=5」（axial (0,10)→(3,12)，(3+2+5)/2=5）。
- 报告已新增 §0 核验章节（含复刻该图的 SVG + 逐点核验表），报告其余结论不变。

### 审计报告发现#1 修复：combat-service 地形代价表缺 6 种地形（2026-07-23 18:10 部署）
- **关键纠正**：审计报告原写"battleState.js:9-14 缺 6 种地形"，但**生产运行时地形表是 `services/combat-service/src/services/combatCore/terrainMovement.cjs` 的 `FALLBACK_TERRAINS`**（被 skillExecutor/AI 加载），`src/state/battleState.js` 是旧参考实现、非生产路径。两处都改了。
- **改动**：
  - `terrainMovement.cjs`：`FALLBACK_TERRAINS` 补 `void=999`(不可穿越)、`ruins=2`(原 cjs 用 `ruin`，前端地图发 `ruins` 会兜底成 1→加别名)、`crystal=2`、`space/moon=1`、`desert=1.5`、`fortress=5`、`wall=99`、`spawn_*=0`、`repair_station=1`；并把 `water` 由 99 改为 2.5 与网关对齐（water 现可通行）。
  - `battleState.js`：`TERRAIN_COST_MAP` 重写为与网关 `terrainCosts.ts` 全对齐（含 void=999 / plain / ruins / crystal / rubble / city_building）。
- **运行时验证**（`node -e require(...).getMoveCost`）：void=999 ruins=2 crystal=2 water=2.5 desert=1.5 fortress=5 moon=1 plain=1 ✅。
- **部署**：rsync → `docker compose build --no-cache mecha-gateway`(tsc 通过) → `up -d --no-deps mecha-gateway`。gateway Recreated/Started，HTTP 200。combat-service 打包进 gateway 镜像（Dockerfile:65 COPY），故只重建 gateway。
- **审计剩余待修**：发现 #2(P0 /skill 无距离校验)、#3(P1 min_cast_range 不校验)、#4(P1 前端技能范围预览不排除 min_range)。用户本轮仅要求修"刚才列出来"的 #1；#2/#3/#4 待确认是否继续。

### 审计报告发现#2/#3/#4 修复（2026-07-23 18:36 部署）
- **#2(P0) + #3(P1) 后端 `/skill` 距离+最小距离校验**：`backend-gateway/src/routes/combat.ts` 新增 `resolveSkillRange(skillKey, skill_id, exeCaster, skillDefinition)` helper（字段兼容 glossary 原生 `max_range/cast_range/min_cast_range/min_range` 与归一化 `range/range_max/range_min`，字符串 "1-3" 取 max/min）。路由重写：兼容前端真实字段别名 `attacker_id/target_id/skill_id/attack_type`（skillKey=skillType||skill_id||attack_type）；execute 前用已有 `hexDistanceOffset` 校验 `dist>max→OUT_OF_RANGE`、`dist<min→BELOW_MIN_RANGE`；无目标(自身技能)/无法解析射程时跳过校验（安全默认）。`executeUniversalSkill`/`skillType` 响应改用 `skillKey`。
- **#4(P1) 前端范围预览排除 min_range**：`NewBattleView.vue` 新增 `getSkillRangeMin(skill)`，并在 `skillRangeHexes` BFS 新增条件 `cur.dist+1 >= rangeMin`（显示黄圈与目标判定同步排除最小施放距离内格）。
- **算法级验证**（node 复刻）：近战 max=1 目标 dist=5→OUT_OF_RANGE；投掷 max=3/min=1 dist=2 合法、dist=0→BELOW_MIN_RANGE；"1-3"→max=3/min=1 ✅。路由空 body→VALIDATION_ERROR（确认编译生效）。
- **部署**：rsync → `build --no-cache mecha-gateway`(tsc 通过) → `up -d --no-deps mecha-gateway`；前端 `npm run build` → `build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`。gateway/frontend 均 HTTP 200。
- **⚠️ 原记录的关键缺口（与 #1 同次发现，未在本轮 scope）**：前端 `executeAttack/executeSkillAttack` 调 `combatAPI.attack`→`POST /api/combat/:id/attack`，但**该路由不存在（404）**——攻击真实路径未接通。✅ **已于 18:51 本轮补齐，见下条。**

### 审计报告发现#1 修复：补齐缺失的 /attack 主路由（2026-07-23 18:51 部署）
- **根因**：前端 `executeAttack/executeSkillAttack` 调 `combatAPI.attack`→`POST /api/combat/:id/attack`，但 `backend-gateway/src/routes/combat.ts` **从未实现该路由**（404）→ 攻击完全不结算、#2/#3 距离校验形同虚设。
- **改动**（`combat.ts:788-878` 新增 `POST /api/combat/:battleId/attack`）：
  - 复用已落地的 `/skill` 执行链路（`.cjs` 核心引擎 + `hexDistanceOffset` 距离校验 + HP 写回）。
  - 基础攻击：`attack_type`=melee/ranged → 内联 `inlineDef`（base_damage 取单位 melee/ranged，max_range 取 `currentStats.range`）→ `executeUniversalSkill(attackType, ..., inlineDef)`。
  - 技能攻击：`attack_type='skill'`+`skill_id`(UUID) → 从 `exeCaster.skills` 按 UUID 映射出 glossary `key` → `executeUniversalSkill(key, ...)`。
  - 距离+最小距离校验：复用 `resolveSkillRange`+`hexDistanceOffset`，`dist>max→OUT_OF_RANGE`、`dist<min→BELOW_MIN_RANGE`（与 /skill 同一套逻辑）。
  - 响应结构对齐前端消费：`{ success, combat_result:{ final_damage, attack_type, attack_stat, damage_kind, dice, message }, skill_id, surprise_triggered, engine }`（前端读 `result.data.combat_result.final_damage`）。
  - 前端无需改动（响应已对齐其既有期望，前端本就调 `/attack`）。
- **验证**：
  - `docker compose build --no-cache mecha-gateway` tsc 通过；gateway HTTP 200。
  - 路由冒烟：空 body→`400 VALIDATION_ERROR(attacker_id 必填)`（确认路由注册、不再 404）。
  - 引擎级（gateway 容器内复刻路由逻辑）：近战 距离1 base_damage=12→`triggered=true, final_damage=12`；远程 距离2 base_damage=8→`final_damage=8`；近战 距离2(>max_range=1)→引擎自身拦截 `melee攻击 需要 1~1 格距离（当前 2 格）` `triggered=false`（**纵深防御**：路由层+引擎层双重校验）。
- **完整报告**：桌面 `距离射程审计报告_修复完成报告.md`（覆盖 #1/#2/#3/#4 全部修复）。
- **至此审计报告四项发现（#1 P0、#2 P0、#3 P1、#4 P1）全部修复并部署验证**。
