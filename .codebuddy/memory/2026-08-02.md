# 2026-08-02

## 七视图上传区新增：方向箭头标注 + 手动裁剪（Phase 31，已上线）
- 需求背景：七视图由玩家自行上传，易出现「外圈透明留白差异导致体型层级错位」(见上方案2回滚)。用户要求：(1)上传区用箭头标注每个方向棋子面朝方位，避免错位；(2)增加手动裁剪功能，让玩家自己裁掉外圈。
- 方向语义（已确认）：0=正面(正对观众)，1~6 顺时针每 60°（NewBattleView 旋转公式 `(facingDir-1)*60°`，dir0 不旋转）。上传区 `directionViews` 每项加 `angle`(0/60/120/180/240/300/360)，label 旁 SVG 箭头 `rotate(angle)` 指向该方向面朝方位；grid 上方加 `views-dir-hint` 文字说明。
- 手动裁剪：每个方向 slot 在已选图后显示「裁剪」按钮 → 打开 `cropModalOpen` 模态框。实现 `computeContentBBox`(alpha 包围盒)+ `openCrop`/`drawCropCanvas`/`cropPointerDown|Move|Up`(拖拽选框四边/四角/平移)/`autoCropAlpha`(一键去留白+2px 余量)/`applyCrop`(canvas 导出 PNG Blob→new File 替换 viewFiles + 刷新预览)。源裁剪不破坏外圈（玩家自控），彻底规避方案2 误伤描边问题。
- 代码：frontend/src/views/NewUnitEditorView.vue（directionViews 加 angle；新增裁剪状态/函数；模板加 dir-arrow + crop 按钮 + crop-modal；样式加 .dir-arrow/.views-dir-hint/.crop-modal 等）。已部署：rsync→build→rebuild mecha-frontend。新 bundle index-Cv_zA8jP.js，验收通过（crop-modal/自动去留白/方向说明/rotate(${N.angle}deg) 均在）。
- 注意：裁剪导出用 `new File([blob], name, {type:'image/png'})`，uploadAllViews 的 FormData image 字段照常接收；预览经 FileReader 刷新。

## 七视图体型层级失真排查与修复（方案2 上线）
- 现象：b5d90b9b 局中 s/m/l 三个体型可见高度接近，但 xl(WeirdNova) 比 l(麦德雷特) 明显高一倍。
- 根因：sevenBox 只约束"盒子外框"，盒子内原图透明留白由美术决定。实测内容占满率：l=50.8%、xl=94.5%（WeirdNova 原图 128×128 机体几乎顶满，麦德雷特 256×256 上下各留 25% 透明）。脚底锚定只钉脚底、不裁顶部留白 → 可见机体高度 = dh × 占满率，xl 凭空 2.17 倍于 l。与体型档无关（sevenBox 本身 l→xl 仅 1.17 倍）。
- 线上实际 sevenBox 覆盖配置（GET /api/combat/size-config 返回）：s=44×40/m=54×54/l=66×67/xl=77×78（xl 被体型工坊改小过，默认是 82×83）。
- 修复=方案2「内容包围框自适应缩放」：computeSevenFootRow 升级为 computeContentBounds(img) 返回完整内容 bbox（带 CONTENT_MIN_RATIO=0.25 安全下限防极端失真），绘制时 aspect 改用 cw/ch（内容框宽高比）锁 sevenBox，drawImage 源区域取内容框裁剪。偏长以高为基准、偏宽以宽为基准（沿用 sevenBox 锁边逻辑）。
- 改造后理论可见高度：s=64/m=86/l=107/xl=125（l→xl 1.17 倍，正常层级）。
- 代码位置：frontend/src/views/NewBattleView.vue（computeContentBounds + 2351 起七视图绘制块）。已部署：rsync→服务器 npm run build→docker compose build --no-cache mecha-frontend + up -d --no-deps。新 bundle index-C_YKcBrA.js，含 9 参 drawImage 裁剪，已验收。
- 注意：本局四个单位 flying/type 均为 None（非飞行），都走脚底锚定分支；脚底锚定逻辑未改（footY 改用内容框 bot）。七视图 7 方向各图按 url 独立算包围盒（_sevenViewCache key=url），天然支持。

## ❌ 方案2 已回滚（用户否决）
- 用户实测后认为"裁的外圈不对了"——内容包围框裁剪把原图应有的外圈（描边/阴影/占位边距等）也裁掉，反而比原本更差。
- 2026-08-02 已完全复原：computeContentBounds→computeSevenFootRow，绘制块恢复整图 drawImage，onload 恢复 _footY。无残留（grep computeContentBounds/_bounds 均 0）。
- 已重新部署回滚版：bundle index-ClMwJ-nT.js，9 参 drawImage=0、contentBounds=0，验收通过。
- 结论：七视图体型层级失真（xl 比 l 高一倍）的**根因在美术资产**（l 档原图留白多、xl 档占满），不是渲染代码 bug。代码侧裁剪会破坏外圈，正确解法应走**美术侧统一各档内容占满率**，或在体型工坊调 sevenBox 参数，而非运行时裁切。
- 教训：涉及"外圈/描边/阴影"的棋子图，运行时按 alpha 包围盒裁剪会误伤设计元素；此类视觉对齐问题优先推给美术资产标准化。

## 🐞 单位编辑器白屏（NewUnitEditorView 整页空白）
- 现象：用户报"单位编辑器整个区域都不见了"——`<main class="main-content">` 空白，App 布局正常，根因在 NewUnitEditorView 组件自身崩溃。
- 根因：Phase 31 加裁剪功能时用了 `reactive()`(cropRect/cropDrag) 与 `nextTick()`(drawCropCanvas)，但脚本 import 只写 `import { ref, computed, onMounted } from 'vue'`，漏了 `reactive`/`nextTick`。两者为 undefined，setup 阶段抛 ReferenceError → 组件白屏（整块编辑器消失，不只是裁剪区）。
- 修复：import 补 `reactive, nextTick`。rsync→服务器 build→rebuild mecha-frontend 已上线（bundle index-C6Aw41Dv.js，资产哈希一致、dir-arrow 在线）。
- 教训：在 .vue `<script setup>` 用到的每个 Vue API 必须显式 import；漏导入不会在本地 lint 立即爆（运行时才崩白屏），提交前务必 `npm run build` 全量校验（Vite 生产构建会报未定义 import 报错）。

## 🐞 一键清除全部视图"点了没反应"（iframe 环境 confirm 被拦截）
- 现象：用户报"一键清除键好像没用，至少前端看不到变化"。
- 根因：clearAllViews 首行 `if (!confirm('确定清除...')) return` 用原生 `window.confirm()`。在 CodeBuddy 预览 iframe / 部分嵌入环境里 confirm 被浏览器拦截，返回 false/undefined → 函数直接 return，啥都不执行 → 点击无反应。
- 修复：去掉 confirm 拦截，改为"点击即时执行 + 应用内 toast/alert 反馈"。clearAllViews 先同步清空 viewPreviews（网格立即可见变化）→ 清 viewFiles/input 句柄/form.view_urls={} → saveUnit(true) 持久化空 view_urls → loadUnits + 成功 toast。hasAnyView 检测改为同时查 viewFiles/viewPreviews/归一化 view_urls（字符串 safeParse），避免漏判/误判置灰。已部署 bundle index-fWbXcaHi.js。
- ⚠️ 通用坑：前端用户交互逻辑里**禁用原生 confirm/alert 做关键流程门控**（尤其 iframe/预览环境），改用自定义模态或即时执行 + toast，否则在嵌入环境会静默失效。

## 🐞 问题1：L 尺寸机体渲染比 S 还小约 15%
- 现象：用户报七视图里 L 尺寸机体比例比 S 小约 15%。
- 排查：后端 GET /api/combat/size-config 返回 sevenBox 正常（S=44×40 / L=66×67 / XL=77×78）；前端代码 unitSize.js 默认 SIZE_SEVEN_BOX 也 L>S；渲染端 NewBattleView 七视图绘制 `dw,dh` 由 sizeSevenBox×1.6 决定、不乘 renderScale，数学上 L 永远 > S。结论：根因是**前端 localStorage `mecha.size-config-override` 残留的体型工坊旧覆盖**，把 L 的 sevenBox 设得比 S 小（后端已正常，但前端 override 优先于后端与默认）。
- 修复：`sizeSevenBox`（前端 unitSize.js + 后端 unitSize.ts）读取覆盖配置时增加层级防护——若覆盖值 w 或 h 小于最小档(s)基准则忽略、回退默认。无论 override 来自 localStorage 还是后端 GET 都生效，自动消除 L<S。已重建 frontend 上线（bundle index-bQyZzlyp.js）。后端 unitSize.ts 改动因 gateway 有预存 tsc 错误（units.ts multer `file`、excel-parser `xlsx` 缺失）本次未重建，前端防护已足够。
- ⚠️ gateway 当前有预存 tsc 错误（`src/routes/units.ts` Property 'file'、services excel-parser `Cannot find module 'xlsx'`），`docker compose build mecha-gateway` 会失败。本次修复只动前端故仅重建 mecha-frontend。后端防护待 tsc 问题处理后再上线。

## 📋 全栈 API 与通信节点全局清查（架构师任务，2026-08-02）
- 受命做"地毯式 API 清查"。产出《全栈API与通信节点全局清查报告.md》（工作区根目录）。
- 方法：网关真相源 vs 前端消费方双向扫描 + URL 归一化（补 `/api` 前缀、展开 `${}`、参数名归一）。两个 code-explorer 子代理并行扫前后端，再本机精确对账。
- 关键结论：
  - 网关自身**无 Socket.IO 服务端**，WS 实时层在独立 `mecha-comm`(3005)。网关房间写操作后 `POST /internal/room-update`(`x-internal-token: mecha-internal-sync`)→comm 广播 `room-update` 到 `prep-<id>`。前端 NewPreparationRoom 订阅的 WS 来自 comm 非网关。
  - **双命名 convention 隐患**：单位 ID 在战斗主链路用 camelCase `unitId`，但 `deploy-unit`/`ace-unit`/`pending-units` 用 snake_case `unit_id`（网关 `String(u.id)` 匹配 pending 池，而 `initialize` 用 `u.unitId`）——新增接口极易静默回退/404。P0 治理项。
  - **幽灵 API**（client.js 定义但前端页面零调用）：matchmaking/leaderboard/battles/comm 聊天变体/joinBattle 共 6 组，后端无对应路由（G1–G6）。
  - **别名路由**：`set-pending-units` 与 `pending-units` 重复（O6）。
  - **跨服务无共享契约**：网关 payload / comm 广播 / 前端监听 三者无共享类型（C4）。
  - **房间 settings 开放透写无校验**（C5）。
  - 战斗主链路 `/move /attack /skill /end-turn /deploy-unit /pending-units` 字段映射整体对齐良好（/skill 后端还做了 attacker_id/target_id 兜底）。
- 行动建议 P0：统一单位 ID 命名 + 抽跨服务 RoomUpdate 契约；P1：删幽灵 API + 别名路由、settings 加 schema 校验。

## 🗂️ 桌面机甲战棋文档全量重分类（2026-08-02）
- 用户要求对"桌面 + 机甲战棋文件夹中所有文档报告"按【历史报告/项目资产/待完善内容/过期报告】四类重新分类，并更新索引目录。
- 决策（已确认）：①范围仅机甲战棋相关文档，不动个人文档(酒单/报销/保单等)与代码文件；②复用现有 `历史报告/` + 新建另三类；③过期报告物理移入。
- 执行：写 /tmp/sort_docs.py 一次性移动 94 个文件。结果：
  - `项目资产/` 31 份（根层活文档白名单 + 桌面散落最新报告：全栈API清查/玩家端战场实现/词条库前端风格）
  - `历史报告/` 98 份根层 md（合并原根层散落）+ 保留既有子目录(reports/桌面散落_2026-07-23/2026-07-21归档/WorkBuddy备份)
  - `待完善内容/` 6 份（idea_box/双子星拆分蓝图/技术债待办/战术板To-Do 2.0+2.1/冲突风险评估）
  - `过期报告/` 10 份（索引第五章落后版本：前端改造系列4+royroy系列2+数值2+Phase29-Debug排查稿，及 .docx 影响评估）
- 重写 `桌面/机甲战棋/索引目录.md` 为四类维度 + 关键词速查 + 过期清单（保留原 keywords）。
- 注意：`经验库/设计资料/角色视图/选手卡/backup0729/mecha-universe-engine/` 未动（用户未要求）。

## 🔍 全栈API清查报告反查根因（2026-08-02）
- 对《全栈API与通信节点全局清查报告》异常清单(G1-6/O1-8/C1-5)反查资料库，判定每个异常根因类型（功能预设/技术迭代/编程错误），已在报告第二章三节追加「根因归因」表。
- 关键反查证据：
  - G1-G3 幽灵 API(matchmaking/leaderboard/battles) 是 `backup0729/services/online-battle-service/` 独立微服务的残留封装，Phase28 审计§4.1 已记录该服务前端无代理 → **技术迭代/功能预设废弃残留**。
  - G4 `/comm/rooms` 路径错位是 comm 独立容器(3005)命名误判。
  - C2/C3 双命名 unit_id vs unitId：`历史报告/docs/ID规范标准_v1.0.md`(草案,2026-04-21)第386行规定 `battle_units.unit_id`(snake_case 源自DB列) → 属**规范未落地的技术迭代**，非笔误。
  - O6/O8/O7 冗余别名/双图床属技术迭代未收敛；G6/C1/C5/O3 属编程错误/边界。
  - C4 跨服务 room-update 无共享契约属架构级技术迭代欠治理。
- 报告(工区+桌面)已同步更新 275 行。

## 📋 文档版本符合性排查（2026-08-02）
- 对用户三类要求（历史报告四分类/项目资产过时/过期报告参考价值）做内容级排查，产出 `桌面/机甲战棋/机甲战棋文档版本符合性排查报告.md`。
- 关键发现：
  - **项目资产严重过时**：`机甲战棋-页面规划表.md` 整篇基于 2026-04 架构（/api/comm/rooms/*、online-battle-service:3006、排行榜/匹配未部署）；`战棋代码细则.md` 仍列已删 `HexGridCanvas.vue`、旧 snake_case 路由；`京都报告`/`全Phase汇总`/`完全体对账单`/`宪法修正报告` 仍引用 `HexGridCanvas.vue`（2026-07-23 删，由 HexGridCanvasEngine.vue 取代）；`Phase29_全阶段需求汇总` 第98行仍写 proxy→matchmaking/leaderboard/battles（与全栈API报告矛盾）；`前端双页面对比分析报告_副本` 对比已不存在的 NewBattlefieldView。
  - **过期报告 7/9 含可参考方法论**：Royroy V2.0 数值基线、前端改造影响评估、数值聚合规则——建议保留加水印，非彻底废弃。
  - **🔴 整理遗留重复**：`前端改造与平顶六边形_实施计划报告_v2.md` 与 `计划完成报告.md` 同时存在于历史报告与过期报告（字节/时间一致），需去重（保留过期报告那份）。
  - 历史报告"未执行"≈20份（2026-04早期框架稿+含计划/方案/蓝图字样预设稿）；"已迭代"占多数。

## 🐞 问题2：七视图自动裁剪右侧不检查
- 现象：自动裁剪的裁剪框右边永远以图上方的顶点像素为准，未检查下方内容更靠右的部分。
- 根因：`computeContentBBox` 内层 x 循环在找到该行第一个不透明像素后立即 `break`，导致只更新 left（每行最左），但 right 依赖"恰好最后设置行"，若下方某行最右像素更靠右则漏检。
- 修复：去掉内层 `break`，完整扫描每行，left 取全局最小、right 取全局最大。已随 frontend 重建上线（bundle index-bQyZzlyp.js）。

## 🗂️ 项目资产重编 + 过期报告价值重评估（2026-08-02）
- 用户要求：①先去重；②项目资产有效部分结合实际现状重新生成一套；③据新资产重新详细检查过期报告参考价值并评估使用价值，生成待确认技术资产文件到项目资产；④按创建时间顺序总结开发弯路与规避经验，生成开发经验报告到项目资产。
- **去重**：复核确认历史报告/过期报告无重复文件（前期去重"前端改造"两份副本已生效），过期报告内部 10 份无重复。✅
- **项目资产重编**：派 code-explorer 核对真实代码现状（4 容器 3006 大一统、combat .cjs 被 gateway nodeRequire 复用、前端仅 HexGridCanvasEngine.vue、api 单文件 client.js、PixiJS 弃用、入口 NewHomeView、坐标仍尖顶 Even-R 保守平顶、faction 仅展示/role 轮转逻辑唯一依据）。产出 `桌面/机甲战棋/项目资产/机甲战棋-项目资产总览（现行版）.md`——权威整合部署/路由/前端/战斗/样式真相源，去伪存真。另对 5 份严重过时文档加"⚠️含过时架构引用"页眉降级：`战棋代码细则.md`(HexGridCanvas.vue/旧微服务端口)、`机甲战棋-使用说明.md`(TerminalView/PixiJS)、`机甲战棋-页面规划表.md`(2026-04微服务)、`机甲战棋完全体基准资产大对账单.md`(已删组件)、`前端双页面对比分析报告_副本.md`(NewBattlefieldView 旧版)。
- **过期报告价值重评估**：逐份评估 10 份过期报告（含 .docx 不可读标注）。高价值且仍适用：坐标距离真相源(offset→axial)、Royroy 属性模型与状态机、阵营轮转+Round边界重置AP、数值计算基线(含修正规格)、伤害分担管线、挤出2.5D立体化路线(drawIsoHexColumn)。产出 `桌面/机甲战棋/项目资产/待确认技术资产清单.md`——每项标来源/核心价值/使用价值评级，待用户确认并入正式资产。
- **开发经验报告**：`桌面/机甲战棋/项目资产/机甲战棋-开发经验与避坑报告.md`——按 2026-04→08-02 时间轴归纳 12 条弯路（微服务拓扑污染、PixiJS摇摆、平顶六边形高风险改写被否决、组件大重写、入口重构、faction语义混淆、掷骰重复7+处、unit_id/unitId双命名、距离公式两路不一致、部署熵增、方向6扇区偏移、文档代码脱节）+ 8 条规避 Checklist。
- **索引目录**已更新：项目资产 31→33 份，新增 3 份置顶，5 份降级标注。
- 全部为桌面文档库操作，未触碰代码/服务器。

## 🧹 全栈 API 清查分段清理（2026-08-02）
- 用户要求：据《全栈API与通信节点全局清查报告》分段清理 G1–G6 前端幽灵（client.js）、后端 O6/O8/O4 孤儿。
- **前端 G1–G6**：在 `frontend/src/api/client.js` 删除 `onlineBattleAPI` 的 matchmaking(.joinQueue/leaveQueue/getQueueStatus)、聊天(getChat/sendChat)、leaderboard(getGlobalLeaderboard/getFactionLeaderboard)、battles(getBattleHistory/getBattleResults) 四块 + 整个 `commAPI` 死代码块 + `combatAPI.joinBattle`。保留 onlineBattleAPI 房间管线（仍被广泛使用）。`node --check` + IDE lint 0 错误。
- **后端核实（关键）**：O6 `set-pending-units` 别名路由、O8 的 `admin/upload-image` 与 `units/upload-image` 经搜索**均已在代码中不存在**（units.ts:495 注释确认单图上传已移除，封面图改七视图正视图派生；仅剩 `asset-gen` AI 生成专用，非通用图床；前端无 `assetAPI.uploadImage` 封装）。故 O6/O8 无可操作，如实向用户报告并跳过。
- **O4 真实化身**：报告写的 `/combat/:battleId/debug-state` 后端不存在，真实调试路由是 `GET /api/debug/test-snapshot/:battleId`（combat.ts，无 authenticate，临时验收用）→ 已加 `process.env.NODE_ENV==='production'` 拦截，生产返回 404 防泄漏。
- 后端 `npx tsc --noEmit` 通过；已在该清查报告末尾追加「分段清理执行记录」标记各项状态。
- 教训：报告是旧快照，清理前必须对照**当前代码实况**复核，避免凭报告删不存在的路由。

## 🛡️ P0 命名统一 + C5 房间防投毒强校验（2026-08-02，已落地代码，未部署）
- **P0 命名分裂根治（C2/C3）**：前端 NewBattleView.vue 硬编码 17 处 `unit_id`（含 `caster_unit_id`/`target_unit_id`）→ 全改 `unitId`/`casterUnitId`/`targetUnitId`（vite build 通过，全文 0 残留）。后端 combat.ts 采用「双接收兜底」`const unitId = req.body?.unitId ?? req.body?.unit_id` 覆盖 13 个接收点（ace-unit/surprise-choice/deploy-unit/move/stealth/action 各分支/duel-check/resolve-duel/resolve-snatch），响应键 `unit_id`→`unitId` 同步统一。tsc 干净。**零停机过渡**：旧前端发 unit_id 仍被兜底识别。
- **C5 房间防投毒（rooms.ts:562 PUT /settings）**：抽独立校验模块 `routes/roomSettingsSchema.ts`（保持路由层纯净，不写死枚举字典）。规则：①基础字段强校验 `name`(1–40 trim)、`maxPlayers`(2–16 int)、`turnTimeLimit`(0 或 30–3600 int)；②未知键显式 Reject(400) 停止静默忽略；③JSON 字段(rules/victoryConditions/factionPasswords)结构防御——限深 4、限体积 8KB，切断 JSON 炸弹；④系统字段黑名单 `host_id/created_at/id`（+决策B越权防护 `mapId/isPrivate/code`）入口拦截(FORBIDDEN_FIELD)。`factionPasswords` 子键白名单+4位数字规则仍复用 rooms.ts 既有 `normalizeFactionPasswords`。tsc 干净，**未部署**（按纪律需服务器 build gateway）。
- 边界确认细节：决策A(结构防御不解耦细碎枚举)、B(mapId/isPrivate/code 不纳入)、C(系统字段隔离) 均由架构师拍板。原代码 `if(name)`/`if(maxPlayers)` 的空串/负数绕过缺陷已修复。
- **C1 加固**：`combat.ts` createBattle 路由 `!battlefield_id` 拦截时检测 `battlefieldId` 驼峰误写，返回明确开发者提示（hint: expected_key/ got_key）。轻量防御，tsc 干净。
- **✅ 全栈已部署（2026-08-02 13:38 上云）**：rsync→服务器 `npm run build`(前端 exit0, 资产 `index-qW1VtoLr.js`)→`docker compose build --no-cache mecha-gateway mecha-frontend`(均 Built, gateway tsc 干净)→`up -d --no-deps`(gateway Healthy, db 孤儿隔离未碰)。**哈希验收通过**：服务器 dist 与容器内 `/usr/share/nginx/html/assets/index-qW1VtoLr.js` md5 均为 `ade7de710ce536de420056116248eafe`；8081 index.html 引用一致；gateway `/api/combat/` HTTP 200。P0+C5+C1 三处改动全部上线。

## 🩺 项目缺陷诊断 + 网络资源调研（2026-08-02）
- 用户要求："先生成缺陷报告到桌面/机甲战棋，再搜集网络现成可复用内容"。
- **产出1**：`桌面/机甲战棋/项目缺陷诊断报告-2026-08-02.md` —— 基于真实代码探查（非凭记忆），12 项缺陷按 P0–P3 分级：
  - P0:#1 JWT 弱密钥(mecha-universe-jwt-secret-change-in-production 仍 fallback)、#2 combat.ts 217 处 any + 双接收兜底固化命名分裂、#3 前端 proxyRefs .value 误用风险。
  - P1:#4 combat-service 幽灵残留(3003/3004 端口散落+死代码外壳未删)、#5 双 DB(SQLite/PG)主权模糊、#6 console.* 散落泄露敏感上下文。
  - P2:#7 combatCore 40 处 unit.faction 直比(红线未全量审计)、#8 静默 catch 半更新态、#9 前端 .tactical-* 绿调死代码、#10 dist-dev 被提交。
  - P3:#11 gateway 路由层零单测、#12 部署无 CI。
- **产出2**：`桌面/机甲战棋/项目缺陷-网络资源调研.md` —— 逐项检索网络现成资源，**12 项全部有可复用内容无需自研**：
  - #1 openssl rand -base64 64 + 环境变量范式；#2 tsconfig strict + pickId helper；#3 Vue 官方 defineExpose 解包语义；#4/#9 Knip 死代码扫描；#5 db-migrate + 主权文档；#6 Pino/Winston + eslint no-console + 构建剥除；#7 grep + unitRoleOf 守卫；#8 better-sqlite3 事务回滚范式；#10 Node .gitignore 模板；#11 Jest+Supertest 契约测试(goldbergyoni 最佳实践)；#12 GitHub Actions SSH+docker compose 模板。
- 结论：低工作量高收益优先项 = #1(一行)+#10(.gitignore)+#4/#9(Knip 一键)+#6(Pino)+#12(Actions 模板)。
- 注意：#2/#4/#7/#8 的"根因"与今日已落地的 P0 命名统一(C2/C3)、C5 房间校验互为补充——命名分裂兜底已做入口层，内部 camelCase 纯净化与 combatCore 审计仍待排 Phase。

## 🗺️ 三阶段重构路线图文档化 + 冲突实测（2026-08-02）
- 用户给出三阶段渐进重构路线（防御快攻→自动化大扫除→模块收口），要求生成系统化文档并核查与项目冲突。**产出**：`桌面/机甲战棋/系统化重构路线图-2026-08-02.md`。
- **实测核对出的关键冲突（决定路线图能否直接套用）**：
  - **A 硬冲突-DB 驱动**：路线图写"用 better-sqlite3 的 db.transaction()"，但项目 SQLite 层实际是 **`sql.js`（WASM 纯 JS）**，无 `db.transaction()`。须改手动 `BEGIN/COMMIT/ROLLBACK`。⚠️ 这是路线图的隐性技术假设错误，未实测会写出无效代码。
  - **B 硬冲突-事务作用面**：战局状态是**纯内存**（`combat.ts:295` 注释"内存战局存储，后续迁移 Redis/PG"；全仓 `persistChanges`/`saveToDisk` 出现 0 次）。`combat.ts:177/186` 的 catch 操作内存对象 `battle`，DB 事务无效。内存战局"半更新态"须用 `structuredClone` 快照+`Object.assign` 回滚。事务只包元数据（房间/部署池/用户）落盘。
  - **C 软冲突-PG 主权**：路线图"降级 PG 只读"与现状**一致**（PG 已是只读复活桥，auth 仅 SQLite 未命中时查），文档据实固化即可。
  - **D 中冲突-strict**：`combat.ts` 217 处 `any` + sqlite/postgres 大量 `any`；一次性 `strict:true` 会致 tsc 报错爆量（历史曾致 compose build 失败）。须先 `noImplicitAny` 渐进。
  - **E 中冲突-Knip 误删**：`services/combat-service/combatCore/*.cjs` 是 gateway 经 nodeRequire 桥接的**真实引擎依赖**，非死代码；Knip 须配 ignore 白名单只删独立服务外壳(db.js/auth.js/battles.js/routes)。
  - **F 中冲突-CI 触 db**：Actions 必须 `--no-deps` 且绝不触 `mecha-battle-db`（MEMORY 强约束），改 comm 须 `up --no-deps mecha-comm`。
- 三份桌面文档关系：缺陷诊断(12项) → 网络资源调研(每项的现成库/范式) → 路线图(三阶段可执行+冲突修正)。结论：12 项均无需自研，但有 6 处冲突须适配，尤其 A/B 直接决定阶段一 1.3 改造是否有效。

## 🛡️ 加固细则三处 + 实测确认 (2026-08-02)
- 用户对主路线图的 1.3/2.3/3.1 提出三项细化(内存快照安全边界 / CI 防御性回滚 / 网关-引擎 DTO 绝对隔离)。**产出**：`桌面/机甲战棋/重构方案加固细则-2026-08-02.md`。
- **实测确认的关键事实**(决定细化是否直接套用)：
  - `battle` 内存对象含 `units: Map`(非绝对纯净),`structuredClone` 可用但须先验证纯净度+设性能边界;回滚用 `Object.assign(battle, snap)` 顶层替换(不能逐字段 merge,否则 Map 引用仍旧)。Command Pattern 分两步走(Step A 快照先行,Step B 抽 `applyCommand/ inverse` 演进)。
  - `req.body` **确实渗透**引擎: `combat.ts:1235 context=req.body.context` → `:1311 battleState: battle` 原样传 `executeUniversalSkill`。证实 3.1 DTO 隔离必要;但 `context` 透传是"请求级注入掷骰分支"设计特性,**不能全拒**→改白名单剥离(仅留 `skillDefinition/diceBranches/isWeaponAttack`,剔除 `battleState/allUnits/terrainMap` 防前端伪造战局)。
  - `@mecha/shared-kernel` 是独立纯类型包,可作 DTO 基座;`toExecutorUnit(u)` 映射器已存在,暗示 BattleUnit 是纯数据。
  - CI 回滚须 `--no-deps` + 不动 db(MEMORY 强约束);建议先补网关 `/health` 端点再上健康探测。
- 三处加固未引入新硬冲突,反而坐实主方案 A/B/F 修正的必要性。落地顺序微调:阶段一先 1.1+1.3-StepA+1.2;阶段二先补 /health 再上 Actions 回滚;阶段三 3.1 pickId+DTO 白名单(与 1.3-StepB 协同)。

## ✅ 防御性任务已实施 (2026-08-02)
- 用户要求立刻动手阶段一最低风险三项。已落地：
  - **.gitignore 清理**:追加 `dist-dev/` + `frontend/dist-dev/`。实测 `git ls-files | grep dist-dev` = **0**,说明 dist-dev 构建产物**未被 git 跟踪**(之前报告里"12 处 TODO"是源文件内字符串引用,非被跟踪文件)。故无需 `git rm -r --cached`,仅加规则即完成防御性预防。已在结论中如实说明,不执行无意义清除。
  - **快照回滚脚手架 Step A**:`combat.ts` 在 `requireBattle`(line 89)后新增 `withBattleRollback<T>(battle, fn)`,用 `structuredClone(battle)` 前置快照 + `Object.assign(battle, snap)` 顶层引用替换还原(针对 battle 含 `units:Map`,不可逐字段 merge)。已接入两处脆弱写回:**(1) 胜利条件绑定路由**(`battle.victoryConditions=` + `battleStore.set` 包进回滚);**(2) 超时清算 `forceSettleOnTimeout`**(`runAttackInternal` 结算包进回滚,异常保留 pendingSurprise='settling' 不卡死)。`npx tsc --noEmit` 通过(无报错)。
  - **JWT 密钥准备**:本地 `openssl rand -base64 64` 生成样例密钥(见下方)。真正生产密钥须在**服务器**执行同命令写入 `.env`,并选低峰期替换(换密钥全员重登;改后须 `up --no-deps mecha-comm mecha-gateway`)。
  - 样例密钥(仅参考,服务器须重新生成): `GqJ2Gp6E/BsRDvzcO2MYkwVUYOMuOkABvXE5HNQUoyQTPzBx0Tm40snWj6yZiYHbpCOzNSK9YcJ0Gaq0eqWJ7g==`
- 待办:密钥替换须用户手动在低峰期于服务器执行(代理不能自作主张触发全员重登)。后续阶段二/三按路线图推进。

## ✅ 阶段二已提交 (2026-08-02)
- 用户给三步走战术（补探活/Pino → Knip 清死代码 → CI 回滚）。已落地并 commit `391955c`（20 文件，+109/-5907）。
- **关键实测修正（避免照抄翻车）**：
  - **/health 已存在**：`app.ts:88` 已有 `/health` 端点（返回 {status:'ok',service,timestamp}），且 `app.ts:70` 已对 /health 做限流豁免。故未重复添加（避免路由冲突）。CI 探活 `curl localhost:3006/health` 可直接用。
  - **Knip 假阳性陷阱**：在 backend-gateway 跑 `npx knip`，报 45 个 unused exports（battleStateFactory/unitSize/combatBridge 的导出）+ `logger.ts`/`pino` unused。**这些是假阳性**——45 exports 极可能是 combatCore 经 nodeRequire 动态调用的（静态分析看不到），`logger.ts`/`pino` 是刚创建未引用。**绝不能按 Knip 盲目 rm 这 45 个**。
  - **combat-service 外壳真死证据**：grep 确认 backend-gateway 与 docker-compose.yml **均不引用** `combat-service/src` 的 `config/database/middleware/routes/validators/state`（无独立 combat 容器，memory 已载）。combatCore 只内部 `require('./configLoader.cjs')`，不依赖上层。故安全删除外壳，**保留 combatCore 引擎库**。
  - **combat-service 真实结构**：`src/index.js`+`src/routes/`+`src/middleware/`+`src/database/`+`src/validators/`+`src/state/`+`src/config/`+`src/services/{combatResolver,socketService,turnManager,unitConverter}.js` 全删；用户给的 `rm src/db.js` 路径错（实际 `src/database/db.js`）。
  - **原子 commit 防污染**：`git add services/combat-service/src` 会把 combatCore 下**历史遗留未提交改动**(M/A) 连带暂存。改为 reset 后**逐文件精准 add 删除项**，排除 combatCore 的 M/A。最终仅提交阶段二真实改动。
- **产物**：`backend-gateway/src/utils/logger.ts`(pino，redact 脱敏)、`combat.ts` withBattleRollback 改用 logger.error、`knip.json`(白名单保 combatCore)、`.github/workflows/deploy.yml`(探活+秒级回滚+--no-deps 隔离 db)。
- **待办**：JWT 密钥仍待低峰期服务器替换；阶段三(pickId/DTO/strict/主权文档)未动；后端 console.* 仅替换了 1 处，批量替换待排期；Knip 45 exports 需专项审计确认是否真死（不要现在删）。

## ✅ 阶段三已提交 (2026-08-02)
- 用户指令"开始阶段三(pickId/DTO/strict)"。已落地并 commit `4f77602`（3 文件，+205/-9）。
- **strict 实测结论**：`tsconfig.base.json` 早已 `"strict": true` 且 `backend-gateway` extends 它，`build`=`tsc`。`npx tsc --noEmit` 当前**干净(退出码0)**——阶段二已把 combat.ts 等大量 `any` 显式化。故 strict 目标**已完成**，无需改 tsconfig。
- **pickId 收口**：新增 `backend-gateway/src/utils/combatId.ts`（`pickId`/`pickUnitId`/`pickCasterId`/`pickTargetId`）。统一归一化：双命名(camel/snake)兜底 + `String()`+`trim()` + 防 `null`/`''`/`'null'`/`'undefined'` 伪命中 `battle.units.get(null)`。combat.ts 5 处裸 `req.body?.unitId ?? req.body?.unit_id` 收口为 `pickUnitId(req.body)`（ace-unit/surprise-choice/deploy-unit/move/stealth）。
- **DTO 白名单隔离**：新增 `backend-gateway/src/utils/skillDto.ts`（`sanitizeSkillContext`/`buildSkillDto`）。`CONTEXT_ALLOWLIST`=skillDefinition/diceBranches/isWeaponAttack/surpriseAllowed/source；`CONTEXT_BLOCKLIST`=battleState/allUnits/units/terrainMap/battle/caster/target/executorState/gameState/state。blocklist 优先级高于 allowlist（双保险）。combat.ts 在 `(1) /skill 路由` 与 `(2) runAttackInternal` 引擎调用前 `...sanitizeSkillContext(context)` 剥离——即使客户端在 context 塞伪造战局对象也被剔掉，且内存权威态已覆盖 allUnits/battleState/terrainMap。
- **边界确认**：`/damage` 路由不接 context、仅用 caster/target 单位对象做纯伤害管道，无伪造战局风险，保持不动。`buildSkillDto`/`pickId`/`pickCasterId`/`pickTargetId` 导出供跨模块复用，combat.ts 仅导入实际使用的 `pickUnitId`+`sanitizeSkillContext`（避免未使用导入）。
- **验收**：`tsc --noEmit` 干净、lint 0 错误。**未部署**（按纪律需服务器 build gateway）。后续：JWT 密钥低峰期替换、console.* 批量替换+ESLint no-console、Knip 45 exports 专项审计、@mecha/shared-kernel DTO 基座深化。三阶段重构主线(pickId/DTO/strict)已完成。

## 🚀 阶段三网关已上云部署 (2026-08-02 15:10)
- 用户指令按红线纪律执行最终部署：rsync → `docker compose build --no-cache mecha-gateway` → `up -d --no-deps mecha-gateway`。
- **rsync**：排除 node_modules/.git/dist/*.db/data/*.log，48 文件投送成功，combatId.ts/skillDto.ts 入库 /root/mecha-universe-engine/backend-gateway。
- **构建**：BUILD_EXIT:0，`--no-cache` 斩断缓存，DTO 校验逻辑全量编入镜像（sha256:764d49b8...）。
- **重启**：`--no-deps` 无损替换，`/health` 返回 HTTP 200；`mecha-battle-db` 仍 `Up 2 days (healthy)`——孤儿容器隔离保护达标，未触发 --remove-orphans。
- **容器内核验**：`/app/dist/utils/combatId.js`(3312B) + `skillDto.js`(2651B) 存在；`combat.js` 含 3 处 `sanitizeSkillContext`（/skill + runAttackInternal + import）。阶段三逻辑线上全量生效。
- ⚠️ 注意：本次 rsync 把工作区**未提交**的 backend-gateway 改动（terrainCosts/units/auth/rooms 等 15 M + 6 ?? 文件）一并上云——均经 `tsc --noEmit` 干净验证。这些改动尚未 git commit，下次会话若需追溯建议先 commit。

## 🚀 最终清扫 + 全量上线 (2026-08-02 17:00)
### 1. JWT 弱密钥斩断 ✅
- 服务器 `/root/mecha-universe-engine/.env` 原先**不存在**——compose 第53(gateway)行 `${JWT_SECRET:-弱密钥兜底}` 与第99(comm)行**硬编码弱密钥** `mecha-universe-jwt-secret-change-in-production` 都 fallback 到弱密钥（comm 因硬编码根本不读 .env）。
- **修复**：(a) 本地 docker-compose.yml 第99行 comm `JWT_SECRET=弱密钥` → `JWT_SECRET=${JWT_SECRET}`，随 rsync 上云；(b) 服务器 `openssl rand -base64 64` 生成强密钥写入 `.env`（JWT_SECRET 用**双引号包裹**——base64 含 `/` 和 `==`，未引用 compose 解析报错 `unexpected character "/"`）；(c) `.env` chmod 600。
- ⚠️ **关键坑**：`.env` 绝不能进 rsync（否则 `--delete` 会删服务器密钥）。所有 rsync 命令已加 `--exclude '.env'`；CI deploy.yml 的 rsync 也补了 `--exclude '.env'`。本地**不保留** `.env`（密钥只在服务器）。
- 联动重启 mecha-gateway + mecha-comm 成功（comm 硬依赖密钥，双 Healthy），全员需重登（符合用户授权）。

### 2. console.* 收束 + ESLint 门禁 ✅
- 写 `scripts/console-to-logger.cjs`(单文件) + `console-to-logger-all.cjs`(全量 src/**/*.ts) 批量把 `console.{log,error,warn,info,debug}` → `logger.{info,error,warn,...}`（Pino，结构化 `{msg:\`...\`}` 合并参数防 Pino 丢多参数）。`console.log`→`logger.info`，`console.error`→`logger.error`，`console.warn`→`logger.warn`。脚本自动注入相对 `../utils/logger.js` import。
- ⚠️ 坑：Pino 无 `logger.log` 方法 → 批量 `sed logger.log( → logger.info(`；import 深度算法初版错（算成 `../src/utils`）→ sed 统一 `src/utils/logger.js'`→`./utils/logger.js'` 与 `../src/utils/logger.js'`→`../utils/logger.js'`；删 logger.ts 自引用 import（造成声明合并冲突）。
- **ESLint 门禁**：装 `eslint@^8.57` + `@typescript-eslint/parser@^7.18` + `eslint-plugin@^7.18`（devDeps），新建 `.eslintrc.cjs`：`no-console: ['error', {allow:['warn','error']}]`，`extends:['eslint:recommended']`，`ignorePatterns:[dist,dist-dev,node_modules,scripts/**,services/combat-service/**]`，并关 `no-unused-vars/no-case-declarations/no-undef/no-empty`（仅守 console 门禁，不误伤历史代码风格）。加 `npm run lint` 脚本 + deploy.yml CI 步骤 `npm install && npm run lint`（阻断门禁）。
- **实测**：`npx eslint . --ext .ts` 0 error；`tsc --noEmit` 0 error。门禁真实生效。

### 3. 全量部署 ✅
- rsync（排除 .env/node_modules/.git/dist/*.db）→ 服务器 `frontend npm run build` → `docker compose build --no-cache mecha-gateway mecha-frontend mecha-comm`。
- ⚠️ **构建失败坑**：Phase2 删了 `services/combat-service/src` 外壳（含 `src/config/glossary-skill-config.json`），但 Dockerfile 第74-79行仍 `rm`+`ln -s` 该路径 → `No such file or directory` 失败。修复 Dockerfile：data 下 glossary 缺失时，若 config 存在则搬移+symlink，否则（Phase2 现状）`echo '{}' > /app/data/glossary-skill-config.json` 创建空壳。重建通过。
- 重启 `up -d --no-deps mecha-gateway mecha-frontend mecha-comm`：三容器均 Healthy；`mecha-battle-db` 仍 `Up 2 days (healthy)`——孤儿隔离达标；`3006/health` HTTP 200。
- **资产哈希验收**：线上 8081 `assets/index-KH22fMWX.js` md5 `6d0ad30...` == 本地 build 产物 `6d0ad30...`，完全一致。

### ⚠️ 未 commit 事项（下次会话建议先 commit）
- 本次所有改动（docker-compose.yml 密钥修复、console 收束、eslint 配置、Dockerfile 修复、scripts/*.cjs）均**未 git commit**，仅 rsync 上云。backend-gateway node_modules 因 npm install eslint 有改动但 node_modules 不进 git。

## 🎯 技能射程 Bug 架构级治本（Phase 31 深化，2026-08-02 晚）
- **背景**：上一轮"表面修复"只把 normalizer/glossary 的兜底 1 改成按分类 3，但埋新雷——四处默认表共存 + 我把引擎 ranged 从 6 改成 3 但 combat.ts/NewBattleView 的 BASIC_RANGED_RANGE 仍是 6（数值又打架）；且数据源仍写死值（只是 1→3），下游兜底仍废。用户结合网关报告+Phase31 审计，给出"四陷阱+三不原则"剖析。
- **四陷阱**：①数据源写死 cast_range 废掉下游兜底（最致命）②默认表四头分化+数值打架 ③旧战局 JSON 残存 cast_range=1 阴魂不散 ④type/category/typeLabel 字段脱节读 undefined 落 ??1。
- **三不原则治本（已全执行+部署）**：
  1. **源头不写死**：`excel-schema-normalizer.mapSkills` 未配置 range 返回 `{}`（`cast_range` 等保持 **undefined**，仅标 `category`）；`glossary.normalizeSkillForSave` 的 `cast_range==null` 不写死，只数字清洗+标 category。✅ 容器内实测 mapSkills([{type:"远程"}]) 输出 cast_range=**undefined**（铁证）。
  2. **默认值不重写**：`DEFAULT_RANGE_BY_CATEGORY` 从 normalizer/glossary/skillExecutor/NewBattleView **全删除**，唯一真相源收归 `shared-kernel/src/hexMath.ts`（ranged=**3**，用户裁定）。combat-core 经 `hexKey.cjs` re-export；gateway 直接 import；前端因未纳入 monorepo workspace 而在 `hexUtils.js` 保留**逐字镜像**+注释约束（与 hexDistance 同惯例）。`skillExecutor._getUniversalFields` / `_getUniversalFields` 的 maxRange/minRange 改调 `getSkillRangeFields(cfg)`。
  3. **前端不独立猜**：`NewBattleView.getSkillRange/getSkillMinRange` 删本地 `RANGE_DEFAULT_BY_CATEGORY`+`resolveSkillCategory`，改调 `hexUtils.getSkillRangeFields`。
- **附带修复真实 bug**：`getSkillRangeFields` 原 `toNum` 不解析 `"2~5"` 字符串 → Number("2~5")=NaN 静默失效落分类兜底。改为 `parseOne` 支持数字/`"a~b"`字符串/`{min,max}`对象（shared-kernel + hexUtils 镜像同步）。
- **数值打架修复**：combat.ts / NewBattleView `BASIC_RANGED_RANGE` 6→3 对齐真相值 3。
- **部署实测**（先确认 SSH 连通+df 18G 可用，吸取断链教训）：shared-kernel build→gateway npx tsc 0 error→rsync(含-e ssh -i 密钥, 排除 node_modules/.git/*.db)→服务器 frontend build(index-BOuJmvzb.js)→`compose build --no-cache mecha-gateway mecha-frontend`(BUILD_EXIT=0)→`up -d --no-deps`(两容器 Recreated+gateway Healthy)。容器内真调用：`getSkillRangeFields({type:"远程"})`→{max:3,min:1}；normalizer 输出 cast_range=undefined；前端 bundle 含 ranged:3；/health 200。
- **⚠️ 陷阱3 需用户配合**：旧战局（如 13:38 那局）JSON 已残存 cast_range=1，代码无法改历史数据，须**重开新局**才能看到正确 3 格。
- **产出报告**：`历史开发报告/技能射程BUG根因剖析与治本报告-Phase31深化.md`（四陷阱剖析+三不原则改动清单+部署验证+遗留事项）。
- **治本收益**：日后调远程射程只需改 shared-kernel 一处 `ranged:3` + build + 部署，其余三处自动同步。

## ✅ 原子 Commit 封印完成 (2026-08-02 17:30)
- 用户要求把"云端已生效本地裸奔"状态封印为 3 个原子 commit，已严格执行：
  1. `1e7f0b7` **chore(deploy)**: docker-compose.yml(comm 硬编码弱密钥→${JWT_SECRET}) + deploy.yml(rsync 排除 .env + Lint Gate 步骤)。2 files, +18。
  2. `58e8f3c` **fix(build)**: backend-gateway/Dockerfile(glossary symlink 兼容空壳降级)。1 file, +16/-9。
  3. `d53cd50` **refactor(core)**: console 收束 + .eslintrc.cjs + package.json lint 脚本 + scripts/*.cjs 迁移脚本 + backend-gateway/src 全部 .ts(含阶段二残留网关源码一并封印)。32 files, +12221/-1511。
- **安全校验**：`.env` 确认未进版本库（`git ls-files | grep .env` 为空）——密钥只在服务器。
- **范围纪律**：frontend 视图 / combatCore .cjs / shared-kernel / docs / memory 等 43 个未跟踪/已改文件**不在本次指令范围**，未擅自 commit（保持精准分组不污染）。这些仍是"云端真理未封印"状态，下次若需追溯应另立 commit。
- **教训加固**：rsync 同步整目录会把工作区所有改动上云，但 git commit 必须精准分组；两者解耦。ESLint devDeps 写入根 package-lock.json（非 backend-gateway 下）。

## 🧩 43 项未提交 → 纵向切片原子 Commit（2026-08-02 晚）
- 用户要求打破"按目录提交"习惯，改为**按业务契约（Domain 纵向切片）**提交，严防 DTO 漂移（风险3）。
- 我先生成《未提交项-历史报告交叉对比与连接推演报告.md》：用 grep 精确匹配核心模块名在 `历史开发报告/`+`历史备份/`（约310篇 md）中 **0 命中**，证明这43项（实际39改+4删+30未追踪项目文件）在老报告中几乎全未提及——它们是 Phase30(DiceUnify)→8/1体型工坊→8/2方向语义这条晚于老报告的演进线，非垃圾。
- **最终提交链**（在 d53cd50 之后）：
  1. `9abe498` **feat(combat)**: combatCore(**diceService单例/唯一掷骰真相源 + victoryChecker/conditionTrigger/reactionHandlers/hexKey/damageModifiers**) + comm-service(socket 转发 reaction/attack-report/room-update) + shared-kernel/types.ts + 前端 contracts/skillContract.js + utils/unitSize.js + utils/diceUtil.js。生同生死同死，杜绝版本代差。
  2. `d61c8dd` **feat(client)**: views/*.vue + components/*(含 HexGridCanvasEngine/AssetGenPanel/AttackReportModal/reactions) + stores/(apStore/logStore/moveStore) + battleSocket/animationQueue + hexDraw/hexUtils + api/client + App/main/variables/glossary-theme.css/nginx/vite。前端消费层。
  3. `384b6b8` **chore(meta)**: scripts/(data-contract-tests/manual-input-audit/syncMeowaTerrains) + 报告/html 归档 + **物理删除4个过时 tracked .md**(WeirdNova/异星对照v2/异星实战/当前版本缺口) + .gitignore 追加 `*.tsbuildinfo`。
  4. `3c9c497` **fix(hangar)**: hangar units.js 支持 AssetGen 贴图写入字段（B 域 AssetGenPanel 的后端落点补完）。
- **关键踩坑**：
  - `shared-kernel/tsconfig.tsbuildinfo` 是构建缓存，已 `git rm --cached` + 加 `.gitignore`（不进库）。
  - 中文文件名在 git 命令中匹配极不稳定（`git rm --cached`/`git add` 反复空操作）。最终用 `git add -A` 全景暂存 → `git reset` 掉 `.codebuddy/.agents/.vscode/skills-lock/memory` 污染 → 保留目标文件提交，才可靠。
  - 4 个"删除 md"中 WeirdNova + 异星v2 + 异星实战 + 当前版本缺口 **全部是 tracked**，用 `git add -u` 一次性正确暂存删除（之前误判"3个未入库"是 `git cat-file` 检测手法有误）。
  - 因中文路径反复失败，先用 `git reset --soft d61c8dd` 把 D/hangar 多个碎 commit 收回，重组为干净 D 域单 commit + hangar 单 commit，避免历史碎片化。
- 验证：项目核心项 0 残留（`git status` 仅剩 memory/codebuddy/agents/vscode/skills-lock，按规则不提交）。

## 🏷️ 技能射程/辐射范围术语统一（2026-08-02 晚）
- 用户要求消除"配置端 Editor 与消费端 UI 的方言"：前端 `NewBattleView.vue`（消费端）+ `SkillsEditor.vue`（配置端）的"范围/距离"统一改为"射程/辐射范围"。
- **射程**=`cast_range`（施法者到目标点最远施法距离）；**辐射范围**=`aoe_radius`（以选中攻击点为中心向外扩散格数）。后端降级链 `cast_range: cfg.cast_range ?? DEFAULT_RANGE_BY_CATEGORY[cfg.category] ?? 1` 健壮（skillExecutor.cjs:141，category 默认 melee=1/ranged=6/automation=0）。
- 改动：SkillsEditor.vue label/placeholder "距离"→"射程" + effectDescriptions 全量（距离→射程、覆盖面→辐射范围）；NewBattleView.vue 两处"范围"→"射程" + 有 aoe_radius 技能追加"辐射范围" + throw/sweep tooltip 同步。GlossaryView 用英文字段名(cast_range/aoe_radius)无中文歧义，保留。
- **已 commit `c3b76fe`** `style(ui): 统一技能射程/辐射范围术语，消除配置端与消费端方言`（2 文件，+14/-14）。按纪律未部署，等底层六角格测距逻辑拿下后再一起打包。

## 🔍 六角格测距"双份真相"核查（2026-08-02 晚，重要更正）
- 用户质疑"检查的是否是实际项目的"。补做调用链验证：前端 hexUtils 被 NewBattleView/HexGridCanvasEngine/NewBattlefieldView/BattleMinimap/hexDraw 真实 import；map-service 的 HexUtils 被 battlefields.js 的 findPath/getHexesInRange 真实调用。
- ⚠️ **更正之前文档错误**：距离真理不是"唯一在 map-service"，而是**两份同源副本互不引用**：
  - 副本A `map-service/src/utils/hexUtils.js:85` `HexUtils.hexDistance`（被 A* findPath 启发式用）
  - 副本B `combat-service/.../skillExecutor.cjs:1178` 私有 `_hexDistance`（被战斗结算全部距离校验调用：_checkObjectRange/_calcHealAmount/executeCounter/Supply/Throw/Stable/Snipe/Scout/Duel/_isInSector）
  - 两者 offToAx 公式与 max(dq,dr,ds) 逐字符相同，目前数值一致；但改一忘二会分叉——这是真漂移风险。
- **架构约束（已验证）**：combat-service 与 map-service 是独立 ESM 包，无共享软链；combat-core 是 .cjs，map-service hexUtils 是 ESM；combat-core `grep shared-kernel`=0（不引用 shared-kernel）。**禁止** skillExecutor.cjs 直接 require map-service（CJS require ESM 运行时报错 + 跨服务硬依赖违规）。
- **正确收敛方案**（写入 md 第8章）：在 shared-kernel/src 新增 hexMath.ts(纯函数 hexDistance/getNeighbors/getHexesInRange/getHexKey)→编译 dist；map-service 改 import re-export；combat-core 用动态 import() 引用 dist 并删 _hexDistance；前端保持镜像手写同算法 hexDistance。涉及跨服务依赖+重建，须连同"六角格测距攻克"那波统一部署，勿零散上线。

## 🔥 全栈测距真理焊死 + 部署（Phase 30-HexTruth，2026-08-02 深夜，已上线）
- 用户下令按文档第8章终极收敛方案分三步焊死全栈测距真理。已执行并部署成功。
- **Step1 真相源**：`shared-kernel/src/hexMath.ts` 新增纯函数(hexDistance/getNeighbors/getHexesInRange/getHexKey)，编译 ESM(`dist/hexMath.js`)+CJS(`dist/hexMath.cjs` 双产物)。package.json build 脚本 `tsc -b --force && npm run build:cjs`；exports 增 `./hexMath` 子路径双格式。
- **Step2 删副本**：map-service 初版改 import shared-kernel 删副本A，但**部署发现 comm-service 容器 `npm ci` 无法解析 workspace 私有包 `@mecha/shared-kernel`**（map-service 在 mecha-comm 容器，context 不含根 shared-kernel）→ 回退 map-service 依赖，保留与真相源逐字一致的同源镜像（待 comm 容器化支持后收口）。combat-service 删副本B(`skillExecutor._hexDistance`)+副本C(`aiStrategies.cjs` 本地 hexDistance)，统一经 `hexKey.cjs` require shared-kernel CJS；aiStrategies 用 `hexDistanceCoord`(对象签名)别名避免 NaN 回归。前端 hexUtils.js 补 hexDistance/getHexesInRange 镜像；NewBattleView 射程枚举复用 getHexesInRange。
- **Step3 单测+部署**：新增 `scripts/hex-truth-contract.test.mjs` 跨端契约单测(五端一致✅)。全量 commit `90d12dc`(refactor) + `69f87d9`(fix) 本地未push。rsync+服务器 build+重建 gateway/frontend 已上线(gateway /health=200, frontend 文案"射程/辐射范围"已打包)。
- **部署踩坑(已修，记入 69f87d9)**：①shared-kernel 缺 typescript devDep(alpine `tsc not found`)→补 devDep；②composite+tsbuildinfo 缓存致 tokens.js/types.js 不 emit(容器 `ERR_MODULE_NOT_FOUND tokens.js`)→build 用 `tsc -b --force` + .gitignore 忽略 *.tsbuildinfo；③gateway 容器内不 build shared-kernel，dist 由 rsync 提供(纯JS跨平台)；④combat.ts 隐式any(TS7006) 阻断 tsc→补 `(r:string)` 注解；⑤单测脚本硬编码 macOS 路径→改相对路径。
- **架构结论**：全栈 hex 距离唯一真相源=`shared-kernel/hexMath.ts`；combat-core 经 CJS 真相源、前端镜像、map-service 暂同源镜像。改动 shared-kernel 必须重 build + 重新部署 gateway/frontend（combat 打包进 gateway）。

## 📐 六角格测距真理文档（2026-08-02 晚）
- 用户要求整理现有六角格距离/坐标代码到 md。产出 `mecha-universe-engine/六角格测距-坐标转换真理.md`。
- **关键发现**：前端 `frontend/src/utils/hexUtils.js` **目前没有 `hexDistance` 函数**（只有坐标转换/邻居/方向/联防共线）。唯一现存的 hex 距离实现在后端 `services/map-service/src/utils/hexUtils.js:85` 的 `HexUtils.hexDistance`（Even-R offset→axial 后取 cube 距离 `max(dq,dr,ds)`），并被 `findPath` 的 A* 启发式复用。
- 文档含：坐标系定义、后端 hexDistance 算法拆解、前端坐标转换(pointyTopCenter/pixelToHex)、前后端邻居关系对齐(Even-R 奇偶行分支)、范围枚举 getHexesInRange、方向 computeDirection、联防共线 cube 转换，以及**下一步接入清单**（补前端 hexDistance、锁定 skillExecutor 距离校验同源、aoe 辐射枚举复用 getHexesInRange、交叉验证单测）。
- 待办：下一步攻克六角格测距真理——把 hexDistance 提升为前后端共用，消除射程校验漂移。

## 💡 跨项目可借鉴工程方法论提炼（2026-08-02）
- 用户要求：基于刚整理的项目资产+开发经验+真实架构，提炼"同样开发内容下能借鉴什么"的通用方法论。
- 产出 `桌面/机甲战棋/项目资产/机甲战棋-可借鉴工程方法论.md`：从 12 条弯路升华为 10 条跨项目原则——①先收敛架构再写完整资产；②单一真相源落到代码层(lint强制)；③高风险改造先 spike 且宪法级显式否决旧方案；④废弃即清除+标注;⑤文档与代码同节奏(每里程碑跑API双向对账);⑥技术栈冻结点单一声明+ADR;⑦展示属性与逻辑属性分离(faction/role模式);⑧部署前双校验(df/tsc/大小写);⑨"过期"做价值评估非删除(待确认资产中转区);⑩跨服务事件共享Schema。附 Checklist 速查。根因归结："真相源不唯一+文档代码脱节+废弃残留未清理"。
- 索引目录已补该文件条目。

## 🔧 Phase 31-RangeNormalize：射程字段归一化 + 校验链路统一（2026-08-02，已落地代码未部署）
- 用户给出三步重构方案，针对射程核定三处口径不一致：①字段优先级相反(_getUniversalFields 第145行 max_range优先 vs 校验点第281行 cast_range优先 vs 前端数组 cast_range优先)；②网关/skill跳过校验但反击/决斗走内部路径硬吃 hexDistance；③minR=1兜底破坏自身格导致 heal/supply 写死 dist===0 特判。
- **Step1 字段归一化**：`skillExecutor.cjs _getUniversalFields` 顶层完成归一化，对外暴露 `minRange`/`maxRange` 权威字段(rawMax=cast_range??max_range??range_max??range首个非空，rawMin=min_cast_range??min_range??range_min)。下游 9 处校验点(executeUniversalSkill/heal/counter/supply/sweep/throw/stable/snipe/duel/_isInSector/getSkillRange)全改读归一化字段。保留 cast_range 等镜像字段(标注 deprecated)仅向后兼容外部消费者。
- **Step2 minRange 默认按 category**：support/auto/automation=0(允许自身格)，melee/ranged/special=1(排除自身)。删除 _executeHealSkill(546行)与 executeSupply(935行)的 dist===0 特判。_isInSector 保留 dist<1(扇形不能扫自身)，executeDuel 保留 dist>1(近战必须相邻)——业务硬语义。
- **Step3 isTargetInRange 纯函数三端共用**：`shared-kernel/src/hexMath.ts` 新增 `isTargetInRange(sq,sr,tq,tr,{minRange,maxRange},terrainContext?)` + `isTargetInRangeCoord` + `SkillRangeFields` 接口。combat-core 经 hexKey.cjs re-export，executeUniversalSkill 距离校验改调此函数；前端 hexUtils.js 镜像同函数，NewBattleView 高亮判定从手写 `d<=range&&d>=rangeMin` 改调 isTargetInRange；网关 /skill 与 /attack 保持"信任前端选中"(避免双重拒绝)但注释更新。terrainContext 参数预留扩展点(未实现地形射程修正，height_bonus_per_diff 是伤害加成非射程加成)。
- **前端 getSkillRange/getSkillRangeMin 归一化**：严格镜像后端 _getUniversalFields——rawMax 取 cast_range??max_range??range_max??range，rawMin 取 min_cast_range??min_range??range_min，默认按 category。抽 _parseRangeField 辅助函数处理数字/"1-3"字符串/{min,max}对象。新增 RANGE_DEFAULT_BY_CATEGORY 常量。
- **验收**：shared-kernel build 通过(dist 含 isTargetInRange CJS+ESM 双产物)；skillExecutor.cjs node --check 通过；网关 tsc --noEmit 0 错误；前端 npm run build 成功(新 bundle index-OyT1_vfe.js)；eslint 0 错误；跨端契约单测五端一致✅。
- **文档**：六角格测距-坐标转换真理.md 新增第 11 章记录本次收敛(起因/三步方案/前端归一化/验收/架构图)。
- **✅ 已部署上线（2026-08-02 晚）**：rsync → 服务器前端 `npm run build`(新 bundle `index-OyT1_vfe.js`) → `docker compose build --no-cache mecha-gateway mecha-frontend`(均 Built) → `up -d --no-deps`(gateway /health=200, 两容器 Recreated+Healthy, db 孤儿隔离未碰)。**哈希验收通过**：线上 8081 `index-OyT1_vfe.js` == 服务器 dist 产物一致；gateway 日志无 error/range 异常。Phase 31-RangeNormalize 全量生效。

### 🔴 新 Bug（用户实测 2026-08-02 晚）：所有远程技能射程错
- **现象**：行动栏标注「远程」的技能，高亮范围与卡片文案都塌缩为 1 格。
- **根因（精确）**：前端 `getSkillRange`/`getSkillRangeMin`（NewBattleView 2942/2954 行）只读 `skill.category` 决定默认射程；但前端 `unit.skills` 的「远程」标注挂在 `type:'ranged'`/`typeLabel:'远程'`（badge 在 364 行读 type），`category` 普遍缺失。`skillGroups` 直接透传 `unit.skills`（`battleStateFactory` 203 行未归一化、未补 category）。当技能未显式配 cast_range/range_max 且 category 缺失 → `RANGE_DEFAULT_BY_CATEGORY[undefined] ?? 1` = 近战 1 格。**路径 A 高亮与路径 B 卡片共用同一 `getSkillRange`，故两者同错**（初版报告误判"路径A正确"已自纠）。
- **关键脱节**：后端 `_getUniversalFields` 自己算好 cast_range 并下发归一化字段（163行），但前端 `unit.skills` 没吃到，前端独立重算却只认 category 不认 type。
- **修复方向**：`getSkillRange` 推导 category 时纳入 `type==='ranged'`→'ranged'（新增 `resolveSkillCategory`）；卡片文案(363行)复用 `getSkillRange`；`skillGroups` 补 `category||(type==='ranged'?'ranged':'melee')`。
- **报告**：已写入 `/Users/dingxuyang/Desktop/机甲战棋/技能射程读取与计算逻辑审计报告-2026-08-02.md`（第1/2/4/7节），旧排查报告已移 `过期报告/`。
### ✅ 修复已实施并部署（2026-08-02 晚，紧随诊断后）
- 前端 `NewBattleView.vue`：新增 `resolveSkillCategory(skill)`（兼容 category/type/typeLabel）；`getSkillRange/Min` 改用其推导（保留 `_parseRangeField` 兼容 "1-3" 字符串，普通攻击仍走 `basicAttackRange`，未退化成 `return 1`）；卡片文案(363)复用 `getSkillRange/Min`；`skillGroups`(2805)循环内补 `skill.category = resolveSkillCategory(skill)`。
- **用户决策：远程基础射程 6 → 3**。前端 `RANGE_DEFAULT_BY_CATEGORY.ranged=3` **且同步后端** `skillExecutor.cjs` `DEFAULT_RANGE_BY_CATEGORY.ranged=3`（保持 Phase31 单一真相源，否则前后端分叉）。
- 部署：rsync → 服务器 `npm run build`(新 bundle `index-CTQ29QXz.js`) → `docker compose build --no-cache mecha-gateway mecha-frontend`(均 Built) → `up -d --no-deps`(gateway /health=200, 两容器 Recreated+Started, db 孤儿隔离未碰)。
- **验收**：线上 8081 `index-CTQ29QXz.js` == 服务器 dist 产物哈希一致；bundle 含 `melee:1,ranged:3,auto:0` 常量；gateway 日志无 error/range 异常。
- **用户 Patch 两处修正（避免退化）**：① `if(!skill) return 1` 改回 `basicAttackRange`/0（普通攻击语义）；② 射程解析保留 `_parseRangeField` 而非 `Number()||1`（兼容字符串区间）。
- 改动文件：shared-kernel(src/hexMath.ts+dist)、combat-service(skillExecutor.cjs+hexKey.cjs)、frontend(hexUtils.js+NewBattleView.vue)、backend-gateway(combat.ts 注释)、测距真理文档。

### 🔴 二次 Bug（用户硬刷新后仍 1 格，2026-08-02 深夜）：后端兜底 cast_range:1 劫持前端
- **现象**：前端 `getSkillRange`（已含 `resolveSkillCategory`+`ranged:3`）硬刷新后仍读成 1 格；后台网络看技能 `type:'远程'` 但 `range:1`。前端 bundle `index-gslKOVGV.js` 经实测 grep **确实含 `ranged:3`**（之前 memory 记的 `CTQ29QXz` 哈希有误，真实线上就是 gslKOVGV 且已含修复）。说明前端代码对，问题在**后端下发数据**。
- **🔴 真根因**：前端 `getSkillRange` 第1步循环优先读 `cast_range`。后端两处兜底把**所有**技能无脑写 `cast_range:1`：
  1. `excel-schema-normalizer.ts:mapSkills` → `parseRange(s.range)` 对空 range 返回 `{min:1,max:1}` → `cast_range: max=1`。
  2. `glossary.ts:normalizeSkillForSave` → `cast_range==null` 兜底 `cast_range=1`。
  远程技能若没显式配 range → 下发 `cast_range:1` → 前端读出 1，永远走不到 `RANGE_DEFAULT_BY_CATEGORY.ranged=3` 分支。这就是「type 远程、range 1」的真相。
- **✅ 治本修复（后端，单一真相源）**：
  - `excel-schema-normalizer.ts`：抽出 `DEFAULT_RANGE_BY_CATEGORY={melee:1,ranged:3,auto:0,automation:0,special:1,support:1}` + `DEFAULT_MIN_RANGE_BY_CATEGORY`（ranged/special/melee 最小=1，auto/automation/support=0）+ `resolveCategoryForMap(s)`（兼容 type/typeLabel='远程'/action_type 数组）；`parseRange(raw, category?)` 改为「未配置 range 时按分类给基础射程 max=DEFAULT_RANGE_BY_CATEGORY[cat]、min=DEFAULT_MIN_RANGE_BY_CATEGORY[cat]」，不再无脑 1。`mapSkills` 调 `resolveCategoryForMap(s)` 传分类入 `parseRange`。
  - `glossary.ts`：新增 `GLOSSARY_DEFAULT_RANGE_BY_CATEGORY` + `resolveCategoryForGlossary(skill)` 镜像；`normalizeSkillForSave` 的 `cast_range==null` 兜底改为按分类给默认（远程=3）而非 1。
  - `battleStateFactory.ts:203` 仅透传 `params.skills`，不重兜底 cast_range——故上游修复即覆盖 `unit.skills` 来源。
- **部署**：本地 `tsc --noEmit` 0 错 → rsync（排除 .env 等）→ 服务器 `npm run build` 前端 → `docker compose build --no-cache mecha-gateway mecha-frontend` → `up -d --no-deps`（gateway /health=200，前端 bundle `index-gslKOVGV.js` 仍是修复版，后端容器内 `/app/dist/services/excel-schema-normalizer.js` 含 `resolveCategoryForMap` 已生效）。
- **验收口径**：刷新页面后，后台网络看远程技能 `cast_range` 应变成 3（之前是 1），前端高亮/卡片即显示 3 格。重点：前端 `getSkillRange` 只读 `cast_range/max_range/range_max`，**不读 `range` 字段**，故即便 `range` 字段显示值也以 `cast_range` 为准。
- **教训**：射程 bug 的根因在**数据下发层**（后端兜底常量），不在前端显示逻辑；前端已按类型给默认，但被后端兜底的 `cast_range:1` 抢先读出。凡是「前端逻辑对、实测仍错」，优先抓真实下发 payload 而非反复改前端。

### 🔴 部署乌龙 + 纯逻辑验证（2026-08-02 深夜续）
- **误判纠正**：容器/服务器 dist 搜 `grep 'ranged:3'`（无空格）全返回 0，误以为部署失败。实际代码写的是 `ranged: 3`（带空格），容器内运行 dist **确实含 `ranged: 3` 和 `resolveCategoryForMap`**——部署是成功的！之前"实测仍1格"是因为测的是 13:38 开局的旧局（开局生成 `unit.skills` 缓存旧值 `cast_range:1`，代码改动不影响已存在的局）。
- **用户贴真实 payload**（`/Users/dingxuyang/Desktop/响应内容.md`）：所有技能 `cast_range/max_range/range` 全=1，`startedAt:2026-08-02T13:38:14`（早于部署，旧局证据）。但 payload 能拉到，说明战局数据在、前端能恢复，"刷新都没了"主要是刷新后战斗视图需手动重进（gateway 重建清空过内存战局）。
- **纯逻辑权威验证**（node 跑抽取版 parseRange）：光束肩炮(远程)→cast_range=3 ✅；镰刀(近战)→1 ✅；mega炮(远程,range"2~5")→max=5,min=2 ✅。
- **发现新缺口**：中文 `type:'自动化'` 未被映射到 `auto`（解析成 melee→cast_range:1）。补映射：`resolveCategoryForMap` + `resolveCategoryForGlossary` 加 `type==='自动化'||typeLabel==='自动化'→auto`。已改代码但未部署。
- **⚠️ 容器 build 失败坑**：`docker compose build --no-cache mecha-gateway` 在 tsc 阶段报 `glossary.ts` 找不到 `../middleware/auth.js`(TS2307)——实际源文件是 `.ts`，本地 `npx tsc --noEmit` 干净(exit 0)，alpine+`file:../shared-kernel` 链接下 moduleResolution 行为差异所致。Dockerfile 注释自称"dist 由 rsync 同步，不在容器内 tsc 编译"，但 build 命令仍含 `npx tsc` 致 --no-cache 时必失败。当前线上跑的是上次成功部署的旧容器(含 ranged:3)，gateway=200，**本次 build 失败未影响线上**。
- **决策**：不强行修 build（风险：重建容器清空内存战局，且可能覆盖好的旧容器）。自动化映射留待下次有后端改动时一并修 build 部署。当前线上远程=3 已生效，请用户**重开一局新战斗**验证（旧局 cast_range 不随代码变）。

### 🔴 部署乌龙复盘 + 真正修复（2026-08-02 深夜，用户要求"先解决再反查"）
- **用户发现 lighthouse SSH 断链**：之前若干轮 rsync/ssh 命令实际因 SSH 断链未执行/返回旧容器缓存结果，我却基于 `docker exec grep` 旧 dist 误判"已部署"。**教训：SSH 断链时命令不报错即成功是错觉，必须先确认连通再下结论；验证不能只 grep 文件，要端到端调函数**。
- **真实根因（容器 build 失败）**：`backend-gateway/Dockerfile` builder 阶段 `npx tsc` 在 alpine 下报 TS2307（找不到 `../utils/logger.js` 等 `.js` 扩展名本地 import）——mac 本地 tsc 干净（版本/缓存差异），alpine tsc 对 `moduleResolution:bundler` + `.js`→`.ts` 解析更严格。Dockerfile 注释自称"dist 由 rsync 同步"，但 build 仍 `npx tsc`，矛盾。这是**预存环境问题，挡住所有 backend-gateway 部署**；之前"成功部署"是用缓存层（`--no-cache` 才暴露）。
- **✅ 真部署路径（绕过容器内 tsc）**：本地 `npx tsc` emit dist（mac 干净）→ rsync `backend-gateway/dist/` 进服务器 → `docker cp dist/. mecha-gateway:/app/dist/` → `docker restart mecha-gateway`（health=200）。**端到端真实验证**（容器内 `node` 实际调 `mapSkills`/`resolveCategoryForMap`/`parseRange`）：远程无range→cast_range=3 ✅；近战→1 ✅；自动化→0 ✅（新增中文"自动化"→auto 映射）；远程range"2~5"→max=5,min=2 ✅。
- **✅ Dockerfile 根因修复**：builder 阶段删除 `npx tsc`（L25），运行阶段 `COPY backend-gateway/dist ./dist`（改自 `--from=builder`）；注释与行为统一"dist 由 rsync 提供"。`docker compose build mecha-gateway` 现在 BUILD_EXIT=0 成功。注意：**rsync 默认 `--exclude dist`，故 `docker compose build` 的 build context 无 dist 会失败——必须单独 rsync dist 进服务器项目目录**（或改 rsync 不排除 dist）才能走 `docker compose build` 路径。当前线上容器已用修复后镜像 Recreated 并验证通过。
- **现状（2026-08-02 末）**：gateway 正式镜像部署、health=200、dist 含 ranged:3 + 自动化映射；前端 bundle `index-gslKOVGV.js` 含 ranged:3。用户重开一局即可见远程=3 格。验证脚本 `verify_range.mjs` 已清理，normalizer.ts 末尾保留了 `export {mapSkills,parseRange,resolveCategoryForMap}` 调试导出（无害）。

### 📄 完整复盘报告（用户要求"从状况到修复完整做成报告"）
- 已生成：`历史开发报告/技能射程显示错误BUG排查与修复报告.md`。结构：①问题上报与初步排查（用户反馈链路 + payload 证据）→ ②根因定位（排除前端、锁定后端 normalizer.ts + glossary.ts 两处兜底 cast_range:1）→ ③修复内容（3 文件代码 diff）→ ④部署过程与乌龙复盘（alpine tsc 失败改用本地 tsc+docker cp、SSH 断链误判教训）→ ⑤验证结果表 → ⑥遗留操作建议（重开新局）。

### 🧹 工作记忆规则审计（用户要求"整理规则，更新/废弃"，2026-08-02 末）
逐条核对 MEMORY.md 与代码现态，结论：
- ✅ **射程真相源（Phase31-RangeNormalize）**：完全正确，shared-kernel `getSkillRangeFields`/`resolveSkillCategory`/两个默认表/前端 hexUtils.js 镜像均已验证存在且一致。保留。
- ✅ **unitSize 七视图尺寸工坊**：前端 `unitSize.js` 与后端 `unitSize.ts` 的 `sizeSevenBox`/`sizeHitFactor` 一致。保留。
- ✅ **方向语义真相源**：`computeDirection` 6 扇区中心角 + `arrowRotate=朝向角+90`（90/150/210/270/330/30）与代码 NewUnitEditorView 一致。保留。
- ✅ **faction×role 双语义 / 房间系统 / 登录态安全 / 部署纪律**：无代码冲突，保留。
- 🔧 **【已修正】等距基准 CTM 公式写反**：原 MEMORY 记 `screenY=oy+s*(shearY*flatX+scaleY*flatY)` 且暗示 `ctx.transform(scaleX,shearY,shearX,scaleY)`——与 hexDraw.js 实际 `screenY=oy+s*(iso.scaleY*flatX+iso.shearY*flatY)` 相反（scaleY 乘 flatX、shearY 乘 flatY）。已在 MEMORY 更正为唯一正确矩阵 `[scaleX, shearX; shearY, scaleY]`。ISO 参数值 0.38/0/1/0.39/-24 正确。
- ⚠️ **【待废弃/过时】开发区宪法 v2.0 第2条引用 `HEX_WIDTH=64, HEX_HEIGHT=72`**：仍正确（hexUtils.js 仍导出此常量），但宪法未提及等距已放弃 `ctx.transform` 改逐顶点变换——属措辞过时，不影响红线，标记"本模块已改逐顶点变换"。
- ⚠️ **【待核查】射程"旧局残留"陷阱3**：代码已治本，但旧战局 JSON 残留 cast_range:1 需重开新局。该条是**用户操作约束**而非代码缺陷，保留但标注"仅影响历史局，新局已无此问题"。
- 🗑️ **无实质废弃项**：所有规则仍适用，无整段可删除；仅等距公式一处错误已修，其余为措辞/时效标注。

### 🔧 射程读取三公式归拢为一条（2026-08-02 末，代码已落地未部署）
- **用户洞察**：机甲/棋子设计里根本没有"普通攻击"这种选项，所有攻击都应通过技能/武器条目表达。原系统"普通攻击"岔路（前端 `basicAttackRange`/`isBasicRanged` + 网关 `BASIC_RANGED_RANGE` 常量 + `resolveSelectedSkill` 读 `currentStats?.range`）是历史外溢，读的是机身派生 stat `unit.range=1+floor(shooting/25)`，与技能表脱钩，导致"射击值=24 的远程机甲被误判近战"。
- **三方改造已实施**（仅代码，未部署）：
  1. **前端 NewBattleView.vue**：删 `BASIC_MELEE_RANGE/BASIC_RANGED_RANGE/isBasicRanged/basicAttackRange`；新增 `getBasicAttackSkill(unit)` 按 `unit.type==='ranged'||unit.isRanged||typeLabel 命中远程正则` 返回隐式技能 `{id:'basic_attack',category:'ranged'|'melee'}`；所有原 `basicAttackRange(selectedUnit)` 调用点（L249 射程显示、L333 普通攻击按钮）改为 `getSkillRangeFields(getBasicAttackSkill(selectedUnit)).maxRange`；`getSkillRange(null)`/`getSkillRangeMin(null)` 兜底改喂隐式技能；`performPlainAttack` 的 `attackType` 由 `getBasicAttackSkill(attacker).category` 推导。
  2. **网关 combat.ts**：删 `BASIC_*` 常量，新增后端版 `getBasicAttackSkill`；`import {getSkillRangeFields,resolveSkillCategory} from '@mecha/shared-kernel'`；`/attack` 三处内联 `inlineDef`（buildSkillDef skill 分支/else 分支/约L2080 基础攻击）的 `cast_range/max_range/min_cast_range/min_range` 全由 `getSkillRangeFields(...)` 派生；`resolveSelectedSkill` null 分支改用 `getBasicAttackSkill(cUnit).category`，射程校验 `range=getSkillRangeFields(owned).maxRange`，不再读 `currentStats?.range`/写死 BASIC_*。
  3. **数据层**：`excel-schema-normalizer.ts` 的 `range=1+floor(shooting/25)` 保留字段但三处注释声明**已废除射程判定用途**（仅作远程/近战分类展示信号）；`shared-kernel/types.ts` 的 `UnitStats.range`/`BattleUnit.range` 同步加"已废除射程判定用途"注释。保留字段避免大范围类型重构破坏编译。
- **前端 hexUtils.js 镜像确认**：`getSkillRangeFields` 已与 shared-kernel 逐字一致，天然支持 `{category}` 隐式技能对象（无显式字段按 category 兜底 ranged=3/melee=1），无需改动。
- **审计报告**：`桌面/机甲战棋/技能射程读取与计算逻辑审计报告-2026-08-02.md` 已追加第 8 章"实施记录"，终态收敛为「一条公式 getSkillRangeFields + 一步标红」。
- **⚠️ 部署约束（与历史相同）：** 改 shared-kernel 类型注释不影响运行时；但 combat.ts 改了需重新 build gateway（dist 由 rsync 提供，走本地 tsc+docker cp 或 compose build 路径，注意 alpine tsc 已修）；前端须在服务器 build。旧战局 JSON 可能残旧射程缓存，**必须重开新局**验证。
