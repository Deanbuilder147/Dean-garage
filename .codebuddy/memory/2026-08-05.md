# 2026-08-05 工作日志

## 战场技能报告收尾：空壳清理 + 全员读取复检（2026-08-05 晚）
- 用户要求"清理掉"空壳单位并用自然语言解释事件。已对线上 gateway 真实 DB（`mecha-universe-engine_gateway_data` 卷）软下架 3 个公开空壳：`主宰公开测试`(dean147)、`裁判公开测试`(ph)、`AdminFinal`(种子 admin)，口径 `UPDATE units SET is_public=0 WHERE is_public=1 AND (skills='[]' OR skills IS NULL)`。未硬删。
- 复检结论：清理后公开单位 6 个全部带技能、空壳=0；`GET /api/units/public` 走 `is_public=1 AND review_status='approved'`，所有人（含游客）进战场均读到完整技能；`passthroughSkills`(rooms.ts) 对所有身份统一聚合+去重+剔绝对射程。最终"所有人进去都能正确读取棋子"。
- 报告《战场技能丢失问题诊断报告-2026-08-05.md》追加第十三章记录清理与复检。
- 注意：宿主机 `/data/mecha-universe.db` 是 0 字节空壳挂载点；真正生效的库在 docker volume `mecha-universe-engine_gateway_data/_data/mecha-universe.db`（gateway 容器 DB_PATH=/data/mecha-universe.db 指向此卷）。sql.js 脚本须放项目目录内运行（/tmp 解析不到 node_modules）。

## 地图审核台样式对齐单位审核台（2026-08-05 晚）
- 需求：地图审核台(MapReviewView.vue)做成与单位审核台(UnitReviewView.vue)一样的"概览表格+卡片网格"混合布局。
- 改动：MapReviewView 新增 `.review-table`（投稿者/名称/格数/出生点/尺寸/地形种类/提交时间/操作）+ `.review-grid`（复用 `.review-card/.review-main/.faction/.review-actions/.pub-toggle` 琥珀配色），原 `.review-list` 单列布局废弃；新增 `terrainTypeCount` 辅助函数。模板里卡片"地形种类"的 `v-if="m.terrainTypeCount != null"` 冗余（属性不存在），已改为直接渲染。
- 部署：rsync→服务器 `npm run build`→`docker compose build --no-cache mecha-frontend`→`up -d --no-deps`。新产物 MapReviewView-CCjSqq-Z.js / MapReviewView-CwDyYT8S.css，容器内 css 已确认含 `review-table`。

## 词条「扫射」效果 + 新规则战场生效验证（2026-08-05 晚）
- 扫射词条定义在 `services/combat-service/src/config/glossary-skill-config.json`(key `sweep`)：category=ranged, bonus_range=-1, min_range=1；effects=[{type:damage,flat_value:-2,armor_pen:0}]；engine_meta.mode=`deterministic_sweep`(扇形2格 deterministic_sweep), sector_angle=60, base_damage=-2, damage_kind=kinetic, aoe 扇形均摊；target=SINGLE_ENEMY/enemy。
- **新规则射程(加法模型)**：maxRange=DEFAULT_RANGE_BY_CATEGORY.ranged(3)+bonus_range(-1)=**2格**；minRange=min_range(1)。（shared-kernel getSkillRangeFields 证实只读 bonus_range，彻底不读 cast_range/range/max_range/range_max/rangeLabel）
- **passthroughSkills 生效验证**：源码+模拟聚合「扫射」确认 cast_range/range/max_range/range_max/rangeLabel 全部剔除，保留 bonus_range/min_range/split_damage。新规则已成功生效到战场。
- 注意：线上 units.skills 仍是旧快照（含 cast_range 脏字段，如 bEXM-21 的"mega扩散炮击"内嵌 effect=扫射），但**开战由 passthroughSkills 重新聚合**会剔除脏字段，单位列快照不等于战场表现。线上无 name 严格="扫射" 的单位技能（扫射仅作 description/effect 内嵌于其他技能）。

## 扫射更新没成功的诊断（2026-08-05 晚，仅诊断未改）
- 用户用 dominator 账号(dean147, role='dominator', permission=999)在前端词条库中枢改了扫射，但磁盘仍是旧规则(damage -2、无 status、min_range=1)。用户质疑权限逻辑。
- **权限逻辑对 dominator 完全放行，不是 403 根因**：`PUT /hub-config/:key` 鉴权=`isAdminOrAbove(role) || resolveRoleFeatures(role).includes('glossary.edit')`；`isAdminOrAbove`=`role===REFEREE||role===DOMINATOR`；`UserRole.DOMINATOR==='dominator'`（shared-kernel/src/tokens.ts:229），登录 JWT role 即 'dominator'。双重保险：`requireFeature` 中间件 dominator 是"硬编码永远放行"逃生通道。之前测到的 403 是用 user 角色 deantest 测的，与 dominator 无关。
- **存储A 写入与引擎读取是同一份文件、无缓存**：前端 GlossayHub→`PUT /hub-config/:key`→backend-gateway glossary.js→`configLoader.cjs`(CONFIG_PATH=`services/combat-service/src/config/glossary-skill-config.json`)→写盘；全服仅此一份；skillExecutor 经同一 configLoader 读；`getGlossaryConfig()` 每次 readFileSync 无内存缓存→落盘即战场可见。
- **真正没落盘的原因**：磁盘当前仍是旧值，证明 dominator 的保存请求从未写入存储A。前端保存仅在"完成编辑"（再点一次绘制模式按钮触发 toggleEdit→saveCurrent→PUT）时发出；失败会 alert('保存失败')。可能：(A)改完没点"完成编辑"改动只在本地草稿未发出；(B)发出但失败(登录态过期/401)被忽略。另一隐患：用户描述"射程加成 -1（最小4）"的"最小4"在前端 draft 无对应字段绑定(buildRangeFromSkill 只算 maxRange=3-1=2)，即使保存成功引擎也读不到"最小4"，会让人误以为没更新。
- 诊断结论已告知用户，未做任何修改。

## 核查：8-04 夜间/8-05 凌晨三份报告问题修复状态（已全部修复+部署验证）
- 报告：执行检查报告-词条编辑器迭代实施 / 词条编辑器-保存失败修复报告 / 词条编辑器-数值字段与系统联动核查报告（均 2026-08-04）。
- 第四章问题清单 + 第五章待办全部已落地并部署：
  - #1 效果级作用对象、#2 target.type 映射、#3 AOE→aoe_radius：已修复。
  - #4 地图炮六向命中判定：NewBattleView.vue L2152-2191 `mapCannonHitHexes` + GlossaryHub chunk 含 `map_cannon`，已部署（线上 bundle 验证）。
  - #5 单位技能强引用下拉：SkillsEditor.vue `onGlossaryBind`+`skill-bind-select`+`backfillSkillKeys`，线上 bundle 含 `skill-bind-select`，已部署。
  - #6 fixed_rate_multiplier 死字段：skillExecutor.cjs L363-373 已接入为真实倍率（T2 落地）。
  - T4 权限矩阵鉴权：featurePermissions.ts + requireFeature 中间件 + referee 写权放开（glossary/units/maps/rooms/bugReport），已实施。
- **验证坑**：Vite 生产 build 会 minify 混淆局部函数名，grep `mapCannonHitHexes`/`onGlossaryBind` 搜不到属正常；应 grep 稳定字符串（如 `skill-bind-select`、`map_cannon`、`glossary.edit`）验证部署。本次 frontend dist 哈希 index-D54WQDUb.js 未变，因 src 自 8-04 22xx 写入后无新改动，03:21 build 已含。
- 唯一"非必须"项：第五章#2 效果级逐 effect 独立命中判定增强建议（报告标"若需"），非 bug，未做。

## 账号权限管理框新增「全部账号列表」
- 需求：等级·功能权限矩阵的「账号权限管理」框显示所有账号列表（原仅有搜索单用户）。
- 后端 `backend-gateway/src/routes/admin.ts` 新增 `GET /api/admin/list-users`（requireRole DOMINATOR），查 `users` 表全部 8 字段降序返回 `{users:[...]}`。
- 前端 `client.js` 加 `adminAPI.listUsers()`；`AdminPermissionsView.vue` 在搜索框下方加「全部账号（N）」列表区（点击选中编辑、按 role 着色、刷新按钮），`onMounted` 仅 dominator 时 `loadAllUsers()`。
- 部署：rsync → 服务器 `backend-gateway npm run build`(tsc 出 dist) + `frontend npm run build` → `docker compose build --no-cache mecha-gateway mecha-frontend` + `up -d --no-deps`。gateway Healthy，前端 bundle 含「全部账号」文案，接口 403 权限验证通过（deantest 非 dominator 被拦），SQL 返回 28 行字段匹配。
- 注意：`users` 表列名是 `password_hash`（非 `password`）；库在 `mecha-gateway` 容器内 `/data/mecha-universe.db`，宿主机 `/data/` 那个 0 字节是未挂载空壳。

## Bug 问题收集表（Phase 33）
- 需求：轻量、玩家用自然语言填写的 bug 收集，管理端方便汇总。
- 后端 `routes/bugReport.ts`：
  - `POST /api/bug-report` 免登录提交（**关键坑**：不能用 `authenticate` 中间件，无 token 会 401；改为 `tryAuth(req)` 可选解 jwt，游客 reporter_name='匿名玩家'）。字段白名单：module∈战斗/棋盘地图/棋子七视图/账号权限/房间联机/UI显示/其他；severity∈崩溃/严重/一般/建议；status 默认待处理。自动记 env(UA/referer)。
  - `GET /api/bug-report/list` + `PUT /api/bug-report/:id` + `GET /api/bug-report/export`(CSV 带 BOM) 均 `requireRole(DOMINATOR)`。列表返回 reports + summary(按状态计数/模块/严重程度/状态枚举)。
  - 建表 `bug_reports`（id/reporter_id/reporter_name/contact/module/severity/status/title/description/env/created_at/updated_at）在 `db/sqlite.ts` createTables 内，索引 idx_bug_status/created。
  - 写完必须 `persistChanges()` 才会落盘（sql.js 不自动保存）。
- 前端：`BugReportView.vue`(玩家页, 侧边栏「问题反馈」全员可见, 路由 /bug-report 无 requiresAuth) + `BugReportAdminView.vue`(汇总页, 侧边栏后台组「Bug 汇总」, 路由 /bug-report-admin 限 dominator, 支持状态/模块/严重程度/关键词筛选 + 改状态 + 导出CSV) + client.js 加 `bugReportAPI` + main.js 注册两路由 + TheSidebar 两入口。
- 部署：rsync→gateway tsc build→frontend build→`docker compose build --no-cache mecha-gateway mecha-frontend`+up。验收：免登录提交返回 success 且落库；bundle 含三处文案。
- 坑：初版用 `authenticate` 致免登录 401，改 tryAuth 后正常。
- **Bug汇总页进不去的根因**：`BugReportAdminView.vue` 初版调 `authAPI.me()`（client.js 无此方法→报错）后改 `authAPI.getProfile()`（指向 PUT /api/auth/profile，GET 调失败）→ `isDominator` 永远 false。真实当前用户接口是 **`GET /api/auth/me`**（auth.ts 路由 `router.get('/api/auth/me')`，非 /auth/profile）。已在 client.js 补 `me: () => apiClient.get('/auth/me')`，Bug页改用 `authAPI.me()`，并保留 `userStore.user.role` 兜底。注意 client.js 的 `getProfile`/`updateProfile` 指向 `/auth/profile` 实为 PUT 修改接口，GET 拉当前用户须用 `me`。
- **账号角色现状**：dominator 仅 dean147（role=dominator, perm=999）。PH/李子 role=admin perm=999，deantest role=user perm=3。**dean147 密码非 'dean147'**（login 实测 AUTH_CREDENTIALS_INVALID），用脚本无法登录，需用户用真实密码。前端 store 字段是 `stores/user.js`（非 userStore.js）。

## 权限体系语义调整（2026-08-05 下午）
- 用户明确：主宰=dominator（dean147 独有，拥有所有权限管理）；管理员=referee（李子和 PH 两人，人选与权限由 dominator 在「权限与用户」中管理）。原 admin 角色降级为「管理员(旧版)」遗留兼容，不再作为管理员语义。
- DB 迁移（容器内 sql.js 改 + export 落盘）：`PH`/`李子` role `admin`→`referee`；`testadmin`(admin)→`user`。dean147 保持 dominator。现状：dominator=dean147；referee=PH、李子；无残留 admin。
- 后端改动（admin.ts/auth.ts/rooms.ts/maps.ts/units.ts/glossary.ts/bugReport.ts）：
  - `DEFAULT_FEATURE_PERMISSIONS.referee = [...FEATURE_KEYS]`（全功能）；`MANAGED_ROLES=[USER, REFEREE]`（admin 不再受管）。
  - `requireFeature` 中间件放行条件加 `role===REFEREE`（原仅 ADMIN）。
  - 各资源写权判定 `role===ADMIN||DOMINATOR` → `role===REFEREE||DOMINATOR`（rooms/maps/units/glossary）。
  - `bugReport` 三个管理接口 `requireRole(DOMINATOR)` → `requireRole(REFEREE, DOMINATOR)`。
  - `ROLE_LABELS`：referee='管理员'、admin='管理员(旧版)'、dominator='主宰'。
- 前端（TheSidebar/AdminPermissionsView/BugReportAdminView）：后台管理分组可见条件 `['admin','dominator']`→`['referee','dominator']`；roleLabel referee='管理员'；Bug 汇总页 checkRole 放开放行 `referee||dominator`。
- 部署：rsync→gateway `npm run build`(tsc 干净)→frontend `npm run build`→`docker compose build --no-cache mecha-gateway mecha-frontend`+`up -d --no-deps`。gateway Healthy，DB 新角色已加载（重启容器重读 DB）。
- 备注：updateUser 端点(PUT /api/admin/users/:userId) 接受任意合法 UserRole，dominator 可把人选设为 referee；前端口角色下拉需含 referee 选项（AdminPermissionsView 角色选择是否已含 referee 待下次交互确认）。

## 棋子公开功能（问题 #3）部署收尾与坑修复（2026-08-05 晚）
- 三接口实现已就位：GET /api/units/public（游客 200）、GET /api/units/my-submissions（带 token 200）、GET /api/units/review-queue（requireRole referee/dominator，无 token 401 / user 403）、POST /api/units/:id/review、POST /api/units/:id/clone。前端三页 UnitLibraryView/MyUnitsView/UnitReviewView 已 build 部署（dist 含 chunk，8081 入口 200）。
- **关键坑1（白名单致 500）**：原 `PUBLIC_UNIT_RE = /^\/(public|my-submissions|review-queue)\/?$/` 把 my-submissions/review-queue 也豁免了全局 `authenticate`，导致 `req.auth` 未挂载，路由内 `req.auth!.userId` 抛 `Cannot read properties of undefined (reading 'userId')` → 500。修复：白名单只留 `/public`；my-submissions/review-queue 必须走全局 authenticate 挂载 req.auth，再由其自身 requireAuth/requireRole 判定放行。
- **关键坑2（docker compose cp 失效）**：`docker compose cp backend-gateway/dist mecha-gateway:/app/dist` 不会真正覆盖容器内 /app/dist（疑似 compose 包装把 dist 当子目录或跳过），容器内文件 mtime 仍是旧版、grep PUBLIC_UNIT_RE=0。有效命令是 **`docker cp /root/mecha-universe-engine/backend-gateway/dist/. mecha-gateway:/app/dist/`**（宿主机绝对路径）+ `docker restart mecha-gateway`。改 backend-gateway/src/*.ts 后流程：本地/服务器 `npm run build`(tsc) → rsync dist 到服务器 → `docker cp dist/. 容器:/app/dist/` → `docker restart 容器`。
- 验收：public=200；my-submissions(deantest token)=200 返回该用户投稿；review-queue 无 token=401、user token=403；clone 未公开单位=404；容器 dist 与宿主机 dist md5 一致；gateway/frontend 均 200。

## 账号等级体系恢复（2026-08-05 晚，用户纠偏）
- **用户原始意图（确认）**：五级严格等级 `dominator > admin > referee > user > guest`，依次递减。来源 `shared-kernel/src/tokens.ts` UserRole 枚举（L215-230）+ `docs/游戏功能需求纪要.md` 第二章。原始语义：guest=游客免Token柔性放行；user=普通用户(基础对战/私人格纳库)；referee=裁判(创建房间/主持对战/上帝视角裁决)；admin=管理员(后台/赠积分/单位地图审核)；dominator=主宰(最高统帅,封禁/撤设管理员)。
- **下午误改（已回退）**：我曾把 `referee` 标成「管理员」、`admin` 降级为「管理员(旧版)」并踢出 MANAGED_ROLES，破坏原体系。本次已恢复：`admin.ts` 的 `MANAGED_ROLES=[DOMINATOR,ADMIN,REFEREE,USER]`、`ROLE_LABELS` 恢复 referee=裁判/admin=管理员/guest=游客；`DEFAULT_FEATURE_PERMISSIONS` 重设五级默认（referee 不含 credits.gift/admin.panel，admin 全功能）。前端 `AdminPermissionsView.vue` roleLabelMap、`TheSidebar.vue` map 与后台入口可见性(`['admin','referee','dominator']`)、`BugReportAdminView.vue` 访问判定均已同步恢复。
- **dominator 权限保护**：`PUT /api/admin/permissions` 对 dominator 强制恒为全开（拒绝收窄，防锁死后台）。其余等级可逐一勾选修改。
- **DB 脏数据清理**：`feature_permissions` 表原有旧 referee 不完整权限（缺 unit.publish/map.publish/glossary.*/credits.gift），已 DELETE 清空使回退代码 DEFAULT。验证：dominator token 调 GET/PUT /api/admin/permissions 正常，PUT 改动经 clearFeatureCache+persistChanges 落盘到 /data/mecha-universe.db（已验证）。
- **DB 现状账号角色**：dominator=dean147；referee=PH、李子；其余均为 user。**无 admin 账号**（按用户意图李子和PH作为referee，admin 暂空）。可用 `PUT /api/admin/set-role`(dominator) 把人选设为 admin。
- 验证手法（仅供排障）：JWT_SECRET=mecha-universe-jwt-secret-change-in-production（弱密钥待换）；容器内 `docker cp` 临时 .mjs 脚本用 sql.js 读 /data/mecha-universe.db；签 dominator token 用 `jwt.sign({userId:'dean147',username:'dean147',role:'dominator'},SECRET,{expiresIn:'1h'})`。

## 权限矩阵门禁补全（2026-08-05 晚，方案 A）
- **背景**：全 17 个功能权限里仅 `glossary.excel` 真正被 requireFeature 消费；`unit.publish`/`map.publish` 此前无任何门禁（形同虚设），`campaign.create` 连对应接口都不存在（保留占位）。
- **本次实施（用户确认）**：`unit.publish`/`map.publish` 接入真实门禁——在单位/地图的**创建与更新端点**，当请求 `is_public=true`（即提交公开申请，computeVisibility 置 pending）时，调用 `resolveRoleFeatures(role).includes('unit.publish'|'map.publish')` 校验；无权限 → 403 ROLE_FORBIDDEN 且不写库（不静默降级为私有）。dominator 恒通过（DEFAULT 全开 + escape 通道）。
- 正确 ErrorCode 成员名是 `ErrorCode.ROLE_FORBIDDEN`（非 FORBIDDEN，tsc 报错修正）。
- **验证**：user(deantest) 提交公开单位=403、私有=201；dominator/referee 提交公开=201(approved)。gateway 已 docker cp dist + restart 部署。
- `campaign.create` 按用户要求保留占位，未接接口。

## 单位编辑器申请公开 + 审核台查看详情（2026-08-05 晚）
- **需求1 编辑器加「申请公开」**：`NewUnitEditorView.vue` 的 `createEmptyForm` 加 `is_public:false`；UI 基础信息区加勾选项「提交后申请发布到公开库」，仅当 `canPublish = user.features.includes('unit.publish')` 时显示可勾选，否则显示提示文案（无权限）。`buildUnitPayload` 用 `...f` 展开已自动带 `is_public` 进 payload，后端门禁二次校验。
- **需求2 审核台查看详情含七视图**：`UnitReviewView.vue` 重写——每行加「查看详情」按钮 → modal 弹窗渲染：七视图（view_urls 0-6，方向标签 正面/右前/右后/背面/左后/左前/等距）、数值 stats、技能 skills、部件 attributes.parts；审核通过时附「审核通过即公开」勾选（默认勾，调 reviewUnit(id,'approve',isPublic)）。
- **/me 返回 features**：`auth.ts` 两处 /me 与 getProfile 加 `features: resolveRoleFeatures(role)`（导入 resolveRoleFeatures）；`shared-kernel/src/types.ts` 的 `UserProfile` 加 `features?: string[]`（并修正注释"四级"→"五级"）。main.js 登录后 userStore.user 已含 features，编辑器可直接用。
- **部署**：shared-kernel 改动须先 `npm run build` 出 dist（ESM+CJS 双产物）→ rsync 三目录 dist → `docker cp /app/dist`(gateway) + `/app/shared-kernel/dist`(软链指向容器 /app/shared-kernel) + `/usr/share/nginx/html`(frontend) → restart gateway+frontend。验证：/me 返回 features(17项含 unit.publish)；referee 提交公开单位→201 pending；dominator review-queue 返回含 view_urls/stats/skills 完整字段。
- 注意：DB 现有 14 条 pending 单位多为历史无 view_urls 的测试数据，弹窗显示"无图"占位，符合预期。

## 删除权限矩阵「发布到公开库」两列（2026-08-05 晚）
- 用户确认 `unit.publish`/`map.publish` 实际无意义，删除矩阵列。改动：admin.ts 的 FEATURE_CATALOG 删两行 + DEFAULT_FEATURE_PERMISSIONS.referee 移除孤儿 key；units.ts/maps.ts 创建与更新端点移除 403 门禁（computeVisibility 不再依赖）；NewUnitEditorView.vue 移除 canPublish 条件分支，「申请公开」对所有可创建者开放（由审核流把关）。前端矩阵 UI 后端动态渲染，删列自动生效。
- **room.create / room.host 也是死 key（发现）**：建房接口（rooms.ts L173）只查 `isGmRole(req.auth.role)` 不查 `room.create`/`room.host`；房内动作查 `host_id` 字段不查 `room.host`。room.host 在任何代码路径都没被读取——纯幻影权限。已写入桌面方案文档但未动代码（待用户决策是否清理）。

## 权限双轨 + 战局快照加固（2026-08-05 晚，组合拳）
- **背景推演**：用户指出 5 个场景——①GM 强制开战死代码（POST /start 外层 host_id!==userId 先 403，isGmRole 跳过全员准备成死代码）；②中途后台改账号 role→canIssueCommand 实时读 token role→全局越权作弊；③房主移交致出战蒸发（房主强制 referee 不下场）；④admin 权限错觉（isGmRole 不含 admin）；⑤room.host 语义重叠。三补丁组合拳修复。
- **Patch A（rooms.ts POST /start）**：外层校验由 `host_id!==userId→403` 放宽为 `isHost || isGmRole(req.auth.role)`，GM 可强制开战处置挂机。名册锁定 L555 已 `roster_locked=1`（所有开战路径含 GM 强开都自动锁，满足"强开战自动锁名册"）。L443 的 `if(!isGmRole)` 全员准备约束保留（非 GM 仍受限）。
- **Patch B（rooms.ts PUT /players）**：移除"房主强制 identity_role='referee'"硬编码（L690-693 改为仅当 identityRole 非 visitor 且非 player 时强制 referee）。房主可显式选 player 下场 1v1；buildJoinedUnits/聚合排除条件 `role!='referee'`（room_players.role 列）天然兼容 player 房主正常出战。
- **Patch C（combat.ts 快照加固）**：①`seedRoomBattle` 新增 `players` 参数（rooms.ts 开战段 JOIN users 取 `users.role`+`identity_role` 传入），固化 `battle.state_snapshot.roles[uid]={role,identity_role}` + hostId；②`battleStore.set` 在 seed 末尾重新触发 → 经 PushBattleStore.set 自动 saveBattleSnapshot 落库（battles.state_snapshot 列已存在，重启 rehydrate 经 requireBattle 还原，**快照未落库问题天然已解**）；③`canIssueCommand` GM 越权改读 `state_snapshot.roles[uid].role`（缺失回退 token role 兼容旧战局），房主代打加门控 `hostIdentity!=='player'`（房主下场则失去代打权，防 1v1 合法开挂）；④`resolveSurpriseChoice` 的 isGM 改读快照 `state_snapshot.roles[userId].role`（修复方案原只改 canIssueCommand 漏掉奇袭代投的后门）。
- **数据结构修正**（方案原假设有误，已修正）：battle 上无 `players` 字段（只有 room.players 接口结构），房主下场判定改读 `state_snapshot.roles[uid].identity_role`；room_players 无 `user_role` 列，账号 role 在 users.role，快照需 JOIN users 取。
- **状态**：本地 tsc --noEmit 通过（exit 0），无 lint 错误。**尚未部署服务器**（待用户确认）。部署流程：gateway `npm run build`(tsc)→rsync dist→`docker cp dist/. mecha-gateway:/app/dist/`+restart。
- 验证要点（部署后）：GM 进玩家房可强制开战；玩家 A 战中改 role 为 referee 不再获得越权（快照锁定）；房主选 player 下场 1v1 无法代打对方；选 referee 不下场保留代打。

## 单位/地图「申请公开」显式按钮 + 地图审核链路从零补齐（2026-08-05 深夜，已部署）
- **用户决策（关键）**：①「申请公开」审核者 = **admin + dominator**（不含 referee）。"这点可由 dominator 在后台修改"=保留 `resolveRoleFeatures(role).includes('units.edit'/'maps.edit')` 兼容（dominator 给谁勾该 feature 谁也能跳过审核）。②单位编辑器现有 checkbox 改为**显式「申请公开」按钮**；地图编辑器**从零加前置「地图选择」界面 + 「申请公开」按钮**。
- **单位（链路原有，本次仅改审核者+按钮形态）**：`computeVisibility` 的 `isAdminOrAbove` 由 `REFEREE||DOMINATOR||units.edit` 改为 `ADMIN||DOMINATOR||units.edit`；`/api/units/review-queue` 与 `/api/units/:id/review` 的 `requireRole(UserRole.REFEREE, UserRole.DOMINATOR)`→`requireRole(UserRole.ADMIN, UserRole.DOMINATOR)`（replace_all）。`NewUnitEditorView.vue` checkbox 块（L102-110）替换为显式按钮 `toggleApplyPublic`（切换 form.is_public），附样式 `.btn-apply-public`/`.public-hint`。
- **地图（链路完全缺失，本次补齐）**：
  - 后端 `maps.ts`：补导入 `requireRole`（原未导入，tsc TS2304 报错修正）；`computeMapVisibility` 的 `isAdminOrAbove` 改 `ADMIN||DOMINATOR||maps.edit`；**新增三端点** `GET /api/map/my-submissions`(authenticate+requireAuth)、`GET /api/map/review-queue`(requireRole ADMIN,DOMINATOR，返回含 cells/spawn_points/attributes/width/height=100)、`POST /api/map/:mapId/review`(requireRole ADMIN,DOMINATOR，action=approve/reject；approve 默认 is_public=1 全员可见，reject 置 is_public=0)。`saveMap` 的 PUT/POST 早已读 `is_public` 走 `computeMapVisibility`，无需改。
  - 前端 `client.js`：`mapAPI` 加 `getMySubmissions/getReviewQueue/reviewMap`。`updateBattlefield` 直接透传 data（已含 is_public，无需改）。
  - `NewBattlefieldView.vue`：新增状态 `applyPublic`/`showMapPicker`/`reviewStatus`；`loadMapContent` 加载后同步 `applyPublic`（review_status=pending/approved/is_public 为真→true）与 `reviewStatus`；`saveMap` 透传 `is_public:applyPublic.value` 并回显 `res.data.review_status`；map-info-bar 加「申请公开」按钮(`:disabled` 无 battlefield.id)+审核徽标(pending/approved/rejected)+「切换地图」入口；新增前置「地图选择」overlay(`showMapPicker`，复用 mapFileList+onSelectMapFile，含「新建地图」)；`toggleApplyPublic`/`pickMap` 方法；样式 `.btn-apply-public`/`.review-badge`/`.map-pick-item`。
  - 新建 `MapReviewView.vue`：仿 `UnitReviewView`，`getReviewQueue` 拉 pending 地图，每行「查看详情」(modal 渲染地形分布/出生点/属性)+「审核通过即公开」勾选+通过/驳回(`reviewMap`)；`onMounted` 顺带拉 `terrainAPI.getTerrainTypes` 着色。
  - `main.js` 注册 `/map-review`(requiresRole ['admin','dominator'])；`TheSidebar.vue` 后台管理分组加「地图审核台」入口 `v-if="['admin','dominator'].includes(user?.role)"`（侧边栏 role 字段在 user.role 且为小写字符串）。
- **部署**：rsync→gateway `npm run build`(tsc 干净，修复 requireRole 导入后通过)→frontend `npm run build`(产物含 MapReviewView-BYYkL998.js)→`docker cp dist/. mecha-gateway:/app/dist/`+restart + `docker compose build --no-cache mecha-frontend`+`up -d --no-deps`。验收：gateway healthy、8081=200、MapReviewView bundle 200、`/api/map/review-queue` 无 token=401(鉴权生效)。
- **现状提示（需告知用户）**：当前 DB 无 admin 账号（仅 dominator=dean147 能审核地图/单位；referee=PH/李子按新语义是"裁判"不再具审核权）。即"向 admin 申请"目前实际只有 dominator 能受理，admin 为预留角色。如需让 referee 也能审，dominator 可在后台给其勾 `units.edit`/`maps.edit`（feature 兼容通道）或改设其为 admin。

## 棋子审核台投稿者名字 + 地图审核台跳登录修复（2026-08-05 深夜，已部署）
- **需求1 棋子审核台用投稿者名字 + 列表显示**：`UnitReviewView.vue` 两处 `unit.owner_id`→`unit.owner_name||unit.owner_id`（列表卡片 + 详情弹窗）；后端 `units.ts` 的 `/api/units/review-queue` SELECT 加 `JOIN users WHERE id=owner_id` 取 `username AS owner_name`。
- **需求2 地图审核台点击跳登录（真实根因）**：实测 `deantest`(referee) token 调 `/api/map/review-queue` 返回 `401 AUTH_TOKEN_MISSING`，而同 token 调 `/api/units/review-queue` 返回 `403 ROLE_FORBIDDEN`。根因：`maps.ts` 的 `/api/map/review-queue`(L326) 与 `/api/map/:mapId/review`(L351) 的 `requireRole(...)` **前漏写 `authenticate` 中间件**（maps router 无顶层 `router.use(authenticate)`，各 GET 路由各自加；但这两个审核路由漏了）。无 authenticate→`req.auth` 未挂载→`requireRole` 见 `!req.auth` 直接 401 AUTH_TOKEN_MISSING→前端 client.js 拦截器(401→window.location='/login')跳登录页。修复：两路由加 `authenticate,` 前缀。
- **附带修正（治本且必要）**：`auth.ts` 的 `PUBLIC_GET_PREFIXES` 中 `'/api/map'` 过于宽泛，把 `/api/map/review-queue` 也当游客公开路径（authenticate 柔性放行→`req.auth` 未设→requireRole 误 401）。已收窄为 `'/api/map/list'` + `'/api/map/battlefields'`（游客仅能浏览，审核接口不再被豁免）。游客浏览地图列表仍 200。
- **部署坑（重要）**：本次首次 `npm run build` 后 `docker compose build --no-cache` 重启，实测 `map/review-queue` 仍 401——排查发现**宿主机 dist/middleware/auth.js 仍含旧 `/api/map`**（tsc 因 `tsconfig.tsbuildinfo` 增量缓存**未重新编译 auth.ts**）。必须 `rm -f tsconfig.tsbuildinfo .tsbuildinfo && rm -rf dist && npm run build` 强制全量重编，dist 才更新；再 rebuild 容器才生效。同理 units.ts 改了也需此强制重编。（注：gateway 容器运行时从镜像 COPY dist，非宿主机卷挂载；`docker cp` 旧经验仍可用但 compose build 已够。）
- **最终验收**：`map/review-queue` 带 token→403(角色不足,前端显示"权限不足"不再跳登录)、无 token→401；`map/list` 无 token→200；`units/review-queue`→403 且返回 `owner_name`。用户(dean147/dominator)登录后地图审核台正常可进。

## 战场技能丢失诊断报告（桌面）深化 + 落地实施（2026-08-05 深夜，已部署）
- 桌面报告 `~/Desktop/机甲战棋/战场技能丢失问题诊断报告-2026-08-05.md` 已补充至**第十二章**：第九章(战场读取逻辑反差审查，指出近48h rooms.ts 有虚构五列脏代码)、第十章(空棋子=dean147手动建的"主宰公开测试" skills=[] 真实记录，非系统生成)、第十一章(5个断点逐点核实：mapSkills白名单抹杀skill_key/min_range真实高危；装备技能部分成立；子部件技能丢失真实；快照/selectedUnit陈旧已不成立)、第十二章(3个细化陷阱：去重真实/cast_range与6.1红线硬冲突/脏代码前置真实)。
- **用户拍板路径 A（采纳）**：passthroughSkills **彻底剔除 cast_range**（及 range/max_range/range_max/rangeLabel），射程走加法模型（分类基准+bonus_range，盲区用 min_range）；白名单仅留 skill_key/min_range/bonus_range/prerequisite/split_damage+原始id；合并 u.skills+attributes.skillsByOwner+equipment.skills 并去重(skill_key优先，无名则name+category)。**路径 B（破红线偷偷透传cast_range）被否决**——会污染 gateway/combat-core/前端hexUtils.js 三端。
- **执行**：①`git checkout -- rooms.ts` 丢弃+48-12虚构五列脏代码回 HEAD 单列逻辑；②新增 `passthroughSkills(rawList)`（router 前定义，严格白名单无...rest 展开）；③`start` 路由改合并三来源经 passthroughSkills 写入 pendingUnits.skills；④部署 rsync→gateway tsc build(强制rm tsbuildinfo+dist)→rebuild容器。
- **线上验证**：gateway healthy(200)；容器内 dist 含 passthroughSkills(3处)；主力 dominator-dean147 的 bEXM-21(8技能)/麦德雷特(6)/exm-h15a2(5) 技能完整；线上 u.skills 确含 cast_range 脏字段，passthroughSkills 精准剔除；bEXM-21 合并去重 8→7（证实陷阱1重复风险真实且被消除）；attributes.skillsByOwner 当前为0(未用部件挂载)、units 表无 equipment 列(S2分支恒空无害)。
- **关键架构红线（再确认，勿破）**：shared-kernel/src/hexMath.ts:239/265 裁定 6.1 红线——**getSkillRangeFields 严禁读取 cast_range，绝对射程字段已废除**，射程=分类基准+bonus_range。任何"透传cast_range"都只产生无人读取的垃圾字段。

## 棋子审核台改为表格+卡片（2026-08-05 深夜，已部署）
- `UnitReviewView.vue` 模板改为"概览表格（投稿者/名称/代号/阵营/分类/体型/等级/总点数/提交时间/操作）+ 卡片网格（操作与要点）"混合；新增 `.review-table`/`.review-grid` 样式。
- 部署：rsync→frontend `npm run build`(产物 UnitReviewView-D3e_xp5I.js)→`docker compose build --no-cache mecha-frontend`+`up -d --no-deps`。容器 dist 哈希与产物一致，8081=200。

## 扫射前端「没区别」根因定位 + 修复（2026-08-05 深夜，已部署）
- **现象**：用户走单位技能面板的「扫射」(走 selectedAttackSkill 路径，本应画 AOE 投影)，但战场视觉无任何变化。战场 1c4bdc26 已不在网关内存(battles 接口返回 0)。
- **诊断链路**：后端结算走 configLoader 存储A 的 skills.sweep(含 aoe_mode:'sector'+range.mcShapes 六向)→多目标结算是对的；前端 getDirectionalHitHexes 的 hasSector = aoeMode==='sector' && !!mcShapes，但 selectedAttackSkill 无这两个字段→投影永远不画。
- **真正根因**：`backend-gateway/src/routes/rooms.ts` 的 `passthroughSkills`(开战聚合单位技能) 用白名单式 `seen.set`，且第38行显式 `const { cast_range, range, max_range, range_max, rangeLabel, ...rest } = s` 把 **`range` 整个剔除**，白名单也未含 `aoe_mode`。结果单位携带技能的 aoe_mode/range.mcShapes 在开战时必然丢失，前端拿不到形状数据。rooms.ts 且未读 glossary(configLoader)，无法从权威源补全。
- **修复**：①顶部加 `import { createRequire } from 'module'` + `getGlossaryConfigLoader()`(复用 glossary.ts 同款 createRequire 加载 combat-service configLoader)；②`passthroughSkills` 不再误删 `range`，显式把 `aoe_mode`、`range` 写入输出，且优先单位快照、缺则从 getGlossaryConfig().skills[skill_key] 补全(与引擎同源一致)。**通用承修**(任意带 aoe_mode 技能自动获得前端投影，非仅 sweep)。
- **部署纪律(再次验证)**：改 backend-gateway/src/*.ts 必须 服务器 `cd backend-gateway && npm run build`(tsc 干净) → `docker compose build --no-cache mecha-gateway` → `up -d --no-deps`。(注：本机 tsc build 后 rsync 同步；容器已重新 COPY 新 dist)。
- **验收**：容器内 dist/routes/rooms.js 含 aoe_mode(5处)+getGlossaryConfig(3处)；模拟聚合确认单位录入快照(无 aoe_mode/range)经补全输出 aoe_mode='sector' 且 mcShapes 含 1-6 六向；gateway healthy。
- **用户侧动作**：旧战场需重开新局验证(开战后单位技能才经 passthroughSkills 重新聚合)；浏览器若仍旧需硬刷新清 bundle 缓存。

## 扫射前端无投影·二次根因（数据层，2026-08-05 深夜，已修）
- **现象**：用户单位 bEXM-21(W) WeirdNova 选「mega扩散炮击」后：无蓝色扇形、黄色射程环+所有射程内敌方红色高亮（疑似射程与打击范围重复计算）。
- **二次根因**：该单位技能「mega扩散炮击」的 `skill_key` 为空（编辑单位时「绑定全局词条」下拉框没选）。passthroughSkills 按空 key 去 glossary `skills` 匹配→匹配不到 `sweep`→`aoe_mode`/`mcShapes` 补不上→`hasSector=false`→退化成圆形攻击（黄色环+全红目标，无蓝色扇形）。用户判断正确：**name 只是显示名不影响管道，`skill_key` 才是词条真实引用**，不应往 glossary 加 mega扩散炮击。
- **修复**：直接改 `/data/mecha-universe.db`（gateway 容器内 sql.js 文件）→ 把该技能 `skill_key` 设为 `'sweep'`（`UPDATE units SET skills=? WHERE id`），写回文件 → `docker compose up -d --no-deps mecha-gateway` 重启刷内存。单位 owner 是 dominator-dean147（非 deantest），API 无权改，故走 db 直改。
- **验证**：db 重启后 `skill_key="sweep"`；容器内模拟 passthroughSkills 补全链 `aoe_mode='sector'` + mcShapes 六向齐全。前端重开战场选该技能应见蓝色扇形+仅扇形内红目标。
- **经验**：glossary 只有 9 个 skill key，`sweep` 是唯一带 aoe_mode 的；任何单位想获得扇形 AOE 必须「绑定全局词条」选 `sweep`。后续可考虑在 SkillsEditor.vue 对 AOE 类技能强制要求绑词条（防呆，未做）。
- **db 操作坑**：sql.js 容器里 require 必须在 `/app` 下（node_modules 解析路径），且 SQL 用 `$1`/`$id` 命名参数绑定避免字符串转义；`docker cp` 脚本到 `/app` 再 `node` 执行。
