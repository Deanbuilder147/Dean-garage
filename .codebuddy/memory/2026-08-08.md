# 2026-08-08 工作日志

## 文档治理：过时归档 + 新旧冲突对照
- 将「词条库/编辑器改造」3 份文档加「过时·已归档」标记并 mv 至 `历史开发报告/归档-词条库编辑器改造/`：
  - 词条库改造落地实施总结报告-2026-08-06.md
  - 词条库UI补充计划-PhaseF-2026-08-07.md
  - mecha-universe-engine/docs/词条库中枢-数据交换整改实施报告-20260805.md
  - （mv 而非 git mv：这些文件未被 git 跟踪；cat+printf 方式加 banner 成功）
- 产出 `新旧对照表-词条库与战斗管线-2026-08-08.md`，核查「数据契约类」「战斗/Segment 类」与新递归六段树(2026-08-08 定调)的冲突：
  - 高冲突：D1 L0–L6 七层硬序 vs 六段有序列表；C1 两段式 vs 统一六段+节点级 WHEN 闸门；C4 Segment damage 走 direct_damage 旁路 vs 须汇流 damagePipe（草稿未落地）；C5 子树 3 层能力。
  - 已解决/低：D2 Phase A 枚举已收敛 shared-kernel；D4 存储A 单一写入口已落地；D6 EFFECT_TYPE≠67 原子已更正。
- 待办（未执行，仅建议）：韦诺报告「〇、机制」段需改两段式→六段树；C 段方案标"草稿/待实施"。
- 已执行：`skill-hierarchy-主谓宾定状补.md`(L0–L6 草案) 于 2026-08-08 加「过时·已废弃」标注前缀（保留原位未移动），对照表 §四项1 已标记完成。
- 报告新增第五部分：在桌面 `词条与战斗引擎综合报告-六段式编排与67原子-2026-08-08.md` 附录前插入「射程/范围画板(Range&Area Canvas)」内容（基于 HexCanvasEditor.vue 实测：射程/打击/条件三图层、WHO 段可视化末端、shared-kernel/src/hexMath.ts 为射程唯一真相源），并加 5.4 节「融入新设计的思考」(非方案)。
- 报告新增第六部分：「战斗引擎真实结算时机」。核查用户草案9项时机(回合开始/移动行动/声明战术/选定目标/伤害值/机动差值/额外值/伤害结算/额外结算)：引擎真实为两层钩子体系(HookChain.phase + GATEWAY_KNOWN_TRIGGERS)，`/attack` 顺序 pre_attack→pre_damage→on_damage→post_damage→on_kill→on_damage_taken→post_attack。结论：4项真实(回合开始[拆轮次+回合]/移动行动/选定目标/伤害结算)、2项近似(声明战术≈on_attack_start/额外结算≈on_after_skill_resolve)、3项引擎无独立节点(伤害值/机动差值/额外值)。
- 唯一真相源：67原子引擎落地与编辑器词库实施计划-2026-08-08.md + shared-kernel/src/enums.ts + MEMORY.md 部署纪律。
- 归拢契约报告：将 `数据契约错位与非对称读写-全盘代码审计报告-2026-08-06.md` 与 `强契约Zod-Schema现状核查与完整契约定义-2026-08-06.md` 两份**汇总**为新报告 `数据契约与强契约Zod-Schema-汇总报告-2026-08-06.md`，并把三份（综合+2原+汇总）全部 cp 到桌面归拢目录。
- 产出 `战场实体真相源-现状与改动需求-2026-08-08.md`（桌面归拢目录）：全面审计战场7大模块(HexGrid坐标/单位实体/地形/ZOC/可见性/移动寻路/战斗状态管理)的代码真相源。结论：硬件层(坐标/寻路/ZOC/绘制)健康；缺口在软件层(schema缺失/effects[]未闭环/地形key中文/default静默降级)。分P0(4项:effects[]消费/BattleState Zod schema/default拒绝静默/地形key英文化)、P1(6项:trigger原样落库/skill_key非空/地形成本入shared-kernel/删ISO_PRESETS旧值/faction审计/hpMax统一)、P2(6项:子树/COST动态/WHEN节点闸门/画板按段节点/可见性升级/特殊移动)。不改动的:ZOC/Dijkstra/射程核定/等距绘制/AP standby/七视图。
- 归拢目录结构定稿：桌面 `词条库报告归拢-2026-08-05至08-07/` = 3篇主报告（根目录）+ `参考文档/`（26份支撑材料）。已核验三篇主报告引用的文档全部齐备无缺失（综合报告附录7份 / 契约汇总2份原报告 / 战场实体引用同目录主报告+MEMORY.md）。
- **三篇主报告改名（按内容重命名 + 加「核心」前缀，后续检索以新名为准）**：
  1. `词条与战斗引擎综合报告-六段式编排与67原子-2026-08-08.md` → **`核心-词条库与战斗引擎-六段式编排与67原子全景报告-2026-08-08.md`**
  2. `数据契约与强契约Zod-Schema-汇总报告-2026-08-06.md` → **`核心-数据契约三端错位与Zod强契约落地方案-2026-08-06.md`**
  3. `战场实体真相源-现状与改动需求-2026-08-08.md` → **`核心-战场实体真相源七模块现状与P0P1P2改动清单-2026-08-08.md`**
  第3篇开头「对照文档」的交叉引用已同步改为新名；本文件上文条目仍按当时旧名记录，检索时按此对照表换算。
- 新增第4篇核心报告 `核心-引擎到战场作用回写链路断点与契约收敛顺序-2026-08-08.md`（桌面归拢目录），补齐「引擎结算→战场状态」这一环。**关键结论（重要，后续改战斗回写必看）**：
  - 断点不在「引擎→前端」而在「引擎→网关」。前端是健康的：socket 只发信号不带数据，前端收到后回头走 HTTP 拉全量覆盖本地（NewBattleView.vue:3820/3548-3562），无数值分叉，不需改。
  - 物理断点 = `combat.ts:1388-1422` `toExecutorUnit()` 把单位摊平成全新字面量对象（statusEffects 还 `[...]` 浅拷贝），与战局真身无引用关系 → 引擎所有原地 mutate 落在临时副本上被丢弃。
  - **★ 双重扣血保险丝（决定施工顺序的硬约束）**：`toExecutorUnit` 只给 `hp` 不给 `current_hp`（:1394）→ 引擎 `effectExecutor.cjs:371-373` 走 `else if (target.hp != null)` 分支扣在副本上；网关 `combat.ts:1692-1693` 又按 `result.final_damage` 独立扣一次。**现在不出事纯粹因为副本被丢弃——「回写丢失」正在充当「双重扣血」的保险丝**。若先接线后整改，保险丝熔断 → 静默双倍伤害无报错。故必须**先定契约后接线**。
  - 引擎 mutate 与 diff 混用无契约；`damagePipe.cjs` 是唯一纯函数样板。`combat.ts:1428` 注释写「引擎保持纯函数（不回写）」与实现完全矛盾，是认知陷阱。
  - 丢失字段：`_meta.stat_modifiers`(effectExecutor:555-557)、`extraTurn`(:492)、`position`/displace。注意 `handleModifyStat`(:523-559) **实现完整无误**，失效纯因写在副本上（代码正确但结果丢失，单测会全过）。真空壳是 `handleApplyBuff`/`handleRemoveBuff`(:508-514) 返回 success:true 零副作用。
  - effects[] 是**半闭环**：只有 `roll.segments[].effects[]` 真走引擎(skillExecutor:1155)；技能顶层 effects[] 走 `routeEffectStackToTargets`(combat.ts:373-439) 仅 push 成 statusEffect 标签，不调任何 handler、不产生 hp/stat 变更。
  - 回写出口散落 7 处；`battleStore.set()`(:1745) 只是提交点非计算点。快照 `combatSnap.ts:185` 全量覆盖单列无版本号/乐观锁，回合制串行掩盖了 lost update 风险。
  - **建议顺序**：①定契约掐双写(倾向引擎变纯，抄 damagePipe) → ②七出口收成单一 `applyPatch()`(引擎返回 statePatch[{unitId,field,op,value}]) → ③接回丢失字段(纯增量近零风险) → ④清伪成功改 `success:false, reason:'not_implemented'`（**必须排在③之后**，便于归因是历史欠账而非新引入）→ ⑤可选 version 乐观锁+事务边界。
- **四份核心报告实机复查（2026-08-08，线上 ssh+node 黑盒）**：为四份主报告各加「实机核查」独立章。重大发现——**契约报告(08-06)与真相源报告的部分 P0/P1 论断已被 08-06~08-07 的 Phase 31~33 改造推翻**，不能按过期结论施工：
  - ❌ 已推翻(无需再做)：shared-kernel 已有完整 zod contracts 目录(9文件, glossary.contract.ts 真用 zod, pkg 含 zod^4.3.6)；skillExecutor default 静默已改 `triggered:false`+UNKNOWN_PREDICATE 诊断(Phase 33)；地形 key 已英文化(plain/forest...，name 才中文)；hpMax 别名全仓零命中(已统一)。
  - ✅ 仍属实：equipment 无 DB 列(全仓 db grep 零命中)；BattleState 仍无 Zod schema(shared-kernel contracts 无 BattleState)；ISO_PRESETS.baseline 仍含旧值 shearY:0.44+cellW/64(hexUtils.js:703，运行时 ISO_DEFAULTS 用0)；**faction 直接比较残留(高危，建议升P0)**——conditionEvaluator.cjs:30-35 / effectExecutor.cjs:301 / skillExecutor.cjs:902 直接用 `.faction` 判敌我，违反「逻辑比较须 unitRoleOf(u)=u.role||u.faction」红线，unitRoleOf 仅 factionSkillRegistry.cjs:46 局部。
  - ⚠️ 回写报告修正：`handleModifyStat`(effectExecutor.cjs:523-560) 已实现完整(项9 修复, duration走statusEffects/瞬时走_meta.stat_modifiers,有失败路径)，**不是空壳**——其"结果丢失"根因是 `toExecutorUnit`(combat.ts:1388-1422)**未透传 `_meta`**，副本连挂载容器都没有。空壳仅 apply_buff/remove_buff/block_movement/assist_choice 四个(:496-514)。位移/额外回合 handler 已实现(handleDisplace:1146 / handleGrantExtraTurn:492)但无网关写回分支。
  - ❌ 全景报告修正：`on_move_path` 非死代码(combat.ts:3286-3287 真实点火 + zoc_block.cjs:109 注册)；`combatIntegrator` 仍被 index.cjs:18 + aiIntegration 引用，非纯废弃。
  - 方法：`ssh -i ~/Desktop/watson.pem root@106.54.197.69`，require effectExecutor.cjs 单例直接调 handler 验证伪成功/副本丢失。
- MEMORY 红线需补：线上代码相对桌面 workspace 已演进，引用旧报告 P0/P1 前必须先实机 grep 确认，避免重复施工冲掉已生效改造。
- 四份核心报告各自加「终态功能蓝图」章（2026-08-08）：分别描述该篇问题全部修复后的完成态功能 + 四篇协同终态图（录入侧→契约侧→回写侧→实体侧 闭环，战场状态=引擎结果唯一落地）。回写报告置于第十一章(实机章之后)、契约/真相源/全景报告置于各自末尾。
- **第五篇核心报告反查修订（2026-08-08）**：`核心-战斗外围（地图地形部署胜负）协同终态-2026-08-08.md` 初稿把 M1/DK1/PK1 标为"新建契约"，实机核查发现 **Phase 33（2026-08-06）已落地部分契约**，修订为「子结构已建、聚合/接入缺失」：① `map.contract.ts` 的 `BattlefieldMapContract` 已含 cells/q/r/terrain_key 强约束（M1 改标"结构已落地，seedRoomBattle 未 safeParse 接入"）；② `unit.contract.ts` 的 `UnitContract` 已建但无 `grants_stealth`（D1）、`BattlePhaseEnum` 类型已存在(primitives.ts:38)但聚合缺 phase 字段(D2)；③ `battle.contract.ts` 只有 BattleUnit/StatusEffect 子结构，**无聚合 BattleStateContract（PK1 仍属根因缺口）**；④ `VictoryContract` 全仓零命中（VK1 完全未建）；⑤ `TERRAIN_COSTS`/`TerrainKeyEnum` shared-kernel 零命中（TK1/T4 确属未做，地形 key 仅 EnglishKey 格式约束非枚举收口）；⑥ V4 关联 D 报告已识别原子缝隙（CROSS_ENTRY_TRIGGER/F6 反向元失效/I2 失效寿命无轴，D 报告 280/282/284 行）作为子集。附表溯源表拆为「已落地契约」+「既往规划溯源」两层，并警告勿重复施工冲掉已生效契约。

- **六份核心报告「终态功能蓝图」归拢为「蓝图-程序总览」（2026-08-08）**：用户要求将六份核心的蓝图章归拢成一份前缀「蓝图」的程序总览。产出 `蓝图-六核心程序总览与关键技术节点-2026-08-08.md`（桌面归拢目录），结构：全局中英文 Glossary → 〇六篇定位与依赖顺序 → 一六篇协同终态图（六环，含第五份 visibilityStatus 环、第六份外围容器环）→ 二关键技术节点（按录入D/契约B/实体C/回写A/信息层/外围 六段，每个节点含 中文/English/自然语言描述/原文档节点引用，共 D-1~4、B-1~3、C-1~5、A-1~4、I-1~5、P-1~7）→ 三协同缺口地图 → 四六篇终态蓝图归拢 → 附原文档节点索引表。每个节点均回链到具体报告章节/行号（如 A 10.4 副本丢失根因、C 241 P0#1 仍属实、第六份 MK/TK/DK/VK/PK 清单），充分引用原文档。

- **六份核心报告「终态功能蓝图」对齐 + 双文件中英对照（2026-08-08）**：
  - 两份指定文件加**中英文术语对照表（Glossary）** + 一级标题英文括注：「核心-可见性与隐匿的前端协同缺漏与收口方案」(信息层/第五份)、「核心-战斗外围（地图地形部署胜负）协同终态」(第六份)。
  - 六份核心报告（D 六段式67原子 / B 数据契约 / C 实体真相源 / A 回写链路 / 第五份信息层 / 第六份战斗外围）的「终态功能蓝图」章节**统一为同一结构**：标题 `# 终态功能蓝图（Completion Blueprint — 本篇完成态应达成的功能）` + 三小节「本篇独立完成态（Standalone Completion）」「一句话功能（One-line Capability）」「六篇协同终态（Six-Report Collaboration End-state）」。协同图从旧"四篇"扩为"六篇"（新增第五份 visibilityStatus 推导环、第六份战斗外围容器环），终态结论句统一为"无丢失、无双写、无静默失败、无契约错位、无信息层裸读、无外围容器缺口"。A 报告原 11.x 编号已去编号对齐。

- **5 个跨篇工程节点补充进六份核心 + 蓝图（2026-08-08 第四轮）**：用户提出 5 个未被文档覆盖的工程节点，经实机核查确认**六份报告与蓝图中均无系统性说明**，遂分别补入归属报告并同步蓝图总览。
  - **A-5 写回原子事务与增量广播** → A 报告新增**第十一章**（11.1~11.6）。要点：一批 statePatch 全成功或全回滚，五条不变量（HP/占位/越界/悬空 unitId/批内双写），回滚只克隆受影响单位子集；增量 Diff `{version, baseVersion, patches[]}` 取代整局重推；CAS 由「可选第五步」转必选并入第 2 步。实机：`applyPatch`/`statePatch`/`PatchContract` **零命中**（属前置设计）；现行 `battleStore.set()` **30+ 调用点**（`combat.ts:526-542` PushBattleStore 重写 set 触发全量推送+落库）；`commPush.ts:56-69` 整局深拷贝 → `socketService.js:51-66` 逐 socket 各跑一次 `applyFog`（N×全量）；`BEGIN TRANSACTION` 零命中；`battles` 表无 version 列（`sqlite.ts:148-158`），`combatSnap.ts:185` 裸 UPDATE。
  - **P-3+ 部署→战斗门控** → 第六份新增**第八·补章**（P+.1~P+.5），新增收口 **DK3/DK4** + DK1-a/b/c。要点：三段式 DEPLOYMENT→**BATTLE_LOADING**→COMBAT，先关门后验货（防 TOCTOU）、失败可回滚、仅房主、走 CAS；点火序列统一 round=1/先攻/ON_BATTLE_START/applySpawnStealth 全体一次性/初始 Buff。**修正第六份 D2 论断**：`phase` 字段与 `BattlePhase` 枚举**均已存在**（`tokens.ts:178-185` 六值 DEPLOYMENT/INITIATIVE/MOVEMENT/COMBAT/RESOLUTION/CLEANUP、`types.ts:346`），切换在 `combat.ts:2795`（裸赋值），缺的是门控不是 phase。**新发现契约错位**：TS enum `BattlePhase` 与 Zod `BattlePhaseEnum`(`primitives.ts:38`) 两套并行且与 D2 建议三值不一致。`ON_BATTLE_START` **零命中**——`shared-kernel/src/enums.ts:14-27` TIMING 只有 ON_TURN_START/ON_ROUND_START，须新增。`applySpawnStealth` 定义 `combat.ts:2654`、调用 `:1162`/`:2767`（逐个，应改为只登记资格）。
  - **B-4 Zod 校验性能边界** → B 报告新增**补充节点 B-4 章**（B4.1~B4.5），§6.5 追加第 7~10 步。要点：按边界性质分层——Macro（房间创建/地图加载/pending-units/end-deployment/快照写**与读**/词条落库/路由入参）全量 parse；Micro（applyPatch 入口）只 parse patch，`field` 须枚举化；五条反模式 AP-1~AP-5 由 CI 断言防回潮。**实机修正**：`validateBody`（B §6.2/§6.4 写作既有设施）**零命中**，属待建；`BattleStateContract`/`PatchContract` 零命中；`shared-kernel/src/contracts/` 已有 9 文件。
  - **I-6 LoS 视线阻挡** → 第五份新增**第四·补章**（I6.1~I6.6），新增收口 **K9~K13**。要点：推导管线原输入全是状态标记、无空间维度；补 `computeLoS(from,to,grid)` 沿 `hexLineDraw` 插值比高，遮挡裁剪为 HIDDEN；高地俯瞰是几何自然结果；**服务端权威**（前端算=可抓包透视），不可见单位在 `applyFog` 下发前剔除；`scanned` 穿透遮挡（序4优先序2），"扫描也穿不透"用 `pierce_los` 表达；不透明地形用新增 `blocks_sight`、与高程正交。**关键修正**：`TerrainCellContract.height` 是**地图尺寸**（`types.ts:171`/`map.contract.ts:25`），地形高度字段是 **`elevation`**（`types.ts:163`/`map.contract.ts:18`）——按 height 施工会取到地图行数。`elevation` 已在契约就位但**零消费**；`deriveVisibilityStatus`/`LoS`/`raycast` 全零命中。
  - **C-6 equipState 活路径与耐久锁** → C 报告新增**补充节点 C-6 章**（C6.1~C6.6），新增 **P0-2b/P1-11/P1-12/P2-x**。要点：`units.equipment` DB 列维持封禁，`equipState` 为唯一活真相源；`equipment_lock` 三写原子（destroyed=true / durability=0 / `_meta.lockedDurability[slotKey]=true`）；E 组回复预检锁、命中显式 `success:false, reason:'EQUIPMENT_LOCKED'`（不得伪成功）；统一 `activeEquip()` helper 拦截 destroyed。**实机关键发现**：`computeWeaponMobilityBonus`（`combat.ts:643`）**不检查 destroyed** → 耐久恢复后会出现"武器已炸毁仍加机动"的幽灵加成；`equipState` 由 parts 构建于 `combat.ts:598`；`lockedDurability`/`equipment_lock` 零命中；`_meta` 现仅写 flags(visibility)/stealth/extraTurn。
  - **蓝图总览同步**：`蓝图-六核心程序总览与关键技术节点-2026-08-08.md` 新增——文首「摘要：本轮增补的 5 个跨篇节点」表 + 三处文档修正提示；Glossary 增 13 条术语（原子事务/亚稳态/不变量检查/增量广播/冻结门控/点火序列/宏观边界/微观循环/视线/地形高程/耐久状态锁/有效装备）；**§1.1 三端运行时数据流图**（Client→Gateway→SkillExecutor→applyPatch→BattleState→SQLite，标注五节点落位 + 当前实机与目标态的距离表）；五节点正文写入 2.2~2.6 各段；§三缺口地图增 5 行跨篇缺口 + 「五节点共同性质=**篇与篇接缝处的工程约束缺失**」；§四六篇独立完成态各追加一条；统一终态结论扩写为含分层校验/原子事务/LoS 裁剪/增量广播/门控点火；文末索引表增五节点落位速查。
  - **提炼两条通用纪律**（已写入蓝图 §三末尾）：①**字段就位 ≠ 功能就位**——`elevation`(I-6) 与 `durability/destroyed`(C-6) 都已在契约存在却零消费，因"看起来已做"而极难发现；新增契约字段须同时登记消费点否则不予合入。②**规划文档中的"既有设施"须实机复核**——`validateBody` 写作既有实为零命中、`phase` 写作缺失实为已存在，两个方向偏差都会导致错误施工（前者以为有而不建，后者以为无而重建冲掉已生效实现）。

## Alpha3.0 五个节点【实机代码落地】（2026-08-08 第五轮，指挥亲自执行）
用户要求对桌面 `aplha3.0 系统/` 的六核心+蓝图设计 Agent Teams 协调方案并落地。结论：内置 code-explorer 子 agent 为**只读**，无法写代码，故由指挥（主 Agent）按批次顺序亲自落地，每批验收。所有改动落在 `/Users/dingxuyang/CodeBuddy/20260604120036/mecha-universe-engine/`。

**批次1 B-4 契约层**（已验收）：
- `shared-kernel/src/enums.ts` 新增 `PATCH_FIELD`(9键, 取 UnitStatsContract) / `TERRAIN_KEY` / `TIMING.ON_BATTLE_START`。
- 新建 `contracts/patch.contract.ts`(`PatchContract`+`StatePatchItemContract`, field 枚举化) + `contracts/battle-state.contract.ts`(`BattleStateContract` 聚合, Macro 边界用)。
- `map.contract.ts` 渐进引用 `TERRAIN_KEY`（保留 EnglishKey 宽松，因存量含 ruins/void/space），预埋 `elevation`/`blocks_sight`（I-6 复用）。
- 新建 `backend-gateway/src/middleware/validateBody.ts`（可组合 safeParse+400），挂 combat.ts 地图加载/pending-units + rooms.ts 房间创建（Macro 边界）。
- **裁决**：PatchFieldEnum 取 UnitStatsContract 轴（不复用 EFFECT_TYPE）；Macro 边界收敛 5 处战斗网关；地形枚举渐进不破坏存量；`zod`/`build:cjs` 已就位未新增。

**批次2a C-6 实体活路径**（已验收）：
- `battle.contract.ts` 增 `EquipmentLockMetaContract`(`_meta.lockedDurability`) + `BattleUnitContract._meta` 可选字段。
- `enums.ts` 增 `EQUIP_SLOT`(left/right/extra/core/chassis)。
- `combat.ts` 提取 `activeEquip()` helper（拦截 destroyed/durability<=0），重构 `computeWeaponMobilityBonus`/`getEquipmentAttackStat` 复用；新增 `applyEquipmentLock`/`isEquipmentLocked`；`/skill` 与 `executeDirectSkill` 加 E 组预检返回 `EQUIPMENT_LOCKED`。
- **复核发现**：`computeWeaponMobilityBonus`(现 1471) 与 `getEquipmentAttackStat`(现 1498) **已内置 destroyed 拦截**（报告假设的 P0-2b 已就位）——再次验证"文档假设 vs 实机"偏差。

**批次2b I-6 视线阻挡**（已验收）：
- `hexMath.ts` 加 `hexLineDraw`(cube lerp+round) + `computeLoS`(消费 elevation/blocks_sight, LOS_TOLERANCE=1) + `cubeRound`。
- `frontend/src/utils/hexUtils.js` 加 `hexLineDraw` 逐字镜像。
- `socketService.js` 内联等价 LoS + `deriveVisibilityStatus`(I6.3 优先级) + `applyFog` 接入 los 剔除 hidden（防抓包透视）。
- `unit.contract.ts` 加 `sight_range`(默认6)。合同已 `elevation` 就位（B-4 预埋）。
- **未做**：网关 push state 挂载 `_losOptions`（terrainGrid+viewerPos）属 A-5/P-3+ 接线，留待后续（不影响几何真相源落地）。

**批次3a A-5 回写原子**（已验收）：
- `toExecutorUnit`(1393) 透传 `_meta`（A-2 修复，C-6 锁不再丢）。
- 新建 `applyPatch()` 单一出口：五条不变量(ORPHAN_UNIT_ID/BATCH_DOUBLE_WRITE/OUT_OF_BOUNDS/HP边界/批内双写) + 原子克隆提交 + 增量 Diff(`_lastDiff={version,baseVersion,patches[]}`) + CAS。
- `sqlite.ts` battles 表加 `version` 列 + `combatSnap.saveBattleSnapshot(expectedVersion)` CAS + `PushBattleStore.set` 自动 version 递增 CAS 落库。
- **范围控制**：未强行改写 30+ `battleStore.set` 调用点（线上风险），`applyPatch` 作为新出口+机制落地，逐步迁移。

**批次3b P-3+ 部署门控**（已验收）：
- `tokens.ts` `BattlePhase` 增 `BATTLE_LOADING`(P-3+ 中间态)。
- `end-deployment` 门控化：仅房主 + 幂等 409 DEPLOY_LOCKED + 先关门 BATTLE_LOADING + 验货(单位>0)失败回滚 + `igniteBattleStart()` 统一点火(round=1/先攻/role回填/ON_BATTLE_START) + 设 COMBAT。
- `deploy-unit` 加 409 DEPLOY_LOCKED 门控（非 DEPLOYMENT 阶段）。
- `igniteBattleStart()` 新函数收敛点火。

**批次4**：shared-kernel `npm run build` 通过(ESM+CJS)、backend-gateway `tsc --noEmit` 零错、hexUtils.js/socketService.js `node --check` 通过。**未上服务器部署**（需用户批准+触碰线上，留部署清单）。

**关键纪律再确认**：改 shared-kernel/src 后必须仓库根 `npm run build` 出双产物；上服务器须 rsync + `docker compose build --no-cache mecha-gateway mecha-frontend` + `up -d --no-deps`；绝不碰 mecha-battle-db。只读 subagent 不能落地代码，指挥需亲自写。

## Alpha3.0 全量部署 + 四红线验收 + 31词条测试（2026-08-08 第六轮）
用户要求按 8 步清单全量部署并跑 4 红线 + 31词条测试。

**部署踩坑（重要，复发必查）**：
- 根目录无 `build` script，shared-kernel 用 `npm run build --workspace=shared-kernel`。
- **致命坑**：sqlite.ts 的 ALTER TABLE battles ADD COLUMN version 被我误插进 `createTables()` 的模板字符串里（db.run(\`...大模板...\`)），导致 SQL 注入 JS 代码 → gateway 启动 `Error: near "/": syntax error` 崩溃。修复：把 ALTER 移进 `migrateTables()` 的 battles 迁移块（与 state_snapshot 同模式，`PRAGMA table_info` 查列后 ADD）。**教训：多语句 schema 必须用 db.run 单条执行，绝不可把 JS 控制流塞进模板字符串。**
- rsync 排除 dist，故**服务器 shared-kernel 的 dist 仍是旧的**（缺 BATTLE_LOADING）→ backend-gateway tsc 报 TS2339。修复：服务器上先 `npm run build --workspace=shared-kernel` 再 build gateway。
- gateway 容器 /app/dist 已含新代码（grep DEPLOY_LOCKED=4 确证）；dist 走 docker build COPY（Dockerfile 注释"dist 由 rsync 同步提供，builder 不 tsc"）。

**4 红线验收**：
- 红线1 ✅：8081 index.html 引 `/assets/index-B5gztj9T.js` + `index-qTrUtYIW.css`，与服务器 frontend/dist 产物哈希 100% 一致（GlossaryHub-D1MS2T--.js 也在）。
- 红线2 ✅：B-4 返回 VALIDATION_ERROR 结构（房间缺 name / 地图加载缺 battlefield_id，含 issues 路径）。
- 红线3 ⚠️：P-3+ 代码已部署上线、路由可达（返回符合新门控的错误）；完整 409 DEPLOY_LOCKED 端到端需真实战斗全生命周期，而地图数据存于受限 mecha-battle-db（按纪律未碰）→ 留待有地图环境验证。curl/zsh 单引号 JSON 在 deploy-unit 测试时 body 丢失导致误报 VALIDATION_ERROR，用 python requests 发即正常（BATTLE_NOT_FOUND）。
- 红线4 ✅：battles 表 version 列迁移成功（启动日志 `[DB] 迁移：battles 增加 version 列`），CAS 逻辑部署、DB 写回健康无崩溃；`PRAGMA table_info(battles)` 确认含 version 列。

**31 词条测试（deantest/referee）**：
- 经 `/api/combat-glossary/hub-config` POST 新建 25 + 6 个 KEY_CONFLICT 转 PUT 更新，最终 **31/31 落库**（skillCount=41）。
- 问题（显微知著）：A) POST/PUT 双入口割裂，撞 key 即 409，缺 upsert；B) skill_key 无自动 slug 派生，依赖手填；C) 存储A deepMerge 落库残留脏键（map_cannon.directions 旧"0"键）；D) 67 原子与编辑器字段语义断层（Part4 冲突段原子无引擎消费点）；E) 403 裸拒无引导。
- 普适方案：S1 upsert 语义 / S2 key 系统派生+唯一性 / S3 整替落库+脏键清除 / S4 67原子枚举双向注册校验 / S5 权限引导。
- 报告：`alpha3-glossary-test-report.md`（工作区根）。

**部署纪律补充**：docker compose exec mecha-battle-db 报"not running"是因 compose 项目名不匹配，改用 `docker exec $(docker ps -q --filter name=mecha-battle-db)`；但按纪律仍**绝不碰该库**。容器名 mecha-gateway/mecha-frontend/mecha-comm/mecha-battle-db。
