# 2026-08-11 工作日志

## glossary-studio 五视图文档迭代（glossary-studio-五视图功能定位与待讨论清单-2026-08-10.md）

### 用户拍板结论（第二轮）
- #1 WHEN 锚点：起始第一个 WHEN **不可擦除、不可移动**（绝对固定）。
- #2 AOE：目标点**复用射程环带里玩家点的某格**，选点受 max_range 约束，但**画出的作用范围可超射程**（范围独立于射程）。
- #3 地图炮：默认绘制阴影**向正右（dir=0）**，之后**顺时针** 60° 补全六向；无需方向键。
- #10 身份枚举：**不替换**（B4 五类、B1/B2 旧两元并存）。
- #12 六段顺序约束：**交给编辑器侧灰掉禁插**（插入非法段时按钮禁用），引擎不改 ORDER、不做 normalizePhaseOrder 重排；whenCtx 惰性化仍建议但非阻塞。

### 战场模拟结算接口（H 节四步法）三文件实证（第三轮）
- `damagePipe.cjs`：真实完整入口 `DamagePipe.calculate(config)`（`config={attacker,defender,attack_type,damage_kind,terrainDefs,baseDamage?}`），另有简版 `calculateQuick(attacker,defender,attackType,terrainDefs)`。**无 calculateDamage**。
- `skillExecutor.cjs` `_walkPhaseTree`（line 1318）：**无 pure 模式**（pure_segment 仅是日志模型名）；会直接 `unit.statusEffects.push`。需新增 `pure` 开关使 statusEffects 写克隆帧、不污染原 battleState。ORDER 固定 `['WHEN','IF','WHO','ROLL','DO','COST']`，depth>3 截断。
- `shared-kernel/src/hexMath.ts`：**无 deriveRange / computeAOECells**；现有 `getHexesInRange(center,maxRange,minRange?)`、`hexDistance`、`isTargetInRange` 可复用为射程判定。需新增 `computeAOECells(casterCoord,targetCoord,aoeShape)`（AOE 作用范围，半径独立于 maxRange）。

### 文档重构（第五轮）
- 用户要求按"架构→运行时契约→视图交互→字典规范→施工图"五层结构重组原混合文档，并纳入其提供的 6 维度阻塞诊断。
- 生成新文档 `glossary-studio-架构重构文档-2026-08-11.md`（五章：1 架构决策/2 运行时契约/3 五视图交互/4 67原子字典/5 施工图+状态矩阵），原 `glossary-studio-五视图功能定位与待讨论清单-2026-08-10.md` 保留备份。
- 6 维度阻塞：①落库双向回写(#5/#6/#7/G) ②字段去重真相源(#9) ③跳转返回状态机(4.1/4.5) ④战场模拟pure补全(H/#4) ⑤bind_to跨层桥接 ⑥暴击移除DO收口(#8)。

### 六段模型核心理解（用户与我对齐的心智模型）
- 主六段（固定框架 WHEN→IF→WHO→ROLL→DO→COST）+ 每 phase 下原子 + 原子可递归子六段（≤3 层）。
- 同名概念（阈值/身份/目标）含义由「所在层(主/分/子) + phase」共同决定，不得擅自脱离层级讨论。
- 子六段 = 原子的可选 `tree` 字段（非新原子）；挂子段的原子照常执行、段落照常加原子；层间不继承靠 `bind_to` 桥接。
- WHO = owner(self 可覆盖) + target({scope 五类身份, granularity 单体/群体, range_filter?})。

## /glossary-studio 重构 Phase 1 + Phase 2 落地与部署（2026-08-11 下午）

### 代码改动（已部署上线 8081）
- **Phase 1（后端底座）**：
  - `shared-kernel/src/hexMath.ts` 新增 `computeAOECells(caster,target,aoeShape)` + `rotateShadowEast`（handDrawn/radius/mapcannon 三形态，半径独立于 maxRange 解决 #2 超射程），`index.ts` 已导出。
  - `services/combat-service/.../skillExecutor.cjs` `_walkPhaseTree` 新增 `opts.pure` 开关：深拷贝 unit/target 为隔离帧（`_deepCloneUnit`），statusEffects 写入克隆体，原 battleState 零污染，返回 `out.cloneFrame`。
- **Phase 2（前端方案 B）**（落点 `GlossaryHubNew.vue` + `HexCanvasEditor.vue`）：
  - 2-1 WHEN 锚点(5,10)：`key→coord` 映射解耦 LANE_SKELETON 顺序，WHEN 恒 (5,10) 不可移动/擦除，画布 🔒 双线锁 + 六色段头色板。
  - 2-2 IF/ROLL 多实例：拖入 IF/ROLL 段弹 2/3/4 数量 modal，自动生成分支占位槽（multiDrop 状态）。
  - 2-3 高亮引导：含原子段高亮 ≤2 环空格（内环绿/外环橙）。
  - 2-4 顺序硬边(#12)：合法序 WHEN→IF→WHO→ROLL→DO→COST，WHEN 锁首位、draggable 禁用、order-locked 灰态；非法 drop 静默拒绝。
  - 2-5 双 Drop 落点：C 区段头 + 底画布六格均已支持 drop-atom（onLaneDrop / onCanvasDropAtom 接通）。
  - 2-6 暴击收口(#8)：确认原子面板无独立暴击控件，暴击倍率统一经 DO 段 D1 `direct_damage` 的 damage_mode='multiplier'（fixed_rate_multiplier 字段）。

### 部署纪律实操（成功路径）
- 本地 `shared-kernel npm run build` 验证 → rsync 排除 node_modules/.git/dist 到 106.54.197.69（SSH 警告可忽略）。
- 服务器 `shared-kernel npm run build` + `backend-gateway npm run build`(tsc) + `frontend npm run build` 均成功。
- **docker compose build 必须去掉 `--no-cache`**：`--no-cache` 会触发容器内 `npm install` 联网失败（npm error network），复用缓存层则跳过 install 顺利重建。命令：`docker compose build mecha-gateway mecha-frontend`（不加 --no-cache）→ `up -d --no-deps mecha-gateway mecha-frontend`。
- 验收：8081 在线 `GlossaryHubNew-BUDNwAa9.js` 哈希 == 服务器 build 产物，内容含 `order-locked`/`multi-drop`；容器内 `/app/services/combat-service/.../skillExecutor.cjs` 含 `pure`；gateway Healthy。
- **未决**：3 个前端交互点（WHEN 擦除拦截/顺序拖拽拒绝/IF·ROLL modal）需浏览器人工实测，终端无法模拟 Canvas 拖拽。

### 下一步
- Phase 3（跳转/回写状态机）+ Phase 4（真实模拟管道端到端接线）待推进。

## 六段式坐标修复 + 删除 /glossary-hub（2026-08-11 晚）

### 修复（上轮用户反馈：WHEN 不在(5,10)/多余点/添加按钮不显）
- **WHEN 锚点错位根因**：六段式坐标在「邻居偏移+平移」后**再叠加 UNIT_ORIGIN(10,10)**，致 WHEN 落到 (15,20)。改为「以 WHEN(5,10) 为锚的相对星形」：`coord[i]=WHEN+(neigh[i]-neigh[whenIndex])`，两处同源（HexCanvasEditor.vue + GlossaryHubNew.vue）。Node 校验 WHEN==(5,10)。
- **多余点**：hexsix 视图移除 20×20 网格底板 + 1/2 环引导高亮；相机按六段包围盒居中放大。
- **添加按钮不显**：段头 `.desc` 加省略号 `flex:1`、`.nest-btn` 加 `flex-shrink:0`。

### 删除 /glossary-hub 页面（用户确认词条库= /glossary-studio）
- 删 `frontend/src/views/GlossaryHub.vue`（旧页面）。
- `main.js`：移除 `/glossary-hub` 路由，仅留 `/glossary-studio → GlossaryHubNew.vue`。
- `NewHomeView.vue`：首页卡片跳转改 `/glossary-studio`。
- `TheSidebar.vue`：删 `/glossary-hub` nav-link，保留 `/glossary-studio` 改名「词条库」。
- 残留 `components/glossary-hub/SkillListPanel.vue` 仍被 GlossaryHubNew 依赖，保留。
- 注：之前六段式所有改动**本就落在 GlossaryHubNew.vue（/glossary-studio）**，删除旧 hub 不影响修复生效。
- 部署：rsync `--delete` 清理服务器旧 `src/` 残留（无害）；frontend build + 重建容器。验收：`GlossaryHubNew-CeLDjJ5x.js` 在线 HTTP200 含 order-locked；旧 hub chunk 引用计数 0；主 chunk 哈希一致；容器 healthy。

## Phase 3 + Phase 4 端到端落地（2026-08-11 晚，用户：先做后续步骤，画面回头处理）

### Phase 4 端到端（已验证打通）
- **后端 `skillExecutor.cjs`**：`executeUniversalSkill` 增加第 6 参 `opts={}`，line 529 `_walkPhaseTree` 末参由 `{}` 改 `opts` 透传 pure；返回体在 pure 下附 `result.cloneFrame = treeResult.cloneFrame`（隔离克隆帧增量：模拟后施法者/目标状态）。
- **后端 `glossary.ts` `/test-skill`**：两处 `executeUniversalSkill(...)` 调用加第 6 参 `{ pure: true }`，返回体 `targets[].cloneFrame` 透传增量帧。靶场临时 units 每次 rebuild，反复试跑不互相污染。
- **前端 `GlossaryHubNew.vue` `runTest()`**：改为 `POST /combat-glossary/test-skill`，传 `key` + `lanes.value.map(laneToNode)` 序列化的 `draft.tree`（schema_version=2, __segment_model__=true）+ `targets`。结果存 `runResult` ref，模板加 `.bs-result` 文本日志块（纯 pre，非画面美化）。保留画布演示调用 `drawBattle()`。
- **验证（服务器真实调用）**：`block` 词条 `success:true, engine:"combat-service/.cjs"`，result 含 `triggered/finalDamage/survivor.hp/dice` 完整结算，`cloneFrame` 字段透传正常。注意 `/test-skill` 返回字段是 `targets`（非 `perTarget`）。`beam_cannon/overdrive/melee_strike` 返回 SKILL_NOT_FOUND（不在 hub-config 存储A，无碍）。

### Phase 3 回写状态机（数据层已就位，画面层待回头）
- `writeLanesToDraft()`（line 1168）已把六段 `lanes`（含 nest 1 层子段）经 `laneToNode`/`atomToContract` 序列化进 `draft.tree` + `schema_version=2`，并投影 v1 `effects`/`roll.segments`。`saveCurrent` 已调用之 → 保存即回写。
- 子六段递归 ≤3 层后端 `_walkPhaseTree` 已支持（node.children 递归）；前端 UI 当前仅 1 层 nest（画面部分留待回头处理）。
- `bind_to` 字段：之前已透传（skill.json 的 bind_to 进入 effect.bind_to），本次未改动——属存量待收口项。

### 部署
- rsync → 服务器 backend-gateway `npm run build`（tsc 干净）+ frontend build → 重建 `mecha-gateway mecha-frontend`（不加 --no-cache）。gateway 镜像 COPY services/combat-service，使 skillExecutor.cjs pure 改动入容器。
- 验收：`GlossaryHubNew-DjpjxoRq.js` 在线 HTTP200 含 `combat-glossary/test-skill`(1) + `bs-result`(2)；主 chunk `index-yPVhJzlC.js` 哈希与 dist 一致；gateway+frontend healthy；`block` 靶场真实引擎结算 success=true。

## 批次2.11 Pre-flight 契约对齐（2026-08-11 深夜）

### 脚本交付
- `scripts/preflight-67atoms.cjs`（纯静态解析，零依赖）：对主计划《67 原子引擎落地与编辑器词库实施计划》真实清单（A11+C7+D7+E3+F6+G8+H6+I3+J1+K4 = **56 项**，文档标题"67"但表格实际 56，标题计数不准）逐条做「文档标注 vs 引擎真相源」偏差审计。
- 真相源：`effectExecutor.handlers`(51) + `_resolveHandlerType` 别名(7) + `reactionHandlers` 注册(8) + `GATEWAY_KNOWN_TRIGGERS` 白名单(21) + `shared-kernel EFFECT_TYPE`(27)。

### 偏差审计结论
- **FAKE 假对齐(1)**：A7 友军受击（文档✅），但 `on_ally_attacked` 注册了却**不在 GATEWAY_KNOWN_TRIGGERS 白名单** → 引擎自检告警 `unknown`，且需确认是否真被点火（combat.ts line 2232 确实 `fireReaction('on_ally_attacked')`，实际能点火，仅白名单漏登记）。
- **真缺口 GAP(6)**：C5/C6（数值运算 expression eval，核心管线未实现）、D4 伤害分担(damage_share 无 handler)、D6 护甲穿透(armor_pierce 无)、D7 反伤(reflect_damage 无)、G7 解除前置(__meta__ 元词条)。这 5 个（除 C5/C6 是核心管线）引擎确无 handler。
- **UPGRADED 已落地(28)**：文档标🔶/❌但引擎已有 handler 的原子——resource_recovery/durability_consumption/shield/charge_layer/displace/map_cannon/scan_reveal/order_rewrite/equipment_lock/slot_occupancy/mutual_exclusion/skip_mobility/action_debt/preempt/forfeit_move/visibility/selectable/plunder/player_optional/interrupt_window 等。属"默默完成"，文档标注过时，需回写文档状态。
- **reactionHandlers 漂移自检**：`[unknown]` on_ally_attacked（已注册不在白名单）；`[missing]` on_attacked/on_attack_start/post_melee/on_counterattack/on_reaction/on_after_skill_resolve/on_phase/on_move/on_item_pickup/on_turn_end/pre_damage_apply/on_faction_skill/on_visibility_changed/on_action_order_changed（白名单但无 handler）——但 on_attacked/on_attack_start/post_melee_damage 是网关内部真 fire 的（combat.ts 1766/1771/2224），`post_melee` 在白名单但引擎注册名是 `post_melee_damage`（拼写漂移）。

### 关键架构认知（写入红线）
- **67 原子 type ≠ EFFECT_TYPE 枚举**：枚举是 27 个粗粒度谓语（damage/recovery/status/displacement...），67 原子是更细 handler 名。引擎靠 `_resolveHandlerType` 别名层（status→modify_stat, damage→direct_damage, recovery/heal→resource_recovery, displacement→displace, buff/debuff→modify_stat）桥接，两者命名不 1:1。
- **reactipHandlers 触发入口在网关层**（combat.ts 直接 `fireReaction('on_xxx', ...)`），不在 combat-service 内部。reactionHandlers/index.cjs `register()` 的 key 必须与 combat.ts fire 名 + GATEWAY_KNOWN_TRIGGERS 白名单**三处一致**，否则静默无效果或自检告警。
- A 组原子是"触发时机"非 effect type；C/D 组数值原子多属核心管线字段（damagePipe 解析），非独立 effectExecutor handler。

### 待办（用户未拍板是否继续修）
- 立即可修小漂移：A7 `on_ally_attacked` 加进 GATEWAY_KNOWN_TRIGGERS 白名单（消告警）；`post_melee`→`post_melee_damage` 白名单拼写对齐。
- GAP handler 实补（D4/D6/D7 armor_pierce/reflect_damage/damage_share）涉及战斗平衡，需用户确认范围再动手。
- 文档回写：28 个 UPGRADED 原子标注过时，应更新主计划 md 现状列。

## 批次2.11 收尾（用户拍板：1+3，GAP handler 留批次2 后半程）

### 代码改动（已部署）
- `backend-gateway/src/routes/combat.ts` 的 `GATEWAY_KNOWN_TRIGGERS`：① 追加 `'on_ally_attacked'`（消 reactionHandlers 自检 `unknown` 告警，A7 友军受击 cover 现在三处一致）；② 删 `'post_melee'`、保留 `'post_melee_damage'`（与 reactionHandlers 注册名 1:1 对齐，修拼写漂移）。
- 部署：rsync → 服务器 `backend-gateway npm run build`（tsc 干净）→ `docker compose build --no-cache mecha-gateway` → up -d。容器内 `/app/dist/routes/combat.js` 已含 `on_ally_attacked`、`post_melee_damage`×2（无孤立 `post_melee`）。容器 **healthy**。

### 文档回写（已落地）
- 主计划 md（67原子落地计划）现状列：28 个 UPGRADED 原子（E1/E2/E3/F2/F3/F4/F5/F6/G2/G3/G4/G5/G6/G8/H1/I1/I2/I3/J1/K1/K2/K3/K4 + A9/A11/A12 + C4/D5 + B5/B6）回写为 ✅，注明"批次2.11 审计确认"。
- 文档标题"67" 与实际表格一致（之前脚本漏数 B 组+ A12 导致数成 56，已补回，脚本现覆盖完整 67 项）。
- 在定级说明后插入「批次2.11 Pre-flight 静态契约审计结论」小节：列 28 个已落地原子、2 处漂移修复、9 个真 GAP 靶子（B4/B8/B10/C5/C6/D4/D6/D7/G7）、3 条架构认知。

### 最终审计口径（脚本 scripts/preflight-67atoms.cjs 现覆盖 67 项）
- 真对齐 OK: 28 | 假对齐 FAKE: 0 | 已落地可升级 UPGRADED: 30 | 真缺口 GAP: 9。
- `[unknown] 无`（漂移已闭环）；`[missing]` 残余项（on_attacked/on_attack_start/on_move 等）是网关内部 fire 钩子名，非 67 原子 trigger，属正常不需处理。
- 9 个 GAP 作为批次2 后半程补全的精准靶子记录在档，暂不补（用户决策）。

### 下一步
- 批次2 后半程：补 9 个 GAP handler（涉及战斗平衡，需用户确认范围）。
- 批次3 编辑器词库接入（前端把 67 原子字典暴露成可选词条）。
- 画面问题（WHEN 锚点/杂点/添加按钮）仍按约定最后调。

## Phase 3 跳转/回写状态机 + Phase 4 真实模拟管道（2026-08-11 深夜，续前 Phase 4 test-skill 桩位）

### Phase 4 新增端点 `/hub-config/simulate-skill`（glossary.ts，已部署）
- 与 `/test-skill`（靶场单回合桩位）区别：接收前端画布**真实单位状态 `battleStateStarter.units`** 作引擎输入，跑 `executeUniversalSkill(key,caster,target,context,skillDef,{pure:true})` 真实隔离结算，把「画布状态→引擎→回写 cloneFrame」串闭环。
- 词条查找改为 `mergedSkills = { ...CORE_SKILLS, ...stored.skills }`，与 `GET /config` 对齐（修复 melee_strike/ranged_shot 等 CORE 只读词条 SKILL_NOT_FOUND）——**test-skill 同步修**。
- 回写真源 `rewind[]`：①优先 `cloneFrame`（六段式词条走 _walkPhaseTree 生成，含真实 HP/statusEffects 增量）；②谓语技能（melee_strike 等旧 script 格式无 cloneFrame）用 `finalDamage` 扣减目标 HP 构造最小快照，保证画布涂抹真实伤害。
- 关键坑：`melee_strike` 的 CORE 配置只有 `script:"SELF→TARGET: DAMAGE_PHYSICAL flat=attack*1.0"`，**无 roll/segments/base_damage 字段**，`_executeAttackSkill` 取 `base_damage=0`，真实引擎算出 final_damage=0 —— 是词条本身未迁移到新模型，**非管道 bug**。验证须用新模型带 segments 词条（如 `sweep` 扫射）。
- 路由前缀：`/combat-glossary` 已含，故前端调 `/combat-glossary/hub-config/simulate-skill`（apiClient baseURL=/api）。

### Phase 3 前端 `GlossaryHubNew.vue`（已部署）
- `buildUnits()` 给画布单位加真实战斗属性（id/q/r/hp/maxHp/armor/attack/defense/statusEffects/currentStats），画布 x/y 即六角 (q,r)（Phase 2 自由画布语义）。
- `runTest()` 升级为跳转/回写状态机：`bsPhase 'idle'→'rolling'→'resolved'`，上传 `battleStateStarter.units` 调 `simulate-skill`，按 `rewind.targetAfter.hp` 与初始 HP diff 决定命中/击杀着色，回写画布单位 HP 条/状态图标，新增 `resetSim()` 重置。
- `drawBattle()` 增强：命中金边/击杀红边、HP 血条、statusEffects ◈ 气泡。模板加 rolling 态按钮 + resolved 重置按钮 + 结构化真实伤害结果区（总伤害/应用原子/目标 HP 变化）。

### 部署与验收
- rsync（gateway src + frontend src）→ 服务器 `backend-gateway npm run build`(tsc 干净) + `frontend npm run build` → `docker compose build --no-cache mecha-gateway mecha-frontend` → up -d。
- 验收（真实调用）：`sweep` 词条 `source:canvas, final_damage:2, targetAfter.hp:98`（100-2 真实扣血），rewind 含完整回写真源；前端 `GlossaryHubNew-D6q4Gnbv.js` 在线含 `hub-config/simulate-skill`(1)；8081 入口 HTTP200；两容器 healthy。
- 策划现已能「在画板配置 67 原子词条 → 真实引擎结算 → 测出真实伤害并回写画布」。

### 下一步
- `melee_strike`/`ranged_shot` 等 CORE 词条仍是旧 script 格式（base_damage=0），若策划需在模拟里看到非零伤害，需把它们迁移到新 roll/segments 模型（或编辑器支持 script→segments 转换）。属批次2后半程或单独迁移任务。
- 批次3 编辑器词库接入、9 个 GAP handler 补全仍待推进。
- 画面问题（WHEN 锚点/杂点/添加按钮）按约定最后调。

## 批次2后半程：9 个 GAP Handler 增量补全（续上，深夜）

### 真相源澄清（口头编号 ↔ 文档编号错位）
用户口头 B4/B8/B10/D4/D6/D7/G7 语义（伤害分担/转移/吸收/穿透/反伤/解除前置）与《67原子计划》文档实际编号对不上。以 **doc:❌ 的 9 个真 GAP** 为准：
B4 三单位共线 / B8 充能叠层 / B10 冷却就绪 / C5+C6 核心表达式（kind:'core'，route `__core_expr__`）/ D4 伤害分担 / D6 护甲穿透 / D7 反伤 / G7 解除前置。脚本真相源 `scripts/preflight-67atoms.cjs` 的 GAP 清单即权威。

### 已落地（7 个独立 handler，已部署+验收）
- 文件 `services/combat-service/src/services/combatCore/effectExecutor.cjs`：
  - 注册映射追加 7 键（collinear/charge_stack/cooldown/damage_share/armor_pierce/reflect_damage/prerequisite_clear）。
  - 新增 7 个 handler 实现（标准模式：`_resolveEffectTargets` + `_resolveValue` + `_ensureMeta` + 写 `u._meta`）。B4 用 axial 共线公式校验；B8 校验充能层数≥threshold；B10 查 `_meta.cooldowns`；G7 复用 `removed_requirements` 机制。
- 平衡敏感项（D4/D6/D7）**全部 JSON 字段驱动，绝不硬编码 skillId**，且带护栏常量：
  - D6 `armor_pierce`：handler 写 `u._meta.armor_pierce`（clamp≤0.8），**真正生效**在 `damagePipe.cjs` 阶段12 读 `defender._meta.armor_pierce` 减防（保留≥20% 护甲，防全穿透失衡）。
  - D4 `damage_share` + D7 `reflect_damage`：handler 写 `_meta` 标记（D7 guard 默认3层防递归），**派生逻辑**在 `skillExecutor.cjs` `_executeAttackSkill` 末尾算 `result.gap_ops`（damage_share 分摊给 ally 池 / reflect 反弹给 attacker 且 guard 自减），由 pure 模式 cloneFrame 或调用方消费，不直接闯入核心单位写回。
- 验收：node 直接 require effectExecutor 确认 7 键均 OK（handlers 总数 58）；buff 路径 simulate `gap_buff`（含7个GAP效果）返回 `triggered:True, err:None`，无 UNKNOWN_TYPE 崩溃。测试词条已 DELETE 清理。

### 留待专项（2 个，因涉及核心数值链平衡红线）
- **C5/C6 核心表达式 `__core_expr__`**：文档 `kind:'core'`，是 roll 求值器核心字段路由（让策划写表达式参与伤害/数值运算），**直接改伤害公式 = 高平衡风险**。按用户之前红线「涉及战斗平衡需确认范围」未擅自闯入核心数值链；仅做识别避免 Unknown，真实运算留作独立「核心表达式/基础技能归一化」专项，待用户确认范围后开启。

### 部署纪律实录
- combat-service 是 .cjs，gateway 用 createRequire 加载；改 .cjs 后**必须重建 gateway 容器**（`docker compose build --no-cache mecha-gateway && up -d --no-deps`）重启进程才会重新 require 最新 .cjs（gateway 自身 .ts 未改则不必 npm run build）。验证用 `node -e require('../services/combat-service/.../effectExecutor.cjs')` 直接 dump handlers 键。
- 验收用 deantest/7654321 取 token，POST /hub-config 写测试词条 → simulate-skill 跑 → DELETE 清理。

### 下一步
- C5/C6 核心表达式：独立专项，需用户确认是否接入核心数值链 + 表达式沙盒（变量白名单/运算符白名单）范围。
- 批次3 编辑器词库接入（67 原子字典暴露为可选词条）。
- 画面问题（WHEN 锚点/杂点/添加按钮）按约定最后调。

## 批次3：编辑器完整词库 & 模拟管道完全闭环（2026-08-11 深夜，用户拍板暂停 C5/C6）

### 用户拍板
- **暂停 C5/C6 核心表达式专项**：现有 58+ handler 已具 95%+ 表达力，C5/C6（策划写字符串公式求值）属极限元计算，非当务之急。优先把成果推向用户体验与可视化编排完全体。
- 直接切入【批次3：编辑器词库 & 模拟管道闭环】：让策划在 /glossary-studio 可视化配置 58+ 已落地原子 + 画布直接测真实伤害与涂抹效果。

### 3.1 原子字典校正（GlossaryHubNew.vue `atomGroups`）
- 7 个 GAP handler **以引擎真名对齐**（消除旧影子命名，避免 UNKNOWN_TYPE）：
  - B5 `collinear_adjacent`→`collinear`；B8 `has_not_acted`→`charge_stack`；B10 `limit_scope`→`cooldown`。
  - D4 `assist_choice`→`damage_share`；**新增 D6 `armor_pierce` / D7 `reflect_damage`**（原 D6/D7 顺延 D8/D9）；G7 `grant_revoke_action`→`prerequisite_clear`。
- 图例「前端缺失 ≈54」过时 → 改为真实计数 `engineHandlerCount`（ENGINE_HANDLERS 常量 58 项）+ `unknownAtomTypes` 校验 computed（原子 effectType 必须命中引擎真 handler）。

### 3.2 动态字段表单渲染（复核已闭环）
- 现有 `buildAtomField`/EFFECT_FIELDS/`defaultParamsOf` 已支持 select/bool/number(双模:固定/公式)/json/text 四类 + v-model 双向绑定，主链/分支/子树三层均已渲染。无需补。

### 3.3 字段级跳转↔返回状态机（核心新增）
- 复用现有 `viewLayer` 状态机（hexsix/range/aoe/mapcannon/battle）。
- 新增 `jumpField` ref + `JUMP_FIELD_MAP`（字段 key→目标画布视图+回填取值）：range/min_range/max_range/cast_range→range 视图；direction→range(方向)；shape/spread→aoe；length/width/mcShapes→mapcannon。
- 主链卡片字段循环加 🎯 跳转按钮（`isJumpField(f)` 判断），点击 `jumpToCanvas(atom,f)`：设 jumpField + 切视图 + 预填画布（range 用 `buildRingCells` 生成环、direction/shape/mcShapes 回填）。
- 顶部 sticky 气泡 `.jump-toast`（含「保存并返回」「取消」）：`saveJumpAndReturn()` 把画布 `rangeCtx` 当前几何精确回填该原子 `params[fieldKey]` 并标 dirty；`cancelJump()` 丢弃。
- 样式 `.jump-toast` + `.toast-pop` 过渡。

### 部署与验收
- 前端：rsync `--exclude node_modules --exclude dist` → 服务器 `npm run build`（成功，`GlossaryHubNew-HfwAB7UQ.js` 新产物）→ `docker compose build --no-cache mecha-frontend` → up -d。
- 验收（curl 8081）：`index.html` 主 chunk 懒加载 `GlossaryHubNew-HfwAB7UQ.js`；bundle 含新增文案「已对齐引擎」「保存并返回」；容器内 `/usr/share/nginx/html/assets/GlossaryHubNew-HfwAB7UQ.js` 哈希一致；容器 healthy。

### 下一步（用户原话：先批次3 完全体，再去 C5/C6）
- 浏览器人工实测 3.3 跳转气泡 ↔ 画布绘制 ↔ 保存返回回填（终端无法模拟 Canvas 拖拽/点击）。
- 画面美化遗留（WHEN 锚点/杂点/添加按钮）按约定最后调。
- C5/C6 核心表达式专项待批次3 验收后开启。

## /glossary-studio 画面布局重构：两栏→三栏（2026-08-11 深夜）

### 用户决策
- 词条列表（C 区 skill-panel）不动。
- **E 词条属性只读浮层并入左栏**（不再独立悬浮层覆盖画布）。
- **原子面板 C：定固定高度 + 面板内可滚动 + 分组默认收起**（点击组标题展开）。
- 画布 B + 六段编排 D 作为中栏主体（画布在上、D 在下，同屏）。

### 重构内容（GlossaryHubNew.vue，仅模板+CSS，组件未动）
- 原 `studio-upper`(列表|a|b|e) + `studio-lower`(d|c) 两段 → **三栏 flex `.studio-3col`**：
  - 左 `.studio-left`（132px 固定）：skill-panel（flex:1）+ entry-float（E 浮层，max-height:40% 可滚）。
  - 中 `.studio-center`（flex:1）：zone-a(视图按钮,改横向) + zone-b(画布,flex:1 自适应) + zone-d(六段编排,overflow-y 可滚)。
  - 右 `.studio-right`（220px 固定）：zone-c 原子面板（整体可滚）。
- `collapsedGroups` 默认 = 全组名（全收起）；`watch(atomSearch)` 有搜索词时自动展开全部组便于拖拽。
- `.atom-group` 加边框，`.grp-title` 可点击 `toggleGroup`（CSS `::before` ▸/▼ 旋转指示，去掉模板内 caret span）。
- `.gh-studio` 维持 min-height:760px；`.studio-3col` height:100vh 撑满视口。

### 部署与验收
- rsync（--exclude node_modules/.git/dist）→ 服务器 `frontend npm run build`（成功，`GlossaryHubNew-BNfJ1QPM.js`）→ `docker compose build --no-cache mecha-frontend` → up -d。
- 验收：容器内 `/usr/share/nginx/html/assets/GlossaryHubNew-BNfJ1QPM.js` 存在；线上直链 HTTP200；容器 healthy。
- 注：index.html 只引主 chunk（GlossaryHubNew 路由懒加载），`assets/GlossaryHubNew-*.js` 直链 200 即验收通过。

### 待人工实测
- 三栏在真实视口下的比例/滚动体验；原子分组收起/展开；E 浮层在左栏底部高度是否够用。
