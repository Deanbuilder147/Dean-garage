# 2026-08-12

## 词条库画布：射程回归纯区间模型（收敛多余落点层）
- 根因：早期"射程自由绘制"引入 `rangeCtx.cells` / `rangeCtx.aoe` / `rangeCtx.map` 落点数组，用户改输入框驱动后未清除，导致 AOE 视图出现整片淡蓝（0.22）落点层（来自"应用到 AOE"映射残留），被误判为错误高亮。
- 改动：
  - `HexCanvasEditor.vue`：AOE 分支删 `placeCells(rangeCtx.aoe)` 整片淡蓝层；射程分支删 `placeCells(rangeCtx.cells)` 自由绘制层；`rangeCtx` 默认去 `cells/aoe/map`，`aoeCenter` 初值改 `null`；`onUp/onRight` 不再在 range 视图自由绘格（仅 aoe 点选目标点、map 地图炮保留）。
  - `GlossaryHubNew.vue`：`rangeCtx` 去 `cells/aoe/map`；删 `onCanvasPlaceCell/onCanvasPlaceAoe` 及模板绑定；`currentRangeCells()` 删自由绘制优先分支；`applyRangeToAoe` 不再存 `cells` 落点（仅 center+spread）；`syncTargetToCanvas` 不再还原 aoe 落点层；`jumpToCanvas` 不再设 `rangeCtx.cells`。
- 现蓝色高亮只剩两种：AOE 目标点单蓝格(0.30) + 目标点周围攻击范围环带。射程=纯区间(min/max_range) 驱动环带，无落点数组。
- 部署：rsync→服务器 `npm run build`→`docker compose build --no-cache mecha-frontend`+`up -d --no-deps`。线上 bundle `GlossaryHubNew-fUufqCsi.js` 与 build 一致，无残留。入口 http://106.54.197.69:8081。

## AOE 映射补起始范围(min_range)
- 用户反馈："aoe只映射出了结束范围，没有映射出起始点"。根因：`applyRangeToAoe` 只把 `spread=maxR`，AOE 渲染用 `drawRingRange(aoeCenter, 0, aoeSpread)` 实心圆（内半径0）吞掉了起始范围。
- 修复：`rangeCtx` 新增 `aoeMinSpread`；`applyRangeToAoe` 写入 `d.aoe.min_spread = minR` + `rangeCtx.aoeMinSpread = minR`；`syncTargetToCanvas` 还原 `aoeMinSpread`；AOE 渲染改蓝色环带 `[aoeMinSpread, aoeSpread]`（内半径=起始范围，外半径=结束范围），不再用绿色 drawRingRange；图例与 tip 显示 `min~spread`。
- 语义：射程 `[min,max]` → AOE 攻击范围=以目标点为心的蓝色环带[起始~结束]，起始点显式可见。
- 部署：bundle `GlossaryHubNew-Bb_PEotg.js`，含 aoeMinSpread。

## AOE 中心=目标点（非单位本体）
- 用户反馈："蓝色环带的中心点是单位本体不是目标本体"。根因：`applyRangeToAoe` 把 `d.aoe.center` 写死成 `{9,9}`（单位本体），导致点开 AOE 时环带以单位本体为心；手动设目标点后 aoeCenter 更新才以目标点为心——语义混乱。
- 修复：`applyRangeToAoe` 不再写死 center（保留用户已设目标点，无则 null）；AOE 环带始终以 `rangeCtx.aoeCenter`(目标点) 为心，单位本体(9,9) 仅画琥珀棋子标记。AOE 输入条新增 `rib-warn`：未设目标点时提示"请先①设置目标点，环带以目标点为心"。
- 语义钉死：AOE 中心永远是"目标点"（射程内某点，玩家点选），单位本体只是"我在哪"的示意，绝不参与环带圆心。
- 部署：bundle `GlossaryHubNew-DCLxDmrr.js`。

## AOE 视图 = 射程区域（去掉独立的 target/spread 概念）
- 用户最终纠正（问题1：删"应用到 AOE"按钮）："你把射程映射给了攻击范围，那不是一回事"。用户语义——AOE 图上要展示的是与「射程」视图**完全一致**的区域（同一中心 9,9、同一 [min,max] 环带），只是换蓝色标注"这就是该技能的作用区域"。例如投掷词条射程 3~5，AOE 图也显示 3~5 同区域蓝色环带。
- 改动：
  - `HexCanvasEditor.vue`：AOE 分支改为调用 `resolveRange()` 取 `[minR,maxR]`，以 `UNIT_ORIGIN` 为心画蓝色环带（与射程视图区域一致，仅配色不同），不再有"目标点/以目标点为心"逻辑；删 `aoeMode` prop 与 `emit('select-aoe-point')`，点击 AOE 视图无意义（区域由射程驱动）；`rangeCtx` 默认去 `aoeCenter/aoeSpread/aoeMinSpread`。
  - `GlossaryHubNew.vue`：删 `applyRangeToAoe` 按钮与函数；删 AOE 输入条（选目标点/攻击范围）及 `onSelectAoePoint`；删 `aoeMode` ref；`d.aoe` 默认与互斥清零统一为 `{ sameAsRange: true }`（不再写 center/spread 脏值）；图例改"技能作用区域（= 射程区域 起始~结束环带）"。
- 语义钉死：射程视图与 AOE 视图是**同一份 [min,max] 区域**（同中心、同区间），区别仅配色（射程=绿/红盲区，AOE=蓝）。AOE 不再有独立 center/spread 编辑。
- 部署：bundle `GlossaryHubNew-LxiTdYHN.js`，含"作用区域"新文案，线上资产哈希一致。

## 射程 vs 攻击范围 权威定义（用户 2026-08-12 终审，推翻上一轮）
- 用户纠正：**射程 ≠ 攻击范围**，二者是两类正交概念，锚点完全不同：
  - **射程（cast_range / min~max）**：锚**自己(9,9)**。决定"我能把哪些格子中的某一个选作**目标落点**"——只有落在射程区间内的格才有资格被选为目标。
  - **攻击范围（AOE / 溅射 / aoe_radius）**：锚**已选定的目标落点**。决定"选定某格为目标后，该目标格**周围**多少格连带成为命中目标"。简化为一个「攻击半径」同心圆即可（不用 range_type 几何/地图炮/扇形）。
- 因此上一轮"删按钮、AOE图=射程区域"是错的，已还原：AOE 视图 = 画布「①设置目标点」(射程内点选，校验 dist∈[min,max]) → 以目标点为心、aoe.radius 攻击半径的蓝色同心圆环（含自己棋子(9,9)示意 + 淡绿射程可选区）。
- 数据模型收口：`d.aoe = { center:{q,r}|null, radius:N }`（删掉 sameAsRange/center=9,9 脏值）；aoe 默认 `{center:null,radius:0}`；地图炮互斥时清 aoe。
- 文档修正：`桌面/机甲战棋/项目资产/术语标准化方案.md` 第 287 行原本"攻击范围→射程"合并条目已废除，改为「射程」「攻击范围」两条独立 + 速查表后补正交定义说明；第 309 行"攻击范围模板"改"命中几何模板"。
- 部署：bundle `GlossaryHubNew-CGW8qb_K.js`，含"攻击半径""目标落点""攻击范围"新文案，线上资产哈希一致。

## 地图炮视图 = 射程环带（紫色版，同内容）
- 用户要求：把"射程画布用同样的方式同样的内容"布置到地图炮。改造 `HexCanvasEditor.vue` 与 `GlossaryHubNew.vue`：
  - `drawRingRange` 增加 `pal` 配色参数（盲区/可命中环带/环号/玩家棋子色），默认绿色不变。
  - map 视图分支：复用 `drawRingRange` 紫色 palette 画同样的 [min,max] 环带（中心 9,9、盲区、环号），保留 mcShapes 六向手绘层叠加。
  - `GlossaryHubNew.vue`：map 视图新增与射程相同的 min/max 输入条（双向绑定 target，@change=onRangeInput）；新增 map 专属图例（紫色：自身/盲区/可命中线）。
- 部署：bundle `GlossaryHubNew-VGhqEknG.js`，线上哈希一致、含"与射程同 [min,max] 环带"文案。

## 词条属性 vs 机体攻击值 / 时机闸门 语义澄清（用户提问）
- 优先级：词条 damage effect 配 `fixed_rate_multiplier(>0)` 或 `flat_value(≠0)` 时 → 覆盖 `legacyBase`（机体/武器攻击值，`skillExecutor.cjs:687-689`），`damagePipe.cjs:124-132` Segment `flat_value` 直接 `baseDamage` 覆盖、`base_attack_override=true` 跳过机体 attack。即词条属性 > 机体/武器原值。
- 条件 effect 未满足：带 `trigger.condition` 的 damage effect 被排除出 `dmgEffect`（`skillExecutor.cjs:681`），回退 `legacyBase`（机体原值）——用户确认此为期望行为（①）。
- 时机闸门（WHEN/IF/HP/地形/距离/充能）在 baseDamage 覆盖之前 `return {triggered:false}`（`executeUniversalSkill` 374-499 行），是硬短路，不结算则词条覆盖不执行。v2 IF 分支 vetoed 时外层 active 但分支内不结算。

## 子段/分段 定义 + ROLL 取代式改造方案（文档产出，未实施）
- 用户定义（已采纳）：
  - **子段**：作用于一个主六段中的某一段，用子段的计算/判定结果定义该段产生的值（或主段判定依据），继续完成主六段。代码载体=主链 `lane.nest`（「＋子段」）或任意段 `_enterChild` 子链。
  - **分段**（branch-segment）：通常复数出现（跟随 roll 字段），按掷骰落入的区间决定主六段接下来动作。代码载体=ROLL 段 `branches[]`，每块含完整 `subLanes` 六段子链。
- 现状调研：前端已就绪（分支块 `subLanes` 含独立 DO/COST，`branchToNode:1306` 序列化为段树 `children`）；差距在引擎 `_walkPhaseTree` 为**叠加式**（ROLL 命中分支后 `continue`，但主链 DO/COST:1494 仍照跑）。
- 用户选定 **(B) 取代式**：ROLL 有 branches 且命中 → 主链 DO/COST 由命中分支内的 DO/COST 取代；无 branches 或全未命中 → 主链兜底（推荐）。
- 改动量：引擎 `_walkPhaseTree` 加 `rollBranchHit` 标志 + DO/COST 段前置 `continue`（~20 行）；前端 `writeLanesToDraft` 可省（默认"有 branches 即取代"）；靶场日志提示 ~5 行；契约注释 ~10 行。v1 `_resolveRollSegments` 不动。总估 0.5~1 天，低风险。
- 文档：`/Users/dingxuyang/CodeBuddy/20260604120036/ROLL分段取代式改造方案.md`（含 D1 全未命中兜底、D2 是否逐词条切换两决策点，均推荐默认项）。

## ROLL 取代式改造【已实施并部署上线】
- 用户以"系统架构师"视角审查文档，确认精准，锁定 D1（空隙兜底主链）/D2（不加 replace_mode 开关，有 branches 命中即取代）默认项，并给出 3 条防线：①空块明确提示 ②rollBranchHit 必为 _walkPhaseTree 局部栈变量（多重 ROLL 递归隔离）③按 §7 顺序推进。
- 实施（`skillExecutor.cjs` `_walkPhaseTree`）：
  - 局部 `let rollBranchHit=false; const rollBranchHas={DO:false,COST:false}`（栈隔离，防线②）。
  - ROLL 段命中分支：置 `rollBranchHit=true` + 遍历 `b.children` 收集 `rollBranchHas[phase]`（用于空结算判断）。
  - DO/COST 段前置：`if(rollBranchHit){ rollBranchHas[phase]? 取代跳过 : 空结算提示 } continue`（防线①）。
  - 注：`node.children`（主链 DO 节点的子链）≠ 分支 DO，空结算判断必须用 `rollBranchHas`（从 `b.children` 收集），不能误用 `node.children`。
- 契约注释：`shared-kernel/src/contracts/phase-tree.contract.ts` 在 PhaseNode 后补 ROLL 取代式语义注释。
- 验证脚本（临时，已删）：直接 `_walkPhaseTree(tree, caster, target, dice, ctx, 0, {})` 跑 dice=2/5/7：
  - dice=2 命中暴击段 → `[段树:DO] 已由 ROLL 分段取代，跳过主链`；分段无 COST → 空结算提示 ✓
  - dice=5 命中空 DO 段 → `该分段无 DO 效果（空结算）` ✓
  - dice=7 空隙 → 主链 DO 兜底结算 ✓
- 部署：rsync 整目录 + gateway `npm run build`（tsc 干净）+ shared-kernel `npm run build` + `docker compose build --no-cache mecha-gateway && up -d --no-deps`。容器 `/app/services/.../skillExecutor.cjs` 含新逻辑（grep 2 处命中），health 200，线上 CGW 前的 gateway 已重建。**线上 GlossaryHubNew 未动，仅 backend 逻辑层改动。**

## 附加修复：_deepCloneUnit Scope 掉帧（休眠地雷）
- 用户以架构师视角指出：`skillExecutor.cjs:1366-1367` 在 `pure:true` 分支裸调 `_deepCloneUnit(unit)`（无 `this.`），CJS 模块下会 `ReferenceError` 崩溃。此前未暴雷纯因普通战斗（非 pure）不走该分支。
- 修复：`pure ? this._deepCloneUnit(unit) : unit` 与 `this._deepCloneUnit(target)`（1366-1367 两处）。已随 ROLL 取代一并部署，容器内 grep `this._deepCloneUnit` 命中 2 处，health 200。
- 教训：类方法在类内部必须 `this.` 调用，不能依赖模块作用域（除非确为模块级函数）。纯函数/纯计算模式下这类休眠路径尤需靶场 pure 用例覆盖。

## 词条库页面清理：删 VisualGlossaryView / 标记 GlossaryView 应急页
- 用户要求防止后续误改旧编辑器。改动：
  - **删除** `frontend/src/views/VisualGlossaryView.vue`（旧 `/glossary-editor` 可视化编辑器），并在 `main.js` 移除其静态导入（原行16）与 `/glossary-editor` 路由（原行46）。
  - **标记应急页**：`/glossary`（GlossaryView）路由 meta 加 `emergency:true` + `emergencyNote:'词条库应急页面，暂不维护'`；组件顶部加红色横幅「⚠ 应急页面 · 暂不维护（旧版词条库中枢 v5.0 备份）。新词条库请前往 /glossary-studio」+ `.emergency-banner` 样式。
  - `JsonCompilerPanel.vue` 过时注释改为「原 VisualGlossaryView 已废弃删除」。
- 真相源钉死：词条库新页面 = `/glossary-studio` = `GlossaryHubNew.vue`（首页卡片「📖 词条造词工厂」进入）。旧记忆"侧边栏词条库→/glossary-hub"已不准确——**项目不存在 /glossary-hub 路由**，导航入口只有首页卡片。
- 部署：rsync→服务器 frontend `npm run build`（200 模块无报错）→`docker compose build --no-cache mecha-frontend`+`up -d --no-deps`。验收：8081 `index-Cc5R22IA.js` 哈希一致、「应急页面」文案在线、`glossary-editor` 路由已从 bundle 移除。

## 分段（branch-segment）内部展示改造：复用主六段标准泳道卡
- 用户诉求：分段（分支）内部六段子链要用**正常的原六段样式**显示，不做压缩小栏。（子段 subtree 不动。）
- 改动 `GlossaryHubNew.vue`：
  - `ensureBranchSubLanes` + `addBranch`：给 `subLanes` 项注入 `ord: idx+1` 与 `desc`（取自 LANE_SKELETON），供标准 `lane-head` 使用。
  - 模板（分支内部 312-340）：`branch-lanes > branch-lane(branch-lane-head 文本标题)` → `branch-lanes > lane-col.branch-col > lane`（复用 `lane-head` 圆 ord + 段名 + `lane-body` 原子卡 + 拖拽态）。
  - 样式：`.branch-lanes` 改为 `display:flex` 横向排列，`branch-col` 宽 200px flex-shrink:0，分支内隐藏 `.lane-head .desc` 仅留段名省空间；删除旧 `.branch-lane/.branch-lane-head`。
- 部署：同上纪律。验收：8081 `index-CiEi1KAa.js` 哈希一致、`branch-col` 类在线、`branch-lane-head` 旧类已从 bundle 移除。

## ROLL 字段定义驱动自动生成分段（取代手动 addBranch）
- 用户诉求：roll 字段设置后，按定义自动生成不一样数量/区间的分段，不再手动 addBranch。
- `GlossaryHubNew.vue` 新增三函数：
  - `makeBranch(lower, upper, label)`：构造标准分段（注入六段 subLanes 骨架）。
  - `generateRollBranches(atom)`：读 `atom.params` 三种语义生成 specs → branches：`roll_segments`(sides+threshold 二分 2 段)、`roll_segments_m`(sides+segment_count 等分 N 段)、`roll_segments_color`(mapping 文本解析区间+标签)；其它默认 1 段 [1..sides||6]。保留旧分支 atoms（按区间中点落入迁移）。
  - `regenRollBranches(a)`：ROLL 原子参数变更触发重算并 `syncLanesToDraft`。
- 触发点：①`buildLaneAtom` 构造 roll_segment 后 `inst.branches = generateRollBranches(inst)`；②主段原子卡 select/number/input 的 `@change` 在 `lane.key==='ROLL' && effectType==='roll_segment'` 时调 `regenRollBranches(a)`；③`confirmMultiInstance` 对 roll_segment 直接 return（不叠空分支，定义已生成）。
- 部署：同上纪律。验收：8081 `index-Bay8z0ot.js` 哈希与 build 产物一致、200 健康。注：Vite 生产 minify 会压缩函数名，grep 函数名为空属正常，以资产哈希匹配为准。

## Roll 类原子移除"成功阈值"选项
- 用户意见：roll 类原子不需要"成功阈值"选项。
- `GlossaryHubNew.vue`：`C2 单数值阈值二分`→改名「C2 单数值二分」，删除 threshold 字段（仅留 method+sides）；`generateRollBranches` 的 `roll_segments` 分支去掉 threshold 依赖，改按骰面数自动中分 `floor(sides/2)` 生成「低区/高区」两段。
- 部署：同上纪律。验收：8081 served `GlossaryHubNew-Bmrxsn8F.js` 与 build 一致、「成功阈值」文案已从 bundle 移除、200 健康。

## 前端六段编辑器多项交互改造（同日累计，最终 build GlossaryHubNew-2nuYA6Jd.js）
1. 分段（branch）内部复用主六段标准泳道卡（lane-col > lane），分支内隐藏 desc 省空间。
2. ROLL 字段定义驱动自动生成分段：generateRollBranches/regenRollBranches；C2 改名「单数值二分」移除成功阈值(threshold)，改按骰面数中分。
3. 伤害模式改中文：flat/override/multiplier → 固定值/覆盖/倍率（值仍英文，后端兼容）；select 渲染统一支持 {label,value} 对象选项。
4. **动态增删段**：泳道区末尾「＋ 添加段」；段头「✕」删除（WHEN 保护）。写回用 lane.key 保真；动态段 dynamicCoord 外环坐标。
5. **添加段改为下拉菜单**（用户最终要求）：移除内联手填表单，点「＋ 添加段」展开 select，预设 ADD_LANE_PRESETS = 附加段(EXTRA)/反应段(REACT)/触发段(TRIGGER)/条件段(COND)/自定义段(CUSTOM)，选中即加（key 唯一校验）。
- 关键约束（务必告知用户）：前端可加任意段，但后端 `_walkPhaseTree` 只识别 WHEN/IF/WHO/ROLL/DO/COST 六语义，自定义段(EXTRA等)当前不参与战斗结算；且 `initLanesFromDraft` 回读仅还原固定 6 段（自定义段存后再打开会丢，需后续扩展回读保真）。
- 部署纪律全程：rsync → 服务器 frontend `npm run build` → `docker compose build --no-cache mecha-frontend && up -d --no-deps`。验收以 served 懒加载 chunk 资产哈希/大小与 build 产物匹配为准（入口 index.html 不含懒加载 chunk 名，须直接 fetch /assets/GlossaryHubNew-*.js 校验）。

## 词条库画布瞬移/被裁切 + 侧栏过高 布局修复
- 根因：`.studio-center`(overflow:hidden) 垂直堆叠 `.studio-zone-d`(lanes, `flex:1 1 48%`) 与 `.studio-zone-b`(画布, `flex:1 1 48%`)。点击词条前 `HexCanvasEditor` 未挂载（v-if draft），点击后挂载并要求内部 canvas≥360×320px（`Math.max(360/320, rect)`），但 48% 槽位在 4:3 屏常 <320px → canvas 超出 `overflow:hidden` 的 `.zb-canvas-wrap` 被裁到画面外（"瞬移下去"）。
- 改动：
  - `GlossaryHubNew.vue`：`.studio-zone-b` 由 `flex:1 1 48%` 改为 `flex:0 0 420px; min-height:420px`（固定画布槽，永不被挤到画面外）；`.studio-zone-d` 改 `flex:1 1 auto; min-height:0`（占剩余并滚动）；`.zb-canvas-wrap` 去 `min-height:160px` 改 `min-height:0`（撑满 420 槽）。
  - `HexCanvasEditor.vue`：`syncCanvasSize` 下限由 `Math.max(360/320,...)` 降到 `Math.max(220/200,...)`，避免小容器下画布比容器还大导致溢出。
- 侧栏"太高"：经核查 `.studio-skill-panel`/`.studio-zone-c` 早已内部滚动（SkillListPanel `.slp-list`、atom-groups 等），无额外改动；视觉失衡主因即画布被裁，已随本修复消解。
- 部署：rsync→服务器 frontend `npm run build`→`docker compose build --no-cache mecha-frontend && up -d --no-deps`。验收：HTTP 200，`GlossaryHubNew-BSh6Xv8n.css` 含 `studio-zone-b[data-v-33830462]{order:2;flex:0 0 420px`，`GlossaryHubNew-CWHVWABz.js` 为最新 chunk。
