# 2026-08-14 工作日志

## Godot 战棋迁移（alpha4.0，路径 /Users/dingxuyang/Desktop/wggodot/alpha4.0）

### Phase 2 完成（上午）
- 坐标真相统一：Battle3D 删内联 `pointy_top_center`(HEX_RADIUS=36/SQRT3) 与 `_screen_to_hex` 逆推(36*1.732)，改调 `HexMath.pointy_top_center` / `HexMath.pixel_to_hex`（新增于 hex_math.gd，与 pointy_top_center 严格互逆）。命中错位根因 36*1.732=62.35≠64 已消除。
- AOE 预览固化：`highlight_renderer.gd` 重写为纯本地坐标 + `draw_aoe`(暗红)/`draw_skill`(琥珀)/`draw_move`(青绿)；Battle3D 三层高亮统一经 `HighlightRenderer`（加 `var _hl`），删 67 行内联 Sprite3D 高亮。
- 验证：`godot --headless --editor --quit` 无 error/parse。

### Phase 3 完成（下午）
- 范围：Phase 3 计划(EventBus 信号总线)核心已在 Phase 1 落地；本批次补完"棋盘渲染收口到 BoardRenderer"。
- 几何真相提升：`hex_math.gd` 加 `TILE_THICKNESS=8`/`COLUMN_HEIGHT=10`/`UNIT_FLOAT_Y=24`；Battle3D 本地常量改引用 HexMath。
- `BoardRenderer` 重写为真实棋盘渲染器（薄砖几何对齐 Battle3D 既有视觉、贴图 `assets/map/T-01_*.png`、暴露 `get_tile(q,r)`）。
- Battle3D 删内联 `_rebuild_board`/`_add_ground_plane`/地形表/`_get_terrain_texture`/`_terrain_color`/`_tile_map`（≈124 行），`set_battle_state` 改 `_board.update(state, Vector3.ZERO)`，居中由 `_board_root.position` 承担（避双重偏移）。
- 验证：`--editor --quit` 通过（单文件 `--check-only --script` 对跨脚本 const 报局限，已把 TEX_LIFT 改 var 规避）。

### 渲染三权分立现状
- 棋盘 BoardRenderer(Phase3收口) / 高亮 HighlightRenderer(Phase2收口) / 单位 unit_renderer.gd(Phase1建，未接入) / 状态入口 EventBus.battle_state_updated(Phase1)。
- 剩余内联：`Battle3D._rebuild_units()` 待 Phase 4 收口到 unit_renderer.gd。

### 关键坑（Godot GDScript）
- 本机 Godot 4.7.1 在 `/Applications/Godot.app/Contents/MacOS/Godot`，可直接跑 `godot --headless --editor --quit` 项目级校验（之前误以为无二进制）。
- **预存 blocker（Phase 4 修复）**：`core/event_bus.gd` 声明 `class_name EventBus`，与 `project.godot` `[autoload]` 同名注册冲突，Godot 4.7.1 报 "Class 'EventBus' hides an autoload singleton" → 脚本编译失败 → 全局 EventBus 单例无法建立 → 所有引用方（Battle3D:128/325）连锁报 "Cannot find member battle_state_updated"。**修复：删 `class_name EventBus`**（autoload 注册已提供全局名）。
- 单文件 `godot --headless --check-only --script X.gd` 不加载 autoload/跨脚本类型，会误报 EventBus 信号未找到、跨脚本 const 引用错误。正确校验用 `godot --headless --editor --quit`（加载完整 project.godot）再 grep error/parse。
- const 初始化不能引用跨脚本 const（部分版本敏感），运行时计算用 var。

### Phase 4 完成（晚上）单位渲染收口
- `unit_renderer.gd` 重写（原 phase-1 半成品 `extends RefCounted`/`static build`/`world_pos` 取 `unit["q"]` 错误、`elevation*10+6` 高度不对齐）：改 `extends Node3D`，坐标走 `HexMath.pointy_top_center`、高度 `HexMath.UNIT_FLOAT_Y`（几何真相一致），七视图降级链+矢量圆、按 size 设 `world_h`/`pixel_size`，`billboard=ENABLED`（对齐原视觉）。
- 接口：`update(units, center_offset)` / `get_unit_node(id)` / `find_unit_at(q,r)` / `set_unit_selected(id,on)`（金色环描边；取消时从 `_unit_data` 缓存重绘底图）。
- Battle3D 删内联 `_rebuild_units`/`_make_unit_texture`/`_unit_sprites`（≈70 行），`set_battle_state` 改 `_unit_renderer.update(_unit_list, Vector3.ZERO)`（居中由 `_unit_root.position`），`_find_unit_at` 经 renderer 反查 id。`UnitRenderer` 挂 `UnitRoot`。
- **渲染三权分立全量收口**：棋盘 BoardRenderer / 高亮 HighlightRenderer / 单位 UnitRenderer / 状态入口 EventBus，控制器零 Mesh/Sprite3D 创建副本（宪法 §1 达标）。
- 验证：`--editor --quit` 无 SCRIPT/Parse Error（`scenes/Battle.gd` File-not-found 为预存场景引用缺文件，与渲染无关）。文档 battle-migration-plan.md 追加 5.5。

### Phase 4 技能系统链路核对（更晚）
- **结论：技能闭环代码已存在且完整**，本次为核对+修复+契约验证（非新建）。
- SkillMenu 已挂载在 Battle3D.tscn（`UILayer/SkillMenu`+`SkillList`）；`_select_unit`→`_build_skill_menu(unit.skills)` 动态生成按钮。
- **纠正旧记忆**：demo 单位**并非** skills:[]——`_ensure_demo_battle` 调 `_demo_skills_u1()`（`T.unit_skill`：集束炮击/强化力场/地图炮，含 id/shape/aoe_radius/target_filter/range）写 `DEMO_U1["skills"]`，菜单有真实可点技能。
- 链路：点单位→弹技能→`_enter_skill_targeting`→`HighlightRenderer.draw_skill`(射程环+阵营过滤)→Hover `_update_aoe_preview`(`HexMath.aoe_cells_from_skill`+`HighlightRenderer.draw_aoe`)→点目标 `_cast_skill_at`(`UnitRenderer.find_unit_at`)→`POST /attack`→`_on_skill_ok`→后端 `battleStore.set`→comm WS `/ws-native` 推送→`WsClient`→`EventBus.battle_state_updated`→`set_battle_state` 重绘。
- **修复**：`_update_aoe_preview` caster 坐标原取 `_selected_unit.get("q")` 恒为0（真实在 `position.q`），改读 `position.q/r`。
- **后端契约核对**：`POST /attack` 必填 `attacker_id`；Godot payload 已对齐（`attacker_id/attack_type:"skill"/skill_id/target_id/target_pos/aoe_shape`）。实测 `curl POST /attack -d '{}'` → 400 `VALIDATION_ERROR: attacker_id 为必填项`，证明路由存活+字段对齐。无 skill_id 的 ranged/melee 后端返 400 `SKILL_REQUIRED`（普通攻击已删除红线）。
- 验证：`--editor --quit` 无 SCRIPT/Parse Error。文档追加 5.6。

### Phase 5 完成（深夜）回合/AP/状态机 HUD
- **后端真相源**：`battle_state` 顶层 `round`/`activeFaction`(轮转角色 attack/defense/ambush)/`factionTurnOrder`/`factionRoles`(faction→role)；单位 `action_points:{MOVE,ATTACK,DEFEND}`/`statusEffects:[{type,duration,remainingTurns}]`。`end-turn` POST /api/combat/:id/end-turn（api.post_battle_action 已封装），WS `battle_state_updated` 推送全量刷新（含新一轮 resetAllActionPoints）。
- **5.1 回合 HUD**：tscn 新增 `TurnHUD`(顶部居中 RoundLabel/TurnLabel/TurnHint, mouse_filter=IGNORE 穿透)；`_refresh_turn_hud(state)` 呈现 回合/行动方/我方回合提示。
- **5.2 AP 置灰**：`_build_skill_menu` 读 `_selected_unit.action_points`，MOVE≤0 禁移动、DEFEND≤0 禁防御、攻击类(target_filter==enemy 或 effect 含 attack)耗 ATTACK、辅助类耗 DEFEND，按钮带 `AP n` 并 disabled。
- **5.3 结束回合**：tscn `EndTurnButton` 锚点改右下角(preset=3)；`_is_my_turn` 用 `factionRoles` 把我的单位 faction 映射为 role 比 `activeFaction`（红线：role 是唯一判定依据，禁直接比 faction），对手回合禁用按钮，WS 推送自动重置。
- **5.4 状态效果**：`unit_renderer.gd` 新增 `_attach_status_icons`——单位 Sprite3D 头顶挂 billboard 小图标(buff绿/debuff红/neutral蓝，按 type 关键字归类)，作为 sp 子节点随 `update()` 重建自动清理；纯渲染消费 `unit.statusEffects`，不订阅 EventBus。
- **关键坑**：当前真实 Battle3D.gd **无** `_skill_menu_open` 变量（菜单开合用 `skill_menu.visible` 判），set_battle_state 引用它会导致 Parse Error；AP 重挂用 `skill_menu.visible`。
- 验证：`--editor --quit` 无 SCRIPT/Parse Error。文档追加 5.7。

### 文档核对（阶段 5 进度 + 待确认）
- 主计划 `docs/battle-migration-plan.md` 阶段 5 子项已更新勾选：5.1(HUD)/5.2(结束回合)/5.3→落地称5.4(状态图标) 完成；5.0(显式状态机 BattleStateMachine) 未完成。

### 用户架构决策（Phase 5 推进指引，2026-08-14）
- 阻塞项1：**自写轻量 FSM Node**（拒绝 gd-YAFSM 插件）；状态枚举 IDLE/UNIT_SELECTED/SKILL_TARGETING/ACTION_ANIMATING/ENEMY_TURN/ROUND_SETTLING。
- 阻塞项2：**立刻查后端 /attack payload**；结论见下。
- 阻塞项3：机动/掩护/ZOC 差值前端不阻塞，预留 HUD 占位。
- 阻塞项4：EventBus 严格用 core/enums.gd 的 TIMING，拒收非法别名。
- 阻塞项5：statusEffects 剩余回合角标 = P2 增强。

### 第一步：后端 /attack payload 核查结论（关键事实）
- `POST /api/combat/:battleId/attack` 200：`combat_result:{triggered,final_damage,dodged,counter_triggered,counter_damage,attack_type,attack_stat,damage_kind,dice,message,formula,sizeBanner,sizeTactic}`；附 `reaction_events[]`/`reaction_log[]`/`victory`。
- ⚠️ **`is_crit` 缺失**：skillExecutor 产 `result.is_crit`（combat.ts:2699 战报引用），但 /attack 未透传进 combat_result → 前端无法判定暴击。已降级。
- ⚠️ **`killed` 无显式字段**：靠 WS 全量 battle_state 中 target.hp 归零推断。
- ✅ **WS 推送 = 纯全量快照**（combatSnap.toPlainState 仅导 PLAIN_TOP_KEYS+units+map，无增量 events）→ 伤害飘字必须以 HTTP 响应 combat_result.final_damage 为唯一驱动，不靠前后帧差值。

### 第二步：落地（Godot 端）
- 新建 `core/battle_state_machine.gd`（自研 FSM，纯逻辑 Node，State 枚举 + transition + 输入谓词 + state_changed 信号）。
- `Battle3D.gd`：实例化 battle_sm + 连接 state_changed；_select_unit→UNIT_SELECTED、_enter_skill_targeting→SKILL_TARGETING、_cast_skill_at→ACTION_ANIMATING、_clear_selection→IDLE、end-turn→ROUND_SETTLING、WS 推送 _refresh_turn_hud 校正 ENEMY_TURN/UNIT_SELECTED。_unhandled_input 左键加 `battle_sm.input_locked()` 拦截。
- 伤害飘字：`_spawn_damage_float_from_result` 读 combat_result.final_damage，Label3D 挂单位节点头顶 Tween 上移淡出；dodged→"MISS"。
- 校验：`--editor --quit` 无 SCRIPT/Parse Error。phase-5 报告 §5/§6 已更新勾选 + payload 结论。
- 下一待补（不阻塞）：击杀特效；statusEffects 回合角标；阶段6 差值。

### 暴击 is_crit 补齐 + 部署（收官，2026-08-14）
- 后端 `backend-gateway/src/routes/combat.ts` 组装 combat_result 加 `is_crit: !!result?.is_crit`（2718 行后）。
- 部署：rsync → 服务器 `cd /root/mecha-universe-engine/backend-gateway && npm run build`（tsc 干净 BUILD_EXIT=0）→ `docker compose build --no-cache mecha-gateway && up -d --no-deps mecha-gateway`（容器内 dist 已含 is_crit×2，容器 Recreated）。
- Godot `Battle3D.gd` `_spawn_damage_float_from_result` 读 `cr.get("is_crit")`，`_spawn_damage_float` 暴击：文本 `CRIT! -N`、字号 32→45(×1.4)、橙红 #ff3b30、Tween TRANS_BACK/EASE_OUT 弹性冲顶回弹+淡出；MISS 灰青；普通淡黄白匀速上浮。
- 校验：godot `--editor --quit` 无 SCRIPT/Parse Error。**Phase 5（状态机+伤害结算+暴击飘字）收官**。
- 登录 smoke：测试号 deantest/7654321 拿 token 成功(279)；`/api/combat/battles` 端点不存在（路由名不同，未深究——真实结算链路需多人对局，Smoke 以编译+部署落地为准）。
- **编号错位**：文档原 5.0/5.1/5.2/5.3 ↔ 落地 5.1/5.2/5.3/5.4；落地 5.4=文档原5.3。
- `docs/phase-plans/phase-5-回合状态机与伤害结算.md` 把「伤害结算」也纳入阶段5，验收项全未勾选（伤害飘字/击杀特效/阶段6差值HUD/枚举收口）。
- **下一步前须确认的阻塞项**：①5.0 显式状态机是否引入 gd-YAFSM 或自写 FSM（进 AI 回合前必做）；②后端 skill_cast_result payload 字段名未确认（_on_skill_ok 未消费伤害明细）；③阶段6机动差值后端未注入（无字段）；④TIMING 枚举三头分化约15处未对齐（EventBus 信号须⊆enums.TIMING）；⑤statusEffects 图标仅着色无数字回合标签（可选项）。

### 后续待办（未做）
- 反应系统三头分化残留（reactionHandlers key / validPhases / GATEWAY_KNOWN_TRIGGERS 约15处未对齐 TIMING 真相源）。
- 技能动画（Tween 经 UnitRenderer.get_unit_node 平滑过渡）；Buff 文本明细悬浮（当前仅图标色，无数字回合标签——可选增强）。

### Phase 6 完成（深夜）敌方 AI 回合表现层全链路打通
- **范围决策**：用户确认前后端一起做。后端/end-turn 此前完全没调用 AI 引擎（旧 aiEngine.cjs 依赖的 combatCore 接口与网关不匹配，硬接会冲突门控），故**不接旧 AI 引擎**，改为 gateway 内自研 `runAiTurn(battle, role)` 复用内部 `runAttackInternal`(force+skipConsume 绕过门控)/`tsFindPath`/`resolveRole`/`getRoleUnits`/`evaluateVictory`/`advanceTurn`。
- **触发链**：createBattleState 注入 `aiRoles`（默认 = factionTurnOrder 除首角色外的全部，即非玩家先手=AI）；end-turn 切到 AI 角色后 fire-and-forget `runAiTurn`（不阻塞 HTTP）；每步 `battle.aiActions.push({type,unitId,from,targetId,skillKey,combat_result})` + `battleStore.set` 推全量快照；runAiTurn 末尾 `advanceTurn` 推进到下一角色并**链式触发**下一个 AI 角色（depth≤40 防死循环，胜利则 clearBattleSnapshot）。
- **协议**：shared-kernel `BattleState` 加 `aiRoles?`/`aiActions?:AiActionEntry[]`（AiActionEntry={type,unitId,from,to?,targetId?,skillKey?,combat_result?}）。WS 仍走全量快照通道（pushBattleState 推整个 battle），不新增 comm 协议——aiActions 作为快照上的增量数组由客户端增量消费。
- **客户端 Godot（/Users/dingxuyang/Desktop/wggodot/alpha4.0）**：
  - tscn 新增 `UILayer/EnemyTurnBanner`(CenterTop 锚点1)+`BannerLabel`("敌方行动中…")。
  - `Battle3D.gd`：`_show_enemy_banner`/`_hide_enemy_banner`（Tween 呼吸 `set_loops(-1)` alpha 0.55↔1.0 脉动；隐藏时 kill breathe tween 避免抢 alpha）；`_refresh_turn_hud` 敌方分支显 banner/我方隐 banner。
  - `set_battle_state`：update 前记录敌方单位旧世界坐标 → update 后对新位置变化的敌方单位 Tween 补间(CUBIC/IN_OUT 0.45s)；`_consume_ai_actions(state)` 增量消费 aiActions 的 attack 项播受击飘字（`_spawn_damage_float_from_result` 读 `targetId`/`combat_result.is_crit`，与玩家攻击同源），`_ai_actions_consumed` 游标防重复，`aiActions.size()<游标` 时重置（新 AI 回合）。
- **GDScript 坑**：for 循环变量前不能写 `var`（写 `for i` 非 `for var i`）；Tween 无限循环用 `set_loops(-1)`（0=执行一次）。
- **部署**：shared-kernel `npm run build`(ESM+CJS) + backend-gateway `npm run build`(tsc 干净) → rsync → `docker compose build --no-cache mecha-gateway && up -d --no-deps` → 容器内 dist 含 runAiTurn×12 命中，/health 200。
- 校验：godot `--editor --quit` 脚本错误数 0。**Phase 6 敌方 AI 表现链路收官**（HUD呼吸提示 + 敌方平滑位移 + 受击飘字）。
- ⚠️ AI 攻击仅选单位 skills 里第一个 attack_type/action_type=='attack' 的技能；若无则取 skills[0]。多技能/走位精度为后续增强点。

## 工作区同步 + Phase 盘点（2026-08-14 收尾）
- **同步**：Godot 工程 `/Users/dingxuyang/Desktop/wggodot/alpha4.0` 已 rsync 到工作区 `/Users/dingxuyang/CodeBuddy/20260604120036/apps/godot-alpha4.0/`（rsync -a --delete 双向可重建）。桌面仍为真源，工作区为镜像副本用于对照/版本管理。
- **Phase 落地核对**：Phase2(坐标真相)/3(BoardRenderer)/4(UnitRenderer)/5(状态机+伤害结算+暴击飘字)/6(AI回合表现层) 全部代码已落地，godot `--editor --quit` 0 错误，后端 tsc 干净部署 /health 200。
- **三头分化状态**：TIMING 枚举三头（shared-kernel/enums.ts ≡ Godot core/enums.gd ≡ 引擎 trigger）已对齐；triggerMap.ts(EDITOR↔ENGINE 1:1) + sync-enums.mjs 同步机制已建。reactionHandlers 自检(register vs gatewayKnownTriggers)已就位——分化残留主要在 EFFECT_TYPE/trigger 实际 handler 收口（Phase C/D 范畴，非阻塞）。
- **原"5 个待确认阻塞项"结论**：①自研 FSM 已建(BattleStateMachine.gd, IDLE/UNIT_SELECTED/SKILL_TARGETING/ACTION_ANIMATING/ENEMY_TURN/ROUND_SETTLING)；②/attack payload 已核查(combat_result.is_crit 已透传)；③机动差值后端无字段(留 HUD 占位)；④TIMING 已对齐；⑤statusEffects 仅图标色无数字角标(可选增强)。
- **Phase 7+ 规划方向**：①AI 智能增强(多技能选择/走位精度/击杀特效)；②statusEffects 回合角标数字；③技能动画平滑过渡(经 UnitRenderer.get_unit_node)；④Phase C/D 反应系统 handler 收口；⑤七视图/方向工坊等前端未纳 monorepo 的镜像同步校验。

## Phase 7.1 落地（2026-08-14）：AI 智能增强 + 击杀表现
- **后端 (combat.ts)**：
  - `runAttackInternal` 写回 hp 后注入 `result.killed`（单目标：final_damage>0 且 hp<=0；多目标：每 target 注入 `t.killed`，汇总 `result.killedTargets[]`，主目标 hp<=0 置 `result.killed`）。
  - `/attack` 响应 `combat_result` 新增强透传 `killed` / `killed_targets`。
  - 新增 `aiPickAttackSkill(battle,unit,nearest)`（Phase 7.1 策略优选）：对每个 attack 类技能按【射程覆盖+2 / AOE覆盖≥2敌+3 / 预估伤害/10】打分取最优；无 attack 技能兜底取 skills[0]；用 selfRole 判定敌我（修掉未定义 `role` 变量）。`runAiTurn` 改用该优选并透传 killed 到 aiActions.combat_result。
- **前端 (apps/godot-alpha4.0, 真源桌面)**：
  - `render/unit_renderer.gd`：新增 `_dying_nodes` + `detach_for_death(unit_id)`（摘节点托管）+ `_release_dying(unit_id)`（queue_free）；`_clear_children` 跳过 `_dying_nodes` 中的节点（避免 update 全清误毁离场动画）。
  - `scenes/Battle3D.gd`：新增 `_play_kill_effect(unit_id)`（detach→闪白→震动8帧→红色爆裂 scale*1.6+alpha渐隐0.4s+上浮→_release_dying）；`_spawn_damage_float_from_result` 检测 `cr.killed` 即触发（玩家 `_on_skill_ok` 与 AI `_consume_ai_actions` 共用入口，双路径均覆盖）。
- **部署**：shared-kernel `npm run build`（AiActionEntry 此前未进 dist 致 gateway tsc 报 aiRoles/aiActions 不存在，rebuild 后干净）→ gateway `npm run build` 干净 → rsync 整目录(含 dist) 到服务器 → `docker compose build --no-cache mecha-gateway && up -d --no-deps` → `/health`=200，容器内 dist 含 `aiPickAttackSkill/killedTargets`(6处)。Godot 校验：Battle3D.gd/unit_renderer.gd 0 错误（遗留 `res://scenes/Battle.gd` 文件未找到为 Phase3/4 重构历史引用，非本次引入）。
- ⚠️ 注意：改 shared-kernel 后**必须**先 `npm run build` 出 dist，否则 gateway tsc 仍读旧类型报 aiRoles/aiActions 缺失。

## Phase 7.2 落地（2026-08-14）：statusEffects 数字角标 + 施法位移动效
- **纯前端改动（apps/godot-alpha4.0，真源桌面），无需后端部署**（statusEffects.duration 后端 Phase6 已写回）。Godot 校验 Battle3D.gd/unit_renderer.gd 0 错误；修正一处位移距离 bug（dir 已 normalize 后 length=1，改为先存 dist 再按比例）。
- **② statusEffects 数字角标（unit_renderer.gd `_attach_status_icons`）**：每个 Buff/Debuff 图标下叠加 `Label3D`（文本=剩余回合 `duration` 优先 / `remainingTurns` 兜底），`outline_size=3`+`outline_modulate=黑` 底衬描边，`billboard` 面向相机，作图标子节点。`_status_icon_texture` 本身仍只画着色圆+白描边（数字走独立 Label3D，更清晰）。
- **③ 施法位移动效（Battle3D.gd `_play_cast_dash`）**：复用 Phase7.1 的节点托管思想——新增 `UnitRenderer._animating_nodes` + `detach_for_anim(unit_id)`（与 `detach_for_death` 对称），`_clear_children` 跳过 `_animating_nodes`，动画结束 `queue_free`（下次 update 自然在正确格重建），彻底避免 WS 刷新 update 重建打断/误清位移动画。
  - 近战（Melee）：向目标突进 60% 距离（CUBIC/OUT 0.14s）→ 弹性回弹归位（ELASTIC/OUT 0.32s）。
  - 远程（Ranged）：后座 25% 距离（CUBIC/OUT 0.12s）→ 回弹归位（CUBIC/OUT 0.22s）。
  - 玩家路径：`_cast_skill_at` 记录 `_cast_attacker_id/_cast_from_pos/_cast_to_pos/_cast_is_ranged`（按 skill.category/type 含 melee 判定近战），`_on_skill_ok` 飘字后调 `_play_cast_dash`。
  - AI 路径：`_consume_ai_actions` attack 分支从 `act.from`(hex) 与 state.units[targetId].position 算世界坐标，默认近战突进调 `_play_cast_dash`。
- 表现层二连击完成：战场从"纸片对撞"升级为带击杀离场+状态角标+施法位移回弹的动作战棋。后续④反应系统 Handler 收口 / ⑤前端 CI 校验待合拢后攻坚。

## Phase 7.3 落地（2026-08-14）：反应系统 Handler 收口（三头分化消除）
- **核查结论颠覆旧 memory**：反应系统并非"各自为政"，已有成熟架构——引擎 `reactionHandlers/index.cjs`(注册表+自动加载+`selfCheck()`自检对照网关点火清单) + 网关 `triggerMap.ts`(EDITOR↔ENGINE 1:1) + `COMPOSITE_TRIGGERS`(复合触发)。旧 memory「三头分化残留」已过时（timing 维度在 Phase 6 已对齐）。
- **实测真实分化 bug（2 处）已修复**：
  1. **`on_target_selected` 确定性覆盖 bug**：`duel.cjs` 第74-77行**残留** `register('on_target_selected', duelCheck)`，与 `generic_target_select.cjs` 抢同一 key。两文件都 register 同一 trigger，last-wins 顺序由 readdirSync 决定（不确定）→ 要么 duel 预检逃逸、要么通用闸门逃逸。duel.cjs 自身 79-81 行注释声称"已解耦移除"，但代码未真删。**修复**：删除 duel.cjs 残留 register，使 `on_target_selected` 仅由 generic 注册（duel 预检经 `duelCheck` 在 DUEL_RESOLUTION 端点单独驱动，4016 行已接）。现 `grep` 确认 duel.cjs 无真实 register、generic 唯一。
  2. **`on_battle_start` 点火缺清单登记**：combat.ts 3171 行 `fireReaction('on_battle_start', ...)` 点火，但不在 GATEWAY_KNOWN_TRIGGERS 清单 → 自检会误告警。补登 `on_battle_start` 进清单（21个）。
- **静态对齐核对（修复后）**：已注册 8 个 trigger（generic/duel解耦后唯一）全部在 GATEWAY_KNOWN_TRIGGERS 内；15 个点火点全部在清单内；无重复注册；selfCheck 无「不在清单/疑似死代码旁路」告警（容器日志验证无 warn）。预留点火点（on_item_pickup/on_action_order_changed/on_turn_end/on_attack_start/on_attacked/pre_damage_apply）为埋点前置、暂无 handler，属预期扩展位，保持不动。
- **部署**：gateway tsc 干净 → rsync 整目录（含 combat.ts + combat-service/*.cjs，gateway 运行时 nodeRequire 直读 combat-service 源，无需 tsc）→ `docker compose build --no-cache mecha-gateway && up -d` → /health=200。
- 三头（编辑器 TIMING ≡ 网关 KNOWN_TRIGGERS ≡ 引擎 register）现已完全对齐，反应系统收口完成。仅剩 ⑤ 前端 CI 校验（hexUtils/unitSize 镜像防漂移）待攻坚。

## Phase 7.⑤ 落地（2026-08-14）：前端镜像防漂移 CI 校验
- **现状核查**：gateway 源 `backend-gateway/src/unitSize.ts` 与前端镜像 `frontend/src/utils/unitSize.js` 常量（SIZE_ORDER/SIZE_RENDER_SCALE/SIZE_HP_FACTOR/SIZE_MOB_FACTOR/SIZE_HIT_FACTOR/SIZE_SEVEN_BOX/SIZE_LABELS）**已对齐**；前端 `hexUtils.js` 的 `DEFAULT_RANGE_BY_CATEGORY`/`DEFAULT_MIN_RANGE_BY_CATEGORY` 与 shared-kernel `hexMath.ts` 真相源**已逐字一致**。当前无漂移。
- **⑤ 正确交付=机器强制约束（非口头）**：新增 `frontend/scripts/check-mirror.mjs`，构建/CI 阶段自动比对三组镜像，任一边改了另一边没改→非零退出阻断部署：
  1. 体型：`backend-gateway/src/unitSize.ts` ⇔ `frontend/src/utils/unitSize.js`（含 SIZE_ORDER 数组 + 6 个 Record 常量）
  2. 射程：`shared-kernel/src/hexMath.ts` ⇔ `frontend/src/utils/hexUtils.js`（`DEFAULT_RANGE_BY_CATEGORY`/`DEFAULT_MIN_RANGE_BY_CATEGORY`）
  3. 等距：`frontend/src/utils/hexUtils.js` 的 `ISO_DEFAULTS` 结构性 sanity（shearX/shearY/scaleX/scaleY/rotation 字段齐全）。
  - 解析用纯 Node 内置（括号平衡扫描截取值字面量，兼容 TS 类型注解与单行/多行对象/数组），无第三方依赖。
- **接入点**：
  - `frontend/package.json`：`build` 改为 `node scripts/check-mirror.mjs && vite build`（构建前自动门禁）；新增独立 `check:mirror` 脚本。
  - `.github/workflows/deploy.yml`：新增 `Frontend Mirror Drift Gate` step（在 Build/Deploy 之前 `cd frontend && node scripts/check-mirror.mjs`）。
- **验证**：本地 `node scripts/check-mirror.mjs` → 全部一致 exit 0；负向测试（临时把 hexUtils.js `ranged:3`→`6`）→ 脚本检测漂移 exit 1，已还原；`npm run check:mirror` 可调。
- **Phase 7 全系列（7.1/7.2/7.3/7.⑤）至此全部收官**。

## Phase 8 落地（2026-08-14）：联调验证与部署（含全 Phase 遗漏盘点）
### 全 Phase 遗漏盘点结论
- **功能 Phase 无重大遗漏**：后端引擎 Phase 29/30/31/32/A/B/C/D/E/F/G 全部落地；Godot 阶段 0-7 落地（含 7.1/7.2/7.3 特效）；枚举收口（阶段 0/Phase A）Godot `Enums.TIMING` ≡ shared-kernel `enums.ts` 已对齐；镜像防漂移（7.5）已收口。子agent 报「三头分化残留」属过时文档结论（结算管线对齐表写于 8-13 前未反映 7.3 收口），实际代码已收口。
- **发现 2 处真实瑕疵（已处理）**：
  1. 后端 `GATEWAY_KNOWN_TRIGGERS` 7 个死条目（on_counterattack/on_reaction/on_after_skill_resolve/on_phase/on_move/on_faction_skill/on_visibility_changed）——清单里但无人 fireReaction 点火、无 handler register。已标注 `// RESERVED 预留埋点`，消除「已接通」误导（不影响 setGatewayTriggers 自检）。
  2. Godot 未消费结算数据（反击链/三路分流）——已通过 Phase 8 表现层对接补上（见下）。

### Phase 8 主线改造（Godot 阶段 8 联调验证与部署）
- **数据已齐备**：后端 `/attack` 响应已含 `counter_triggered`/`counter_damage`/`sizeTactic`(含 amount 体型机动补偿)/顶层 `reaction_log`/`reaction_events`，但 Godot 旧 `_on_skill_ok` 只用了 `final_damage`+`killed`，完全没消费反击链数据。
- **Battle3D.gd 扩展**（`apps/godot-alpha4.0/scenes/Battle3D.gd`，已 rsync 回桌面真源 `wggodot/alpha4.0`）：
  - 新增 `_battle_log: RichTextLabel`（_ready 动态创建挂 UILayer，鼠标穿透，440×250 左下角）——显示 reaction_log 反击链/三路分流文本流。
  - `_spawn_damage_float_from_result` 扩展：消费 `counter_triggered`→`_spawn_counter_float`（暗红「反 -N」飘字在攻击者身上）；消费 `sizeTactic`→`_show_size_banner`+日志（体型战术机动补偿）；消费 `reaction_log`→`_push_log` 文本流。
  - 新增 `_spawn_counter_float`/`_push_log`/`_show_size_banner` 三个方法。
  - AI 回合（`_consume_ai_actions` attack 分支调同一 `_spawn_damage_float_from_result`）自动获得反击链显示，无需额外改。
- **Godot 编辑器校验**：`godot --headless --editor --quit` 扫描 Battle3D.gd 无新增错误（仅历史 `res://scenes/Battle.gd` 缺失告警，非本次引入）。
- **部署**：后端 combat.ts 死条目修复 tsc 干净 → rsync 到 106.54.197.69 → `docker compose build --no-cache mecha-gateway && up -d` → /health=200。
- **阶段 8 验收矩阵状态**：① 9 阶段逐阶段核对——阶段 6/8 表现层缺口已补（反击链/体型战术可见化）；② Godot Web 导出挂 8081 仍待桌面 Godot 导出+nginx（重操作，未做）；③ 枚举收口已通过（Enums.TIMING⊆shared-kernel TIMING）。
- ⚠️ 阶段 6 后端缺口残留：`/attack` 的 combat_result 未透传 `mobility_diff`（仅 sizeTactic.amount），Godot 机动 HUD 用 sizeTactic 近似显示；完整机动差值需后端补 `result.stages.mobility_diff` 透传（标注后端依赖，非本次阻塞）。
