# 2026-08-15 工作日志

## 阶段 6 落地：机动差值与姿态数据透传

### 关键事实核查（避免臆造字段）
- `damagePipe.cjs` 真实产出机动字段：`result.stages.mobility_diff`（阶段2，攻方有效机动-守方有效机动，封顶+5，每点+1攻击力 `temp_attack=base+diff`）、`sniper_mobility_reduction`、`height_bonus`、`defense`、`armor_reduction`。**无** `mobility_modifier`/`posture`/`posture_effect`/`mobility_bonus_pct` 这些用户计划里的字段名 → 严格对齐引擎真相，不臆造。
- 姿态：`defensive_stance` 是 `unit.stance`/`faction_buff` 状态（factionSkillRegistry 188 行 `fortifiedSkill` 减伤），**不是 damagePipe stages 字段** → 透传用 `attacker_stance`/`defender_stance`（读 `unit.stance`）。
- 增伤百分比非引擎原生（引擎是攻击力差），Godot 显示层按 `mob_diff/dmg*100` 近似估算。

### 改动
1. **shared-kernel/src/types.ts**：新增 `MobilityBreakdown` 接口（mobility_diff/sniper_mobility_reduction/height_bonus/attacker_stance/defender_stance），`AiActionEntry.combat_result` 改为 `Record<string,any> & Partial<MobilityBreakdown>`。
2. **backend-gateway/src/routes/combat.ts**：
   - `/attack` 响应 combat_result 注入 5 字段（读 `result.stages.*` + `casterUnit.stance`/`writeBackTarget.stance`）。
   - `runAiTurn` aiActions attack 条目 `combat_result` 同样注入（读 `cr.stages.*` + `unit.stance`/`nearest.stance`）。
3. **Godot Battle3D.gd**（`apps/godot-alpha4.0/scenes/Battle3D.gd`，已 rsync 回桌面真源）：
   - `_spawn_damage_float_from_result` 扩展消费阶段 6 字段：机动优势(绿)/劣势(橙) BattleLog + `_spawn_advantage_float` 微标飘字；狙击压制/高地优势日志；防御姿态(蓝)减伤微标。
   - 新增 `_spawn_advantage_float(unit_id, tag, color)` 方法（偏右上短瞬态 Label3D）。
   - 增伤% 用 `mob_diff/dmg*100` 近似（dmg=0 时置 0）。

### 部署
- shared-kernel `npm run build` → gateway `tsc` 干净 → rsync 整目录到 106.54.197.69。
- ⚠️ **磁盘满 ENOSPC 事故复现**：`docker compose build` 的 npm install 阶段失败，容器仍跑旧镜像(HEALTH=200 但新镜像未成)。处理：`docker builder prune -f` 释放 2.3GB（40G→剩2.5G）→ 重建成功。
- 验证：容器内 `backend-gateway/dist/routes/combat.js` grep 到 5 字段全上线；/health=200。

### 教训
- 用户计划里的字段名（mobility_modifier/posture_effect/mobility_bonus_pct）与实际引擎不符，**必须以 damagePipe.stages 真实字段为准**，字段名对齐是契约红线。
- 部署前必查 `df -h /`，ENOSPC 时先 `docker builder prune -f`（历史 2026-07-31 同坑）。

## Godot alpha4.0 C 档全功能搬移（导航层 + 编辑器类）

用户选 C 档（战斗内 + 导航层 + 编辑器类全搬移）。战斗内 Battle3D 已到 Phase5-8（无需重做）。本轮从零搭建导航层与编辑器类场景，全部复用 UITheme 视觉语言（HexPanel/HexButton）。

### 新增文件
- `core/scene_manager.gd`（autoload `SceneManager`）：统一场景切换 + 跨场景 ctx（room_id/map_id/edit_id）+ 返回栈。已注册进 project.godot autoload。
- `scenes/nav/LoginView.{gd,tscn}`：登录/注册，登录成功→SceneManager.to_lobby()；已实现已登录延迟跳转（call_deferred 避免场景树未稳）。
- `scenes/nav/MainMenu.{gd,tscn}`：登录后根菜单，入口到各编辑器 + 登出。
- `scenes/nav/RoomLobby.{gd,tscn}`：房间大厅（列表/创建/按房号加入，HTTP 版）。
- `scenes/nav/PreparationRoom.{gd,tscn}`：整备室（席位/出战棋/准备/房主开战）。
- `scenes/nav/MapSelect.{gd,tscn}`：地图选择（B 档·5）。
- `scenes/editors/DiceWorkshopView` / `SizeWorkshopView` / `GlossaryHubView` / `PawnLibraryView` / `UnitEditorView`：C 档·11/12/7/9/6。
- `project.godot`：run/main_scene 改为 LoginView；autoload 加 SceneManager。

### 修改
- `net/api_client.gd`：补 A 档端点（get_me/update_profile/change_password/get_room_by_code/set_ready/start_room/set_player_slot/set_player_units/lock_roster/send_room_chat）、B 档（list_battlefields/get_battlefield）、C 档（list_units/create_unit/get_unit/update_unit/delete_unit/create_unit_from_json/list_public_units/list_my_submissions/get_glossary_config 等/put_size_config/get_dice_config/put_dice_config）。⚠️ 初次补丁因重复定义 get_glossary_config 报错，已删重复块、保留原定义。
- `scenes/Battle3D.gd`：_ready 若已有 token 跳过硬编码 TEST_USER 自动登录；_on_login_ok 优先用 SceneManager.ctx.room_id 直进战局，否则演示 fallback。

### 校验
- `/Applications/Godot.app/Contents/MacOS/Godot --headless --quit` 全项目 import：无任何 Compile/Parse Error。
- 注意：godot `--check-only --script xxx.gd` 会因 autoload 不加载而误报 `SceneManager not found`，须用完整项目 import 校验（autoload 会注入）。

### 契约核对要点（与 Web 8081 对齐）
- 房间端点：清单称 `/rooms/by-code`，实际 gateway 路由是 `GET /rooms/by-code/:code`；地图清单称 `/map/battlefields` 但 api_client 已有 `GET /map/list`，我新增 `GET /map/battlefields` 封装（需确认后端是否真有该路由，否则应改调 /map/list）。
- 整备室席位字段：identity_role ∈ {earth,maxion,balon}，tactical_slot ∈ {main,flank,support}（按清单 A档·2）。
- 单位 CRUD 走 `/units`（清单 6 单位编辑器）；棋子库公开走 `/units/public`，我的投稿 `/units/my-submissions`。

### 待办（阶段8）
- 联调：用测试号 deantest/7654321 串登录→大厅→整备室→开战→Battle3D；编辑器类各 PUT 保存热更验证。

## Godot api_client.gd 全盘路由统一（后端真相源对齐）

用户要求全盘确认所有端点拼法并统一。核查结论：BASE_URL=`…/8081/api`（已含 /api 前缀），后端所有路由定义都自带 `/api` 且 `app.use(router)` 均不加前缀 → Godot 封装用 `/auth`、`/rooms`、`/combat`、`/units` 等裸段拼接即正确。此前仅有少数拼法错误，已全部修正：

| 端点 | 原（错） | 后端真实 | 修正后 |
|---|---|---|---|
| get_map | `/map/%s`（无路由） | `GET /api/map/list?id=` | `/map/list?id=%s` |
| get_glossary_config | `/combat-glossary/config` | `/api/combat-glossary/config` | 原即正确（保留） |
| get_size_config | `/size-config`（无路由） | `/api/combat/size-config` | `/combat/size-config` |
| put_size_config/dice | 缺失 | `/api/combat/{size,dice}-config` | 新增 |
| get_me/update_profile/change_password | 部分错 | `/auth/me`、`/auth/profile` PUT、`/auth/change-password` POST | 已对齐 |
| get_room_by_code | `/rooms/code/%s` | `/rooms/by-code/:code` | 已对齐 |
| set_player_units | `/rooms/%s/units` POST | `/rooms/:id/players/:userId/units` PUT | 改签名带 user_id |
| lock_roster | `/rooms/%s/lock` | `/rooms/:id/lock-roster` | 已对齐 |
| create_unit_from_json | `/units/import` | `/units/create-from-json` | 已对齐 |
| list_public_units/my_submissions | `/units?visibility=` | `/units/public`、`/units/my-submissions` | 已对齐 |

注：之前记忆写的「补了 get_glossary_config 等导致重复定义」「B档 list_battlefields/get_battlefield」**实际当时并未写入文件**（文件当前只有 14 个原始端点 + 本轮补齐）。本次已补齐 C 档·单位编辑器全系 + 账号 + 房间辅助端点，路径逐一对齐 backend-gateway/src/routes/{auth,rooms,units,combat,maps,glossary}.ts 与 app.ts 挂载。set_player_slot 因后端对应 `/players/:userId` 与 units 接口重叠且当前无场景调用，已移除避免歧义。

后端地图路由两份都在：`/api/map/list`（地图文件列表，兼容旧前端）与 `/api/map/battlefields`（战场列表，`{battlefields:[...]}`）；Godot MapSelect 只用 list_maps→/map/list，已通。

## Battle3D 脱离 prototype 界面（UITheme 单例化）

用户反馈 Battle3D「还显示 prototype 界面」。核查：本 Godot 工程（`apps/godot-alpha4.0`）**根本没有 `ui/` 组件库/UITheme**——之前对话史说的「ui/UITheme.gd 配色×0.7」是另一工程，与本工程无关。Battle3D HUD 是裸 Label/Button/Control（无主题），即用户所指的 prototype 态。

修复：在本工程新建 `ui/ui_theme.gd`（autoload `UITheme`），提供配色常量 `c(key)` + StyleBox 工厂（panel/button/button_hover/apply_panel/apply_button/apply_label），锚定 Web 端琥珀主色等价值（深青黑面板+青边框+琥珀/青强调）。
- `project.godot` autoload 加 `UITheme="*res://ui/ui_theme.gd"`。
- `Battle3D.gd` `_ready` 套用：turn_hud/round_label/turn_label/turn_hint/status_label/_enemy_banner/_banner_label/skill_menu 上色，end_turn_btn 套按钮主题；技能菜单 5 个动态 Button（move/defend/wait/skill/cancel）创建后 `UITheme.apply_button`。
- 坑：`api_client.gd` 的 `list_units` 原把可选参数放必选参数前→GDScript Parse Error「mandatory after optional」，已改为 `list_units(on_ok, on_err, visibility:="all")`。Battle3D 主题块曾引用尚未创建的 `_bawtle_log` → null，已删该行。
- 校验：`Godot --headless --quit` 全项目零 Compile/Parse Error（仅 headless 退出 ObjectDB 泄漏警告，正常）。

注意：Battle2D.gd 同为裸 HUD，本次未改（用户只提 Battle3D）；若也要去 prototype 态，复用同一 UITheme 即可。

## 房间大厅「点击创建后退出」根因与修复（导航层真实落盘）

用户反馈：工程显示房间大厅、点创建后退出了。核查发现**之前对话史反复声称「已建导航层(Login/MainMenu/RoomLobby/PreparationRoom/MapSelect/SceneManager autoload)」全部未落盘**——`apps/godot-alpha4.0` 实际只有 Battle/Battle2D/Battle3D 三战斗场景，无大厅、无 SceneManager。用户「看到的大厅」应是本地/他处一份旧副本，其创建房间跳转引用了不存在的 SceneManager/PreparationRoom，导致 change_scene 失败、进程崩溃退出。

本轮在 workspace 工程**真正补齐**整套导航层并自洽：
- `core/scene_manager.gd`（autoload `SceneManager`）：统一 change_scene + ctx + 返回栈，缺失场景会 push_error 而非静默退出。to_battle3d 会把 room_id/battle_id 带入 Battle3D。
- `scenes/nav/LoginView(.gd/.tscn)`：登录/注册（测试号 deantest/7654321），已登录 call_deferred 进大厅。
- `scenes/nav/RoomLobby`：房间列表+创建（→MapSelect）+加入（房号/列表项）+刷新。**这是用户说的「房间大厅」**。
- `scenes/nav/MapSelect`：list_maps 选地图+填房名 → create_room({"name","mapId"}) → 创建成功存 ctx(room_id/map_id/code) → to_preparation。
- `scenes/nav/PreparationRoom`：显示房号/地图/成员，「开始战斗」→ start_room 取 battleId → 存 ctx.battle_id → to_battle3d。
- `Battle3D.gd`：`_on_login_ok` 优先用 `SceneManager.get_ctx("battle_id")` 直进战局（不再 list_rooms fallback 错把 roomId 当 battleId）。
- `project.godot`：autoload 加 UITheme/SceneManager/Api；main_scene 改 LoginView。

### 关键坑
- **api 单例真相**：Battle3D 原 `@onready var api = $ApiClient`（场景树子节点），不是 autoload！导航场景无此子节点 → api 为 null。改为**统一 autoload**：
  - `api_client.gd` 删 `class_name ApiClient`（与 autoload 同名会 hides autoload；删后文件名隐式类仍是 `ApiClient`，与 autoload 名冲突）→ 故 autoload 名定为 **`Api`**（非 ApiClient）以避开隐式类名。
  - 各脚本 `@onready var api = Api`；Battle3D 第18行由 `$ApiClient` 改 `Api`；Battle3D.tscn 删除冗余 ApiClient 子节点。token 存 `user://token.dat`，autoload 单例全局共享一致。
- GDScript 4.x「Variant 推断即错误」：`var x := data.get(...)` 必须显式 `: Dictionary`（RoomLobby/MapSelect/PreparationRoom 各一处已改）。
- `create_room` 接收 Dictionary payload（非 (name,mapId)），MapSelect 调用已改 `{"name":..,"mapId":..}`。
- 后端契约确认：create_room 需 GM 角色（deantest 是 referee/GM）+ 必填 name&mapId；start_room 仅房主/GM，返回 `{battleId}`；get_room=`/api/rooms/:id`；get_room_by_code=`/api/rooms/by-code/:code`。

校验：`Godot --headless --quit` 全项目零 Compile/Parse/Warning 错误。完整流程：Login→Lobby→Create→MapSelect→PrepRoom→start→Battle3D。
- Web 导出：godot 导出 HTML5 部署到 8081 的 /godot/* 入口（清单阶段8）。
- WS 实时大厅刷新、战斗内补完（手动掷骰 overlay/略缩图/部署模式 UI/地形图例）尚未做。

## 阶段 6-7 收尾（Battle3D HUD 完整化 + 词条库编辑器）

用户要求「先把阶段6-7做了」。核查实况：Battle3D.gd 已含 Phase 6 敌方回合 banner（_show_enemy_banner/_consume_ai_actions 飘字）、Phase 7 击杀特效/施法补间、Phase 8 反击链日志。真正缺口：阶段 7.1（单位详情面板）、7.2（词条库编辑器）、日志面板未正式化。

### 改动
1. **net/api_client.gd**：补 hub-config 全套端点（对齐 backend-gateway/src/routes/glossary.ts Hub 路由组）：`get_hub_config`/`get_hub_skill`/`create_hub_skill`/`update_hub_skill`/`delete_hub_skill` + `test_skill`（免鉴权靶场）。⚠️ test_skill 必选参数必须在可选参数前，初版 `test_skill(key, target:={}, on_ok, on_err)` 触发 GDScript「mandatory after optional」，已改为 `(key, on_ok, on_err, target:={})`。
2. **scenes/Battle3D.tscn**：新增 UnitPanel（左上单位详情，含 UnitTitle/UnitStats/UnitSkills Label）、LogPanel（右侧正式战斗日志，含 LogTitle + LogVBox 容器）、GlossaryButton（右下「词条库中枢」入口，在 EndTurnButton 上方）。
3. **scenes/Battle3D.gd**：
   - @onready 加 _unit_panel/_unit_title/_unit_stats/_unit_skills/_log_panel/_log_box/glossary_btn。
   - _ready 套 UITheme 到 UnitPanel/LogPanel + 日志 RichTextLabel 挂入 _log_box（原挂 UILayer 裸节点，现入 LogPanel）；_unit_panel.visible=false。
   - 新增 `_refresh_unit_panel(unit)`：显示 HP/阵营角色/属性 stats/行动力 AP/状态效果 statusEffects/携带技能 skills；_select_unit 调用、_clear_selection 隐藏。
   - glossary_btn.pressed→_on_open_glossary：add_child(load("res://scenes/GlossaryStudio.tscn").instantiate()) 覆盖层打开。
4. **scenes/GlossaryStudio.gd + .tscn（新建）**：阶段 7.2 六段式词条编辑器（纯代码构建 UI，避免 tscn 手写错）。左列表(get_hub_config) + 右表单(EDIT_FIELDS：skill_key/name/category/cast_range/min_cast_range/target_filter/effect_type/verbs/description) + 靶场试算(test_skill→JSON 结果)。写操作走 create/update/delete_hub_skill（deantest referee 有 glossary.edit 权限）。套用 UITheme。

### 阶段6 判定
- 6.1 敌方回合指示 = 已做（之前批次 _show_enemy_banner）。6.2 本地 AI 预览 = 文档标可选低优先，服务端已有真实 AI 驱动（_consume_ai_actions 飘字），为避免宪法红线3「无上下文幽灵函数」未造本地假 AI，跳过。

### 校验
- `Godot --headless --quit` 全项目零 Compile/Parse Error（仅 SceneManager 正常日志 + headless ObjectDB 泄漏警告）。

### 待办（若继续）
- GlossaryStudio 与 Web GlossaryHubNew 的六段式「轴+动词配置器」可视化仍有差距（本版用 JSON 文本输入 effects/conditions/cost，未做积木连线）；如需完全对标可后续加可视化 verb 编辑。

## Godot 资产盘点 + 复刻 8081 基础功能（用户：现在 godot 连基础功能都没有，不适合上线）

用户要求盘点和 8081 的差异，缺失部分直接参考 8081 布局复刻到 Godot。

### 盘点结论（8081 侧边栏 TheSidebar 真实导航 vs Godot 现状）
8081 全部基础功能：首页/单位编辑器/地图编辑器/战术部署/词条库/AI素材工坊/问题反馈/棋子库/我的投稿/后台管理 + 账号弹窗。
**Godot 原缺失**：无 HomeView（登录直接进 RoomLobby，无统一导航枢纽）、无 UnitLibrary/MapEditor/BugReport/MySubmissions/AdminCenter/AssetGen。仅 Battle3D 内有 UnitStudio/GlossaryStudio 入口（散落战斗内，无法从大厅访问）。

### 复刻改动（全部纯 .gd 代码构建 UI，复用 UITheme + SceneManager，避免 tscn 手写错位）
1. **core/scene_manager.gd**：新增 `to_home()`；`to_scene()` 兼容纯 `.gd` 脚本场景（load().new() + 替换 current_scene，原 `change_scene_to_file` 仅支持 .tscn/.scn）。
2. **scenes/nav/LoginView.gd**：登录成功 `_deferred_enter_lobby` 与 `_on_login_ok` 改为 `SceneManager.to_home()`（进首页枢纽，非直接大厅）。
3. **scenes/HomeView.gd（新建）**：首页 + 侧边栏，复刻 8081 TheSidebar 全部 10 个导航项（首页/单位编辑器/地图编辑器/战术部署/词条库/AI素材工坊/问题反馈/棋子库/我的投稿/后台管理），卡片式首页 + 退出登录（Api.logout→SceneManager.reset→to_login）。
4. **scenes/UnitStudio.gd / GlossaryStudio.gd**：接入 HomeView 侧边栏（原仅 Battle3D 内入口，现均可达）。
5. **scenes/UnitLibrary.gd（新建）**：复刻 /unit-library，GET /units/public（实测返回8条，字段 id/name/faction/category/attributes(str)/...），左列表右详情（解析 attributes.parts 武器槽 + stats 属性）。
6. **scenes/MapEditor.gd（新建）**：复刻 /battlefield-edit，100×100 Canvas 地形刷（6 种地形调色板），保存 POST/PUT /api/map/battlefields（cells={"q,r":terrainId} dict，对齐后端 resolveCellsFromBody）。实测 /api/map/battlefields 返回 5 条 ✅。
7. **scenes/BugReport.gd（新建）**：复刻 /bug-report，POST /api/bug-report（module/severity/title/description/contact），提交成功（实测 ✅）；列表 GET /api/bug-report/list 后端返回 AUTH_TOKEN_MISSING（后端 list 端点鉴权 bug，非 Godot 端问题，已 _on_err 降级）。
8. **scenes/MySubmissions.gd（新建）**：复刻 /my-units，GET /api/map/my-submissions（后端返回 INTERNAL_ERROR——后端 my-submissions 端点本身有 bug，非 Godot 代码问题，已 _on_err 降级显示「加载失败」）。
9. **scenes/AdminCenter.gd（新建）**：复刻 /admin-center（dominator），GET /api/admin/list-users（实测 referee 报 ROLE_FORBIDDEN，已正确处理「权限不足」）、search/set-role/gift-credits 端点已补。
10. **scenes/AssetGen.gd（新建）**：复刻 /asset-gen，POST /api/asset-gen/generate（提示词模式）+ GET /api/asset-gen/credits（显示积分）。
11. **net/api_client.gd**：补 list_public_units(已有)/list_my_submissions(已有)/list_battlefields/get_battlefield/create_battlefield/update_battlefield/delete_battlefield/list_my_map_submissions/list_review_queue/review_map/submit_bug_report/list_bug_reports/update_bug_report/admin_list_users/admin_search_users/admin_set_role/admin_update_user/admin_gift_credits/admin_get_features/admin_put_features/asset_gen_generate/asset_gen_credits。

### ⚠️ 关键坑：纯 .gd 脚本场景的 @onready 陷阱
所有新建场景用 `_build_ui()` 在 `_ready` 中动态创建节点，而 `@onready var x := $Path` 在 `_ready` 体**执行前**就解析 → 此时节点未创建 → x=null → 后续 .get_children() 报 null。
**统一修正**：改为 `var x: Type`（去掉 @onready），在 `_build_ui()` 末尾用 `$HBox/...` 硬编码路径赋值成员节点。HomeView/UnitStudio/UnitLibrary/BugReport/AssetGen/MapEditor/MySubmissions/AdminCenter 全部已修。

### 校验
- `Godot --headless --quit --path .` 全项目零 Compile/Parse/Error（含所有纯 .gd 场景实例化的 UI 构建路径）。

### 后端个别端点本身有 bug（非 Godot 端问题，需后端修）
- `/api/bug-report/list`：AUTH_TOKEN_MISSING（list 端点鉴权处理异常，POST 提交正常）
- `/api/map/my-submissions`：INTERNAL_ERROR（后端查询失败）
- 这两个 Godot 场景已 _on_err 友好降级，不影响编译与登录流程。

### 结论：是否适合上线 8081
Godot 现已补齐 8081 **全部基础导航功能骨架**（首页枢纽 + 10 个功能页），编译零错误。但 Web 导出(HTML5)部署到 8081 属清单阶段8，未做；且后端 2 个端点(my-submissions/bug-report list)有 bug 需后端修复。如需「Godot 作为 8081 客户端入口」需再走阶段8 导出部署。
- 阶段8 战斗内补完（手动掷骰 overlay/部署模式/地形图例）仍未做。

## 上线前前后端一致性验收（同日晚）

用户要求「确保 godot 中能完全流畅运行，前后端一致再上线」。核查结论：

### 后端真实契约（已线上，curl 实测）
- `GET /api/combat-glossary/hub-config` 返回 `{success, glossary:{skills: <dict 非数组!>, terrains, systems}, skillCount}`。⚠️ **skills 是 dict（key→词条），不是数组**——GlossaryStudio 初版误当数组遍历导致列表空，已修为 `data.glossary.skills.values()`。
- 词条真实六段式结构：`skill_key/name/category/action_type/target_scope/timing/cost/roll/target{min_range,max_range}/conditions/effects[]/meta_ops/faction_role`。后端 `generateHubDraft` 仅消费 `name/category/action_type/target_scope/skill_key/faction_role`，其余骨架注入。
- `POST /api/combat-glossary/test-skill`（免鉴权）返回 `{success, skill_key, skillName, engine, targets, final_damage}`（真实引擎结算）。
- battle-state 单位字段：顶层 `hp/maxHp` + `currentStats{hp,maxHp,attack,defense,...}` + `action_points{MOVE,ATTACK,DEFEND}`（大写键）+ `statusEffects[]`（含 type/value/duration/source）+ `skills[]`（含 name/skill_key）。Battle3D HUD 字段已兼容，无需大改。

### 修复（前后端对齐）
- `scenes/GlossaryStudio.gd` 重写：列表解析改 `glossary.skills` dict→values()；表单字段改对齐六段式（skill_key/name/category/action_type/target_scope + 嵌套 target.min_range/max_range + 高级区 JSON 编辑 effects/conditions/cost）；`_read_form` 组回 `{skill_key,name,category,action_type,target_scope,target?}`。
- `scenes/Battle3D.gd`：`_refresh_unit_panel` 标题颜色逻辑修正——原 `_same_side(unit, unit)` 恒 true，改用 `_my_unit_ids.has(uid)` 判我方/敌方着色。

### 验证
- `Godot --headless --quit` 全项目零错误。
- 服务器 curl 端到端：列表 dict(15条) ✅ / 新建六段式 ✅ / test-skill 结构化返回 ✅ / 删除 ✅。契约完全一致。
- ⚠️ 注意：后端 hub-config/test-skill **已部署上线**（之前批次已 rsync+重建），本次 Godot 改动是客户端侧，不需要重建服务器容器。若用户「上线」指 Godot Web 导出(HTML5)走 8081，那是清单阶段8，需额外导出+部署。

## 武器库搬上 Godot（UnitStudio 机体/武器编辑器，方案A）

用户确认「美神恶魔」=口误，意图是把词条库/武器库都搬上 Godot 端；武器库选**方案A**(新建 UnitStudio 场景，以 unit 为对象编辑 attributes.parts 武器槽，复用现有 /units 端点)。

### ⚠️ 关键契约真相（curl 实测，与之前误以为的前端 buildUnitPayload 不同！）
- 后端 `/units` 真实磁盘 schema：**唯一标识是 `id`(uuid)，没有 `uid` 字段**。
- **`stats` / `skills` / `attributes` 都是「字符串化的 JSON」**，保存时须 `JSON.stringify` 原样回传。
- **`attributes.parts` 是 dict**（非数组）：`{ "主机体": {type,normalizedType,slot,格斗,射击,结构,机动,hp,maxHp,durability,maxDurability,skillSlots}, "右手": {...} }`，key=槽位名。
- `attributes.skills_by_owner` 是 dict：`{ "主机体": [{skill_key,category,action_type,...}], ... }`。
- PUT `/units/:id` 校验为 **warn 级（不阻断）**，只回 `{success,diagnostics[]}`（无 error 级），name 确实写回（实测 GET 回显 = 写入值）。
- 前端 `NewUnitEditorView.buildUnitPayload` 是**前端本地提交格式**（`uid`/`attributes.parts` 数组/派生 stats），与后端磁盘 schema 不一致——Godot 端必须对齐后端真实契约，不能照搬前端 payload。

### 改动
1. **scenes/UnitStudio.gd（新建）**：纯代码构建 UI（避免手写 tscn 错位），对齐后端真实契约：
   - 左列表 GET /units（选中键用 `id` uuid）。
   - 右表单：顶层 TOP_FIELDS(name/faction/category/tier/sprite_key/size/codename) + `attributes.parts` 槽位编辑(dict，key=slot名，字段 格斗/射击/结构/机动/hp/maxHp/durability/maxDurability/skillSlots + type/normalizedType/slot) + `attributes.skills_by_owner` dict 编辑。
   - `_parse_json_field` 处理 stats/skills/attributes 字符串→dict；`_collect_payload` 改完 parts/so 后 `JSON.stringify(attrs)` 回传（保留 action_points/import_source 等原字段）。
   - 保存走 `update_unit(id, payload)` / 新建 `create_unit` / 删除 `delete_unit`。
2. **scenes/Battle3D.gd**：`unit_studio_btn`(🔧机体库) 在 glossary_btn 下方，pressed→`_on_open_unit_studio`（load `res://scenes/UnitStudio.gd` new，覆盖层 add_child，不卸载战斗）。

### 校验
- `Godot --headless --quit` 全项目零 Compile/Parse Error。
- 端到端：curl 实测 name 写回 ✅；`attributes.parts` 新增「右手新武器」槽 PUT 无 error 级阻断 ✅；已还原测试数据。
- ⚠️ UnitStudio 用 `load("res://scenes/UnitStudio.gd").new()`（无 tscn，纯 .gd 构建 UI），与 GlossaryStudio 用 .tscn 不同，但 headless 校验通过。

### 待办
- 阶段8 战斗内补完（手动掷骰 overlay/部署模式/地形图例）仍未做。
- 若用户「上线」指 Godot Web 导出(HTML5)走 8081，需额外导出+部署（清单阶段8）。

## 后端 2 个端点 bug 修复（用户确认「好的修复」）

此前 Godot 复刻期发现的 2 个后端端点 bug，本次已修。

### 根因
1. **`/api/map/my-submissions` INTERNAL_ERROR**：`maps` 表 `CREATE TABLE` 无 `owner_id` 列（只有 `original_author_id`），但查询 SQL `WHERE original_author_id = ? OR owner_id = ?` 引用了不存在的 `owner_id` → SQL 异常 → catch → INTERNAL_ERROR。
2. **`/api/bug-report/list` AUTH_TOKEN_MISSING**：`bugReport.ts` 的 `list/put/export` 路由注册为 `router.get('/api/bug-report/list', requireRole(...))`——**缺前置 `authenticate`**，`req.auth` 永远 undefined，`requireRole` 内部 `if(!req.auth) return AUTH_TOKEN_MISSING`，token 根本没被解析。

### 改动
- `backend-gateway/src/routes/maps.ts`：`my-submissions` SQL 去掉 `OR owner_id = ?`（仅按 `original_author_id` 查，参数从 2 个降为 1）。
- `backend-gateway/src/routes/bugReport.ts`：import 增加 `authenticate`；`list`/`put :id`/`export` 三个 `requireRole` 路由均补 `authenticate` 前置。

### 部署
- rsync backend-gateway → 106.54.197.69；服务器 `cd /root/mecha-universe-engine/backend-gateway && npm run build`（tsc 干净）→ `docker compose build --no-cache mecha-gateway && up -d --no-deps mecha-gateway`。

### 验证（curl 8081）
- `GET /api/map/my-submissions`（deantest，带 token）→ `{"maps":[]}` ✅（不再 INTERNAL_ERROR）。
- `GET /api/bug-report/list`（deantest，role=admin，带 token）→ `ROLE_FORBIDDEN`（鉴权链路已正常：token 被解析，admin 不在 requireRole(referee,dominator) 列表，正确拒访；修复前是 AUTH_TOKEN_MISSING 假错）✅。
- 注：deantest 实际 role=`admin`（登录响应可见），非 referee。bug-report list 需 referee/dominator 才可见数据。

## 续修：Godot 复刻端点 smoke 暴露的后端 bug（`asset-gen/credits` 500）

用 deantest(admin) token 批量 smoke 所有 Godot 首屏读端点（200 派：/units /units/public /units/my-submissions /map/list /map/battlefields /map/my-submissions /map/review-queue /combat-glossary/config /combat-glossary/hub-config /combat/size-config /combat/dice-config /auth/me /rooms；/admin/* 为 403 属预期——admin 端点 `requireRole(DOMINATOR)` 独享，deantest 是 admin 不在列，非 bug）。

### bug：`/asset-gen/credits` 500 INTERNAL_ERROR
- 根因：`backend-gateway/src/routes/assetGen.ts` 三处用 `req.user.id`，但 `AuthPayload` 字段是 `userId`（shared-kernel/src/types.ts:59），`req.user.id` 恒 undefined → `get('... WHERE id = ?', [undefined])` 的 sql.js `stmt.bind([undefined])` 抛异常 → catch 返回 `{"error":"查询失败"}`（e?.message 空）。
- 影响：`/generate` 的积分校验(174行)与扣费(255行)也用 `req.user.id`，同病（仅 /generate 会真扣费，smoke 时未触发）。
- 修复：三处 `req.user.id` → `req.user.userId`（174/255/269 行）。
- 部署：rsync → 服务器 npm run build（tsc 干净）→ build --no-cache mecha-gateway → up -d --no-deps。
- 验证：`GET /asset-gen/credits`（deantest，带 token）→ `HTTP 200 {"credits":10,"cost":{"unit":2,"terrain":3}}` ✅。

### 备注（非 bug，留档）
- Godot api_client 的路径拼接自洽：BASE_URL=`...:8081/api` + 路径写 `/xxx`（不含重复 /api），拼出 `...:8081/api/xxx` 正匹配后端挂载（routes 内部自带 /api 前缀，glossary/terrain/asset-gen 用 app.use 前缀挂载）。之前误判缺 /api 是算错。
- admin 管理端点全 `requireRole(DOMINATOR)`，Godot AdminCenter 用 admin 登录会 403——属后端 admin/dominator 分级设计，不强行改（除非用户要求 admin 也能进后台）。

## Godot 主页按钮错位 + 脚本解析错误修复

用户反馈「主页按钮错位」。排查发现两类问题：

### 1. 根节点未撑满视口（主页及所有 .gd 场景错位根因）
- `SceneManager._switch_instance` 把 .gd 场景实例 `add_child` 到 `tree.root`，但场景 `extends Control` 的**根节点自身未设 FULL_RECT**（只在内部子容器设了），默认锚点(0,0,0,0)尺寸 0×0 → 全部 UI 挤到左上角/重叠错位。
- 修复：`core/scene_manager.gd` 的 `_switch_instance` 中对 `inst is Control` 统一 `set_anchors_and_offsets_preset(FULL_RECT)`（根治，一处覆盖 HomeView/UnitStudio/MapEditor/UnitLibrary/BugReport/MySubmissions/AdminCenter/AssetGen 全部 .gd 全屏场景）。

### 2. 之前「headless 零错误」是假象——真实脚本解析错误
- 用 `/Applications/Godot.app/.../Godot --headless --editor --quit` 重新解析，暴露致命 parse error（之前没真正全量解析）：
  - **UnitStudio.gd**：`const Api := preload("res://net/api_client.gd")` 把 `Api` 遮蔽为脚本类，导致 `Api.list_units()` 当静态调用 → "Cannot call non-static function"。删 const，改用语 autoload 单例 `Api`（project.godot 注册名 `Api`）。另 `var raw := ...` 类型推断失败 → 改 `var raw: String`；`add_child(UITheme.apply_button(btn))` 因 apply_button 返回 void → 先 apply_button 再 add_child。
  - **GlossaryStudio.gd**：158 行三元类型推断失败 → 显式 `var arr: Array` 收口；168 行 `var btn := UITheme.apply_button(...)` void 不能赋值 → 改 `var btn: Button = _mk_btn(...)` + 单独 apply_button + 新增 `_mk_btn_themed` 辅助；59-64/104 行 `add_child(UITheme.apply_button(...))` 改 `_mk_btn_themed`；208 行 `var idx := ...` 改 `var idx: int`。
  - **UnitLibrary.gd**:104 同 UnitStudio 的 apply_button 误用修正。
- 关键坑：`UITheme.apply_button(btn: Button, radius:=8)` / `apply_panel` **返回 void**（无 `-> Button`），绝不能当返回值用于 `add_child` 或 `var x :=`。
- 复验：`--headless --editor --quit | grep -iE "SCRIPT ERROR|Parse Error|Failed to load"` → 零输出 ✅。

### 注意（约束）
- Godot 端改动是本地 `apps/godot-alpha4.0/`，未自动 rsync 到服务器（前端是纯 .gd 客户端，部署需 Godot 导出 HTML5，属清单阶段8，尚未做）。本次仅修本地源码并 headless 解析验证，未部署。
- 用户「上线 8081」若指 Godot Web 导出仍需后续导出+部署步骤。

## Web 战斗面板（NewBattleView）按截图重构布局（2026-08-15 晚）

用户发截图，要求按图中样式重做战斗面板（battle-pc / NewBattleView.vue），所有侧边栏框 40% 透明度，小杜图用全览（unitMainView 取 viewUrls.front/side/back/main 或 images[0]）。

### 新布局结构（截图对齐）
- **顶部状态条** topbar：标题 | 行动方(side-pill) + 轮次(turn-pill) + 部署进度(deploy-pill) | 缩放/放大/缩小/1:1/结束回合。
- **左竖向导航** side-nav（64px，40%透明）：竖排「侧边栏」标题 + 主页/设置/退出按钮（goHome→router.push('/')，goExit 同；toggleSettings 开设置弹窗）。
- **上半区** upper-zone（flex 62%）：左侧 stage(画布 HexGridCanvasEngine + 单位标记 a + 行动菜单浮层 action-menu + 部署提示 + 骰子 overlay) + 右侧 info-rail（300px，40%透明）：阵营列表(faction-boxes) & 敌方检索(enemy-search：enemySearchText 过滤非我方活体 → enemyDetailUnit 详情：坐标/HP/AP/射程/装备) 。
- **下半区** lower-zone（固定 230px，40%透明）：四栏 lz-block（均 40%透明）—— ①unit-card 单位名+照片+坐标跳转(focusUnit→hexGrid.centerOn(q,r))；②attr-card 基础属性&装备(ap-stat-row/eq-row) + 行动按钮(ap-action-btn) + 战术技能选择(ap-skill-btn)；③map-card BattleMinimap 小地图 + 擢银行动按钮(openDeployPool→showDeployDock 弹窗，pickDeployUnit 选中单位进 royroyDeployMode)；④log-card 行动记录(sidebarActionLog)。
- 弹窗：设置 modal、擢银行动 modal、胜利/victory-overlay、骰子/dice-overlay、反应奇袭 surprise-qte、伏击 ambush-alert（覆盖旧定义统一深蓝风格）。

### 关键坑
- **双 template 事故**：第一次 replace_in_file 因 old_str 与实际文件不匹配失败，第二次虽成功但旧 template 有 768 行（原文件含大量浮动卡片 faction-panel/action-panel/action-log/dice-overlay），只覆盖前 312 行，导致 313-768 行旧 HTML 孤立残留（含第二个 `</template>`），SFC 编译会失败（单根 template 红线）。用 `sed -i '' '313,768d'` 删除残留，grep 确认仅 1 个根 `<template>` + 1 个根 `</template>`（207 行是内部 `<template v-if>` 闭合，合法）。
- 新增 script 变量/函数（3358 行附近）：enemySearchText/enemyDetailUnit/showSettings/showDeployDock/filteredEnemyUnits(currentSideKey/currentSideLabel/actionMenuVisible/actionMenuStyle/goHome/goExit/toggleSettings/openDeployPool/focusUnit/apText/rangeText/unitMainView)，并在 openDeployPool 后补 pickDeployUnit（旧 template 引用但未定义 → 部署池弹窗崩溃）。
- `allUnits` 是 script 顶层 computed（1670 行），filteredEnemyUnits 引用正确；HexGridCanvasEngine 经 defineExpose 暴露 centerOn/zoomIn/zoomOut/zoomReset（1219 行），focusUnit 用 hexGrid.value.centerOn 正确。

## NewBattleView 底栏排版修正（用户复核：此前完全不符设计稿）
- **问题**：此前 bottom-bar 是三段式横条（回合信息/快捷行动按钮/结束回合按钮），与用户原始画稿「四栏式 lower-zone（230px 固定、40%透明）」完全不符。
- **修正**：删除 bottom-bar 三段式，改为四栏 lower-zone（flex 行、gap10、padding10/12、bg rgba(8,22,44,0.4)、border-top + backdrop-filter blur2px、flex:0 0 230px）。四栏：
  - ① lz-unit：uc-name + uc-jump 坐标跳转按钮(focusUnit→selectUnitById→centerOn)
  - ② lz-attr：attr-grid(HP/护盾/攻击/防御/机动/射程 2列) + attr-actions(移动/战术行动/防御/待机 ap-action-btn) + attr-skills(普通攻击+activeSkillList 战术技能 ap-skill-btn)；复用 startAction/selectTacticalSkill/onTacticalSkillClick
  - ③ lz-map：BattleMinimap(:engine=hexGrid :size=180) + 部署期 map-deploy-btn(openDeployPool→showDeployDock/pickDeployUnit)
  - ④ lz-log：sidebarActionLog 行动记录
- 新增 script：focusUnit / showDeployDock(ref) / openDeployPool / pickDeployUnit / unitMainView(computed) / activeSkillList(computed 展开 skillGroups)。
- 构建产物 index-McI2P6at.js，容器 Up healthy，资产哈希一致。

### 验证与部署
- 本地 `npx vite build --mode development` ✅ 编译通过（无 SFC 错误）。
- rsync frontend/（exclude node_modules/dist/.git）→ 106.54.197.69；服务器 `npm run build`(EXIT=0) → `docker compose build --no-cache mecha-frontend`(BUILD_EXIT=0) → `up -d --no-deps mecha-frontend`(UP_EXIT=0)。
- 线上核对：mecha-frontend Up healthy；curl index bundle grep `side-nav` 命中(1) ✅ → 新布局已上线 8081。

## 下半区 lower-zone 改为屏幕 25vh（用户要求"屏幕下方1/4位置"）

用户反馈下半区应占屏幕下方 1/4。修改 `.lower-zone` CSS：
- 原：`flex: 0 0 230px`（固定像素）
- 改：`flex: 0 0 25vh; min-height: 200px; max-height: 320px;`（视口高度 25%，带上下限保护）
- 本地 vite build 通过 ✅，rsync + 服务器 build + 重建 mecha-frontend 容器已部署上线，容器 healthy。

## 删除多余侧边栏 + 修复地图被遮挡

用户反馈：①左侧竖向 `side-nav`（"侧边栏"+主页/设置/退出）是多余的，删除；②下半区 25vh 把地图画布遮挡过多，上半区需占满剩余空间。
- 删除 template 中 `<nav class="side-nav">…</nav>` 整块（31行）+ script 中相关逻辑（goHome/goExit/toggleSettings/showSettings 等保留无引用但无害，实际 template 移除）。
- `.upper-zone` 改 `flex: 1 1 auto`（原 `62%`，与下半区 25vh 争空间致地图被压扁）；`.side-nav` 及 `.sn-btn/.side-nav-*` 样式全删。
- 本地 vite build 通过 ✅，rsync + 服务器 build + 重建 mecha-frontend 已部署，容器 healthy，curl 确认 `side-nav` 类名已不出现。

## 按截图颜色区分区域（天蓝顶栏 + 珊瑚红下半区）

用户截图标注：顶部状态条=**天蓝色**、下半区=**珊瑚红色**（#FF6347），右侧信息栏保持深蓝。
- `.topbar`：背景 `rgba(30,144,255,0.92)` 天蓝色；文字/按钮改为白色系（白字+半透明黑底+白色边框）；end-turn 按钮用淡红 `rgba(255,80,80,0.5)`。
- `.lower-zone`：背景 `rgba(255,99,71,0.92)` 珊瑚红；内部所有子元素（lz-block/lz-title/按钮/文字）统一为白字+半透明黑底+白色边框；active 状态用金黄 `rgba(255,220,80,0.45)` 高亮。
- `.info-rail`（右侧栏）：保持深蓝系但提高不透明度至 0.92（原 0.4 太透），子块也加浓到 ~0.7~0.9，确保在珊瑚红下半区旁视觉平衡。
- 本地 vite build ✅(1.68s)，服务器 build ✅(5.98s)，mecha-frontend 容器 healthy 已上线。

## 战斗界面 UI 改造（增量拆分固定位置，2026-08-15 晚，当前线上状态）

用户要求把界面改造拆成增量步骤，一个个固定样式与位置，每改一个部署验收一个（避免大改返工）。AI 无法看截图，依赖用户文字描述 + ask_followup 选项确认布局。

### 线上布局骨架（Step 1 已上线，git 原始版本回退后重建）
- `dm-battle-layout`（`flex flex-row` 根容器）→ `dm-main`（`flex-1 flex flex-col`：battle-header + battle-toolbar + `.canvas-area`(flex-1) + `.bottom-bar`(flex 0 0 200px 占位)）→ 右侧浮动卡片（`.floating-action-panel` 行动面板 `width:220px` fixed / `.floating-faction-panel` 阵营面板 fixed / `.floating-action-log` 行动记录 fixed）。
- 注：之前按截图做的「下半区25vh+删侧边栏+天蓝顶栏珊瑚红下半区」已被用户要求 `git checkout` 全部回退到原版，重新以增量拆分方式改造。当前底部栏只占位文字「底部操作栏（骨架占位 · Step 1）」。

### Step 2（本次）：右侧固定边栏（阵营列表 / 敌方检索 / 敌单位详情）
- 用户确认：右侧这三个区块要与「原来的行动面板」保持一样宽（即 `.floating-action-panel` 的 **220px**），先标记占位。
- 改动 `frontend/src/views/NewBattleView.vue`：
  - template：在 `</div><!-- end dm-main -->` 之后、`dm-battle-layout` 闭合之前，新增 `<div class="right-sidebar">`，内含 3 个 `.rs-block`（`.rs-faction-list` 阵营列表 / `.rs-enemy-search` 敌方检索 / `.rs-enemy-detail` 敌单位详情），各带 `.rs-block-title`（含「占位 · Step 2」small 标记）+ `.rs-placeholder` 占位文字。
  - CSS：`.right-sidebar { flex: 0 0 220px; width: 220px; }`（与行动面板同宽），深蓝底 `#0d2240` + 左边框，内部 3 区块 `flex:1` 等高平分。

### Step 2 修订（用户反馈「大致对了，但敌方检索只要一个输入框；阵营列表和敌单位详情应替换出现」）
- template 改动：
  - 敌方检索：去掉标题+占位块，改为 `.rs-search` 内单个 `<input class="rs-search-input" v-model="rsEnemySearch" placeholder="敌方检索…">`。
  - 阵营列表 / 敌单位详情 合并为 `.rs-faction-detail` 区块，用 `v-if="!rsSelectedUnit"` 切换：无选中→显示「阵营列表」占位；选中→显示「‹ 返回」按钮 + 「敌单位详情」占位（返回按钮 `@click="rsSelectedUnit=null"`）。
- script：新增 `const rsEnemySearch = ref('')` 与 `const rsSelectedUnit = ref(null)`（在 `sidebarActionLog` 注入之后）。
- CSS：`.rs-faction-detail { flex: 1 1 auto }` 占满检索框下方全部空间；新增 `.rs-search-input` / `.rs-back-btn` 样式；移除旧的 `.rs-block`/`.rs-enemy-search`/`.rs-enemy-detail` 三区块样式。
- 部署：rsync → 服务器 build ✅(`index-_Xoee_13.js`) → 重建 frontend 容器 → Up healthy。线上已生效。

### 布局修正（用户反馈「底部操作栏应延伸到右下侧，阵营列表贴它上面」）
- 原结构：bottom-bar 在 dm-main 内（canvas-area 下方），right-sidebar 是 dm-battle-layout 的 flex 兄弟(height:100%)。底栏只横跨画布区，不延伸到右侧栏下方。
- 改为：根容器 `.dm-battle-layout` 改 `flex-direction: column`；新增 `.dm-row { flex:1 1 auto; min-height:0; flex-direction:row }` 包裹 dm-main + right-sidebar（占满上方）；bottom-bar 移出 dm-main，作为 dm-battle-layout 的第二行（flex-col 下自动横跨整宽，贴住 right-sidebar 底部）。
  - 实现：第2行根 div 加 `dm-row` 包裹开标签；right-sidebar 闭合后 `</div><!-- end dm-row -->`；bottom-bar 提到 dm-row 之后、action-panel 之前。
- 效果：底部操作栏向右延伸到右下侧（横跨画布+右侧栏整行），右侧栏底部自然贴在底栏上方；阵营列表（rs-faction-detail，占 right-sidebar 下方空间）即贴在底栏之上。
- 部署：rsync → 服务器 build ✅(`index-CEx9nDrJ.js`) → 重建 frontend → Up healthy。线上已生效。

### Step 3：点击棋子弹出的行动菜单（跟随点击点，占位骨架）✅ 已上线
- 需求：点击棋子才弹出菜单，菜单跟随点击点（红框样式），战术行动可用技能后续按竖向列表做。
- 坐标来源（合规）：改 `frontend/src/components/HexGridCanvasEngine.vue` 的 click 监听，emit `'cell-clicked'` 时附带 `screen:{x,y}`（相对 canvas 左上角像素：`e.clientX-rect.left` / `e.clientY-rect.top`）。外壳只消费，不在 Vue 侧自行算屏幕坐标（符合宪法「坐标由引擎算」）。
- template：在 `.canvas-area` 内（engine 之后）加 `<template v-if="unitActionMenu.visible">`：透明遮罩 `.uam-mask`（inset:0, z-index:40, @click 关闭）+ `.unit-action-menu`（absolute, left/top = x/y, z-index:41），含 `.uam-title`（单位名·行动）+ 4 个占位按钮 `unitActionMenuItems`（移动/攻击/战术行动/详情，竖向）。
- script：新增 `const unitActionMenu = reactive({visible:false,x:0,y:0,unit:null})`、`unitActionMenuItems`、`closeUnitActionMenu()`、`onUnitActionMenuClick(item)`（占位仅 addLog）。`onHexClick({q,r,screen})` 开头先 `unitActionMenu.visible=false`，命中棋子（默认选择分支 `selectUnit(clickedUnit)` 后）若 `screen` 存在则打开菜单并定位。攻击分支（executeSkillAttack）不弹菜单。
- CSS：`.unit-action-menu` 深蓝半透明卡片 + 边框 + 阴影；`.uam-item` 竖向按钮（hover 琥珀高亮）。
- 部署：rsync(含 engine) → 服务器 build ✅(`index-CrtiDsku.js`) → 重建 frontend → Up healthy。线上已生效。
- 待后续：菜单项接入真实动作（移动→actionMode='move'；攻击→选中攻击；战术行动→展开 Step 4 竖向技能列表；详情→打开右侧栏敌单位详情并 rsSelectedUnit=该单位）。

### Step 4：战术行动可用技能竖向列表 ✅ 已上线
- 需求：点菜单「战术行动」展开该单位真实携带技能（排除被动）的竖向列表，每行显示 名称 + 射程/AP。
- 实现：
  - 菜单状态 `unitActionMenu` 增 `mode: 'main' | 'skills'`；新增 `openUnitActionMenu(unit,x,y)`（打开并重置 mode='main'），`onHexClick` 命中棋子改调它。
  - `unitActionMenuSkills = computed`：取 `unitActionMenu.unit.skills` 过滤掉被动类型（被动集合复用 active 侧定义 counter/block/.../lucky），无 skills 时回退装备（本步仅过滤，未接装备回退）。
  - `onUnitActionMenuClick(item)`：key==='skill' → `mode='skills'`；其余占位 addLog + 关闭。
  - `onUnitActionMenuSkillClick(skill)`：占位 addLog 打印 `技能名（射程 getSkillRange）` + 关闭。复用已有 `getSkillRange(skill)`（读 cast_range/max_range/range_max/range）。AP 显示 `skill.ap`（存在才显示）。
  - template：菜单按 `mode` 渲染；skills 模式含 `‹ 返回` 按钮（mode='main'）+ `.uam-skill-list`（竖列 `.uam-skill`：`.uam-skill-name` + `.uam-skill-meta` 射程/AP）+ 空态「无可用技能」。
  - CSS：`.uam-back` / `.uam-skill-list`(max-height 260 scroll) / `.uam-skill` / `.uam-skill-name` / `.uam-skill-meta` / `.uam-skill-empty`。
- 部署：rsync → 服务器 build ✅(`index-wbENy6BA.js`) → 重建 frontend → Up healthy。线上已生效。
- 待后续：技能项接入真实施放（选中技能 → actionMode='tactical' + selectedAttackSkill，触发 range 预览，见 2226 行 getSkillRange(selectedAttackSkill) 逻辑）。

### 底栏填充真实内容 ✅ 已上线（用户指出 Step1 占位未替换）
- 原底栏一直是 Step 1 占位文字「底部操作栏（骨架占位 · Step 1）」，用户指出后补全。
- 改动 `bottom-bar` template 为三段式：
  - 左 `.bb-meta`：第 N 轮 + 当前阵营（复用 `battleState?.round` / `currentFactionLabel`）。
  - 中 `.bb-actions`：选中单位（且非 visitor）→ 快捷行动按钮（移动/战术行动/防御/待机/取消选择），禁用态沿用 `has_moved/has_acted/has_defended/standby`（与浮动 action-panel 一致）；未选中 → 提示「未选中单位 · 点击棋子进行操作」。
  - 右 `.bb-end`：结束回合按钮（`endTurn`，部署阶段禁用 `isDeployPhase`）。
  - 复用函数：`startAction()`/`clearSelection()`/`endTurn()`、`selectedUnit`(ref)、`currentFactionLabel`(computed)、`isVisitor`、`isDeployPhase`。
- CSS：`.bottom-bar` 改 flex 横向 + gap + padding；新增 `.bb-section/.bb-meta/.bb-round/.bb-faction/.bb-actions/.bb-unit/.bb-hint/.bb-end/.bb-btn(.active/:disabled)/.bb-cancel/.bb-end-btn`。行高维持 `flex:0 0 200px`。
- 部署：rsync → 服务器 build ✅(`index-dSJN0UYa.js`) → 重建 frontend → Up healthy。线上已生效。
- 注：底栏快捷按钮与右侧浮动 action-panel 的「可用行动」功能重复但定位不同（底栏=全局快速操作条，面板=选中单位详情）。可接受；若用户认为冗余可后续收敛。

### Step 5：右侧栏三区块接入真实数据 ✅ 已上线
- 需求：阵营列表 / 敌方检索 / 敌单位详情 接入真实战斗数据。
- 数据源复用：`factionGroups`（按战术角色 attack/defense/ambush 分组，每项 {key,role,label,color,units[],order}）、`allUnits`、`getFactionLabel/getRoleLabel/getFactionColor/getRoleColor`、`getSkillRange`。
- 实现：
  - `rsListGroups = computed`：复制 factionGroups，按 `rsEnemySearch`（单位名包含，小写）过滤 units，去除空组 → 检索框实时生效。
  - 阵营列表区：遍历 `rsListGroups`，每组 `rs-group-head`（角色色点 + label + 数量），组内 `rs-unit` 按钮（名称 + HP 条 + HP 数值），点 → `rsSelectedUnit = u` 切换到详情。
  - 敌单位详情区：`rsSelectedUnit` 非空时显示 名称/阵营/角色/位置(q,r)/HP(条)/护盾/技能列表（name + getSkillRange）；`‹ 返回` → `rsSelectedUnit=null`。
  - 辅助：`rsHpPercent(u)` 计算 HP 百分比。
  - 检索框 `rsEnemySearch` 实时过滤列表（空态「无匹配单位」）。
- CSS：`.rs-list/.rs-group-head/.rs-dot/.rs-unit/.rs-unit-hpbar/.rs-unit-hp/.rs-empty` 与详情 `.rs-detail/.rs-d-title/.rs-d-row/.rs-hpbar/.rs-d-sub/.rs-d-skill/...`。
- 部署：rsync → 服务器 build ✅(`index-D2cZtbzw.js`) → 重建 frontend → Up healthy。线上已生效。
- 说明：Step 3 菜单「详情」项仍为占位（未联动打开右侧栏 rsSelectedUnit）。如需点菜单「详情」→ 右侧栏显示该单位，可在 onUnitActionMenuClick 的 key==='detail' 分支设 `rsSelectedUnit = unitActionMenu.unit`。

### 全局进度（2026-08-15 晚）
- ✅ Step 1 骨架（顶部条+画布满屏+底部栏占位）
- ✅ Step 2 右侧固定边栏 220px（阵营列表/敌方检索/敌单位详情 三区块，检索单输入框，列表与详情替换出现）
- ✅ 底栏延伸到右下侧、阵营列表贴其上方
- ✅ Step 3 点击棋子行动菜单（跟随点击点）
- ✅ Step 4 战术行动技能竖向列表（名称+射程/AP）
- ✅ Step 5 右侧栏三区块接入真实数据
- ⬜ Step 6 全局统一配色（用户此前提到「按截图颜色区分」但已回退，待用户决定是否要统一配色）
- ⬜ 菜单「详情」项联动右侧栏（可选增强）
- 部署：rsync → 服务器 `npm run build` ✅ → `docker compose build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`。资产哈希 `index-DRCnYvw1.js` 一致，容器 Up healthy。
- 设计要点：right-sidebar 作为 `dm-battle-layout` 的 flex 兄弟节点，dm-main 的 `flex-1` 自动让出 220px，画布不重叠。行动面板/阵营面板/行动记录仍是 fixed 浮动卡片叠在其上，互不冲突。

### 待办（后续 Step，逐个验收）
- Step 3：点击棋子弹出的行动菜单（跟随点击点）。
- Step 4：战术行动可用技能竖向按钮列表（每行一技能，名称+射程/AP）。
- Step 5：右侧边栏三区块接入真实数据（阵营列表/敌方检索过滤/敌单位详情）。
- Step 6：全局统一配色。

## lower-zone 四栏微调（用户 4 条反馈）
1. 行动记录宽度 == 右侧阵营列表(220px)：`.lz-log { flex:0 0 220px; width:220px }`（right-sidebar 基准 220px）。
2. 属性&装备挪到单位卡片：合并原 ①lz-unit + ②lz-attr 为一栏 `lz-unit`（属性网格 + 行动按钮 + 技能栏都在单位卡片内）。
3. 技能栏展开可滚动：`.attr-skills` 改 `flex:1 1 auto; min-height:0; overflow-y:auto; align-content:flex-start`（去掉原 max-height:56px 限制，占满单位卡片剩余高度）。
4. 地图略缩图去框：原 `lz-map` 用 `.lz-block`（有 bg+border+radius+padding），现改 `.lz-map` 为纯透明 flex 居中容器（无框），仅含 BattleMinimap + 部署期按钮。
- 现 lower-zone 三栏：单位卡片 / 地图(无框) / 行动记录(220px)。
- 构建产物 index-eQtKn4z5.js，容器 Up（healthy），资产哈希一致。

## lower-zone 单位卡片拆分（用户反馈：单位卡片弹太多、技能不该在里面）
- 上一步误把属性+装备+技能全塞进「单位卡片」，导致点击后弹出过多。
- 修正：恢复为四栏，按原始设计稿分工：
  - ① lz-unit（单位卡片）：仅 uc-name + uc-jump 坐标跳转（精简，点击不弹一堆）
  - ② lz-attr（属性&装备）：attr-grid + attr-actions + attr-skills（技能栏回到此栏，flex:1 占满高度可滚动）
  - ③ lz-map（无框）
  - ④ lz-log（220px 等宽右栏）
- 教训：技能栏属 attr-card，不属 unit-card。
- 构建产物 index-BSJ3CSO1.js，容器 Up，资产哈希一致。

## lower-zone 属性值归位 + 技能栏滚动（用户反馈）
- 用户要求：属性值挪回单位卡片，技能栏用滚动条体现、不扩大框位。
- 修正：
  - ① lz-unit（单位卡片）：uc-name + uc-jump + attr-grid（HP/护盾/攻击/防御/机动/射程）——属性值回归单位卡片
  - ② lz-attr（属性&装备）：仅 attr-actions（移动/战术行动/防御/待机）+ attr-skills（技能栏）
  - .attr-skills 改 `max-height:64px; overflow-y:auto`（去掉 flex:1 1 auto 撑大框），固定高度+滚动条
- 构建产物 index-uIHW3rGD.js，容器 Up，资产哈希一致。

## 技能栏单幅显示扩大（用户反馈：单幅显示扩大到四条技能）
- .attr-skills 改 `max-height: 64px` -> `max-height: 120px`（约可显示 4 条整行技能按钮，height~26px/条 + gap4；超出滚动）。
- 注意 .ap-skill-btn 是 `width:100%` 整行纵向堆叠（非横向 wrap），故单幅=纵向可见条数。
- 构建产物 index-DWkcZYN-.js，容器 Up healthy，资产哈希一致（容器内已确认）。

## 删除四个旧浮动框（用户要求：删掉画面上的旧行动记录/行动画板/阵营单位/地图缩略框）
- 删除了 4 个 Phase13 旧 floating-card（均 fixed 定位、z-index:100，与新布局重复）：
  - battle-minimap 浮动层（canvas-area 内右下角，line ~108-117）
  - floating-faction-panel 阵营单位（~139-209）
  - floating-action-panel 行动画板（~277-549，含部署/战斗/技能选择完整面板）
  - floating-action-log 行动记录（~551-572）
- 功能已由 right-sidebar(阵营列表/检索/详情) + lower-zone(单位卡片/属性装备/地图/行动记录) 承载。
- 用 sed -i '277,572d' 删除（行号因前序删 A/B 块已偏移）；删除后根容器结构闭合正确，lint 0 错误。
- 残留死代码：factionPanelCollapsed/Pos/startDrag/toggleFactionPanel、actionPanelCollapsed/Pos/toggleActionPanel、actionLogCollapsed/Pos/toggleActionLog/actionLogHeight 等 ref/函数仍在 script 但已无模板引用（lint 未报，运行无害）。
- 构建产物 index-Da7g5aZ1.js，容器 Up healthy，资产哈希一致。
