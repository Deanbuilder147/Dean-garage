# 2026-08-17

## NewBattleView.vue 删除残留「普通攻击」按钮
- 用户澄清早期误选主战场 canvas 实为想删「属性&装备」中的普通攻击按钮。
- 删除 `frontend/src/views/NewBattleView.vue` 第 236 行 `⚔ 普通攻击` 按钮（`selectTacticalSkill(null)` 入口，Phase 32 已移除普通攻击，属死代码），并更新函数注释。
- 主战场 `<canvas>` 未动（删之棋盘即消失）。
- 部署：rsync → 服务器 `npm run build` → `docker compose build --no-cache mecha-frontend` → `up -d --no-deps`。8081 验收 HTTP 200，资产 `index-Ct0YH3p-.js`。bundle 中余 2 处「普通攻击」为合法文案（骰子说明 + 技能兜底 `${l?l.name:"普通攻击"}`），非按钮。
- 技能卡片改单行布局：`.sdc-line`（flex-wrap）合并技能名/类型/属性/词条于同一行，用 `·` 分隔；旧 `.sdc-head`/`.sdc-row` 样式替换为 `.sdc-line`/`.sdc-sep`。重建后资产 `index-B6GzgNoJ.js`，8081 HTTP 200。
- 修复属性&装备栏技能重复（蓝=默认态/黄=选中.active态 #ffb000，实为同技能渲染两次）。根因：combat.ts 3135 行 `skills: safeParseValue(unitData.skills)` 战斗初始化未去重，rooms.ts 的 `passthroughSkills`（按 skill_key 优先 / name|category 兜底）只覆盖整备列表未覆盖战斗路径。修复：前端 `activeSkillList` computed 加 Map 去重（同语义 key）。重建后资产 `index-B3bmg7g2.js`，8081 HTTP 200。后端战斗路径去重未接（如需彻底根治可让 combat.ts 接入 passthroughSkills）。
- 单位卡片浮层化：删 `.uc-name`/`.uc-jump`/`.attr-grid` 旧样式；`uc-sprite` 改 `position:relative` 高 150px，名字→`.uc-name-overlay`（顶部 absolute + text-shadow + pointer-events:none），属性→`.attr-overlay`（底部 absolute + 自下而上渐变底衬 0.82→0），与机体视图叠合。清理「单位卡片」「属性&装备」lz-title 标题。重建资产 `index-C_mGR0rK.js`，8081 HTTP 200。
- 机体图片不显示（卡片一直「无视图」）：根因链路 combat.ts `createBattleUnit` 写入 `viewUrls` 取自 `pendingUnits`，而 pending-units 经 `...u` 保留原字段；若前端整备/部署时 unit 对象未带 `view_urls`，战斗单位即缺。前端修复：①`pickAnyView` 增强回退（方向3→0→遍历0-6任一）；②新增 `unitViewUrlsCache` + `watch(selectedUnit)` 在战斗单位缺 viewUrls 时按 id 调 `hangarAPI.getUnit` 补全。注意前端用 `safeParseJson`（非后端 `safeParseValue`）。重建资产 `index-CZ6IZpE8.js`，8081 HTTP 200。若库里该单位根本没上传七视图则仍「无视图」（数据上传问题，非前端）。
- 主战场跟随光标黄色框改虚线尖角：`HexGridCanvasEngine.vue`（注意是 .vue 不是 .ts）悬停高亮段（~917 行）原整圈实线描边改为 `setLineDash([5,4])` 虚线框 + 6 尖角顶点黄色小圆点（r=2.2，顶点按 planar/iso-deformed/iso 三种与描边路径严格对齐）。绘制后 `setLineDash([])` 复位。重建资产 `index-DLmmVhJo.js`，8081 HTTP 200。
