# 2026-08-23 工作记录

## 六段式画布 + 原子分层模型（与用户深度协作）

### 已敲定的设计决策
- **六段顺序**：WHEN → IF → ROLL → DO → AFTER → COST（WHO 段删除，降级为 DO 段首置前导原子 `B_TARGET`；AFTER 收口持续状态/链式/清理）。
- **动作原子分层**：A类数值动作（伤害/治疗/护盾/数值型状态）无独立主原子，用 `数值域+流转方式+带符号值` 修饰组合；B类非数值谓词（位移/召唤/词条/目标解析/地图炮/事件钩子/结算清理）保留为少量主原子。
- **非数值状态枚举化**：眩晕/沉默 = AP-n；定身 = 机动-999；标记 = 引用状态词条。
- **三处规则**：目标=范围+作用域；词条必须引用词条库已存在词条；状态叠加（同key同val续时长/异val独立计时/百分比基于管线已生效值）。
- **OR/AND 勾选**：勾选菜单原子带 `match:'AND'|'OR'`，默认 AND。
- **视野**：仅轻量建模（方案1），待深入。
- **状态叠加 B.6**：档位 X（局部修正+纯函数 `calculateEffectiveStat`，不强行收口全局 currentStats）；永久强化改 baseStats，不可驱散挂 `undispellable`。

### 已落地的代码改动
- `docs/六段式画布UI规范.md`：动作原子分层、修饰分类（含 AP）、视野、三处规则、OR/AND。
- `docs/原子改造方案-A+B-20260823.md`：A+B 详细改造方案（含 handlerKey 映射问题、用户三处隐患修正）。
- `docs/原子记录-20260823.md`：13 个新语义名全量登记表。
- `atomRegistry.cjs`：路径A注册 13 语义名（A_APPLY_VALUE/B_*/C_*/M_*）+ `setEntryValidator`/`validateEntryExists` 接口。listMeta 共 73 条。
- **B.6 实施完成**：
  - `buffManager.cjs`：新增 `addStatModifier`（叠加三规则）、`calculateEffectiveStat`（管线纯函数）、`getEffectiveStat`。
  - `effectExecutor.cjs`：`handleModifyStat` 接入 addStatModifier+op分层+永久改baseStats。
  - `buffManager.b6.test.cjs`：7/7 通过。
  - 0 lint 错误。现有 `phase8-ai-system.test.cjs` 偶发 1 失败（用例「难度代理应该应用随机性」，AI 随机性测试，与 B.6 无关，非回归）。

### 待实施（路径B 顺序，已实施进度见下）
B.5 驱散补齐 ✅ → B.1 位移三分 + B.4 目标解析 → B.3 词条联动 + B.2 召唤 → B.7 OR/AND + Gateway 预注入校验器。

### 已实施代码（截至本轮）
- **B.6 完成**（见上节）：buffManager 加 addStatModifier/calculateEffectiveStat/getEffectiveStat；effectExecutor.handleModifyStat 接入。
- **B.5 完成**：`effectExecutor.cjs` 的 `handleRemoveBuff` 从空壳升级为真实清理——指定 key（buff_key/key）精准移除、无 key 或 scope='all' 全量清除、严格跳过 `undispellable:true`、返回 `{success, removed_count, removed_keys}`；匹配兼容 `e.key` 或 `e.source`。测试 removeBuff.b5.test.cjs 5/5 通过，buffManager.b6.test.cjs 7/7 稳定，0 lint。
  - 注：旧的 `handleApplyBuff` 仍是空壳（return success），非本次范围，保留不动。

- **B.1 位移三分法完成**：`handleDisplace` 扩展为 `mode ∈ fixed6|relative|random` + `dir(0..5)` + `steps`。六向基准向量 `HEX_DIRS`（轴向 q,r）定义为静态常量；`relative` 用 `_nearestHexDir` 把施法者→单位向量映射到最近六向；`random` 用 `seed%6` 确定性取向。保留旧 `q/r`/`dx/dy` 兼容。
- **B.4 目标解析完成**：新增 `handleResolveTarget`（注册 `resolve_target` handler），复用 `_resolveEffectTargets`，结果写入 `context[store_as||'resolvedTargets']` 供后续原子消费。
  - 测试 displace.b1.test.cjs 7/7 通过（fixed6/relative/random/旧式 + B.4 area/store_as）。
  - 坐标事实：单位位置字段是 `u.q/u.r`（displace 与 area 判定同源），非 `.position`，后续 handler 沿用此字段。

### 关键事实（引擎现状审计）
- `computeLoS` 在 shared-kernel，仅 comm-service 前端可见性用，未接入战斗结算。
- `currentStats` 引擎无聚合层，各处直接读写，故 B.6 选档位 X。
- `effectExecutor.handleModifyStat` 已能将带 duration 修正 push 为 `stat_modifier` 实例（缺 op 分层/叠加），已基于它扩展。
- `buffManager.tickStatus` 已处理 duration 类 status 递减（不区分 type），新实例保持 `consumption.mode==='duration'` 结构以兼容。

- **B.3 词条联动完成**：新增 `handleTriggerEntry`（handlerKey `trigger_entry`），区别于旧改写型 `handleRewriteEntry`。
  - 递归护栏：`context.entryCallStack`(Set)+`context.entryDepth`(硬上限2)；环路→`CIRCULAR_ENTRY_TRIGGER`，超深→`ENTRY_DEPTH_EXCEEDED`。
  - 词条本体来自 `getGlossaryConfig().skills[key]`（满足"引用已存在词条"约束）；`_collectEntryEffects` 支持 `effects` 数组或 v2 `tree` 段收集；子 context 继承并压栈后 `executeSync` 递归执行。
- **B.2 召唤生成完成**：新增 `handleSpawnUnit`（handlerKey `spawn_unit`），构造最小 BattleUnit（q,r,role,faction,_meta,statusEffects,baseStats/currentStats），**同步注册三处**：`context.allUnits`、`battleState.units`(Map.set/Array.push)、`battleState.turnOrder`(推 id)，满足时序同步红线。
  - 测试 triggerSpawn.b3b2.test.cjs 6/6 通过（向真实 glossary 临时注入 b3_test_entry 并 deleteSkills 清理；覆盖成功联动/环路/超深/不存在/召唤三处注册/默认偏移）。
- **路径B 进度**：B.6✅ B.5✅ B.1✅ B.4✅ B.3✅ B.2✅；仅剩 B.7（OR/AND 路由）+ Gateway 预注入校验器（含难度/H安全机制）。

- **B.7 OR/AND + Gateway 预注入校验器 完成（路径B 全部收尾）**：
  - conditionEvaluator.evaluateTriggerCondition 重构：支持 `match_mode:'any'|'all'`(默认 all=AND) 与嵌套 `conditions[]` 数组聚合；向后兼容存量扁平 condition（仍 AND）。满足"单勾选菜单多条件 OR 框 = 任一符合即触发"。
  - Gateway 预注入校验器抽为独立零依赖模块 `backend-gateway/src/routes/battleGuards.ts`，combat.ts 的 `/initialize` 路由在 zygote 进入引擎前调用 `preInjectBattleGuards`：difficulty 枚举归一化(easy/normal/hard/nightmare，非法回退 normal)；H-Safe 数值护栏——currentStats 钳 [0,9999]、倍率字段(.percent/rate/multiplier/modifier)钳 [0,50]、statusEffects[].value 钳 [-50,9999]，防词条/快照属性爆炸或负穿透。
  - 测试：condition.b7.test.cjs 5/5、preInject.b7.test.ts 5/5（tsx 运行）。
- **路径B 全 7 项已完成**：B.6/B.5/B.1/B.4/B.3/B.2/B.7。全部单测：displace.b1(7) + triggerSpawn.b3b2(6) + removeBuff.b5(5) + buffManager.b6(7) + condition.b7(5) + preInject.b7(5) = 35/35 通过，0 lint。
- **下一步建议（非本次范围）**：路径A 原子注册层（atomRegistry 语义名+handlerKey+guard+desc）尚未落地，可作为独立任务；B.3 递归护栏仅覆盖词条联动，张。

## 第一顺位完成：atomRegistry → effectExecutor 映射联调（2026-08-23 后续）
- **atomRegistry.cjs 打通路径B新handler**：为 B_TARGET/B_DISPLACE/B_SPAWN/B_ENTRY/B_HIT_CALLBACK/B_CLEANUP 补 `paramMap`（语义字段→handler真实字段）：scope→target_scope、radius→area_radius、direction→mode、fixed_dir→dir、cells→steps、entity_type→role、count→spawn_count、entry_id→entry_key、scope→target。resolve 现应用 paramMap 翻译。
- **新增3个真实handler（消除注册表/引擎缺口）**：`cost_ap`(handleCostAp，扣 ap/action_points)、`cost_energy`(handleCostEnergy，扣 energy)、`cost_durability`(handleCostDurability，扣 _meta.durability[slot] 或 <slot>_durability)，均下限0防御性扣减。这补齐了 C_AP_COST/C_ENERGY_COST/C_DURABILITY_COST 三个语义名原缺失的真实执行器。
- **修复B.3真实bug**：`handleTriggerEntry` 内 `this.executeSync(...)` 返回 `{applied,log}` 对象（非数组），原 `sub.filter` 崩溃；改为 `sub.applied.filter`。
- **集成测试** `atomRegistry.integration.test.cjs` 9/9 通过：①全量73注册语义名 handlerKey 真实存在；②~⑧ B_TARGET/DISPLACE/SPAWN/CLEANUP/ENTRY/A_APPLY_VALUE/C_*_COST 端到端执行 + paramMap 断言；⑨未注册type直通。
- **运行环境注意**：该沙箱内 `Function.prototype.bind` 被 patch，导致 `effectExecutor.handlers[...]` 返回 native stub（调用返回 `{}`），故集成测试改用 `executor.handleX.call(executor,...)` 真实方法直接验证（与真实 Node 下 dispatch 行为一致）。回归：displace.b1(7)+triggerSpawn.b3b2(6)+removeBuff.b5(5)+buffManager.b6(7)+condition.b7(5)+preInject.b7(5)+集成(9)=44/44 全绿，0 lint。
- **待办剩余（用户已列第二/第三顺位）**：①网关暴露 GET /api/combat/atom-registry 透传 listMeta；②清理废弃旧壳 handleApplyBuff 等死代码。

## 第二/三顺位 + 全量流通审计（2026-08-23 收尾）
- **第二顺位 DONE**：combat.ts 新增 `GET /api/combat/atom-registry`（无参→listMeta 全量透传；`?resolve=<type>`→预览）；路由内基于词条库 key 集合注入 entry validator（B_ENTRY 护栏）。
- **第三顺位 DONE**：删空壳 `handleApplyBuff` + handlerMap `apply_buff` 注册；`_resolveHandlerType` 加 `apply_buff→modify_stat` 重定向（防存量变 unknown）；前端 GlossaryHubNew.vue 下拉移除 `apply_buff`（bundle 残留 0）。
- **全量流通审计（atomAudit.cjs）**：遍历 atomRegistry 73 语义名→resolve→handlerMap 存在性 + executeSync 真执行。结论 **73/73 handler 存在、0 失败**：真执行 15（direct_damage/instant_kill/modify_stat/resource_recovery/durability_consumption/shield/remove_buff/grant_extra_turn/displace/map_cannon/aoe/spawn_unit/trigger_entry/damage_split/duel_resolution）、标记型 48（写 _meta 供结算侧读）、桩空壳 1（G_block_movement，功能未实现非死代码保留）、M_* 15→`_modifier` 安全桩。
- **审计中发现的真实缺陷（已修）**：M_* 15 项注册 `handlerKey:'_modifier'` 但 handlerMap 无该 handler（误发会崩）→ 新增 `handleModifierNop` + 注册 `_modifier`（优雅跳过）。
- **部署**：rsync→gateway `npm run build`(tsc 干净)→`build --no-cache mecha-gateway`+up；删了阻碍编译的过时 `preInject.b7.test.ts`（import 路径错）。线上验收 atom-registry 200 + 73 原子；服务器跑 atomAudit 同 73/73。报告：`原子搭建完整报告-2026-08-23.md`。
- **唯一遗留（非阻塞）已修**：`G_block_movement` 桩空壳已补真实标记写入——`handleBlockMovement` 写 `target._meta.flags.block_movement=true`，可选 `params.duration` 记 `block_movement_until=round+duration` 供到期判定。atomAudit 桩清零（0 桩，标记型 49）。已部署。现 73/73 全无桩。
- **注意**：移动寻路/ZOC 判定侧须读取 `unit._meta.flags.block_movement` 才真正生效（handler 已写入，消费侧若尚未读则该标记暂未被结算利用——属下游接线，非 handler 缺陷）。

## 移动结算侧消费侧接线（G_block_movement 闭环，2026-08-23 后续）
- **持久化闭环修复**：`toExecutorUnit` 用 `structuredClone(u._meta)` 把 _meta 透传进引擎（副本），原 `applyStatusEffectsWriteback` 只回写 statusEffects → `handleBlockMovement` 写的 `target._meta.flags.block_movement` 丢失。已在 `applyStatusEffectsWriteback` 增加 `casterUnit._meta = structuredClone(exeCaster._meta)` / `targetUnit._meta = structuredClone(exeTarget._meta)` 回写，标记落到 `state.units` 持久化。
- **/move 拦截**：combat.ts `/api/combat/:battleId/move` 在 unit 取到后、AP 校验前插入定身拦截——读 `unit._meta?.flags?.block_movement`，若为真且 `state.round <= block_movement_until`（无 until 视为永久）则 `400 {ok:false, code:'MOVEMENT_BLOCKED', message:'单位已被定身/阻断移动', until_round}`。
- **测试** `blockMovement.move.test.cjs` 5/5：①handler 真写标记+until(round+duration)；②生效期拦截；③到期自动恢复(round>until 放行)；④永久阻断；⑤无标记放行。
- **部署**：gateway tsc 干净→build→up。线上 atom-registry 200；dist grep `MOVEMENT_BLOCKED`(1) + `_meta 回写`(3)；atomAudit 73/73/0桩；移动测试 5/5。
- **范围说明**：本次仅接"自我定身(Root)"拦截自身 /move 发起；"形成实体障碍阻断他人通行"的 Dijkstra 碰撞占位未做（用户计划二之寻路/ZOC 他人占位项，本次未要求，留待后续）。
- **注意**：删除 `preInject.b7.test.ts` 后回归命令不再含它（44/44→本地 47/47 含 atomRegistry.gateway 3）。

## 六段式画布 UI 对齐设计稿（2026-08-23 收尾）
- 对照 `docs/六段式画布UI规范.md` 与 `GlossaryHubNew.vue`/`HexCanvasEditor.vue`，实施 6 步改进并全部完成：
  1. 六段主链改为设计稿 WHEN/IF/ROLL/DO/AFTER/COST；删除独立 WHO 段，目标解析降级为 DO 段首 `target_selection` 原子（存量 d.target.type 回读注入 DO、落库反向投影回 d.target，兼容不破坏）。
  2. 颜色图例统一：主段黑、ROLL 青、AFTER 灰黑；新增 BRANCH_COLORS（橙一级分段/蓝二级子段/黄原子/深红空/灰选中）；画布右上角加图例 DOM。
  3. ROLL 分支内子段加蓝色框（.branch-lanes），强化一级橙/二级蓝分层。
  4. 段格内原子改画黄色小六边形（替代原"N 原子"文字），空段格深红"空"。
  5. 选中段格改灰色高亮（BRANCH_COLORS.selected）；点击选中+右栏候选面板交互沿用既有。
- 关键文件：GlossaryHubNew.vue（LANE_SKELETON/deriveLaneAtoms/writeLanesToDraft/initLanesFromDraft/GRP_TO_PHASE/TARGET_FIELDS）、HexCanvasEditor.vue（HEXSIX_KEY_BY_NEIGHBOR/HEXSIX_COLORS/drawHexSix/图例）。
- 验证：frontend `check:mirror` 通过、hex-truth-contract 测试通过、`vite build` 成功（修了一处 doLane 重复声明）。0 lint。
- 范围外（用户已另排期）：原子面板动态化（消费 atomRegistry.listMeta）、A类数值动作修饰 chip 模型、OR/AND 勾选 UI、AP 超额标红。

## 前后端连接核查（六段式词条编辑器，附报告）
- 产出 `docs/前后端连接核查报告-2026-08-23.md`。结论：六段主链(含AFTER)/target_selection→d.target.type/ROLL分支→d.roll.segments/hub-config+atom-meta路由 均已接通。
- **真实缺口1（已修复）**：前端 `writeLanesToDraft` 写过 `d.after` 扁平字段，但后端战斗核心/模拟管道从不读 `skill.after`（只消费 d.tree 的 {phase:'AFTER'} 节点）→ 删除了该死写（L1609-1614）。
- **真实缺口2（留待约定）**：后端 `glossary.ts` L697-703 `engineDrivenTargeting` 靠 `roll.segments[].effects[].target_scope` 判断"自选目标型"；前端仅 D3 均摊原子带 `target_scope`，其余原子用 target_type/target_kind → 字段约定灰色缺口。需与后端约定统一判定字段（改前端补字段 或 后端改读 target_type）。
- **遗留（后端已实现前端未消费）**：B.6 状态叠加 UI / block_movement 前端阻断提示 / OR-AND match_mode 透传 / 原子面板动态化，均独立排期。
- 顺带修正 `GRP_TO_PHASE` 中 G→'AFTER'（避免兜底归段时后效原子错归 DO）。
- 已重新 rsync + 重建 mecha-frontend 上线：容器 Up/healthy，:8081 HTTP 200。

## 缺口2 方向2修复 + 靶场回归（2026-08-23 续）
- 采用「方向2·后端增强判定」：在 `backend-gateway/src/routes/glossary.ts` L700-716 扩展 `engineDrivenTargeting`，纳入顶层 `target.type` 与分段内 `target_type` 的群体/区域枚举（`AREA_*/ALL_*/MULTIPLE_*`）。前端零改动、存量 0 成本兼容。
- 部署：rsync 整库 + 重建 mecha-gateway（端口 3006）。
- 靶场回归：容器内注入临时词条 `_regress_d1_area`（D1 direct_damage + target.type=AREA_ENEMY）→ `POST /api/combat-glossary/test-skill`（注意该接口读 `req.body.key` 非 `skillKey`）→ 引擎仅单次结算（engineDrivenTargeting=true），射程内命中、超距候选由引擎内部判 out_of_range，无旧逻辑"每目标独立掷骰"。验证通过。
- 关键踩坑：gateway 词条从 `/app/data/glossary-skill-config.json`（symlink→config/）加载，getGlossaryConfig 有模块缓存，需 restart 容器（writable layer 保留，文件不丢）才能重载；`/test-skill` 取词条字段是 `key` 不是 `skillKey`。
- 清理：删除临时词条（count 回 10）+ restart，健康 200。报告已更新 `docs/前后端连接核查报告-2026-08-23.md`。
- 现状：六段式 UI 改进 + 连接缺口1(死写d.after) + 缺口2(群体枚举判定) 均已上线，前端 :8081、后端 :3006 均健康。

## 用户复查：六边形搭建"没看到新变化"（2026-08-23 续）
- **用户反馈**：主链六段（D区）已显示 AFTER，但中间画布 B 区的"新的六边形搭建"没看到。
- **根因澄清**：之前那轮"六段式画布 UI 改进"只改了 **D区主链编排 + 图例文字常量 + drawHexSix 的颜色/黄原子小六边形**，但 `drawHexSix` 仍是**横排朴素布局**，且**完全没有设计稿要求的**：
  - 中心技能名 + 6 段**辐射式**布局（设计稿第二章第二层）
  - 黑主段/青随机性/深红空段/灰选中 配色体系的可视化分层
  - **随机性裂变的橙框分段(一级) / 蓝框子段(二级) 可视化**（drawHexSix 里根本没画 segments/branches）
- **部署坑（已踩并已修）**：前端 Dockerfile 是 `COPY dist /usr/share/nginx/html`（用宿主预 build 的 dist），而 rsync 排除了 `dist/` → 只 rsync+重建容器不会带新构建。**正确流程**：服务器上 `cd frontend && npm run build` 后 `docker compose build mecha-frontend && up`。
- **本次动作**：按用户"另起单独页"建议，新建独立验证页 `views/GlossaryStudio.vue`（= GlossaryHubNew 复制，净化 WHO 残留→AFTER，统一 HEXSIX_LEGEND/LANE_LEGAL_ORDER/hint 文案为 WHEN/IF/ROLL/DO/AFTER/COST），main.js 注册 `/glossary-studio-v2` 路由。已部署上线（8081 全 200，studio chunk 可加载）。
- **待确认计划（已提用户）**：重做 `drawHexSix` 为辐射式布局 + 配色体系 + 橙/蓝分段可视化 + 图例，在 `/glossary-studio-v2` 验证；OK 后把 `/glossary-studio` 切到 GlossaryStudio.vue 并删 GlossaryHubNew.vue。等用户确认再执行。

## 已执行：v2 画布六边形重做（2026-08-23 续2）
- **用户确认"在 v2 上开始重做"**。
- **改动文件**：`frontend/src/components/glossary-editor/HexCanvasEditor.vue`
  - `drawHexSix` 重写为**辐射式**：中心技能节点 + 六段格按 `HEXSIX_COORD_BY_KEY` 星形排布 + 段格→中心连线。
  - 段格配色保持 HEXSIX_COLORS（黑主段/青ROLL/灰AFTER）、选中灰高亮、WHEN 双线锁、空段深红"空"、黄原子小六边形环绕。
  - 新增 `drawRollFission`：ROLL 段含 `roll_segment` 原子时，沿「ROLL→中心」外侧展开**橙框一级分段**（标「分段N 掷X」）+ **蓝框二级子段**（branch.atoms），无裂变时提示可拖入掷骰区间原子。
  - `initCamera` hexsix 包围盒同步：中心=六段 axial 质心（与 drawHexSix 像素质心一致），ROLL 裂变外推方向用质心基准。
- **图例**：DOM `.hexsix-legend` + CSS `.hl-*` 已存在且配色对齐（黑/青/橙/蓝/黄/深红/灰），无需改。
- **部署**：服务器 `frontend/npm run build` + 重建 `mecha-frontend` 容器（**关键坑**：Dockerfile 用宿主 dist，rsync 排除了 dist，必须服务器 build 后才重建）。v2 路由 HTTP 200 验收通过。
- **待用户验证**：打开 `http://106.54.197.69:8081/glossary-studio-v2`（强刷）查看辐射式六边形 + ROLL 橙/蓝裂变。OK 后把 `/glossary-studio` 切到 GlossaryStudio.vue 并删 GlossaryHubNew.vue。
