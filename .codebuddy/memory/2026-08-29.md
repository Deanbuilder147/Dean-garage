# 2026-08-29 工作日志

## 战斗画布背景功能（mecha-universe-engine / frontend）
- 背景由 `composables/useBattleCanvasBg.js` 提供（模块级 reactive 单例 + localStorage 键 `battleCanvasBg`，含 `color`/`image` dataURL）。
- 引擎 `HexGridCanvasEngine.vue` 通过 CSS 背景层（`sandboxStyle`）显示底色/背景图，canvas 本身透明；背景固定贴容器视口，**不随格子层 transform（offsetX/offsetY/scale）移动**，仅被 `ShapedCanvas` 的 clip-path 裁成六边形。
- 设置入口已从 `AdminCenterView.vue`（后台"战斗画布背景"区块）**迁移**到 `NewBattleView.vue` 的「⚙ 地图视图设置」面板（`MapViewSettingsPanel.vue`，dominator 专用），复用同一 `bg` 单例。AdminCenterView 原区块已删除。

## 地图编辑器地形库（NewBattlefieldView.vue）
- 修复：新增地形 `addTerrainType()` 之前只写内存、需手动点"保存地形库"才会落库（删除 `deleteTerrainType` 却是即时落库，行为不一致）。已改为新增后也立即 `saveTerrainConfig()` 落库，硬刷新不再丢失。

## 部署
- 前端通过 `rsync src/ → 服务器 → docker compose build mecha-frontend → 重启` 部署；线上 JS hash 最新为 `index-Cx8Do92I.js`，容器 `mecha-frontend` healthy。
- 服务器：106.54.197.69:8081，ssh key `~/Desktop/watson.pem`。
