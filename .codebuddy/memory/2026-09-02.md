# 2026-09-02 工作日志

## 1. 解读 UI 素材 `资源 12.svg`（桌面文件）
- 尖顶正六边形宝石图标，`viewBox 0 0 76.66 88.51`，共 **7 个 polygon**：1 个外框（`fill #002763` 深藏青 + `stroke #ff3b7c` 粉，宽 0.66）+ 内部 **6 个三角切面**（均 `fill #ff3b7c`，opacity 顺时针递减 1.0/0.85/0.7/0.55/0.4/0.25，形成风车式明暗渐变，光源来自右上）。
- 精算：外六边形外接半径 43.88、内六边形 40.05，环形边框厚 3.83；宽高比 √3/2:1；无 text/渐变/滤镜/位图/阴影（符合「阴影程序化」约定）；有 Illustrator 导出冗余重复点（无害）。
- 用户意图：把该素材整体视作**一个完整词条**，点击后 6 个切面可全开/单开，展开后露出**原子输入框**。

## 2. 六边形词条「切面 3D 轴翻转」原型（4 轮迭代，用户已验收）
原型文件：`mecha-universe-engine/hex-word-prototype.html`（独立 HTML 规范原型，用户工作流：先原型确认再替换 Vue 组件）

迭代过程：
1. **v1**：2D `rotate(180deg)` + 右侧固定灰色六角面板。用户否决 → 要求编辑框在切片内。
2. **v2**：明确「翻转 = 沿外边镜面翻转」+「编辑框内嵌在翻开的切片里」。此时我用 2D 绕点旋转（等腰三角形下绕底边中点转 180° ≡ 沿底边镜面反射，几何等价）。
3. **v3**：用户纠正 → 要的是**真 3D 轴翻转**（给出 flip-card 参考代码：`perspective` + `preserve-3d` + `rotateY(180deg)` + `backface-visibility:hidden`）。据此重写为 flip-card 结构。
4. **v4**：修复「点任何一片都只翻开左上角那片」的 bug → 重叠矩形容器吞点击，改为容器几何角度命中。

用户结论：**「可以，很棒，请把它记录下来，作为词条编辑器的重要 UI 之一」**

详见 `MEMORY.md` 新增章节「★『词条编辑』= 六边形切面 3D 轴翻转 UI」，含完整几何常量、翻转轴/背面轴/质心表、4 个踩坑与交互汇总。

## 3. ★ 正式命名：「词条编辑」（用户 2026-09-02 拍板）
- 该 UI 定名 **「词条编辑」**，此后勿再称「六边形词条 / hex-word」。
- 原型文件随命名改名：`hex-word-prototype.html` → **`glossary-edit-prototype.html`**（内容中 title/h1 同步改为「词条编辑」）。
- 归属：词条库（GlossaryHub）体系下的**编辑界面组件**，与既有词条库浏览/管理界面并列。

## 4. 待办
- 用户表示「我再来完善一下词条库的整体」→ 词条库整体架构/信息架构由用户主导规划，后续大概率会有词条库结构调整的系列需求（六段式、原子面板、词条列表与「词条编辑」的挂载关系等）。
- 「词条编辑」集成进 `frontend/src/views/GlossaryHubNew.vue`（用户尚未下达集成指令，等确认后再动）。
- 集成时需将 6 片对接六段式 WHEN/IF/ROLL/DO/AFTER/COST，编辑框接 atomRegistry 的 21 个 B 原子下拉与参数表单。

## 5. ★★ 语义原子定义核查（用户指令「重新核查语义原子」）
产出报告：`mecha-universe-engine/docs/语义原子定义核查报告-2026-09-02.md`

**用户的线索「应该是某一份有 after 段的才是正确的」→ 完全正确。**

### 五份定义与判定
1. ✅ `GlossaryStudio.vue` **L1055-1065** `LANE_SKELETON` = `WHEN→IF→ROLL→DO→AFTER→COST`（grp: A/B/D/F/G/E）—— **现行正确版本，有 AFTER**
2. ❌ `GlossaryHubNew.vue` **L766** `HEXSIX_LEGEND` = `['WHEN','IF','WHO','ROLL','DO','COST']` —— **旧版残留**（有 WHO 无 AFTER），而 Studio L766 已是 `['WHEN','IF','ROLL','DO','AFTER','COST']` → **两副本在此处分歧（本次新发现）**
3. ❌ `docs/phase-tree-六段式合并方案.md` L33 `PhaseEnum=['WHEN','IF','WHO','ROLL','DO','COST']` —— 自称"唯一真相源"但**无 AFTER**，写于 2026-08-19，代码对齐的是 **2026-08-23 规范**更新 → **文档已过期，声明失效**
4. ⚠️ 后端 `atomRegistry.cjs`（73 个 register / 54 个去重 handlerKey）—— 有 4 个【AFTER】原子（`B_ENTRY`→trigger_entry、`B_HIT_CALLBACK`→trigger_entry、`B_KILL_STACK`→charge_layer、`B_CLEANUP`→remove_buff）、3 个【COST】、2 个【DO】；但**无段位体系**
5. ❌ **我自己的错误**：`frontend/src/battle/glossary/atomGroups.js` 的 `PHASES`(8 段 PREP/COST/CHANNEL/WHEN/IF/ROLL/DO/AFTER) 与 `GRP_TO_PHASE` —— **是我上次凭空编写的，仓库中无任何出处**，与所有现存定义均不符，**待修正**为六段 `WHEN/IF/ROLL/DO/AFTER/COST`

### 原子层差异（后端 73 ↔ 前端 90，两副本 atomGroups 90/90 完全一致）
- 引擎已注册但编辑器无入口 7 个：`cost_ap` / `cost_energy` / `cost_durability` / `resolve_target` / `spawn_unit` / `trigger_entry` / `_modifier`
  - 最严重：**COST 三原子缺失** → 玩家无法给词条加"消耗 AP/能量"
  - AFTER 段无 `trigger_entry`（词条联动）入口
- 编辑器有但后端未注册 6 个：`trigger` / `condition` / `roll_segment`（**UI 分类名，违反 atomGroups 顶部 L1620-1623 自己声明的红线**）/ `target_selection`(后端名 resolve_target) / `spawn_items`(后端名 spawn_unit) / `custom`
- `api/client.js` 已封装 `getAtomMeta()`（对接 `/combat-glossary/atom-meta`）但**全前端无人调用** → 漂移根源

### 待用户定夺 3 点
1. `LANE_SKELETON` 标 `ROLL→grp 'D'`，但 atomGroups 里 **D 组是伤害族、C 组才是掷骰** → 两套编号体系还是真错？
2. docs 与代码谁为真相源（按用户线索代码胜，docs 需标注过期）
3. 是否删除 `GlossaryHubNew.vue` 副本

### 待办（等用户确认后再动）
- 修正 `atomGroups.js` 的 PHASES 六段 + GRP_TO_PHASE
- 同步 HubNew L766 HEXSIX_LEGEND
- 补 COST 三原子与 trigger_entry 到原子面板
- 前端改消费 getAtomMeta()

## 6. ★★★ 找到权威真相源（用户提示「工作区 docs 文件夹」→ 此前只搜了仓库内目录，漏掉）
**工作区根目录 `/Users/dingxuyang/CodeBuddy/20260604120036/docs/` 另有 4 份关键文档（≠ `mecha-universe-engine/docs/`）：**
- `docs/原子记录-20260823.md`（103 行）—— **设计定稿**
- `docs/原子改造方案-A+B-20260823.md`（247 行）—— **实施方案 + 用户 2026-08-23 拍板决策**
- `67原子引擎落地与编辑器词库实施计划-2026-08-08.md`（462 行）—— **此前判定"缺失"的 67 原子清单，实存**
- `原子搭建完整报告-2026-08-23.md`（92 行）

### 六段定稿（原子记录 L11-14，用户判断完全正确）
```
WHEN(时机) → IF(条件) → ROLL(随机性) → DO(执行) → AFTER(后效) → COST(代价)
```
- `WHO` 段已删除，**降级为 `DO` 段首置前导原子 `B_TARGET`**；`AFTER` 收口持续状态/链式/清理
- 与 `GlossaryStudio.vue` LANE_SKELETON 一致 ✅；与 `phase-tree-六段式合并方案.md`（有WHO无AFTER）冲突 → 后者过期

### 原子总览（原子记录 二/三/四）
- B 类主原子 11：DO = B_TARGET(resolve_target)/B_DISPLACE(displace)/B_SPAWN(spawn_unit)/H_MAP_CANNON(map_cannon)；AFTER = B_ENTRY(trigger_entry)/B_HIT_CALLBACK(trigger_entry)/B_KILL_STACK(charge_layer)/B_CLEANUP(remove_buff)；COST = C_AP_COST/C_ENERGY_COST/C_DURABILITY_COST
- A 类数值入口 1：`A_APPLY_VALUE`(modify_stat)（伤害/治疗/护盾/加减属性统一入口）
- 修饰分类 15（横切）：M_SCOPE/M_FACTION/M_STAT/M_PART/M_STATUS/M_SKILL_TYPE/M_SKILL_ATTR/M_TERRAIN/M_PHASE/M_RANGE/M_OP/M_PROB/M_DURATION_UNIT/M_FLOW/M_VISION
- L103 自述 listMeta() 共 73 条，新增 13 语义名

### ★ 路径 B 实测已全部落地（09-02 核查，原子记录的"❌待新增"已过时）
B.1 位移三分(fixed6/relative/random, effectExecutor L1287-1309) / B.2 spawn_unit / B.3 trigger_entry(含 entryCallStack + depth≥2 递归护栏) / B.4 resolve_target / B.5 remove_buff(真实移除+有测试) / B.6 calculateEffectiveStat(buffManager:451) / COST 三原子(handleCostAp/Energy 真实扣减下限0) —— **全为真实实现，非空壳**
→ 故「补前端原子入口」是**纯收益、立即可用**（此前误判为后端也未实现）

### ★ 唯一明确未落地：B.0 契约对齐（方案第 1 步）
`shared-kernel/src/contracts/phase-tree.contract.ts` **L26** 仍为 `z.enum(['WHEN','IF','WHO','ROLL','DO','COST'])`（含 WHO 无 AFTER），且 `dist/contracts/phase-tree.contract.js:24` 同步为旧值 → 改后必须 `npm run build`

### ★ 疑点 1 已查明（LANE_SKELETON.grp 多处错误）
实测 atomGroups 各组：**C 组 9 个 = 掷骰/判定类**（roll_none/roll_segments/duel_resolution/damage_bonus_dice/manual_roll/luck_resolution 等）；**D 组 9 个 = 伤害族**（direct_damage/damage_reduction/instant_kill/armor_pierce/reflect_damage 等）
- `ROLL→grp:'D'` ❌ **应为 'C'**
- `COST→grp:'E'` ❌ E 组是 resource_recovery **资源回复**，非代价
- DO 段首原子(L1088) 标 `grp:'C'` + `effectType:'target_selection'` ❌ 真相源规定应为 **B_TARGET / `resolve_target`**
- `AFTER→grp:'G'`（G 组 grant_extra_turn 等）⚠ 与真相源（应为 B_ENTRY/B_CLEANUP 等 B 类）不符
→ **LANE_SKELETON.grp 字段整体不可靠，需按真相源重排**

### 用户拍板决策（改造方案 L234-239）
1 路径 A+B 全做先 A 后 B；2 空壳 handler 直接升级为真实实现；3 校验器接口认可 + Gateway 启动预注入（加载 glossary-skill-config.json 后 setEntryValidator）；4 记录文档路径确认为 docs/原子记录-20260823.md
实施顺序：契约对齐 → 路径A注册 → 路径B实现(B.1→B.4→B.3→B.2→B.5→B.6→B.7) → Gateway 预注入 → 测试+记录

## 7. 归档过时文档 + 执行六段 WHO→AFTER 对齐（用户指令「先归档盖过期戳，然后再执行」）
产出：报告已更新 `mecha-universe-engine/docs/语义原子定义核查报告-2026-09-02.md`（新增第五节「执行记录」）

### 归档（沿用既有惯例：顶部 4 行过期戳 + `历史开发报告/归档-<主题>/`）
- 新建 `历史开发报告/归档-六段式与语义原子/`
- `mecha-universe-engine/docs/phase-tree-六段式合并方案.md` → 归档 + 盖「过时文档·已归档」戳（含"自述唯一真相源已失效"警示 + 指出 shared-kernel 仍沿用其旧值属未同步遗留）
- `mecha-universe-engine/docs/skill-hierarchy-主谓宾定状补.md` → 归档 + 盖**二次过期**戳（其 08-08 戳内引用的六段 WHEN/IF/WHO/ROLL/DO/COST 亦已过期）
- **不归档**：`mecha-universe-engine/docs/结算管线-原子触发时机对齐表.md`（全文无段位定义、无更高版本取代，属结算管线领域仍有效），仅加状态说明

### 真相源就地更新（文件不过期，仅状态列过时）
- `docs/原子记录-20260823.md`：新增「✅ 2026-09-02 状态复核」；二节 11 行 `❌/⏳/⚠` → `✅ 已实现（09-02）`
- `docs/原子改造方案-A+B-20260823.md`：新增「✅ 2026-09-02 落地复核」，标明仅剩第①步契约对齐未完成

### 代码修正 8 处（六段 WHO→AFTER 全线对齐）
1. `shared-kernel/src/contracts/phase-tree.contract.ts` L26 `PhaseEnum` 删WHO插AFTER + 依据注释，**已 `npm run build` 同步 dist** ✅
2. `services/.../skillExecutor.cjs` L1507 `ORDER` 7段（WHO+AFTER并存）→ 6段；L1515 `phase==='WHEN'||'WHO'` → 仅 `'WHEN'`
3. `backend-gateway/src/routes/glossary.ts` L229 `PHASES` 删WHO插AFTER —— **修复 AFTER 段节点被 `?:'DO'` 强制归为 DO 的真实 bug**
4. `backend-gateway/scripts/migrate-phase-tree.cjs` L22 `LANE_SKELETON` 删WHO插AFTER
5. `GlossaryHubNew.vue` L766 `HEXSIX_LEGEND` 删WHO插AFTER
6. `GlossaryHubNew.vue` L1396 `LANE_LEGAL_ORDER` 删WHO插AFTER
7. `GlossaryHubNew.vue` L226 用户可见文案 → `六段式（WHEN/IF/ROLL/DO/AFTER/COST）`
8. 两副本 L1457/L1489 段位枚举注释订正（保留"原 WHO 段"等合理历史语境）

### 安全前提（已验证）
存量 `glossary-skill-config.json` 中 WHO 节点 = **0**（仅 DO×9、WHEN×9）；且 skillExecutor 原注释 L1503-1505 自述六段为「时机→条件→随机性→执行→后效→代价」不含 WHO → 代码留 WHO 系 08-23 插 AFTER 时**漏删**

### 验证
全仓 `'WHO'` 字面量归零 ✅ / shared-kernel src↔dist 一致 ✅ / 后端PHASES·引擎ORDER·迁移脚本三处一致 ✅ / 两副本 HEXSIX_LEGEND·LANE_LEGAL_ORDER 一致 ✅ / backend-gateway `tsc --noEmit` **exit 0** ✅ / 改动文件 lint **0 错误** ✅

### ★ 两处自我澄清（重要）
1. **「8 段 PHASES」从未写入代码**：`PREP`/`CHANNEL` 全前端搜索为 0，仅存在于报告初版描述，**未污染代码库**。实际共享内核 `frontend/src/battle/glossary/laneSkeleton.js` 完全正确（六段 + GRP_TO_PHASE 与前端 L1480 逐字一致 + 注释标来源行号）
2. **此前「LANE_SKELETON.grp 整体不可靠」的批评过重，已撤回**：`LANE_SKELETON.grp`（段默认展示组，仅供 createEmptyLanes，不参与归段）≠ `GRP_TO_PHASE`（实际归段映射，**正确**）。ROLL→grp'D' / COST→grp'E' 仅是展示瑕疵，**不影响归段结果**；真相源未定义 grp 字段，"按真相源重排"无依据

### 未执行（待用户决定）
1. 补前端 6 个原子入口（后端 handler 已全部真实实现，补即立即可用）：`cost_ap`/`cost_energy`/`cost_durability`/`trigger_entry`/`resolve_target`/`spawn_unit`
2. 命名对齐：`target_selection`→`resolve_target`、`spawn_items`→`spawn_unit`（现写法引擎侧无对应 handler）
3. 前端改消费 `getAtomMeta()`（api/client.js 已封装但全前端无人调用）
4. 部署上线（服务器 shared-kernel → backend-gateway → frontend 依次 npm run build 后重建容器）

## 8. 语义原子补齐 —— 执行完成（用户三决策全选推荐方案）
计划文档：`docs/语义原子补齐实施计划-2026-09-02.md`（已补第八节「执行记录」）

### 决策
① trigger_entry 语义名冲突 → **A 硬编码优先**；② target_selection vs resolve_target → **A 新增并保留 W 组**；③ 执行范围 → **A 三阶段全做**

### 实际改动
1. **三处 atomGroups**（GlossaryStudio.vue / GlossaryHubNew.vue / battle/glossary/atomGroups.js）各新增 7 个原子：
   - E 组 +3：`cost_ap`(E4) / `cost_energy`(E5) / `cost_durability`(E6)
   - G 组 +3：`trigger_entry`(G10, semantic:'B_ENTRY') / `hit_callback`(G11, semantic:'B_HIT_CALLBACK') / `cleanup`(G12, effectType remove_buff)
   - W 组 +1：`resolve_target`(W7) —— 与 W1–W6 的 target_selection 语义不同，注释已区分
   - H6 **改造**：`spawn_items` → `spawn_unit`（后端 handleSpawnItems 是空壳，handleSpawnUnit 才真实实现）
2. 两副本 atom-meta 回填逻辑加 `if (a.semantic) return`（硬编码优先）
3. 两副本 `ENGINE_HANDLERS` 登记 6 个新 effectType + `spawn_items`→`spawn_unit`
4. 共享内核 `atomGroups.js` 以 Vue 为准重建数组区块，消除提取时引入的空行差异 → 三处现**字节一致**

### ★ 两个执行中发现的关键点（计划外）
1. **`ENGINE_HANDLERS` 必须登记**：`GlossaryStudio.vue` L1792 的 ENGINE_HANDLERS 白名单被 `unknownAtomTypes`（L1810-1817）用于校验「原子面板 effectType 是否命中引擎真 handler」。**新增原子不登记会被标记为未知类型**。原计划未覆盖此步。
2. **`frontend/src/contracts/enums.mirror.js` 是自动生成文件，勿手动编辑**：真相源 `shared-kernel/src/enums.ts`，由 `scripts/sync-enums.mjs` 生成，CI 用 `test-enum-consistency.mjs` 断言相等。故 `SPAWN_ITEMS` 空壳枚举本次**未动**（须改源头再跑同步脚本）。

### 原子数
90 → **97**（新增 7；H6 是改造非新增，故非 98 —— 原计划写的 98 已订正）

### 验收（全部通过）
- 三处副本**字节一致** 97 原子 ✅
- ENGINE_HANDLERS 新增 6 个全部登记（白名单 64 条）✅
- lint **0 错误** ✅
- 8 个原子 `semantic → handlerKey` 转译全部成功 ✅
- paramMap 翻译全正确：`entry_id→entry_key`、`scope→target_scope`、`entity_type→role`、`count→spawn_count`、`radius→area_radius`、`buff_key/scope→target` ✅
- 硬编码优先**解决 B_ENTRY 与 B_HIT_CALLBACK 覆盖冲突**（trigger_entry→B_ENTRY、hit_callback→B_HIT_CALLBACK）✅

### 遗留（非本次引入）
`ENGINE_HANDLERS` 未登记 `condition` / `roll_segment` / `trigger` —— 历史遗留的 UI 分类名，违反 atomGroups 顶部 L1620-1623 自述红线（「禁止再用 UI 分类名」）

## 9. 构建验证 + 发现 2 处显示层遗留（用户问「能正常显示到前端了吗」）
- **已通过 `npm run build`**（2.21s，含 `check-mirror.mjs` 校验）。本地 8081 = node 静态服务器，cwd=`frontend/`，服务 **`dist/`**（不是 dist-dev）
- **验证通过**：8 个新原子全部进入 `dist/assets/GlossaryStudio-*.js` 与 `GlossaryHubNew-*.js`；`B_HIT_CALLBACK`/`resolve_target`/`spawn_unit` 均在；旧值 `spawn_items`/"H6 生成地图实体" 已移除；AFTER 已进产物（WHO 仅剩注释级残留）
- 🔴 **方法论教训（重要）**：词条编辑器是**代码分割的异步 chunk**（`GlossaryStudio-*.js` / `GlossaryHubNew-*.js`，各 ~82KB），**验证产物时不能只 grep 入口 `index-*.js`** —— 我第一次就这么误判成"改动没进去"。正确做法：先 curl 首页拿 chunk 名，再 grep 对应 chunk（或 `grep -r` 整个 `dist/assets/`）
- ⚠️ **发现 2 处显示层遗留（我此前注意到 L31 却遗漏处理，已告知用户待确认）**：
  1. `GlossaryStudio.vue` **L31**（HubNew 同位置）：右侧词条摘要面板仍渲染 `<div class="ec-phase">WHO 主语</div>`，且**缺 IF 与 AFTER 两行**（面板只列 WHEN / WHO / ROLL / DO / COST）
  2. W 组 `title` 仍为 `'W · 目标定义 WHO'`
  - 两者**不影响功能**（draft 字段读写与落库均正常），但界面会显示已删除的段名
  - 修法参考：WHO→「目标」，并按六段补齐 IF（`draft.conditions`）与 AFTER（`draft.after.effects`，见 deriveLaneAtoms L1102-1112）
- 提醒：线上（106.54.197.69:8081）为独立部署，本地 build **不会**同步过去，需另行 rsync + 服务器 build + 重建容器

## 10. galaxy-btn 星云按钮 → 加入动态效果库（anime-demo）
- 用户提供一段 `.galaxy-btn` CSS（conic-gradient 旋转描边 + 星点漂移），要求放进动态效果库
- 新建 `anime-demo/galaxy-btn.html`（独立演示页，沿用 rainbow-border.html 视觉规范）：7 个按钮实例（默认/极光/熔岩 三配色 + 大20px/标准16px/小12px 三尺寸 + 外溢光晕）、6 张技法卡片、折叠区展示完整源码（可一键复制）
- 同步进 `anime-demo/index.html`：① `<style>` 加 galaxy-btn 全套 CSS（带坑点注释）；② `buttons` 分类加 demo 卡片（该分类原只有 download-button 一个）
- **忠实保留用户原版 CSS 未改动**，变体通过覆盖 `--btn-primary/secondary/accent` 与 `--btn-font-size` 实现（原版全 em 单位，改字号即整体等比缩放）
- 发现并说明一处原版瑕疵：`overflow:hidden` 会裁掉 `::before` 的 `blur(8px)` 光晕 → 外部看不到发光；页面给了「外溢光晕」变体（去 overflow + 外层 drop-shadow）作对照
- 两个自查修复：① 复制按钮 `onclick` 加 `stopPropagation()`，否则点击会连带展开 `<details>`；② 源码抽取改用 `style.textContent` + 正则截取（CSSOM 的 `cssRules` 会丢注释，且会把变体代码混进来）
- 验证：JS 语法 ✅ / 正则抽到 139 行纯净原版 CSS（不含变体）✅ / HTTP 200 ✅

## 11. 后续方向（用户已确认）
1. 六边形切面「词条编辑」UI 将落到 **Prism 页**（`/glossary-forge-prism`），6 片对接六段 WHEN/IF/ROLL/DO/AFTER/COST
2. **优先待办：词条库面板的布局问题**（用户下一步要解决）
3. 可选：让 GlossaryStudio / GlossaryHubNew 改为 import 共享内核，消除三副本同步
4. 遗留：W 组 title `'W · 目标定义 WHO'` 含过时 WHO 字样（会显示在 Prism 原子面板）

## 12. anime-demo 全部 sample 六边形化（用户：重试任务）
- 在 `index.html` 的 `:root` 注册 `--hex-flat`(平顶) / `--hex-pointy`(尖顶) / `--hex-slim`(细长六棱柱) 三个 clip-path 多边形变量
- 用 `clip-path: var(--hex-*)` 改造全部 CSS 驱动的演示元素（**改 CSS 即让 43 个 demo 自动生效，无需逐改 JS**）：
  `.box`(42→48×42 平顶) / `.cell`(22→24×21) / `.bar`(改 hex-slim 六棱柱态) / `.mt-card` / `.tilt-card` / `.mag-btn` / `.dl-btn`(后两者加大 padding 防裁文字) — 共 7 类
- **刻意不改**：`.card`/`.stage` 容器（改六边形会裁掉 title/desc/code 文本）；`cube-stack` 的 `.item` 3D 立方体（属"3D"分类语义，改六棱柱要重写 6 面）；`svg` 分类（演示 SVG 路径动画本身）；`galaxy-btn`（专门设计的星云按钮，形状是其设计语言）
- 验证：clip-path 注入 7 处 ✅ / :root 变量 ✅ / `curl localhost:8126` HTTP 200 ✅
- 待用户确认：是否进一步把 cube-stack 改六棱柱、svg 分类路径改六边形

## 13. 六边形「向右旋转 30°」= 尖顶（pointy-top）
- 用户原话：所有六边形要和他「梦常用」一致，**向右旋转 30 度**。平顶右旋 30° 即标准尖顶（pointy-top），也是战棋地图最常用形态
- 改造：`:root` 删 `--hex-flat`/`--hex-slim`，仅留 `--hex-pointy`(polygon(50% 0%,100% 25%,100% 75%,50% 100%,0% 75%,0% 25%))；7 个引用处（box/cell/bar/mt-card/tilt-card/mag-btn/dl-btn）全部改用 `--hex-pointy`
- 同步把 `.box`(48×42→42×48)、`.cell`(24×21→21×24) 宽高交换，避免尖顶被压扁
- 验证：hex-flat/hex-slim 残留 0 ✅ / `--hex-pointy` 8 处 ✅ / box cell 高>宽 ✅ / curl 8126 HTTP 200 ✅

## 14. cube-stack 调参台新增「2D 六边形」选项
- 用户：调参台添加 2d 选项吧（仅改 cube-stack.html，不动 index 总览卡片的 3D 立方体）
- 实现：① select 加 `value="flat">2D 六边形（平面平铺）` ② 新增 `.cubes.flat` 纯 2D CSS（`--step: size*1.732`、`transform:none`、`clip-path` 尖顶六边形、`item:before/after{display:none}`）③ `buildStage` className 支持 flat ④ `exportCss` 用 `shapeVal/isFlat` 三分支（flat 不输出 preserve-3d、loop/pivot 无 3D 变换、item 走 2D clip-path）
- 验证：5 项注入 grep 全 1、exportCss 三元 3 处、HTTP 200、`new Function` JS 语法 OK
- ⚠ 并行 replace 时 Edit5/Edit7 报"未找到"但实际已生效，Edit6（loop/pivot 三元）真正未生效，已补做后重新验证通过
