# 2026-09-03

- 创建 `glossary-nav.html`（工作区根）：词条系统入口导航页，串联现有词条系统各入口——六段三层+原子组合编辑器 `glossary-six-phase-prototype.html`、六边形切面 3D 编辑 `mecha-universe-engine/glossary-edit-prototype.html`、Vue 视图源码（GlossaryForgePrismView/Studio/HubNew 等）、后端 `glossary.ts`、设计文档。含模块关系连接线 + Prism 六段泳道卡片。本地静态服务 `python3 -m http.server 8127`（根=工作区根）可访问。
- IDE 预览隧道报 `ERR_TUNNEL_CONNECTION_FAILED`（环境网络问题，与文件/服务无关）。已告知用户改用本机浏览器 `http://localhost:8127/glossary-nav.html` 或 `file://` 直接双击打开（相对链接正常跳转）。
- 修复 `glossary-six-phase-prototype.html`：凯库勒式六段分子从旧顺序 WHEN→IF→WHO→ROLL→DO→COST 改为 WHEN→IF→ROLL→DO→AFTER→COST；苯环标签同步重排；PHASES 数组删除 WHO、插入 AFTER。修复射程画布「形态=线性」时仍画整圈的 bug，circle/line 现在互斥。
- 清理 Vue 生产代码中 WHO 残留：`GlossaryStudio.vue` / `GlossaryHubNew.vue` 左侧面板 `WHO 主语` → `DO 目标解析`；`GlossaryStudio.vue` / `GlossaryHubNew.vue` / `atomGroups.js` 的 W 组标题 `W · 目标定义 WHO` → `W · 目标定义`；目标解析原子 key 从 `who_` 改为 `target_`；多处注释同步去 WHO。
- 纠正长期记忆：全仓 WHO 字面量并未完全归零，源码仍有少量历史注释与局部变量名。
