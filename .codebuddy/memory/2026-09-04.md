# 2026-09-04

- 下载并安装 **Chrontor 字体**（作者 Tianhao Zhang / TianType，SIL **OFL-1.1** 许可，科幻模块化几何字体，灵感来自 New Alphabet）。来源 `github.com/TianType/Chrontor`。已下载 `Chrontor.ttf`/`Chrontor.otf` 到 `mecha-universe-engine/frontend/public/fonts/`，并复制到 `~/Library/Fonts/`（系统级安装）。OFL-1.1 允许在游戏/软件中自由使用、内嵌、商用，限制为：保留版权声明、衍生版同 OFL 分发、修改版不得用 "Chrontor" 原名（RFN）、不得单独售卖字体本体。
- **修正旧网络约束记忆**：《本机网络约束》记 github.com 443 超时无法 clone/下载；本次实测 `raw.githubusercontent.com` 与 `api.github.com` 经 curl 可访问（之前因字体文件名误判为不可达）。`git clone` 仍可能超时，但单文件 raw/api 下载可用。
- Chrontor 字体已**全局接入** `mecha-universe-engine/frontend`：在 `variables.css` 的 `:root` 定义 `--font-chrontor` 字体栈（Chrontor 优先 + 中文/系统 fallback），在最后导入的 `glossary-theme.css` 顶部加 `@font-face` 并把 `html,body,#app` 默认 `font-family` 设为该变量。字体文件位于 `frontend/public/fonts/Chrontor.{ttf,otf}`（Vite 构建随 dist 拷贝，运行时 `/fonts/Chrontor.*` 可访问）。注意该字体为 New Alphabet 风格几何字，主要覆盖拉丁字符，中文自动回退。

- **词条库全页面中英双语化完成（2026-09-04）**，风格=中文主 + 英文小字副标 `.en-sub`（Chrontor 几何字 + 灰蓝小字，定义于 `glossary-theme.css`）。覆盖：① Prism 主编辑器（标题/按钮/labels/提示 + 方向 dirEn + 命中范围模式 enLabel + AOE 形状中英 + 原子面板接 enLabel）；② 原子选项层 `atomGroups`（共享内核 + Studio + HubNew 三副本加 enLabel/enTitle，97 原子/13 组，三副本一致，三模板接 .en-sub）；③ Carousel 主视图+组件（标题/表头/翻页按钮）；④ Blueprint 英文终端皮肤补 27 处中文副标；⑤ Studio/HubNew 旧单体页各 110 处页面 chrome 静态副标（原子面板已由选项层接好，未碰数据/逻辑）。全部 lint 0。**已部署**：rsync→服务器 `npm run build`(5.83s)→重建 `mecha-frontend` 上线 8081，`grep` 产物确认 Semantic Atoms/Trigger Timing/Blueprint 等双语文案已进入 Prism/HexHitGrid/Studio/HubNew/Blueprint/index chunk。

- **GlossaryStudio 可用性测试（2026-09-04）**：用户真实意图=模拟玩家在前端编辑《扫射》词条、测试六段式编辑器是否可用（**非还原、非修复**）。agent-browser 真实操作：登录 deantest → /glossary-studio → 点「扫射」→ 六段式 tab → 编辑。结果：列表 click 完全未生效（扫射无选中高亮，画布始终"请先选择词条"），编辑器不可用，阻塞于词条选中。已生成可用性测试报告 `历史开发报告/GlossaryStudio-六段式编辑器可用性测试报告-2026-09-04.md`（取代旧的"还原失效"报告）。遵守用户要求未改代码。

- **GlossaryStudio 源码缺失清单（2026-09-04 静态审查）**：六段式编辑器功能主体已完整（六段骨架+自定义段增删 / 原子拖拽 / 字段表单 / 子段≤3 / ROLL分支 regenRollBranches / AFTER后效段 / 保存写回 apiClient.put / 双语化 / 粗校验）。真实 gap：① `initLanesFromDraft` 按 `tree` 回读，ROLL/COST 段走顶层 `roll`/`cost` 字段时还原为空（`GlossaryStudio.vue:1226-1256` 缺失段只补 `{atoms:[],nest:null}`），且 `deriveLaneAtoms` 对 ROLL/COST/WHEN 仅生成只读 label 不带 effectType/fields/params（`1083-1086`/`1118-1121`/`1074-1077`）——即《扫射》还原后掷骰段与AP代价段为空；② 列表选中→画布不加载实测 bug 的运行时根因在 `skills.value[key]` 取空（`592-593`），非功能缺失（onSelect 链路本身完整 `617-618`）。薄弱项：原子字段级校验缺失、无撤销/重做、无导入/导出。待用户确认是否修复①。
