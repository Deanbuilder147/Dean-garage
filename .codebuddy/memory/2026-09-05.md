# 2026-09-05

- **词条类型两套分类（前端代码确认）**：① `entryType`（词条性质/大类，左侧列表筛选 activeCat 也用此）：`skill` 技能词条 / `special` 特殊词条 / `faction` 阵营词条 / `other` 其它词条（DetailPanel.vue:46-49；JsonCompilerPanel.vue:293 ENTRY_LABELS）。② `category`（战斗射程分类，决定默认射程）：`melee` 近战(射程1+bonus) / `ranged` 远程(3+bonus) / `auto` 自动化(0)（skillContract.js:22-27 SKILL_CATEGORIES + CATEGORY_LABELS）。另有 `action_type`(attack/passive 等动作类型) 与 `target_scope`(敌/友目标范围) 属不同维度，非"词条类型"。《扫射》= category:ranged + action_type:passive。

- 已根据用户 SVG 分镜 `/Users/dingxuyang/Desktop/词条库分镜.svg` 生成文档 `历史开发报告/词条库界面分镜解析-2026-09-05.md`，已升级为确定版 v3（按镜头组织）。SVG 内用 `<g>` 分组并命名出 **3 个镜头**：镜头1《词条类型》(entryType 四分类彩色六边形)、镜头2《词条选择》(左列表+中射程地图黄色轮廓背景)、镜头3《词条编辑》(右栏 when/if/roll/do/effect/cost 六段式 + 详细排列标题 + 词条明细/提示栏基础属性卡)。每个镜头 1220×783、红/绿/蓝框=左/中/右三栏边界；是对 GlossaryStudio 的重设计，目标用户=玩家(查看+编辑)。仍待补：各镜头中/右/左栏精确内容、6条形语义、effect是否=AFTER、基础属性字段清单、双语副标。
- 分镜文档已升级确定版 v4：固化 effect=AFTER 后效段、镜头2 流程(镜头1选 entryType→镜头2按类型列词条→选中进镜头3)、六段组件名(when/if/roll/do/effect/cost)。新增实现约束：后续前端实现须**直接复用 SVG `<g id>` 组件名**，不自行发明；文档第6节列全组件清单（含 词条类型选择/技能词条/特殊词条/阵营词条/其它词条/运动方向、词条选择/射程地图/map_wire、棱镜/宝石嵌入/详细排列/词条明细/提示栏 等）。该偏好已写入 MEMORY.md。
- 用户补充镜头3右栏细节：右侧「六条形」即 when/if/roll/do/after/cost 六组件(L→R)作六段导航/状态条；下方「编辑段落」大框(`详细排列`)可在其中选择编辑单独某一段。文档 v4 已更新，第8节现仅余 4 项(镜头2右栏/镜头3左栏精确内容、基础属性卡字段清单、双语副标、红绿蓝框语义)。
- 用户确认基础属性卡仅「近战/远程/自动化」三项(词条形态切换)。文档 v4 固化，第8节现仅余 3 项(镜头2右栏/镜头3左栏精确内容、双语副标、红绿蓝框语义)。分镜解析已基本完整，可按 SVG 组件名直接开工做前端(用户偏好：先原型确认再替换组件，历史有 glossary-edit-prototype.html 验收先例)。
- 原型 `/prototype/glossary-prototype.html` 迭代：① 镜头2 白色六边形(`cls-5`网格)即为该类型下词条列表(直接复用，可点→镜头3)；② 镜头1 交互改为**环形轮播+点击**：四个类型六边形(`技能词条/特殊词条/阵营词条/其它词条`)沿圆环排布、缓慢自转、可拖动旋转、点击任一即选中并平移到镜头2；原"拖入黄框"隐喻移除(镜头1_运动方向组隐藏)。透镜=相机取景(viewBox平移)模型不变。③ 景深：按世界角度 `t=(sin φ+1)/2` 连续缩放(0.68~1.34)+透明度(0.5~1)模拟远近(底部6点最大、顶部≈11点最小)，用户已确认"可以"。④ 四个六边形**原始尺寸已归一化**(`ns=TARGET/bw`，`sc=ns·depth`)，使远近纯由位置决定；落地真实组件时 SVG 源四个 `<g id=…词条>` 也应统一尺寸。
- **部署**：Lighthouse 实例 `root@106.54.197.69`（SSH 别名 `myserver`，密钥 `~/Desktop/watson.pem`，config 在 `~/.ssh/config`）。站点放 `/var/www/glossary/`，用 `systemd` 服务 `glossary`（`python3 -m http.server 80`）托管，对外地址 **http://106.54.197.69/**（端口80防火墙已放行）。注：Lighthouse 的 API 集成令牌失效(调用报 Token verification failed)，故改用 SSH/SCP+systemd 部署。更新=重新 scp `prototype/` 下两文件到该目录。下一步用户看效果后改 镜头3（词条制作）。
- 已将 mecha 前端菜单(`frontend/src/components/layout/TheSidebar.vue` 的 `navItems`)中「词条展示」改为外链 `http://106.54.197.69/glossary2/`(新标签打开)，使应用内可点击访问该词条库原型页；流程：scp 单文件→服务器 `cd frontend && npm run build`(check-mirror+vite)→`docker compose build --no-cache mecha-frontend && up -d --no-deps` 部署生效(8081 在线200，链接已进产物)。原型最终应统一进 mecha-frontend 词条库页面(单一入口8081约束)。
