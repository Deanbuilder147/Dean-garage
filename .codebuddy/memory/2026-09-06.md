# 2026-09-06

- 完成全机报告文件盘点与技术债整理：覆盖工作区根(32 .md)、历史备份(119 .md)、历史开发报告(194 .md)、mecha 项目根(8 .md)、桌面「机甲战棋」(267 .md/699 文件)、「alpha3.0 系统」(38 .md)、`.codebuddy/memory`(31 .md)、桌面其它技术报告(Godot-MCP 适用性分析/正六边形地图制作方法等)；并清点 agents/rules/plans 等持久化目录。**soul 文件未定位**(全机 `*soul*` 递归搜索 0 命中)。
- 产出《项目文件盘点与技术债报告-2026-09-06.md》存于工作区根。技术债分级：P0=glossary 独立站违反单入口约束(端口80)、atomGroups 三副本一致性、userId 误用、数据契约错位；P1=Lighthouse 令牌失效/dist-dev 污染/compose version 废弃/orphan 容器/JS chunk>500kB/非 git；P2=13 文件 TODO/FIXME 扫描；P3=历史报告堆积+backup0729+副本；P4=镜头3 未实现+tactical 死代码。
- **TD-02 收口完成（本地）**：核查发现 `hexUtils.js`「致命 TODO」系误报（仅 `@deprecated` 注解+解释性注释，数学 truth 完整且与 shared-kernel 逐字镜像），未改动；`atomGroups` 三副本中 `battle/glossary/atomGroups.js` 已是单一源，但 `GlossaryStudio.vue`/`GlossaryHubNew.vue` 仍内嵌副本→已改两文件为 `import { atomGroups } from '../battle/glossary/atomGroups.js'` 并物理删除内嵌块(各30030字符)，lint 0 错误。备份在 /tmp/*.vue.bak。待用户确认后按强约束构建部署（前端 build + compose build --no-cache mecha-frontend + rsync）。
- **TD-01 本地归整完成**：将 `prototype/glossary-prototype.html` 拷为 `frontend/public/glossary2/index.html`、`prototype/词条库分镜.svg` 拷为同目录（Vite 构建后由 8081 同源 `/glossary2/` 提供，原型用相对路径 `./词条库分镜.svg` 取图天然匹配）；`TheSidebar.vue:163`「词条展示」外链由 `http://106.54.197.69/glossary2/` 改为 `/glossary2/`，全局扫描确认无其它 80 端口 hardcode。待与 TD-02 同批构建部署后，停止 80 端口 systemd `glossary` 服务。
- **TD-01 + TD-02 同批部署上线（2026-09-06）**：rsync 前端源码→服务器 `npm run build`→`compose build --no-cache mecha-frontend`→`up -d --no-deps`；验证 `mecha-frontend` healthy、`root=200`/`glossary2=200`/`svg=200`；已 `systemctl stop+disable glossary` 释放 80 端口，全站归口 8081 单一入口。hexUtils 数学真相镜像一致性校验构建期通过（全部镜像一致）。
- **TD-01 复核（新会话重下任务3）**：核验确认 TD-01 仍完好——本地 `frontend/public/glossary2/` 两文件在、TheSidebar:163 外链为 `/glossary2/`、服务器 8081 /glossary2/ 与 svg 均 200、`mecha-frontend` healthy、80 端口 `glossary` inactive 且 `:80` 无监听。无需重做；相关本地改动仍未提交（git 未跟踪 glossary2 资源 + TheSidebar.vue modified）。
- **TD-03/TD-11/TD-12 执行（2026-09-06）**：用户「并入并执行」——将 TD-03 并入并实为修了 `NewBattleView.vue` 两处真 bug（`user.value?.userId`/`userStore.user?.userId`→`userStore.user?.id || user.value?.id`，与同文件 738/750 正确写法对齐；原写法使 myId=undefined，导致我方单位识别/房主判定/突袭QTE 全部失效）。后端 `req.user.userId` 与 comm-service 的 `p.userId` 是合法契约字段，**未改动**。前端 grep 复核 0 命中、lint 0 错误。TD-12：`Desktop/机甲战棋/backup0729` 移入工作区 `历史备份/backup0729`（可逆）。TD-11：非破坏性 `cp -n` 将桌面 `机甲战棋/历史报告` 未重复文件并入 `历史开发报告/历史报告/`，归档数 154→231（+77）；桌面原件保留、未覆盖同名。
- **TD-03 部署上线（2026-09-06）**：用户「全部执行」→ ①`git commit` 仅 `mecha-universe-engine/frontend/`（aef2c54，80 文件，不动无关 backend-gateway/shared-ui 等改动）；②删桌面 `机甲战棋/历史报告` 副本（归档已兜底）；③rsync→服务器 `npm run build`（6.03s）`compose build --no-cache mecha-frontend`→`up -d --no-deps` 重建成功；验证 `mecha-frontend` healthy、`root=200`/`glossary2=200`/`svg=200`。告警均已知非阻断（JWT_SECRET 默认空、version 废弃 TD-07、mecha-battle-db orphan TD-08）。
- **技术债复查（2026-09-06）**：对照 `项目文件盘点与技术债报告-2026-09-06.md`，已清 TD-01/02/03/11/12；**仍待处理**：P1=TD-05(Lighthouse令牌)/06(dist-dev根因)/07(compose version:实测 docker-compose.yml:11 仍在)/08(orphan battle-db)/09(frontend chunk 实测 index 602kB>500)/10(rsync非git)；P2=13文件TODO/FIXME（hexUtils.js 实测2处@deprecated良性）；P3=TD-13(副本md)/14(待完善内容·过期报告)/15(文档版本混乱)；P4=TD-16(前端 .tactical-* 死代码 实测 components.css 32+legacy-compat.css 10=42处)/17(镜头3未实现)/18(文档散两处)。**本次新债**：N1=工作树大量未提交漂移（backend-gateway~20、combat-service多、shared-kernel、shared-ui、deploy.yml、18个memory文件删除、根目录大量未跟踪md），其中 backend/combat 改动已 rsync 上服务器但容器未重建→源码新运行旧；N2=前后端部署不一致（仅 frontend 提交+部署）。
- **TD-13/14/15 文档清理（2026-09-06）**：用户「先清理文档吧」→ P3 文档债三项统一移入 `历史备份/文档清理-2026-09-06/`（可逆归档，非硬删）：TD-13 `mecha-universe-engine/词条编辑器-数值字段与系统联动核查报告-2026-08-04_副本.md`（与同目录原件 40.61KB 同体积，确为副本）；TD-14 `Desktop/机甲战棋/项目资产/待确认技术资产清单.md`+`待完善内容/`+`过期报告/`；TD-15 `Desktop/机甲战棋/机甲战棋文档版本符合性排查报告.md`。源均已移走，归档含 5 项。至此 **P3 文档债（TD-11~15）全清**；剩余 TD-05/06/07/08/09/10(P1)、13文件TODO(P2)、TD-16/17/18(P4) + N1 工作树漂移。
- **TD-05 复核（2026-09-06）**：用户称 Lighthouse 集成 UI 显示「已连接」，但实测 `analyze_lighthouse_instances` 与 `describe_running_instances(ap-shanghai)` 均返回 `Token verification failed. Please check your Token is correct.` → **TD-05 仍真实有效，非过时债**。结论：集成面板「已连接」仅表示集成已配置/握手，不代表 API Token 当前在服务端有效（绿点不因 Token 失效翻红）。部署继续走 SSH/SCP+systemd+execute_command 通道（不受影响）。要真正修复需用户在 CodeBuddy 集成面板对 Tencent Lighthouse 重新授权刷新 Token。
- **TD-05 已解决（2026-09-06 续）**：用户重新授权 Tencent Lighthouse 集成后，实测 `analyze_lighthouse_instances`（ap-shanghai=1）与 `describe_running_instances`（lhins-2fs1rzs8/Watson/RUNNING/106.54.197.69）均成功 → **TD-05 已闭环**，集成 API 通道恢复。部署仍走 SSH/SCP+systemd（`deploy_project_preparation` 上传超时未变）。
- **TD-07 已解决（2026-09-06）**：删除 `mecha-universe-engine/docker-compose.yml` 顶部的 `version: '3.8'`（已废弃字段，部署告警源）。compose 仍为合法 YAML。
- **TD-16 复核为误报（2026-09-06）**：报告称「前端绿色 `.tactical-*` 死代码」不实。实测 `.tactical-*` 是**活跃 UI 组件库样式**：`ui/Button.vue`(`tactical-btn*`)、`ui/Card.vue`(`tactical-card*`)、`ui/Tag.vue`(`tactical-label*`)、`NewHomeView.vue`(`tactical-header`)、`NewBattleView.vue`(`.ap-tactical`)。删除会破坏首页头部与全部按钮/卡片/标签样式。**已关闭 TD-16，未删任何代码**。同步纠正 MEMORY「`.tactical-*` 死代码」错误记为「非死代码」。

- **剩余技术债实证复核（2026-09-06）**：对 TD-06/08/09/10/17/18/P2/N1 逐项实证，新发现 **TD-16 同类误报三处**：TD-06（全仓 grep `dist-dev` 0 命中、构建脚本 `check-mirror.mjs && vite build`、无 dist-dev 目录）、TD-10（`git rev-parse`=true 项目实为 git、287 文件已跟踪、非「非git」）、TD-08（`--no-deps` 是 `docs/思想纪要-开发设计哲学.md` L88 指定正确做法、battle-db 在 compose 已定义、非 orphan）。真实项：TD-09（index chunk 实测 623KB>500KB、vite 无 manualChunks）、TD-17（生产 Vue 组件未实现镜头3、仅 prototype `buildLens3()` 有原型）、TD-18（docs 散 workspace 根与 mecha-universe-engine 两处且内容不同）、P2（TODO/FIXME 实 12 文件、vendor dicescript 须排除、hexUtils.js `@deprecated` 为故意委托须保留）、N1（287 未提交文件·高危）。判定已写入债务报告各 TD 行并新增 P5/N1 节；MEMORY 中 dist-dev 误报已纠正。
