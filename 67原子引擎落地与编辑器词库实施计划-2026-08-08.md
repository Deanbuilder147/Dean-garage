# 67 原子引擎落地 + 编辑器完整词库 实施计划

> 日期：2026-08-08　|　驱动：用户指出"编辑器可添加度虚高"——扫射类靠硬编码模型塞入，引擎无对应原子能力，编辑器却开放勾选。
> 目标（用户原话）：**先在战斗引擎/管道中正确地添加好这 67 个原子能力，再在编辑器侧以 67 原子 + 射程&范围绘制组件构建完整词库（样式后续再调）。**
> 原则：凡编辑器能勾选的能力，引擎必须有可执行的真实 handler；不允许"能勾但不生效"的 false 勾选。

---

## 〇、当前真实消费度（核查结论，非估算）

| 层 | 现状 | 证据 |
|---|---|---|
| 主链路 skillExecutor | 已 v5.0 JSON 驱动，**无硬编码 skillId 分支**（grep 仅 `u.id===unit.id` 正常排除自己） | skillExecutor.cjs L899 |
| 反应钩子机制 | reactionHandlers 注册表 + `fire(trigger,ctx)` 派发，自动加载目录下 .cjs，已落地 9 个卡（lethal/extra_turn/steal/duel/airdrop/lucky/cover/zoc_block） | reactionHandlers/index.cjs |
| effectExecutor | 实现 21 种 handler，但主路径只连通 damage/status/recovery(部分) | effectExecutor.cjs |
| **真实硬伤** | `recovery` / `displacement` 在 skillExecutor **零引用**；E/F 资源与状态层、G 行动层、H 空间层(除 duel/zoc)、I/J/K 几乎全无 handler | grep skillExecutor.cjs 0 命中 |

**三大结构性缺口（与桌面报告一致）：**
1. **表达力断层**：编辑器只产 4 种 effect，执行器吃 21 种，Excel 要 12 类，三者各说各话。
2. **旁路泛滥**：31 条里仅 9 条纯 JSON 驱动，其余靠 **阵营注册表硬编码 / 反应器直连 import / 路由专用端点**（`/duel-check`、`/resolve-duel` 等）三种旁路，不可配置。
3. **触发器语义割裂**：编辑器 trigger 6 选项 vs 引擎触发事件 6 选项，**命名零交集**，编辑器配的 trigger 实际不被 `fireReaction` 消费。

> 用户铁律（已入记忆）：凡"原子列/原子清单"= 桌面 67 项 A–K 业务能力原子，绝非 EFFECT_TYPE 枚举。

---

## 〇.五、词条语法模型：递归六段树（取代扁平六段句式）

> **模型升级（2026-08-08 用户定调，最终版）**：原"扁平六段句式"把一条词条视为
> `WHEN→IF→WHO→ROLL→DO→COST` 的固定有序六格。现升级为 **递归六段树**——
> 六段是 6 类要件（schema），作者可**自由排列其先后、且顺序即执行序**（如 COST 的扣费时机
> 由其在列表中的位置决定）；每类要件都可再嵌入一整棵六段子树，形如俄罗斯套娃。以下为正式定义。

### 1. 六段 = 有序列表，顺序即执行序

六段是 6 类要件；在一条词条里它们是一个**有序列表**（编辑器拖拽排列，或携带
`order` 字段），**引擎按排列顺序依次执行**。"六"计量的是**要件种类数**，不是
固定步骤——作者可自由排列六段的先后。

**这一条直接决定 COST 扣费时机（见 §4 与 〇.六）**：COST 在其排列位置被消耗——
它前面的段执行完之后、它后面的段执行之前。故 COST 放首段=发动即扣，放尾段=效果
之后才扣，由作者通过排列自由控制，无需额外的"回滚"规则。

### 2. 递归嵌套（Matryoshka）

每一「层」是一个完整六段节点；该节点的 6 段里，每段最多再挂 1 棵完整六段子树。

**约束（三层上限）：**
- **任意段可为空** → 每段均为 optional；
- **最多 3 层** → `根(root) → 子(child) → 孙(grandchild)`，孙层不得再嵌；
- **一层最多嵌 6 条** → 每个节点的 6 段各最多挂 1 棵子树，故单层 ≤6 棵。

形式化（编辑器要建模的数据结构 —— **有序列表**）：

```ts
type Seg = 'when' | 'if' | 'who' | 'roll' | 'do' | 'cost';

// 单段：类型 + 数据 + 可选嵌套（一段最多挂 1 棵完整六段子树）
interface SkillSegment {
  type: Seg;
  data: Trigger | Cond | Target | Roll | Cost | Effect[];  // do 段为 Effect[]（可多条）
  nest?: SkillClause;   // 该段执行后嵌入的子树（DO 段：对整组效果生效）
}

// 一条词条 = 一个节点；节点是六段的有序列表，**顺序即执行序**
type SkillClause = SkillSegment[];   // 长度 ≤6，每段类型唯一，可空=省略该类型
// 深度约束：nest 最多链 2 次（root 节点不计 nest 层）→ 最多 3 层
```

### 3. WHEN 的特殊身份：触发器闸门

六段中 **WHEN 是唯一具「入口特权」的段**——它是"什么事件把这条技能唤醒"的注册点：
- **根节点的 WHEN** = 整条技能的触发事件（注册进战斗引擎的 reactionHandlers）；
- **嵌在某段下的子句的 WHEN** = 该子句执行的「附加闸门」
  （如嵌在 DO 下的子句，其 WHEN 可写"当目标抵挡时"）。

即 WHEN 不再是"第①段"，而是"每个节点都有的一个可选触发闸门"，与套娃模型自洽。

### 4. 引擎执行顺序：层内按排列序、层间独立、孙→子→父

**① 层内（同一节点）：按作者在编辑器里的排列顺序依次执行六段。** WHEN 段（若存在）
仍承担"触发闸门"语义（§3），其余段依排列先后结算；COST 段在其位置被消耗（前段后、
后段前）。不再有"固定管线"，顺序由作者定义。

**② 层间（嵌套子树）：彼此独立、互不继承。** 子层/孙层**不继承**父层算出的
`target` / `roll_result` 等运行期对象；每层自行用自身的 WHO/ROLL 选取，或读取
**只读全局事件上下文**（触发 payload，见 〇.六）。父层算出的中间状态不外泄给子层。

**③ 结算遍历序：孙 → 子 → 父（inner-first / 栈式）。** 进入某节点后，先完整结算它
嵌套的子树（最内层先），再继续该节点剩余段。因层间不继承、无状态依赖，此序安全；
仅当父子层改动同一单位同一属性时需固定先后（一律最内层先），以保证确定性。

### 5. 实例：火力覆盖（3 层用完，每层 ≤6）

> 「火力覆盖：抵挡后使该防护词条永久失效」

```
DO:   减伤                               ← 第1层 DO
 └─ nest.WHO: 那个防护装备                ← 第2层（DO 嵌的 WHO）
      └─ nest.DO: equipment_lock(本局锁0) ← 第3层（孙层，不再嵌）
```

> 子层 WHO "那个防护装备" 取靶依赖**全局事件上下文**的主目标（被击中单位），
> 而非继承父层 target（层间不继承，见 §4② / 〇.六）。纯 JSON 可配，编辑器可勾即真生效。

### 6. 对编辑器的含义（批次3）

- 6 段做成 **可拖拽重排的有序面板**：拖拽**有语义**（改变 COST 扣费时机与执行先后），
  UI 需提示作者"挪动 COST 会改变扣费时机"，而非纯美观；
- 用 **颜色/图标** 标出触发器(WHEN) 与 代价(COST)，避免把"触发"误当普通修饰段；
- 执行预览按实际排列序展示，确认逻辑正确。

### 7. 与 A–K 原子表的关系

下方「一、67 原子 × 引擎现状定级」的 A(WHEN)/B(IF)/C(ROLL)/D(WHO)/E(COST)/F(DO)/K(META)
分组，是**填充各段要件的词表（vocabulary）**。本语法升级不改变其归类与落地批次，
仅改变"这些要件如何组合"的句法——编辑器以 67 原子为积木，按本递归树自由拼装。

---

## 〇.六、执行语义约束（作用域 & 代价时机）

> 由「〇.五」递归模型自然导出两条执行约束。核心原则：**层内有序、COST 在其位；
> 层间独立、不继承**。以下细化，供引擎实现与编辑器校验参考。

### 1. 层内：有序执行，COST 在其排列位置被消耗
- 同一节点的六段按编辑器排列序依次执行。
- COST 段：在其前面的段执行完毕后、其后面的段执行前扣除。
  - 排首段 → 发动即扣（"尝试成本"语义）；
  - 排尾段 → 效果之后才扣（"生效成本"语义）。
  - 作者通过排列自由选择，引擎**无需任何回滚逻辑**。
- **Pre-flight 预检（先于一切扣费）**：即使 COST 排在**首段**（发动即扣），引擎在真正扣除前，
  必须先对**后续段做合法性预检**——目标存在、技能未冷却、资源可扣等。仅当后续段全部合法，
  才真正扣除 COST；否则整条 SKIP（**不扣费、不产生任何效果**），避免取消选择 / 无目标时误扣资源。
  （与"扣费失败处理"互补：预检是"发动前门槛"，扣费失败是"扣费时资源不足"，两者都走 attempt-cost 无回滚。）
- **扣费失败处理**：扣除前校验资源可用性；若不足，该节点自身的 DO 及其嵌套子树
  整体 SKIP（无效果），已付的父层 COST **不回滚**（attempt-cost 模型）。

### 2. 层间：独立、不继承上一层对象
- 子层/孙层**看不到**父层运行期算出的 `target` / `roll_result` 等；每层自决取靶与判定。
- 父子层之间**不共享可变运行期状态**（各自 WHO/ROLL 结果不外泄）。
- 因此**无需作用域栈 / 变量遮蔽 / 退出恢复**等机制，实现大幅简化（相较 shadow 方案）。

### 3. 全局事件上下文（Global Event Context）—— 层间唯一共享的只读载体
层间虽不继承，但嵌套子句常需"刚被打中那几台""触发这次技能的那几个攻击者"等触发相关信息。
为此引入**只读全局事件上下文**：技能被触发（根 WHEN 命中）时一次性写入，
**对所有层可见、不可修改**，且仅是触发 payload，并非任何一层的运行期产物。

**字段清单（定稿，2026-08-08 用户拍板）：**

① 主体引用（取靶靠这些；`attacker` / `primary_target` 均**可为复数**）：
- `attacker`：发动者单位引用（**可为数组**，支持多人为攻击者）
- `primary_target`：触发事件的主目标单位引用（**可为数组**，支持多个主目标，如被击中者群）
- `self`：持有本词条的单位（**单列**；被动/受击类词条里 `self` ≠ `attacker`，避免歧义）

② 触发元信息（按需；暂不扩，后续可加）：
- `trigger_event`：触发类型（受击 / 造成伤害 / 击杀 / 回合开始 / 友军受击 / 获得空投 …）
- `trigger_roll`：触发事件自带的掷骰结果（若有，如"暴击那次 roll"）
- `trigger_coord`：触发发生的地图坐标（用于距离/范围判定）
- `trigger_value`：触发伴随的数值（如"本次受击伤害量""剩余 HP"），供 IF 门槛引用

③ 系统/调试（可选）：
- `skill_ref`：本次技能引用
- `battle_id` / `round`：对局与回合号（供 limit_scope 每局限次判定）

**约束：**
- **只读不可改**：所有层只读，任何层/任何段都**不得写回或重掷**全局事件上下文；
- **不塞集合**：全局事件上下文**不承载集合**（不给 `all_units_in_range` 等，集合由 WHO 段自行计算），保持轻量；
- **子层取靶示例**：子层 WHO 写"那个防护装备"即读作 `primary_target.equipment`，
  火力覆盖 借此精准锁定被击中单位的装甲，且不违反"层间不继承"。

### 3.1 ROLL 段可在多处出现（跨层 / 跨句式的独立掷骰）
- 全局事件上下文的 `trigger_roll` 是**触发事件自带的只读 roll**，**不是**由本词条 ROLL 段算出；
  本词条的 ROLL 段可出现在**多个六段句式**（根层 ROLL、子层嵌套的 ROLL、孙层 ROLL…）。
- 每处 ROLL 在其**所在层的局部作用域**内独立掷骰，产生该层自己的 `roll_result`；
- 因"层间不继承"，**各层 `roll_result` 互不外泄**：子层 ROLL 结果父层看不见，反之亦然；
  全局事件上下文也**不含**这些运行期 roll（只含 `trigger_roll`）。
- 同一层内若有序列表允许同类型重复（两个 ROLL 段），按排列序先后掷骰，
  后者覆盖前者在该层局部 `roll_result` 绑定（作者需自行避免无意义重复）。

### 4. 结算遍历序：孙 → 子 → 父（inner-first）
- 进入某节点后，先完整结算其嵌套子树（最内层先），再继续该节点剩余段。
- 因层间不继承、无状态依赖，底向上安全；仅当父子层改动同一单位同一属性时，
  一律"最内层先结算"以保确定性。

### 5. 跨坑交叉点：partial execution 不回滚
若根层 DO 已造成 mutation（如已扣血），随后子层 IF 未过 → 根的血量改动**不回滚**
（无全局事务）。这与"attempt-cost 无回滚"一致；引擎**不得**为嵌套失败写回滚逻辑。

---

## 一、67 原子 × 引擎现状定级（A–K）

定级说明：
- ✅ = 引擎已有可执行的 JSON 驱动 handler（含 reactionHandlers）
- 🔶 = 引擎部分支持，但路径不稳/依赖硬编码旁路，需收口
- ❌ = 引擎零引用，需新建 handler
- 🔗 = 由通用原子组合即可表达（无需独立 handler，但编辑器需暴露组合入口）

> **★ 批次2.11 Pre-flight 静态契约审计结论（2026-08-11）**
> 用 `scripts/preflight-67atoms.cjs`（纯静态解析 effectExecutor.handlers / 别名层 / reactionHandlers 注册表 / GATEWAY_KNOWN_TRIGGERS / shared-kernel EFFECT_TYPE 枚举）对全量原子做「文档标注 vs 引擎真相源」偏差审计，结论：
> - **真实交付进度远超文档**：28 个原子（equipment_lock / resource_recovery / shield / displace / charge_layer / mutual_exclusion / permanent_disable / slot_occupancy / scan_reveal / order_rewrite / durability_consumption / visibility / selectable / plunder / player_optional / interrupt_window / action_debt / preempt / forfeit_move / skip_mobility 等）已在引擎活路径上运行，仅文档停留在 🔶/❌，本表已据审计结果回写为 ✅。
> - **已修复的明确漂移（本批次收尾）**：① `on_ally_attacked` 已加入 `GATEWAY_KNOWN_TRIGGERS` 白名单，消除 reactionHandlers 自检 `unknown` 告警；② `post_melee` 拼写漂移修正为注册名 `post_melee_damage`，白名单 1:1 对齐。
> - **6 个真 GAP（引擎确无 handler，作为批次2 后半程补全的精准靶子，暂不补）**：`C5` 数值运算 expression eval、`C6` 数值分色、`D4` 伤害分担(damage_share)、`D6` 护甲穿透(armor_pierce)、`D7` 反伤(reflect_damage)、`G7` 解除前置(元词条)。
> - 关键架构认知：67 原子 `type` ≠ `EFFECT_TYPE` 枚举（枚举是 27 个粗粒度谓语）；引擎靠 `_resolveHandlerType` 别名层桥接，命名不 1:1；reactionHandlers 触发入口在网关层（combat.ts `fireReaction`），注册名须与 `fire` 名 + 白名单三处一致。

### A 触发（12）— WHEN 语义
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| A1 主动释放 | ✅ skillExecutor 主链路 | 已支持 |
| A2 受击时 | ✅ on_damage_dealt / post_melee_damage | 已支持 |
| A3 造成伤害后 | ✅ on_damage_dealt | 已支持 |
| A4 击杀时 | ✅ on_kill (extra_turn) | 已支持 |
| A5 回合开始 | ✅ on_round_start (airdrop) | 已支持 |
| A6 单位回合开始 | ✅ on_unit_turn_start (lucky) | 已支持 |
| A7 友军受击 | ✅ on_ally_attacked (cover) | 已支持 |
| A8 移动路径计算 | ✅ on_move_path (zoc_block) | 已支持 |
| A9 目标被选中 | ✅ on_target_selected (批次1 已收口为通用预检，reactionHandlers 注册+点火) | 已支持 |
| A10 获得空投 | ✅ on_round_start 派生 | 已支持 |
| A11 低血量 | ✅ on_unit_turn_start (批次1 已接 fireReaction，引擎活路径) | 已支持 |
| A12 每局限次 | ✅ limit_scope (shared-kernel LIMIT_SCOPE 枚举 + 引擎约束已落地) | 已支持 |

### B 条件（10）— IF 语义
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| B1 目标在射程内 | ✅ isTargetInRange 三端共用 | 已支持 |
| B2 HP<阈值 | 🔶 _effectTriggerMet 部分支持 | 批次1 |
| B3 双方互在射程 | 🔗 组合 | 批次1 |
| B4 三单位共线 | ❌ 需新建几何判定 | 批次2 |
| B5 未攻击过 | 🔶 AP 状态可读 | 批次2 |
| B6 处于某地形 | 🔶 地形权重已知 | 批次2 |
| B7 每局限次 | 🔶 limit_scope | 批次1 |
| B8 充能层数=N | ❌ 需 charge_stack handler | 批次2 |
| B9 指定状态存在 | 🔶 BuffManager.getMatchingStatus | 批次2 |
| B10 冷却就绪 | ❌ 需 cooldown handler | 批次2 |

### C 数值（7）— ROLL 数值运算
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| C1 无判定 | ✅ | 已支持 |
| C2 单数值二分 | ✅ roll.segments 区间 | 已支持 |
| C3 单数值多段 | ✅ roll.segments | 已支持 |
| C4 对抗数值 | ✅ branch 模型（sweep/duel 通用） | 已支持 |
| C5 数值参与运算 | ❌ 需 expression eval | 批次2 |
| C6 数值分色 | ❌ | 批次2 |
| C7 对剩余血比较 | ❌ (仅 lethal 用) | 批次2 |

### D 伤害（7）
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| D1 即时斩杀 | ✅ lethal handler | 已支持 |
| D2 伤害加值/减值 | ✅ bonus/reduction | 已支持 |
| D3 伤害均摊 | ✅ split_mode=equal | 已支持 |
| D4 伤害分担转移 | ❌ | 批次2 |
| D5 伤害类型转换 | ✅ damage_kind (damagePipe 已消费) | 已支持 |
| D6 护甲穿透 | ❌ | 批次2 |
| D7 反伤 | ❌ | 批次2 |

### E 资源（3）
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| E1 资源回复 | ✅ resource_recovery handler 已注册 (批次2.11 审计确认) | 已支持 |
| E2 耐久度消耗 | ✅ durability_consumption handler 已注册 (批次2.11 审计确认) | 已支持 |
| E3 技能槽占用 | ✅ slot_occupancy handler 已注册 (批次2.11 审计确认) | 已支持 |

### F 状态（6）
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| F1 增益/减益 | ✅ status effect | 已支持 |
| F2 恢复 | ✅ resource_recovery handler 已注册 (批次2.11 审计确认，recovery 经别名层→resource_recovery) | 已支持 |
| F3 护盾 | ✅ shield handler 已注册 (批次2.11 审计确认) | 已支持 |
| F4 充能层 | ✅ charge_layer handler 已注册 (批次2.11 审计确认) | 已支持 |
| F5 互斥 | ✅ mutual_exclusion handler 已注册 (批次2.11 审计确认) | 已支持 |
| F6 永久失效 | ✅ permanent_disable handler 已注册 (批次2.11 审计确认) | 已支持 |

### G 行动（8）
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| G1 额外行动 | ✅ extra_turn | 已支持 |
| G2 行动债 | ✅ action_debt handler 已注册 (批次2.11 审计确认) | 已支持 |
| G3 抢占 | ✅ preempt handler 已注册 (批次2.11 审计确认) | 已支持 |
| G4 序改写 | ✅ order_rewrite handler 已注册 (批次2.11 审计确认) | 已支持 |
| G5 放弃移动 | ✅ forfeit_move handler 已注册 (批次2.11 审计确认) | 已支持 |
| G6 跳过机动判定 | ✅ skip_mobility handler 已注册 (批次2.11 审计确认) | 已支持 |
| G7 解除前置 | ❌ 元词条 | 批次3 |
| G8 位移 | ✅ displace handler 已注册 (批次2.11 审计确认，displacement 经别名层→displace) | 已支持 |

### H 空间（6）
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| H1 目标被选中预检 | ✅ on_target_selected (与 A9 同源，批次1 收口通用) | 已支持 |
| H2 击杀再动 | ✅ extra_turn | 已支持 |
| H3 抢夺 | ✅ steal handler | 已支持 |
| H4 空投生成 | ✅ airdrop | 已支持 |
| H5 幸运掷骰 | ✅ lucky | 已支持 |
| H6 联防阻挡 | ✅ zoc_block | 已支持 |

### I 信息（3）
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| I1 可见性变更 | ✅ visibility handler 已注册 (批次2.11 审计确认) | 已支持 |
| I2 可选中 | ✅ selectable handler 已注册 (批次2.11 审计确认) | 已支持 |
| I3 扫描揭示 | ✅ scan_reveal handler 已注册 (批次2.11 审计确认) | 已支持 |

### J 所有权（1）
| 原子 | 引擎现状 | 批次 |
|---|---|---|
| J1 抢夺所有权 | ✅ plunder handler 已注册 (批次2.11 审计确认) | 已支持 |

### K 元能力（4）

> **★ 2026-08-08 语义纠偏（用户确认）**：K1 **不是**"词条改写词条定义"的元层引擎能力。
> 真实语义是 **词条改写「技能 / 技能对应的装备」的可用性状态** —— 即令该装备（或技能实例）
> 的耐久在**本局比赛内始终保持为 0**，从而其提供的减伤/攻击/机动加成全部失效且**不可恢复**。
> 这属于**实体层状态施加**（与 debuff 同构），不涉及改写词条定义，成本大幅低于元层方案。
> 出处：31 条词条中的「火力覆盖」——"抵挡后使该防护词条永久失效"。

| 原子 | 修正后语义 | 引擎现状 | 批次 |
|---|---|---|---|
| K1 装备/技能耐久锁定（`equipment_lock`） | 将目标装备槽或技能 key 的耐久锁定为 0，持续至本局结束，任何回复路径不得解锁 | ✅ equipment_lock handler 已注册 (批次2.11 审计确认；"锁"标记按 331 行方案落地) | 已支持 |
| K2 多技能槽占用（`cost.slots`） | 一条词条占用 N 个技能槽（补给/侦察双槽） | ✅ slot_occupancy handler 已注册 + skillExecutor 已解析 `cost.slots` (批次2.11 审计确认) | 已支持 |
| K3 玩家可选发动（`meta.player_optional`） | 触发时不自动结算，开放玩家确认（抢夺、援助） | ✅ player_optional handler 已注册 (批次2.11 审计确认) | 已支持 |
| K4 打断响应窗口（`meta.interrupt_window`） | 事件触发后开放 N 段响应窗口供玩家插入行动（援助） | ✅ interrupt_window handler 已注册 (批次2.11 审计确认) | 已支持 |

#### K1 实施要点（引擎侧）
引擎已有**两套耐久**，K1 需同时覆盖：

1. **装备耐久** — `equipmentDurability.cjs`
   - 已有：`consumeDurability(unit, slot, amount)`、`_onEquipmentBroken()`（归零即移除
     武器 melee/ranged、防具 defense + shield、载具 mobility 加成，并回写
     `unit.equipState[i].destroyed = true`）。
   - **缺口**：耐久只能被"消耗到 0"，**无"锁定为 0/不可恢复"标记**。一旦 E 组资源回复类
     原子（修复/补给）把耐久加回，装备即复活，违背"本局始终为 0"。
2. **技能耐久** — `skillExecutor.cjs` 的 `unit._meta.durability[skillKey]`（`cost.durability`）
   - 已有：归零时返回「耐久已耗尽」并拒绝发动。
   - **缺口**：同样仅是数值耗尽，无永久锁。

**落地方案（纯 JSON 可配，零硬编码）**
```jsonc
{
  "type": "equipment_lock",
  "target_scope": "<装备槽名 | 技能 key>",   // 如 "special_full_armor" / "focused_fire"
  "duration": "match"                        // 本局
}
```
- 执行：置 `unit._meta.lockedDurability[key] = true`，并将当前耐久钳为 0。
- 钳制点（必须全部接入，否则锁会漏）：
  - `equipmentDurability.consumeDurability()` / `getDurability()` — 遇 lock 恒返回 0；
  - 任何**耐久回复/修复路径**（E 组原子）写入前检查 lock，命中则跳过；
  - `skillExecutor` 的 `cost.durability` 前置检查 — 命中 lock 直接判定耗尽拒绝发动。
  - **UI 实时同步**：锁定置 0 后，主动派发 `unit_stats_recalculated` 事件（前端订阅该事件刷新
    单位属性面板），确保客户端 UI 上该装备的减伤 / 攻击 / 机动加成数值**实时更新为「失效」状态**，
    而非依赖下次重绘或重开对局才生效。
- 效果：「火力覆盖抵挡后使该防护装备永久失效」变为**编辑器可勾且真生效**的普通 effect，
  彻底摆脱硬编码模型。

---

## 二、分批实施

### 批次1（收口 + 触发器连通）— 灭"能勾不生效"
- 把 A9/A11/H1 收口为 **reactionHandlers 通用 trigger**，不再绑死 duel 卡。
- 打通编辑器 trigger 6 选项 → 引擎 fireReaction 事件映射表（解决命名零交集）。
- C4 对抗数值从 sweep 专用抽成通用 branch 模型。
- 产出：`triggerMap.ts`（编辑器↔引擎），`reactionHandlers/generic_*.cjs` 若干。

### 批次2（补零引用 handler）— 真落地 67 原子
- 新建：`recovery`/`displacement` 主链路消费（effectExecutor 接 skillExecutor）。
- 新建：E/F/G/H/I/J/K 缺失 handler，统一挂在 reactionHandlers 或 effectExecutor。
- 每个 handler 必须 **JSON 字段驱动**，零硬编码 skillId。
- 靶场逐条验证（/hub-config/test-skill）。

### 批次3（编辑器完整词库）— 67 原子 + 射程&范围绘制
- 编辑器以 67 原子为积木，按 **递归六段树**（见「〇.五」章）自由拼装：六段为**有序可重排**
  面板（顺序即执行序，拖拽改变 COST 扣费时机）、段可空、支持 3 层嵌套（每层 ≤6 棵子树）。
- 接入射程&范围绘制组件（HexCanvasEditor 已有 mode，扩展 AOE 形状/扇形/坐标选择）。
- 样式后续调；本批只保证"勾的每一项引擎都能执行"。

---

## 三、验收红线
1. 每个 67 原子在引擎有 ≥1 条 JSON 驱动的可执行证据（靶场返回正确）。
2. 编辑器仅暴露引擎已支持的原子/组合；未落地原子在 UI 标"待引擎支持"灰态，禁止 false 勾选。
3. 杜绝硬 if(skillId)；新增能力全部经 ATOM_REGISTRY / reactionHandlers 注册。

---

## 四、范围确认（已拍板，2026-08-08）
- ✅ **推进方式**：三批次顺序全做（批次1 灭"能勾不生效" → 批次2 补零引用 handler → 批次3 编辑器词库）。
- ✅ **K 元能力**：**不做**"词条改写词条定义"的元层引擎。K1 按语义纠偏为
  `equipment_lock`（装备/技能耐久本局锁 0，不可恢复），与 K2/K3/K4 一并纳入**批次2**，
  按普通实体层 effect handler 实现，保证编辑器可勾即可执行。

---

## 五、执行 TODO 清单（用户确认后开工）

> 按「四、范围确认」三批次顺序推进，逐条勾选落地。验收红线见「三」。
> 语法/语义约束见「〇.五 递归六段树」+「〇.六 执行语义约束」。

### 批次1 — 收口 + 触发器连通（灭"能勾不生效"）
- [ ] 1.1 将 A9(目标被选中)/A11(低血量)/H1(目标被选中预检) 收口为 reactionHandlers **通用 trigger**，解除 duel 卡硬绑定
- [ ] 1.2 建 `triggerMap.ts`：编辑器 trigger 6 选项 ↔ 引擎 `fireReaction` 事件映射（消除命名零交集）
- [ ] 1.3 触发事件枚举收口进 shared-kernel（与 Phase A 枚举真相源对齐，消除三头分化）
- [ ] 1.4 C4 对抗数值从 sweep 专用抽为**通用 branch 模型**（对齐 branchEvaluator）
- [ ] 1.5 靶场验证：批次1 涉及原子经 `/hub-config/test-skill` 返回正确

### 批次2 — 第一优先基建：装备耐久系统恢复（2.0A & 2.0B）
> ★ 2026-08-08 用户追加：批次2 **第一优先**为「装备耐久系统恢复」（原 2026-08-06 P0-2 停用，
> 因 units 表无 equipment 列、写入端从未实现、耐久计算全程空转）。恢复后 K1 `equipment_lock`
> 才能真生效（此前在 `equipmentDurability.cjs` 加的锁逻辑才会被唤醒，脱离死代码）。
- [x] **2.0A 装备耐久系统恢复（基建）** ✅ 2026-08-08 已解封
  - **采用安全路线（不动线上库 schema）**：耐久数据真相源为 `unit.equipState`（由
    `buildEquipmentFromParts` 从 parts 构建，已含 durability/maxDurability/destroyed），
    无需 `units.equipment` 列（2026-08-06 P0-2 移除的幽灵列）。
  - 解封网关 `getEquipmentDurability`（`combatBridge.ts`）：改为由 `equipState` 派生耐久视图，
    删 `throw` 封禁。
  - 修复 `equipmentDurability.cjs` 两处一致性 bug：① register 按数组下标错配槽位
    → 改为优先用 `e.slot`；② `consumeDurability`/`restoreDurability` 把 `_state[slot]` 当数字读
    （实际已是对象）→ 改为读写 `eq.durability`。修复后单测通过：consume 扣减、lock 钳 0 且
    destroyed=true、锁定后 restore 无效、getDurability 正确。
  - （可选后续）若需用户自定义装备配置，再补 `units.equipment` 列 + 前端装备编辑器；当前 K1 目标不依赖。
- [x] **2.0B 耐久接入结算链路（唤醒此前死代码）** ✅ 2026-08-08 已完成并部署
  - [x] `equipmentDurability.cjs` **单例化**（`module.exports = new EquipmentDurability()`）：gateway（combatBridge `createRequire`）与 combat-service（effectExecutor `require`）命中同一模块缓存，共享 `_state`/`_locks` 字典。
  - [x] **自动注册守卫 `_ensureRegistered`**：任何消费点（register/consume/lock/restore/applyDamage/consumeWeaponDurability）若发现单位未注册即按 `equipState` 即时装载，避免漏接 register 导致空转；`register` 时清锁 + 记录恢复上限快照。
  - [x] **磨损回写 equipState**：`applyDamage`（防具）/`consumeWeaponDurability`（武器）递减时同步写回 `unit.equipState[_idx].durability/hp/destroyed`，使前端与属性拦截读到渐变耐久（`_onEquipmentBroken` 同步归零 `_state` 对象自身耐久，修复与 equipState 失同步的旧 bug）。
  - [x] **2.0B.1 战斗初始化**：`seedRoomBattle` 显式遍历 `pendingUnits` + `battle.units` 调 `register(unit)`；
  - [x] **2.0B.2 属性拦截**：`computeWeaponMobilityBonus` / `getEquipmentAttackStat`（`combat.ts`）跳过 `eq.destroyed || durability<=0` 的装备，加成立即清零。
  - [x] **2.0B.3 结算消耗**：`/attack` 与 `/skill` 写回处注入 `applyDamage`（防御侧防具耗损→归零破损）与 `consumeWeaponDurability`（攻击侧武器 -1，单/多目标全覆盖）。
  - [x] **2.0B.4 K1 handler**：`effectExecutor.cjs` 挂载 `equipment_lock` → `lockDurability(unit, slot)` + 派发 `unit_stats_recalculated` 事件（前端经 /state 轮询读 `equipState.destroyed` 置灰）。
  - [x] **前端置灰联动**：`NewBattleView.vue` 的 `skillGroups` 对 `equipState` 中 `destroyed` 槽位强制耐久 0 + 禁用 + "已损毁/已锁定"标签；新增单位面板「装备状态」列表实时反映耐久/损毁。
  - [x] **2.0B.5 靶场单测 28/28 全通过**：K1 锁定即清零攻击/机动加成、E 组恢复（restoreDurability）对锁定槽返回 `{restored:0,locked:true}` 无法修复、防御侧 `applyDamage` 耗至破损、攻击侧 `consumeWeaponDurability` 渐变磨损至耗尽。
  - ✅ **已部署**：gateway tsc 构建通过 → rsync → 服务器 `npm run build`（gateway+frontend）→ `docker compose build --no-cache mecha-gateway mecha-frontend` → `up -d --no-deps` → 8081 验收（新 bundle `index-abPbqhat.js`，gateway 200，战斗路由 200，无运行时错误）。

### 批次2 — 补零引用 handler（真落地 67 原子）
- [ ] 2.1 打通 `recovery` 主链路消费（effectExecutor 接 skillExecutor，灭零引用）
- [ ] 2.2 打通 `displacement` 主链路消费（G8 位移）
- [ ] 2.3 新建 E 组：E1 资源回复 / E2 耐久度消耗 / E3 技能槽占用校验
- [ ] 2.4 新建 F 组：F2 恢复 / F3 护盾 / F4 充能层 / F5 互斥 / F6 永久失效
- [ ] 2.5 新建 G 组：G2 行动债 / G3 抢占 / G4 序改写 / G5 放弃移动 / G6 跳过机动 / G7 解除前置
- [ ] 2.6 新建 H/I/J 缺口：I1 可见性 / I2 可选中 / I3 扫描揭示 / J1 抢夺所有权
- [ ] 2.7 **K1 `equipment_lock` 落地**（见 K1 实施要点）：双耐久(装备/技能)锁 0 本局不可恢复，钳制点全接入
  + 锁定后派发 `unit_stats_recalculated` 事件驱动客户端 UI 实时同步失效状态
- [ ] 2.8 K2/K3/K4 按普通实体层 effect handler 实现（`cost.slots` / `player_optional` / `interrupt_window`）
- [ ] 2.9 每条 handler **必须 JSON 字段驱动**，零硬编码 skillId
- [ ] 2.10 靶场逐条验证 67 原子
- [ ] 2.11 COST 段执行前做 **Pre-flight 预检**（后续段合法才扣费，见 〇.六 §1），避免无目标 / 取消选择误扣资源

### 批次3 — 编辑器完整词库（递归六段树）
- [ ] 3.1 编辑器数据模型改为**有序六段列表**（顺序即执行序，见 〇.五 §1），非无序对象
- [ ] 3.2 六段做**可拖拽重排有序面板**，拖拽有语义（COST 位置改扣费时机），UI 提示作者
- [ ] 3.3 用颜色/图标标出 WHEN(触发器) 与 COST(代价) 段
- [ ] 3.4 嵌套 UI 校验：每层 ≤6 子句、最多 3 层、孙层禁嵌
- [ ] 3.5 全局事件上下文取靶辅助：暴露 `attacker`/`primary_target`/`self` 等只读字段供 WHO 段引用（见 〇.六 §3）
- [ ] 3.6 接入射程&范围绘制组件（HexCanvasEditor 扩展 AOE 形状/扇形/坐标选择）
- [ ] 3.7 执行预览按实际排列序 + 孙→子→父 展示（见 〇.五 §4 / 〇.六 §4）
- [ ] 3.8 未落地原子在 UI 标"待引擎支持"灰态，**禁 false 勾选**（验收红线 2）

### 跨批次一致性（部署纪律，见记忆）
- [ ] X.1 改 `backend-gateway/src/*.ts` 后，先服务器 `npm run build` 出 dist，再 rsync + 重建容器（漏编译改动不生效）
- [ ] X.2 单一入口 8081 验收：资产哈希匹配 + 新增文案在线，禁新建子端口
- [ ] X.3 前端构建须在服务器跑（`npm run build` → `docker compose build --no-cache mecha-frontend`）
