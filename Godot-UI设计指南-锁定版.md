# Godot 客户端 UI 设计指南（锁定版）

> 用途：供 UI 设计师/你自己设计 Godot 全功能客户端界面时遵循的规范
> 状态：基线已锁定（2026-08-15 用户确认 5 项决策）
> 配套：本文档为设计侧规范；工程侧落地见 `Godot六边形UI样式规范-v01.md`

---

## 0. 一句话设计纲领

**以游戏本身的六边形战场语言贯穿全 UI**——所有面板用切角矩形（四角 45° 斜切），边框/装饰源自六边形几何，配色用"绿色（战斗系）+ 琥珀（编辑系）"双主色，文字可竖排。整体像一个"从六边形世界长出来的操作终端"。

---

## 1. 已锁定的 5 项决策

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 面板基础形状 | **切角矩形**（四角斜切），纯六边形仅用于特殊组件（雷达/地图预览/状态指示） |
| 2 | 连接线装饰 | **保留**电路节点风（角标圆点 + 连接线 + 节点） |
| 3 | 竖排标题 | **保留**左侧竖排文字标签 |
| 4 | 配色 | **绿色 `#2fae6a` 系（战斗）+ 琥珀 `#ffb000` 系（编辑）双主色**（原深青已替换为绿） |
| 5 | 字体 | 先用 JetBrains Mono + Noto Sans CJK SC（等宽科幻），后续可替换 |

---

## 2. 形状系统（Shape System）

### 2.1 切角矩形（Chamfered Rectangle）— 默认面板形状
- 四角切掉约 `12px` 的 45° 斜角（小面板可降到 6~8px，大面板可升到 16px）
- 边框 2px，颜色用主题主色
- 禁止在 UI 中出现未经切角的直角矩形（除特殊功能性控件如滚动条内部）

### 2.2 纯六边形（Hexagon）— 特殊组件专用
- 用于：雷达图、地图预览缩略、当前回合/特殊状态指示器、Logo
- flat-top 或 pointy-top 须与战场六角格保持一致（参考 `hexUtils` 的 HEX 朝向）
- 不得用于需要大量文本排版的常规面板

### 2.3 几何衍生装饰
- **角标圆点**：每个切角矩形在左上/右下（或四角）放一个 3px 主色圆点
- **连接线**：相邻面板之间用 1px 主色暗线 + 末端节点圆点相连，模拟电路
- **分段边框**（可选）：边框呈刻度虚线而非连续线
- **内发光**：面板内边缘 0.12 alpha 主色光晕

---

## 3. 配色系统（Color System）

### 3.1 完整色板（HEX 值，直接可用）

**底色层**
| 名称 | HEX | 用途 |
|---|---|---|
| BACKGROUND | `#070b0d` | 应用根背景（近黑略带青灰） |
| PANEL_BG | `#0a1416` | 面板底 |
| PANEL_BG_ALT | `#06100f` | 交替行 / 嵌套面板 |
| GRID_LINE | `#0c2622` | 背景六边形网格线（低对比） |

**主色 A：绿色**（`GREEN` 系 — 战斗语境，原深青已替换为绿）
| 名称 | HEX | 用途 |
|---|---|---|
| GREEN | `#2fae6a` | 主强调 / 边框 / 主按钮底 |
| GREEN_BRIGHT | `#4fe08c` | hover / 发光 / 角标点 |
| GREEN_DARK | `#15643c` | 分割线 / 暗边框 |
| GREEN_DIM | `#0a3320` | 背景装饰 |
| GREEN_GLOW | `rgba(47,174,106,0.12)` | 内发光 |

**主色 B：琥珀**（`AMBER` 系 — 编辑/数据语境）
| 名称 | HEX | 用途 |
|---|---|---|
| AMBER | `#ffb000` | 主强调 / 边框 / 主按钮底 |
| AMBER_BRIGHT | `#ffcf4d` | hover / 发光 |
| AMBER_DARK | `#b37700` | 分割线 / 暗边框 |
| AMBER_DIM | `#5c3d00` | 背景装饰 |
| AMBER_GLOW | `rgba(255,176,0,0.12)` | 内发光 |

**语义色**（不随主题切换，固定含义）
| 名称 | HEX | 含义 |
|---|---|---|
| SUCCESS | `#2fae6a` | 成功 / 友方 |
| DANGER | `#ffb000` | 危险 / 错误（=AMBER 琥珀，无红色） |
| WARNING | `#ffb000` | 警告（=AMBER） |
| ENEMY | `#d98a3a` | 敌人 / 敌方单位 |

**文字层**
| 名称 | HEX | 用途 |
|---|---|---|
| TEXT_PRIMARY | `#d6e8e2` | 主文字（青白） |
| TEXT_SECONDARY | `#6d8a85` | 次要 / 说明 |
| TEXT_MUTED | `#355049` | 禁用 / 暗淡 |
| TEXT_ON_GREEN | `#041a0e` | 绿底上的深色字 |
| TEXT_ON_AMBER | `#1a1200` | 琥珀底上的深色字 |

**装饰层**
| 名称 | HEX | 用途 |
|---|---|---|
| BORDER | `#2fae6a` | 主边框（默认绿） |
| BORDER_DIM | `#15643c` | 暗边框 / 分割 |
| CORNER_DOT | `#4fe08c` | 角标圆点 |
| LINE_DECOR | `#15643c` | 电路连接线 |
| NODE_DOT | `#4fe08c` | 节点圆点 |

### 3.2 双主色分工（全局一致，必须遵守）

| 语境 | 主色 |
|---|---|
| 战斗（战场 / 技能菜单 / HUD / 房间 / 对战） | **绿色 GREEN 系** |
| 编辑 / 数据 / 配置（单位编辑器 / 词条库 / 地图编辑器 / 骰子工坊 / 尺寸工坊） | **琥珀 AMBER 系** |
| 中性组件（按钮 / 列表 / 弹窗 / 导航） | 默认绿色，编辑页内可切琥珀 |
| 警告 / 错误 / 敌人 | 用语义色，不混入主题色 |

> 设计含义：**用户一眼通过配色就能区分"我在打仗"还是"我在编辑"**。

---

## 4. 字体系统（Typography）

- **主字体**：JetBrains Mono（等宽，科幻终端感）
- **中文 fallback**：Noto Sans CJK SC（半等宽观感）
- **标题字重**：Bold / SemiBold
- **正文**：Regular
- 字号建议：标题 18~22px，正文 13~15px，次要 11~12px，标签 10~11px
- 字符间距（letter-spacing）：标签/竖排标题可加 1~2px 增强科技感

---

## 5. 组件清单（Component Inventory）

每个组件的设计要点（视觉，不含实现）：

| 组件 | 形状 | 配色 | 状态 | 备注 |
|---|---|---|---|---|
| **Panel 面板** | 切角矩形 | 底 PANEL_BG + 主色边框 | — | 角标点 + 可选连接线 |
| **Button 按钮** | 切角矩形 | normal 透明底+边框 / hover 发光 / pressed 实心反色 | 3 态 | 主操作=实心主色，危险=实心 DANGER |
| **Progress 进度条** | 切角外框 + 分段填充 | HP 琥珀→黄→绿 / AP 绿 / EXP 琥珀 | — | 分段方块非连续渐变 |
| **ListRow 列表行** | 切角矩形 + 左侧色条 | 色条编码状态(绿/琥珀/灰) | hover 边框亮 | 右侧可选箭头 |
| **Card 信息卡** | 切角矩形 + 角标点 | 同 Panel | — | 含图标+标题栏+属性条+描述区 |
| **NavBar 导航栏** | 六边形按钮串联 | 当前页实心高亮 | 当前/未选 | 横排顶部或竖排侧边 |
| **Modal 弹窗** | 切角矩形 + 遮罩 | 标题带图标 | — | 半透明黑遮罩 0.7a |
| **Toast 通知** | 小切角矩形 | success绿✓/warning琥珀⚠/error琥珀✗ | slide-in→fade | 右滑入 3s 消失 |
| **VerticalTitle 竖排标题** | 竖排文字 + 顶部色条 | 主色亮 | — | 面板左侧 |
| **HexIcon 六边形图标区** | 纯六边形 | 主色描边 | — | 单位头像/地图缩略 |

---

## 6. 页面布局原则（Layout Principles）

1. **无直角、无直角、无直角**（重要说三遍）
2. **信息密度分级**：核心操作（开战/结束回合）永远最显眼（实心主色大按钮）
3. **边框语言统一**：所有面板边框 2px 主色，嵌套面板用 1.5px 暗色
4. **装饰克制**：连接线/角标点用于"分区"而非"堆砌"，避免过度
5. **响应式锚定**：Godot 用锚点(anchor)而非固定像素，适配不同分辨率
6. **背景层次**：根背景纯色 + 六边形网格纹理(低透明度) + 面板半透明，形成纵深

---

## 7. 13 项功能页的配色语境映射（供设计时参考）

| 功能页 | 语境 | 主色 |
|---|---|---|
| 1 战场指挥 | 战斗 | 绿色 |
| 2 战术部署(整备室) | 战斗 | 绿色 |
| 3 登录注册 | 中性 | 绿色 |
| 4 房间大厅 | 战斗 | 绿色 |
| 5 地图选择 | 战斗 | 绿色 |
| 6 单位编辑器 | 编辑 | 琥珀 |
| 7 词条库 | 编辑 | 琥珀 |
| 8 地图编辑器 | 编辑 | 琥珀 |
| 9 棋子库 | 浏览 | 琥珀(轻) |
| 10 AI 素材工坊 | 编辑 | 琥珀 |
| 11 骰子工坊 | 编辑 | 琥珀 |
| 12 尺寸工坊 | 编辑 | 琥珀 |
| 13 后台/反馈 | 中性 | 绿色 |

---

## 8. 设计交付要求

当你（设计师）产出具体页面设计时，请标注：
- 每个面板的切角尺寸（px）
- 使用的精确 HEX（从 §3.1 取，勿自创）
- 所属语境主色（绿色 / 琥珀）
- 组件类型（从 §5 选，新组件需补充到清单）
- 交互状态（hover/pressed/disabled 的视觉差异）

这样工程侧可直接对照 `Godot六边形UI样式规范-v01.md` 落地，无需二次猜测。

---

## 9. Amber Terminal HUD 补充规范（来自 stitch_ui 参考）

> 来源：用户提供的 `stitch_ui.zip`（Amber Terminal HUD 设计系统，HTML/CSS 实现）。
> 它**不能直接用于 Godot**（是 Web 技术），但其形状/配色/排版规律已吸收进我们的 Godot 组件。

### 9.1 额外形状规则（新增，叠加在 §2 之上）
- **反切角变体 (inverse)**：标准面板切"左上+右下"，反切角面板切"右上+左下"。两者交替使用产生"模块化咬合硬件感"。→ Godot `StyleBoxChamfer.inverse=true`
- **标题 tab**：面板左上角一个 solid 主色小色块标签（如 `ACTIVE` / `SYSTEMS`），用 `chamfer-panel-inverse` 切角。→ Godot `StyleBoxChamfer.title_tab=true`
- **玻璃拟态 (glass)**：非交互面板用半透明填充 `rgba(5,8,10,0.85)` + 8px 背景模糊。Godot 用半透明 Panel 模拟（`glass=true`，blur 用 `BackBufferCopy` 或暂用低 alpha 纯色）。

### 9.2 分段进度条精确规则
- 由 N 个独立方块组成（`flex gap-1`），每个方块 `h-4 flex-1`
- 激活段 = solid 主色 + glow；未激活段 = dim 主色 1px 边框
- → Godot `HexProgress.segmented=true, segments=10`

### 9.3 连接线/节点精确规则
- `hud-line`：1px 主色线，多用 45° 折线（leader lines）
- `hud-node`：4px 圆形节点 + `0 0 8px` 主色 glow
- 线末端必须终止于圆形节点或 2px 方形点
- → Godot `StyleBoxChamfer.decor_lines=true`（已实现短斜线+节点）

### 9.4 排版细节（对齐 stitch_ui）
- 所有面板标题用 `label-caps`：JetBrains Mono 11px / 600 / letter-spacing 0.12em / **全大写**
- 文字默认主色，次要数据用主色 70% 透明度
- 扫描线动画（scanline）：一条 4px 渐变横条从顶到底循环（Godot 可用 `TextureProgressBar` 或 Tween 实现，可选）

### 9.5 配色对应（stitch_ui → 我们的色板）
| stitch_ui | 我们锁定值 | 说明 |
|---|---|---|
| `primary #ffb000` | AMBER `#ffb000` | 完全一致 |
| `background #05080a` | BACKGROUND `#070b0d` | 我们稍亮一点，可改 `#05080a` |
| `signal-red #e63946` | DANGER `#ffb000` | 我们改用琥珀（用户要求去红） |
| `glass-fill rgba(5,8,10,0.85)` | glass_alpha 0.85 | 一致 |
| `tertiary #a1e5ff` (青) | GREEN 系 | 它做"第三色"点缀，我们做"战斗主色" |

> 注：stitch_ui 是**纯琥珀单色**系统。我们的双主色中"琥珀编辑系"直接以它为标杆；"绿色战斗系"按同构形状语言、换主色即可（原深青已替换为绿）。

### 9.6 预览场景
- Godot 内直接打开 `res://ui/UIPrototype.tscn` 即可看到全组件实机预览（含主题切换按钮）。
- 它演示：导航栏、标准切角面板(标题tab)、反切角面板、玻璃拟态+连接线面板、分段进度条、列表行、警告框+竖排标题。
