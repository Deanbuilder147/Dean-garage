# Godot 六边形 UI 样式规范（基于 p1 参考图）

> 初步设计 v0.1 — 基于 p1 截图提取的视觉语言，适配 Godot 4.x Control 节点体系
> 确认基调后落地为 `ui/Theme.tres` + `ui/components/*.gd` + 样机场景

---

## 一、视觉语言核心

### 1.1 形状哲学：一切皆六边形

p1 的灵魂在于**没有直角矩形**。所有面板都是：

| 组件 | p1 原型 | Godot 实现方式 |
|---|---|---|
| 面板/卡片 | 切角矩形（四角斜切 45°）或纯六边形 | `StyleBoxFlat` + 9-patch 切角纹理 **或** 自定义 `StyleBoxHexagon`（GDScript 绘制六边形路径） |
| 按钮 | 六边形/切角矩形 + 青色边框 | 同上，focus/pressed/hover 三态变色 |
| 进度条 | 六边形外框 + 内部分段方块 | `ProgressBar` + 自定义 StyleBox |
| 列表项 | 每行带左侧六边形色条 | `ItemList` 或 VBox+自定义行 |
| 对话框/警告框 | 六边形 + 角标装饰 | `AcceptDialog` + 自定义面板样式 |
| 标题栏 | 左侧竖排文字 + 顶部青色标签条 | HBox(Label 旋转) + Panel |

### 1.2 配色方案（最终确认 — 老 CRT 深青 + 琥珀双主色）

> 2026-08-15 用户确认：青色要"更深、老电脑/CRT 终端青"感（非亮荧光青），
> 同时保留琥珀色作为第二主色（战斗=深青，编辑器/数据=琥珀）。

```
┌─────────────────────────────────────────────┐
│  底色层                                      │
│  BACKGROUND     = #070b0d  (近黑，略带青灰)    │
│  PANEL_BG       = #0a1416  (面板底，深青灰)    │
│  PANEL_BG_ALT   = #06100f  (交替行/嵌套面板)    │
│  GRID_LINE      = #0c2622  (六边形网格线，低对比)│
│                                             │
│  【主色 A：老 CRT 深青】                       │
│  CYAN            = #1f9e8a  (深青，主强调，非荧光)│
│  CYAN_BRIGHT     = #38c4ab  (亮青，hover/发光)  │
│  CYAN_DARK       = #0d5f50  (深青，边框/分割线)  │
│  CYAN_DIM        = #073b32  (暗青，背景装饰)    │
│  CYAN_GLOW       = rgba(31,158,138,0.12)     │
│                                             │
│  【主色 B：琥珀】                             │
│  AMBER           = #ffb000  (经典琥珀终端色)    │
│  AMBER_BRIGHT    = #ffcf4d  (亮琥珀，hover)    │
│  AMBER_DARK      = #b37700  (深琥珀，边框)      │
│  AMBER_DIM       = #5c3d00  (暗琥珀，装饰)     │
│  AMBER_GLOW      = rgba(255,176,0,0.12)      │
│                                             │
│  语义色（从双主色派生）                         │
│  SUCCESS         = #2fae8f  (青绿，成功/友方)   │
│  DANGER          = #d9534f  (暗红，危险/错误)    │
│  WARNING         = #ffb000  (琥珀，警告)        │
│  ENEMY           = #d98a3a  (橙褐，敌人)        │
│                                             │
│  文字层                                    │
│  TEXT_PRIMARY    = #d6e8e2  (主文字，青白)     │
│  TEXT_SECONDARY  = #6d8a85  (次要/说明文字)    │
│  TEXT_MUTED      = #355049  (禁用/暗淡)       │
│  TEXT_ON_CYAN    = #04110e  (青底上的深色文字)  │
│  TEXT_ON_AMBER   = #1a1200  (琥珀底上的深色文字)│
│                                             │
│  装饰层                                    │
│  BORDER          = #1f9e8a  (主边框)          │
│  BORDER_DIM      = #0d5f50  (暗边框/分割)      │
│  CORNER_DOT      = #38c4ab  (角标圆点)        │
│  LINE_DECOR      = #0d5f50  (电路连接线)      │
│  NODE_DOT        = #38c4ab  (节点小圆)        │
└─────────────────────────────────────────────┘
```

**双主色使用约定（全局一致）：**
- 战斗相关（战场/技能/HUD/房间/对战）= **深青 CYAN 系**
- 编辑/数据/配置（单位编辑器/词条库/地图编辑器/骰子工坊）= **琥珀 AMBER 系**
- 中性组件（按钮/列表/弹窗/导航）= 默认深青，编辑页内可切琥珀主题
- 警告/错误/敌人用语义色，不混入主题色

### 1.3 科技装饰元素（p1 特征）

从参考图可见的"科技感"来源：

1. **角标装饰**：面板四角有小圆点 + 斜向短线（像电路节点）
2. **分段边框**：边框不是连续线，而是分段虚线/刻度线
3. **标题竖排**：某些面板左侧有竖排大字标题（如 "TITLE"、"ENIM"）
4. **连接线**：面板之间有细线 + 圆点相连（像电路图）
5. **顶部导航箭头**：一排不同样式的 chevron 箭头（`>>>` 变体）
6. **内发光**：面板内部边缘有微弱的青色光晕

> **Godot 落地策略**：角标/连接线用独立 `Line2D` / `Polygon2D` 子节点叠加；内发光用 `BackBufferCopy` + `ColorRect` 混合模式或直接在 StyleBox 里预烘焙。

---

## 二、组件样机（每个组件的视觉效果描述）

### 2.1 HexPanel（基础面板 — 最重要）

所有面板的基类。两种变体：

**变体 A：切角矩形（ChamferedRect）**
```
  ┌──────────────────────────────┐
 ╱  ┌── TITLE BAR ──────────╮   ╱
│  │ 内容区域                │  │
│  │                        │  │
 ╲  └────────────────────────┘ ╱
  └──────────────────────────────┘
   ●                          ●    ← 角标圆点
```
- 四角切掉约 12~16px 的 45° 斜角
- 边框 2px `PRIMARY_DARK`，内填充 `PANEL_BG`
- 左上/右下角各一个 `CORNER_DOT` 小圆点（半径 3px）
- 可选：顶部左侧竖排标题（旋转 -90° Label）

**变体 B：纯六边形（Hexagon）**
```
       ╱╲
      ╱  ╲
     ╱    ╲    ← 用于特殊高亮面板（如当前回合提示）
    │      │
    │      │
     ╲    ╱
      ╲  ╱
       ╱╲
```
- 正六边形（flat-top 或 pointy-top，和战场六角格一致）
- 用途：雷达图、地图预览、特殊状态指示器

### 2.2 HexButton（按钮）

```
  ╱────────╲
 ╱  LABEL   ╱     ← 正常态
╱──────────╲

  ╱▓▓▓▓▓▓▓▓╲
 ╱▓ LABEL  ▓╱     ← hover 态（填充 PRIMARY_GLOW）
╱▓▓▓▓▓▓▓▓▓╲

  ╱========╲
 ╱= LABEL = ╱     ← pressed 态（反色/实心填充）
╱============╲
```

- 三种尺寸：Small(图标按钮) / Medium(操作按钮) / Large(主行动按钮)
- 主操作按钮用 `PRIMARY` 填充 + 深色文字
- 危险操作（退出/删除）用 `ACCENT` 填充
- 图标按钮：纯边框 + 居中图标，hover 时显示图标发光

### 2.3 HexProgress（进度条）

```
  ╱▓▓▓▓▓▓▓▓░░░░░░░░╲     ← 外框六边形/切角矩形
 ╱▓▓▓▓▓▓▓▓▓▓░░░░░░░╱    ← 已填充段 = ACCENT_SOFT
╱═════════════════════╲   ← 未填充 = PANEL_BG
```

- 用途：HP 条、AP 条、加载进度、经验值
- 分段式（每段一个小方块，非连续渐变）—— 更符合 p1 风格
- HP: 红→黄→绿 渐变（按百分比）
- AP: 青色
- EXP: 琥珀色

### 2.4 HexListRow（列表行）

```
  ╱──────────────────────────────────╲
 ╱■ 名称描述              详情  >  ╱    ← ■ = 左侧色条（可编码状态）
╱──────────────────────────────────╲
```

- 每行是一个切角矩形
- 左侧 4px 宽的竖条颜色表示状态：
  - 青色 = 可选/可用
  - 绿色 = 友方/已选中
  - 红色 = 敌方/不可用
  - 灰色 = 锁定/未解锁
- 右侧可选箭头/复选框
- hover 时整行边框亮起

### 2.5 HexCard（信息卡片 — 如单位信息/技能详情）

```
  ╱━━━━━━━━━━━━━━━━━━━━━━━━━╲
 ╱│ [图标]  单位名称    Lvl.5 │╱
 ╱│ ───────────────────────── │╱
 ╱│ HP  ████░░░░  45/100     │╱
 ╱│ AP  ██░░░░░░   2/3       │╱
 ╱│                              │╱
 ╱│ 描述文字区域...             │╱
  ╲______________________________╱
  ●                          ●
```

- 比 HexPanel 多了结构化内容区（图标+名称栏+属性条+描述）
- 用于：单位 tooltip、技能详情、房间列表中的房间卡片

### 2.6 HexNavBar（顶部/侧边导航栏）

```
  ╱═══╲╱═══╲╱═══╲╱═══╲╱═══╲    ← 六边形按钮串联
 ╱ 战斗╱╱ 大厅╱╱ 编辑╱╱ 设置╱╱ 更多╱
╱═════╲╱═════╲╱═════╲╱═════╲╱═════╲
```

- 导航按钮紧密排列，相邻按钮共享边框线（像 p1 顶部的箭头排列）
- 当前页面对应的按钮高亮（实心填充）
- 可横排（顶部导航）或竖排（侧边导航）

### 2.7 HexModal（弹窗/对话框）

```
        ╱╲
       ╱  ╲
      ╱ ┌──────────────────┐  ╲
     ╱  │  ⚠ WARNING!      │   ╲
    │   │  ──────────────── │    │
    │   │  确认执行此操作？   │    │
    │   │                  │    │
    │   │   [取消]  [确定]  │    │
     ╲  └──────────────────┘  ╱
      ╲                    ╱
       ╲  ╱
        ╱╲
```
- 半透明黑色遮罩 (`ColorRect` + `modulate.a = 0.7`)
- 弹窗本身是六边形/切角矩形
- 标题栏带图标 + 文字
- 底部按钮右对齐

### 2.8 Toast（浮动通知）

```
  ╱✓ 操作成功 ╲     ← 从右侧滑入，3秒后消失
 ╱──────────────╱
```
- 小型切角矩形
- 三种类型：success(绿✓) / warning(橙⚠) / error(红✗)
- 动画：slide-in-right → 停留 → fade-out

---

## 三、页面布局样机（关键页面的整体样子）

### 3.1 主菜单 MainMenu

```
╔══════════════════════════════════════════════════╗
║  >>> >>> >> >>>> >> > >>>                         ╝  ← 顶部装饰箭头
║                                                    ║
║         ╱╲                                        ║
║        ╱  ╲   ╔═══════════════╗                   ║
║       ╱ MECHA╲ ║  开始游戏      ║  ← 大六边形Logo区  ║
║      ╱ UNIVERSE║╟───────────────╢                   ║
║      ╲        ╱║  设置选项      ║                   ║
║       ╲  ╱  ╱  ╚═══════════════╝                   ║
║        ╱╲ ╱                                       ║
║                                                    ║
║  ╔═══════════╗  ╔═══════════╗                     ║
║  ║ 服务器公告 ║  ║ 版本信息   ║  ← 信息卡片          ║
║  ╚═══════════╝  ╚═══════════╝                     ║
╚══════════════════════════════════════════════════╝
  ●                                            ●
```

- 居中大 Logo（六边形边框 + 项目名）
- 中央 2~3 个大按钮（开始游戏/设置/退出）
- 底部两侧小信息卡片
- 背景：深色 + 微弱的六边形网格纹理（呼应战场）

### 3.2 战斗界面 Battle3D（改造后）

```
╔═══════════════════════════════════════════════════════════╗
║ ╱ 回合 3 ╲  ╱ 我方行动 ╲        ╱ 敌方剩余 5 单位 ╲       ║
║                                                    ║
║ ╔═══════════════════════════════╗  ╔═══════════════╗  ║
║ ║                               ║  ║ 技能菜单       ║  ║
║ ║       3D 战场渲染区域           ║  ╠═══════════════╣  ║
║ ║       (SubViewport)           ║  ║ [移动] [攻击]  ║  ║
║ ║                               ║  ║ [防御] [待机]  ║  ║
║ ║                               ║  ║ ──────────── ║  ║
║ ║                               ║  ║ [技能A]       ║  ║
║ ║                               ║  ║ [技能B]       ║  ║
║ ╚═══════════════════════════════╝  ╚═══════════════╝  ║
║                                                    ║
║ ╔═══════════════╗  ╔═══════════════╗  ╔══════════╗  ║
║ ║ 当前单位       ║  ║ 战斗日志       ║  ║ 结束回合  ║  ║
║ ║ HP ████░░ 80  ║  ║ > 命中! -25   ║  ║ [结束]   ║  ║
║ ╚═══════════════╝  ╚═══════════════╝  ╚══════════╝  ║
╚═══════════════════════════════════════════════════════════╝
```

- 顶部：回合数 + 行动方 + 敌方统计（一行 HexPanel）
- 中央：战场 3D 视口（不变，已有）
- 右侧：技能菜单（HexPanel 内 VBox 按钮）
- 底部三栏：当前单位信息 / 战斗日志滚动 / 结束回合按钮
- 所有面板统一切角矩形 + 青色边框

### 3.3 整备室/准备大厅 PrepRoom

```
╔═══════════════════════════════════════════════════════════╗
║ ╱ 准备大厅 ╲  ╱ 房间: Alpha-7749 ╲    ╱ [开战!] ╲        ║
╠═══════════════════════════════════════════════════════════╣
║                                                    ║
║ ╔═══════════════════╗  ╔═════════════════════════════╗  ║
║ ║ 我方阵容            ║  ║ 地图预览                    ║  ║
║ ╠═══════════════════╣  ║  ╱╲                          ║  ║
║ ║■ [单位A] Lvl.5  ✓  ║  ║ ╱  ╲  六边形地图缩略        ║  ║
║ ║■ [单位B] Lvl.3  ✓  ║  ║╱────╲                       ║  ║
║ ║■ [单位C] Lvl.1  ✓  ║  ║╲    ╱                       ║  ║
║ ║░ [空槽]      +     ║  ║ ╲  ╱                         ║  ║
║ ╚═══════════════════╝  ║  ╲╱                           ║  ║
║                        ╚═════════════════════════════╝  ║
║ ╔═════════════════════════════════════════════════════╗  ║
║ ║ 聊天 / 日志                                         ║  ║
║ ║ > 玩家B 加入了房间                                   ║  ║
║ ╚═════════════════════════════════════════════════════╝  ║
╚═══════════════════════════════════════════════════════════╝
```

- 左侧：己方单位列表（拖拽排序/添加/移除）
- 右上：地图六边形预览
- 底部：聊天日志 + 开战按钮（仅房主可见）

---

## 四、Godot 技术实现要点

### 4.1 六边形 StyleBox 方案

Godot 4 没有"内置六边形 StyleBox"，有两种实现路径：

**方案 A：StyleBoxTexture + 预渲染九宫格（推荐首发）**
- 用代码/工具生成一张切角矩形的 PNG 九宫格（3x3 切片）
- `StyleBoxTexture` 加载它，支持任意尺寸拉伸
- 优点：性能最好，即插即用
- 缺点：需要预生成资源文件（或启动时 GDScript 生成并缓存）

**方案 B：自定义 StyleBox（GDScript 继承 StyleBox）**
- 继承 `StyleBox`，重写 `_draw()` 用 `DrawingCommand` 画六边形路径
- 优点：完全程序化，参数可调（切角大小、圆角等）
- 缺点：每帧绘制开销略大（但 UI 元素数量可控，问题不大）

**建议**：先用方案 A 快速落地（我可以写一个 GDScript 工具脚本，首次运行时生成一套切角矩形 PNG 资源到 `ui/assets/`），后续迭代到方案 B。

### 4.2 字体

- 主字体：等宽或半等宽科幻风（如 JetBrains Mono / Share Tech Mono / Noto Sans Mono）
- 中文 fallback：Noto Sans CJK SC（Google 出的等宽/半等宽中文）
- 标题可用稍粗字重（Bold/SemiBold）
- 字体文件放 `ui/fonts/`

### 4.3 动画

- 按钮 hover：边框亮度 tween 0.15s
- 页面切换：fade 或 slide（tween 0.25s）
- Toast：slide_from_right + auto_hide
- 进度条填充：tween value（0.3s）
- 全部用 Godot Tween 系统，不用 AnimationPlayer（轻量）

### 4.4 背景处理

- 主背景：纯色 `BACKGROUND` + 可选六边形网格图案（`TextureRect` + 平铺 + 低不透明度）
- 网格图案可以和战场的六角格视觉语言统一
- 面板背景：`PANEL_BG` 微透明（90%），让底层网格隐约透出来增加层次

---

## 五、待确认项 — ✅ 已全部确认（2026-08-15）

1. **[✅] 面板形状**：切角矩形（chamfered rect）为主，纯六边形仅用于雷达/地图/状态指示器
2. **[✅] 连接线装饰**：保留电路节点风装饰（角标圆点 + 连接线 + 节点）
3. **[✅] 竖排标题**：保留左侧竖排文字标签
4. **[✅] 配色**：**老 CRT 深青 `#1f9e8a` 系 + 琥珀 `#ffb000` 系双主色**（已更新 §1.2）
5. **[✅] 字体**：先用 JetBrains Mono + Noto Sans CJK SC，用户后续可能替换

---

## 六、下一步动作

确认以上 5 点后，我将：

1. **生成 UI 资产生成工具** `ui/tools/generate_assets.gd` —— 运行一次即可产出切角矩形 PNG 九宫格（各种尺寸/状态）
2. **创建 Theme.tres** —— 把配色/字体/StyleBox 全局主题写入
3. **创建 `ui/components/` 复用组件** —— HexPanel / HexButton / HexProgress / HexListRow / HexCard / HexModal / Toast / NavBar
4. **搭建样机场景** `ui/prototypes/UIPrototype.tscn` —— 把所有组件摆在一个场景里供你实时预览
5. **替换 Battle3D.tscn 的 UILayer** —— 用新组件重建战斗 HUD
