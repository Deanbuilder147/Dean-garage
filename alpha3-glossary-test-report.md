# 词条添加测试报告（Alpha 3.0 · 31 基础词条回归）

> 测试时间：2026-08-08　测试账号：`deantest`（referee，具 `glossary.edit` 授权）
> 入口：`POST /api/combat-glossary/hub-config`（新建）/ `PUT /api/combat-glossary/hub-config/:key`（更新）
> 数据来源：`桌面/alpha3.0 系统/核心-词条库与战斗引擎-六段式编排与67原子全景报告-2026-08-08.md` 第三部分「基础 31 条词条」
> 环境：线上 8081（部署于本次 Alpha3.0 发布后）

---

## 一、测试结论速览

| 指标 | 结果 |
|---|---|
| 词条总数 | 31 条（报告 Part 3：近战4 / 远程4 / 自动化4 / 特殊加成3 / 攻击6 / 协同4 / 阵营6） |
| POST 新建成功 | 25 条（200） |
| KEY_CONFLICT 转 PUT 更新成功 | 6 条（200） |
| **最终落库** | **31 / 31（100%）** |
| 库内 skillCount | 41（31 新增 + 10 核心兜底技能） |
| 缺失词条 | 无 |

**所有能添加的词条均已保存。** 6 条"失败"实为 `KEY_CONFLICT`（skill_key 已存在），经 PUT 更新后同样落库，并非真正不可添加。

---

## 二、逐条结果（节选关键冲突）

| # | 词条 | skill_key | POST 结果 | 处置 |
|---|---|---|---|---|
| 1 | 反击 | f_counter | 409 KEY_CONFLICT | PUT 更新 ✓ |
| 2 | 格挡 | block | 409 KEY_CONFLICT | PUT 更新 ✓ |
| 3 | 长柄 | polearm | 200 ✓ | — |
| 6 | 投掷 | throw | 409 KEY_CONFLICT | PUT 更新 ✓ |
| 16 | 斩杀 | execute | 409 KEY_CONFLICT | PUT 更新 ✓ |
| 17 | 决斗 | duel | 409 KEY_CONFLICT | PUT 更新 ✓ |
| 20 | 幸运 | lucky | 409 KEY_CONFLICT | PUT 更新 ✓ |
| 其余 25 条 | — | — | 200 ✓ | — |

> 6 个冲突 key（f_counter/block/throw/execute/duel/lucky）经查为存储A 历史业务词条（Alpha3.0 中枢改造迁移脚本 `migrate-legacy-skills.cjs` 写入的 9 条业务词条的子集），故 POST 撞键，非编辑器缺陷。

---

## 三、遇到的问题（显微知著 · 由表及里）

### 问题 A：「添加」动作被割裂为新建/更新两个入口，玩家认知与系统行为错位
- **表象**：用编辑器"添加"一个已存在的词条，系统返回 409 `KEY_CONFLICT` 并拒绝，玩家以为"加不进去"。
- **显微**：`POST /hub-config` 与 `PUT /hub-config/:key` 是两个独立路由，POST 逻辑是"不存在才建、存在即拒"（glossary.ts:514）。编辑器 UI 的"保存"按钮若绑定到 POST，遇到同名即撞墙。
- **普适性**：**任何"保存"语义的资源，都应提供 upsert（存在即更新）而非强制区分 create/update**。这是 CRUD 接口设计的通用坑——"我想存这个东西"与"它是新是旧"对用户是同一意图，系统却拆成两个错误码。

### 问题 B：skill_key 无自动派生，强依赖玩家手填，埋下命名冲突与中文 key 隐患
- **表象**：脚本须人工映射「中文名→拼音 slug」才能避免冲突；若玩家直接填中文 key 或留空，行为不可预期（留空则生成 `skill_draft_<时间戳>` 匿名草稿，极易重复/混淆）。
- **显微**：`generateHubDraft` 仅 `b.skill_key || ('skill_draft_'+Date.now())`（glossary.ts:426），无基于 `name` 的 slug 派生、无唯一性二次校验。
- **普适性**：**用户态资源的主键应由系统保障唯一性（slug 化 + 查重兜底），而非委托玩家手工维持**。这是"字段就位≠功能就位"纪律的又一实例——`skill_key` 字段存在，但"如何生成合法且不冲突的 key"这一功能缺失。

### 问题 C：存储A 写回是 deepMerge 而非 replace，历史脏键永久残留
- **表象**：同一词条反复存改后，旧结构（如 `map_cannon.directions` 遗留的 `"0"` 脏数组键）无法清除。
- **显微**：`saveGlossaryConfig` 内部 deepMerge（glossary.ts:551-554 注释已自陈此症状），`{...old, ...normalized}` 只能加不能删。
- **普适性**：**"配置写回"必须区分"整对象替换"与"字段增量"两种语义，且默认应为 replace**。merge 式落库天然产生"删不掉的过去"，是长期运维毒药，任何持久化层都应显式提供 `_reset`/整替入口。

### 问题 D：67 原子与编辑器字段存在语义断层，部分词条"存得进但引擎读不出"
- **表象**：报告 Part 4「冲突段」词条的 67 原子（如「隐匿」的"不可被选中"[I2]、"联防"的"阻断穿越"[H4]）在编辑器六段式骨架里无对应落位字段，存为草稿后引擎无消费点。
- **显微**：`generateHubDraft` 仅注入 `timing/cost/roll/target/conditions/effects` 骨架（glossary.ts:437-442），`effects[]` 的 `type` 枚举与 67 原子编号（A~K）未建立映射；存进去的 `effects` 仅 `{type:'damage',value:5}` 这类已实现的，未实现的原子成"死配置"。
- **普适性**：**数据契约与引擎消费点必须双向可校验**。新增一个语义原子，必须同时（1）在编辑器暴露为可选枚举，（2）在引擎注册消费点，否则"配置能存"≠"功能生效"——这正是蓝图两条纪律的核心：字段就位≠功能就位。

### 问题 E（非缺陷，记录）：权限门槛正确但无提示
- `glossary.edit` 授权外角色调用返回 403 `FORBIDDEN`。deantest(referee) 具权，普通玩家无——属设计约束，但编辑器应在无权限时给出"申请授权"引导而非裸 403。

---

## 四、普适性解决方案设计

| 方案 | 针对问题 | 设计要点 | 适用范围 |
|---|---|---|---|
| **S1 · 统一 upsert 语义** | A | 编辑器"保存"统一调一个 `saveEntry()`：后端检测到 KEY_CONFLICT 自动转 PUT 全量替换（或新增 `PATCH /hub-config/:key` 语义=upsert）。前端不再区分"新建/编辑"按钮。 | 所有"保存即覆盖"的资源型接口 |
| **S2 · skill_key 系统派生 + 唯一性兜底** | B | 保存时若 `skill_key` 为空，按 `name` 生成 `slugify(key)`；生成后若冲突，追加短哈希后缀保证唯一。玩家填的 key 经 `^[a-z0-9_]+$` 校验，中文 key 直接拒绝并提示。 | 任何带人类可读名的主键资源 |
| **S3 · 存储A 整替落库 + 脏键清除** | C | `saveGlossaryConfig` 改为"读取基线→对该 key 整对象替换（非 merge）→写回"；提供 `_reset:true` 标志允许清空子结构。迁移时一次性清理 `map_cannon.directions` 类脏键。 | 所有 JSON 配置型持久化 |
| **S4 · 67 原子枚举双向注册 + 编辑器语义校验** | D | 在 shared-kernel 建 `ATOM_ENUM`（A1~K4 全 67 项）作为唯一真相源；编辑器「DO/Cost 段」只能从枚举拖入；保存时校验所用原子是否已在引擎注册消费点，未注册者在 UI 标黄"该原子暂未接入引擎"，并写入 `_unimplemented_atoms[]` 供后续收敛。 | 所有"配置驱动引擎"的插件化系统 |
| **S5 · 权限引导而非裸拒** | E | 403 时返回 `required_feature:'glossary.edit'` + 申请入口，前端弹"向房主申请编辑权"。 | 所有基于 feature 的鉴权端点 |

---

## 五、验证与回归

- 31 条落库后 `GET /hub-config` 返回 `skillCount:41`，31 个报告词条 `name` 全部命中，缺失列表为空 ✅
- 6 条冲突词条经 PUT 更新后内容可正常读回（200 `词条已保存至存储A`）✅
- 本次测试**未触碰 mecha-battle-db**（地图数据受限库），仅操作 gateway 自身存储A（SQLite 词条库），符合部署纪律。

## 六、遗留与建议

1. **建议优先落地 S1+S2**（成本最低、直接消除"加不进去"的玩家体感问题），与本次 Alpha3.0 的 B-4（Macro 边界校验）、A-5（单一写回出口）精神一致——都是"入口收敛 + 主键保障"。
2. **S3 脏键清理**建议与下一轮存储A 迁移脚本合并执行，避免线上配置腐化累积。
3. **S4 是根治"字段就位≠功能就位"的系统级方案**：67 原子枚举注册后，未来任何新节点（如本次 I-6/C-6）新增的语义字段都能被编辑器自动暴露、被引擎消费点反向校验，从根上消灭"看起来做了其实没接"的盲区。
4. 报告 Part 4 的「冲突段」词条（如隐匿/联防/奇袭）目前仅能存为骨架草稿，其 67 原子（I2/H4/G5 等）需先在引擎 `skillExecutor` 注册消费点，才能真正"填写生效"——这超出本次添加测试范围，列为后续 Phase 任务。
