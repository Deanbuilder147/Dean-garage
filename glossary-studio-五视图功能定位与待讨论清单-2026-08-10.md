# /glossary-studio 五视图功能定位 + 待讨论清单

> 生成日期：2026-08-10
> 主操作空间锁定：`/glossary-studio`（`GlossaryHubNew.vue` + `HexCanvasEditor.vue`）
> 旧页 `glossary` / `glossary-editor` 暂挂（路由保留、导航不挂），后续再定。
> 最近更新：2026-08-11 第三轮——查证 H 节三文件实证：damagePipe 真实入口 `DamagePipe.calculate(config)`（非 calculateDamage）；skillExecutor `_walkPhaseTree` 无 pure 模式（需新增）；hexMath 无 deriveRange/computeAOECells（需新增 computeAOECells，getHexesInRange 可复用）。已修正契约函数名与落地依赖。

## 一、已确认的设计决策

### A. 六段式 WHEN 锚点 + 列序发动
- 每个词条都需要一个 `WHEN` 结构开头。
- `WHEN` 作为锚点放在**第 2 列（索引 1）**；编辑器内可视排布自由（不固定泳道），但最终**发动顺序由引擎约束归一化**（非纯列序自由）。
- **六段顺序约束（2026-08-11 拍板·详见「六段约束集」一节）**：顶层六段引擎侧锁死为 `WHEN→IF→WHO→ROLL→DO→COST`，但支持**带约束的自由调序（拓扑排序）**——少数硬边不可违反：
  - `WHEN → 其余所有`（触发闸门最前）
  - `IF → DO`（IF 为闸门，`return` 后 DO 已结算不可回滚）
  - `WHO → DO`（及建议 `WHO → ROLL`，选靶须先于结算/掷骰）
  - `ROLL → 消费 roll 的 IF`（ROLL 掷骰后 `rollV` 透传，IF 早于 ROLL 则 `roll=0`）
  - **可自由部分**：`ROLL ↔ COST` 先后、`COST` 可多次出现、同 phase 内原子顺序。
  - 编辑器允许拖拽生成 `user_order`，引擎入口 `normalizePhaseOrder(tree)` 按上述硬边归位（撞边时 UI 提示"已自动调整"）。
- **交互流程**：玩家先**拖拽「字头」（段头/词条头）进入格子内**，落格后再在该格子内部进行下一步操作（段内字段编辑 / 子结构组织）。即「先拖入格子占位 → 格内再细化」的两步式。

### B. 单位自身原点统一
- 射程 / 地图炮 / AOE / 战场模拟 **全部以地图格中心点（画布正中心）当作「单位本身」**。
- 代码侧已落地：`HexCanvasEditor.vue` 新增 `UNIT_ORIGIN = { q: GRID_COLS/2, r: GRID_ROWS/2 }`，射程中心、地图炮六向补全、六段式绘制与点击检测、拖拽落点全部改用 `UNIT_ORIGIN`；战场模拟独立 canvas 的 `shooterCell().center` 同样以 `(BC_COLS/2, BC_ROWS/2)` 为锚。

### C. AOE 原点 = 目标点（非玩家原点）
- AOE 形状由用户在 **AOE 视图手动画形状**，逻辑类似地图炮（用户自由绘制阴影 = 攻击范围）。
- 区别在于**攻击范围原点**：AOE 的原点由**攻击目标点**决定（玩家先在射程内/战场选一个目标点，再以其为原点画 AOE 形状）；地图炮的原点是**玩家自身（UNIT_ORIGIN）**。
- 即 AOE = 「地图炮逻辑 + 原点换成目标点」。

### D. 地图炮：纯六向补全，不用方向键
- 地图炮**不需要方向键**，只需一个**六向补全**按钮。
- **默认绘图朝向 = 正右方向（东，dir=0）**：玩家第一次手绘的阴影形状，程序默认当作「正右方向」的范围基准。即玩家无需画方向，只需画**一块相对原点向右延伸的阴影**，系统以这块正右阴影为基准做后续旋转。
- 程序逻辑：以正右阴影为基准，以 60° 为步长旋转复制，得到 6 组阴影坐标（每组相对原点的旋转偏移不同，但形状一致）。
- 这 6 组阴影坐标按**原点指向各组的方位**映射为 6 个攻击方向（正右=dir0，顺时针每 60° 一个：右下/下/左下/左上/上/右上），从而自动定义六个方向的攻击范围。即 `mcShapes[dir] = rotate(baseShadowEast, dir*60°)`，`dir=0` 即玩家画的正右阴影。

### E. 战场模拟联动：消费真实结算 + 手动布子
- 战场模拟只模拟**当前词条的实际计算**：即调用战斗核心（combat-service / damagePipe / skillExecutor）对 range / aoe / map 的真实数据做结算，并**展示**计算结果（命中区、伤害数值、状态等）。
- 另外支持**手动添加并布置敌方 / 友方单位**：
  - 敌 / 友 / 自身 单位**默认血量 100、机动值 14**（DEFAULT：HP=100，MOB=14）。
  - 玩家可在战场格上放置、拖拽这些单位，再触发当前词条结算，观察命中与伤害。
- 不再只是示意，必须回放 rangeCtx（射程环带）+ AOE 形状 + 地图炮方向 的真实数据。

### F. 数据库落库：需求确定后再设计
- **暂缓**：等上述功能需求完全确定后，再检查数据应落在哪里、如何建模。
- 当前 `rangeCtx` 仍是前端临时态；落库契约（rangeCtx → draft.target/range/aoe/mapcannon 等）在功能定稿后设计，本次不实现。

### G. 射程真相源回写：需要双向
- 射程真相源**需要回写**：当前是单向（draft → 画板），需改为**双向**——
  画板修改了 min/max（或形状）后，写回 `draft.target`（或对应 range 段）。
- 优先级仍保持：`target.min_range/max_range` > `draft.range` 旧字段。
- 落库字段（F）定稿后，回写目标字段随之确定。

### H. 战场模拟结算接口契约（2026-08-11 拍板·四步法）
> 战场模拟（battle 视图）必须消费**真实结算路径**，不再只是示意。契约如下：

**第一步：命中格推导（Range + AOE）**
- 调用 `shared-kernel/src/hexMath.ts`（**2026-08-11 实证：现有 `getHexesInRange` / `hexDistance` / `isTargetInRange`，尚无 `deriveRange` / `computeAOECells`**）。
  - 射程环带：`getHexesInRange(center, maxRange, minRange?)` 已存在，可直接复用为"选点受射程限"的判定。
  - AOE 作用范围（可超射程）：需**新增** `computeAOECells(casterCoord, targetCoord, aoeShape)` —— 以 targetCoord 为原点、按 AOE 形状（用户手绘阴影 / 地图炮六向补全）展开，半径**独立于 maxRange**（满足 #2 "作用范围可超射程"）。
  - `deriveRange()` 可不单独建，直接用 `getHexesInRange` 替代；若需统一入口再包一层。
- 导出最终命中的六角格集合 `affectedCells`；从 `battleState.units` 中筛选出位于这些格子上的 `targetUnitIds`。

**第二步：模拟上下文构建（Simulated Context Frame）**
- 构造临时 `simulatedCtx`，将施法者、目标单位列表、指定的 `rollValue` 传入。
- **隔离红线**：深拷贝单位的 `equipState` 与 `stats`，**严禁直接修改原 `battleState` 对象**（只读消费，结果增量返回）。`effectExecutor` 内部对 `unit.statusEffects` 的 push 必须改为"写克隆帧、不回写原对象"（见第三步 pure 开关）。

**第三步：求值与伤害计算（Skill & Damage Pipe）**
- 调起 `skillExecutor._walkPhaseTree`（**2026-08-11 实证：当前无 pure 模式**，`pure_segment` 仅是日志模型名；会直接 `unit.statusEffects.push`）。**须新增 `pure` 开关参数**（如 `_walkPhaseTree(tree, unit, target, rollV, context, depth, { pure: true })`）：pure 模式下 `effectExecutor` 的 statusEffects 写入改落 `context.cloneFrame` 不回写原 unit。
- 针对每个受击单位，调用 `DamagePipe.calculate(config)`（**2026-08-11 实证：真实完整入口是 `DamagePipe.calculate(config)`，不是 `calculateDamage`**；`config = { attacker, defender, attack_type, damage_kind, terrainDefs, baseDamage? }`）走**真实结算路径**，返回 `result.final_damage` / `result.is_crit` / `result.stages`（含减伤明细）。
  - 简版入口 `DamagePipe.calculateQuick(attacker, defender, attackType, terrainDefs)` 亦可，但缺失 `damage_kind`/`baseDamage` 等词条维度，战场模拟建议用完整 `calculate`。

**第四步：返回增量结果**
- 组装 `SimulateSkillImpactOutput`（`{ affectedCells, targetUnitIds, perUnit: [{ unitId, finalDamage, isCrit, stages }] }`）返回；前端 `HexCanvasEditor` 接收后直接在画布上**高亮 `affectedCells`** 并**弹框显示 `finalDamage`**。

**落地依赖（2026-08-11 实证结论）**：
1. `hexMath.ts`：**新增** `computeAOECells(casterCoord, targetCoord, aoeShape)`（AOE 作用范围，半径独立于 maxRange）；`getHexesInRange` 已可复用为射程判定，无需新建 `deriveRange`。
2. `skillExecutor._walkPhaseTree`：**新增 `pure` 开关**，使 statusEffects 写入落克隆帧、不污染原 `battleState`（满足隔离红线）。
3. `damagePipe.cjs`：真实接口为 `DamagePipe.calculate(config)` / `calculateQuick(...)`，**无 `calculateDamage`**；调用方按 `config` 形状组装，确认其只读 `attacker/defender` 不内部改写（当前 `calculate` 仅读不写 unit，符合红线）。

## 二、五视图功能定位（用户定义 + 已拍板逻辑）

| 视图 | 功能定位 | 确认逻辑 | 当前实现状态 |
| # | 分组 | 原子编号 | 字段 | 字段翻译·编号 | 字段翻译·编号 | 形式 | 描述（参考 alpha3.0 权威 67 原子清单） |
|---|---|---|---|---|---|---|
| **六段式 (hexsix)** | 不固定泳道，以列序为发动顺序 | 玩家**拖拽字头入格**→格内再操作；WHEN 在第 2 列作锚点 | 6 段星形已绘制；WHEN 已移第 2 列；**缺拖拽字头入格 + 列序发动链路** | F1 6 段星形已绘制；WHEN 已移第 2 列；**缺拖拽字头入格 + 列序发动链路** (×1) | F1 6 段星形已绘制；WHEN 已移第 2 列；**缺拖拽字头入格 + 列序发动链路** (×1) |  
| **射程 (range)** | 确定词条射程范围 | 真相源双向回写（G）：画板改 min/max → 写回 draft.target | 环带射程已实现（盲区+环带+环号+线性射线）；**缺画板→draft 回写** | F2 环带射程已实现（盲区+环带+环号+线性射线）；**缺画板→draft 回写** (×1) | F2 环带射程已实现（盲区+环带+环号+线性射线）；**缺画板→draft 回写** (×1) |  
| **AOE (aoe)** | 设目标点后，算伤害作用范围形状 | 用户在 AOE 视图**手动画形状**；原点 = 目标点（非玩家原点，见 C） | 底层圆形扩散+落点蓝圈；**缺手绘形状 + 目标点原点链路** | F3 底层圆形扩散+落点蓝圈；**缺手绘形状 + 目标点原点链路** (×1) | F3 底层圆形扩散+落点蓝圈；**缺手绘形状 + 目标点原点链路** (×1) |  
| **地图炮 (map)** | 玩家原点 + 攻击方向与形状 | **不用方向键**，画阴影 → 60° 旋转补全六向（见 D） | 6 向工具栏+六向补全+清除；**待改为纯六向补全（去掉方向键依赖）** | F4 6 向工具栏+六向补全+清除；**待改为纯六向补全（去掉方向键依赖）** (×1) | F4 6 向工具栏+六向补全+清除；**待改为纯六向补全（去掉方向键依赖）** (×1) |  
| **战场模拟 (battle)** | 模拟词条实际结算 + 手动布子 | 调用真实战斗核心/管道结算并展示；手动加敌/友/自身（HP100/MOB14，见 E） | 独立 canvas 示意；**缺真实结算消费 + 布子（默认 HP100/MOB14）** | F5 独立 canvas 示意；**缺真实结算消费 + 布子（默认 HP100/MOB14）** (×1) | F5 独立 canvas 示意；**缺真实结算消费 + 布子（默认 HP100/MOB14）** (×1) |  

## 四、六段式视图交互设计（用户定义）

> 本节为六段式 (hexsix) 视图的**完整交互规格**，含按钮、界面显示规则与基础操作流程。

### 4.1 按钮栏

| 按钮 | 行为 | 备注 |
|---|---|---|
| **段式按钮** | 6 个彩色段头：`IF` / `WHEN` / `WHO` / `ROLL` / `DO` / `COST`，**各用不同颜色区分** | 每个按钮可拖拽入画布格子；点击 IF 或 ROLL 时**弹出数量选择器**（2~4，默认 2） |
| **原子面板** | 展示当前可用的 67 原子列表（A-K 分组），样式与现有 C 区 `studio-zone-c` 一致 | 支持搜索/折叠/拖拽 |
| **居中** | 将已布置到画布上的所有段格保持相对坐标关系，整体平移至画布中心 | 不改变格间相对位置 |
| **清除** | 清除整个六段式面板所有内容 | **必须弹窗确认**后才执行 |
| **擦掉** | 进入「擦除模式」：选中后点击/拖拽画布上已有内容的坐标格，消除该格内容 | 再次点击或按 Esc 退出擦除模式 |
| **保存** | 将当前六段式编排逻辑写回该词条（调用 `writeLanesToDraft()` + `saveCurrent()`） | 同现有 D 区「✎ 保存 / 编辑」按钮 |
| **跳转按钮**（字段型） | 当在段格内编辑原子、遇到「射程 / 方向 / AOE形状 / 地图炮」等**依赖画布的字段**时，该字段在表单内**不以输入框呈现，而是渲染为一个跳转按钮** | 点击跳转按钮 → 切换到对应画布视图（射程 / AOE / 地图炮），并**带提示气泡**说明「正在为〔原子名·字段〕设置，请在画布上绘制后点『保存并返回』」；六段式与画布**同一区域互斥显示**，故用跳转而非同屏并排 |

#### 段式按钮颜色方案
```
IF   → 青色 #00d4aa（条件门槛）
WHEN → 琥珀 #ffb000（触发锚点）
WHO  → 蓝色 #5b9bff（作用对象）
ROLL → 紫色 #a855f7（掷骰判定）
DO   → 绿色 #22c55e（效果执行）
COST → 红色 #ef4444（代价消耗）
```

#### IF / ROLL 数量选择
- 点击 IF 或 ROLL 段式按钮时，弹出微型选择器：
  - 「生成几个 IF 段？」→ 可选 2 / 3 / 4（默认 2）
  - 「生成几个 ROLL 段？」→ 可选 2 / 3 / 4（默认 2）
- 选择数量 N 后，自动在画布相邻空位放置 N 个同色段格（见 4.2.3 插入规则）

### 4.2 界面显示规则

#### 4.2.1 起始点（WHEN 锚点）
- 六段式画布初始化时，在坐标 **(5, 10)** 设置一个起始 **WHEN** 框（灰色底 + 琥珀边框 + "WHEN" 字样）
- 此格是整个词条的**根节点**，不可被擦除（除非点「清除」整板）
- WHEN 格子固定在 (5,10) 作为视觉锚点，但镜头可平移/缩放

#### 4.2.2 高亮引导（距离-2 环带）
- 当某个段格（如图中的 **a**）已经插入了原子（即 `lane.atoms.length > 0`），系统**高亮显示其周围距离 ≤ 2 的所有空格**
- 高亮分两层（参考截图）：
  - **内环 b1~b6**（距离 = 1）：绿色半透明填充（`rgba(0,212,170,0.25)`），表示「推荐下一格」
  - **外环 c1~c6**（距离 = 2）：橙色半透明填充（`rgba(255,176,0,0.20)`），表示「可选扩展格」
- **已有内容的格不高亮**（避免重叠混淆）
- 高亮实时跟随当前焦点格变化

```
        b6    c1    b1
      c6  [a]    c2
        b5       b2
      c5       c3
        b4    c4   b3
```
- `[a]` = 当前已填原子的段格（操作点）
- `b1~b6` = 距离 1 的 6 个邻居（绿色高亮）
- `c1~c6` = 距离 2 的 12 个候选格（橙色高亮，截图中只标了 c1~c6 共 6 个可见格）

#### 4.2.3 插入规则（IF/ROLL 多实例）
- 当插入的新段是 **IF** 或 **ROLL** 时：
  1. 先根据用户选择的数量 N（2~4）
  2. 从当前操作点 **a** 的**最近空邻居**开始，沿顺时针方向找 N 个连续空格（优先 b 环，不够再取 c 环）
  3. 在这 N 个格中各自放入一个 **IF**（或 **ROLL**）段格，颜色与段式按钮一致
  4. 多个 IF/ROLL 实例之间**隐式串联**（引擎按列序从左到右、从上到下执行）

### 4.3 基础操作流程

```
步骤 1: 选择一个段式按钮（IF/WHEN/WHO/ROLL/DO/COST）
         ↓ 若选 IF/ROLL → 弹出数量选择器(2~4)
步骤 2: 将段头拖入高亮提示的某一空格中（b环优先，其次c环）
         ↓ 若是 IF/ROLL 且数量>1 → 自动向相邻空格补全剩余实例
步骤 3: 对已落入的段格，从原子面板拖入所需原子（或双击打开格内编辑）
步骤 4: 双击/单击已放入的原子卡片，编辑其参数（fields 表单）
步骤 5: 重复 步骤1~4，直到词条逻辑完整
步骤 6: 点击「保存」，将编排结果写回词条
```

#### 操作细节
1. **选择段添加到高亮格**：拖拽段式按钮 → 落入高亮区域内的任一空格 → 该格变为对应颜色的段框
2. **对空白格添加原子**：从右侧原子面板拖拽原子 chip → 落入目标段格内 → 该格显示原子卡片缩略图
3. **编辑已添加的原子**：单击段格内的原子卡片 → 右侧/D区展开该原子的 fields 编辑表单（复用现有 `atom-card.ac-params` 样式）
4. **循环迭代**：每次添加新段/新原子后，高亮引导自动更新（以最新操作的格为新锚点 a）
5. **保存**：确认无误后点「保存」→ 弹出确认（若 dirty）→ 写回 `draft.tree` + 扁平投影

### 4.4 与现有代码的差异对照

| 项目 | 现有实现 (`HexCanvasEditor.vue` drawHexSix) | 新设计要求 |
|---|---|---|
| 段格位置 | 固定 6 格环绕 UNIT_ORIGIN（星形） | 自由布置在 20×20 网格任意位置，仅起始 WHEN 锚定在 (5,10) |
| 段数 | 固定 6 个（HEXSIX_COORDS） | 动态：IF/ROLL 可多实例（2~4），总格数不限 |
| 交互 | 点击格 → select-lane → D 区高亮 | 拖拽段头入格 + 高亮引导(b/c环) + 原子拖入 + 擦除模式 |
| 颜色 | 统一琥珀 `#ffd479` | 6 段各一色（IF青/WHEN琥珀/WHO蓝/ROLL紫/DO绿/COST红） |
| 工具栏 | 无（仅有 hexsix-hint 文字提示） | 完整按钮栏：段式按钮×6 + 原子面板 + 居中/清除/擦掉/保存 |
| 列序发动 | 无（纯展示） | 以列序(x坐标优先)作为发动顺序依据 |

### 4.5 画布区「保存并返回」按钮（与六段式互斥切换）

> 背景：六段式画布与射程/AOE/地图炮画布**共用同一显示区域**，无法同屏并排。原子内的「射程/方向/AOE形状/地图炮」字段改用**跳转按钮**进入对应画布（见 4.1）。进入画布后必须有办法返回并回填六段式。

| 按钮 | 行为 | 备注 |
|---|---|---|
| **保存并返回** | 在射程 / AOE / 地图炮画布中，完成绘制后点击，**将画布结果回写**到跳转来源的那个原子字段（如 `range` 环带 → 原子 `range`；`mcShapes` → 原子 `direction`/地图炮段），然后**切回六段式视图**，定位并高亮回来源段格 | 与 G 决策「射程真相源回写」打通：画布改 min/max/形状 → 写回 `draft.target` / 原子字段 |
| **取消返回** | 放弃本次画布绘制，直接切回六段式视图，来源原子字段保持跳转前的值 | 可做成「保存并返回」旁的次要按钮，或点 Esc 等同取消 |

#### 跳转 ↔ 返回 链路状态
- **来源标记**：从六段式跳转时，记录 `{ 段格坐标, 原子 id, 字段名 }`，作为画布「保存并返回」的回填目标（避免多原子多字段混淆）。
- **提示气泡**：进入画布时顶部显示提示「正在为〔原子名·字段〕设置 —— 绘制后点『保存并返回』」，离开画布后消失。
- **脏检查**：画布有改动未保存时，直接点「取消返回」应弹窗确认「放弃本次绘制？」，与六段式「清除」确认同风格。

#### 与现有代码差异
| 项 | 现有实现 | 新设计要求 |
|---|---|---|
| 画布↔六段式切换 | 五视图靠顶部 tab 切换整视图（`viewMode` prop），六段式与射程**各自独立页** | 增加「段内字段 → 跳转进入某画布 → 保存返回回填」的**子级跳转**，而非整页切换 |
| 画布保存 | 射程/AOE/地图炮各自独立保存（部分仍单向） | 画布「保存并返回」除写画布态外，**必须回填来源原子字段**并切回六段式 |
| 字段展示 | 原子表单里 `range`/`direction` 为普通输入/下拉 | 改为**跳转按钮 + 提示气泡**，进入画布绘制 |

## 五、待讨论清单（仍未决）

1. **WHEN 锚点是否可被覆盖/移动**：✅ **2026-08-11 拍板**：起始第一个 WHEN **不可擦除、不可移动**（绝对固定在锚点）。其余段可拖拽编排。
2. **AOE 与射程目标点联动**：✅ **2026-08-11 拍板**：AOE 的「目标点」**复用射程环带里玩家点的某格**（不单独点选）；**目标点选取受 `max_range` 约束（必须在射程内），但画出/生效的攻击范围可超过射程**（如射程 3，目标点在 3 内，但攻击范围可覆盖距离 ≥3 的格子，这些格子上的单位同样视为有效命中）。即"选取点受射程限、作用范围可超射程"。
3. **地图炮阴影旋转映射方向**：✅ **2026-08-11 拍板**：默认绘制阴影**向正右（dir=0）**，之后**顺时针**以 60° 为步长旋转补全六向（`mcShapes[dir] = rotate(baseShadowEast, dir*60°)`，dir=0 即正右阴影，顺时针 1=右下/2=下/3=左下/4=左上/5=上/6=右上）。无需方向键，与 `DIRECTIONS`/`computeDirection` 顺时针扇区对齐。
4. **战场模拟结算接口**：✅ **2026-08-11 拍板契约**（详见「H. 战场模拟结算接口契约」一节，已按三文件实证修正函数名）：四步法——① 复用 `hexMath.getHexesInRange`（射程判定）+ **新增** `computeAOECells(casterCoord,targetCoord,aoeShape)`（作用范围可超射程）导出 `affectedCells` 并筛 `targetUnitIds`；② 构造 `simulatedCtx`（深拷贝 `equipState`/`stats`，隔离红线严禁改原 `battleState`）；③ 调 `skillExecutor._walkPhaseTree(**pure 开关**)` + 对每个受击单位走 `DamagePipe.calculate(config)`（**非 calculateDamage**）真实结算得 `finalDamage`；④ 组装 `SimulateSkillImpactOutput` 返回，前端 `HexCanvasEditor` 高亮 `affectedCells` 并弹框显示 `finalDamage`。**落地依赖**：`hexMath` 新增 `computeAOECells`；`_walkPhaseTree` 新增 `pure` 开关；`damagePipe` 用 `calculate(config)`。
5. **落库契约**（暂缓，F）：功能定稿后设计 `rangeCtx → draft` 字段映射；涉及 `target`/`range`/`aoe`/`mapcannon` 段。
6. **射程回写字段**（G 已确认需要，待落库定稿）：回写目标 `draft.target.min_range/max_range` 或 `draft.range`，与 #5 一并落地。
7. **原子 direction / range 字段是否消费 + 跳转画布回填**：当前 A1/A2/I3 的 `direction`、A2/E1/I1/I2/G4 的 `range` 后端均未消费（射程走 `cast_range` 真相源）。若要在六段式内用跳转按钮进画布设值（见 4.5），需定义：① 前端跳转回填机制；② 后端是否把原子级 `range`/`direction` 接入结算（`getSkillRangeFields` 认原子 range、`skillExecutor` 加方向约束）。
8. **暴击字段消费问题**：✅ **2026-08-11 拍板方向**：权威 67 原子清单**不含独立的「暴击」子段**，`critBonus`(暴击加成) 经确认是无效字段、已不再保留；`critThreshold`(阈值) 后端已生效（1d6≥阈值），`critMult`(倍率) 后端用全局随机区间 `critMin~critMax` 不读固定值。若需在六段式内表达暴击，应作为 DO 段新增原子或并入 `D1 伤害加减值` 的 `multiplier` 模式（**方向已定：走 DO 段，不新增独立暴击段**）；是否改读词条固定值待定（非阻塞）。
9. **原子字段命名统一 + 冗余清理**：权威表已按 WHEN/IF/ROLL/DO/COST/META 六段统一编号，旧 `A1~A5 攻击/暴击` 类命名已废弃；`E1 HP回复` 的 `durability` 问题在权威表中改为 `E2 装备耐久回复`/`E3 耐久扣减`（独立原子），与 COST 段需明确主从；H1/H2 的 `cast_range`/射程类字段以「原点+射程/延伸长度」表达，与词条 `cast_range` 真相源的关系需去重。
10. **身份类下拉枚举统一**：✅ **2026-08-11 拍板**：**不替换**。B4 维持五类身份、B1/B2 维持旧两元（`self/target`）现状，二者的语义差异按既有设计保留（`target` 为具体单位锚点，正交维度），不再强行统一。
11. **WHO 段 = owner + target 双字段**：✅ **2026-08-11 拍板**（详见正文「WHO=owner+target」相关章节）：WHO 段拆为 `owner`（默认 self，可覆盖：装备持有者/反射来源等）+ `target`（复合：`{scope: 身份五元组, granularity: 单体/群体, range_filter?: 射程过滤}`）。WHO 在树内只"记录规则"，真正选靶由外层调度器消费规则执行；`target.scope` 适合用五类身份枚举，但 `granularity` 维度不可省（否则群体语义引擎无法捞取）。
12. **六段顺序：带约束自由调序落地**（2026-08-11 拍板方向）：引擎侧仍保留 `ORDER = [WHEN,IF,WHO,ROLL,DO,COST]` 作为默认序，**不做运行时自动重排**；约束由**编辑器侧强制**而非引擎重排——当用户试图在某个段后插入/放置一个"违反硬边的段"时，**该段按钮直接灰掉（禁止插入）**，而不是事后重排或弹窗。硬边：`WHEN→其余`、`IF→DO`、`WHO→DO/ROLL`、`ROLL→消费roll的IF`。`whenCtx` 惰性化仍建议做（消除构造期依赖），但重排逻辑在编辑器拦截，不交给 `normalizePhaseOrder`。
13. **子六段承载规范落地**：✅ **2026-08-11 拍板方向**（详见正文「子六段承载规范」一节）：原子 `tree` 字段递归子六段（≤3 层），编辑器强制"可嵌套原子白名单"（ROLL/IF/DO 可挂；WHO/WHEN 受限；COST/META 不建议）；层间靠 `bind_to` 桥接。待定子层专属原子 `read_bound`/`dynamic_target`（非阻塞，可后续）。

## 六、已落地代码改动（本次累计）
- `HexCanvasEditor.vue`：`UNIT_ORIGIN` 常量；`HEXSIX_LABELS` WHEN 移第 2 列；射程/地图炮/六段式/拖拽 全部以 `UNIT_ORIGIN` 为锚；`completeSixDirs` 空源 `mcTip` 提示。
- 上次已修：镜头重置 bug（rangeCtx 变化只重绘不重置镜头）。

## 七、待实现（依据本文件）

### 高优先级（交互规格已明确）
1. **六段式画布重构**：
   - 从固定 6 格星形 → 自由网格（20×20），起始 WHEN 锚定 (5,10)
   - 新增按钮栏：6 色段式按钮（IF/ROLL 带数量选择器 2~4）、原子面板、居中、清除(弹窗确认)、擦掉(模式切换)、保存
   - 高亮引导系统：距离≤2 环带（b环绿色/c环橙色），已有内容格不高亮
   - IF/ROLL 多实例自动补全（顺时针找相邻空格）
   - 拖拽系统：段头入格 + 原子入格 + 擦除模式
   - 列序发动顺序计算（x优先 → y次序）
2. **六段式↔画布 字段跳转 + 保存返回回填**：
   - 原子表单中「射程/方向/AOE形状/地图炮」字段改为**跳转按钮**（非输入框），点击进入对应画布并显示提示气泡（见 4.1 / 4.5）
   - 跳转时记录来源 `{段格坐标, 原子id, 字段名}` 作为回填目标
   - 画布区新增「保存并返回」+「取消返回」按钮：绘制结果回写来源原子字段（`range`环带→原子`range`；`mcShapes`→原子`direction`/地图炮段）并切回六段式高亮来源格
   - 脏检查：画布有改动未保存时取消返回需弹窗确认
   - 与 G 决策射程真相源回写打通（画布→`draft.target`/原子字段）

### 中优先级（逻辑已拍板，接口待定）
2. **射程双向回写**：画板改 min/max/shape → 写回 `draft.target`
3. **AOE 手绘形状 + 目标点原点**：复用地图炮绘制逻辑，原点改为目标点
4. **地图炮纯六向补全**：去掉方向键依赖，改为「画阴影→60°旋转×6→方向映射」

### 低优先级（暂缓 / 依赖其他）
5. **战场模拟真实结算 + 布子(HP100/MOB14)**：需先定结算函数签名（#4 待讨论）
6. **落库契约**：等全部功能定稿后设计（#5 待讨论，F 决策暂缓）

## 八、67 原子字段明细表（可编辑内容 + 表单形式）

> 来源：权威 67 原子清单 `/Users/dingxuyang/Desktop/alpha3.0 系统/原子清单-可编辑版-2026-08-09.md`（六段式 WHEN/IF/ROLL/DO/COST/META，A~K 组号按该清单，共 67 原子）。
> 表单形式图例：**[输入]** = 文本框/数字框；**[下拉]** = 选项选择；**[勾选]** = 布尔开关；`*` = 必填。
> 颜色：attack 绿 / heal 蓝 / buff 橙 / debuff 紫 / passive 灰 / 装备相关 青。

| # | 分组 | 原子编号 | 字段 | 字段翻译·编号 | 形式 | 描述（参考 alpha3.0 权威 67 原子清单） |
|---|---|---|---|---|---|---|
| 1 | WHEN | A1 受击时 | source | F1 来源 (×3) | (*2)[下拉]来袭类型(any 任意/melee 近战/ranged 远程) | 触发来源的攻击类型 |
| 2 | WHEN | A2 造成伤害后 | source | F1 来源 (×3) | (*2)[下拉]伤害来源(self 自身) | 造成该伤害的单位 |
| 3 | WHEN | A2 造成伤害后 | min_damage | F2 最低伤害 (×1) | (*1)[输入]数值 最低伤害门槛=0 | 低于此值不触发 |
| 4 | WHEN | A3 近战伤害结算后 | attack_stat | F3 攻击属性 (×1) | (*2)[下拉]攻击属性(melee 近战) | 判定的攻击属性 |
| 5 | WHEN | A4 击杀时 | source | F1 来源 (×3) | (*2)[下拉]击杀者(self 自身) | 完成击杀的单位 |
| 6 | WHEN | A6 目标选定时 | gate | F4 否决开关 (×1) | (*3)[勾选]是否可否决选定=false | 允许否决本次目标选定 |
| 7 | WHEN | A7 回合开始 | faction | F5 阵营 (×2) | (*1)[输入]文本 所属阵营(self 自身) | 生效阵营 |
| 8 | WHEN | A8 轮次开始 | from_round | F6 起始轮次 (×2) | (*1)[输入]数值 自第几轮起=1 | 起始轮次 |
| 9 | WHEN | A9 友军受击时 | radius | F7 半径 (×4) | (*1)[输入]数值 感知半径·格=99 | 感知半径 |
| 10 | WHEN | A11 获得空投时 | from_round | F6 起始轮次 (×2) | (*1)[输入]数值 自第几轮起=2 | 起始轮次 |
| 11 | WHEN | A12 踩点时 | point_type | F8 点类型 (×1) | (*1)[输入]文本 标记点类型(recon 侦察点) | 触发点类型 |
| 12 | WHEN | 手动释放 | phase | F9 相位 (×1) | (*2)[下拉]可释放相位(any 任意/turn_start 回合开始/turn_end 回合结束) | 主动释放允许相位 |
| 13 | IF | B1 射程内 | from | F10 起点方 (×3) | (*2)[下拉]起点(self 自身) | 起点方 |
| 14 | IF | B1 射程内 | to | F11 终点方 (×4) | (*2)[下拉]终点(target 目标) | 终点方 |
| 15 | IF | B1 射程内 | min_range | F12 最小射程 (×1) | (*1)[输入]数值 最小射程·格=1 | 最小射程 |
| 16 | IF | B1 射程内 | max_range | F13 最大射程 (×1) | (*1)[输入]数值 最大射程·格=3 | 最大射程 |
| 17 | IF | B2 互在射程 | a | F14 对比方A (×3) | (*2)[下拉]对比方A(self 自身) | 对比方 A |
| 18 | IF | B2 互在射程 | b | F15 对比方B (×3) | (*2)[下拉]对比方B(target 目标) | 对比方 B |
| 19 | IF | B3 HP阈值 | subject | F16 判定对象 (×1) | (*2)[下拉]判定对象(target 目标) | 判定对象 |
| 20 | IF | B3 HP阈值 | op | F17 比较运算符 (×3) | (*2)[下拉]比较符(< / <= / > / >= / ==) | 比较运算符 |
| 21 | IF | B3 HP阈值 | value | F18 数值 (×6) | (*1)[输入]数值 阈值=5 | 阈值 |
| 22 | IF | B3 HP阈值 | is_percent | F19 按百分比 (×1) | (*3)[勾选]按百分比=false | 是否按百分比判定 |
| 23 | IF | B4 属性对比 | a | F14 对比方A (×3) | (*2)[下拉]对比方A(self 自身/ally 友方/ally_group 友方群体/enemy 敌方/enemy_group 敌方群体) | 对比方 A |
| 24 | IF | B4 属性对比 | b | F15 对比方B (×3) | (*2)[下拉]对比方B(self 自身/ally 友方/ally_group 友方群体/enemy 敌方/enemy_group 敌方群体) | 对比方 B |
| 25 | IF | B4 属性对比 | stat | F20 属性 (×1) | (*2)[下拉]对比属性(melee 近战值/shooting 射击值/mobility 机动值) | 对比属性 |
| 26 | IF | B4 属性对比 | op | F17 比较运算符 (×3) | (*2)[下拉]比较符(> 大于) | 比较运算符 |
| 27 | IF | B5 共线并列 | faction | F5 阵营 (×2) | (*2)[下拉]阵营要求(same 同阵营) | 阵营要求 |
| 28 | IF | B5 共线并列 | count | F21 数量 (×4) | (*1)[输入]数值 并列单位数=3 | 并列单位数 |
| 29 | IF | B5 共线并列 | distance | F22 距离 (×2) | (*1)[输入]数值 间隔·格=1 | 间隔格数 |
| 30 | IF | B6 地形匹配 | terrain | F23 地形 (×2) | (*2)[下拉]地形(plain 平原/forest 森林/mountain 山地/water 水域/ruin 废墟/crystal 晶体/moon 月面) | 匹配地形 |
| 31 | IF | B7 伤害种类匹配 | kind | F24 种类 (×2) | (*2)[下拉]伤害种类(kinetic 实体系/beam 光束系) | 伤害种类 |
| 32 | IF | B8 未攻击过 | action | F25 动作 (×3) | (*2)[下拉]未执行动作(deal_damage 造成伤害/move 移动/attack 攻击) | 未执行的动作 |
| 33 | IF | B9 不在扫描范围 | radius | F7 半径 (×4) | (*1)[输入]数值 敌方扫描半径·格=2 | 扫描半径 |
| 34 | IF | B10 限次 | limit_scope | F26 作用域 (×1) | (*2)[下拉]限次作用域(match 整局/faction 阵营/personal 个人/per_turn 每回合) | 作用域 |
| 35 | IF | B10 限次 | limit_count | F27 次数上限 (×1) | (*1)[输入]数值 可触发次数=1 | 可触发次数 |
| 36 | ROLL | C2 单数值阈值二分 | method | F28 生成方式 (×2) | (*2)[下拉]生成方式(dice 骰子/spinner 转盘/card 抽卡/rps 猜拳) | 生成方式 |
| 37 | ROLL | C2 单数值阈值二分 | sides | F29 骰面数 (×2) | (*1)[输入]数值 骰面数=6 | 骰面数 |
| 38 | ROLL | C2 单数值阈值二分 | threshold | F30 阈值 (×2) | (*1)[输入]数值 成功阈值=4 | 成功阈值 |
| 39 | ROLL | C3 单数值多段 | method | F28 生成方式 (×2) | (*2)[下拉]生成方式(dice 骰子) | 生成方式 |
| 40 | ROLL | C3 单数值多段 | sides | F29 骰面数 (×2) | (*1)[输入]数值 骰面数=6 | 骰面数 |
| 41 | ROLL | C3 单数值多段 | segment_count | F31 分段数 (×1) | (*1)[输入]数值 分段数量=3 | 分段数量 |
| 42 | ROLL | C4 对抗数值 | a | F14 对比方A (×3) | (*2)[下拉]对抗方A(self 自身) | 对抗方 A |
| 43 | ROLL | C4 对抗数值 | b | F15 对比方B (×3) | (*2)[下拉]对抗方B(target 目标) | 对抗方 B |
| 44 | ROLL | C4 对抗数值 | compare | F32 比较方式 (×1) | (*2)[下拉]胜负比较符(> 大者胜/>= 大于等于胜) | 胜负判定 |
| 45 | ROLL | C4 对抗数值 | tie | F33 平局处理 (×1) | (*2)[下拉]平局处理(mutual_destruction 同归于尽/none 无事发生) | 平局处理 |
| 46 | ROLL | C5 数值参与运算 | bind_to | F34 绑定字段 (×1) | (*1)[输入]文本 结果绑定字段(flat_value 伤害数值) | 绑定字段 |
| 47 | ROLL | C5 数值参与运算 | base | F35 基数 (×1) | (*1)[输入]数值 基数=0 | 基数 |
| 48 | ROLL | C5 数值参与运算 | coefficient | F36 系数 (×2) | (*1)[输入]数值 系数=1 | 系数 |
| 49 | ROLL | C6 数值分色 | palette | F37 色板 (×1) | (*1)[输入]文本 色板(red 红,black 黑) | 色板 |
| 50 | ROLL | C6 数值分色 | mapping | F38 映射表 (×1) | (*1)[输入]文本 点数→颜色映射(1-3:red;4-6:black) | 映射表 |
| 51 | ROLL | C7 数值 vs 动态值 | op | F17 比较运算符 (×3) | (*2)[下拉]比较符(>= 大于等于) | 比较运算符 |
| 52 | ROLL | C7 数值 vs 动态值 | compare_to | F39 比较对象 (×1) | (*1)[输入]文本 动态比较对象(target.hp 目标剩余生命) | 比较对象 |
| 53 | ROLL | 手动掷骰 | prompt | F40 提示文案 (×2) | (*1)[输入]文本 提示文案(请掷骰) | 提示文案 |
| 54 | DO | D1 伤害加减值 | damage_mode | F41 伤害模式 (×1) | (*2)[下拉]伤害模式(flat 固定值/override 覆盖/multiplier 倍率) | 伤害模式 |
| 55 | DO | D1 伤害加减值 | flat_value | F42 固定值 (×1) | (*1)[输入]数值 伤害数值·负为减伤=10 | 伤害数值 |
| 56 | DO | D1 伤害加减值 | fixed_rate_multiplier | F43 固定倍率 (×1) | (*1)[输入]数值 伤害倍率=1 | 伤害倍率 |
| 57 | DO | D1 伤害加减值 | armor_pen | F44 护甲穿透 (×1) | (*1)[输入]数值 护甲穿透=0 | 护甲穿透 |
| 58 | DO | D2 按 kind 减免 | kind | F24 种类 (×2) | (*2)[下拉]伤害种类(kinetic 实体系/beam 光束系) | 伤害种类 |
| 59 | DO | D2 按 kind 减免 | flat_reduction | F45 固定减免 (×1) | (*1)[输入]数值 固定减免值=3 | 固定减免值 |
| 60 | DO | D2 按 kind 减免 | rate | F46 比率 (×1) | (*1)[输入]数值 减免系数=1 | 减免系数 |
| 61 | DO | D3 伤害均摊 | total_damage | F47 总伤害 (×1) | (*1)[输入]数值 待均摊总伤害=10 | 待均摊总伤害 |
| 62 | DO | D3 伤害均摊 | split | F48 均摊方式 (×1) | (*2)[下拉]均摊方式(equally 平均均摊/weighted 加权均摊) | 均摊方式 |
| 63 | DO | D3 伤害均摊 | target_scope | F49 作用范围 (×1) | (*2)[下拉]作用范围(area 区域) | 作用范围 |
| 64 | DO | D3 伤害均摊 | min_per_target | F50 每目标最小值 (×1) | (*1)[输入]数值 每目标最低伤害=0 | 每目标最低伤害 |
| 65 | DO | D4 伤害分担转移 | from | F10 起点方 (×3) | (*2)[下拉]转出方(target 目标) | 转出方 |
| 66 | DO | D4 伤害分担转移 | to | F11 终点方 (×4) | (*2)[下拉]承接方(ally 友方) | 承接方 |
| 67 | DO | D4 伤害分担转移 | ratio | F51 比例 (×1) | (*1)[输入]数值 转移比例=0.8 | 转移比例 |
| 68 | DO | D5 即死 | condition_hp | F52 生命门槛 (×1) | (*1)[输入]数值 触发生命门槛=5 | 生命门槛 |
| 69 | DO | D6 同归于尽 | targets | F53 目标对象 (×1) | (*1)[输入]文本 同归对象(self 自身,enemy 敌方) | 同归对象 |
| 70 | DO | D7 延迟易伤标记 | target_stat | F54 目标属性 (×3) | (*1)[输入]文本 修改属性(DMG_TAKEN 受到伤害) | 修改属性 |
| 71 | DO | D7 延迟易伤标记 | value | F18 数值 (×6) | (*1)[输入]数值 增减值=5 | 增减值 |
| 72 | DO | D7 延迟易伤标记 | delay | F55 延迟 (×1) | (*1)[输入]数值 延迟回合数=1 | 延迟回合 |
| 73 | DO | D7 延迟易伤标记 | duration | F56 持续时长 (×7) | (*1)[输入]数值 持续回合数=1 | 持续回合 |
| 74 | DO | E1 HP 回复 | resource | F57 资源 (×2) | (*2)[下拉]回复资源(hp 生命值/energy 能量/ap 行动点/shield 护盾) | 回复资源 |
| 75 | DO | E1 HP 回复 | amount | F58 数量 (×2) | (*1)[输入]数值 回复量=8 | 回复量 |
| 76 | DO | E1 HP 回复 | by | F59 计算方式 (×2) | (*2)[下拉]计算方式(flat 固定值/stat_coeff 属性系数/terrain 地形系数) | 计算方式 |
| 77 | DO | E1 HP 回复 | coefficient | F36 系数 (×2) | (*1)[输入]数值 系数=1 | 系数 |
| 78 | DO | E2 装备耐久回复 | resource | F57 资源 (×2) | (*1)[输入]文本 回复资源(equipment_durability 装备耐久) | 回复资源 |
| 79 | DO | E2 装备耐久回复 | amount | F58 数量 (×2) | (*1)[输入]数值 回复量=1 | 回复量 |
| 80 | DO | E3 耐久扣减 | value | F18 数值 (×6) | (*1)[输入]数值 扣减值=1 | 扣减值 |
| 81 | DO | E3 耐久扣减 | slot | F60 槽位 (×2) | (*2)[下拉]装备槽位(weapon 武器/armor 防具) | 装备槽位 |
| 82 | DO | F1 属性增减益 | target_stat | F54 目标属性 (×3) | (*2)[下拉]修改属性(melee 近战值/shooting 射击值/mobility 机动值/defense 防御值) | 修改属性 |
| 83 | DO | F1 属性增减益 | value | F18 数值 (×6) | (*1)[输入]数值 增减值=2 | 增减值 |
| 84 | DO | F1 属性增减益 | duration | F56 持续时长 (×7) | (*1)[输入]数值 持续回合数=2 | 持续回合 |
| 85 | DO | F2 机动差项修正 | target_stat | F54 目标属性 (×3) | (*1)[输入]文本 修改属性(MOBILITY 机动值) | 修改属性 |
| 86 | DO | F2 机动差项修正 | value | F18 数值 (×6) | (*1)[输入]数值 增减值=-2 | 增减值 |
| 87 | DO | F2 机动差项修正 | stacks | F61 层数 (×1) | (*1)[输入]数值 叠加层数=1 | 叠加层数 |
| 88 | DO | F3 跳过机动判定 | scope | F62 范围 (×2) | (*2)[下拉]生效范围(this_attack 本次攻击/this_turn 本回合) | 生效范围 |
| 89 | DO | F4 充能层 | charge_layers | F63 充能层数 (×1) | (*1)[输入]数值 充能总层数=3 | 充能总层数 |
| 90 | DO | F4 充能层 | consume_per_use | F64 每次消耗 (×1) | (*1)[输入]数值 每次消耗层数=1 | 每次消耗层数 |
| 91 | DO | F4 充能层 | on_event | F65 触发事件 (×1) | (*1)[输入]文本 消耗触发事件(on_damage_dealt 造成伤害后) | 触发事件 |
| 92 | DO | F5 互斥组 | mutex_group | F66 互斥组 (×1) | (*1)[输入]文本 互斥组名(g1) | 互斥组名 |
| 93 | DO | F5 互斥组 | priority | F67 优先级 (×1) | (*1)[输入]数值 优先级=0 | 优先级 |
| 94 | DO | F6 永久失效 | target_entry | F68 目标词条 (×1) | (*1)[输入]文本 目标词条ID | 目标词条 ID |
| 95 | DO | F6 永久失效 | scope | F62 范围 (×2) | (*2)[下拉]失效范围(self 自身/entry 指定词条) | 失效范围 |
| 96 | DO | 护盾 | value | F18 数值 (×6) | (*1)[输入]数值 护盾值=5 | 护盾值 |
| 97 | DO | 护盾 | duration | F56 持续时长 (×7) | (*1)[输入]数值 持续回合数=2 | 持续回合 |
| 98 | DO | G1 额外回合 | count | F21 数量 (×4) | (*1)[输入]数值 额外回合数=1 | 额外回合数 |
| 99 | DO | G1 额外回合 | no_chain | F69 禁止连锁 (×2) | (*3)[勾选]禁止连锁触发=true | 禁止连锁 |
| 100 | COST | G2 放弃移动 | requires_unmoved | F70 需未移动 (×1) | (*3)[勾选]需本回合未移动=true | 需本回合未移动 |
| 101 | COST | G3 预支行动 | action | F25 动作 (×3) | (*2)[下拉]预支的行动(attack 攻击/move 移动) | 预支行动 |
| 102 | COST | G3 预支行动 | debt_turns | F71 欠账回合 (×1) | (*1)[输入]数值 欠账回合数=1 | 欠账回合 |
| 103 | COST | G4 预支回合 | skip_next_turn | F72 跳过下回合 (×1) | (*3)[勾选]跳过下个回合=true | 跳过下回合 |
| 104 | DO | G5 抢占顶替 | from | F10 起点方 (×3) | (*2)[下拉]被抢占方(target 目标) | 被抢占方 |
| 105 | DO | G5 抢占顶替 | mode | F73 模式 (×3) | (*2)[下拉]抢占模式(takeover 顶替行动/first_strike 先制攻击) | 抢占模式 |
| 106 | DO | G6 行动序改写 | unit | F74 单位 (×1) | (*2)[下拉]被改写单位(self 自身) | 被改写单位 |
| 107 | DO | G6 行动序改写 | position | F75 排位 (×1) | (*1)[输入]数值 新排位序号=0 | 新排位序号 |
| 108 | DO | G7 授予/剥夺行动 | mode | F73 模式 (×3) | (*2)[下拉]操作模式(grant 授予/revoke 剥夺) | 操作模式 |
| 109 | DO | G7 授予/剥夺行动 | action | F25 动作 (×3) | (*2)[下拉]行动类型(move 移动/attack 攻击/both 二者皆是) | 行动类型 |
| 110 | COST | G8 防连锁 | no_chain | F69 禁止连锁 (×2) | (*3)[勾选]禁止连锁触发=true | 禁止连锁 |
| 111 | COST | G8 防连锁 | cooldown | F76 冷却 (×1) | (*1)[输入]数值 冷却回合数=1 | 冷却回合 |
| 112 | DO | 阻止移动 | duration | F56 持续时长 (×7) | (*1)[输入]数值 持续回合数=1 | 持续回合 |
| 113 | WHO | H1 地图炮 | origin | F77 原点 (×2) | (*1)[输入]文本 原点(self 发动者自身·固定不可改) | 原点（固定） |
| 114 | WHO | H1 地图炮 | direction | F78 方向 (×2) | (*2)[下拉]发动方向(0~5 六向之一/all 六向齐发) | 发动方向 |
| 115 | WHO | H1 地图炮 | shape | F79 形状 (×2) | (*2)[下拉]覆盖形状(line 直线/sector 扇形/cone 锥形/custom 自定义手绘) | 覆盖形状 |
| 116 | WHO | H1 地图炮 | length | F80 长度 (×1) | (*1)[输入]数值 延伸长度·格=3 | 延伸长度 |
| 117 | WHO | H1 地图炮 | width | F81 宽度 (×1) | (*1)[输入]数值 展开宽度·格=1 | 展开宽度 |
| 118 | WHO | H1 地图炮 | mcShapes | F82 手绘格集 (×1) | (*4)[输入·json] 六向手绘格集合 | 六向手绘格 |
| 119 | WHO | H1 地图炮 | friendly_fire | F83 友军伤害 (×2) | (*3)[勾选]是否波及友军=false | 是否波及友军 |
| 120 | WHO | H2 AOE | origin | F77 原点 (×2) | (*1)[输入]文本 原点(impact_point 射程内选定落点·固定不可改) | 原点（固定） |
| 121 | WHO | H2 AOE | cast_range | F84 施法射程 (×1) | (*1)[输入]数值 可选落点射程·格=3 | 落点射程 |
| 122 | WHO | H2 AOE | shape | F79 形状 (×2) | (*2)[下拉]覆盖形状(circle 圆形/ring 环形/line 直线/sector 扇形/cone 锥形/custom 自定义手绘) | 覆盖形状 |
| 123 | WHO | H2 AOE | spread | F85 扩散半径 (×1) | (*1)[输入]数值 覆盖半径/延伸长度·格=2 | 覆盖半径 |
| 124 | WHO | H2 AOE | direction | F78 方向 (×2) | (*2)[下拉]朝向(none 无朝向/0~5 六向之一) | 朝向 |
| 125 | WHO | H2 AOE | inner_radius | F86 内圈半径 (×1) | (*1)[输入]数值 内圈盲区·格=0 | 内圈盲区 |
| 126 | WHO | H2 AOE | friendly_fire | F83 友军伤害 (×2) | (*3)[勾选]是否波及友军=false | 是否波及友军 |
| 127 | DO | H3 强制位移 | mode | F73 模式 (×3) | (*2)[下拉]位移模式(push 推开/pull 拉近/teleport 传送) | 位移模式 |
| 128 | DO | H3 强制位移 | distance | F22 距离 (×2) | (*1)[输入]数值 位移距离·格=2 | 位移距离 |
| 129 | DO | H3 强制位移 | to | F11 终点方 (×4) | (*1)[输入]文本 指定落点坐标 | 指定落点 |
| 130 | DO | H4 共线阻挡 | count | F21 数量 (×4) | (*1)[输入]数值 构成阻挡所需单位数=3 | 阻挡单位数 |
| 131 | DO | H4 共线阻挡 | force_detour | F87 强制绕行 (×1) | (*3)[勾选]强制绕行=true | 强制绕行 |
| 132 | DO | H5 区域揭示 | radius | F7 半径 (×4) | (*1)[输入]数值 揭示半径·格=3 | 揭示半径 |
| 133 | DO | H5 区域揭示 | duration | F56 持续时长 (×7) | (*1)[输入]数值 持续回合数=1 | 持续回合 |
| 134 | DO | H6 生成地图实体 | entity | F88 实体 (×1) | (*2)[下拉]生成物类型(weapon 武器/armor 防具/item 道具) | 生成物类型 |
| 135 | DO | H6 生成地图实体 | count | F21 数量 (×4) | (*1)[输入]数值 生成数量=1 | 生成数量 |
| 136 | DO | H6 生成地图实体 | at | F89 位置 (×1) | (*1)[输入]文本 生成坐标(random 随机) | 生成坐标 |
| 137 | DO | I1 可见性 | state | F90 状态 (×1) | (*2)[下拉]可见状态(visible 可见/hidden 隐藏) | 可见状态 |
| 138 | DO | I1 可见性 | duration | F56 持续时长 (×7) | (*1)[输入]数值 持续回合数·0为永久=0 | 持续回合 |
| 139 | DO | I2 可选中性 | selectable | F91 可选中 (×1) | (*3)[勾选]是否可被选中=false | 是否可选 |
| 140 | DO | I2 可选中性 | expiry | F92 失效条件 (×1) | (*2)[下拉]失效条件(on_attack 攻击后失效/duration 按回合数失效) | 失效条件 |
| 141 | DO | I3 扫描范围 | radius | F7 半径 (×4) | (*1)[输入]数值 扫描半径·格=2 | 扫描半径 |
| 142 | DO | I3 扫描范围 | by | F59 计算方式 (×2) | (*2)[下拉]计算方式(flat 固定值/stat_coeff 属性系数) | 计算方式 |
| 143 | DO | 进入隐匿 | duration | F56 持续时长 (×7) | (*1)[输入]数值 持续回合数=2 | 持续回合 |
| 144 | DO | 退出隐匿 | trigger_on | F93 触发条件 (×1) | (*2)[下拉]退出触发条件(on_attack 攻击后/on_move 移动后/manual 手动/immediate 立即) | 退出触发条件 |
| 145 | DO | 隐匿攻击加成 | bonus | F94 加成 (×3) | (*1)[输入]数值 伤害加成值=3 | 伤害加成值 |
| 146 | DO | 隐匿闪避 | bonus | F94 加成 (×3) | (*1)[输入]数值 闪避加成值=2 | 闪避加成值 |
| 147 | DO | J1 装备抢夺转移 | item | F95 物品 (×1) | (*2)[下拉]抢夺物类型(weapon 武器/armor 防具) | 抢夺物类型 |
| 148 | DO | J1 装备抢夺转移 | to | F11 终点方 (×4) | (*2)[下拉]归属方(self 自身) | 归属方 |
| 149 | DO | J1 装备抢夺转移 | threshold | F30 阈值 (×2) | (*1)[输入]数值 抢夺成功阈值=4 | 成功阈值 |
| 150 | DO | 装备锁定 | slot | F60 槽位 (×2) | (*2)[下拉]锁定槽位(weapon 武器/armor 防具) | 锁定槽位 |
| 151 | COST/META | K1 词条改写词条 | unlock | F96 解锁 (×1) | (*1)[输入]文本 解锁的词条ID | 解锁词条 ID |
| 152 | COST/META | K1 词条改写词条 | prerequisite | F97 前置 (×1) | (*1)[输入]文本 前置词条ID | 前置词条 ID |
| 153 | COST/META | K1 词条改写词条 | remove_requirement | F98 解除约束 (×1) | (*1)[输入]文本 解除的前置约束 | 解除的前置约束 |
| 154 | COST | K2 多技能槽占用 | slots | F99 槽位数 (×1) | (*1)[输入]数值 占用槽位数=2 | 占用槽位数 |
| 155 | META | K3 玩家可选发动 | player_optional | F100 玩家可选 (×1) | (*3)[勾选]由玩家决定是否发动=true | 玩家可选 |
| 156 | META | K3 玩家可选发动 | prompt | F40 提示文案 (×2) | (*1)[输入]文本 询问提示文案 | 提示文案 |
| 157 | META | K4 打断响应窗口 | interrupt_window | F101 响应窗口 (×1) | (*1)[输入]数值 响应窗口回合数=1 | 响应窗口回合 |
| 158 | DO | 高地优势 | bonus | F94 加成 (×3) | (*1)[输入]数值 高地加成值=1 | 高地加成值 |
| 159 | DO | 地形种类修正 | terrain | F23 地形 (×2) | (*2)[下拉]地形(plain 平原/forest 森林/mountain 山地/water 水域/ruin 废墟/crystal 晶体/moon 月面) | 地形 |
| 160 | DO | 地形种类修正 | modifier | F102 修正 (×1) | (*1)[输入]数值 修正系数=1 | 修正系数 |
| 161 | ROLL | 幸运判定 | segments | F103 分段 (×1) | (*4)[输入·json] 分段映射表 | 分段映射表 |
| 162 | DO | 移除 Buff | buff_id | F104 增益ID (×1) | (*1)[输入]文本 要移除的增益ID | 增益 ID |
| 163 | DO | 自定义效果 | payload | F105 payload (×1) | (*4)[输入·json] 自定义载荷 | 自定义载荷 |

> **⚠️ 本表已按权威 67 原子清单（`/Users/dingxuyang/Desktop/alpha3.0 系统/原子清单-可编辑版-2026-08-09.md`）重排**：
> - 分组沿用权威六段式 **WHEN / IF / ROLL / DO / COST / META**（H 组空间原子标 WHO）。
> - 原有「A 组攻击 / B 治疗…K 终结」旧结构**已废弃**：旧 `A5 暴击`（阈值/倍率/critBonus）不属于权威 67 原子，已整体移除；`critBonus`(暴击加成) 为无效字段不再保留。
> - 字段形式按权威清单变量类型标注：`number→[输入]数值`、`text→[输入]文本`、`bool→[勾选]`、`select→[下拉]`、`json→[输入·json]`，并附默认值与枚举中文。
> - 权威清单含 9 个「—」手动/护盾/隐匿/阻止移动/装备锁定项，已并入本表（无独立组号）。
> - H1 地图炮 与 H2 AOE 按「原点」判别为两个独立原子（详见权威清单说明），`origin` 固定不可编辑。

### 阈值 / 门槛语义对照表（★ 重要：被比较值从哪来）

> **核心结论**：表里凡是"阈值 / 门槛 / 成功线"类数值，**不是都掷骰**。分两类，编辑器填值前必须分清：
> - **【属性门槛·不掷骰】**：被比较值 = 战斗进行到此刻该单位的**当前真实状态值**（如 HP、AP、属性值），由之前确定性结算累积而来；拿它跟你填的门槛做 `≥/≤/==` 比较。**无前置 roll**，确定性判定。
> - **【骰点成功线·掷骰】**：被比较值 = **这一刻刚掷出的骰点**；拿骰点跟你填的阈值比，是概率判定。
>
> 判别口诀：**凡挂在 HP / 属性 / 百分比条件上的"门槛"=属性门槛（不掷骰）；凡挂在 dice / 掷骰 / 抢夺成功上的"阈值"=骰点成功线（掷骰）。**

| 行 | 原子 | 字段 | 引擎消费点 | 被比较值来源 | 类型 | 是否掷骰 |
|---|---|---|---|---|---|---|
| 19-22 | B3 HP阈值 | `value`(F18) / `is_percent`(F19) | `skillExecutor` `hp_compare` | 判定对象 `subject` 的**当前真实 HP**（percent 时转 `hp/maxHp×100`） | 属性门槛 | ❌ 不掷骰 |
| 36-38 | C2 单数值阈值二分 | `sides`(F29) / `threshold`(F30) | ROLL 组掷骰后 `DiceService.check` | 刚掷出骰点（`rollOnce(sides)`） | 骰点成功线 | ✅ 掷骰 |
| 39-41 | C3 单数值多段 | `sides`(F29) / `segment_count`(F31) | ROLL 组多段掷骰 | 刚掷出骰点 | 骰点成功线 | ✅ 掷骰 |
| 42-45 | C4 对抗数值 | `a`/`b`/`compare`/`tie` | `branchEvaluator.evaluateContest` | 双方各掷一次骰点 | 骰点成功线 | ✅ 掷骰 |
| 51-52 | C7 数值 vs 动态值 | `op`(F17) / `compare_to`(F39) | ROLL 组掷骰后比 `compare_to`（如 `target.hp`） | 左=刚掷骰点，右=动态对象当前属性 | 混合（左掷骰·右属性） | ✅ 左值掷骰 |
| 68 | D5 即死 | `condition_hp`(F52) 生命门槛 | `effectExecutor.handleInstantKill` | 目标**当前真实 HP**（`threshold_percent` 转 `maxHp×%`，或 `threshold_fixed`） | 属性门槛 | ❌ 不掷骰（代码注释明写"去骰化"） |
| 149 | J1 装备抢夺 | `threshold`(F30) 成功阈值 | 抢夺成功判定（掷骰 vs 阈值） | 刚掷出骰点 | 骰点成功线 | ✅ 掷骰 |
| （全局） | 暴击 | `critThreshold` | `damagePipe.checkCrit` / `DiceService.critThreshold` | 刚掷 1d6 点数 | 骰点成功线 | ✅ 掷骰 |
| （全局） | 命中 | `hitCheck.successLine` | `DiceService` 全局命中 | 刚掷骰点 | 骰点成功线 | ✅ 掷骰 |
| （词条） | 隐匿攻击加成 | `bonusDice.threshold` | `stealth-tags` `stealth_ambush` | 刚掷骰点（sides:6, threshold:5） | 骰点成功线 | ✅ 掷骰 |
| （词条） | 伪装闪避 | `dice.threshold` | `stealth-tags` `stealth_camouflage` | 刚掷骰点（sides:6, threshold:3） | 骰点成功线 | ✅ 掷骰 |
| （词条） | HP 触发类 | `hp_threshold` / `hp_threshold_percent` | `factionSkillRegistry` `checkCondition` / `skillExecutor.executeExecute` | 单位**当前真实 HP**（比值 `hp/maxHp`） | 属性门槛 | ❌ 不掷骰 |

**⚠️ 设计提醒**：编辑器应在"属性门槛"类字段旁标注"被比较值=单位当前属性（不掷骰）"，在"骰点成功线"类字段旁标注"被比较值=本次骰点（掷骰）"，避免填写者误以为所有阈值都靠掷骰断定。当前原子大表中 `value`(F18) 被 B3/ D7/ E3/ 护盾/ 隐匿加成等多原子复用为纯数值，**是否掷骰完全取决于它挂靠的原子分组（IF 组=属性门槛不掷骰；ROLL/DO-抢夺=掷骰）**，需在 UI 上按原子上下文区分说明。

### 阈值被比较值判定流程图（填表时自判 4 步）

> 一句话法则：**被比较值 = 该"阈值"字段挂靠的原子在引擎里实际拿谁去比。** 不是看字段名，而是看它属于哪个原子、引擎那个原子的 `case` 里 `cmp(左, op, 右)` 的左是什么。

```
第1步：看分组（WHEN / IF / ROLL / DO / COST / META）
  ├─ IF 组（B3 / B4 …）──────────────► 拿单位当前属性比 →【属性门槛·不掷骰】
  ├─ ROLL 组（C2 / C3 / C4 / C7）─────► 必然先掷骰 →【骰点成功线·掷骰】
  ├─ DO 组·即死/生命门槛（D5）────────► 拿当前 HP 比 →【属性门槛·不掷骰】
  └─ DO 组·抢夺成功阈值（J1）─────────► 掷骰 →【骰点成功线·掷骰】

第2步：看同原子是否带 dice / sides / 掷骰 字段
  ├─ 有 ─────────────────────────────► 阈值 100% 是"骰点成功线"（掷骰后比）
  └─ 无，但带 subject/target/hp/stat ─► 阈值是"属性门槛"（拿该对象当前属性比）

第3步：拿不准 → 搜 skillExecutor.cjs 的 case '<原子ID>'
  └─ 看 cmp( 的第一个参数：
        subj.hp / target.hp / maxHp×%  → 当前属性（不掷骰）
        DiceService.roll / rollOnce    → 骰点（掷骰）

第4步：照常理校验（防误判）
  ├─ 阈值语义是"条件门"（斩杀需血量低于 X%）→ 必为属性门槛，确定性
  └─ 阈值语义是"概率门"（闪避/奇袭掷骰≥5）→ 必为骰点成功线，随机性
```

**速查表（原子 → 被比较值左）**
| 原子 | 被比较值（左） | 来源 |
|---|---|---|
| B3 HP阈值 | `subject` 当前 HP（或 `hp/maxHp%`） | 战斗累积真实血量 |
| B4 属性对比 | A方属性值 vs B方同属性值 | 双方当前属性 |
| C2/C3 阈值二分·多段 | 本次掷骰点数 | 刚掷的骰 |
| C4 对抗 | 双方各掷一次骰点 | 刚掷的骰 |
| C7 vs 动态值 | 左=本次骰点，右=动态对象属性（如 `target.hp`） | 混合 |
| D5 即死 | 目标当前 HP | 战斗累积真实血量 |
| J1 抢夺 | 本次掷骰点数 | 刚掷的骰 |
| 暴击 / 命中 | 本次掷骰点数 | 刚掷的骰 |

**为什么不能"看字段名"判断**：`value`(F18) 被 6 个原子复用（B3、D7、E3、护盾、隐匿加成、装备扣减），它只是裸数字容器。在 B3 里它是"HP 门槛"（不掷骰），在 J1 抢夺里同名字段是"成功阈值"（掷骰）。**被比较值永远由"原子 + 引擎 case"决定，不由字段名决定。**

### 跨段引用阈值的正确插法（段 a 要看"其它阈值"结果时）

> **场景**：在某段 a 插入"阈值判定触发"，但该阈值不看当前属性、也不掷骰，而是要和**另一段已经算出来的阈值结果**比较。
>
> **结论先讲**：引擎**不支持"阈值字面量互相引用"**（没有 `threshold = @另一段阈值` 这种语法糖）。正确做法 = **分两段 + 用 `bind_to` 桥接**，且段 a 必须排在"产出阈值"的段**之后**。

**机制依据（skillExecutor `_walkPhaseTree`，schema_version=2 段树）**
- 段按约束顺序遍历（引擎 `ORDER` 常量 = `WHEN → IF → WHO → ROLL → DO → COST`；若词条带 `user_order` 则经 `normalizePhaseOrder` 按硬边归位，详见「六段约束集」）。
- 每层重建局部帧 `fxCtx / whenCtx`（注释明写"流程变量隔离"）：**前段算出的普通数值默认不会自动透传给后段**。
- 因此"看其它阈值"只有两条合法通道：
  - **通道A（掷骰结果透传）**：前段是 ROLL 段，掷骰点数经 `rollV` 参数向下透传；后段 IF 用 `gate_condition` / `when` 表达式里的 `roll` 左值引用它。引擎原生支持。
  - **通道B（属性/计算结果桥接）**：前段 IF/B3 算出的 HP 比值或属性差，没有自动存进 context。必须先借 **C5「数值参与运算」的 `bind_to`** 把结果绑定到 `unit` 或 `battleState` 的某个可追溯字段（如 `unit.temp_threshold`），后段 `when` 表达式再去读该字段。

**标准插法（最小 JSON 示例）**
```json
{
  "schema_version": 2,
  "tree": [
    { "phase": "ROLL", "atoms": [ { "type": "roll", "sides": 6 } ] },          // 前段：掷骰，rollV 透传
    { "phase": "IF", "atoms": [ { "type": "condition",
        "expr": "roll >= 4" } ] },                                             // 段a（消费前段掷骰）：不掷骰，读 rollV
    { "phase": "DO", "atoms": [ { "type": "calc", "bind_to": "unit.temp_threshold",
        "op": "set", "value": 50 } ] },                                        // 桥接：把结果落地到字段
    { "phase": "IF", "atoms": [ { "type": "condition",
        "expr": "unit.temp_threshold >= 30" } ] }                              // 段a（消费桥接值）：读 bind_to 字段，不掷骰
  ]
}
```

**排错要点**
1. **顺序错**：段 a 必须放在"产出阈值"的段**之后**，否则读到 `undefined`/旧值。
2. **忘了 `bind_to`**：直接写 `threshold = 另一段阈值` 会被引擎忽略或判为常量。
3. **误以为自动透传**：`fxCtx/whenCtx` 是隔离帧，普通计算结果不会自动进后段 context，必须显式 `bind_to` 或走 `rollV`。

**⚠️ 设计盲区提示**：当前原子大表的 IF/ROLL 组"阈值"都假设被比较值来自"当前属性"或"本次骰点"，**没有原子显式表达"被比较值 = 另一段算出的中间结果"**。要原生支持此类玩法，可选：
- 复用 **C5 的 `bind_to` + IF 段 `when` 表达式引用**（现有能力，无需新原子）；或
- 新增「**中间值引用阈值**」原子，让"被比较值来源"多一类 = `bind_to 字段`。

### 字段形式汇总（按权威 67 原子清单）
- **[输入]数值** 占绝大多数（伤害/回合/半径/系数/阈值等自由数值，附默认值）；凡标 `:number` 的字段编辑器应支持「固定值 / 公式」双模输入（见权威清单补充）。
- **[输入]文本** 用于：固定文本/ID/坐标/映射表（如 `origin`/`bind_to`/`mapping`/`to` 落点/`buff_id`/`target_entry`）。
- **[输入·json]** 用于：结构化的手绘格集合与载荷（`mcShapes`/`segments`/`payload`）。
- **[下拉]** 用于：各类 `select` 枚举（来袭类型/伤害来源/比较符/地形/伤害种类/资源/位移模式/覆盖形状/可见状态/抢占模式/失效范围 等），选项含中英文对照。
- **[勾选]** 用于：所有 `bool` 字段（`gate`/`is_percent`/`no_chain`/`requires_unmoved`/`skip_next_turn`/`friendly_fire`/`force_detour`/`selectable`/`player_optional`）。
- **结构折叠**：H1 地图炮 / H2 AOE 的 `origin` 固定不可编辑（显示且禁用）；H1 `mcShapes` 走画板六向手绘，H2 `cast_range`+覆盖区走落点画板，二者为两个独立原子。

### 子六段承载规范（schema_version=2 段树递归）

> **背景**：六段模型 = 主六段（固定框架 `WHEN→IF→WHO→ROLL→DO→COST`）+ 每个 phase 下可挂原子 + 原子可递归子六段（**最多 3 层**：主+子+孙）。`skillExecutor._walkPhaseTree` 已原生支持此递归（`tree` 字段）。本节约定编辑器/文档侧如何暴露该能力。

**1. 子六段不是新原子，是原子的可选 `tree` 字段**
- 子六段 = 某个原子 JSON 内的可选 `tree` 字段（如 `{ "type":"roll","sides":6,"tree":[...] }`），引擎递归进入 `_walkPhaseTree` 再走一遍完整六段。
- **不需要新增"子段/Subtree"原子类型**；引入包装原子反而多一层无意义封装。

**2. 挂子段的原子自身照常执行，且段落照常可加原子**
- 带 `tree` 的原子先执行自身逻辑（如 ROLL 先掷骰），再触发子六段。**不存在"挂了子段就不能当原子"**。
- 子六段内部的每个 phase 节点也都有 `atoms` 数组，与主段完全一样可放任意原子。**不存在"有子段就不能加原子"的禁令**。
- 唯一限制是"哪些原子类型允许带 `tree`"（见第 4 点白名单），与"段落能否加原子"无关。

**3. 层间不继承，靠 `bind_to` 桥接**
- 每层重建局部帧 `fxCtx/whenCtx`（代码注释明写"流程变量隔离"），父层普通计算结果**不会自动透传**给子层。
- 子层要读父层某值，父层须先 `bind_to` 到 `unit` / `battleState` 的可追溯字段，子层 `when`/`expr` 再读该字段。
- 递归深度 ≤ 3 层，再深由引擎拒收（防无限递归）。

**4. 可嵌套原子白名单（编辑器须强制）**
| 原子类型 | 能否带 `tree` | 说明 |
|---|---|---|
| ROLL（C2/C3/C4/C5/C7/手动掷骰/幸运判定） | ✅ 允许 | 掷骰后挂子六段做分支/结算，最常用 |
| IF（B3/B4…） | ✅ 允许 | 条件满足后挂子六段 |
| DO（D1~D7/E/F/G/H/I/J…） | ✅ 允许 | 结算后挂子六段（如连锁/反射） |
| WHO（H1/H2） | ⚠️ 受限 | 选靶规则建议静态；动态选靶（依赖父层中间值）待定（见第 5 点） |
| WHEN（A1~A12） | ⚠️ 受限 | 触发闸门在外层调度，树内只记录；子六段意义有限 |
| COST / META（G2~G8/K1~K4） | ❌ 不建议 | 代价/元数据段挂子六段易致扣费时机歧义 |

**5. 待定的子层专属原子（如需原生支持动态能力）**
- `read_bound`：显式读取父层 `bind_to` 值做判定（当前靠约定字段名，无专门原子）。
- `dynamic_target`：子层 WHO 按父层算出的中间值动态选靶（当前 WHO 只消费静态配置）。
- 上述为"补齐子层能力"可选项，非"子段容器"必需。

**6. 与六段约束集/阈值层级的关系**
- 子层内部每个 phase 仍遵守同一套六段硬约束（`IF→DO`、`WHO→DO/ROLL`、`ROLL→消费roll的IF`）。
- 阈值/身份等同名概念的含义由"所在层 + phase"共同决定（见上方「阈值语义对照表」「六段约束集」），**讨论任何字段必须先指定它活在主/分/子哪一层**，否则必混淆。

### 待你确认的可修改点（举例）
1. **方向字段是否应统一为下拉**：H1 `direction` 已是 `0~5 六向/all` 下拉；旧自由文本方向已不存在于权威表。
2. **骰式字段是否应加格式校验/预设**：权威表 C2/C3 `method+sides` 以"生成方式+骰面数"表达，非直接 `NdM` 文本。
3. **坐标/手绘字段**：H1 `mcShapes`、H2 落点、H3 `to`、H6 `at` 建议走画板点选回填。
4. **勾选项偏少**：是否需要给 buff/debuff 增加「是否可叠加」「是否可被驱散」等布尔开关（参考 F1 `stacks` 已部分覆盖）。
5. **资源回复双模**：E1 `by`(flat/stat_coeff/terrain) 已暴露计算方式；建议把「固定值/公式」双模做进数值控件本身（权威清单建议）。
