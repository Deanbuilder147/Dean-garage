# /glossary-studio 架构重构文档（五层递进结构）

> 生成日期：2026-08-11（基于 `glossary-studio-五视图功能定位与待讨论清单-2026-08-10.md` 重构）
> 主操作空间锁定：`/glossary-studio`（`GlossaryHubNew.vue` + `HexCanvasEditor.vue`）
> 旧页 `glossary` / `glossary-editor` 暂挂（路由保留、导航不挂），后续再定。
> 最近更新：2026-08-11 按"架构→运行时契约→视图交互→字典规范→施工图"五层结构重组，并纳入 6 维度阻塞诊断。

---

# 第一章：架构总览与核心设计决策（Architectural Principles）

> **定位**：定义编辑器与引擎协同的全局宪法红线与坐标基准。后端引擎与前端画布必须共同遵守。

## 1.1 主操作空间与路由隔离
- 主操作空间锁定 `/glossary-studio`（`GlossaryHubNew.vue` 外壳 + `HexCanvasEditor.vue` 画布核心）。
- 旧页 `glossary` / `glossary-editor` 路由保留但导航不挂，作为历史兼容，后续再定去留。
- 五视图（hexsix / range / aoe / map / battle）在 `/glossary-studio` 内部通过 `viewMode` 切换，**非独立路由页**。

## 1.2 顶层六段式秩序与拓扑约束
- 每个词条都必须以 `WHEN` 开头；`WHEN` 作为锚点放在**第 2 列（索引 1）**。
- **编辑器内可视排布自由（不固定泳道）**，但最终发动顺序受引擎硬约束（非纯列序自由）。
- **六段顺序约束（2026-08-11 拍板）**：顶层六段引擎侧锁死为 `WHEN→IF→WHO→ROLL→DO→COST`，支持**带约束的自由调序（拓扑排序）**，少数硬边不可违反：
  - `WHEN → 其余所有`（触发闸门最前）
  - `IF → DO`（IF 为闸门，`return` 后 DO 已结算不可回滚）
  - `WHO → DO`（及建议 `WHO → ROLL`，选靶须先于结算/掷骰）
  - `ROLL → 消费 roll 的 IF`（ROLL 掷骰后 `rollV` 透传，IF 早于 ROLL 则 `roll=0`）
  - **可自由部分**：`ROLL ↔ COST` 先后、`COST` 可多次出现、同 phase 内原子顺序。
- **约束实施方式（2026-08-11 拍板方向 #12）**：引擎侧保留 `ORDER` 默认序、**不做运行时自动重排**；约束由**编辑器侧强制**——当用户试图在某段后插入/放置"违反硬边的段"时，**该段按钮直接灰掉（禁止插入）**，而非事后重排或弹窗。硬边同上。`whenCtx` 惰性化仍建议做（消除构造期依赖），但重排逻辑在编辑器拦截，不交给 `normalizePhaseOrder`。
- **交互流程**：玩家先**拖拽「字头」（段头/词条头）进入格子内**，落格后再在格内细化（段内字段编辑 / 子结构组织），即「先拖入格子占位 → 格内再细化」两步式。
- **WHEN 锚点不可擦除/不可移动（#1 拍板）**：起始第一个 WHEN **绝对固定在锚点（5,10），不可擦除、不可移动**；其余段可拖拽编排。

## 1.3 空间物理基准

### 1.3.1 单位自身原点（UNIT_ORIGIN）
- 射程 / 地图炮 / AOE / 战场模拟 **全部以地图格中心点（画布正中心）当作「单位本身」**。
- 代码侧已落地：`HexCanvasEditor.vue` 新增 `UNIT_ORIGIN = { q: GRID_COLS/2, r: GRID_ROWS/2 }`，射程中心、地图炮六向补全、六段式绘制与点击检测、拖拽落点全部改用 `UNIT_ORIGIN`；战场模拟独立 canvas 的 `shooterCell().center` 同样以 `(BC_COLS/2, BC_ROWS/2)` 为锚。

### 1.3.2 AOE 目标点原点（可超射程）
- AOE 形状由用户在 **AOE 视图手动画形状**，逻辑类似地图炮（用户自由绘制阴影 = 攻击范围）。
- **攻击范围原点 = 攻击目标点**（玩家先在射程内/战场选一个目标点，再以其为原点画 AOE 形状）；地图炮原点是玩家自身（UNIT_ORIGIN）。即 AOE = 「地图炮逻辑 + 原点换成目标点」。
- **AOE 与射程目标点联动（#2 拍板）**：AOE 的「目标点」**复用射程环带里玩家点的某格**（不单独点选）；**目标点选取受 `max_range` 约束（必须在射程内），但画出/生效的攻击范围可超过射程**（如射程 3，目标点在 3 内，但攻击范围可覆盖距离 ≥3 的格子，这些格子上的单位同样视为有效命中）。即"选取点受射程限、作用范围可超射程"。

### 1.3.3 地图炮正右朝向（东向 dir=0 顺时针 60° 补全）
- 地图炮**不需要方向键**，只需一个**六向补全**按钮。
- **默认绘图朝向 = 正右方向（东，dir=0）**：玩家第一次手绘的阴影形状，程序默认当作「正右方向」的范围基准。玩家无需画方向，只需画一块相对原点向右延伸的阴影，系统以这块正右阴影为基准做后续旋转。
- 程序逻辑：以正右阴影为基准，以 60° 为步长旋转复制，得到 6 组阴影坐标（每组相对原点的旋转偏移不同，但形状一致）。
- 这 6 组阴影坐标按**原点指向各组的方位**映射为 6 个攻击方向（正右=dir0，顺时针每 60° 一个：右下/下/左下/左上/上/右上），即 `mcShapes[dir] = rotate(baseShadowEast, dir*60°)`，`dir=0` 即玩家画的正右阴影。与 `DIRECTIONS`/`computeDirection` 顺时针扇区对齐。

---

# 第二章：运行时结算与数据契约（Runtime & Engine）

> **定位**：规范后端执行管道、模拟计算与数据回写边界。属后端引擎 / shared-kernel 落点。

## 2.1 战场模拟结算契约（battle 视图四步法，#4 拍板）

> 战场模拟（battle 视图）必须消费**真实结算路径**，不再只是示意。契约如下（已按 2026-08-11 三文件实证修正函数名）。

**Step 1：空间命中格推导（Range + AOE）**
- 调用 `shared-kernel/src/hexMath.ts`（实证：现有 `getHexesInRange` / `hexDistance` / `isTargetInRange`，**尚无 `deriveRange` / `computeAOECells`**）。
  - 射程环带：`getHexesInRange(center, maxRange, minRange?)` 已存在，可直接复用为"选点受射程限"的判定。
  - AOE 作用范围（可超射程）：需**新增** `computeAOECells(casterCoord, targetCoord, aoeShape)` —— 以 targetCoord 为原点、按 AOE 形状（用户手绘阴影 / 地图炮六向补全）展开，半径**独立于 maxRange**（满足 #2 "作用范围可超射程"）。
  - `deriveRange()` 可不单独建，直接用 `getHexesInRange` 替代；若需统一入口再包一层。
- 导出最终命中的六角格集合 `affectedCells`；从 `battleState.units` 中筛选出位于这些格子上的 `targetUnitIds`。

**Step 2：隔离帧上下文构建（Simulated Context Frame）**
- 构造临时 `simulatedCtx`，将施法者、目标单位列表、指定的 `rollValue` 传入。
- **隔离红线**：深拷贝单位的 `equipState` 与 `stats`，**严禁直接修改原 `battleState` 对象**（只读消费，结果增量返回）。`effectExecutor` 内部对 `unit.statusEffects` 的 push 必须改为"写克隆帧、不回写原对象"（见 Step 3 pure 开关）。

**Step 3：纯计算模式管道（Skill & Damage Pipe）**
- 调起 `skillExecutor._walkPhaseTree`（**实证：当前无 pure 模式**，`pure_segment` 仅是日志模型名；会直接 `unit.statusEffects.push`）。**须新增 `pure` 开关参数**（如 `_walkPhaseTree(tree, unit, target, rollV, context, depth, { pure: true })`）：pure 模式下 `effectExecutor` 的 statusEffects 写入改落 `context.cloneFrame` 不回写原 unit。
- 针对每个受击单位，调用 `DamagePipe.calculate(config)`（**实证：真实完整入口是 `DamagePipe.calculate(config)`，不是 `calculateDamage`**；`config = { attacker, defender, attack_type, damage_kind, terrainDefs, baseDamage? }`）走**真实结算路径**，返回 `result.final_damage` / `result.is_crit` / `result.stages`（含减伤明细）。
  - 简版入口 `DamagePipe.calculateQuick(attacker, defender, attackType, terrainDefs)` 亦可，但缺失 `damage_kind`/`baseDamage` 等词条维度，战场模拟建议用完整 `calculate`。

**Step 4：增量结果输出（SimulateSkillImpactOutput）**
- 组装 `SimulateSkillImpactOutput`（`{ affectedCells, targetUnitIds, perUnit: [{ unitId, finalDamage, isCrit, stages }] }`）返回；前端 `HexCanvasEditor` 接收后直接在画布上**高亮 `affectedCells`** 并**弹框显示 `finalDamage`**。

**落地依赖（2026-08-11 实证结论）**：
1. `hexMath.ts`：**新增** `computeAOECells(casterCoord, targetCoord, aoeShape)`（AOE 作用范围，半径独立于 maxRange）；`getHexesInRange` 已可复用为射程判定，无需新建 `deriveRange`。
2. `skillExecutor._walkPhaseTree`：**新增 `pure` 开关**，使 statusEffects 写入落克隆帧、不污染原 `battleState`（满足隔离红线）。
3. `damagePipe.cjs`：真实接口为 `DamagePipe.calculate(config)` / `calculateQuick(...)`，**无 `calculateDamage`**；调用方按 `config` 形状组装，确认其只读 `attacker/defender` 不内部改写（当前 `calculate` 仅读不写 unit，符合红线）。

## 2.2 树级嵌套与上下文作用域规则

### 2.2.1 递归六段树约束（子六段承载规范，#13 拍板）
- 六段模型 = 主六段（固定框架）+ 每个 phase 下可挂原子 + 原子可递归子六段（**最多 3 层**：主+子+孙）。`skillExecutor._walkPhaseTree` 已原生支持（`tree` 字段）。
- **子六段不是新原子，是原子的可选 `tree` 字段**：如 `{ "type":"roll","sides":6,"tree":[...] }`，引擎递归进入 `_walkPhaseTree` 再走一遍完整六段。**不需要新增"子段/Subtree"原子类型**。
- **挂子段的原子自身照常执行，且段落照常可加原子**：带 `tree` 的原子先执行自身逻辑（如 ROLL 先掷骰），再触发子六段；子六段内部每个 phase 节点也都有 `atoms` 数组，与主段完全一样可放任意原子。**不存在"有子段就不能加原子"的禁令**。
- **可嵌套原子白名单（编辑器须强制）**：

  | 原子类型 | 能否带 `tree` | 说明 |
  |---|---|---|
  | ROLL（C2/C3/C4/C5/C7/手动掷骰/幸运判定） | ✅ 允许 | 掷骰后挂子六段做分支/结算，最常用 |
  | IF（B3/B4…） | ✅ 允许 | 条件满足后挂子六段 |
  | DO（D1~D7/E/F/G/H/I/J…） | ✅ 允许 | 结算后挂子六段（如连锁/反射） |
  | WHO（H1/H2） | ⚠️ 受限 | 选靶规则建议静态；动态选靶（依赖父层中间值）待定 |
  | WHEN（A1~A12） | ⚠️ 受限 | 触发闸门在外层调度，树内只记录；子六段意义有限 |
  | COST / META（G2~G8/K1~K4） | ❌ 不建议 | 代价/元数据段挂子六段易致扣费时机歧义 |

- **待定子层专属原子（非阻塞）**：`read_bound`（显式读父层 `bind_to` 值）、`dynamic_target`（子层 WHO 按父层中间值动态选靶）。

### 2.2.2 变量隔离与跨段/跨层桥接（bind_to 机制，#5 诊断）
- **诊断**：六段式树在递归解析（子六段 ≤3 层）时，每层会重建隔离的 `fxCtx / whenCtx`，导致父层算出的数值无法自动透传给子层/后段（例如前段算出的阈值，后段 IF 无法直接比较）。
- **桥接规则**：明确必须通过 **C5 原子的 `bind_to` 机制**将中间结果显式绑定至 `unit.temp_*` 字段，后段表达式再读该字段；UI 上需对此类"属性门槛（不掷骰）"与"骰点成功线（掷骰）"做区别标注。
- **层级心智模型（关键）**：同名概念（阈值/身份/目标）的含义由「所在层（主/分/子）+ phase」共同决定，讨论任何字段必须先指定它活在主/分/子哪一层，否则必混淆。
  - 主段 IF 里的阈值 = 属性门槛（不掷骰）
  - 主段 ROLL 里的阈值 = 掷骰成功线
  - 子段 IF 里的阈值 = 读父层 `bind_to` 值做判定（若父层 ROLL 掷出，须 `bind_to` 显式传）

## 2.3 射程真相源与双向回写机制（G 决策 + #5/#6/#7 诊断）

- **射程真相源回写：需要双向（G 已确认）**：当前是单向（draft → 画板），需改为**双向**——画板修改了 min/max（或形状）后，写回 `draft.target`（或对应 range 段）。优先级仍保持：`target.min_range/max_range` > `draft.range` 旧字段。落库字段（F）定稿后，回写目标字段随之确定。
- **契约落库与双向回写（#5/#6/#7 诊断·未决）**：
  - 当前 `rangeCtx`（射程、盲区、AOE 形状、地图炮阴影）在前端均为 `HexCanvasEditor` 的临时 state，保存时未能稳定映射回 `draft.target` 或 `draft.range`。
  - 待解决：① 定义 `rangeCtx → draft` 的落库 JSON Schema（确定落在 `draft.target.min_range/max_range` 还是原子级 `range` 字段）；② 建立画板调整后的**双向回写机制**：画板修改环带/形状 → 驱动 `draft.target` 更新 → 触发六段式树相关节点同步。
- **字段语义去重与真相源归一（#9 诊断·未决）**：
  - 多处存在空间字段重复定义或冗余（词条顶层 `cast_range` 与原子级 `range`/`distance`；H1 地图炮与 H2 AOE 内部的 `direction`/`range`）。部分原子级的 `range`/`direction` 在后端 `skillExecutor.cjs` 路径中尚未被消费（引擎只认 `cast_range` 真相源）。
  - 待解决：① 清理废弃旧命名，明确"词条顶层 `cast_range`"为绝对射程真相源，原子级 `range` 仅作为"相对延伸/辐射半径"；② 后端结算管道须补齐对原子级 `direction` 与 `range` 的约束判定。

---

# 第三章：五视图功能定位与交互规格（UI & Visual Specs）

> **定位**：前端视图交互、控件逻辑与状态流转细则。属前端 Vue 组件落点。

## 3.1 五视图职责与渲染状态矩阵

| 视图 | 功能定位 | 确认逻辑 | 当前实现状态 |
|---|---|---|---|
| **六段式 (hexsix)** | 不固定泳道，以列序为发动顺序 | 玩家**拖拽字头入格**→格内再操作；WHEN 在第 2 列作锚点 | 6 段星形已绘制；WHEN 已移第 2 列；**缺拖拽字头入格 + 列序发动链路** |
| **射程 (range)** | 确定词条射程范围 | 真相源双向回写（G）：画板改 min/max → 写回 draft.target | 环带射程已实现（盲区+环带+环号+线性射线）；**缺画板→draft 回写** |
| **AOE (aoe)** | 设目标点后，算伤害作用范围形状 | 用户在 AOE 视图**手动画形状**；原点 = 目标点（非玩家原点，见 1.3.2） | 底层圆形扩散+落点蓝圈；**缺手绘形状 + 目标点原点链路** |
| **地图炮 (map)** | 玩家原点 + 攻击方向与形状 | **不用方向键**，画阴影 → 60° 旋转补全六向（见 1.3.3） | 6 向工具栏+六向补全+清除；**待改为纯六向补全（去掉方向键依赖）** |
| **战场模拟 (battle)** | 模拟词条实际结算 + 手动布子 | 调用真实战斗核心/管道结算并展示；手动加敌/友/自身（HP100/MOB14，见 2.1） | 独立 canvas 示意；**缺真实结算消费 + 布子（默认 HP100/MOB14）** |

## 3.2 六段式 (hexsix) 视图交互设计

> 本节为六段式视图的**完整交互规格**，含按钮、界面显示规则与基础操作流程。

### 3.2.1 按钮栏与六色段头

| 按钮 | 行为 | 备注 |
|---|---|---|
| **段式按钮** | 6 个彩色段头：`IF` / `WHEN` / `WHO` / `ROLL` / `DO` / `COST`，**各用不同颜色区分** | 每个按钮可拖拽入画布格子；点击 IF 或 ROLL 时**弹出数量选择器**（2~4，默认 2） |
| **原子面板** | 展示当前可用的 67 原子列表（A-K 分组），样式与现有 C 区 `studio-zone-c` 一致 | 支持搜索/折叠/拖拽 |
| **居中** | 将已布置到画布上的所有段格保持相对坐标关系，整体平移至画布中心 | 不改变格间相对位置 |
| **清除** | 清除整个六段式面板所有内容 | **必须弹窗确认**后才执行 |
| **擦掉** | 进入「擦除模式」：选中后点击/拖拽画布上已有内容的坐标格，消除该格内容 | 再次点击或按 Esc 退出擦除模式（WHEN 锚点格不可擦除，见 1.2） |
| **保存** | 将当前六段式编排逻辑写回该词条（调用 `writeLanesToDraft()` + `saveCurrent()`） | 同现有 D 区「✎ 保存 / 编辑」按钮 |
| **跳转按钮**（字段型） | 当在段格内编辑原子、遇到「射程 / 方向 / AOE形状 / 地图炮」等**依赖画布的字段**时，该字段在表单内**不以输入框呈现，而是渲染为一个跳转按钮** | 点击跳转按钮 → 切换到对应画布视图（射程 / AOE / 地图炮），并**带提示气泡**说明「正在为〔原子名·字段〕设置，请在画布上绘制后点『保存并返回』」；六段式与画布**同一区域互斥显示**，故用跳转而非同屏并排 |

**段式按钮颜色方案**：
```
IF   → 青色 #00d4aa（条件门槛）
WHEN → 琥珀 #ffb000（触发锚点）
WHO  → 蓝色 #5b9bff（作用对象）
ROLL → 紫色 #a855f7（掷骰判定）
DO   → 绿色 #22c55e（效果执行）
COST → 红色 #ef4444（代价消耗）
```

**IF / ROLL 数量选择**：
- 点击 IF 或 ROLL 段式按钮时，弹出微型选择器：
  - 「生成几个 IF 段？」→ 可选 2 / 3 / 4（默认 2）
  - 「生成几个 ROLL 段？」→ 可选 2 / 3 / 4（默认 2）
- 选择数量 N 后，自动在画布相邻空位放置 N 个同色段格（见 3.2.4 插入规则）。

### 3.2.2 画布网格排布与 20×20 坐标系
- 六段式画布从固定 6 格星形 → 自由网格（**20×20**），起始 WHEN 锚定 **(5,10)**。
- 段格位置自由布置在网格任意位置，仅起始 WHEN 锚定（5,10）不可移动。
- 段数动态：IF/ROLL 可多实例（2~4），总格数不限。
- 颜色：6 段各一色（IF青/WHEN琥珀/WHO蓝/ROLL紫/DO绿/COST红），区别于现有统一琥珀 `#ffd479`。
- 列序发动：以列序（x 坐标优先，y 次序）作为发动顺序依据（非纯展示）。

### 3.2.3 高亮引导系统（距离 ≤2 环带）
- 当某个段格（如图中的 **a**）已经插入了原子（即 `lane.atoms.length > 0`），系统**高亮显示其周围距离 ≤2 的所有空格**。
- 高亮分两层：
  - **内环 b1~b6**（距离 = 1）：绿色半透明填充（`rgba(0,212,170,0.25)`），表示「推荐下一格」
  - **外环 c1~c6**（距离 = 2）：橙色半透明填充（`rgba(255,176,0,0.20)`），表示「可选扩展格」
- **已有内容的格不高亮**（避免重叠混淆）；高亮实时跟随当前焦点格变化。

```
        b6    c1    b1
      c6  [a]    c2
        b5       b2
      c5       c3
        b4    c4   b3
```
- `[a]` = 当前已填原子的段格（操作点）；`b1~b6` = 距离 1 的 6 个邻居（绿色高亮）；`c1~c6` = 距离 2 的候选格（橙色高亮）。

### 3.2.4 多实例段插格规则（IF/ROLL 2~4）
- 当插入的新段是 **IF** 或 **ROLL** 时：
  1. 先根据用户选择的数量 N（2~4）
  2. 从当前操作点 **a** 的**最近空邻居**开始，沿顺时针方向找 N 个连续空格（优先 b 环，不够再取 c 环）
  3. 在这 N 个格中各自放入一个 **IF**（或 **ROLL**）段格，颜色与段式按钮一致
  4. 多个 IF/ROLL 实例之间**隐式串联**（引擎按列序从左到右、从上到下执行）

### 3.2.5 双 Drop 落点机制
- **C 区面板 Drop**：从原子面板（C 区）拖拽原子 chip → 落入目标段格内 → 该格显示原子卡片缩略图。
- **底画布六边形格 Drop**：拖拽段式按钮 → 落入高亮区域内任一空格 → 该格变为对应颜色的段框。
- 两种 Drop 均触发高亮引导自动更新（以最新操作的格为新锚点 a）。

### 3.2.6 基础操作流程
```
步骤 1: 选择一个段式按钮（IF/WHEN/WHO/ROLL/DO/COST）
         ↓ 若选 IF/ROLL → 弹出数量选择器(2~4)
步骤 2: 将段头拖入高亮提示的某一空格中（b环优先，其次c环）
         ↓ 若是 IF/ROLL 且数量>1 → 自动向相邻空格补全剩余实例
步骤 3: 对已落入的段格，从原子面板拖入所需原子（或双击打开格内编辑）
步骤 4: 双击/单击已放入的原子卡片，编辑其参数（fields 表单）
步骤 5: 重复 步骤1~4，直到词条逻辑完整
步骤 6: 点击「保存」，将编排结果写回词条
```
- **选择段添加到高亮格**：拖拽段式按钮 → 落入高亮区域内的任一空格 → 该格变为对应颜色的段框
- **对空白格添加原子**：从右侧原子面板拖拽原子 chip → 落入目标段格内 → 该格显示原子卡片缩略图
- **编辑已添加的原子**：单击段格内的原子卡片 → 右侧/D区展开该原子的 fields 编辑表单（复用现有 `atom-card.ac-params` 样式）
- **循环迭代**：每次添加新段/新原子后，高亮引导自动更新（以最新操作的格为新锚点 a）
- **保存**：确认无误后点「保存」→ 弹出确认（若 dirty）→ 写回 `draft.tree` + 扁平投影

## 3.3 六段式 ↔ 画布子级跳转与返回状态机

> 背景：六段式画布与射程/AOE/地图炮画布**共用同一显示区域**，无法同屏并排。原子内的「射程/方向/AOE形状/地图炮」字段改用**跳转按钮**进入对应画布（见 3.2.1）。进入画布后必须有办法返回并回填六段式。

### 3.3.1 跳转上下文记录与提示气泡

| 按钮 | 行为 | 备注 |
|---|---|---|
| **保存并返回** | 在射程 / AOE / 地图炮画布中，完成绘制后点击，**将画布结果回写**到跳转来源的那个原子字段（如 `range` 环带 → 原子 `range`；`mcShapes` → 原子 `direction`/地图炮段），然后**切回六段式视图**，定位并高亮回来源段格 | 与 G 决策「射程真相源回写」打通：画布改 min/max/形状 → 写回 `draft.target` / 原子字段 |
| **取消返回** | 放弃本次画布绘制，直接切回六段式视图，来源原子字段保持跳转前的值 | 可做成「保存并返回」旁的次要按钮，或点 Esc 等同取消 |

- **来源标记（跳转上下文记录器）**：从六段式跳转时，记录 `{ sourceSlotCoord, atomId, fieldKey }`，作为画布「保存并返回」的回填目标（避免多原子多字段混淆）。
- **提示气泡**：进入画布时顶部显示提示「正在为〔原子名·字段〕设置 —— 绘制后点『保存并返回』」，离开画布后消失。
- **脏检查**：画布有改动未保存时，直接点「取消返回」应弹窗确认「放弃本次绘制？」，与六段式「清除」确认同风格。

### 3.3.2 与现有代码差异对照

| 项 | 现有实现 | 新设计要求 |
|---|---|---|
| 画布↔六段式切换 | 五视图靠顶部 tab 切换整视图（`viewMode` prop），六段式与射程**各自独立页** | 增加「段内字段 → 跳转进入某画布 → 保存返回回填」的**子级跳转**，而非整页切换 |
| 画布保存 | 射程/AOE/地图炮各自独立保存（部分仍单向） | 画布「保存并返回」除写画布态外，**必须回填来源原子字段**并切回六段式 |
| 字段展示 | 原子表单里 `range`/`direction` 为普通输入/下拉 | 改为**跳转按钮 + 提示气泡**，进入画布绘制 |

---

# 第四章：67 原子标准化字典与字段表达

> **定位**：为六段式编排与表单渲染提供标准化数据字典。属 Schema 字典落点。

## 4.1 门槛与阈值判定逻辑（属性门槛 vs 骰点成功线）
- **属性门槛（不掷骰）**：出现在主段 IF / 子段 IF，比较的是当前属性/状态值（如 `hp <= 30`、`role == enemy`）。UI 标注为"门槛"，不触发掷骰。
- **骰点成功线（掷骰）**：出现在主段 ROLL，由 `rollV` 与阈值区间比较（如 `roll >= 4`）。UI 标注为"骰点成功线"，先掷后比。
- **关键区别标注**：编辑器须在 IF/ROLL 字段旁以徽标区分"门槛型"与"骰点型"，避免与 2.2.2 的层级心智模型混淆；跨段引用阈值必须靠 `bind_to`。

## 4.2 权威 67 原子明细表（按 WHEN/IF/ROLL/DO/COST/META 分组）
- 原子字典已按六段统一编号（旧 `A1~A5 攻击/暴击` 类命名已废弃）。
- **WHO 段 = owner + target 双字段（#11 拍板）**：WHO 段拆为 `owner`（默认 self，可覆盖：装备持有者/反射来源等）+ `target`（复合：`{scope: 身份五元组, granularity: 单体/群体, range_filter?: 射程过滤}`）。WHO 在树内只"记录规则"，真正选靶由外层调度器消费规则执行；`target.scope` 适合用五类身份枚举（self/ally/ally_group/enemy/enemy_group），但 `granularity` 维度不可省（否则群体语义引擎无法捞取）。
- **身份类下拉枚举（#10 拍板：不替换）**：B4 维持五类身份、B1/B2 维持旧两元（`self/target`）现状，二者语义差异按既有设计保留（`target` 为具体单位锚点，正交维度），不再强行统一。

## 4.3 字段表单控件渲染规范
- 控件类型：`[输入]` / `[下拉]` / `[勾选]` / `[JSON]` / **[跳转按钮]**。
- **跳转按钮**：仅用于依赖画布的字段（射程/方向/AOE形状/地图炮），见 3.2.1 与 3.3。
- **暴击字段彻底移除与 DO 段收口（#8 拍板方向）**：旧版 `A5 暴击` 及其内部 `critBonus`（暴击加成）已被证实为无效字段，正式从 UI 控件与 67 原子字典中抹去独立暴击段，统一归入 `DO` 段的 `D1 伤害加减值`（通过 `multiplier` 倍率模式表达），彻底完成字段收口。是否改读词条固定 `critMult` 待定（非阻塞）。

---

# 第五章：施工图与待讨论事项状态矩阵（Roadmap & Status）

> **定位**：研发排期、代码改动落点与未决事项追踪。

## 5.1 决策状态总览

### 5.1.1 已拍板列表（✅）
| 编号 | 议题 | 结论摘要 |
|---|---|---|
| #1 | WHEN 锚点可覆盖/移动 | 起始第一个 WHEN **不可擦除、不可移动**（绝对固定锚点 (5,10)） |
| #2 | AOE 与射程目标点联动 | 目标点复用射程环带某格、受 max_range 约束；**作用范围可超射程** |
| #3 | 地图炮阴影旋转映射 | 默认正右（dir=0）**顺时针** 60° 补全六向，无需方向键 |
| #4 | 战场模拟结算接口 | 四步法契约（见 2.1），已按三文件实证修正函数名 |
| #8 | 暴击字段消费 | 移除独立暴击段，归入 DO 段 D1（multiplier 模式） |
| #10 | 身份类下拉枚举统一 | **不替换**（B4 五类、B1/B2 旧两元并存） |
| #11 | WHO = owner + target | 双字段复合结构（见 4.2） |
| #12 | 六段顺序约束落地 | 编辑器侧灰掉禁插（非引擎重排） |
| #13 | 子六段承载规范 | 原子 `tree` 字段递归（≤3 层）+ 可嵌套白名单（见 2.2.1） |

### 5.1.2 待决列表与推进建议（⬜）
| 编号 | 议题 | 阻塞点 | 推进建议 |
|---|---|---|---|
| #5 | 落库契约 | `rangeCtx → draft` JSON Schema 未定义 | 待功能定稿后设计，涉及 `target`/`range`/`aoe`/`mapcannon` 段 |
| #6 | 射程回写字段 | 回写目标 `draft.target.min_range/max_range` 或 `draft.range` 待定 | 与 #5 一并落地 |
| #7 | 原子 range/direction 消费 + 跳转回填 | 后端 `skillExecutor` 未消费原子级 `range`/`direction` | 定义前端跳转回填机制 + 后端接入 `getSkillRangeFields` 认原子 range |
| #9 | 字段命名统一 + 冗余清理 | `cast_range` vs 原子 `range`/`distance` 真相源未归一 | 明确 `cast_range` 为绝对真相源，原子 `range` 仅作相对延伸半径 |
| #14 | **六段式格子画布缺失** | hexsix 视图当前仅渲染图例文字，**无实际六段星形格子 DOM**（2026-08-11 修复误删 20×20 网格底板后未恢复格子本体） | 重建六段星形格子画布（WHEN 锚 (5,10) + 5 邻居段格），恢复 `onCanvasDropAtom` 拖入原子入段的可视化编辑入口 |
| #15 | **编辑模型澄清：词条是真回写，非仅显示** | `writeLanesToDraft()` 已把 `lanes` 序列化进 `draft.tree`+v1 投影，`saveCurrent` 调用之 → 词条真实落库 | 当前可视化编辑面（六段格子）缺失，策划靠 C 区面板选原子 + 段头按钮加段 + 卡片填参完成编辑；六段格子恢复后 (`#14`) 即可在画布上直接拖原子入段 |

## 5.2 阶段性重构 Tasks Checklist

### Phase 1：基础库补齐（后端 / shared-kernel）✅ 已落地
- [x] `hexMath.ts` 新增 `computeAOECells(casterCoord, targetCoord, aoeShape)`（半径独立于 maxRange）
- [x] `skillExecutor._walkPhaseTree` 新增 `pure: true` 隔离帧开关（statusEffects 写克隆帧不污染 battleState）
- [x] 验证 `DamagePipe.calculate(config)` 在 pure 模拟链路中只读不写 unit

### Phase 2：六段式画布重构与高亮引导（前端）⚠️ 部分完成，格子画布缺失
- [x] WHEN 锚定 (5,10) 不可移动/擦除（相对星形坐标 `coord[i]=WHEN+(neigh[i]-neigh[whenIndex])`）
- [x] 按钮栏：6 色段头 + 原子面板 + IF/ROLL 数量选择器（2-2 多实例 modal）
- [x] 高亮引导 + 顺序硬边（WHEN 锁首位、非法 drop 静默拒绝）
- [x] 双 Drop 落点：段头 + 底画布六格（onLaneDrop / onCanvasDropAtom 接通）
- [ ] **[#14 缺口] 六段星形格子 DOM 重建**：当前 hexsix 视图仅剩 `HEXSIX_LEGEND` 图例文本，无格子本体（2026-08-11 修复误删网格底板后未恢复）。需重绘 6 格星形 + 段格 drop 高亮，使 `onCanvasDropAtom` 可视化生效。

### Phase 3：跳转返回状态机与双向回写接入（前端 + 部分后端）✅ 已落地（含 3.3 强化）
- [x] 跳转上下文记录器 `jumpField = { atom, fieldKey, label, toast }`
- [x] 「保存并返回」/`取消返回` + 顶部提示气泡 `.jump-toast`（3.3 字段级跳转：range/direction/shape/mapcannon/aoe）
- [x] 画板 → 原子 `params[fieldKey]` 精确回填（`saveJumpAndReturn` 写 `rangeCtx` 几何 + 标 dirty）
- [x] 双向回写真源 `writeLanesToDraft()` 序列化 `lanes` → `draft.tree`（schema_version=2）+ v1 投影（effects/conditions/roll.segments）

### Phase 4：战场模拟真实管道接入与布子功能（前端 + 后端）✅ 已落地
- [x] battle 视图接入 2.1 四步法（`/hub-config/simulate-skill` 真实引擎 pure 结算）
- [x] 手动布子（敌/友/自身，默认 HP100/MOB14）+ `drawBattle()` 命中/击杀着色 + HP 血条 + statusEffects 气泡
- [x] `runTest()` 状态机 idle→rolling→resolved + `resetSim()`；`bsPhase` 结构化真实伤害结果区
- [ ] 子六段白名单强制 + 待定子层原子 `read_bound`/`dynamic_target`（关联 #13，画面层 1 层 nest 已支持，≤3 层引擎已支持）

### 批次3：编辑器完整词库接入（67 原子字典）✅ 已落地（2026-08-11 深夜）
- [x] 3.1 原子字典以引擎真名对齐 7 个 GAP handler（collinear/charge_stack/cooldown/damage_share/armor_pierce/reflect_damage/prerequisite_clear），图例改真实计数 `engineHandlerCount` + `unknownAtomTypes` 校验
- [x] 3.2 动态字段表单渲染复核闭环（select/bool/number 双模/json/text + v-model 双向绑定，主链/分支/子树三层）
- [x] 3.3 字段级跳转↔返回状态机（详见 Phase 3）
- [ ] 浏览器人工实测 3.3 跳转气泡↔画布绘制↔保存返回回填（终端无法模拟 Canvas 拖拽/点击）

### 后续专项（非阻塞，按优先级）
1. **CORE 词条迁移**：`melee_strike`/`ranged_shot` 等旧 script 格式（base_damage=0）模拟伤害为 0，需编辑器支持 script→segments 转换。
2. **C5/C6 核心表达式专项（暂停）**：58+ handler 已 95%+ 表达力，C5/C6 属极限元计算，开启需表达式沙盒（变量/运算符白名单 + 默认关闭开关）+ 核心数值链平衡评估。
3. **`bind_to` 跨层桥接**：存量透传未消费，待收口。

---

## 附：6 维度阻塞诊断索引（对应上文章节）
| 维度 | 诊断摘要 | 对应章节 |
|---|---|---|
| 1. 契约落库与双向回写 (#5,#6,#7,G) | rangeCtx 临时态未稳定映射回 draft | 2.3 / 5.1.2 / Phase 3 |
| 2. 字段语义去重与真相源归一 (#9,H1/H2) | cast_range 与原子 range/distance 冗余 | 2.3 / 4.2 / 5.1.2 |
| 3. 画布跳转 & 回填状态机 (4.1,4.5) | 缺跳转上下文记录器与脏检查 | 3.3 / Phase 3 |
| 4. 战场模拟纯计算模式补全 (H,#4) | 缺 computeAOECells 与 pure 开关 | 2.1 / Phase 1 |
| 5. 跨段/跨层变量桥接 (bind_to) | 层间 fxCtx/whenCtx 隔离无自动透传 | 2.2.2 |
| 6. 暴击字段移除与 DO 段收口 (#8) | 旧 A5 暴击/critBonus 无效字段 | 4.3 / 5.1.1 |
