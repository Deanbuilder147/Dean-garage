# 语义原子定义核查报告

> 核查日期：2026-09-02
> 核查范围：`mecha-universe-engine` 全仓（前端 / 后端 / docs）
> 结论先行：**你（用户）的判断正确 —— 含 `AFTER` 段的那份是正确的。**

---

## 〇、一句话结论

> **2026-09-02 补充**：用户指出工作区根目录另有 `docs/` 文件夹，内含
> **`原子记录-20260823.md`** 与 **`原子改造方案-A+B-20260823.md`** ——
> 这两份才是**「之前讨论出来的」权威真相源**，用户「有 AFTER 才对」的判断据此得到完全印证。
> 详见 **第 0 节**。

项目中「语义原子 + 段位」共存在 **7 份定义**，其中：

| | 定义 | 含 AFTER? | 判定 |
|---|---|---|---|
| ⭐ | **工作区 `docs/原子记录-20260823.md`** | **有** | **权威真相源（设计定稿）** |
| ⭐ | **工作区 `docs/原子改造方案-A+B-20260823.md`** | **有** | **权威真相源（实施方案 + 用户拍板）** |
| ✅ | `GlossaryStudio.vue` 的 `LANE_SKELETON` + `HEXSIX_LEGEND` | **有** | 已对齐真相源，正确 |
| ❌ | `GlossaryHubNew.vue` 的 `HEXSIX_LEGEND` | 无（残留 WHO） | 旧版残留，未同步 |
| ❌ | `docs/phase-tree-六段式合并方案.md` 的 `PhaseEnum` | 无（有 WHO） | 旧版文档，已被架空 |
| ❌ | `shared-kernel/.../phase-tree.contract.ts` **L26** `PhaseEnum` | 无（有 WHO） | **方案 B.0 第 1 步未做** |
| ⚠️ | 后端 `atomRegistry.cjs` | 有【AFTER】标注 4 个 | 原子元数据源，但**无段位体系** |
| ❌ | 我提取的 `battle/glossary/atomGroups.js` 的 `PHASES`（8 段） | 有（但整体是我编的） | **我的错误，待修正** |

---

## 0. ⭐ 权威真相源：两份 2026-08-23 文档

> **路径为工作区根目录 `docs/`**（`/Users/dingxuyang/CodeBuddy/20260604120036/docs/`），
> **不是**仓库内 `mecha-universe-engine/docs/`。此前核查漏掉，是因只搜了仓库内目录。

| 文件 | 行数 | 定位 |
|---|---|---|
| `docs/原子记录-20260823.md` | 103 | 设计定稿：六段归属 + 原子总览 + 修饰分类 + 规则固化 |
| `docs/原子改造方案-A+B-20260823.md` | 247 | 实施方案：路径 A（语义注册）+ 路径 B（handler 实现）+ 用户拍板决策 |
| （附）`67原子引擎落地与编辑器词库实施计划-2026-08-08.md` | 462 | 此前判定"缺失"的 67 原子清单，**实存于工作区根目录** |
| （附）`原子搭建完整报告-2026-08-23.md` | 92 | 同期产出 |

### 0.1 六段定稿（`原子记录` L11-14）

```
WHEN(时机) → IF(条件) → ROLL(随机性) → DO(执行) → AFTER(后效) → COST(代价)
```
- `WHO` 段已删除，**降级为 `DO` 段首置前导原子 `B_TARGET`**
- `AFTER` 收口持续状态 / 链式 / 清理

→ 与 `GlossaryStudio.vue` `LANE_SKELETON`（L1055-1065）**完全一致**；
→ 与 `phase-tree-六段式合并方案.md`（有 WHO 无 AFTER）**冲突**，后者确认为过期。

### 0.2 原子总览（`原子记录` 二/三/四）

| 类 | 数量 | 语义名 |
|---|---|---|
| **B 类主原子** | 11 | DO：`B_TARGET`(resolve_target) / `B_DISPLACE`(displace) / `B_SPAWN`(spawn_unit) / `H_MAP_CANNON`(map_cannon)<br>AFTER：`B_ENTRY`(trigger_entry) / `B_HIT_CALLBACK`(trigger_entry) / `B_KILL_STACK`(charge_layer) / `B_CLEANUP`(remove_buff)<br>COST：`C_AP_COST` / `C_ENERGY_COST` / `C_DURABILITY_COST` |
| **A 类数值入口** | 1 | `A_APPLY_VALUE`(modify_stat) —— 伤害/治疗/护盾/加减属性统一入口 |
| **修饰分类（横切）** | 15 | `M_SCOPE` `M_FACTION` `M_STAT` `M_PART` `M_STATUS` `M_SKILL_TYPE` `M_SKILL_ATTR` `M_TERRAIN` `M_PHASE` `M_RANGE` `M_OP` `M_PROB` `M_DURATION_UNIT` `M_FLOW` `M_VISION` |

> `原子记录` L103 自述：`atomRegistry.listMeta()` 共 **73 条**，新增 13 语义名全部注册成功。

### 0.3 落地状态复核（**2026-09-02 实测，文档状态已过时**）

`原子记录` 二节标注的「❌ 待新增 / ⏳ 待补」是 2026-08-23 当时状态。
经 2026-09-02 核查 `effectExecutor.cjs` / `buffManager.cjs`，**路径 B 已全部落地**：

| 项 | 文档标注(08-23) | 实测(09-02) | 证据 |
|---|---|---|---|
| B.1 位移三分法 | ⚠ 需扩展 | ✅ 已实现 | `effectExecutor.cjs` L1287-1309（`fixed6`/`relative`/`random`） |
| B.2 召唤 `spawn_unit` | ❌ 待新增 | ✅ 已实现 | 返回 `unit_id/q/r/role/faction` |
| B.3 词条联动 `trigger_entry` | ❌ 待新增 | ✅ 已实现 | 含递归护栏 `entryCallStack` + `depth≥2` 阻断 |
| B.4 目标解析 `resolve_target` | ⏳ 待实现 | ✅ 已实现 | 返回 `targets/count/unit_ids` |
| B.5 驱散 `remove_buff` | ❌ 空壳 | ✅ 已实现 | 真实遍历移除，含 `removeBuff.b5.test.cjs` |
| B.6 状态叠加 pipe | ⚠ 扩展待做 | ✅ 已实现 | `buffManager.cjs:451 calculateEffectiveStat` |
| COST 三原子 | ⏳ 待补 | ✅ 已实现 | `handleCostAp` / `handleCostEnergy` 真实扣减（下限 0） |
| **B.0 契约对齐** | 待做 | ❌ **未完成** | `shared-kernel/.../phase-tree.contract.ts` **L26** 仍是 `['WHEN','IF','WHO','ROLL','DO','COST']` |

### 0.4 用户拍板决策（`原子改造方案` L234-239）

1. **路径 A+B 全做，先 A 后 B 紧接**
2. **空壳 handler 直接升级为真实实现**（`remove_buff` / `spawn_items`→`spawn_unit` 无外部依赖，直接改写）
3. **校验器接口认可 + Gateway 启动预注入**（加载 `glossary-skill-config.json` 后调 `setEntryValidator`）
4. 记录文档路径确认为 `docs/原子记录-20260823.md`

**实施顺序**（L241-247）：契约对齐 → 路径A注册 → 路径B实现（B.1→B.4→B.3→B.2→B.5→B.6→B.7）→ Gateway 预注入 → 测试+记录

> ⚠️ **唯一明确未落地的是第 1 步「契约对齐」** —— `shared-kernel` 的 `PhaseEnum` 至今仍是含 `WHO`、无 `AFTER` 的旧值，
> 且其 `dist/` 产物同步为旧值（`dist/contracts/phase-tree.contract.js:24`）。

---

## 一、五份定义的出处清单

### ① 后端真相源 · 原子元数据（无段位）

| 项 | 值 |
|---|---|
| **路径** | `services/combat-service/src/services/combatCore/atomRegistry.cjs` |
| **规模** | 73 个 `this.register(...)`，去重后 **54 个 handlerKey** |
| **导出** | `listMeta()`（L114-123）/ `list()`（L106-108）/ `setEntryValidator()`（L131） |
| **标识体系** | 语义名 → handlerKey，如 `D1_direct_damage` → `direct_damage` |
| **自述** | L111-112 注释：「这是"语义底座完备态"的单一真相源——前端编辑器 dropdown/表单参数全部由此派生」 |

**段位标注分布**（仅 9 个原子带 `【段位】` 前缀，其余未标）：

| 标注 | 数量 | 原子 |
|---|---|---|
| `【AFTER】` | 4 | 见下表 |
| `【COST】` | 3 | `C_AP_COST` / `C_ENERGY_COST` / `C_DURABILITY_COST` |
| `【DO】` | 2 | `B_TARGET`(目标解析) / `B_SPAWN`(召唤生成) |

**4 个【AFTER】原子**（L466-503）：

| 语义名 | handlerKey | 说明 | params |
|---|---|---|---|
| `B_ENTRY` | `trigger_entry` | 词条联动（含递归护栏 depth≤2） | `entry_id`(必填) / `entry_type` |
| `B_HIT_CALLBACK` | `trigger_entry` | 命中回调（复用 trigger_entry，`on` 区分时机） | `entry_id`(必填) / `on`(hit\|kill) |
| `B_KILL_STACK` | `charge_layer` | 击杀叠层（复用 charge_layer 语义） | `layer_key`(必填) / `amount`(必填) |
| `B_CLEANUP` | `remove_buff` | 结算清理/驱散（跳过 undispellable） | `buff_key` / `scope`(self\|target) |

> 注：`B_HIT_CALLBACK` 与 `B_KILL_STACK` **复用**已有 handlerKey，故 73 → 去重后仅 54。

---

### ② 前端原子面板硬编码（两副本一致）

| 项 | 值 |
|---|---|
| **路径 A** | `frontend/src/views/GlossaryStudio.vue` **L1624-1757** |
| **路径 B** | `frontend/src/views/GlossaryHubNew.vue` **L1624-1757** |
| **规模** | **90 个原子**，13 组（A-K + W + X），区块 134 行 |
| **两份关系** | 经逐行比对 **完全一致**（90 原子、134 行、零差异） |

原子结构：`{ grp, key, label, effectType, st, fields[] }`

顶部自我约束（L1620-1623）：

```
★ effectType 必须是引擎 effectExecutor handler 真名（或其已注册别名），
  写回词条时直接作为 effect.type，禁止再用 UI 分类名（status/damage/meta/cost）。
```

---

### ③ ✅ 前端六段骨架（现行正确版本）

| 项 | 值 |
|---|---|
| **路径** | `frontend/src/views/GlossaryStudio.vue` **L1055-1065** |
| **另一副本** | `GlossaryHubNew.vue` L1055-1065（**内容相同**） |

```js
// 六段固定骨架（对齐设计稿 / 2026-08-23 规范）：WHEN→IF→ROLL→DO→AFTER→COST
// WHO(主语/目标解析) 已并入 DO 段内首原子，不再作为独立主段。
// 存量词条 d.target.type 在 initLanesFromDraft 投影为 DO 段首原子，落库时反向写回。
const LANE_SKELETON = [
  { key: 'WHEN',  name: 'WHEN 时机',  desc: '触发闸门',          grp: 'A' },
  { key: 'IF',    name: 'IF 条件',    desc: '前置判定',          grp: 'B' },
  { key: 'ROLL',  name: 'ROLL 掷骰',  desc: '随机/分支',         grp: 'D' },
  { key: 'DO',    name: 'DO 执行',    desc: '效果堆叠/目标解析', grp: 'F' },
  { key: 'AFTER', name: 'AFTER 后效', desc: '延时生效/回合末结算', grp: 'G' },
  { key: 'COST',  name: 'COST 代价',  desc: 'AP/冷却',           grp: 'E' }
]
```

**这就是你说的「有 AFTER 段」的正确版本。**

各段原子派生逻辑（`deriveLaneAtoms`，L1067-1116）：

| 段 | 派生来源 | 生成原子 grp |
|---|---|---|
| WHEN | `d.timing.trigger` / `d.trigger.type` | `A` |
| IF | `d.conditions[]` | `B` |
| ROLL | `d.roll.mode` | `D` |
| DO | **首原子**：`d.target.type` → 目标解析（`effectType: 'target_selection'`） | `C` |
| DO | **其余**：`d.effects[]` | `F` |
| AFTER | `d.after.effects[]` | `G` |
| COST | `d.cost.ap` / `d.ap_cost` | `E` |

---

### ④ ❌ HEXSIX_LEGEND —— 两副本分歧（本次新发现）

六边形蜂窝图例，两副本**不一致**：

| 文件 | 行号 | 内容 | 判定 |
|---|---|---|---|
| `GlossaryStudio.vue` | **L766** | `['WHEN','IF','ROLL','DO','AFTER','COST']` | ✅ 有 AFTER，与 LANE_SKELETON 一致 |
| `GlossaryHubNew.vue` | **L766** | `['WHEN','IF','WHO','ROLL','DO','COST']` | ❌ 有 WHO / 无 AFTER，**旧版残留** |

**关键**：两个文件的 `LANE_SKELETON` 注释**都已更新为新六段**（含 AFTER、WHO 已并入 DO），
但 `GlossaryHubNew.vue` 的 `HEXSIX_LEGEND` **漏改** —— 仍停留在旧六段。

→ 即 HubNew 的六边形蜂窝 UI 会渲染出 `WHO` 格，与它自己的泳道（AFTER）**对不上**。

---

### ⑤ ❌ 文档权威定义（已过期）

| 项 | 值 |
|---|---|
| **路径** | `docs/phase-tree-六段式合并方案.md` |
| **L33** | `PhaseEnum = ['WHEN','IF','WHO','ROLL','DO','COST']` |
| **L21** | 「六段有序：`WHEN → IF → WHO → ROLL → DO → COST`」 |
| **自述** | L3-5：「本文档是…**唯一真相源**…遍历器、归一化、可视化编辑器均以本文档为准」 |

**矛盾**：该文档自称唯一真相源、且声明旧文档已被取代，
但它定义的六段**无 AFTER、有 WHO**，与现行 `LANE_SKELETON`（有 AFTER、无 WHO）**直接冲突**。

判断：该文档写于 2026-08-19，而 `LANE_SKELETON` 对齐的是 **2026-08-23 规范**（更新），
故**文档已落后于代码**，其「唯一真相源」声明已失效。

> 另：文档 L66 引用的 `67原子引擎...md` —— 仓库内确实不存在，
> 但**实存于工作区根目录** `67原子引擎落地与编辑器词库实施计划-2026-08-08.md`（462 行）。
> （此前判定"缺失"是因只搜了仓库内目录。）

---

### ⑥ ❌ 我的产物：8 段 PHASES（**错误，我编造的**）

| 项 | 值 |
|---|---|
| **路径** | `frontend/src/battle/glossary/atomGroups.js` |
| **内容** | `PHASES = [PREP, COST, CHANNEL, WHEN, IF, ROLL, DO, AFTER]`（8 段）+ `GRP_TO_PHASE` 映射 |

**必须坦白**：这两个常量**不是从前端提取的**，是我上次新建 `atomGroups.js` 时**凭空编写的**，
仓库中**无任何出处**。经核查，它与所有现存定义都不符：

| 我的 8 段 | 真相六段 |
|---|---|
| `PREP` | ❌ 不存在 |
| `COST` | ✅ 存在，但在**末位** |
| `CHANNEL` | ❌ 不存在 |
| `WHEN` | ✅ |
| `IF` | ✅ |
| `ROLL` | ✅ |
| `DO` | ✅ |
| `AFTER` | ✅ 存在，但在 **COST 之前** |

正确顺序应为：`**WHEN → IF → ROLL → DO → AFTER → COST**`

---

## 二、原子层差异（后端 73 ↔ 前端 90）

以 `handlerKey ↔ effectType` 对齐：

### 引擎已注册、编辑器无入口（7 个）

| handlerKey | 语义名 | 说明 | 影响 |
|---|---|---|---|
| **`cost_ap`** | `C_AP_COST` | 【COST】AP 代价，amount≥1 | **玩家无法给词条加"消耗AP"** |
| **`cost_energy`** | `C_ENERGY_COST` | 【COST】能量代价 | **无法加"消耗能量"** |
| `cost_durability` | `C_DURABILITY_COST` | 【COST】耐久代价 | 前端仅 `durability_consumption`，key 不同 |
| `resolve_target` | `B_TARGET` | 【DO】目标解析，写 `context.resolvedTargets` | 前端用 `target_selection`，**引擎可能不认** |
| `spawn_unit` | `B_SPAWN` | 【DO】召唤/生成单位+地形 | 前端 H6 叫 `spawn_items`，**命名分歧** |
| **`trigger_entry`** | `B_ENTRY` | 【AFTER】词条联动（递归护栏 depth≤2） | **AFTER 段无"词条联动"入口** |
| `_modifier` | `M_SCOPE`/`M_FACTION` | 修饰标记 | 正常（非独立原子） |

### 编辑器有、后端未注册（6 个）

| effectType | 性质 |
|---|---|
| `trigger` | A 组 13 个原子全用 —— **UI 分类名** |
| `condition` | B 组多个 —— **UI 分类名** |
| `roll_segment` | C 组 8 个 —— **UI 分类名** |
| `target_selection` | W 组 6 个（后端名 `resolve_target`） |
| `spawn_items` | H6（后端名 `spawn_unit`） |
| `custom` | X 组自定义 |

> ⚠️ 前 3 个**违反了 atomGroups 顶部自己声明的红线**（L1620-1623「禁止再用 UI 分类名」）。

---

## 三、待你确认的 3 个疑点

### 疑点 1：ROLL 段的 grp 是 `D`，但 D 组是"伤害族"？—— **已查明，确为错误**

`LANE_SKELETON` 标注 `ROLL → grp: 'D'`。经实测 `atomGroups` 各组实际内容：

| 组 | 数量 | 内容 | 应属段 |
|---|---|---|---|
| **C** | 9 | `roll_none` / `roll_segments` / `roll_segments_m` / `duel_resolution` / `damage_bonus_dice` / `roll_segments_color` / `roll_check_compare_to` / `manual_roll` / `luck_resolution` | **ROLL** |
| **D** | 9 | `direct_damage` / `damage_reduction` / `direct_damage_split` / `damage_share` / `instant_kill` / `armor_pierce` / `reflect_damage` / `duel_resolution_k` / `modify_stat_delay` | **DO** |

→ **C 组才是掷骰/判定类，D 组是伤害族**。故 `ROLL → grp:'D'` **确为错误**（应为 `C`）。

同理复核其余段：

#### 澄清：`LANE_SKELETON.grp` 与 `GRP_TO_PHASE` 是**两套独立映射**

| 常量 | 位置 | 用途 | 正确性 |
|---|---|---|---|
| `GRP_TO_PHASE` | L1480（两副本逐字一致） | **实际归段映射**：原子组 → 段 | ✅ **正确** |
| `LANE_SKELETON.grp` | L1059-1065 | 段**默认展示组**，仅供 `createEmptyLanes()` 初始化，**不参与归段** | ⚠️ 见下 |

```js
GRP_TO_PHASE = { A:'WHEN', B:'IF', C:'DO', D:'DO', E:'COST', F:'DO', G:'AFTER', H:'DO', I:'DO', J:'DO', K:'COST' }
```
`A→WHEN`、`B→IF`、`E→COST`、`G→AFTER` **全部正确**；实际归段由 `phaseOfEffect(effectType, grp)` 执行
（优先 `effectType`，回退 `grp`），**归段逻辑无误**。

> ⚠️ **修正**：此前「`LANE_SKELETON.grp` 整体不可靠、需按真相源重排」的结论**过重**，予以撤回。
> 真相源（`原子记录`）**未定义 `grp` 字段**，"按真相源重排"无依据。
> 实测各组内容后，实际只有内部命名直觉问题：

| 段 | 骨架 grp | 说明 |
|---|---|---|
| WHEN→`A` / IF→`B` / AFTER→`G` | | ✅ 合理 |
| ROLL→`D` | | ⚠️ 展示瑕疵：D 组是伤害族，ROLL 段默认组应是 C 组（掷骰）。**不影响归段结果** |
| DO→`F` | | ⚠️ 展示瑕疵：DO 主要应是 D 组（伤害族）。**不影响归段结果** |
| COST→`E` | | ⚠️ 展示瑕疵：E 组是资源回复而非代价。但归段映射 `E→'COST'` 本身正确 |

#### ⚠️ 但这一条仍是真问题（保留）

`deriveLaneAtoms` DO 段首原子（L1088）用 `effectType: 'target_selection'`，
而真相源（`原子记录` L13）与后端 `atomRegistry` 注册的 handlerKey 均为 **`resolve_target`**（语义名 `B_TARGET`）。

→ 前端写 `target_selection` 时**引擎侧无对应 handler**，属**真实命名分歧，应改**。
（同类：`H6` 的 `spawn_items` vs 后端 `spawn_unit`。）

### 疑点 2：docs 与代码，谁是真相源？

- `docs/phase-tree-六段式合并方案.md` 自称唯一真相源 → `WHEN/IF/WHO/ROLL/DO/COST`
- `GlossaryStudio.vue` LANE_SKELETON 对齐 2026-08-23 规范 → `WHEN/IF/ROLL/DO/AFTER/COST`

按你的线索（有 AFTER 才对），**代码胜出**。但文档需要同步更新或标注废弃，否则后人还会踩坑。

### 疑点 3：HubNew 的定位？

`GlossaryHubNew.vue` 与 `GlossaryStudio.vue` 的 `atomGroups` 完全一致（90 原子），
但 `HEXSIX_LEGEND` 漏改。是否直接删掉 HubNew 这份副本（同此前前端重构的收敛方向）？

---

## 四、修正建议（按优先级）

> 依据：`docs/原子改造方案-A+B-20260823.md` 第 0 节（用户 2026-08-23 拍板 + 2026-09-02 实测落地状态）。

| # | 动作 | 目标 | 依据 |
|---|---|---|---|
| **1** | **契约对齐（B.0 第 1 步，唯一明确未落地项）**：`shared-kernel/src/contracts/phase-tree.contract.ts` **L26** `PhaseEnum` 删 `WHO` 插 `AFTER`，并 `npm run build` 同步 `dist/` | 消除契约层与代码/真相源的三方不一致 | 改造方案 L68-72 |
| 2 | **修正 `atomGroups.js` 的 `PHASES`** → 六段 `WHEN/IF/ROLL/DO/AFTER/COST`，删掉我编造的 `PREP`/`CHANNEL` | 消除我引入的错误 | 原子记录 L11 |
| 3 | **按真相源重排 `LANE_SKELETON.grp`**：`ROLL→C`、`COST→代价类`、DO 首原子改 `resolve_target`（现误为 `target_selection`/grp `C`） | 修正已查明的 3 处错误 | 见疑点 1 |
| 4 | **同步 `GlossaryHubNew.vue` L766 `HEXSIX_LEGEND`** → 补 AFTER、去 WHO | 消除副本漂移 | 本次新发现 |
| 5 | **标注 `phase-tree-六段式合并方案.md` 与旧 `PhaseEnum` 已过期** | 避免后人踩坑 | 与真相源冲突 |
| 6 | **补前端原子入口**：`cost_ap` / `cost_energy` / `cost_durability` / `trigger_entry` / `resolve_target` / `spawn_unit` | **后端 handler 已全部真实实现**（09-02 实测），补前端即立即可用，解锁"消耗 AP/能量""词条联动""目标解析""召唤" | 见 0.3 节 |
| 7 | **前端改消费 `getAtomMeta()`**（`api/client.js` 已封装，但**全前端无人调用**） | 根治前后端漂移 | 后端自述单一真相源 |

---

## 附：核查命令备查

```bash
# 后端原子数与段位标注
grep -c "this\.register(" services/combat-service/src/services/combatCore/atomRegistry.cjs   # 73
grep -o "【[A-Z]*】" .../atomRegistry.cjs | sort | uniq -c        # AFTER×4 COST×3 DO×2

# 两副本 atomGroups 一致性（90/90，零差异）
sed -n '1624,1757p' frontend/src/views/GlossaryStudio.vue   > /tmp/a
sed -n '1624,1757p' frontend/src/views/GlossaryHubNew.vue   > /tmp/b
diff /tmp/a /tmp/b

# 六段骨架（两副本 LANE_SKELETON 相同）
sed -n '1055,1065p' frontend/src/views/GlossaryStudio.vue

# HEXSIX_LEGEND 分歧（本次发现 → 已于 09-02 修复，现两副本一致）
sed -n '766p' frontend/src/views/GlossaryStudio.vue    # ...'ROLL','DO','AFTER','COST'
sed -n '767p' frontend/src/views/GlossaryHubNew.vue    # ...'ROLL','DO','AFTER','COST'  ← 已同步

# 六段 WHO→AFTER 全线校验（09-02 修复后应全部一致且无 WHO 残留）
grep -rn "'WHO'" --include="*.ts" --include="*.js" --include="*.vue" --include="*.cjs" . | grep -v node_modules | grep -v "/dist/"   # 应为空
grep -o "PhaseEnum = z.enum(\[[^]]*\])" shared-kernel/src/contracts/phase-tree.contract.ts shared-kernel/dist/contracts/phase-tree.contract.js
grep -o "const ORDER = \[[^]]*\]" services/combat-service/src/services/combatCore/skillExecutor.cjs
grep -o "const PHASES = \[[^]]*\]" backend-gateway/src/routes/glossary.ts
```

---

## 五、执行记录（2026-09-02 已实施）

### 5.1 归档 + 过期戳

| 文件 | 处理 |
|---|---|
| `mecha-universe-engine/docs/phase-tree-六段式合并方案.md` | → `历史开发报告/归档-六段式与语义原子/`，加盖「过时文档 · 已归档」戳（含「自述唯一真相源已失效」警示） |
| `mecha-universe-engine/docs/skill-hierarchy-主谓宾定状补.md` | → 同目录，加盖**二次过期**戳（其 08-08 戳内引用的六段 `WHEN/IF/WHO/ROLL/DO/COST` 亦已过期） |
| `mecha-universe-engine/docs/结算管线-原子触发时机对齐表.md` | **不归档** —— 全文无段位定义、无更高版本取代，仅加状态说明（属结算管线领域，仍有效） |

> 归档目录沿用既有惯例：顶部 4 行过期戳 + `历史开发报告/归档-<主题>/`。

### 5.2 真相源就地更新（不归档，仅状态列过时）

- `docs/原子记录-20260823.md`：新增「✅ 2026-09-02 状态复核」章节；二节表格 11 行 `❌/⏳/⚠` → `✅ 已实现（09-02）`
- `docs/原子改造方案-A+B-20260823.md`：新增「✅ 2026-09-02 落地复核」，标明**仅剩第 ① 步契约对齐未完成**

### 5.3 代码修正（六段 `WHO`→`AFTER` 全线对齐）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `shared-kernel/src/contracts/phase-tree.contract.ts` **L26** | `PhaseEnum` 删 `WHO` 插 `AFTER` + 补依据注释；**已 `npm run build` 同步 `dist/`** |
| 2 | `services/.../skillExecutor.cjs` **L1507** | `ORDER` 由 7 段（`WHO`+`AFTER` 并存）→ 六段；L1515 `phase === 'WHEN' \|\| 'WHO'` → 仅 `'WHEN'` |
| 3 | `backend-gateway/src/routes/glossary.ts` **L229** | `PHASES` 删 `WHO` 插 `AFTER` —— **修复 AFTER 段节点被 `?:'DO'` 强制归为 DO 的 bug** |
| 4 | `backend-gateway/scripts/migrate-phase-tree.cjs` **L22** | `LANE_SKELETON` 删 `WHO` 插 `AFTER` |
| 5 | `frontend/.../GlossaryHubNew.vue` **L766** | `HEXSIX_LEGEND` 删 `WHO` 插 `AFTER`（与 Studio 对齐） |
| 6 | `frontend/.../GlossaryHubNew.vue` **L1396** | `LANE_LEGAL_ORDER` 删 `WHO` 插 `AFTER`（与 Studio 对齐） |
| 7 | `frontend/.../GlossaryHubNew.vue` **L226** | 用户可见提示文案 → `六段式（WHEN/IF/ROLL/DO/AFTER/COST）` |
| 8 | 两副本 L1457 / L1489 注释 | 段位枚举注释订正（保留"原 WHO 段"等合理历史语境） |

**安全前提（已验证）**：存量词条 `glossary-skill-config.json` 中 `WHO` 节点数 = **0**（仅 `DO`×9、`WHEN`×9），删除无数据风险；
且 `skillExecutor` 原注释（L1503-1505）自述六段为「时机→条件→随机性→执行→后效→代价」**不含 WHO**，代码中留 `WHO` 系 08-23 插 AFTER 时的**漏删**。

### 5.4 验证结果

| 项 | 结果 |
|---|---|
| 全仓 `'WHO'` 字面量 | ✅ 归零 |
| `shared-kernel` src ↔ dist | ✅ 一致 |
| 后端 `PHASES` / 引擎 `ORDER` / 迁移脚本 | ✅ 三处一致 |
| 两副本 `HEXSIX_LEGEND` / `LANE_LEGAL_ORDER` | ✅ 一致 |
| `backend-gateway` `tsc --noEmit` | ✅ **exit 0** |
| 改动文件 lint | ✅ **0 错误** |

### 5.5 关于「8 段 PHASES」—— 澄清

经复查，`PREP` / `CHANNEL` 在全前端代码中**搜索结果为 0**：该 8 段**从未写入任何代码文件**，
仅存在于本报告初版的描述中，**未污染代码库，无需修正**。

实际共享内核 `frontend/src/battle/glossary/laneSkeleton.js` 经核查**完全正确**：
`LANE_SKELETON` 六段 = `WHEN→IF→ROLL→DO→AFTER→COST`，`GRP_TO_PHASE` 与前端 L1480 逐字一致，注释均标注来源行号。

### 5.6 未执行（待决定）

| # | 事项 | 说明 |
|---|---|---|
| 1 | 补前端 6 个原子入口 | `cost_ap` / `cost_energy` / `cost_durability` / `trigger_entry` / `resolve_target` / `spawn_unit` —— 后端 handler **已全部真实实现**，补前端即立即可用 |
| 2 | 命名对齐 | `target_selection` → `resolve_target`、`spawn_items` → `spawn_unit`（现写法引擎侧无对应 handler） |
| 3 | 前端改消费 `getAtomMeta()` | 根治前后端漂移（长期项，`api/client.js` 已封装但无人调用） |
| 4 | 部署上线 | 按既有纪律：服务器 `shared-kernel` → `backend-gateway` → `frontend` 依次 `npm run build` 后重建容器 |
