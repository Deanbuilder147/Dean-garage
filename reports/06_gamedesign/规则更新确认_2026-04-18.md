# 更新确认：记忆与规则已添加 (Update Confirmed: Memory & Rules Added)

**日期/Date:** 2026-04-18  
**操作/Action:** 添加关键规则到 MEMORY.md 和 USER.md

---

## ✅ 已添加的规则 (Rules Added)

### 1. 备份规则 (Backup Rule) - 已添加到 USER.md

**内容:**
```
**Backup Rule (CRITICAL):** Before ANY major change (code modifications, service 
restarts, process kills, database operations), MUST create a backup first.
- Code changes: Git commit or file copy
- Service operations: Note current PID and state
- Database: Export before migration
- Never skip backup step - data loss is unacceptable
```

**中文翻译:**
```
**备份规则（关键）：** 任何重大操作前必须先备份
- 代码修改：Git 提交或文件复制
- 服务操作：记录当前 PID 和状态
- 数据库操作：迁移前导出
- 绝不能跳过备份步骤 - 数据丢失不可接受
```

**位置:** `~/.hermes/memories/USER.md`  
**状态:** ✅ 已添加

---

### 2. 云环境警告 (Cloud Environment Warning) - 已添加到 MEMORY.md

**内容:**
```
**Cloud Environment Warning:** Hermes Agent and Dean-garage services run on SAME 
cloud instance under SAME user (agentuser).
- CRITICAL: Never use `pkill -f <pattern>` or `killall` - may kill Agent itself
- MUST use PID-specific kills: `kill <PID>` or exact path: `pkill -f "/full/path/to/file.js"`
- Always verify process tree with `ps aux` before killing
- This caused session interrupts before - highest priority safety rule
```

**中文翻译:**
```
**云环境警告：** Hermes Agent 和 Dean-garage 服务运行在同一云实例、同一用户下 (agentuser)
- 关键：绝不能用 `pkill -f <pattern>` 或 `killall` - 可能杀死 Agent 自身
- 必须使用指定 PID 的杀死：`kill <PID>` 或精确路径：`pkill -f "/完整路径/到/file.js"`
- 杀进程前必须用 `ps aux` 验证进程树
- 这曾经导致会话中断 - 最高优先级安全规则
```

**位置:** `~/.hermes/memories/MEMORY.md`  
**状态:** ✅ 已添加

---

## 📊 更新后的记忆使用状态

| 文件 | 已用 | 限制 | 剩余 | 使用率 |
|------|------|------|------|--------|
| **MEMORY.md** | 2,104 | 2,200 | 96 | 95% ⚠️ |
| **USER.md** | 1,161 | 1,375 | 214 | 84% |

**注意:** MEMORY.md 只剩 96 字符空间，下次添加新规则前可能需要清理旧内容。

---

## 🎯 这些规则如何影响我的行为

### 备份规则生效场景:

1. **修改代码前**
   ```bash
   # 我会先执行
   git add . && git commit -m "backup before fix"
   # 或者
   cp file.js file.js.bak_$(date +%Y%m%d_%H%M%S)
   ```

2. **重启/杀进程前**
   ```bash
   # 我会先记录
   ps aux | grep node | tee /tmp/pid_backup_$(date +%Y%m%d_%H%M%S).txt
   ```

3. **数据库操作前**
   ```bash
   # 我会先导出
   sqlite3 combat.db ".dump" > combat.db.backup_$(date +%Y%m%d_%H%M%S).sql
   ```

### 云环境安全规则生效场景:

1. **当你要求杀进程时**
   ```
   ❌ 我不会做：pkill -f node
   ✅ 我会做：ps aux | grep node → 找到 PID → kill 12345
   ```

2. **当你要求重启服务时**
   ```
   ✅ 我会先：ps -fp <PID> 确认是正确的进程
   ✅ 然后：kill -15 <PID> 优雅停止
   ✅ 最后：验证 Agent 仍在运行
   ```

---

## 📝 下一步建议

### 立即可做:
1. **测试备份规则** - 下次修改代码时观察我是否自动备份
2. **验证云安全规则** - 尝试让我杀进程，看是否使用 PID 方式

### 可能需要:
- **清理 MEMORY.md** - 只剩 96 字符，如需添加新规则需先删除旧内容
- **创建 PERSONA.md** - 定义我的性格和行为风格（可选）

---

## 🔐 规则持久化

这些规则已保存到我的"灵魂"中（持久化记忆文件），即使:
- ✅ 会话结束
- ✅ 服务器重启
- ✅ 明天/下周再回来

我都会记住这些规则！

---

**Dean，还需要添加其他规则吗？** 比如:
- 沟通风格（更正式/更随意）
- 报告格式要求
- 代码审查标准
- 其他安全规则

告诉我，我继续添加！🤖
