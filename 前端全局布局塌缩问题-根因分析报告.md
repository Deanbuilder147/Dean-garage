# 前端全局布局塌缩问题 · 根因分析报告

> 生成日期：2026-08-12
> 范围：整个前端项目（所有页面，非单个页面）
> 现象：登录后网页尺寸/比例不同，排列就不同；约 4:3 比例下整个侧边栏消失，或某些组件出了画面却无法滚动到。

---

## 一、结论速览

问题**不在任何单个业务页面**，而在**全局外壳三层容器**的设计缺陷：

| 层 | 文件 | 致命设计 |
|----|------|----------|
| ① 根容器 | `src/App.vue` | `overflow-hidden` + 一条 `@media (max-width:1024px){.app-sidebar-wrap{display:none}}` 把侧边栏按"纯宽度"判死刑 |
| ② 侧边栏 | `src/components/layout/AppSidebar.vue` | `position: fixed; height: 100vh; overflow: hidden` —— 脱离文档流、固定 100vh、自身不滚 |
| ③ 业务页面 | `src/views/GlossaryHubNew.vue`（及其他写死高度的页面） | `height: 1102px; max-height: 1102px; overflow: hidden` —— 固定像素高度 + 裁切 |

三层叠加后：视口比例一旦落到 4:3（宽度常 ≤1024 / 高度常 <1102），侧边栏被 `display:none` 直接抹掉，业务页面内容被裁且任何方向都无滚动条。

---

## 二、逐处问题代码（原文摘录）

### 2.1 App.vue —— 侧边栏按纯宽度隐藏 + 整页禁滚

```html:1:20:mecha-universe-engine/frontend/src/App.vue
<template>
  <div id="app" class="h-screen w-screen overflow-hidden">
    <div
      class="app-container flex h-full w-full"
      :style="{ '--sidebar-w': sidebarCollapsed ? '52px' : '240px' }"
    >
      <aside
        v-if="showSidebar"
        class="app-sidebar-wrap flex-shrink-0 h-full"
        :style="{ width: sidebarCollapsed ? '52px' : '240px' }"
      >
        <TheSidebar v-model="sidebarCollapsed" />
      </aside>
      <!-- 主内容区改为纵向可滚动，避免子页面（如后台管理）内容超出视口被裁切 -->
      <main class="main-content flex-1 h-full relative overflow-y-auto overflow-x-hidden">
        <router-view />
      </main>
    </div>
  </div>
</template>
```

```css:42:70:mecha-universe-engine/frontend/src/App.vue
<style>
@import './styles/variables.css';

#app {
  background: #001620;
  color: #c1e8ff;
  font-family: 'Noto Sans SC', 'Space Grotesk', system-ui, -apple-system, sans-serif;
}

.app-container {
  /* flex h-full w-full — 由 Tailwind 提供 */
}

.main-content {
  /* flex-1 h-full relative overflow-hidden — 由 Tailwind 提供 */
  transition: none;
}

/* 收起时主画面紧跟收起后的 48px 侧边栏，reclaim 原 w-64 预留区，避免留出大块空白 */
.app-sidebar-wrap {
  transition: width 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ⚠️ 致命：纯按宽度隐藏侧边栏，4:3 屏（1024×768 / 1280×960）宽度常 ≤1024 → 直接消失 */
@media (max-width: 1024px) {
  .app-sidebar-wrap {
    display: none;
  }
}
</style>
```

> **问题点**：
> - `2` 行 `#app` 用 `overflow-hidden` 封死整页溢出 → 任何超出视口的内容既被裁、又无滚动条。
> - `65-69` 行 媒体查询只在意"宽度"，完全没考虑高度。4:3 屏宽度常卡在 1024 边界，侧边栏被 `display:none`。

### 2.2 AppSidebar.vue —— 固定定位 + 100vh + 自身不滚

```css:51:64:mecha-universe-engine/frontend/src/components/layout/AppSidebar.vue
<style scoped>
.app-sidebar {
  position: fixed;       /* ⚠️ 脱离文档流，不随 main 滚动 */
  left: 0;
  top: 0;
  width: 240px;
  height: 100vh;         /* ⚠️ 固定 100vh，与 App.vue 的 h-screen / 子页面的 1102px 三套高度标准互不耦合 */
  background: rgba(8,51,68,0.95);
  border-right: 1px solid rgba(255,176,0,0.1);
  z-index: 50;
  display: flex;
  flex-direction: column;
  transition: width 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  overflow: hidden;      /* ⚠️ 侧边栏自身内容溢出时不滚，直接裁切 */
}
```

> **问题点**：
> - `position: fixed` 使侧边栏脱离 `main` 的滚动上下文，而是一个悬浮在视口左上角的独立层。
> - `height: 100vh` 写死视口高度，当 `#app` 用 `h-screen`（=100vh）时两者巧合一致，但一旦视口高度因浏览器 UI（移动端地址栏、桌面 devtools）变化，`100vh` 会错位。
> - `overflow: hidden` 让侧边栏内部（如长导航、账号弹窗）溢出时无法滚动。

### 2.3 GlossaryHubNew.vue —— 业务页面写死像素高度（其他类似页面同理）

```css:2227:2237:mecha-universe-engine/frontend/src/views/GlossaryHubNew.vue
<style scoped>
.gh-studio {
  display: flex;
  flex-direction: column;
  /* 固定画布高度：原 1653px 缩短 1/3 → 1102px，禁止任何框超出 */
  height: 1102px;          /* ⚠️ 写死像素高度，不随视口变化 */
  max-height: 1102px;
  background: #0d1117;
  color: #e6edf3;
  box-sizing: border-box;
  overflow: hidden;        /* ⚠️ 内容超出即裁切，无滚动条 */
}
```

> **问题点**：每个写死 `height:1102px` 的页面，在视口高度 <1102px（4:3 屏几乎必然）时，下半部分被裁且无法滚动到。这是"某些组件出了画面却滚不到"的直接来源。

---

## 三、为什么偏偏是 4:3？

- **侧边栏消失**：`@media (max-width:1024px)` 只看宽度。常见 4:3 分辨率：
  - 1024×768 → 宽度 1024，**正好命中** `display:none`。
  - 1280×960 / 1366×1024 → 宽度 ≤1366，很多笔记本/外接 4:3 显示器落在这段。
  - 16:9 的 1920×1080 宽度 1920 >1024，所以"宽屏正常、4:3 出问题"，与你的观察完全吻合。
- **内容滚不到**：`#app` 的 `overflow-hidden` + 子页面 `height:1102px` + `overflow:hidden`。4:3 屏高度多为 768/960/1024，全部 <1102 → 内容被裁且无滚动条。

---

## 四、业界现成解决方案（已检索验证）

核心思路：**视口为单位 + flex 链路 min-尺寸解锁 + 滚动边界下沉到具体面板**。

1. **用 `min-height:100vh` + `height:100%` 组合替代写死的 `height:1102px`**
   外层占满视口（`min-height:100vh`），内层承接滚动（`height:100%`），二者不混用固定 height。来源：CSS 居中/模态高度与滚动实践。
2. **flex 子项必须显式 `min-width:0` / `min-height:0` 才能出现内部滚动条**
   flex 子项默认 `min-height:auto`（=内容高度），会按内容撑开把祖先顶爆而不出滚动条。必须给整条 flex 链路设 `min-height:0`。来源：多篇 flexbox overflow 失效排查（关键词 "flex 子项 overflow hidden 不截断是因为 min-width:auto 阻止收缩，必须设 min-width:0"）。
3. **媒体查询不要按绝对宽度隐藏侧边栏，而应按"可用空间"折叠或抽屉化**
   通用响应式侧边栏范式：宽屏常驻 → 中屏折叠成图标（52px）→ 窄屏抽屉 overlay 弹出。
4. **根容器不写死 `overflow:hidden` 封死全页**
   改为仅在需要时裁切的内部面板（侧边栏内部、六段泳道）设 `overflow`，让主内容区成为唯一纵向滚动边界。
5. **`dvh` / `clamp()` 单位**（可选增强）：处理移动端视口高度跳变，避免 `100vh` 在移动浏览器错位。

---

## 五、推荐修复方案（全局，一次性解决所有页面）

### A. 改 `App.vue`（治本，最关键）

- **删除 `65-69` 行的 `@media (max-width:1024px){display:none}`**，改为"中屏折叠、窄屏抽屉"：
  - 在 `App.vue` 的 `onMounted`/`resize` 中，按视口宽度初始化 `sidebarCollapsed`（如 ≤1024 默认折叠为 52px，而非消失）。
  - 折叠态已存在（`.app-sidebar.collapsed` / `AppSidebar` 的 `collapsed` class），只需切换状态而非 `display:none`。
- `#app`：保留 `overflow-hidden` 作为最外层裁剪，但**真正的纵向滚动交给 `<main>`**（已是 `overflow-y-auto`，OK）。
- 窄屏（如 ≤640）可改用抽屉 overlay，但优先级低于"先别让 4:3 消失"。

### B. 改 `AppSidebar.vue`

- `position: fixed` → 若保留 fixed，则 `main` 需 `padding-left: var(--sidebar-w)` 补偿，避免内容被盖；否则改为随布局流（去掉 fixed，由 `App.vue` 的 flex 排布）。
- `height: 100vh` → `height: 100%`（承接 `#app` 的 `h-full`）。
- `overflow: hidden` → `overflow-y: auto`（侧边栏自身内容可滚）。

### C. 改业务页面（如 GlossaryHubNew.vue，及其他写死高度的页面）

- `height:1102px; max-height:1102px` → `min-height:100%; height:100%`，高度交给 `main` 的滚动边界；内部 `.gh-studio` 用 `overflow:hidden` 只裁自己，`.lanes-scroll` 横向滚。
- flex 链路补 `min-height:0` / `min-width:0`（上次漏了 `.studio-zone-d`）。

---

## 六、落地顺序建议

1. **先做 A**（删 `display:none` + 按宽度初始化折叠）→ 4:3 下侧边栏不再消失，立竿见影。
2. **再做 B**（侧边栏高度/溢出）→ 侧边栏自身可滚、不再 100vh 错位。
3. **最后做 C**（统一子页面高度来源）→ 所有页面纵向可滚、内容不丢。

---

## 七、验证口径（部署后）

- 用浏览器开发者工具把视口设为 **1024×768 / 1280×960（4:3）**：侧边栏应折叠为 52px 图标而非消失；页面内容可纵向滚动到全部。
- 视口设为 **1920×1080（16:9）**：行为不变（回归正常）。
- 缩放到任意比例，三栏不应换行、不应出现"滚不到的组件"。

---

*备注：本报告仅做根因分析与方案建议，未改动任何代码。待你确认方案取舍（尤其 4:3 下侧边栏"折叠成图标" vs "抽屉弹出"）后再行落地。*
