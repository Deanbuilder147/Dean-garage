# 前端功能与控件规格说明

> 适用项目：`mecha-universe-engine/frontend`（Vue3 + Tailwind，琥珀深色主题 `#001620` / `#ffb000`）
> 文档定位：本文档只描述**「页面需要哪些功能」以及「每个功能对应使用哪种按键 / 展示菜单 / 控件」**，不涉及任何机体位置、坐标等仿真内部细节。
> 更新日期：2026-08-24（基于原《前端尺寸与按钮规格说明》重构）

---

## 一、全局导航与框架功能

| 功能 | 用途 | 对应按键 / 展示菜单 |
|------|------|---------------------|
| 页面导航 | 在首页 / 词条库 / 战斗 / 终端 / 账号之间切换 | 左侧边栏菜单项（点击切换路由）；菜单项含图标(20×20) + 文字(11px) |
| 账号操作 | 查看 / 退出账号 | 头像(36×36)点击 → 弹出账号弹窗(宽 340px，最大 90vw) |
| 侧边栏形态切换 | 宽屏常驻、中屏省空间、窄屏不占主区 | 宽屏(>1280)：展开态 240px；中屏(1024~1280)：图标折叠态 52px；窄屏(<1024)：汉堡抽屉 240px（遮罩点击关闭） |
| 登录 / 注册 | 进入系统 | 登录页全宽主按钮（padding 14px，100% 宽，13px/700） |

---

## 二、词条库编辑功能（核心区）

| 功能 | 用途 | 对应按键 / 展示菜单 |
|------|------|---------------------|
| 词条选择 / 切换 | 在词条库中挑选要编辑的词条 | 左栏词条列表（点击选中高亮） |
| 词条只读预览 | 查看当前词条的只读信息 | 左栏底部只读卡片（定高可滚） |
| 六段式编排（WHEN / IF / ROLL / DO / AFTER / COST） | 编辑技能六段逻辑链 | 中栏上部编排区：每段一张卡片，含<br>① 段头（拖拽排序、点击选中）<br>② 原子拖入区<br>③ 「＋ 子段」按钮（挂一层子树）<br>④ 保存按钮（写回词条） |
| 六段式可视化画布 | 直观查看六段结构 / 选中 / 缩放 | 中栏下部画布区（高 420px，可滚动兜底）：<br>① 顶部 Tab 切换：六段式 / 射程 / AOE / 地图炮 / 战场模拟<br>② 画布内：点击选段、拖拽平移、滚轮缩放<br>③ 右上角色板图例（黑/青/橙/蓝/黄/深红/灰） |
| 原子 / 素材面板 | 提供可拖入的原子素材 | 右栏原子面板（宽 220px）：分类列表 + 搜索 / 筛选菜单；点击或拖拽添加 |
| 保存词条 | 提交编辑结果 | 保存按钮（琥珀主按钮 `.btn-save`，全大写，渐变 `#ffb000→#ff8a00`） |
| AI 辅助生成 | 由 AI 生成词条草稿 | AI 导入按钮（深紫渐变 `.btn-ai-import`） |
| 文件上传 | 上传素材 / 配置 | 上传按钮（天蓝 `.btn-upload`） |

---

## 三、战斗视图功能

> 本节只描述「玩家需要做的操作」与「对应的控件」，不描述任何机体位置 / 坐标。

| 功能 | 用途 | 对应按键 / 展示菜单 |
|------|------|---------------------|
| 视角缩放 / 复位 | 调整战场可视范围 | 顶部工具栏按钮：放大 / 缩小 / 1:1（约 8px 12px） |
| 结束回合 | 结束当前回合 | 工具栏「结束回合」按钮 |
| 掷骰结算 | 进行随机判定 | 掷骰按钮（胶囊形）→ 弹出骰子面板：roll / 确认 / 取消 |
| 方向选择（AOE 等） | 指定技能作用方向 | 六向罗盘按键（30×30 圆形，六向箭头 ↗→↘↙←↖ + 自动◎，环绕布局） |
| 技能目标选择 | 选定技能作用对象 | 战术技能卡（约 132×120，点击选目标） |
| 部署 / 行动 | 布置与执行战斗行动 | 右侧行动面板（宽 220px） |
| 阵营 / 队伍查看 | 查看双方阵容 | 阵营列表抽屉（左 / 右浮动，宽 210~220px） |
| 战斗信息查看 | 查看日志与属性 | 底部日志 / 属性条（高 230px） |

---

## 四、移动端触控功能

| 功能 | 用途 | 对应按键 / 展示菜单 |
|------|------|---------------------|
| 导航 | 切换页面 | 汉堡抽屉（240px 浮层，默认收起） |
| 画布操作 | 平移 / 缩放 / 点选 | 单指拖动平移、双指捏合缩放、点按选择（Tap-Slop 8px） |
| 信息展示 | 显示战斗状态 | 底部 HUD（固定，含标题 / 状态文字，穿透点击） |

---

## 五、通用按钮类型与用途

> 所有子页面复用以下按钮规格（单一真相源：琥珀 / 暖灰主题）。

| 按钮类型 | 用途场景 | 内边距 | 字号 | 圆角 | 视觉 |
|----------|----------|--------|------|------|------|
| 基础按钮 `.btn` | 通用操作 | 8px 16px | 11px/700 | 4px | 暖灰描边，hover 转琥珀 |
| 迷你 / 小按钮 `.btn-mini` `.btn-small` | 紧凑处操作 | 4px 10px | 10px | 4px | 小型变体 |
| 保存 / 主按钮 `.btn-save` `.btn.primary` | 提交、确认 | 8px 16px | 11px | 4px | 琥珀渐变 `#ffb000→#ff8a00`，全大写 |
| 删除 / 危险按钮 `.btn-delete` `.danger` | 删除操作 | 8px 16px | 11px | 4px | 红 `#ff6e81` |
| 强调按钮 `.btn-accent` | 次要强调 | 8px 16px | 11px | 4px | 紫渐变 |
| AI 导入按钮 `.btn-ai-import` | AI 辅助生成 | 8px 16px | 11px | 4px | 深紫渐变 |
| 上传按钮 `.btn-upload` | 文件上传 | 8px 16px | 11px | 4px | 天蓝 |
| 登录按钮 `.btn-login` | 登录主行动 | 14px | 13px/700 | 4px | 全宽 100% |
| 首页大按钮 `.btn-hero` | 首页主行动 | 14px 36px | 13px | 4px | 首页主按钮 |
| 卡片按钮 `.card-btn` | 卡片内操作 | 8px 18px | 11px | 4px | 卡片内嵌 |

---

## 六、通用输入与弹窗

| 控件 | 用途 | 规格 |
|------|------|------|
| 参数输入框 `.param-input` | 数值 / 文本输入 | padding 8px 10px，字号 12px，圆角 4px，focus 琥珀描边 + 外发光 |
| 参数下拉框 `.param-select` | 枚举选择 | 同输入框规格 |
| 模态弹窗 `.modal` | 确认 / 表单 / 详情 | 宽 90%，最大 480px，padding 24px，圆角 6px，z-index 1000 |
| 账号弹窗 `.profile-modal` | 账号信息 | 宽 340px（最大 90vw） |

---

## 七、字体 / 间距 / 圆角基准

- **字号**：xs 11px / sm 13px / base 14px / lg 16px / xl 20px / 2xl 24px
- **间距**：space-1 4px … space-8 32px
- **圆角**：全局默认直角 0px；按钮 4px、弹窗 6px 为局部设定（硬朗终端风）
- **强调色**：主色 `#ffb000`，危险 `#ff6e81`，信息青 `#00d4ec`

---

## 八、按键关联与联动关系

> 本节描述按键 / 菜单之间的**触发、互斥、前后置依赖与提交闭环**，弥补「功能—控件」单列表的不足。

### 8.1 触发 / 弹出关系（点 A → 出现 B）

| 触发按键 | 弹出 / 启用 | 关闭条件 |
|----------|------------|----------|
| 头像（36×36） | 账号弹窗（340px） | 弹窗内操作或点外部 |
| 掷骰按钮（胶囊） | 骰子面板（roll / 确认 / 取消） | 确认或取消后关闭 |
| 汉堡按钮（窄屏） | 侧边栏抽屉 + 遮罩 | 点遮罩或选中菜单项 |
| 原子面板（点击 / 拖拽原子） | 进入六段编排区对应段 | 拖入完成 |
| 「＋ 子段」按钮 | 在选中段上挂一层子树输入框 | 输入完成或取消 |

### 8.2 互斥 / 单激活关系（同组仅一个生效）

- **侧边栏三态**：展开(240) / 折叠(52) / 抽屉(240) 由屏宽决定，同一时刻仅一种形态。
- **画布 Tab**：六段式 / 射程 / AOE / 地图炮 / 战场模拟 **互斥切换**，仅一个高亮激活。
- **导航菜单项**：仅当前路由项 `active` 高亮。
- **六段编排选中**：点击某段头"选中"该段，点其他段切换选中（**单选**，用于联动画布高亮与右侧卡片滚动）。

### 8.3 前后置依赖（先 X 才能 Y）

| 前置 | 后续 | 说明 |
|------|------|------|
| 选词条（左栏） | 编辑其六段编排 / 画布 | 未选词条无可编辑对象 |
| 编辑六段（拖原子 / 子段） | 「保存」出有效结果 | 空编排保存无意义 |
| 选技能卡（战术技能卡） | 进入"选目标"状态 | 未选技能无法选目标 |
| 掷骰（roll） | 「确认」结算结果 | 确认依赖 roll 已执行 |
| AI 导入生成草稿 | 用户复查 → 再「保存」落地 | 草稿需人工确认 |

### 8.4 提交闭环

- **编辑链路**：选词条 → 编排 / 画布编辑 → 保存写回（或 AI 导入 → 复查 → 保存）。
- **战斗链路**：选技能 → 选目标 / 方向 → 掷骰 → 确认结算 → 结束回合。
- **危险操作保护**：删除按钮通常需二次确认弹窗后才生效。

### 8.5 响应式联动

- 屏宽变化 → 侧边栏形态自动切换（展开 ↔ 折叠 ↔ 抽屉），主内容区 reclaim 释放宽度。
- 移动端：侧边栏常驻改为汉堡抽屉；画布操作由鼠标点击改为单指平移 / 双指缩放 / 点按。

---

## 九、说明

1. 本文档已从「代码实现溯源」重构为「功能 → 控件」映射，便于设计与开发对齐。
2. **不包含机体位置 / 坐标 / 格点等仿真内部细节**，战斗部分仅描述玩家操作与对应控件。
3. 若后续样式改动，以实际组件主题为准；本文件聚焦功能与控件语义，不绑定具体类名。
