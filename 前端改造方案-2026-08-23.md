# 前端改造方案（2026-08-23 计划文档 · 待确认范围）

> 背景：后端路径 B 七项原子 + 集成层（atom-registry 接口、apply_buff 清理、block_movement 消费侧闭环）已全部上线 8081。
> 本文档**不写码**，仅盘点前端现状、列出候选改造方向与优先级，供确认后实施。

---

## 一、前端现状盘点（基于真实代码）

### 1.1 词条编辑器 `GlossaryHubNew.vue`（154KB，已部分对接 atom-registry）
- **原子面板**：`const atomGroups`（line 1608）**硬编码 58 个原子**（A 触发时机 / D 伤害 / E 资源 / F 状态 / G 行动 / H 地图炮 / I 视野隐匿 / J 装备 / K 结算改写 / B 非数值谓词 / C 消耗 / M 修饰），每个含 `grp/key/effectType/fields` 表单结构。
- **接口对接**：`loadConfig()`（line 543）已 `GET /combat-glossary/atom-meta`，把返回的 `atoms[].semantic` **回填**到 atomGroups 的 `a.semantic`（line 552-561）。保存时 `a.semantic` 优先作 type（line 1446-1454，方案 A 闭环）。
- **结论**：原子结构仍硬编码前端，`atom-meta` 仅补 semantic 名，**未完全动态化**。但语义名→引擎 key 翻译链路已通。
- **已知缺口**：前端接口路径是 `/combat-glossary/atom-meta`，网关新暴露的是 `/api/combat/atom-registry`。需确认两者是否同源（glossary.ts 是否代理/别名），否则存在双路径风险。

### 1.2 战斗界面
- `NewBattleView.vue`（211KB，PC 端）：巨型组件，复用 `HexGridCanvasEngine` + `drawBattleUnits` + `useBattleState`。已含移动/攻击/技能/结束回合交互。
- `MobileBattleView.vue`（13KB，移动端）：2026-08-20 重写为真战斗界面，复用 PC 端共享模块。
- `BattleEntryView.vue`（2.5KB）：手动 PC/手机版分流选择页。

### 1.3 六段式编辑器子组件
- `components/glossary-editor/`：`EffectStackBuilder`(15KB) / `HexCanvasEditor`(35KB) / `JsonCompilerPanel`(22KB) / `TierDimensionForm` / `ConditionsEditor`。
- `components/glossary-hub/`：SkillListPanel / DetailPanel / BattleTestPanel / RollPanel / CostPanel / TimingPanel / ConditionsEditor。

---

## 二、候选改造方向（供选择）

### 方向 A：词条编辑器完全动态化（方案 A 完整收口）
**目标**：`atomGroups` 不再硬编码 58 原子，改为从 `GET /api/combat/atom-registry` 动态拉取并渲染表单。
**依据**：atomRegistry.listMeta 已返回每个原子的 `params[{key,type,label,options,default}]`，可直接驱动字段表单，消除前端硬编码 + 后端 registry + 引擎 handler 三头分化。
**范围**：
- 重构 `atomGroups` 为接口数据派生（按 `kind`/`grp` 分组，fields 来自 `params`）。
- 处理接口未覆盖的展示字段（label 中文名、grp 分组标题）— 可让后端 listMeta 补充 `label`/`group` 或前端维护轻量 i18n 映射。
- 统一 `/combat-glossary/atom-meta` 与 `/api/combat/atom-registry` 双路径（建议前端改调 `/api/combat/atom-registry`，删除旧路径依赖或让 glossary.ts 别名）。
**风险**：中。表单字段类型（select/number/bool）需与 listMeta.params 对齐；存量草稿兼容（旧 type 直传回退）。
**工作量**：中（约 1-2 天）。

### 方向 B：战斗界面 UI/交互打磨
**目标**：PC/移动端战斗体验优化。
**可选子项**：
- B1 单位详情 sheet（点击棋子弹属性/状态/装备详情）— 当前缺失。
- B2 技能特效精化（AOE/地图炮命中高亮、伤害飘字）。
- B3 NewBattleView 巨型组件拆分（211KB 难维护，但属重构风险）。
- B4 移动端交互增强（两步式外的复杂操作、阵型预览）。
**风险**：低-中（B3 重构风险高，建议谨慎）。
**工作量**：依子项（B1/B2 约 0.5-1 天，B3 约 2-3 天）。

### 方向 C：六段式画布可视化编排增强
**目标**：在编辑器内把 WHEN/IF/ROLL/DO/AFTER/COST 六段泳道做实时校验与可视化编排，消费 atomRegistry 的 `paramMap`/`guard`。
**依据**：`HexCanvasEditor` 已做六段星形坐标（WHEN 锚 (5,10)）；`EffectStackBuilder` 已做拖拽。
**范围**：拖拽原子携带 semantic + fields 实时校验参数合法性（如 B_DISPLACE 的 direction 枚举）；COST 段与 DO 段联动。
**风险**：中（坐标/校验逻辑复杂）。
**工作量**：中（约 1-2 天）。

### 方向 D：前端测试与契约一致性（质量保障）
**目标**：补前端→atom-registry 的集成测试（mock 接口验证 atomGroups 派生、保存 payload 含正确 semantic）。
**工作量**：低（0.5 天）。

---

## 三、推荐优先级（建议）

| 优先级 | 方向 | 理由 |
|--------|------|------|
| **P0** | A（完全动态化） | 直接闭环本次后端 atom-registry 投入，消除三头分化，投入产出比最高 |
| P1 | B1（单位详情 sheet） | 战斗界面最缺的实用功能，风险低 |
| P2 | C（六段可视化校验） | 提升编辑效率，但依赖 A 的数据底座 |
| P3 | B2/B4（特效/移动端增强） | 体验优化，非阻塞 |
| 暂缓 | B3（NewBattleView 拆分） | 高风险重构，无明确 bug 不急 |

---

## 四、验收口径（通用）
- 前端 `npm run build` 成功，8081 资产哈希更新。
- 编辑器原子面板正确渲染（动态/或保持硬编码但语义对齐），保存草稿后后端 `GET /api/combat/atom-registry?resolve=<type>` 能翻译该语义名。
- 战斗界面无控制台报错，交互可达。

---

## 五、待确认问题
1. 选哪个方向为主？（A / B / C / 组合）
2. 若选 A：是否接受后端 listMeta 补充 `label`/`group` 中文字段，还是前端维护映射？
3. `/combat-glossary/atom-meta` 与 `/api/combat/atom-registry` 是否需统一（建议统一到后者）？
