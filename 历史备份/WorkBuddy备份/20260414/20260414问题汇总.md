# 2026-04-14 问题汇总

> 整理时间: 2026-04-15
> 来源: `/Users/dingxuyang/WorkBuddy/20260414174321/.workbuddy/memory/2026-04-14.md`

---

## 问题1: 棋子导入失败 (Failed to fetch)

| 项目 | 内容 |
|------|------|
| **时间** | 2026-04-14 |
| **症状** | `Failed to fetch` - Excel导入时无法连接到后端 |
| **原因** | 前端 `API_BASE` 配置错误，指向了 auth-service (3000端口)，而 hangar-service 实际运行在 3001 端口 |
| **涉及文件** | `frontend/src/views/UnitEditorView.vue` 第367行 |
| **修复** | 将 `API_BASE = 'http://localhost:3000/api'` 改为 `'http://localhost:3001/api/hangar'` |
| **状态** | ✅ 已修复 |

**修复前:**
```javascript
const API_BASE = 'http://localhost:3000/api';  // ❌ 错误的端口
```

**修复后:**
```javascript
const API_BASE = 'http://localhost:3001/api/hangar';  // ✅ 正确的端口
```

---

## 问题2: 端口配置不统一

| 项目 | 内容 |
|------|------|
| **时间** | 2026-04-14 |
| **症状** | 多个服务端口混乱，无法正确通信 |
| **原因** | 各服务独立开发时未统一端口分配 |
| **涉及服务** | auth-service, hangar-service, map-service, combat-service, comm-service |
| **修复** | 统一端口分配规范 |
| **状态** | ✅ 已修复 |

**统一后的端口配置:**
| 服务 | 端口 | API前缀 |
|------|------|---------|
| auth-service | 3001 | /api/auth |
| hangar-service | 3002 | /api/hangar |
| map-service | 3003 | /api/map |
| combat-service | 3004 | /api/combat |
| comm-service | 3005 | /api/comm |
| frontend | 8081 | - |
| WebSocket | 3004 | /socket.io |

---

## 问题3: map-service 端口冲突

| 项目 | 内容 |
|------|------|
| **时间** | 2026-04-14 |
| **症状** | map-service 无法启动，端口被占用 |
| **原因** | map-service 原来运行在3002端口，与其他服务冲突 |
| **涉及文件** | `services/map-service/src/index.js` |
| **修复** | 改为3003端口，并更新API前缀 |
| **状态** | ✅ 已修复 |

**修复内容:**
1. **端口**: 3002 → 3003
2. **API前缀**: `/api/battlefields` → `/api/map/battlefields`
3. **完整API端点更新**:
   - GET /api/map/battlefields - 战场列表
   - GET /api/map/battlefields/:id - 战场详情
   - POST /api/map/battlefields - 创建战场
   - PUT /api/map/battlefields/:id - 更新战场
   - DELETE /api/map/battlefields/:id - 删除战场
   - GET /api/map/battlefields/:id/spawn-points - 出生点
   - POST /api/map/battlefields/:id/terrain - 批量更新地形
   - DELETE /api/map/battlefields/:id/terrain - 清除地形
   - GET /api/map/battlefields/terrain/types - 地形类型
   - POST /api/map/battlefields/utils/path - A*寻路
   - POST /api/map/battlefields/utils/range - 范围查询

---

## 问题4: CORS 配置问题

| 项目 | 内容 |
|------|------|
| **时间** | 2026-04-14 |
| **症状** | 前端无法访问后端API，跨域错误 |
| **原因** | hangar-service CORS配置不允许前端来源 |
| **修复** | 从允许所有来源改为只允许 http://localhost:8081, http://localhost:8080 |
| **状态** | ✅ 已修复 |

---

## 问题5: 棋子数据验证过于严格

| 项目 | 内容 |
|------|------|
| **时间** | 2026-04-14 |
| **症状** | Excel导入时因点数分配不合规被拒绝 |
| **原因** | 棋子验证规则过于严格 |
| **修复** | 放宽棋子数据验证规则，Excel导入不再强制检查点数分配 |
| **状态** | ✅ 已修复 |

---

## 待确认清单

| # | 问题 | 确认状态 |
|---|------|----------|
| 1 | API_BASE端口已改为3001 | ⬜ 待确认 |
| 2 | 端口配置已统一（auth:3001, hangar:3002, map:3003...） | ⬜ 待确认 |
| 3 | map-service端口已改为3003 | ⬜ 待确认 |
| 4 | CORS配置已更新 | ⬜ 待确认 |
| 5 | 棋子数据验证已放宽 | ⬜ 待确认 |

---

*整理完成时间: 2026-04-15*
