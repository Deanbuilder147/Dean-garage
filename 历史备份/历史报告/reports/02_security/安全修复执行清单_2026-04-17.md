# 🔒 安全修复执行清单
## 逐步修复指南 - 不改变原有功能
**执行日期：** 2026-04-17  
**执行原则：** 仅修复安全漏洞，保持向后兼容

---

## 📋 执行前准备

### 1. 创建 Git 备份分支
```bash
cd /home/agentuser/Dean-garage
git checkout -b backup-before-security-fixes
git checkout main  # 回到主分支继续修复
```

### 2. 当前状态确认

**当前 .env 配置：**
- ✅ JWT_SECRET 已存在（但 docker-compose 中仍有回退）
- ✅ NODE_ENV=production
- ❌ 无 POSTGRES_PASSWORD（docker-compose 中硬编码）

**当前 docker-compose.yml 问题：**
- 第 11 行：`POSTGRES_PASSWORD: mecha_secure_password` ❌
- 第 31 行：DATABASE_URL 中有密码 ❌
- 第 32 行：JWT_SECRET 有回退值 ❌
- 第 49 行：DATABASE_URL 中有密码 ❌
- 第 66 行：DATABASE_URL 中有密码 ❌
- 第 83 行：DATABASE_URL 中有密码 ❌
- 第 84 行：JWT_SECRET 有回退值 ❌
- 第 101 行：JWT_SECRET 有回退值 ❌

**当前代码问题：**
- map-service/battlefields.js:14-17：认证绕过 ❌
- combat-service/battles.js:8：JWT 回退 ❌
- combat-service/db.js:9：DB 在/tmp ❌

---

## ✅ 修复步骤（按顺序执行）

### 步骤 1: 创建 .env 示例文件

**文件：** `/home/agentuser/Dean-garage/.env.example`

```bash
# 数据库配置
POSTGRES_DB=mecha_battle
POSTGRES_USER=mecha_user
POSTGRES_PASSWORD=<使用 openssl rand -base64 32 生成>

# JWT 配置
JWT_SECRET=<使用 openssl rand -base64 64 生成>

# 环境变量
NODE_ENV=production

# 服务端口
AUTH_SERVICE_PORT=3001
HANGAR_SERVICE_PORT=3002
MAP_SERVICE_PORT=3003
COMBAT_SERVICE_PORT=3004
COMM_SERVICE_PORT=3005
FRONTEND_PORT=8081

# 数据库 URL（Docker 内部）
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
```

---

### 步骤 2: 修改根目录 docker-compose.yml

**修改内容：**
1. 第 11 行：使用环境变量
2. 第 31、49、66、83 行：DATABASE_URL 使用环境变量
3. 第 32、84、101 行：移除 JWT_SECRET 回退

**修改前：**
```yaml
environment:
  POSTGRES_DB: mecha_battle
  POSTGRES_USER: mecha_user
  POSTGRES_PASSWORD: mecha_secure_password  # ❌
```

**修改后：**
```yaml
environment:
  POSTGRES_DB: ${POSTGRES_DB:-mecha_battle}
  POSTGRES_USER: ${POSTGRES_USER:-mecha_user}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}  # ✅ 必须设置
```

**修改前（auth-service）：**
```yaml
environment:
  DATABASE_URL: postgresql://mecha_user:***@postgres:5432/mecha_battle  # ❌
  JWT_SECRET: ${JWT_SECRET:-mecha-battle-auth-secret-key}  # ❌ 有回退
```

**修改后（auth-service）：**
```yaml
environment:
  DATABASE_URL: ${DATABASE_URL}  # ✅ 使用环境变量
  JWT_SECRET: ${JWT_SECRET}  # ✅ 必须设置
```

---

### 步骤 3: 修改 map-service 认证绕过

**文件：** `services/map-service/src/routes/battlefields.js`

**修改前（14-17 行）：**
```javascript
// 开发环境：如果没有 token，使用测试用户 ID=1
if (!authHeader) {
  req.user = { userId: 1, username: 'test' };
  return next();
}
```

**修改后：**
```javascript
// 开发模式：允许无 token 访问（向后兼容）
// 生产模式：必须提供 token
if (!authHeader) {
  if (process.env.NODE_ENV === 'production') {
    return res.status(401).json({ error: '未授权访问' });
  }
  // 开发环境保持 test 用户兼容
  req.user = { userId: 1, username: 'test' };
  return next();
}
```

**同时修改 28-31 行：**
```javascript
// 修改前：
if (process.env.NODE_ENV !== 'production') {
  req.user = { userId: 1, username: 'test' };
  return next();
}

// 修改后：移除这个回退，让错误自然抛出
```

---

### 步骤 4: 修改 combat-service JWT 回退

**文件：** `services/combat-service/src/routes/battles.js`

**修改前（第 8 行）：**
```javascript
const JWT_SECRET=process.env.JWT_SECRET || 'mecha-battle-auth-secret-key';  # ❌
```

**修改后：**
```javascript
// 强制要求 JWT_SECRET 环境变量
const JWT_SECRET = process.env.JWT_SECRET;
if (!JWT_SECRET) {
  throw new Error('[配置错误] JWT_SECRET 环境变量必须设置！');
}
```

---

### 步骤 5: 修改 combat-service DB 路径

**文件：** `services/combat-service/src/database/db.js`

**修改前（9-10 行）：**
```javascript
const DB_DIR = '/tmp';
const DB_PATH = path.join(DB_DIR, 'combat.db');
```

**修改后：**
```javascript
// 使用数据目录（避免/tmp 被清空）
const DB_DIR = process.env.DB_PATH || path.join(__dirname, '../../data');
const DB_PATH = path.join(DB_DIR, 'combat.db');
```

**在 initializeDatabase 方法中添加：**
```javascript
async initializeDatabase() {
  // 确保数据目录存在
  if (!fs.existsSync(DB_DIR)) {
    fs.mkdirSync(DB_DIR, { recursive: true, mode: 0o700 });
  }
  // ... 原有逻辑
}
```

---

### 步骤 6: 更新 .gitignore

**确保以下模式被忽略：**
```bash
# 环境变量
.env
.env.local
.env.*.local

# 数据库文件（sqlite）
*.db
*.sqlite

# 日志
*.log
```

---

## ✅ 验证清单

### 验证 1: 检查无硬编码密码
```bash
# 应无输出
grep -r "PASSWORD.*=.*mecha" docker-compose.yml services/*/docker-compose.yml | grep -v "\${"
```

### 验证 2: 检查 JWT 无回退
```bash
# 应无输出
grep -r "JWT_SECRET.*||" services/*/src/
```

### 验证 3: 检查认证绕过
```bash
# 应只在 map-service 看到开发模式注释
grep -rn "userId.*test" services/*/src/
```

### 验证 4: 检查 DB 路径
```bash
# 应无/tmp 路径
grep -r "DB_DIR.*=.*'/tmp'" services/*/src/
```

### 验证 5: 服务启动测试
```bash
# 设置环境变量
export POSTGRES_PASSWORD=$(openssl rand -base64 32)
export JWT_SECRET=$(openssl rand -base64 64)
export DATABASE_URL="postgresql://mecha_user:${POSTGRES_PASSWORD}@localhost:5432/mecha_battle"

# 启动服务
docker-compose up -d

# 检查状态
docker-compose ps  # 所有服务应 Up
```

### 验证 6: 功能测试
```bash
# 测试登录
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"test123"}'

# 测试 map-service（有 token）
TOKEN=<从上一步获取>
curl -X GET http://localhost:3003/api/battlefields \
  -H "Authorization: Bearer ${TOKEN}"

# 测试 map-service（无 token - 开发环境应允许）
curl -X GET http://localhost:3003/api/battlefields
```

---

## 🔄 回滚命令

### 完全回滚：
```bash
cd /home/agentuser/Dean-garage
git checkout .
docker-compose down
docker-compose up -d
```

### 部分回滚（单个文件）：
```bash
git checkout docker-compose.yml
git checkout services/map-service/src/routes/battlefields.js
git checkout services/combat-service/src/routes/battles.js
git checkout services/combat-service/src/database/db.js
```

---

## 📝 Git 提交模板

```bash
# 提交 1：安全配置
git add .env.example docker-compose.yml .gitignore
git commit -m "fix(security): 移除硬编码密钥

- 创建.env.example 模板
- docker-compose.yml 使用环境变量
- 更新.gitignore 忽略敏感文件

Breaking Change: 需要设置环境变量后启动"

# 提交 2：认证修复
git add services/map-service/src/routes/battlefields.js
git commit -m "fix(security): 修复 map-service 认证绕过

- 生产环境拒绝未授权访问
- 开发环境保持向后兼容

不影响现有功能"

# 提交 3：JWT 安全
git add services/combat-service/src/routes/battles.js
git commit -m "fix(security): 移除 JWT_SECRET 弱回退

- 强制要求 JWT_SECRET 环境变量
- 启动时检查配置

Breaking Change: 必须设置 JWT_SECRET"

# 提交 4：数据持久化
git add services/combat-service/src/database/db.js
git commit -m "fix(security): 修复数据库存储位置

- 使用专用数据目录替代/tmp
- 设置正确的文件权限

不影响现有功能"
```

---

## ⚠️ 注意事项

1. **不要修改的内容：**
   - ❌ 不修改游戏逻辑
   - ❌ 不修改 API 接口
   - ❌ 不修改数据库结构
   - ❌ 不添加新依赖

2. **保持向后兼容：**
   - ✅ 开发环境仍可无 token 调试
   - ✅ 现有 token 仍有效
   - ✅ 现有功能不受影响

3. **必须测试：**
   - ✅ 服务能正常启动
   - ✅ 用户能正常登录
   - ✅ 游戏功能正常

---

*执行清单版本：1.0*  
*创建：2026-04-17*  
*项目：Dean-garage (机甲战棋)*
