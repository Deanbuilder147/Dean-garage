# 待确认技术资产清单（来自「过期报告」重新评估）

> **生成日期**：2026-08-02
> **来源**：对「过期报告」目录 10 份文件，依据现行 3006 大一统架构（见《机甲战棋-项目资产总览（现行版）》）重新逐项核查后的筛选结果。
> **状态**：**待用户确认**。下列内容从历史报告/过期报告中提炼，核心价值仍成立，但部分路径/状态描述需以当前代码为准核对。确认后可并入正式项目资产。
> **评估方法**：每份来源文件标注「核心价值 / 过时点 / 使用价值评级」，再汇总为可复用资产条目。

---

## 一、来源文件逐项评估

### 1. `Phase29-Debug_排查报告_20260624.md`
- **核心价值**：路由对账矩阵方法（Nginx location ↔ gateway 前缀 ↔ 外网状态）；CSP `script-src` 需放行 `'unsafe-eval'`（`frontend/nginx.conf`，否则 Vue 白屏）；`hangarAPI`/`glossaryAPI` 调用方索引。
- **过时点**：基于「3006 SQLite + 独立 hangar 微服务」旧拓扑，现行已是 postgres + gateway 内复用 combat `.cjs`，无独立 hangar-service；`/hangar/*` 404 根因已随路径统一改变。
- **使用价值评级**：**中**。诊断方法论 + CSP 结论仍可用，但故障根因须在当前 codebase 重新验证。

### 2. `royroy与阵营顺序修改报告.md`
- **核心价值**：Royroy 属性模型 `unit.royroy`（name/attack/defense/hp/maxHp/mobility/skills/isAuto/deployed/destroyed/q/r/cooldownRound）；6 条部署/回收/冷却规则（部署不耗 ATTACK、攻击耗主机 ATTACK、冷却 `cooldownRound=round+2`、auto 跟随 1 格内）；阵营轮转三件套字段（`factionTurnOrder`/`activeFactionIndex`/`round`）+ 行动门控 `403 NOT_YOUR_TURN`。
- **过时点**：引用 `battleStateFactory.ts`/`shared-kernel/types.ts` 旧微服务路径；「后端无 `/action` 路由→404」旧结论需在现架构核对。
- **使用价值评级**：**高**。Royroy 机制与阵营顺序当前版本仍未完全落地，规则定义清晰，最直接可复用。

### 3. `前端改造与平顶六边形_实施计划报告_v2.md`
- **核心价值**：阶段 B 核心风险排查框架（邻居方向前后端一致性矩阵、移动范围公式两路不一致 `max(1,round(rawMob/10))` vs `Math.floor(mobility/2)`、边界残留 `.width/.height` 扫描清单）；距离语义统一 `offset→axial`；「跳过全局 FLAT_TOP、维持保守平顶」的决策依据（避免后人重蹈高风险改造）。
- **过时点**：引用 `services/map-service/.../hexUtils.js`、`battleState.js`、`terrainMovement.cjs` 为未挂载旧拓扑；部分为中间态文档。
- **使用价值评级**：**高**。核心逻辑一致性专项正是当前「仍保持尖顶 Even-R、距离数学正交」必须持续守护的红线。

### 4. `前端改造与平顶六边形_计划完成报告.md`
- **核心价值**：距离公式修正 `offset(q,r)→axial = {q-(r+(r&1))/2, r}`，6 个 offset 邻居 cube 距离均为 1（坐标体系真相源）；修复落点清单（combat.ts `hexDist`、skillExecutor `_hexDistance`、battleState/map-service `hexDistance`）；旧地图 `migrateCells` 平滑迁移补丁（cells 空 + width/height 存在时按 100×100 补全 `{q,r,terrain:'moon'}`）；部署红线（镜像名、`--no-cache`、`up -d --no-deps`、严禁动 db）。
- **过时点**：引用 `services/combat-service/src/state/battleState.js` 等旧拓扑残路径。
- **使用价值评级**：**高**。对当前坐标/距离/边界现状的权威确认，与现行一致。

### 5. `前端视图改造与架构变更综合方案.md`
- **核心价值**：挤出式 2.5D 立柱路线（路线 A `drawIsoHexColumn` + 光影路线 C，后端零逻辑改动）；`drawIsoHexColumn` 草案（顶面+侧面双档明暗 `0.55/0.38`+画家算法远→近+脚底投影）；架构解耦红线（hexUtils 唯一数学真理、Canvas 单向管道、显式依赖、否决 `RenderableCell`/Driver-Bridge 重型抽象 YAGNI）；全局地形素材库设计（terrain.ts + terrainAPI + CanvasPattern 平铺 + 纯色回退）。
- **过时点**：多处主张「平顶六边形 Even-Q 切换」已被判定跳过/失效；`HexGridCanvas.vue` 已删（本报告自身已要求删）。
- **使用价值评级**：**中**。挤出立体路线、drawIsoHexColumn、架构红线、地形素材库设计高度可用，但属「方案」非「已落地」。

### 6. `单位数值计算说明.md`
- **核心价值**：当前生效数值公式（导入即定）：`hp=结构×5+20`、`armor=floor(结构×0.25)`、`attack=格斗×2+5`、`defense=floor(机动×0.3)`、`speed=2+floor(机动/20)`、`range=1+floor(射击/25)`、shield=0；「装备仅经部件四维间接影响数值」真相说明；疑似问题点清单（shield 恒 0、range 仅看射击、speed 上限 6 等）。
- **过时点**：「装备加成系统未接入」结论若后续已落实 equipManager 则部分失效，需以最新实现为准。
- **使用价值评级**：**高**。当前数值基线直接描述，平衡/调参核心参考。

### 7. `固定画布与平顶六边形架构变更影响评估报告.docx`
- **核心价值**：无（二进制，文本不可读）；信息已被同名 `.md`（第 8 份）完全覆盖。
- **过时点**：主张「全局平顶 Even-Q 切换」为推荐路线，已被推翻。
- **使用价值评级**：**低（文本不可读）**。建议用 Office/提取工具另行读取其独有图表，否则单独价值低。

### 8. `固定画布与平顶六边形架构变更影响评估报告.md`
- **核心价值**：历史架构全景（尖顶 Even-R Offset、HEX_WIDTH=64/HEX_HEIGHT=72、稀疏 cells、Canvas CTM + 等距伪 3D 渲染管线、寻路两路对比）；精确改动清单（行号锚点）；数据迁移方案 B（旧图 100×100 内缺 cells 视为 moon 补全，与完成报告 `migrateCells` 一致）；§9 关键词清单与风险表。
- **过时点**：核心主张「平顶 Even-Q 切换」为阶段二必做已被推翻；行号基于旧快照；提及 `HexGridCanvas.vue` 已删。
- **使用价值评级**：**中**。历史架构底图 + 改动索引，需与 v2/完成报告对照。

### 9. `数值计算规则对比报告.md`
- **核心价值**：修正规则全集（A/B/C/D/E）：部件 type 过滤聚合（格斗/射击仅机体+武器，防具/背包独立 HP=结构×2 且作 shield 吸收槽，耐久=5，武器/载具耐久=结构，移动范围=机体+载具+背包机动，机动差额 >+5 封顶 5）；修正后总公式 v2（可编码规范）；必改文件清单；与 V2.0 §4「已实现」条目互证。
- **过时点**：自称「尚未实施」与 V2.0「已实现」存在时间差矛盾；路径属旧拓扑；第七节省未决项（speed/defense 基数、royroy 不互通精确含义等）仍待确认。
- **使用价值评级**：**高**。数值规则修正权威规格，与 Royroy 数值基线语境直接可用。

### 10. `系统重构与Royroy机制方案报告V2.0.md`
- **核心价值**：§0 实现状态对照表（固化数值净化、Speed=机体+载具+背包机动、Defense=0、护甲=结构×0.25、防具/背包独立 HP=结构×2、耐久=5、伤害分担每次 3 点、机动差额封顶+5、逐段行走+朝向、迁移脚本）；Royroy 属性模型完整状态机（Auto/Non-Auto 坐标、HP=0 销毁、`cooldownRound=round+2`、AP 消耗矩阵）；阵营轮转 + Round+1 统一重置 AP 伪逻辑 + 行动门控 `403 NOT_YOUR_TURN`；伤害分担公式（equipmentDurability 各吸 `min(remaining,3)`、durability-1、hp-absorb、破损回写）；Royroy 机动与机体不互通。
- **过时点**：「上一轮独立单位实现方向错误需回退」需核对现状；路径属旧拓扑；自称 Royroy/阵营「待实施」需以代码为准。
- **使用价值评级**：**高**。系统重构与 Royroy/阵营机制终稿规格，固化了数值/伤害基线，最高优先级参考。

---

## 二、建议纳入正式资产的可复用条目（待确认）

| # | 资产条目 | 来源文件 | 核心价值 | 落地建议 |
|---|---|---|---|---|
| 1 | **坐标与距离真相源**（offset→axial 修正、`offset(q,r)→{q-(r+(r&1))/2,r}`、邻居统一 Even-R Offset、移动范围 `max(1,round(rawMob/10))`） | 3、4 | 当前尖顶 Even-R 现状下必须守护的红线 | 并入总览 §3.3 / 距离范围计算说明文档 |
| 2 | **固定画布 100×100 + cells 边界迁移与 `migrateCells` 兼容补丁** | 4、8 | 数据层正确性保障 + 旧地图平滑迁移 | 并入距离/地图相关资产 |
| 3 | **Royroy 属性模型与状态机**（`unit.royroy` 结构、6 规则、Auto 跟随、HP=0 销毁、机动不互通） | 2、10 | 与现 Royroy 数值基线直接对齐 | 新建/并入 GDD |
| 4 | **阵营轮转 + 仅 Round 边界统一重置 AP**（`factionTurnOrder`/`activeFactionIndex`/`round`、`403 NOT_YOUR_TURN`） | 2、10 | 当前 `faction_turn` 显示/回合制设计基线 | 并入战斗逻辑资产 |
| 5 | **数值计算基线（当前生效 + 修正规格）** | 6、9、10 | 平衡/数值系统权威对照（导入即定公式 + type 过滤 + 防具背包独立 HP + 机动差额封顶） | 并入 GDD / 新建数值基线文档 |
| 6 | **伤害分担管线公式**（equipmentDurability 各吸 `min(remaining,3)`、durability-1、破损回写、Defense 废弃改 armor 减伤） | 9、10 | 伤害结算核心逻辑 | 并入 GDD |
| 7 | **前端挤出 2.5D 立体化路线 + `drawIsoHexColumn` 草案** | 5 | 纯前端立柱 + 光影、后端零改动，当前伪 3D 实现起点 | 并入渲染宪法/前端开发文档 |
| 8 | **全局地形素材库设计**（terrain 上传 + CanvasPattern 平铺 + 纯色回退） | 5 | 与当前单文件 API 风格一致，可直接排期 | 并入前端资产 |
| 9 | **部署红线与镜像规范**（镜像名、`build --no-cache` + `up -d --no-deps` 避开 db、严禁动 db） | 3、4、5、10 | 现行 4 容器部署安全约束 | 已纳入总览 §1.3，确认保留 |
| 10 | **架构解耦红线（轻量版，否决重型抽象）** | 5 | hexUtils 唯一数学真理、Canvas 单向管道、显式依赖、YAGNI | 已纳入总览 §3.3 / 宪法文档 |

---

## 三、结论与下一步
- **高价值且建议确认并入**：条目 1–6（坐标距离、Royroy、阵营轮转、数值基线、伤害分担）是支撑当前版本继续演进的核心设计资产，缺口在于「是否已在代码中完全落地」需以现行实现核对（部分来源文件自称「待实施」）。
- **已落地可执行的方法论**：条目 7–10（2.5D 立体、地形素材、部署红线、架构红线）多数已部分实施或已写入总览，确认后即可从「待确认」移出。
- **低价值**：第 7 份 `.docx` 文本不可读，建议如需其独有图表另行提取，否则不纳入。
- **待用户确认动作**：勾选上述条目 → 我将把选定条目正式展开为项目资产文档（或并入现有 GDD / 总览相应章节），并从本「待确认」清单移出。
