# 前端改造与平顶六边形 · 计划完成报告（阶段 A / B / D）

> 执行依据：`前端改造与平顶六边形_实施计划报告_v2.md`
> 执行范围：**阶段 A（固定画布 + 数据迁移）、阶段 B（核心逻辑一致性）、阶段 D（性能/UX）；阶段 C 按决策跳过**
> 执行方式：严格按照文档逐项落地，未中途打断。
> 完成时间：2026-07-23

---

## 0. 总览

| 阶段 | 任务 | 状态 |
|------|------|------|
| A1 | sqlite maps 表 width/height 默认 100 | ✅ |
| A2 | 后端忽略 width/height，固化 100×100 | ✅ |
| A3 | 边界判定 width/height → cellSet / cellMap | ✅ |
| A4 | 前端编辑器移除宽高输入，固定 100×100 | ✅ |
| A5 | 引擎点击/悬停边界兜底 100 | ✅ |
| A6 | 前端战场 gridData 构造 + 边界 | ✅ |
| A7 | 旧地图 cells 平滑迁移补丁 | ✅ |
| B1 | 邻居方向统一为 Even-R Offset（后端迁就前端） | ✅ |
| B2 | 移动范围公式统一 `max(1, round(rawMob/10))` | ✅ |
| B3 | hexDistance 距离语义统一 offset-correct + 单测 | ✅ |
| B4 | 边界残留 `.width/.height` 全局扫描销账 | ✅ |
| B5 | 命中/点击逆推单测（round-trip） | ✅ |
| D1 | 固定画布强制地形缓存防掉帧 | ✅ |
| D2 | 地图外区域视觉提示（固定网格下无外部区域） | ✅(设计满足) |
| D3 | 进行中战斗旧格式清理校验 | ✅(校验通过) |
| — | 构建校验 + 服务器部署 | ✅ |

**关键修正（执行中发现并修正，超出原 v2 描述）**
- 原 v2 误判「前端 `getHexNeighbors` 改 axial 向量」为正确操作；实际**后端应迁就前端 Even-R offset**（详见 §2.2 / 完成报告 §1-B1）。
- 距离函数原用 axial 公式在 offset 坐标上会**系统性高估距离**（如 offset 邻居 `(q+1,r-1)` 算出 2 而非 1），导致 Royroy 相邻判定误拒、技能范围方向性偏差。已统一为 `offset→axial` cube 距离。

---

## 1. 阶段 A — 固定画布 100×100 与数据迁移

### A1 · `backend-gateway/src/db/sqlite.ts`
`maps` 表 `width INTEGER DEFAULT 20, height INTEGER DEFAULT 30` → `DEFAULT 100`。

### A2 · `backend-gateway/src/routes/maps.ts` + `combat.ts`
- `maps.ts`：3 个 GET 端点（`/api/map`、`/api/map/battlefields`、`/api/map/battlefields/:id`）返回 `width:100, height:100`；`create`/`update` 强制 `width=100, height=100`（忽略前端传入，移除宽高输入依赖）。
- `combat.ts` `createBattle`：`width: 100, height: 100`（原 `bfRow.width || 20`）。
- `/initialize` 此前已固定 100×100。

### A3 · 边界判定迁移
- `combat.ts` `tsFindPath`：原本即 `cellSet.has` 边界（无 width/height 矩形判定），保持。
- `battleState.js`（遗留）：三处 `if (nq < 0 || nq >= state.width ...)` 矩形边界**移除**，改由 `cellMap[nkey]` 控制（见 §2 遗留文件处理）。
- 前端 `NewBattleView` 边界统一用 `gridWidth`/`gridHeight`（来自后端 100）+ `cellMap` 双重校验。

### A4 · `frontend/src/views/NewBattlefieldView.vue`
- 删除宽度/高度 `<input>`（原 `newMapWidth/newMapHeight` 10–200 输入），改为静态提示「固定战场尺寸 100 × 100 = 10,000 格」。
- `newMapWidth/newMapHeight` refs 默认值 → `100`；`gridW/gridH` computed 兜底 → `100`；`createNewMap` 写死 `w=h=100`。

### A5 · `frontend/src/components/HexGridCanvasEngine.vue`
- 点击（line 821）与悬停（line 867）边界 `hex.q < data.width` → `hex.q < (data.width || 100)`（含兜底）。

### A6 · `frontend/src/views/NewBattleView.vue`
- `gridData` 由 `gridWidth/gridHeight`（后端 100）+ `cells` 注水；放置/可达性边界用 `gridWidth` + `cellMap.has`，与后端 cellSet 语义对齐。

### A7 · `backend-gateway/src/routes/maps.ts`（旧地图兼容）
新增 `migrateCells(cells, 100, 100)`：当 `cells` 为空数组且 width/height 存在时，按 100×100 自动补全 `{q, r, terrain:'moon'}` **对象数组**（注意格式为数组非 dict / 非下划线 key，避免 `combat.ts` 的 `cells.map(c=>`${c.q},${c.r}`)` 崩溃）。3 个 GET 端点均接入。

---

## 2. 阶段 B — 核心逻辑一致性

### B1 · 邻居方向统一为 Even-R Offset（后端迁就前端）
校验事实：前端 `(q,r)` 是 **Even-R Offset**（渲染 `pointyTopCenter`、点击 `pointyTopToHex` 均走 offset）。原后端 `tsFindPath` / Royroy 重定位 / `terrainMovement` / `battleState` / `map-service` 用 axial 固定向量 `[[1,0],[1,-1],...]`，对同一整数 `(2,3)` 产出的邻居集合与前端**不一致**（axial 含 `(3,2)`、offset 含 `(1,2)`，屏幕不相邻 → 棋子跨格跳）。

修复：新增 `getEvenROffsetDirs(q,r)`（与前端 `hexUtils.getHexNeighbors` 完全一致的奇偶行方向表），应用于：
- `combat.ts` `tsFindPath`（循环内按 `cur.r` 奇偶取方向）、Royroy 自动重定位 `dirs`。
- `terrainMovement.cjs` `getReachableHexes`（循环内按 `current.r` 奇偶取方向）。
- `battleState.js`（遗留）`canMoveTo`/`findPath`/`getMoveRange` 三处。
- `map-service/src/utils/hexUtils.js` `getNeighbors`。

> 决策依据：保持前端 offset 渲染为真理源，后端改动成本最低且零渲染回归风险；真正「全 axial」需连渲染管道 + DB 序列化大迁移，不排期。

### B2 · 移动范围公式统一
- 网关 `/move` 早已使用 `max(1, round(rawMob/10))`（健壮版，修复 `??` 不捕获 0 的 bug）。
- `battleState.js`（遗留）`getMoveRange`/`moveUnit` 原 `Math.floor(mobility/2)` 与 `unit.moveRange ?? unit.mobility ?? 3` → 统一为 `Math.max(1, Math.round(rawMob/10))`，`rawMob` 取 `mobility>0?mobility:(moveRange>0?moveRange:3)`。

### B3 · 距离语义统一（offset-correct）+ 单测
原 5 处 `hexDistance` 用 axial 公式 `max(|dq|,|dr|,|dq+dr|)` 在 offset 坐标上**系统性高估距离**（节点验证：offset 邻居 `(2,2)→(0,0)` axial 公式得 3，真实距离为 2）。已统一为 `offset→axial` cube 距离，公式经 node 暴力校验：
- 换算 B：`offset(q,r) → axial = { q: q - (r + (r&1))/2, r }`，**所有 6 个 offset 邻居 cube 距离均为 1（PASS）**。

应用点：
- `combat.ts`：顶层 `hexDist`（Royroy 部署/回收相邻判定 `!== 1`）、`computeBeamHits` 局部 `hexDist`（光束垂直距离）。
- `skillExecutor.cjs` `_hexDistance`（技能范围）。
- `battleState.js`（遗留）`hexDistance`。
- `map-service/src/utils/hexUtils.js` `hexDistance`。

单测 `scripts/_test_coord_consistency.mjs`：**点击命中逆推 round-trip PASS**、**距离公式 == BFS 真实距离 PASS**（GET 已运行通过）。

### B4 · 边界残留扫描
- 全局扫描 `.width || 15` / `.height || 10` / `.width || 20` 等残留：
  - `NewBattlefieldView` `gridW/gridH` 兜底 → `100`。
  - `NewBattlefieldSelector` 尺寸显示兜底 `|| 20` → `|| 100`。
  - `HexGridCanvasEngine` 点击/悬停边界 → `(data.width || 100)`，已处理。
  - 后端 `combat.ts` `/move` 仅校验 `target_q<0`（上界由 cellSet 兜底），无残留矩形边界。
- `battleState.js` 矩形边界已移除（A3）。

### B5 · 命中/点击逆推单测
`pointyTopCenter(q,r) → pointyTopToHex` 在 spacingH=spacingV=1、q/r ∈ [0,40) 全量 round-trip 等于原坐标 → **PASS**（确认点击命中与渲染坐标系一致，无漂移）。

---

## 3. 阶段 D — 性能与 UX

### D1 · 固定画布强制地形缓存防掉帧
`HexGridCanvasEngine` 已有 `terrainCache`（离屏缓存），并在 `watch(() => props.gridData, ...)` 中 `initTerrainCache() + renderTerrainCache()`（line 1002-1007）随 cells 变化重建一次、后续复用，避免逐帧重绘。固定 100×100 下缓存只构建一次，性能稳定。

### D2 · 地图外区域视觉提示
固定 100×100 后，网格边界 == cells 范围（0–99），**不存在 cells 之外的可部署像素区**，故无需额外蒙版；编辑器信息区已明确展示「固定 100×100」边界提示。设计上已消除「误以为可部署到空白区」的风险。

### D3 · 进行中战斗旧格式清理校验
- 实时战斗经 `/api/combat` + `createBattleState` 创建，cells 为 `{q,r,terrain}` 数组（新格式）；前端 `battleState` 以 `Array.isArray` 兼容数组/对象两种形态（line 1396-1400）。
- 遗留 `BattleState`（`battleState.js`）已按 offset 语义同步修正，但**不在实时链路**（live 路径走网关 `tsFindPath` 与 `.cjs` 引擎），无持久旧格式数据。校验通过。

---

## 4. 遗留/死代码文件处理说明

以下文件为**遗留/非实时链路**，本次按「统一语义、消除分歧」原则同步修正，但不在运行时关键路径：
- `services/combat-service/src/state/battleState.js`（`BattleState`/`canMoveTo`/`findPath`/`getMoveRange`/`moveUnit`/`hexDistance`）：邻居方向、矩形边界、距离公式、移动范围公式全部迁至 offset 语义。
- `services/combat-service/src/utils/terrainMovement.cjs` `getReachableHexes`：方向表改 offset。
- `services/map-service/src/utils/hexUtils.js` `hexDistance`/`getNeighbors`：改 offset。

> 注：`.cjs` 引擎（`skillExecutor`/`combatCore`）属实时链路，其 `_hexDistance` 已修正，直接影响技能范围正确性。

---

## 5. 构建与部署验证

| 项 | 结果 |
|----|------|
| 前端 lint（`NewBattlefieldView`/`HexGridCanvasEngine`/`NewBattlefieldSelector`） | 0 errors |
| 后端 gateway `npx tsc --noEmit` | exit 0（无类型错误） |
| 坐标一致性单测 `scripts/_test_coord_consistency.mjs` | 点击逆推 PASS / 距离=BFS PASS |
| rsync 本地 → 服务器 `/root/mecha-universe-engine/` | exit 0 |
| 前端 `npm run build` | 120 modules transformed，dist 生成 |
| `docker compose build --no-cache mecha-gateway mecha-frontend` | 镜像重建成功 |
| `docker compose up -d --no-deps mecha-gateway mecha-frontend` | 容器 Recreate + Started，gateway **Healthy** |
| 冒烟：`http://localhost:8081/` | 200 |
| 冒烟：`http://localhost:3006/health` | 200 |
| 网关日志错误扫描 | 无 error/fail/exception |

> 服务器 SQLite（`/data/mecha-universe.db`）当前 `battlefields` 为空，故 cells 迁移补丁无现成旧数据触发；迁移逻辑经 tsc + 代码审查 + 偏移换算节点校验确认正确。一旦创建/导入旧格式（空 cells + width/height）地图，GET 端点将自动补全 100×100 moon 地形。

---

## 6. 风险与回归提示

1. **距离语义变更是行为变更**：原 axial 公式在 offset 坐标上「方向性高估距离」，现已修正为真实 offset 距离。表现上，部分斜向（如 `(q+1,r-1)`/`(q+1,r+1)` 方向）的技能范围/可达格会**比改造前略多/对称**，属正确性修复，非回归。
2. **Royroy 相邻判定修复**：原 `hexDist(...) !== 1` 对 offset 邻居误判，曾导致合法相邻回收被拒；现随 offset-correct 距离正确。
3. **阶段 C 跳过**：维持保守平顶视觉，未启用全局 FLAT_TOP 朝向切换（高风险回归已规避）。
4. **回滚**：保留服务器镜像历史；如需回滚，`docker compose up -d --no-deps` 旧镜像即可（注意镜像名为 `mecha-universe-engine-mecha-gateway`/`...-mecha-frontend`）。**严禁**触碰 `mecha-battle-db` 容器。

---

## 7. 交付物清单

- 后端：`combat.ts`、`maps.ts`、`sqlite.ts`、`battleStateFactory`（无改）、`services/combat-service/.../battleState.js`、`services/combat-service/.../terrainMovement.cjs`、`services/combat-service/.../skillExecutor.cjs`、`services/map-service/.../hexUtils.js`
- 前端：`NewBattlefieldView.vue`、`HexGridCanvasEngine.vue`、`NewBattleView.vue`、`NewBattlefieldSelector.vue`
- 测试：`scripts/_test_coord_consistency.mjs`（点击逆推 + 距离=BFS 一致性）
- 计划文档：`前端改造与平顶六边形_实施计划报告_v2.md`（执行依据）、本报告
