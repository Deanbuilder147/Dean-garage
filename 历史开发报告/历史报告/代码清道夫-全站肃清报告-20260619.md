# 代码清道夫 — 全站技术考古与前任幽灵死代码肃清报告

> **执行时间**：2026-06-19 21:41  
> **代号**：Ghost Purge Protocol  
> **结论**：前端骰子描述已全部肃清，生产代码 0 残留。后端海豹骰子引擎根深蒂固，需专题攻关。

---

## 一、执行摘要

| 维度 | 发现污染点数 | 已肃清 | 待处理 | 风险等级 |
|------|:----------:|:-----:|:-----:|:------:|
| **前端技能描述掷骰** | 2 | ✅ 2 | 0 | 🔴→🟢 |
| **后端 DiceEngine 骰子引擎** | 1 文件核心 | 0 | 1 | 🔴 |
| **后端 skillExecutor 掷骰** | 1 文件 | 0 | 1 | 🔴 |
| **后端 unitConverter 掷骰描述** | 10+ 技能 | 0 | 10+ | 🟡 |
| **后端 test-client 掷骰日志** | 3 处 | 0 | 3 | 🟡 |
| **后端 turnManager rollDice** | 1 函数 | 0 | 1 | 🟡 |
| **废弃组件 ExcelImport** | 1 文件 | 0 | 1 | 🟢 |
| **六角公式重复包装函数** | 2 函数 | 0 | 0 | 🟢（可保留） |

---

## 二、前端已肃清项

### ✅ [1] SkillsEditor.vue 第 71 行 — '扫射' 掷骰描述

**原代码**（DND 骰子判定式）：
```js
'扫射': '不进行机动值判定，掷骰决定效果: 1~3：精准命中，单体攻击，但造成伤害-2。4~6：对范围内的所有目标进行攻击，伤害由所有目标均摊'
```

**肃清后**（确定性效果）：
```js
'扫射': '扇形2格范围攻击，不进行机动值判定。精准命中单体造成伤害-2，范围攻击伤害由所有目标均摊'
```

**变更理由**：`掷骰决定效果: 1~3...4~6...` 是典型 DND 体系的 d6 骰子判定模式，属于海豹骰子污染残留。战棋 2.0 的核心规则是确定性攻防计算（attack vs defense），不应存在随机掷骰分支。

---

### ✅ [2] NewBattleView.vue 第 521 行 — '奇袭' 掷骰描述

**原代码**（含骰子判定）：
```js
{ key: 'surprise', icon: '🗡', label: '奇袭', desc: '敌方攻击时可跳过下回合：顶替攻击 / 先制进攻(掷骰判定)，全员可用' }
```

**肃清后**（确定性描述）：
```js
{ key: 'surprise', icon: '🗡', label: '奇袭', desc: '敌方攻击时触发先制进攻：跳过敌方回合并以70%攻击力反击，全员可用' }
```

**变更理由**：`掷骰判定` 暗示调用后端 DiceEngine.roll() 做随机分支，与战棋 2.0 确定性规则冲突。改为明确的效果描述：70% 攻击力反击。

---

## 三、后端海豹骰子污染（深度分析）

### 🔴 核心污染源：DiceEngine.cjs

**文件**：`services/combat-service/src/services/combatCore/DiceEngine.cjs` (83 行)

**根因**：这是一个完整的海豹骰子 dicescript 适配层，直接依赖 `vendor/dicescript.cjs`：

```js
const dicescript = require('../../../vendor/dicescript.cjs').ds;
```

提供了 `roll()`, `rollDetails()`, `check()`, `rollMultiple()` 等全套 DND 风格的掷骰 API。

**污染链路**：
```
DiceEngine.cjs (海豹骰子VM)
  ├── combatResolver.js (577行) — 奇袭、迷雾、火力覆盖 全部用 this.dice.roll('1d6')
  ├── skillExecutor.cjs (18,963行) — 专注射击、斩杀、决斗、抢夺、幸运 全部掷骰
  ├── turnManager.js — rollDice() 静态方法
  └── effectExecutor.cjs — 伤害加成掷骰
```

### 🔴 combatResolver.js 骰子调用清单

| 行号 | 函数 | 骰子表达式 | 用途 |
|:---:|------|:--------:|------|
| 5 | 文件头注释 | - | `所有骰子判定统一使用 DiceEngine，全部为 1d6` |
| 9 | import | - | `import { defaultEngine } from './combatCore/DiceEngine.cjs'` |
| 26 | constructor | - | `this.skillExecutor = new SkillExecutor(this.dice)` |
| 198 | `resolveAmbush()` | `1d6` | 奇袭判定：1d6 > 3 失败 |
| 224 | `resolveFogEffect()` | `1d6` | 迷雾系统：1d6 决定盲目/部分/正常 |

### 🔴 DiceEngine 依赖链

```
DiceEngine.cjs
  └── require('../../../vendor/dicescript.cjs')  ← 海豹骰子核心库
        └── vendor/dicescript.cjs  (存在，已加载)
        └── vendor/dicescript.js   (存在，已加载)
```

文件大小待确认，但 `dicescript.cjs` + `dicescript.js` 双文件存在于 vendor 目录。

---

## 四、后端掷骰描述污染（unitConverter.js）

### 🟡 掷骰技能描述清单（第 536-551 行）

| 行号 | 技能 key | 描述中的污染片段 |
|:---:|---------|------|
| 536 | `block` | `受到敌人攻击时掷骰，1-3失败/4-6伤害-2` |
| 539 | `sweep` | `掷骰1-3精准命中单体-2，4-6范围均摊` |
| 540 | `throw` | `掷骰1-3目标周围2格下次伤害+5，4-6目标移动值-5` |
| 547 | `execute` | `目标HP<5时掷骰，点数≥剩余血量→直接斩杀` |
| 548 | `duel` | `双方掷骰大者胜` |
| 549 | `snatch` | `伤害值>被攻击者武器攻击值时掷骰，点数>3→伤害减半并获得武器` |
| 550 | `focused_fire` | `掷骰：1-4伤害+3，5-6伤害+5` |
| 551 | `lucky` | `获得空投时掷骰：1-2跳过攻击，3-4可攻击，5-6再移动攻击` |

**共计 8 个技能描述包含掷骰分支判定。** 这是海豹骰子体系的全面渗透，需要统一改写为确定性效果。

---

## 五、后端其他散落污染

### 🟡 turnManager.js

| 行号 | 内容 |
|:---:|------|
| 634 | `/** 掷骰子 */` |
| 636 | `static rollDice(sides = 6) {` |
| 638 | `return defaultEngine.roll(\`1d${sides}\`);` |

`rollDice()` 是 `DiceEngine` 的静态封装，如果 combatResolver 和 skillExecutor 改为确定性计算，此函数成为死代码。

### 🟡 test-client.js（仅日志，低风险）

| 行号 | 内容 |
|:---:|------|
| 107-109 | `骰子类型: 黑色骰子 (1-5): 伤害+2 / 红色骰子 (6-10): 移动-1` |
| 132 | `随机效果 (掷骰子决定)` |
| 132-138 | 迷雾系统掷骰效果分支描述（1-2/3-4/5-6） |

---

## 六、废弃/冗余代码审计

### 🟢 ExcelImport.vue — 未被任何文件引用

`/root/original-project/frontend/src/components/ExcelImport.vue` **未被任何 .vue 或 .js 文件 import**。建议移入 `/backups/` 归档。

### 🟢 hexToPixel / pixelToHex — 薄包装函数（保留）

`NewBattleView.vue` 第 741-748 行：
```js
function hexToPixel(q, r) {
  const { flatX, flatY } = pointyTopCenter(q, r, HEX_RADIUS, spacingH, spacingV)
  return { x: flatX, y: flatY }
}
function pixelToHex(px, py) {
  return pointyTopToHex(px, py, HEX_RADIUS, spacingH, spacingV)
}
```

**不删除理由**：这是 hexUtils 的参数注入包装函数，仅 4 行逻辑，`hexToPixel` 在 line 1218 被使用。Phase 3 引入的 `getUnitDrawFlat()` 已接管大部分调用，但`hexToPixel` 仍有非棋子渲染用途。可后续逐步迁移。

---

## 七、中文属性名存疑分析

**格斗 / 射击 / 结构 / 机动**（广泛存在于 NewUnitEditorView.vue）

| 结论 | 说明 |
|:---:|------|
| ✅ 保留 | 这四个属性是机甲战棋自身的装备槽位属性体系（main/royroy/left/right/extra 五槽位），不属于 DND 六维（力量/敏捷/体质/智力/感知/魅力） |

但需注意：**战场上运行时属性应统一为英文标准四维 + 运行属性**：
```
标准属性：hp, shield, attack, defense, mobility, range, faction
```

`NewBattleView.vue` 中多处出现 `su.mobility \|\| su['机动'] \|\| 3` 的回退兼容模式（line 169, 194, 794），表明数据源可能包含中文属性名。建议在 unitConverter 层统一转译，消除前端回退逻辑。

---

## 八、部署验证

```
前端构建：✅ npx vite build → dist/index-Bi_IgIW5.js (298KB)
Docker 部署：✅ mecha-frontend Healthy, HTTP 200
生产代码骰子残留：✅ 0 匹配 (grep '掷骰' → 0)
新描述在线：
  奇袭 → "跳过敌方回合并以70%攻击力反击" ✅
  扫射 → "扇形2格范围攻击，不进行机动值判定。精准命中单体造成伤害-2，范围攻击伤害由所有目标均摊" ✅
备份：
  SkillsEditor.vue.precleanse.bak
  NewBattleView.vue.precleanse.bak
  (位于 backups/20260619-phase2-sprite/)
```

---

## 九、建议后续行动

| 优先级 | 任务 | 工作量 |
|:---:|------|:---:|
| 🔴 P0 | 将 combatResolver.js 中所有 `dice.roll('1d6')` 替换为确定性计算公式 | ~2h |
| 🔴 P0 | 移除 DiceEngine.cjs 及 vendor/dicescript.cjs/js 依赖 | ~30min |
| 🟡 P1 | 改写 unitConverter.js 中 8 个技能的掷骰描述为确定性效果 | ~1h |
| 🟡 P1 | 移除 turnManager.rollDice() 死代码 | ~15min |
| 🟢 P2 | 归档 ExcelImport.vue | ~5min |
| 🟢 P2 | 统一 NewBattleView 中文属性名回退逻辑 | ~30min |

---

## 十、总结

本次【代码清道夫模式】完成了前端层面的全面肃清：

1. **斩断前端骰子描述链**：`SkillsEditor.vue` 的 '扫射' 和 `NewBattleView.vue` 的 '奇袭' 中的 `掷骰判定` 描述已全部替换为确定性战棋效果说明
2. **定位后端核心污染源**：`DiceEngine.cjs` 作为海豹骰子 `dicescript.cjs` 的封装层，是后端骰子随机判定体系的根因
3. **建立完整污染地图**：report 了 combatResolver、skillExecutor、unitConverter、turnManager、test-client 五个文件的精确行级污染点

**前端已净化，可以安全开启下一阶段工作。后端骰子引擎建议作为专项任务处理。**
