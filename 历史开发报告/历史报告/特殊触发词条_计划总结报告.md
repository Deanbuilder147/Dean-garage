# 特殊触发词条 · 计划总结报告

> 生成时间：2026-07-25
> 执行模式：**I（编排者）直接实现**（经用户确认，因平台仅能派发只读 `code-explorer` 子 agent，写码任务由编排者直接落地）
> 依据文档：《特殊触发词条_功能开发实现路径报告.md》（附录 E 分工方案）

---

## 一、任务分配与模块分工

按附录 E 的拓扑，本次执行把"集中冲突文件"按四招拆成"文件天然隔离"（见原报告 E.3）。因改为单编排者实现，原"glossary 分片加载"简化为直接往单一 `glossary-skill-config.json` 追加 `trigger_types` + `special_trigger_words` 段（更低风险，效果等价）。

| 角色 | 原方案 | 本次实际 | 职责 / 交付物 |
|---|---|---|---|
| **ARCH** | 现有 `code-explorer`（只读） | 现有 `code-explorer`（只读） | 核验报告锚点；确认 triggerEngine 不存在、combatIntegrator DEPRECATED、glossary 缺 5 个抗性键、实时路径 0 处 DamagePipe.calculate 调用 |
| **F** 基础/契约 | 定制写码 agent | 编排者直接实现 | reactionHandlers 注册表 + damageModifiers + glossary 新增段 + 7 钩子调用点 + 4 个 resolve 路由 + 前端共享纯函数/绘制 |
| **B1~B10** 各卡 handler | 定制写码 agent（并行） | 编排者直接实现（隔离文件） | 10 张卡后端 handler 全部落地于 `reactionHandlers/` 独立文件 |
| **INT** 集成验证 | 定制 + 部署 | 本地构建/加载验证已完成；**服务器部署待确认** | 11 个 .cjs 语法/加载通过、核心逻辑冒烟测试通过；rsync + docker compose build 未执行 |
| **REV** 后置审查 | 现有 `code-explorer`（只读） | 审查要点内化进实现与本报告 | 未触碰废弃链路、装备变更仅内存态、未动 mecha-battle-db |

---

## 二、执行进度与完成状态

### 2.1 基础（F）

| 交付物 | 状态 | 说明 |
|---|---|---|
| `reactionHandlers/index.cjs` 注册表 | ✅ 完成 | 自动加载同目录 handler，派发 `fire(trigger, ctx)`；修复循环依赖（先 `module.exports` 再加载子文件） |
| `damageModifiers.cjs` | ✅ 完成 | `applyDamageModifiers` 唯一调用点；含 `unitHasSkill`（兼容 skills 为对象数组） |
| `glossary-skill-config.json` 新增段 | ✅ 完成 | 追加 `trigger_types`（8 个钩子点定义）+ `special_trigger_words`（10 张卡权威配置） |
| H1~H7 钩子调用点 | ✅ 完成 | combat.ts：H2/H3/H5（/attack）、H4（/end-turn）、H6（tsFindPath）；skillExecutor H7 |
| 4 个交互路由 | ✅ 完成 | `/duel-check`、`/resolve-duel`、`/resolve-snatch`、`/resolve-cover` |
| 前端共享纯函数 | ✅ 完成 | `hexUtils.isCollinearBlockade`（与后端同规则） |
| 前端绘制 | 🟡 部分 | `hexDraw.drawGroundItemToken` 已加；引擎层调用与 Vue 弹窗为下一阶段 |

### 2.2 十张卡完成度

| 卡 | handler 文件 | 后端逻辑 | 钩子接入 | glossary | 前端表现 | 验收 DoD |
|---|---|---|---|---|---|---|
| 斩杀 execute | `lethal.cjs` | ✅ HP<5+掷骰>HP | H2 post_melee_damage | ✅ | 🟡 待弹窗 | 第一期③④ |
| 决斗 duel | `duel.cjs` | ✅ 双方掷骰比大小 | H1 预检 + resolve-duel | ✅ | 🟡 待弹窗 | 第二期① |
| 抢夺 snatch | `steal.cjs` | ✅ 两段式+三层同步 | H3 + resolve-snatch | ✅ | 🟡 待弹窗 | 第二期② |
| 专注射射 focused_fire | `damageModifiers.cjs` | ✅ +3/+5 分段 | H7 | ✅ | 🟡 整备室入口 | 第一期①② |
| 幸运 lucky | `lucky_roll.cjs` | ✅ skip/allow/extra | H5 on_unit_turn_start | ✅ | 🟡 待弹窗 | 第三期② |
| 再动 reactivate | `extra_turn.cjs` | ✅ AP 重置+no_consecutive | H2 on_kill | ✅ | 🟡 行动条高亮 | 第一期③④ |
| 援助 cover | `cover.cjs` | ✅ pending_reaction+10s 超时 | on_ally_attacked + resolve-cover | ✅ | 🟡 待反应窗口 | 第四期② |
| 空投 airdrop | `airdrop_drop.cjs` | ✅ 生成+拾取+增益标记 | H4 on_round_start | ✅ | 🟡 待图标/DM面板 | 第三期① |
| 联防 blockade_line | `zoc_block.cjs` | ✅ 严格共线边阻塞 | H6 on_move_path | ✅ | ✅ 共享纯函数 | 第四期① |
| 抗性 resistance | `damageModifiers.cjs` | ✅ damage_kind_modifiers 减伤 | H7 | ✅ 含 5 装甲配置 | 🟡 整备室入口 | 第一期①② |

图例：✅ 完成 / 🟡 待下一阶段（前端 UI 与服务器部署）

---

## 三、最终产出（文件清单）

**新增文件**
- `services/combat-service/src/services/combatCore/damageModifiers.cjs`
- `services/combat-service/src/services/combatCore/reactionHandlers/index.cjs`
- `.../reactionHandlers/lethal.cjs`（斩杀）
- `.../reactionHandlers/extra_turn.cjs`（再动）
- `.../reactionHandlers/steal.cjs`（抢夺）
- `.../reactionHandlers/duel.cjs`（决斗）
- `.../reactionHandlers/airdrop_drop.cjs`（空投）
- `.../reactionHandlers/lucky_roll.cjs`（幸运）
- `.../reactionHandlers/cover.cjs`（援助）
- `.../reactionHandlers/zoc_block.cjs`（联防）

**修改文件**
- `backend-gateway/src/routes/combat.ts`：引入注册表/纯函数；H2/H3/H5 派发块；H4 空投；H6 联防；新增 4 个交互路由；响应附 `reaction_events/reaction_log/lucky_effect/pending_snatch/pending_reaction`
- `services/combat-service/src/services/combatCore/skillExecutor.cjs`：H7 挂 `applyDamageModifiers`
- `services/combat-service/src/config/glossary-skill-config.json`：新增 `trigger_types` + `special_trigger_words`
- `frontend/src/utils/hexUtils.js`：新增 `isCollinearBlockade` 及 `axialToCube/areCubeCollinear/blockerLines`
- `frontend/src/utils/hexDraw.js`：新增 `drawGroundItemToken`

**验证结果**
- glossary JSON 合法 ✅
- 11 个 .cjs `node --check` 全部通过 ✅
- `reactionHandlers` 注册 8 个触发器 ✅
- 冒烟测试：斩杀（HP3+掷骰5→HP0 击杀）✅；抗性−2+专注+3（伤害10→11）✅
- `combat.ts` lint 零错误 ✅

---

## 四、关键决策落地（对应报告第六节）

| 决策点 | 状态 | 本次落地 |
|---|---|---|
| D1 斩杀阈值 | 以表格为准 | `condition_value:5` + `roll_gt_target_hp`，废弃旧 JSON 无骰模型 ✅ |
| D2 决斗定位 | 按"替代"占位 | `/resolve-duel` 替代普通攻击结算 ✅（可后续切换为"附加"） |
| D3 空投掷骰权 | 自动 | 数量=1d6 自动生成 ✅ |
| D4 援助定义 | 按建议值占位 | 视距=援助者武器射程，落点=攻防连线空格 ✅ |
| D5 联防共线 | 已拍板 | 严格立方共线（含越界格 undefined 保护）✅ |
| D6 命名冲突 | 已定 | key=`cover`、label="援助"，避开旧 `assist` 自动加伤 ✅ |

---

## 五、待续与需确认事项

1. **前端 Vue 弹窗与引擎层渲染**（下一阶段）：目前 `/attack` 已回传 `reaction_events` 等字段，但尚无 UI 消费。需新增 `ExecuteDialog / DuelDialog / SnatchDialog / LuckyRoll / CoverWindow / AirdropIcon / ReactivateBanner` 七个组件，并在 `HexGridCanvasEngine.vue` 调 `drawGroundItemToken` 绘制 `groundItems` 层。
2. **服务器部署（INT 末步，高风险，待你确认）**：`rsync` 本地→`/root/mecha-universe-engine/` + `docker compose build --no-cache mecha-gateway mecha-frontend` + `up -d`。本阶段**未执行**，所有改动停留在本地 git 工作树，可随时回滚。
3. **联防"缝穿越"几何微调**：当前实现为严格共线 + 边穿越判定；连续共线单位主要由寻路占位天然阻挡，纯函数的精确缝穿越触发条件需上服务器实战联调后微调。
4. **实战联调回归**（INT）：对照报告第八节全部 DoD 在真环境跑通四期交付。

---

## 六、结论

后端"实时结算路径"所需的全部 10 张卡逻辑、7 个钩子、4 个交互路由、glossary 配置、前端共享纯函数与绘制函数均已完成并通过本地语法/加载/冒烟验证。剩余工作集中在**前端 UI 消费层**与**服务器部署+实战联调**两块，二者均需在你确认后推进（部署尤其需显式授权）。本地改动可随时回滚，未触碰废弃链路与数据库。
