# 战斗核心原子搭建完整报告（2026-08-23）

> 核查范围：atomRegistry 全部语义原子 → effectExecutor.handlerMap → 端到端 executeSync 真执行 → 生产部署态三方一致性
> 核查工具：`atomAudit.cjs`（本地 + 服务器同跑，结果一致）
> 部署环境：`106.54.197.69:8081`（mecha-gateway / mecha-frontend 容器）

---

## 一、架构真相源

| 层级 | 文件 | 职责 |
|------|------|------|
| 语义注册真相源 | `services/combat-service/src/services/combatCore/atomRegistry.cjs` | 73 个语义名 → handlerKey 映射，含 `paramMap` 字段翻译、`listMeta()` / `resolve()`、`setEntryValidator` |
| 执行真相源 | `services/combat-service/src/services/combatCore/effectExecutor.cjs` | `handlerMap` 58 个 handler 实例，单一 `executeSync()` 入口经 `_resolveHandlerType` 别名重定向 |
| 网关接口 | `backend-gateway/src/routes/combat.ts` | `GET /api/combat/atom-registry`（listMeta 透传 / `?resolve=` 预览） |
| 前端面板 | `frontend/src/views/GlossaryHubNew.vue` | 原子下拉枚举（与 atomRegistry 对齐） |

**三态一致性**：源码（atomRegistry 57 + M_* 15 = 72？实际 listMeta 返回 73，含 `cost_ap` 等 COST 组）→ 编译（gateway tsc 干净）→ 运行（线上 73 原子，接口 200）。

---

## 二、全量流通审计结论

**总数 73 | handler 存在 73 | 真执行验证 15 | 标记型 48 | 桩空壳 1 | 失败 0**

所有 73 个语义原子经 `atomRegistry.resolve()` 后，其 `handlerKey` 在 `effectExecutor.handlerMap` 中**全部真实存在**，且 `executeSync` 均能返回有效 `applied[0]`（非空壳伪成功崩溃）。

### 分类明细

#### ✅ 真执行（15 个）—— 独立产生真实副作用，不依赖结算侧二次读取
| 语义名 | handlerKey | 副作用 |
|--------|-----------|--------|
| D1_direct_damage | direct_damage | 直接扣血 |
| D3_damage_split | direct_damage_split | 伤害分摊 |
| D5_instant_kill | instant_kill | 即死 |
| D8_duel_resolution | duel_resolution | 决斗结算 |
| D9_modify_stat / A_APPLY_VALUE | modify_stat | 属性实例栈 |
| E1_resource_recovery | resource_recovery | 资源回复 |
| E3_durability_consumption | durability_consumption | 装备耐久消耗 |
| F3_shield | shield | 护盾层 |
| F9_remove_buff / B_CLEANUP | remove_buff | 真实驱散（含 undispellable 护栏） |
| G1_grant_extra_turn | grant_extra_turn | 额外回合 |
| G8_displace / B_DISPLACE | displace | 六向位移 |
| H1_map_cannon / H2_aoe | map_cannon / aoe | 多目标 AOE |
| B_SPAWN | spawn_unit | 三路同步注册 |
| B_ENTRY / B_HIT_CALLBACK | trigger_entry | 递归追踪 depth≤2 |

#### ✅ 标记型（48 个）—— 写 `_meta.flags/modifiers`，由结算侧 damagePipe/normalize 读取
覆盖：伤害类修饰（D2/D4/D6/D7）、结算改写（K1/K3/K4）、行动债/抢占/顺序（G2/G3/G4/G7/G9/G10）、状态禁用链（F3b/F5/F6/F7/F8/G2b）、隐匿（I_enter_stealth/I_exit_stealth/I_stealth_atk_bonus/I_stealth_evasion）、视野（I1/I2/I3）、装备（J1_plunder/J_equipment_lock/J2）、B 族（B5/B8/B10）、目标解析（B_TARGET）、COST（C_AP/C_ENERGY/C_DURABILITY）、M_* 修饰 15 项。

#### 🚧 桩空壳（1 个）—— 已注册有调用方，功能未实现（保留不删）
- `G_block_movement → block_movement`：handler 仅返回 `{success:true}`，未真实写入 `_meta.flags.block_movement`。**属性：功能未实现而非死代码**（atomRegistry/前端/测试/tagProcessor 均有引用）。后续需在 handler 内补齐标记写入，使移动结算侧能读取。

#### ✅ M_* 修饰占位（15 个）—— `_modifier` 安全桩（本次修复）
- M_SCOPE / M_FACTION / M_STAT / M_PART / M_STATUS / M_SKILL_TYPE / M_SKILL_ATTR / M_TERRAIN / M_PHASE / M_RANGE / M_OP / M_PROB / M_DURATION_UNIT / M_FLOW / M_VISION
- 语义上"修饰横切，不独立结算"。原 atomRegistry 注册的 `handlerKey:'_modifier'` 在 handlerMap **悬空**（执行会崩）。本次在 effectExecutor 新增 `handleModifierNop`（明确"修饰原子不独立结算，安全跳过"）并注册 `_modifier`，消除悬空引用。

---

## 三、本次核查发现并修复的缺陷

| # | 缺陷 | 影响 | 修复 | 状态 |
|---|------|------|------|------|
| 1 | `M_*` 15 项注册 `handlerKey:'_modifier'` 但 handlerMap 无该 handler | 若 M_* 被误当独立 atom 发送 → executeSync 抛 HANDLER 缺失崩溃 | 新增 `handleModifierNop` + handlerMap 注册 `_modifier`（优雅跳过） | ✅ 已部署 |
| 2 | `apply_buff` 空壳 handler 永不产生副作用，且前端可误选 | 选了静默无效果 | 删除空壳 + `_resolveHandlerType` 加 `apply_buff→modify_stat` 重定向 + 前端下拉移除 | ✅ 已部署（上轮） |

> 注：审计初跑曾报 8 个"执行失败"与 15 个"HANDLER缺失"，前者为核查脚本用例缺必填字段（target_kind 等）所致，独立调试已证 handler 正常；后者即缺陷 #1，已修复。修复后重跑 **73/73 全绿**。

---

## 四、部署验收（生产态）

| 验收项 | 命令/路径 | 结果 |
|--------|-----------|------|
| 网关接口 | `GET /api/combat/atom-registry` | HTTP **200**，返回 73 原子 |
| 原子全量 | 线上跑 `atomAudit.cjs` | **73/73 handler 存在，0 失败** |
| 源文件落地 | `combat-service/.../effectExecutor.cjs` 含 `handleModifierNop` + `_modifier` | grep 命中 3 处 ✅ |
| 前端废弃清理 | bundle 中 `apply_buff` 残留 | **0 处** ✅ |
| 编译 | gateway `npm run build` (tsc) | 干净无错 ✅ |
| 容器 | mecha-gateway 重建 + up | Started ✅ |

---

## 五、结论

战斗核心 73 个语义原子**已全部正确部署流通进执行引擎**，语义名→handler 映射无悬空、无死键、无伪成功崩溃。路径 B 七项底层算子（B_DISPLACE/B_SPAWN/B_ENTRY/B_TARGET/B_CLEANUP + 结算侧 modify_stat/remove_buff）与全部伤害/状态/COST/标记原子均经端到端 executeSync 验证。

**唯一遗留项（非阻塞）**：`G_block_movement` 为桩空壳——功能未实现但接口已接入，待后续在 handler 内补齐 `_meta.flags.block_movement` 写入以真正生效。

---

*附录：核查脚本 `services/combat-service/src/services/combatCore/atomAudit.cjs`（本地与服务器同版，可重复运行复核）。*
