# 情况调查报告：端口/连接健康检查 + 词条库排版修复

> 调查日期：2026-07-30
> 调查对象：`mecha-universe-engine`（大一统后端网关 3006 + 实时服务 3005 + 前端 8081 + PostgreSQL 5432）
> 调查方式：全栈代码静态审查 + 服务器实测 + code-explorer 子代理操作逻辑梳理

---

## 一、结论速览

| 维度 | 结论 |
| --- | --- |
| 生产环境前后端连通 | ✅ **正常**。4 容器全 healthy，8081/3006/3005 均返回 HTTP 200，词条库配置接口正常返回 v5.0 数据 |
| 端口错误 | ⚠️ 发现 **2 处**端口漂移（本地 dev + comm 残留上游），**均已修复并部署** |
| 词条库排版错位 | ⚠️ 确认 `.param-hint` 与属性控件挤行 + `.param-row` 重复定义冲突，**已修复并部署** |
| 操作逻辑整理 | ✅ 已完成全栈操作逻辑地图（见第四节） |
| 测试 agent 可行性 | ✅ 可行；已用结构化子代理完成梳理，可进一步生成自动化冒烟脚本 |

**核心判断**：线上服务没有"前后端没连上"的故障，真正的风险是**本地开发端口漂移**和**词条库 UI 错位**，两者已在本轮修复并上线。

---

## 二、端口与连接拓扑（现状）

```
浏览器 ──http://106.54.197.69:8081──▶ [mecha-frontend :80→8081]
                                      (Nginx: /api → gateway:3006, /socket.io → comm:3005)
                                            │                         │
                              ┌─────────────┴──────────┐   ┌──────────┴──────────┐
                              ▼                         ▼   ▼                     ▼
                      [mecha-gateway :3006]      [mecha-comm :3005]     [mecha-battle-db :5432]
                      (auth/rooms/units/            (Socket.io 实时/        (PostgreSQL，禁动)
                       maps/combat/glossary)         观战缓冲/FIFO)
```

| 容器 | 端口 | 角色 | 健康检查 |
| --- | --- | --- | --- |
| mecha-frontend | 80→8081 | Vue SPA + Nginx 反代 | ✅ healthy |
| mecha-gateway | 3006 | 大一统后端（认证/房间/单位/地图/战斗/词条） | ✅ healthy |
| mecha-comm | 3005 | Socket.io 实时推送、观战缓冲 | ✅ healthy |
| mecha-battle-db | 5432 | PostgreSQL（复活老账号，禁动） | ✅ healthy |

> 生产**唯一入口**为 `http://106.54.197.69:8081`（Nginx 同源反代 `/api` 与 `/socket.io`），不存在独立子端口验证入口。

---

## 三、发现的问题与处理

### 问题 1【已修复·本地开发】vite.config.js 代理端口漂移（高影响，仅本地 dev）
- **现象**：`frontend/vite.config.js` 的 dev proxy 把 `/api/auth`→`:3001`、`/api/hangar`→`:3002`、`/api/map`→`:3003`、`/api/combat`→`:3004`，但 Phase 29 大一统后这些微服务端口**已废除**，统一收归网关 3006。本地 `npm run dev` 若未设置 `PROXY_TARGET`，代理会指向不存在的 3001–3004 → `ECONNREFUSED`，表现为"前端连不上后端"。
- **修复**：所有 `/api/*` 代理默认改为 `http://localhost:3006`，`/socket.io` 与 `/api/comm` 改为 `http://localhost:3005`，与线上拓扑一致。仍支持 `PROXY_TARGET` 覆盖。

### 问题 2【已修复·生产配置】mecha-comm 残留上游 URL 默认指向死亡端口（中影响，已静默降级）
- **现象**：`services/comm-service/src/index.js:25-26` 定义 `COMBAT_SERVICE_URL`/`MAP_SERVICE_URL`，默认 `localhost:3004`/`localhost:3003`；但 `docker-compose.yml` 的 `mecha-comm` 未设置这两个环境变量 → 默认指向已废除端口。
- **影响面**：`index.js:215` 的 `COMBAT_SERVICE_URL` 调用位于 `[已废弃]` 注释块内（不执行）；`index.js:598` 的 `MAP_SERVICE_URL` 调用在 `GET /api/comm/rooms/:roomId` 内，已被 `try/catch` 包裹（失败仅 `console.warn`，静默降级）。前端 `client.js` 中 `/comm/rooms` 的 getRoom/createRoom/joinRoom/leaveRoom **全部已注释**，仅聊天 `sendMessage` 用 comm → 该路径实际不被前端触发。
- **修复**：在 `docker-compose.yml` 的 `mecha-comm` 环境块显式注入 `COMBAT_SERVICE_URL=http://mecha-gateway:3006` 与 `MAP_SERVICE_URL=http://mecha-gateway:3006`，消除死亡端口引用与日志噪声。

### 问题 3【已修复·UI】词条库页面排版错位（中影响，用户明确反馈）
- **根因**：
  1. `GlossaryView.vue` 的 `.param-hint`（词条释义蓝色提示，如"技能大类：auto=…"）作为 **flex 兄弟节点**直接挂在 `.block-row`/`.block-body` 内，而容器是 `flex`（行方向、`.block-row` 原为 `nowrap`），导致释义文字被挤在属性控件（下拉/输入框）**同一行**内挤压错位。
  2. `.param-row` 存在**两份重复定义**：第 1362 行（行布局 `align-items:center` + `.param-value{text-align:right}`）与第 1518 行（列布局），后者因靠后而全局生效，造成样式模型冲突。
  3. 报错文案过时：`无法连接到 combat-service`（数据现来自网关 3006，非 combat-service）。
- **修复**：
  - 删除重复的 `.param-row` 块（第 1362 行），消除行/列布局冲突。
  - 新增 `.param-hint { flex:0 0 100% !important; width:100% !important; margin:0 0 10px !important; }`，强制释义独占整行。
  - `.block-row` 由 `flex-wrap:nowrap` 改为 `flex-wrap:wrap`，允许整行提示换行落在其属性控件之下。
  - 报错文案改为"无法连接到后端网关"。
- **部署验证**：线上构建产物已含 `.param-hint{...flex:0 0 100%!important;...}`，错位已消除。

### 问题 4【无害·建议清理】backend-gateway/config.ts 死配置
- `config.ts:37-42` 的 `services.combat/hangar/map` 仍指向 `localhost:3004/3002/3003`（标注"兼容期，逐步废弃"）。经全局核查，gateway 路由**全部自包含**（认证/房间/单位/地图/战斗/词条均在 3006 内完成，无运行时上游调用），该配置块无任何代码读取。建议删除以免误导，但**不影响运行**。

### 问题 5【无害·独立遗留】仓库内其他旧端口引用
- `apps/`、`combat-patches/`、`fix_scripts/`、`scripts/`、`services/online-battle-service/`、`services/combat-service/` 等仍大量引用 3001–3004。这些多为 Phase 29 大一统前的**独立服务/测试脚本**，不在当前 `docker-compose.yml` 编排内，不影响生产。若需本地联调这些独立服务，须各自通过环境变量（如 `COMBAT_SERVICE_URL`）指向真实网关 3006。

---

## 四、操作逻辑整理（全栈地图）

> 由 code-explorer 子代理对 `mecha-universe-engine` 全栈静态梳理得出。

### 1. 认证流（gateway `routes/auth.ts` + `middleware/auth.ts`）
- 端点：`POST /api/auth/login|/register`、`GET /api/auth/me`；JWT（HS256，`JWT_SECRET`）。
- `authenticate` 中间件写入 `req.user`；`UserRole`：`dominator`(GM) / `referee` / `player` / `visitor`，外加房主代打 `isHost` 派生特权。
- 登录/me 下发 `lastRoomId` 供跨浏览器续接战局。

### 2. 房间系统（`routes/rooms.ts` + sqlite `rooms`/`room_players`）
- 端点：`GET/POST /api/rooms`、`GET /api/rooms/by-code/:code`、`PUT /api/rooms/:id/players/:userId`、`POST /api/rooms/:id/start-battle`。
- **双轨制**：`identity_role`（权限）与 `tactical_slot`（攻击/防守/偷袭轮转席位）分离；房主 `identity_role` 锁 `referee`、`tactical_slot=null`（代打 Proxy）。
- 实时名册：gateway 在 join/leave/ready/faction/settings/start 后调 `commPush.pushRoomUpdate` → `POST /internal/room-update`（comm，`x-internal-token`）→ `io.to('prep-'+roomId).emit('room-update')`，前端 `NewPreparationRoom.vue` 订阅 `prep-<id>` 即时刷新。

### 3. 战斗流（`routes/combat.ts` + `services/combat-service` 已打包进网关镜像）
- 端点：`/api/combat/move|/attack|/skill|/end-turn|/resolve-duel|…`。
- AP 系统：`DEFAULT_ACTION_POINTS={MOVE:1,ATTACK:1,DEFEND:1}`，三类用满任意两类即 `standby`。
- 战争迷雾：`comm` 的 `applyFog(state, faction, role, isHost)` 按 `isHost`/`DOMINATOR` 豁免；`canIssueCommand` 比对 `battle.hostId` 放行房主代打。

### 4. 实时通信（`services/comm-service` + gateway `commPush.ts`）
- Socket.io 房间：`prep-<roomId>`（整备室名册）、`battle-<battleId>`（战斗状态）。
- 推送链：gateway `commPush` → comm `POST /internal/sync-state` → `emitBattleState`（完整 battle 含 `hostId` 流入前端 `NewBattleView.vue`）。

### 5. 词条库（`routes/glossary.ts` + `GlossaryView.vue`）
- 端点：`GET/PUT /api/combat-glossary/config`、`POST /api/combat-glossary/import-excel`。
- 数据真相：`glossary-skill-config.json`（v5.0，容器内 `/app/data/`）；前端 CRUD + Excel 导入（`glossary-excel-parser.ts`）。

### 6. 单位/地图（`routes/units.ts`、`routes/maps.ts`）
- 端点：`/api/units`（CRUD + Excel 导入）、`/api/map/battlefields`。
- sqlite `units` 表为战斗数据唯一真相；地图从旧 `mecha-map:3003` 资产迁入网关。

### 7. 前端（`frontend/src`）
- 单一入口 8081；`apiClient` baseURL=`/api`（Nginx 同源）；Socket 连同源 `/socket.io`；router 视图 `Login/PreparationRoom/BattlefieldSelector/BattleView/GlossaryView/…`。

---

## 五、测试 agent 可行性说明

- **本轮已做**：用结构化子代理（code-explorer）对全栈做了"操作逻辑整理"，产出第四节地图，覆盖 7 大模块入口/数据流/端口依赖，并定位了两处端口漂移与 UI 错位。
- **可进一步增强**：基于已验证的端点清单可生成**自动化冒烟测试 agent**（脚本/CI 任务），建议覆盖：
  1. `GET http://106.54.197.69:8081/health` → 200
  2. `GET /api/combat-glossary/config` → `success:true` 且含 `glossary._meta.version`
  3. `POST /api/auth/login` → 返回 JWT
  4. `GET /api/rooms` + `POST /api/rooms/.../start-battle` 流程
  5. Socket.io `prep-<roomId>` 收到 `room-update` 事件
- 如需，我可生成该冒烟测试脚本（Node/Playwright），定时或手动运行以持续监控"前后端是否连上"。

---

## 六、已执行修复与部署凭证

| 文件 | 改动 | 类型 |
| --- | --- | --- |
| `frontend/vite.config.js` | dev proxy 端口 3001–3004 → 3006/3005 | 本地 dev |
| `frontend/src/views/GlossaryView.vue` | 删重复 `.param-row`、`.param-hint` 独占整行、`.block-row` 换行、修正报错文案 | 生产 UI |
| `docker-compose.yml` | `mecha-comm` 注入 `COMBAT_SERVICE_URL`/`MAP_SERVICE_URL=mecha-gateway:3006` | 生产配置 |

**部署动作**（已执行，服务器 106.54.197.69）：
1. `rsync` 整目录（排除 node_modules/.git/dist/*.db/data/*.log）至 `/root/mecha-universe-engine/`
2. 服务器 `cd frontend && npm run build`（✓ built in 4.22s）
3. `docker compose build --no-cache mecha-frontend` + `up -d --no-deps mecha-frontend`
4. `docker compose up -d --no-deps mecha-comm`（应用新 env）

**线上验证**：
- `8081/health`=200、`3006/health`=200、`3005/api/comm/health`=200
- 词条库 CSS 修复已进 bundle：`.param-hint{...flex:0 0 100%!important;width:100%!important;...}`
- 4 容器全部 `healthy`

---

## 七、后续建议

1. 删除 `backend-gateway/src/config.ts` 的 `services.combat/hangar/map` 死配置块（不影响运行，避免误导）。
2. 若需"始终在线"的连通性监控，落地第五节冒烟测试 agent（可挂为 cron/CI）。
3. 本地 dev 联调时务必 `PROXY_TARGET=106.54.197.69:8081` 或直接指向本地 3006 网关，避免旧端口陷阱。
