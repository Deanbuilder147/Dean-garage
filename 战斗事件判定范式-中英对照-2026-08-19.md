# 战斗事件判定范式（中英对照）

> 版本：v1.0 ｜ 日期：2026-08-19
> 目的：梳理传统 RPG（尤其是战棋 / TRPG）编程中"套路化"的事件判定句式（fixed event-predicate patterns），
> 并标注本项目（mecha-universe-engine）中对应的真实代码落点。
> 约定：所有英文术语 / 代码标识符后均附中文翻译（Chinese translation）。

---

## 0. 核心范式（Core Pattern）

传统 RPG 事件系统本质是同一个模板（template）：

```
On<事件 Event>(参数 Args) {
  if (<条件 Condition>) <响应 Response>
}
```

即：**触发器（Trigger / 触发器）+ 条件谓词（Predicate / 谓词）+ 响应（Response / 响应）** 三段式。
本项目把这套范式"配置驱动化"：词条 JSON（glossary entry / 词条）驱动谓语 switch（predicate switch / 谓词分支），而非硬编码 if-else。

---

## 1. 触发器判定（Trigger / 触发器）

| 传统句式 | 中文 | 本项目落点 |
|----------|------|-----------|
| `OnUnitEnterTile(unit, tile)` | 单位进入格子 | 移动结算 `/move` → `Dijkstra`（Dijkstra 寻路）地形加权 |
| `OnTurnStart(faction)` / `OnTurnEnd(faction)` | 回合开始 / 回合结束 | 网关 `/end-turn`：isNewRound 时 `round+1` + 重置 AP（Action Point / 行动点） |
| `OnHPBelow(unit, threshold)` | 血量低于阈值 | `damagePipe.cjs` 伤害管线结算后判定 `hp <= 0` → `OnDeath` |
| `OnSkillCast(skill, caster, target)` | 技能释放 | 网关 `/attack`、`/skill` → `executeUniversalSkill`（执行通用技能） |
| `OnDeath(unit)` / `OnKill(killer, victim)` | 死亡 / 击杀 | 状态机 `statusEffects`（状态效果）写回双方 |

---

## 2. 命中 / 伤害判定（Hit / Damage / 命中 / 伤害）

| 传统句式 | 中文 | 本项目落点 |
|----------|------|-----------|
| `if (rollDice(attackRoll) >= targetDefense) → hit` | 掷骰 ≥ 防御则命中 | `diceService.cjs` 的 `roll(expr)` / `check(roll, target)`（唯一掷骰真相源 / single source of truth） |
| `if (hit && rollCrit()) → critical` | 命中且暴击判定 → 暴击 | `diceService` 的 `critThreshold` / `critMin` / `critMax`（暴击阈值 / 暴击下限 / 暴击上限） |
| `damage = base + variance() - reduction` | 伤害 = 基础 + 浮动 - 减伤 | `damagePipe.cjs`：`_calcDefense`（累加防御）+ 地形/装备/技能减伤 |

> 关键红线：掷骰逻辑已收拢到 `diceService.cjs`（Phase 30-DiceUnify），全局参数集中在
> `glossary-skill-config.json` 顶层 `dice` 段（rollMult[6] / critThreshold / availableDiceTypes 等），
> 引擎其他模块一律委托（delegate），不得自行 `Math.random()` 掷骰。

---

## 3. 范围 / 距离判定（Range / Distance / 射程 / 距离）

| 传统句式 | 中文 | 本项目落点 |
|----------|------|-----------|
| `if (distance(a, b) <= weaponRange) → in range` | 距离 ≤ 武器射程 → 在范围内 | `shared-kernel/src/hexMath.ts` 的 `isTargetInRange(sq, sr, tq, tr, {minRange, maxRange})`（目标在射程内判定） |
| `if (hexDist <= moveRange) → movable` | 六角距离 ≤ 移动力 → 可移动 | `Dijkstra` 移动寻路 + 地形权重（terrain weight / 地形权重） |
| `if (isInAoE(center, target, radius))` | 在范围效果内 | 技能 `cast_range` / `aoe_radius`（施法范围 / 范围半径）词条字段 |

> 射程核定真相源（Phase 31-RangeNormalize）：`DEFAULT_RANGE_BY_CATEGORY`（按分类默认射程）、
> `resolveSkillCategory`（解析技能分类）、`getSkillRangeFields`（获取射程字段）。
> 远程唯一真相值 = **3**；普通攻击已彻底删除（Phase 32-NoBasicAttack），射程只来自技能自身 `cast_range` 词条。

---

## 4. 状态 / 阵营判定（Status / Faction / 状态 / 阵营）

| 传统句式 | 中文 | 本项目落点 |
|----------|------|-----------|
| `if (unit.faction == enemyFaction)` | 固有阵营 == 敌方阵营 | ⚠️ **红线禁止**：禁止直接比 `u.faction` |
| `if (unit.role == 'attack')` | 轮转角色 == 攻击 | ✅ 唯一逻辑依据：用 `unitRoleOf(u) = u.role \|\| u.faction` 兜底后比较 `unit.role` |
| `if (unit.hasStatus('poison'))` | 单位持有状态"中毒" | `statusEffects[]`（状态效果数组）+ `effectExecutor`（效果执行器） |
| `if (isSpectator(player))` | 是否为观战者 | 房间系统 `room_players.is_spectator`（是否观战） |

> 双语义红线（dual-semantic red line）：
> - `faction`（固有阵营 / inherent faction：earth / maxion / balon）= 仅展示 / UI 着色（display / UI coloring）
> - `role`（轮转角色 / rotating role：attack / defense / ambush）= 逻辑判定**唯一**依据（敌我 / 掩护 / ZOC / 可见性 / 胜负 / 技能发放）
> - 任何比较都走 `unit.role`，绝不直接比 `unit.faction`。

---

## 5. 交互事件（UI / Interaction / 交互）

| 传统句式 | 中文 | 本项目落点 |
|----------|------|-----------|
| `onCellClick(q, r)` | 点击格子 | `HexGridCanvasEngine.vue` emit `cell-clicked`（格子被点击）→ `{q, r, screen}` |
| `onUnitSelect(unit)` | 选中单位 | NewBattleView 的 `selectUnitById` / 阵营列表 `rsSelectedUnit` |
| `onSkillSelect(skill)` | 选中技能 | 技能栏 `selectedSkill` → 走 `/attack` 或 `/skill` |

触控扩展（方案 A，仅 `/battle-mobile`）：
- `OnTap(q, r)`（点按 / Tap，Slop 8px 内）→ 复用 `cell-clicked`
- `OnPan(dx, dy)`（单指平移 / pan）
- `OnPinch(factor, pivot)`（双指捏合缩放 / pinch，锚点锁中点 / pivot lock）

---

## 6. 词条驱动谓语（Glossary-Driven Predicate / 词条驱动谓词）

本项目对"触发器 + 条件"的最高层抽象：

```
skillExecutor.cjs v5.0:
  switch (predicate) {        // 谓语 / 谓词
    case 'attack':  ...        // 攻击
    case 'heal':    ...        // 治疗
    case 'buff':    ...        // 增益
    case 'debuff':  ...        // 减益
    case 'passive': ...        // 被动
    default:       ...         // 未知落 default
  }
```

词条 JSON（glossary entry / 词条）携带 `cast_range`（施法范围）、`effect_type`（效果类型）、
`timing`（时机）、`roll_mode`（掷骰模式）等字段，由 `shared-kernel/src/enums.ts` 的枚举真相源
（DEFAULT enums：TIMING / EFFECT_TYPE / ROLL_MODE / LIMIT_SCOPE / VALUE_METHOD）统一约束，
消除"编辑器枚举 ≡ shared-kernel 枚举 ≡ handler key"三头分化。

---

## 7. 一句话总结（Summary）

传统 RPG 的固定判定句式，本质是 **`On<事件>(参数) { if (<条件>) <响应> }`** 模板，
贯穿五大层面（触发器 / 命中伤害 / 射程距离 / 阵营状态 / UI 交互）。
本项目的战斗引擎把这五大范式"配置驱动化"：以词条 JSON 为输入、以 `skillExecutor` 谓语 switch 为分发、
以 `diceService` / `shared-kernel` 为数学与枚举真相源，而非散落的硬编码 if-else。
