# 战斗核心 × 六段式 盘点与合并方案报告

> 生成日期：2026-08-19
> 范围：`mecha-universe-engine` 战斗引擎（`combat-service` + `backend-gateway` + `shared-kernel`）
> 目标：盘点"传统 RPG 战斗核心"与"六段式（WHEN/IF/WHO/ROLL/DO/COST 递归树）"两套现状的真实运作逻辑，诊断断点，给出将二者**揉成一个统一规则引擎内核**的具体合并方案。

---

## 〇、一句话结论（先给答案）

**两套并非"二选一"关系，而是"语法层已建好、执行层已建好、但二者之间没接通数据"的断裂态。**

- **六段式语法层（tree 契约 + `_walkPhaseTree` 遍历器）已真实落地**，不是空想。
- **传统 RPG 执行层（`damagePipe` / `effectExecutor` / `diceService` / `combatIntegrator` 点火链）已真实运行**，不是摆设。
- **真正的断点**：业务词条 JSON 里 `tree` 字段覆盖率为 **0**（9 条全为 v1 扁平 `effects[]`），所以六段遍历器**从未被真实数据触发**；且触发命名三头分化，WHEN 闸门挂不进统一点火体系。

**因此"合并作业"不是重写，而是**：把现有 v2 遍历器从"已建未用"接成"唯一真相路径"，让 `tree` 成为词条唯一描述形态，让 `damagePipe`/`effectExecutor` 降级为遍历器在 DO/ROLL 段调用的原子零件。**这是低风险渐进熔铸，不是推倒重来。**

---

## 一、传统 RPG 战斗核心 · 现状盘点

### 1.1 模块清单（绝对路径 + 职责）

| 模块 | 路径 | 行数 | 核心职责 | 调用关系 |
|---|---|---|---|---|
| `skillExecutor` | `services/combat-service/src/services/combatCore/skillExecutor.cjs` | ~2257 | v5.0 万能语法中枢；`switch(action_type)` 谓语路由（attack/heal/buff/debuff/passive）；同时支持 v1 `effects[]` 与 v2 `tree` 双入口 | → `effectExecutor`(executeSync) / `diceService` / `damageModifiers` / `conditionEvaluator` / `branchEvaluator` / `buffManager` / `hexKey` |
| `damagePipe` | `.../combatCore/damagePipe.cjs` | ~591 | 伤害计算**固定 13 阶段**（注释写 12，实为 13，含 crit） | 被 `effectExecutor.handleDirectDamage` 与 `combatIntegrator.executeAttack` 调用 |
| `effectExecutor` | `.../combatCore/effectExecutor.cjs` | ~1641 | 按 `effects[].type` 映射 handler；注册约 **64 键**（约 60 独立 handler） | 被 `skillExecutor` 调 |
| `diceService` | `.../combatCore/diceService.cjs` | — | 唯一掷骰真相源；`Math.random()` 无 seed，**不可复现** | 被全链路掷骰调用 |
| `combatIntegrator` | `.../combatCore/combatIntegrator.cjs` | — | 攻击流程点火链：`pre_damage → on_damage → post_damage → on_kill/on_death → on_damage_taken → post_attack` | 弃用中，但 `damagePipe` 与部分 reaction 仍走它 |
| `hookChain` | `.../combatCore/hookChain.cjs` | — | 约 20 phase 钩子链 | 与 `combatIntegrator` 钩子名**不同套** |
| `reactionHandlers` | `.../combatCore/reactionHandlers/*` | — | 词条 reaction 注册（steal/lethal/zoc/duel/cover…），注册 key 用第三套命名 | 由 `combat.ts` 的 `fireReaction` 点火 |

### 1.2 传统 RPG 的实际运作逻辑

```
前端/Gateway /attack|/skill
   └─> skillExecutor.executeUniversalSkill(skillType, unit, target, ctx)
         └─ switch(action_type):
              attack  -> effectExecutor.handleDirectDamage -> damagePipe.calculate(13阶段) -> diceService 掷骰
              heal    -> effectExecutor.handleHeal
              buff    -> effectExecutor.handleBuff -> buffManager
              ...
         └─ (可选) combatIntegrator.executeAttack -> 点火链 pre_damage...post_attack
         └─ (可选) reactionHandlers.fireReaction('on_damage_dealt'...)  // 第三套命名
```

**本质**：这是一个**以 `action_type` 为入口、以硬编码阶段管道为骨架、以 `effectExecutor` handler 表为扩展点**的经典线性结算机。它的"自由度"来自 `effectExecutor` 的 handler 数量（已膨胀到 64 键），而非来自结构化编排。

### 1.3 传统 RPG 层的问题（已核实）

1. **`damagePipe` 阶段数注释与实现不符**：注释"12 阶段"，实际 `DAMAGE_STAGES` getter 为 13 阶段（含 crit）。维护者读注释会误判。
2. **`damagePipe` 暴击段用 `Math.random()` 而非 `diceService`**：绕过了唯一掷骰真相源，破坏"单点掷骰"红线，且不可复现。
3. **`effectExecutor` 严重膨胀 + 零引用原子**：约 30 个键（`E/F/G/H/I/J` 组，如 `durability_consumption`/`mutual_exclusion`/`map_cannon`/`aoe`/`plunder` 等）代码注释自认"零引用原子"；`apply_buff`/`remove_buff` 是**空壳返回**（`return {success:true}` 无副作用）。handler 表已变成"注册了但没接"的死代码坟场。
4. **触发命名三头分化**（详见第三节）。
5. **不可复现**：`diceService` 与 `damagePipe` 暴击段均用 `Math.random()`，战报无法重放/对战复盘。

---

## 二、六段式 · 现状盘点

### 2.1 定义文档归属（归置问题）

- **权威定义**：根目录 `67原子引擎落地与编辑器词库实施计划-2026-08-08.md` 之「〇.五 词条语法模型：递归六段树」「〇.六 执行语义约束」。
- **`docs/` 下**：只有 `docs/skill-hierarchy-主谓宾定状补.md`，顶部已标 **【过时文档·已废弃】**，指向新模型。即 `docs/` 仅留墓碑，新模型权威定义散落在根目录实施计划，**未落入 `docs/` 体系**——文档治理隐患。

### 2.2 六段式契约（`shared-kernel/src/contracts/phase-tree.contract.ts`）—— 数据模型权威定义

> 本节是六段式数据模型的**唯一真相源**。整合自 `67原子引擎落地与编辑器词库实施计划`（〇.五/〇.六 递归六段树正式定义）、历史对照表（C1/C2/C5/C6/C8）及本次设计对话新确认的认知（子树自相似、不必六段齐全、上下文继承意图）。后续所有实现（遍历器、归一化、编辑器）须与本节一致。

**路径**：`shared-kernel/src/contracts/phase-tree.contract.ts`

```ts
PhaseEnum = ['WHEN','IF','WHO','ROLL','DO','COST']
PHASE_TREE_MAX_DEPTH = 3
PhaseNode   = { phase, atoms: PhaseAtom[], branches: PhaseBranch[], children?: PhaseNode[] }
PhaseBranch = { label, when?, lower?, upper?, effects: PhaseAtom[], children? }
PhaseAtom   = { type: 必须是 effectExecutor handler 真名或别名, atom_key? }  // atom_key 引擎不消费
// 兼容策略：v2 与 v1 双写，引擎优先 tree，缺失回落 v1
```

#### 2.2.1 六段语义（六个维度各管一件事）

| 段 | 语义 | 类比 |
|---|---|---|
| `WHEN` | 触发事件（根节点相位，也是触发器身份） | 菜谱的"什么时候做" |
| `IF` | 条件守卫（可多分支，分支命中走各自子树） | "如果…" |
| `WHO` | 作用域 / 目标选择 | "对谁做" |
| `ROLL` | 随机性（每层独立掷骰） | "看运气" |
| `DO` | 效果（点名 atom，真正改变状态；为**有序效果列表**，可承载多谓语/多效果组合，不限于单效果，见对照表 C8） | "做什么" |
| `COST` | 代价（AP/资源，仅在 DO 之前位置扣） | "耗什么" |

六段**有序**：`WHEN → IF → WHO → ROLL → DO → COST`，顺序即执行序。`WHEN` 具双重身份——既是一棵树的「根节点相位」，也是「触发事件」本身，两者同形。

#### 2.2.2 递归结构（单相位节点 + 子树）

- **单相位节点**：一个 `PhaseNode` 只表示六段中的**一段**（`phase:'WHEN'|'IF'|...`），自己不同时持有六个槽位。六个维度靠**一层层节点串联**成树，而非塞进一个节点。`WHEN` 是**节点级可选闸门**（对照表 C1）——任何节点都可带 WHEN 作为附加触发条件，而非单独抽出的段。
- **子树挂载点**：递归 `children` / `branches[].children` 仅允许挂在 `DO` 与 `IF` 分支上（表达"执行后连锁"与"条件分叉"）；`WHEN/WHO/ROLL/COST` 的值只能是原子/标量，**绝不能是另一棵完整的六段树**（见 2.2.5 红线）。**子树在父段的 `nest` 处就地展开执行**（对照表 C2/C5），不是独立孤岛。
- **约束**：整树 `MAX_DEPTH=3`；每层 ≤6 段，每段最多 1 个 `nest` 子树。

#### 2.2.3 子树「自相似」且「不必六段齐全」（本次对话新确认 ✅）

用户设计直觉：**用同一套 `WHEN/IF/WHO/ROLL/DO/COST` 句式去描述其中复杂的一段**（句式同构 / 自相似），且**子树不必从 `WHEN` 起头、不必六段写满**——它只需补上「相对父节点多出来的那层语义」，其余维度靠上下文或留空。

> 例：根 `WHEN(被攻击)` 下，`IF(攻击者=火系)` 子树只写 IF，`DO(反伤)` 子树只写 DO，更深一层 `DO(对相邻友军+10%)` 还是只写 DO。全树靠**上下文继承**保持连贯，没有一层是六段写满的。

- **空值合法**：某段 `atoms:[]` 或整段缺失 = 该维度不参与（如 `ROLL` 为空 = 本次不用掷骰，直接用确定值）。
- **不完整合法**：子树只填关心的格子，不关心的留空或继承。
- **这要求"上下文继承"**：事件源（`WHEN` 触发的事件）、作用域（`WHO`）等须从根沿树向下传递，子树无需重复声明。

#### 2.2.4 上下文传递（与「层间不继承」的张力及本报告选边）

| 来源 | 立场 |
|---|---|
| `67原子引擎...md` 〇.六 | **层间不继承**——子层只读「全局事件上下文」不可变快照，**不读父层运算产物**（roll_result / target 等）；运行期产物传不下去被列为**断点③**。 |
| 本次对话用户模型 | **上下文继承**——子树继承父层语义（事件/作用域），且应能看到父层 `roll_result` 等产物（否则 `IF(父层掷出暴击) DO(追加伤害)` 无法实现）。 |

**本报告选边（merged 方案定调）**：采用「**默认隔离 + 显式帧传递**」折中，即步骤 3a 的 `stepFrame` 方案——
1. **全局事件上下文（triggerCtx）不可变只读快照**：`trigger_event` / `attacker` / `primary_target` / `self` / `trigger_roll` / `trigger_coord` / `trigger_value` / `skill_ref` / `battle_id` / `round`。子层只读不写，永不塞集合。
2. **`stepFrame`（帧局部栈）**：父层产生的临时结果（`roll_result` / `if_passed`）在进子层前 `push`，子层执行期间从其读取，结束 `pop()`。子层需显式声明 `reads:['parent.roll_result']` 才透传，**默认不继承**（保持隔离，杜绝递归污染），**按需继承**（满足用户"看父层产物"诉求）。
3. `WHEN` 闸门用的触发 payload 走独立 `triggerCtx`，不与 `stepFrame` 混用。

> 即：用户的「上下文继承」意图被接纳，但落地为**受控的显式继承**（stepFrame），而非无限制隐式继承——既修复断点③，又守住「单相位节点、不污染全局」的红线。

#### 2.2.5 红线：禁止「相位互套坍缩」

> 若允许「每一段的值都可是任意完整六段树且六段互相等价无限制套娃」，会退化出 `WHEN=when, IF=when, WHO=when, ROLL=when, DO=when, COST=when` 的六同型怪物，语义无限套娃、引擎无法判定执行序（本次对话推演揭示的坍缩风险）。

**硬约束**：
- 单相位节点：一个节点只属六段之一，禁止"一个节点内六槽位各自嵌套"。
- 子树只能从 `DO` / `IF` 分支挂出，且子树的"根相位"须明确（它是在父上下文里的 IF / DO / ROLL，不是重开一棵无主树）。
- `WHEN/WHO/ROLL/COST` 的值禁止是另一棵六段树（触发器/标量/作用域不应再被规则树包裹）。
- 全局参数（掷骰/范围/AP）集中在配置顶层，禁止在 tree 内硬编码。

#### 2.2.6 执行语义约束

- **层内有序**：同层六段按 `WHEN→IF→WHO→ROLL→DO→COST` 顺序执行；`COST` 段出现在 `DO` 之前的位置才扣费（资源在效果前扣除）。
- **Pre-flight 预检**：进入 `COST` 段前做合法性预检（射程/目标/AP 足够），不通过则整条 SKIP（不扣费、无副作用）。
- **partial execution 不回滚**：进入 `DO` 后若中段失败，已执行的 atom 不回滚（战术游戏语义，部分成功即部分生效）。
- **ROLL 跨层独立**：每层 `ROLL` 段各自掷骰、互不影响（不继承父层 roll_result，除非子层显式 `reads` 父帧）。

#### 2.2.7 编辑器句式（更高层抽象）

面向策划的可视化编辑器，用三段式 `On<Event>(Args){ if(<Condition>) <Response> }` 表达（`战斗事件判定范式` 0 节）——它是一棵六段树的「语法糖」：
- `On<Event>` = `WHEN` + `WHO`（事件与作用域）
- `if(<Condition>)` = `IF`（`WHO`/分支嵌套补作用域与条件）
- `<Response>` = `ROLL`/`DO`/`COST`（效果与代价，复杂 Response 用子树自相似展开）

编辑器所见即六段树，二者同构，无二次翻译损耗。

### 2.3 六段遍历器（已真实落地于 `skillExecutor.cjs`）

**关键发现**：六段执行器**不是空壳，已建好**。

- `skillExecutor.cjs:524-529`：`executeUniversalSkill` 在 `schema_version===2 && Array.isArray(tree) && tree.length` 时调用 `this._walkPhaseTree(uf.tree, ...)`。
- `_walkPhaseTree`（`:1355` 起）：按 `byPhase` 聚合六段，顺序遍历 WHEN→IF→WHO→ROLL→DO→COST；IF 段支持 `when` 表达式分叉（与门 + else 兜底）；ROLL 段支持 `lower/upper` 闭区间分叉 + 取代式语义；children 递归下探（深度上限 3，栈帧隔离 `rollBranchHit`）；DO/COST 段 atoms 经 `effectExecutor.executeSync` 结算。
- `_evalWhenExpr`（`:1181`）：支持 `hp<50` / `distance<=2` / `terrain==forest` 等表达式；左值白名单限定。
- `_evalConditionAtom`（`:1211`）：B1~B10 条件原子原生求值（射程/HP阈值/属性对比/共线/地形/伤害种类匹配…）。

### 2.4 六段式的真实断点（核心病灶）

**断点①：词条 JSON 里 `tree` 覆盖率为 0。**

对 `glossary-skill-config.json` 全量 grep：`tree` / `schema_version` / `"phase":` **0 命中**；全部 9 条业务词条结构为：

```json
{ "version":"5.0", "effects":[...], "timing":{...}, "conditions":[...], "cost":{...} }
```

即：**v2 遍历器已建好，但没有任何一条真实词条使用它**。引擎走的一直是 v1 扁平 `effects[]` 投影路径（`configLoader.cjs:104` 把旧字段 map 成 `timing/conditions/target/roll/effects/cost`）。

**断点②：触发命名三头分化。** 见第三节。

**断点③：层间数据流断裂。** 旧文档规定"层间不继承，只读全局上下文"，但 ROLL 结果 / IF 判定结果无法传给子层（全局上下文清单不含 `roll_result`/`if_passed` 这类运行期产物），导致"ROLL 出 6 → 子层暴击追加"这类正常数据流被切断。详见 2.2.4 选边——本报告已修订为「默认隔离 + 显式 `stepFrame` 帧传递」，子层经 `reads` 声明即可继承父层运算产物，修复此断裂。

**断点④：COST 预检未实现。** TODO `2.11` 未勾选，编辑器拖拽 COST 改变时机 ≠ 引擎真按序扣费，存在"false 勾选"风险。

**断点⑤：文档归置失当。** 权威定义不在 `docs/`，新人难寻真相源。

---

## 三、触发命名三头分化（WHEN 挂不进去的根因）

`docs/结算管线-原子触发时机对齐表.md` 第四节已自认存在三套管子：

| 维度 | 命名示例 | 来源文件 |
|---|---|---|
| ① 原子 `TIMING` 枚举 | `on_attacked` / `on_damage_dealt` / `post_melee_damage` | `shared-kernel/src/enums.ts` |
| ② `combatIntegrator` 实际点火 | `pre_damage` / `on_damage` / `post_damage` / `on_damage_taken` / `post_attack` | `combatIntegrator.cjs:314-354` |
| ③ `reactionHandlers` 注册 key | `on_damage_taken` / `post_melee_damage` / `on_damage_dealt` | `reactionHandlers/*.cjs` + `combat.ts` `fireReaction` |

**后果**：WHEN 段到底挂哪套名无定论；`combat.ts` 里 `fireReaction('on_attacked',...)`（`:1963`）此前"仅登记于 `GATEWAY_KNOWN_TRIGGERS` 却从未点火"（代码注释自述），`on_damage_dealt`（`:2416`）与 `post_melee_damage`（`:2429`）并用。这正是 Phase A 枚举要消灭的"三头分化"，但 Phase A 只建了枚举，未把 ②③ 收口。

---

## 四、合并方案：把两套揉成一个统一规则引擎内核

### 4.1 目标形态（一张图）

```
            ┌─────────────────────────────────────────┐
            │            CombatEngine (单一内核)         │
            │                                           │
  词条 JSON  │   tree:[ WHEN, IF, WHO, ROLL, DO, COST ]  │  ← 唯一描述形态（v2）
  (schema_   │        ↓ 递归 children (depth≤3)          │
   version=2)│   executeClause(clause, ctx) 遍历器       │  ← 调度器（已建：_walkPhaseTree）
            │        ↓ 在 DO/ROLL/COST 段 dispatch       │
            │   ┌──────────────────────────────────┐    │
            │   │  Atom Registry（原子注册表）        │    │
            │   │  damage / heal / buff / status /   │    │
            │   │  roll / ap_cost / visibility / ... │    │  ← 传统 RPG 零件降级为原子
            │   └──────────────────────────────────┘    │
            └─────────────────────────────────────────┘
```

**传统 RPG 的"流水线"从此消失**——`damagePipe` 的 13 阶段不再是必经链，而是 DO 段里一个可选 `atom: damage`；`effectExecutor` 的 64 handler 不再由 `action_type` switch 路由，而是由 `tree` 的 DO 段 atoms 直接点名调用。六段树 = 引擎输入，原子注册表 = 零件库，遍历器 = 唯一调度路径。

### 4.2 合并四步作业（每步可独立上线，零风险递进）

#### 步骤 1：消灭断点① —— 让真实词条长出 `tree`（迁移 + 运行时双保险）

**1a 离线迁移（migrate-to-phase-tree.cjs）**
- **动作**：写迁移脚本 `migrate-to-phase-tree.cjs`，把 `glossary-skill-config.json` 的 9 条 v1 词条**富集**为 v2 `tree`（timing→WHEN，conditions→IF，target→WHO，roll.segments→ROLL，effects→DO，cost→COST），保留旧扁平字段作投影兜底（契合契约"双写"策略）。
- **验收**：grep `tree` 命中 9 条；`isPhaseTreeV2` 对全部词条返回 true。
- **风险**：零（双写兜底，v1 路径仍可用）。

**1b 运行时自动升维兜底（Auto-Promotion）⭐ 关键技术把关补充**
- **问题**：离线迁移只覆盖"已入库的 9 条"。但老数据、临时 Mock 数据、靶场测试数据、或将来漏迁移的词条，若只有 v1 扁平 `effects[]` 且缺失 `tree`，会让 `_walkPhaseTree` 走空（断点①的残余面）。
- **动作**：在 `configLoader.cjs`（词条加载入口）或 `skillExecutor.cjs` 的 `executeUniversalSkill` 入口处，加一层**运行时自动升维兜底**：
  ```js
  // Auto-Promotion：v1 扁平词条在内存中动态包裹为最简单层六段树
  function autoPromote(skill) {
    if (Number(skill.schema_version) === 2 && Array.isArray(skill.tree) && skill.tree.length) {
      return skill; // 已是 v2，直通
    }
    // 最简单层：WHEN=active（或 timing.trigger），DO=effects[]，COST=cost
    const tree = [
      { phase: 'WHEN', atoms: [{ type: 'timing', trigger: skill.timing?.trigger || 'active' }] },
      ...(skill.conditions?.length ? [{ phase: 'IF', atoms: skill.conditions }] : []),
      ...(skill.target ? [{ phase: 'WHO', atoms: [skill.target] }] : []),
      ...(skill.roll?.segments?.length ? [{ phase: 'ROLL', atoms: skill.roll.segments }] : []),
      { phase: 'DO', atoms: skill.effects || [] },
      ...(skill.cost ? [{ phase: 'COST', atoms: [skill.cost] }] : []),
    ];
    return { ...skill, schema_version: 2, tree };
  }
  ```
- **收益**：
  1. 引擎内部**只有且仅有一条遍历路径**（`_walkPhaseTree`），彻底消灭"`action_type` switch 旁路"与"tree 走空"的双路径隐患。
  2. 任何来源（存量/ Mock / 靶场 / 漏迁移）的词条，进入引擎时都被统一升维为 `tree`，下游遍历器无需再判断 v1/v2 分支。
  3. 离线迁移脚本（1a）退化为"性能优化 + 可读性"，即便它漏跑，运行时兜底也不致炸。
- **注意**：Auto-Promotion 是**内存态包裹**，不回写磁盘（避免污染 `glossary-skill-config.json`）；编辑器保存时仍走双写（tree + 扁平投影），保证 `isPhaseTreeV2` 在落库层也为真。

#### 步骤 2：消灭断点② —— 触发命名统一收口
- **动作**：
  1. 以 `shared-kernel/src/enums.ts` 的 `TIMING` 枚举为**唯一真相源**，删除语义漂移别名（`on_attacked` 归并到 `on_damage_taken` 或明确区分"被攻击前/受击后"）。
  2. `combatIntegrator.cjs` 点火名改为直接引用 `TIMING` 枚举值（② = ①）。
  3. `reactionHandlers/*.cjs` 的 `register(key)` 与 `combat.ts` 的 `fireReaction` 的 key 必须来自 `TIMING` 枚举（③ = ①）。
  4. 启动自检 `GATEWAY_KNOWN_TRIGGERS` 已防死代码旁路，把 ② 点火清单注入校验。
- **验收**：三套管子合并为一；`fireReaction('on_attacked')` 不再"登记未点火"。
- **风险**：中（需全量回归 reaction 测试），但命名映射有对齐表可对照。

#### 步骤 3：修断点③④⑤ —— 让遍历器真正可靠
- **3a 显式数据流（修断点③）⭐ 实现边界把关补充**：把"层间不继承"改为"显式声明传递"，但严格界定作用域，杜绝递归污染：
  1. **只读事件上下文（triggerCtx）不可变快照**：`trigger_event` / `attacker` / `primary_target` / `self` / `trigger_roll` / `trigger_coord` / `trigger_value` / `skill_ref` / `battle_id` / `round` 保持**不可变只读快照**，子层只能读、不能写，且永不塞集合。
  2. **新增 `stepFrame`（帧局部栈）**：仅用于存放**当前父节点产生的临时结果**（如 `parent.roll_result`、`parent.if_passed`）。结构为栈：`_walkPhaseTree` 进入子层前 `push(parentFrame)`，子层执行期间从其 `stepFrame` 读取父层临时变量，子层执行完毕 `pop()` 出栈。
     ```js
     // 父层 ROLL 段结算后，把结果压入帧栈（而非全局 ctx）
     const parentFrame = { roll_result: dice.roll, if_passed: condResult };
     stepStack.push(parentFrame);
     const sub = this._walkPhaseTree(children, simUnit, subTarget, rollV, childCtx, depth + 1, opts);
     stepStack.pop(); // 子层结束即出栈，不残留
     ```
  3. **边界收益**：临时变量**绝不污染全局 `ctx`**，在递归深度 ≤3 时彻底杜绝"变量覆盖"（子层写了同名 key 盖掉父层）与"闭包内存泄漏"（帧随栈出栈自动释放，不被闭包长期持有）。
  4. **读取语义**：子层声明 `reads: ['parent.roll_result']` → 遍历器从 `stepStack` 栈顶取父帧对应字段；未声明则不透传（保持"默认不继承"语义）。WHEN 闸门用的触发 payload 仍走独立 `triggerCtx`，不与 `stepFrame` 混用。
- **3b COST 预检（修断点④）**：在 `_walkPhaseTree` 进入 COST 段前，先做 Pre-flight 合法性预检（射程/目标/AP 足够），不通过则整条 SKIP（不扣费、无副作用），勾掉 TODO `2.11`。
- **3c 文档归置（修断点⑤）**：把根目录实施计划的六段式定义**搬进 `docs/`**（如 `docs/phase-tree-六段式语法模型.md`），并标废弃文档指向它，统一真相源。
- **风险**：低（遍历器已有结构，仅扩展 ctx 传递 + 补预检）。

#### 步骤 4：传统 RPG 零件降级为原子（治本收口 · 平滑过渡）

**4a Atom Registry 与 effectExecutor 的平滑过渡 ⭐ 关键技术把关补充**
- **核心原则**：**初期不需要完全推翻 `effectExecutor.cjs`**，把它作为 Atom Registry 的**底层 Handler Provider** 即可。已通过生产验证的核心计算逻辑（`handleDirectDamage` / `handleHeal` / `handleBuff` 等）100% 复用，零重写风险。
  ```js
  // atomRegistry.cjs —— 初期直接委托 effectExecutor 的已验证 handler
  const { effectExecutor } = require('./effectExecutor.cjs');
  atomRegistry.register('damage', (atom, ctx) => effectExecutor.handleDirectDamage(atom, ctx));
  atomRegistry.register('heal',   (atom, ctx) => effectExecutor.handleHeal(atom, ctx));
  atomRegistry.register('buff',   (atom, ctx) => effectExecutor.handleBuff(atom, ctx));
  atomRegistry.register('roll',   (atom, ctx) => diceService.roll(atom.expr));
  atomRegistry.register('ap_cost',(atom, ctx) => apCost.consume(ctx.unit, atom.kind, atom.amount));
  ```
- **收益**：
  1. 立即**清理掉 30 个无用死键**（`E/F/G/H/I/J` 组零引用原子）——这些键从 `effectExecutor.handlers` 表里移除，但 Registry 只注册真正被用的几个，死键自然失活，不污染遍历器 dispatch。
  2. 核心算法（伤害/治疗/护盾计算）100% 复用，不引入新 bug。
  3. 后续若要替换某原子（如伤害改走 `@soonfx` 表达式引擎），只需改 Registry 里那一行注册，不动 `effectExecutor`。

**4b 主线切换与收口**
- **动作**：
  1. 建 `Atom Registry`（`combatCore/atomRegistry.cjs`），按 4a 委托 `effectExecutor` + `diceService` + `apCost` 注册命名原子。
  2. `_walkPhaseTree` 的 DO/ROLL/COST 段从"直接调 `effectExecutor.executeSync`"改为"**查 Atom Registry 按 `atom.type` 分发**"——彻底消除 `action_type` switch 与 64-handler 表的耦合。
  3. **清理零引用死键**：从 `effectExecutor.handlers` 移除自认"零引用"的 `E/F/G/H/I/J` 组 30 个死键 + 空壳 `apply_buff`/`remove_buff`（重定向到 `modify_stat` 已覆盖）。这些键不在 Registry 注册，自动失活，不影响已验证路径。
  4. **统一掷骰真相源**：把 `damagePipe` 暴击段的 `Math.random()` 改调 `diceService`，并给 `diceService` 注入 seed（解决不可复现）。
- **验收**：`skillExecutor` 的 `switch(action_type)` 主干可逐步退役；新增一条纯 `tree` 词条无需改引擎代码即可生效。
- **风险**：中（原子注册表是结构性改动），但按 4a 平滑过渡——先委托、后切换、再删老路径，已验证逻辑零重写。

### 4.3 合并后"六段式"与"传统 RPG"的关系对照

| 合并前 | 合并后 |
|---|---|
| 六段式 = 编辑器 JSON 结构 + 文档约定（断点①：无真实数据） | 六段树 = 词条**唯一描述形态**，遍历器唯一入口 |
| 传统 RPG = 硬编码 `action_type` switch + 13 阶段管道 | `damagePipe` 等降级为 Atom Registry 里的可选 `damage` 原子 |
| 触发三套管子 | 单一 `TIMING` 枚举真相源 |
| 层间不继承（数据流断） | 显式 `ctx.export/reads` 声明传递 |
| 64 handler 表膨胀 + 30 死键 | Atom Registry 委托 effectExecutor，零引用死键失活 |
| `Math.random` 不可复现 | `diceService` seed 注入，战报可重放 |
| 漏迁移/Mock/靶场数据使 tree 走空（双路径隐患） | 运行时 Auto-Promotion 兜底，引擎内部仅一条遍历路径 |
| 递归层间临时变量污染全局 ctx（闭包泄漏） | `stepFrame` 帧局部栈，子层结束即出栈 |

### 4.4 为什么这是"揉成一个"而非"二选一/重写"

- **不是去掉六段式**：六段遍历器（`_walkPhaseTree`）已建好，直接复用，只需接数据（步骤 1）。
- **不是推翻传统 RPG**：`damagePipe`/`effectExecutor`/`diceService` 的**算法**不重写，只把它们**包成原子**挂进 Registry（步骤 4），成熟逻辑零浪费。
- **不是引入外部框架**：完全基于你已有的独有六段语义 + 已有零件，比任何外部引擎更契合。
- **渐进零风险**：四步各自独立可上线、可回滚（双写兜底），不需"大重写"导致炸服。

---

## 五、执行路线图（建议顺序）

```
Week 1  步骤1a  migrate-to-phase-tree.cjs 富集 9 条词条为 tree（双写验证）     [零风险]
Week 1  步骤1b  运行时 Auto-Promotion 兜底（复用 configLoader 归一化管道）    [零风险]
Week 1  步骤3c  六段式权威定义合入 docs/（归正文档真相源）                      [零风险]
Week 1  ★切断1  combatIntegrator 退位为纯广播器（拔除内部 damagePipe 结算）   [高风险·必做]
Week 1  ★切断2  diceService.setSeed 预埋 seed 管道（隐患3 前置）               [低风险]
Week 2  步骤2   触发命名三头收口为 TIMING 枚举（全量回归）            [中风险]
Week 2  步骤3a/3b 遍历器 stepFrame 帧栈 + COST 预检                  [低风险]
Week 3  步骤4a  Atom Registry 委托 effectExecutor（平滑过渡·this 绑定） [中风险]
Week 3  步骤4b  主线切换 + 零引用死键清理 + seed 掷骰算法替换          [中风险]
Week 4  逐步退役 skillExecutor 的 action_type switch 主干            [中风险]
```

> **第一阶段（Week 1）开工清单**（已确认优先级）：
> 1. 步骤 1a：编写 `migrate-to-phase-tree.cjs`，将现有 9 条词条无损升维出 `tree` 字段，完成双写验证。
> 2. 步骤 1b：在 `configLoader.cjs` / `skillExecutor.cjs` 入口加运行时 Auto-Promotion，**必须复用 `configLoader` 归一化管道**（隐患 1 防御）。
> 3. 步骤 3c：将六段式权威定义正式合入 `docs/` 目录，归正文档真相源。
> 4. **★切断 1**：`combatIntegrator.executeAttack` 内部 `damagePipe.calculate` 调用拔除，降级为仅 `emitPipelineEvents` 广播（隐患 2 防御，阻塞项）。
> 5. **★切断 2**：`diceService.setSeed` 注入入口落地，预埋 seed 管道（隐患 3 前置）。

---

## 六、关键风险与对策

| 风险 | 对策 |
|---|---|
| 步骤2 命名收口引发 reaction 全量回归失败 | 以对齐表为映射基准，先建枚举→点火名映射表，再批量改 `register`/`fireReaction` key，保留旧名 alias 过渡期 |
| 步骤4 Atom Registry 切换导致老词条失效 | 双写兜底期内 v1 路径保留；切换前用迁移脚本生成的 tree 做 A/B 战报比对 |
| 不可复现改造破坏现有战报 | seed 默认从 `battle_id` 派生，保证同局可重放；旧局不回溯 |
| 文档归置引发新人误解 | 废弃文档统一指向 `docs/phase-tree-*.md` 新真相源 |

---

## 七、附录：运行期防线与过渡期避坑指南

> 本附录沉淀**方案落地后运行期会暴露的 5 个隐患**，并给出代码级防御方案。
> 核心结论：方案逻辑正确，但真正的雷在**"传统 RPG 线性流水线 → 递归六段树调度"的过渡窗口**。其中**隐患 1（归一化管道不一致）与隐患 2（`_walkPhaseTree` × `combatIntegrator` 双结算）必须在 Week 1 即切断**，否则上线即数据错乱。

### 7.1 五个隐患总览

| # | 隐患 | 触发时机 | 严重度 | 必须在 |
|---|---|---|---|---|
| 1 | autoPromote 与老路径归一化管道不一致 → 静默失败 | Week1 兜底上线 | 高 | Week1 |
| 2 | `combatIntegrator` 点火链与 tree DO/damage 双结算 | Week1-2 过渡 | 高 | Week1 切断 |
| 3 | `diceService` 无 seed → stepFrame.roll_result 不可复现 | Week1-3 过渡 | 中 | Week1 预埋 seed 管道 |
| 4 | `effectExecutor` 单例被委托给 Registry 时 `this` 丢失 | Week3 切换 | 中 | Week3 绑定 |
| 5 | `triggerCtx` 不可变快照 vs 实时 `BattleState` 时效性 | 多单位并发结算 | 中 | 设计期定 |

### 7.2 隐患 1：Auto-Promotion 必须复用 `configLoader` 归一化管道

**核心病灶**：若 Auto-Promotion 仅在内存中粗暴地 `tree:[{phase:'DO', atoms: raw_effects}]`，会跳过老路径已有的别名重定向（如 `status→modify_stat`、`damage→handleDirectDamage`）和默认字段注入，导致 `_walkPhaseTree` 底层调 `executeSync` 时找不到 handler 或参数缺失 → **静默失败 / 结算落空**。

**代码级方案**：在 `configLoader.cjs` 将既有"原子归一化逻辑"抽为可复用纯函数 `normalizeEffectAtom(rawEffect)`，autoPromote 强制走同一条管道：

```javascript
// configLoader.cjs (或 skillExecutor.cjs 依赖处)

function normalizeEffectAtom(raw) {
  // 1. 别名与真名重定向 (status -> modify_stat, etc.) —— 必须与老路径 executeSync 同构
  const type = effectExecutor._resolveHandlerType(raw.type || raw.action);
  // 2. 字段兜底与类型清洗
  return {
    ...raw,
    type,
    value: Number(raw.value) || 0,
    target: raw.target || 'target',
    // 保持与老路径 executeSync 完全同构的属性结构
  };
}

function autoPromoteV1ToV2(v1Skill) {
  // 必须经由相同的归一化管道，禁止裸包 raw_effects
  const normalizedAtoms = (v1Skill.effects || []).map(normalizeEffectAtom);

  return {
    ...v1Skill,
    schema_version: 2,
    tree: [
      { phase: 'WHEN', atoms: [{ type: v1Skill.timing?.trigger || 'active' }] },
      { phase: 'DO',   atoms: normalizedAtoms },
    ],
  };
}
```

**验收**：同一词条走"离线迁移 tree"与走"autoPromote 兜底"产出的 `normalizedAtoms` 必须**逐字段一致**（可写单测断言）。

### 7.3 隐患 2：`combatIntegrator` 降级退位（只点火、不结算）

**核心病灶**：`combatIntegrator.executeAttack` 内部串联了 `damagePipe.calculate` 结算 + `pre_damage…post_attack` 点火链。一旦 `_walkPhaseTree` 的 DO/damage 原子开始真正扣血，`combatIntegrator` 若还在跑，伤害会被算并扣两遍 → **双结算数据错乱**。

**代码级方案（三权剥离）**：Week 1 将 `combatIntegrator` 降级为**纯粹的"时机广播器"**，拔除其内部算伤逻辑，严禁任何 `BattleState` 写入副作用：

```javascript
// combatIntegrator.cjs 改造目标

class CombatIntegrator {
  // 废弃旧的 executeAttack（或重写为纯点火分发）
  async emitPipelineEvents(timing, payload, battle) {
    // 1. 严格使用 TIMING 枚举（见步骤2 收口）
    // 2. 仅向 EventBus / Comm / Reaction 广播事件
    // 3. 绝不调用 damagePipe 算伤，绝不修改 unit.hp
    await this.fireHooks(timing, payload);
    await reactionSystem.trigger(timing, payload, battle);
  }
}
```

**执行流重归一统**：
- **唯一算伤与扣血方**：`_walkPhaseTree` → DO/damage 原子 → `damagePipe.calculate`（仅算值）→ 统一应用扣血。
- **时机广播方**：damage 原子在结算前后，调用 `combatIntegrator.emitPipelineEvents('pre_damage', …)` 与 `emitPipelineEvents('on_damage', …)`。
- 彻底消灭"tree 结算线"与"点火链结算线"两条独立结算路径。

### 7.4 隐患 3 快速防御：Week 1 预埋 Seed 管道

无需等到 Week 3，在 Week 1 构建 `autoPromote` 的同时，直接给 `diceService` 补 Seed 注入入口，使 stepFrame 里的 `parent.roll_result` 从第一天起就确定可复现：

```javascript
// diceService.cjs
let _currentRng = Math.random;

function setSeed(battleId, round = 1, step = 0) {
  // 轻量 PRNG (如 Mulberry32 / SplitMix32)，保证同局同参可重放
  _currentRng = createPrng(`${battleId}-${round}-${step}`);
}

function roll(d = 100) {
  return Math.floor(_currentRng() * d) + 1;
}
```

**收益**：`_walkPhaseTree` 压入 `stepFrame.parent.roll_result` 的数值具备**确定性与可复现性**，为 Week3 真正消 `Math.random` 铺好管道，过渡期不出现"父层 roll 与子层暴击判定对不上"的不可重放问题。

### 7.5 隐患 4：`effectExecutor` 单例 `this` 丢失

**核心病灶**：报告 4a 将 `effectExecutor` 委托给 Registry（`atomRegistry.register('damage', effectExecutor.handleDirectDamage)`）。但 `handleDirectDamage` 内部依赖 `this` 指向单例（读 `this.handlers` / `this._state`）。若以普通函数引用注册，调用时 `this` 绑定丢失 → 内部报错。

**代码级方案**：注册时绑定实例，或 Registry 调用时显式传 provider：
```javascript
atomRegistry.register('damage', (atom, ctx) => effectExecutor.handleDirectDamage(atom, ctx));
// 或：atomRegistry.register('damage', effectExecutor.handleDirectDamage.bind(effectExecutor));
```

### 7.6 隐患 5：`triggerCtx` 快照时效性

**核心病灶**：报告 3a 将 `triggerCtx` 设为不可变只读快照（attacker/primary_target/self 等）。但战棋是多单位异步结算：父层 WHO 选中目标 A 的快照，子层执行期间 A 可能已被另一并发结算击杀/位移。快照不反映最新态 → 对尸体结算或坐标错位。

**代码级方案**：`triggerCtx` 里的单位引用应是**指向 `BattleState` 实时对象的指针/ID**，而非值拷贝；只读约束只限制"不能改快照字段"，读取时实时解引用：
```javascript
// 只读约束：禁止 triggerCtx.unit.hp = x
// 时效保证：triggerCtx.primaryTargetId 解引用 battleState.getUnit(id)
const liveTarget = battleState.getUnit(triggerCtx.primaryTargetId);
```
这样既不破坏"不可变快照"语义，又避免高并发多技能同时结算时的过期数据问题。

### 7.7 Week 1 代码级切断清单（新增）

结合 7.2 / 7.3 / 7.4，Week 1 除原"步骤1a/1b/3c"外，应追加两项**硬切断**任务：

1. **切断双结算**：`combatIntegrator.executeAttack` 内部 `damagePipe.calculate` 调用拔除，改为仅 `emitPipelineEvents` 广播（隐患 2）。
2. **预埋 seed 管道**：`diceService.setSeed` 注入入口落地（隐患 3），即便算法仍走 `Math.random` 包裹，也先接管道。

> 二者与 autoPromote（隐患 1）同步上线，确保 Week 1 结束时引擎内部**只有一条结算线**（tree → DO 原子 → damagePipe 算值 → 统一扣血）+ **一个时机广播器**（combatIntegrator 只点火）。

---

## 附录〇、设计对话沉淀：用户直觉如何演进六段式模型

> 本章节记录 2026-08-19 设计对话中，用户通过连续追问逼出的三个关键认知，及其如何被吸收进本报告 2.2 节（六段式数据模型权威定义）。目的是把"口头直觉"固化为"文档约束"，避免后续实现 reopen 争议。

### 〇.1 用户直觉 A：「用同句式描述复杂子段」（→ 2.2.3 子树自相似）

- **原话意图**：六段式中某一段（如复杂的 `DO`）若自身也很复杂，希望**同样用 `WHEN/IF/WHO/ROLL/DO/COST` 句式去描述它**，而非引入第二种语法。
- **沉淀为约束**：2.2.3「子树自相似」——句式同构/自相似被正式接纳；子树**不必从 WHEN 起头、不必六段齐全**，只补相对父节点多出的那层语义。
- **价值**：规则无论多深都用同一套词汇，策划不需学第二种语法，是六段式最强大的扩展力来源。

### 〇.2 用户直觉 B：「子树不必六段齐全」（→ 2.2.3 空值/不完整合法）

- **原话意图**：基于自相似框架，子树**不一定需要是完整的六段句式**，只写关心的维度即可。
- **沉淀为约束**：2.2.3「空值合法 + 不完整合法」——`atoms:[]` 或整段缺失 = 该维度不参与；不写的维度靠上下文继承或留空。写规则变成"只填关心的格子"。

### 〇.3 用户直觉 C：「六段互套会坍缩？」（→ 2.2.5 红线）

- **原话意图**：若"六段任意一段都可嵌套另一棵完整六段树、且值可空"，是否会退化出 `WHEN=when, IF=when, WHO=when, ROLL=when, DO=when, COST=when` 的六同型怪物？
- **AI 推演确认**：按"无限制相位互套"定义确实会坍缩（语义无限套娃、执行序不可判定）。但用户的真实意图是"有方向、有主次的嵌套"（主树描述规则、复杂子段用子树补充），**并非无限制互套**——故采纳用户的自相似能力，同时立 2.2.5 红线**禁止相位互套坍缩**（单相位节点 + 子树仅挂 DO/IF + WHEN/WHO/ROLL/COST 值禁为六段树）。

### 〇.4 用户直觉 D：「上下文应继承」（→ 2.2.4 选边）

- **原话意图**：既然子树继承父上下文，那父层的 `roll_result` 等产物子层应当能看到（否则"父层暴击→子层追加"无法实现）。
- **沉淀为约束**：2.2.4 将"层间不继承"（67原子报告原定调）与"上下文继承"（用户模型）的张力摆上台面，本报告**选边为「默认隔离 + 显式 stepFrame 帧传递」**——既修复断点③，又守住不污染全局的红线。用户的继承意图被接纳为"按需 `reads` 显式继承"。

### 〇.5 一句话总结

> 用户的四次直觉，把六段式从一个"死板的有序六段列表"演进为**「单相位节点 + 自相似子树 + 按需上下文继承 + 禁相位互套」**的活模型。本报告 2.2 节是这次对话的最终落点。

---

## 附：本次盘点核实的关键证据

- `skillExecutor.cjs:524-529` —— v2 遍历器已接入主链路（`_walkPhaseTree`）。
- `skillExecutor.cjs:1355` —— `_walkPhaseTree` 定义，支持 IF/ROLL 分叉 + children 递归（depth≤3）。
- `shared-kernel/src/contracts/phase-tree.contract.ts` —— 六段契约已落地（PhaseEnum / PHASE_TREE_MAX_DEPTH=3 / PhaseNode / PhaseBranch）。
- `glossary-skill-config.json` —— grep `tree`/`schema_version` 全 0 命中，9 条全为 v1 扁平 `effects[]`（断点①确凿）。
- `combatIntegrator.cjs:314-354` —— 点火链 `pre_damage→on_damage→post_damage→on_kill/on_death→on_damage_taken→post_attack`（② 套）。
- `combat.ts:1963/2416/2429` —— `fireReaction('on_attacked'/'on_damage_dealt'/'post_melee_damage')`（③ 套，且 `on_attacked` 注释自述"从未点火"）。
- `shared-kernel/src/enums.ts` —— `TIMING` 枚举（`on_attacked`/`on_damage_dealt`/`post_melee_damage` 等，① 套）。
- `effectExecutor.cjs` —— 注册约 64 键，含自认"零引用原子"组与空壳 handler（断点：handler 膨胀）。

> 结论重申：合并作业的本质是**接通已建好的六段遍历器与已运行的传统 RPG 零件**，而非二选一或重写。四步渐进、双写兜底、每步可回滚。
