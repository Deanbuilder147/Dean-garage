# 可用替换战棋战斗引擎汇总与适用性检查报告

> 生成日期:2026-08-19
> 目的:为 mecha-universe-engine 现有战斗引擎评估可替换/可参考的开源方案,并给出适用性结论。
> 范围:现有自研引擎结构盘点 + 业界 6 个候选引擎/框架 + 适用性矩阵。

---

## 一、现有自研引擎盘点(现状基线)

### 1.1 引擎位置与规模

- 物理位置:`mecha-universe-engine/services/combat-service/src/services/combatCore/`(`.cjs` 集群)
- 桥接层:`backend-gateway/src/combatBridge.ts`(用 `createRequire(import.meta.url)` 加载 CJS,**硬编码** `CORE_DIR`)
- 真相源:`shared-kernel/src/enums.ts`(枚举)、`shared-kernel/src/hexMath.ts`(坐标/射程)

### 1.2 核心单点(替换时最需谨慎)

| 模块 | 文件 | 体积 | 职责 |
|---|---|---|---|
| 技能执行中枢 | `skillExecutor.cjs` | 115 KB | 主谓宾定状补 6 插槽语法驱动全部技能 |
| 效果执行器 | `effectExecutor.cjs` | 70 KB | `effects[].type` 路由到 handler |
| 伤害管道 | `damagePipe.cjs` | 25 KB | 12+1 阶段泛化伤害结算 |
| 钩子链 | `hookChain.cjs` | 6.8 KB | 20 个 phase 钩子点,支持中断 |
| 骰子服务 | `diceService.cjs` | 6.5 KB | 唯一掷骰真相源(`Math.random`,无 seed) |
| 条件/分支 | `conditionEvaluator.cjs` / `branchEvaluator.cjs` | 13.5 / 5.7 KB | 条件求值 + 投骰多分支 |

### 1.3 架构红线(替换前提)

1. **无引擎抽象层**:全仓无 `ICombatEngine` / `EngineAdapter` 接口。引擎通过 `nodeRequire` 硬编码路径直接耦合进 `routes/combat.ts`(185 KB)、`glossary.ts`、`rooms.ts` 等至少 5 个路由文件。
2. **单例共享内存契约**:`diceService`/`hookChain`/`effectExecutor`/`equipmentDurability` 全部 `new Xxx()` 单例,依赖同一 require 缓存共享 `_state`/`_locks`。
3. **API 契约已绑定实现**:`routes/combat.ts` 多处硬编码 `engine: 'combat-service/.cjs'` 字面量。
4. **返回体非确定性**:`diceService` 用 `Math.random()`,无 seed 注入点,战斗回放/单测不可复现。
5. **双份实现风险**:`backend-gateway/src/skillExecutor.ts`(27 KB)与 `combatCore/skillExecutor.cjs`(115 KB)同名双份;体型逻辑存在三头(`unitSize.ts` / skillExecutor 内联 / `unitTypeModel.cjs`)。

> **结论**:现有引擎是「硬耦合单体」,任何外部引擎替换必须先做"抽象层收口"(把散落的 `nodeRequire` 收敛回 `combatBridge`,定义引擎接口),否则替换会被直连路径绕过。

---

## 二、候选替换/参考引擎汇总

### 候选 A:game-hex-tactics(纯函数战斗引擎,Web)
- 仓库:https://github.com/CesarAugustoAndrade/game-hex-tactics
- 技术栈:React 19 + Vite 8 + PIXI.js v8 + TypeScript(严格模式)
- 核心特性:
  - **纯函数确定性战斗模拟器**(`src/battle/simulate.ts`),无 React/PIXI 依赖,可 headless 驱动(`npm run sim`)
  - 程序化双层级地图(战略总览 → 战术战场下钻)
  - 指挥点(CP)系统、5 种运动模式、编队循环、AI 控制器
- 许可证:未声明(仓库无 LICENSE 文件)⚠️
- 最近更新:341 commits(2026 年活跃)

### 候选 B:Battle for Wesnoth(韦诺之战)引擎
- 仓库:https://github.com/wesnoth/wesnoth
- 技术栈:C++ 核心 + Lua 脚本化单位/技能(WML/Lua)
- 核心特性:
  - 成熟回合制战术战斗(2003 至今,GPLv2+)
  - 基于地形加权的移动消耗、时间/阵营克制、单位经验升级
  - 完善的 WML/Lua 数据驱动单位定义(高度可参考其**数据契约设计**)
- 许可证:GPLv2+(强 copyleft,商用需谨慎)⚠️
- 最近更新:持续活跃

### 候选 C:Godot Hex Toolkit(Godot 4 六角格工具集)
- 代表:Hex Strategy Map(https://github.com/JavierIslas/hex-strategy-map-free)、GDHexGrid、Godot Asset Library `hex` 筛选
- 技术栈:GDScript(Godot 4.6+),MIT 为主
- 核心特性:
  - 六边形坐标/路径寻找/战争迷雾/渲染/相机控制开箱即用
  - 与本项目 Godot 端(`apps/` 下的 `.gd` 文件)天然契合
- 许可证:多为 MIT ✅
- 最近更新:2026 年活跃

### 候选 D:Hex Engine(浏览器 2D 游戏框架)
- 仓库:https://hex-engine.dev/
- 技术栈:TypeScript,React-like 组件模型
- 核心特性:
  - 浏览器端 2D 游戏引擎,组件化(类似 React)
  - 偏"渲染/游戏循环框架",**非战斗规则引擎**
- 许可证:MIT(通常)
- 适用性:仅作渲染层参考,不解决战斗逻辑

### 候选 E:Phaser 3 Grid Engine
- 仓库:https://github.com/supermedium/phaser-grid-engine(及 SourceForge 同名)
- 技术栈:JavaScript/TypeScript,Phaser 3
- 核心特性:
  - 基于 tilemap 的网格移动系统,支持战术 RPG 移动逻辑
  - 纯网格(非六角优先),六角需自定义
- 许可证:MIT ✅
- 适用性:移动/网格系统参考,战斗规则需自写

### 候选 F:simple-srpg-hexagon-engine(GameMaker)
- 仓库:https://github.com/teej043/simple-srpg-hexagon-engine
- 技术栈:GameMaker Studio 2 (GML)
- 核心特性:完整战术 TBS 引擎(2025-05 更新)
- 许可证:未明确 ⚠️
- 适用性:**与 Web/TS 技术栈不兼容**,仅作算法参考

---

## 三、适用性检查矩阵

> 维度说明:
> - **栈兼容**:能否直接嵌入现有 Node/TS/Web 技术栈
> - **引擎解耦**:是否自带清晰的引擎抽象层(降低替换成本)
> - **六角原生**:是否原生支持 pointy-top 六角网格
> - **数据驱动**:技能/单位是否 JSON/配置驱动(对齐现有"词条库"模式)
> - **确定性**:能否 seed 复现(现有引擎缺失项)
> - **许可风险**:商用/闭源集成风险

| 候选 | 栈兼容 | 引擎解耦 | 六角原生 | 数据驱动 | 确定性 | 许可风险 | 综合推荐 |
|---|---|---|---|---|---|---|---|
| A game-hex-tactics | ✅高 | ✅纯函数 | ✅ | ⚠️中(场景驱动) | ✅(确定性) | ⚠️无 LICENSE | **★ 首选参考** |
| B Wesnoth | ❌低(C++) | ✅(Lua 模块) | ✅ | ✅极高 | ✅ | ⚠️GPL 强 copyleft | 仅契约参考 |
| C Godot Hex | ✅(Godot 端) | ⚠️工具级 | ✅ | ⚠️中 | ⚠️ | ✅MIT | Godot 端适用 |
| D Hex Engine | ✅Web | ❌框架 | ❌网格 | ❌ | ⚠️ | ✅MIT | 仅渲染参考 |
| E Phaser Grid | ✅Web | ⚠️工具级 | ❌方网格 | ❌ | ⚠️ | ✅MIT | 移动系统参考 |
| F simple-srpg | ❌GML | ⚠️ | ✅ | ⚠️ | ⚠️ | ⚠️未知 | 仅算法参考 |

---

## 四、适用性结论与建议

### 4.1 直接"整盘替换"不推荐
现有引擎已深度耦合(`nodeRequire` 散落 5+ 路由、单例共享内存、API 契约绑定实现),**任何外部引擎整盘替换 = 重写 gateway 战斗层 + 重写前端契约**,成本极高且会丢失已上线的词条库/触控/设备分流成果。**不建议整盘替换。**

### 4.2 推荐策略:「抽象层收口 + 局部借鉴」
按优先级:

1. **收口抽象层(必做前提)**:将 `routes/combat.ts`、`glossary.ts`、`rooms.ts` 等散落的 `nodeRequire(...)` 全部收敛回 `combatBridge.ts`,在其上定义 `ICombatEngine` 接口(输入 `BattleState`+指令,输出新 `BattleState`+事件流)。这是让引擎"可替换"的唯一前提,也契合宪法「显式依赖解耦」红线。

2. **借鉴 A 的纯函数确定性模拟**:`game-hex-tactics` 的 `simulate.ts` 模式最适合。可把现有 `skillExecutor`/`effectExecutor`/`damagePipe` 重构成**纯函数**(`(state, action) => {state, events}`),并引入 seed(参考 Wesnoth 的 RNG 注入),解决现有 `Math.random` 不可复现问题。⚠️ 但该仓库无 LICENSE,**仅可借鉴架构思路,不可直接复制代码**。

3. **借鉴 B 的数据契约**:Wesnoth 的 WML/Lua 单位/技能定义是"数据驱动战斗"的业界标杆,可参考其字段设计优化现有 `skillContract.cjs`(消除同名双份、三头体型)。

4. **Godot 端(C)直接可用**:`apps/` 下的 Godot 项目可直接引入 Hex Strategy Map(MIT),补齐六角坐标/寻路/迷雾,与现有 `hexUtils.js` 真相源对齐。

5. **渲染层(D/E)按需**:前端 Canvas 渲染已自研且符合宪法,`Hex Engine`/`Phaser Grid` 仅作移动/补间动画参考,**不引入新框架避免破坏单向渲染管线红线**。

### 4.3 快速决策表

| 你的目标 | 推荐动作 |
|---|---|
| 让引擎可插拔/可测试 | 先做 §4.2-1 抽象层收口(不换引擎) |
| 战斗回放/单测确定性 | 引入 seed RNG,借鉴 A 纯函数模式 |
| 优化词条/技能数据模型 | 参考 B 的 WML 契约,清理双份实现 |
| Godot 端六角能力 | 引入 C(Hex Strategy Map, MIT) |
| 想用现成完整引擎 | **不建议**(见 §4.1),代价=重写战斗层 |

---

## 五、风险提示

- **A 无 LICENSE**:仅借鉴思想,禁止直接 fork 代码入库,避免法律风险。
- **B GPL**:若参考其代码需保持开源/隔离,建议只借鉴数据契约设计文档。
- **现有双份实现**:替换前务必先确认 `backend-gateway/src/*.ts`(skillExecutor.ts/damagePipe.ts)是否为死代码,避免抽象层接到错误实例。
- **TIMING 真相源漂移**:`enums.ts` 声明以已废弃的 `combatIntegrator.cjs` 为唯一真相源,收口时应先固化 TIMING 枚举到独立文件。
