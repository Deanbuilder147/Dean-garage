# 手动输入数值入口 + 输入框/按钮样式核查报告（2026-07-30）

> 范围：前端所有「需/可手动输入数值」的入口，逐一核对对应输入框是否有框线、按钮是否有按键样式。**重点核查词条库（GlossaryView）**。
> 仓库：`mecha-universe-engine/frontend/src`

---

## 一、可手动输入数值的入口清单（全栈前端）

### 1. 词条库 `views/GlossaryView.vue`（重点）
**A. 词条创建向导（Wizard）**
| 字段 | 类型 | 绑定 | 输入类 |
|------|------|------|--------|
| 触发条件 HP 阈值 `requires_hp_below` | number | `wiz` | `wiz-input` |
| 施法距离 `cast_range` | number | `wiz` | `wiz-input` |
| 最小施法距离 `min_cast_range` | number | `wiz` | `wiz-input` |
| AOE 半径 `aoe_radius` | number | `wiz` | `wiz-input` |
| 光束宽度 `beam_width` | number | `wiz` | `wiz-input` |
| 高度差每级加成 `height_bonus_per_diff` | number | `wiz` | `wiz-input` |
| 成功线 `success_line` | number | `wiz` | `wiz-input` |
| 成功伤害加成 `success_bonus_damage` | number | `wiz` | `wiz-input` |
| 命中修正 `accuracy_mod` | number | `wiz` | `wiz-input` |
| 闪避修正 `evasion_mod` | number | `wiz` | `wiz-input` |
| 基础伤害 `base_damage` | number | `wiz` | `wiz-input` |
| 分支生效点数 `br.pointsStr` | 文本(1,2,3 / 4-6) | `br` | `wiz-input` |
| 分支效果数值 `ef.value` | number | `ef` | `wiz-input` |
| 分支效果状态 key `ef.status` | 文本 | `ef` | `wiz-input` |
| 状态效果串 `wiz.statusEffectsStr` | 文本域 | `wiz` | `ai-import-textarea`（textarea） |

**B. 词条卡片编辑**
- 词条 key/名（`skill-id-input`，文本）、标签（`skill-label-input`，文本）、描述（`skill-desc-input`，文本）
- 类别（`param-select` 下拉）、攻击距离/命中/闪避/伤害/AP 成本等（`param-input`，number/文本）

**C. AI 导入**
- 文本框（`ai-import-textarea`）+ 按钮「导入技能」(`btn btn-primary`)

**D. Excel 导入弹窗**
- 按钮「确认导入」(`btn btn-primary`)、「取消」(`btn btn-ghost`)

### 2. 骰子工坊 `views/DiceConfigView.vue`
- `rollMult[0..5]`（6 个 number）、`critThreshold`、`critMin`、`critMax`、`manualRollDefault.successLine`、`bonusDamage`(模拟)、`sim.count`、`availableDiceTypes`(文本)
- 按钮：保存(`btn.primary`)/重置/模拟/应用(`btn.ghost`)，本地 scoped 已定义

### 3. 单位编辑器 `views/NewUnitEditorView.vue`（数值最密集）
- 主机体 格斗/射击/结构/机动（4 个 stepper number，min0-max40）
- royroy 格斗/射击/结构/机动（4，max25）
- left 格斗/射击/结构/机动（4，max15）
- right 格斗/射击/结构/机动（4，max15）
- left/right dkm 光束/动能/爆炸/腐蚀/热熔（各 5 个，step 0.1，min-5~max5）
- 按钮：保存/导入/导出/重置（`btn-primary`/`btn-ghost` 全局已定义）

### 4. 战场选择 `views/NewBattlefieldSelector.vue`
- 加入房间密码（4 位，文本）、房间号/代码（文本，`join-input`）
- 按钮：加入/创建/刷新（本地已定义）

### 5. 准备房 / 登录注册（以文本为主，非数值，列示备查）
- `NewPreparationRoom.vue`：房间名/密码（文本）
- `NewLoginView.vue` / `NewRegisterView.vue`：用户名/密码（文本）

### 6. 反应弹窗（**无手动数值输入**，仅展示+按钮）
- `DuelDialog` / `SnatchDialog` / `CoverWindow` / `ExecuteDialog` / `LuckyRoll`：纯展示，无 `<input>`

### 7. 其它组件（文本输入）
- `SkillsEditor.vue`：技能名/距离/特效说明（文本，`skill-name-input`/`skill-range-input`/`skill-special-input`）
- `AssetGenPanel.vue`：风格/地形 ID（文本，全局 `select,input,textarea` 统一框线）

---

## 二、样式核查结果（输入框框线 / 按钮样式）

| 位置 / 元素 | 输入框框线 | 按钮样式 | 结论 |
|---|---|---|---|
| 词条库 向导 `wiz-input` | 原 `rgba(159,142,120,0.18)` 极淡（几乎不可见） | — | **本轮已强化** 为 `rgba(255,176,0,0.25)` 琥珀框线 + 聚焦高亮 |
| 词条库 `ai-import-textarea` | 原 `rgba(159,142,120,0.15)` 极淡 | — | **本轮已强化** 为 `rgba(255,176,0,0.22)` |
| 词条库 Excel/AI 按钮 `btn-primary`/`btn-ghost` | — | 原 **无 scoped 定义**（仅继承基础 `.btn`，无主/次区分） | **本轮已补齐** 主按钮琥珀渐变 + 次按钮幽灵描边 |
| 词条库 `param-input`/`param-select`/`skill-*-input` | 已有 `rgba(159,142,120,0.3)` 框线 | `btn-add`/`btn-save`/`btn-delete` 等已定义 | 通过 |
| 词条库 `btn-add`/`btn-save`/`btn-delete`/`btn-reload` | — | 已定义 | 通过 |
| 骰子工坊 `DiceConfigView` | 输入有 `rgba(255,176,0,0.2)` 框线 | `btn.primary`/`btn.ghost` 本地已定义 | 通过 |
| 单位编辑器 stepper/dkm | stepper 有上下边框；dkm `rgba(159,142,120,0.12)` 框线 | `btn-primary`/`btn-ghost` 全局已定义 | 通过 |
| 战场选择 `join-input` | `rgba(193,232,255,0.25)` 框线 | 本地已定义 | 通过 |
| `SkillsEditor.vue` | `rgba(159,142,120,0.25)` 框线 | — | 通过 |
| `AssetGenPanel.vue` | 全局 `select,input,textarea` `rgba(255,176,0,0.15)` 框线 | `ag-gen-btn` 渐变已定义 | 通过 |

**结论**：生产环境所有「手动输入数值」入口均已有输入框框线、所有按钮均有按键样式。问题集中在**词条库**——向导输入框框线过淡、AI 文本框框线过淡、Excel/AI 导入按钮缺少主/次样式区分，已在本轮修复。

---

## 三、本轮修复（仅 `GlossaryView.vue`）

1. **向导输入框 `.wiz-input`**：框线由 `rgba(159,142,120,0.18)` → `rgba(255,176,0,0.25)`，聚焦加 `box-shadow: 0 0 0 2px rgba(255,176,0,0.15)`，文字色由 `#c1e8ff` 改为 `#ffd597` 与词条库金色主题一致。（影响向导全部 11 个数值字段 + 分支点数/效果/状态 key）
2. **AI 导入文本框 `.ai-import-textarea`**：框线由 `0.15` → `rgba(255,176,0,0.22)`。
3. **按钮 `.btn-primary` / `.btn-ghost`**：补 scoped 定义。主按钮琥珀渐变（`linear-gradient(90deg,#ffb000,#ff8c00)` + `:disabled` 置灰），次按钮幽灵描边。覆盖 Excel 弹窗「确认导入/取消」与 AI「导入技能」。

其它视图（骰子工坊、单位编辑器、战场选择、SkillsEditor、AssetGenPanel）经核对输入框与按钮样式均已合规，未改动。

---

## 四、部署与线上验证

- 流程：`rsync` 同步 → 服务器 `npm run build`（✓ 4.34s）→ `docker compose build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`。
- 线上校验（已执行）：
  - `GET http://106.54.197.69:8081/health` → **200**
  - bundle 含 `btn-primary[data-v-a0758c44]{...background:linear-gradient(90deg,#ffb000,#ff8c00)...}` ✅
  - bundle 含 `wiz-input[data-v-a0758c44]{...border:1px solid rgba(255,176,0,.25)...}` ✅
  - bundle 含 `ai-import-textarea{...border:1px solid rgba(255,176,0,.22)...}` ✅

---

## 五、后续建议
- 词条库 `param-input`/`param-select` 现用 `rgba(159,142,120,0.3)`（暖灰），与向导新琥珀框线（`rgba(255,176,0,0.25)`）风格略不统一，可一并改为琥珀系以保持全页一致（非必须）。
- 全站输入类命名分散（`.param-input`/`.wiz-input`/`.stat-input`/`.join-input` 等），建议后续抽公共 `.numeric-input` 基类统一框线，降低回归风险。
