# 技能-隐匿功能专项报告

> 编写时间：2026-08-08
> 范围：以「隐匿（stealth / conceal）」这一技能为单一对象，梳理其既有机制、六段式+原子设计、前端展示方式，并对照四份「核心」报告的协同终态，指出仍缺失的功能。

---

## 一、隐匿功能既有的工作机制及其出处

### 1.1 自然语言概述

「隐匿」是机甲宇宙战斗引擎中一套围绕 **`unit.stealth` 布尔字段** 运转的隐形/潜行子系统。它同时具备两种进入途径：

- **被动登场即隐**：战术角色为 `ambush`（偷袭方）的单位（或携带 stealth 系词条/技能的单位），在部署或战局载入瞬间被直接打上 `stealth=true` 标记，**不耗 AP、不依赖按钮**。
- **主动按钮进入**：玩家可在行动面板点「👻 隐匿」按钮，经后端通道再调一次 `enter_stealth`，耗 1 点 MOVE 行动点，用于"破隐后重新隐匿"。

隐匿状态受 **四条破隐规则** 约束，一旦触发即强制显形：
1. 真实攻击自动破隐（网关 `/attack` 路由 D9-10）；
2. 释放任意主动技能破隐（`/skill` 路由，但白名单 `STEALTH_ENTER_KEYS` 例外——进入隐匿类技能本身不破隐）；
3. 任一敌方单位停在距离 ≤2 内破隐（移动后 proximity 全量扫描）；
4. 奇袭结算后反应方强制显形（D9-4）。

在引擎层，隐匿还配有四个效果处理器：`enter_stealth`（进入）、`exit_stealth`（退出/破隐）、`stealth_attack_bonus`（奇袭伤害加成，去骰化乘算）、`stealth_evasion`（伪装闪避，去骰化概率判定）。

### 1.2 原文完整引用

**出处 A — 后端资格判定与破隐（门控口径已从 faction 改为 role）：**
> ```2626:2662:mecha-universe-engine/backend-gateway/src/routes/combat.ts
> // （stealth_initiate / stealth_ambush / stealth_camouflage 等）。用于"登场即隐匿"。
> // ★ 修正（2026-07-31）：隐匿资格以「战术角色 role」为准，不再依赖固有阵营 faction。
> //   旧逻辑误将 maxion 固有阵营直接判为隐匿，导致方案A 下 defense→maxion 的防守方被误隐。
> function isStealthCapableUnit(unitData: any, factionRoles?: Record<string, string>): boolean {
>   ...
>   // 严格把关：仅战术角色为 ambush 的阵营单位才享"登场即隐匿"被动
>   const role = ... resolveRole(factionRoles, unitData.faction) : '';
>   if (role === 'ambush') return true;
>   ...
> }
> const STEALTH_ENTER_KEYS = ['stealth_initiate', 'enter_stealth', 'stealth_ambush'];
> const STEALTH_PROXIMITY_RANGE = 2;
> ```

**出处 B — 破隐四规则与白名单（落地注释）：**
> ```3495:3505:mecha-universe-engine/backend-gateway/src/routes/combat.ts
> // Batch B 任务4.1: 进入隐匿（stealth_initiate）
> // ① 登场即隐匿：deploy-unit / 载入存档时，isStealthCapableUnit 命中即 applySpawnStealth
> //    （不耗 AP、不依赖按钮），满足"偷袭单位登场时即生效"。
> // ② 手动按钮仍保留：前端 → 后端 enter_stealth（耗 1 MOVE AP）可破隐后重新隐匿。
> // 破隐规则（4 条，已落地）：
> //   ① 真实攻击自动破隐（/attack 路由 D9-10）；
> //   ② 释放任意主动技能破隐（/skill 路由，STEALTH_ENTER_KEYS 例外：进入隐匿类不破隐）；
> //   ③ 敌方单位停在其距离 ≤STEALTH_PROXIMITY_RANGE(2) 内破隐（/move 后 proximity 全量扫描）；
> //   ④ 奇袭结算后反应方 C 强制显形（D9-4）。
> // 复用核心裁决现有 enter_stealth 通道，不新建第二套隐身体系。
> ```

**出处 C — 需求纪要中的拨乱反正（隐性口径分歧确认）：**
> ```52:53:mecha-universe-engine/docs/游戏功能需求纪要.md
> 🐞 隐匿拨乱反正（已修）：隐匿资格本误绑固有 faction=maxion（防守方），
> 用户预期应属 ambush（偷袭方）；改为 resolveRole()==='ambush' 判定。
> ```

**出处 D — 引擎四个效果处理器（非空壳）：**
> ```55:59:mecha-universe-engine/services/combat-service/src/services/combatCore/effectExecutor.cjs
> // 隐身效果
> enter_stealth: this.handleEnterStealth.bind(this),
> exit_stealth: this.handleExitStealth.bind(this),
> stealth_attack_bonus: this.handleStealthAttackBonus.bind(this),
> stealth_evasion: this.handleStealthEvasion.bind(this),
> ```

> ```574:591:mecha-universe-engine/services/combat-service/src/services/combatCore/effectExecutor.cjs
> async handleEnterStealth(params, context) {
>   const unit = context.unit || context.attacker;
>   if (!unit) return { type: 'enter_stealth', success: false, reason: 'no_unit' };
>   const stealthType = params.type || 'conceal';
>   const duration = params.duration || 2;
>   unit.stealth = true;
>   unit.stealthData = { type: stealthType, duration, appliedAt: Date.now() };
>   return { type: 'enter_stealth', success: true, unitId: unit.id, stealthType, duration };
> }
> ```

---

## 二、六段式（WHEN/IF/WHO/ROLL/DO/COST）+ 三层递归 + 原子 设计方式

> 依据 2026-08-08 拍板的《67原子引擎落地与编辑器词库实施计划》递归六段树模型：六段为 6 类要件的有序列表（顺序即执行序），每段可空、可下挂 1 棵完整子树，深度上限 3 层（root + 1 嵌套 + 1 孙层），层间不继承、仅靠只读全局事件上下文连通。

### 2.1 「隐匿」在六段模型下的设计展开

以「主动隐匿（按钮触发）」为典型词条，按六段映射：

| 六段 | 要件内容 | 对应原子（67 原子表归类） | 设计参数 |
|---|---|---|---|
| **WHEN** | 玩家主动释放（A1） | A1 主动释放 ✅已支持 | trigger: `manual` |
| **IF** | ① 单位具备 stealth 资格（role=ambush 或带 stealth 词条）② 当前未处于隐匿 ③ 拥有 ≥1 MOVE AP | B9 指定状态存在 / 资源门控 | `requires_not_stealth=true`、`requires_ap.move >= 1` |
| **WHO** | 作用目标 = 施法者自身 | D 组目标（SELF） | `target_type: SELF` |
| **ROLL** | 去骰化（隐匿为确定性状态切换） | C1 无判定 ✅已支持 | 无掷骰 |
| **DO** | 写 `unit.stealth=true` + `stealthData={type,duration}` | F 状态组派生（内核扩展原子 `enter_stealth`） | `type:'conceal'`、`duration: 2`（手动）/ `-1`（登场即隐） |
| **COST** | 耗 1 MOVE AP（手动路径）；登场即隐路径 COST 为空 | E 组代价 | `cost: { move: 1 }`（手动）/ 空（被动） |

**三层递归示例（奇袭连锁）**：

```
根层 WHEN=手动释放 / DO=enter_stealth
 └─ nest.WHEN: 当目标为敌方且处于"未察觉"   ← 第2层（DO 嵌的 WHEN 闸门）
      └─ nest.DO: stealth_attack_bonus(multiplier=1.5)  ← 第3层（孙层，奇袭加成）
```

### 2.2 四个隐匿相关原子在 67 原子表的位置与现状

| 原子 | 六段归属 | 引擎现状 | 是否进 EFFECT_TYPE 枚举（shared-kernel 真相源） |
|---|---|---|---|
| `enter_stealth`（进入隐匿） | F 状态 / DO | ✅ `handleEnterStealth` 已实现 | ✅ `ENTER_STEALTH` 已登记 |
| `exit_stealth`（破隐/退出） | F 状态 / DO | ✅ `handleExitStealth` 已实现 | ❌ **枚举缺失** |
| `stealth_attack_bonus`（奇袭加成） | F 状态 / DO | ✅ `handleStealthAttackBonus` 已实现 | ❌ **枚举缺失** |
| `stealth_evasion`（伪装闪避） | F 状态 / DO | ✅ `handleStealthEvasion` 已实现（去骰概率） | ❌ **枚举缺失** |
| `visibility` / `scan_reveal`（扫描揭示敌方隐身） | I 组感知 / DO | ✅ 已实现 | ✅ `VISIBILITY` / `SCAN` 已登记 |

### 2.3 关键设计参数汇总

- 隐匿资格：`role === 'ambush'`（战术角色，非固有 faction），或单位 `skills` 含 stealth 系 key。
- 持续轮数：登场即隐 `duration = -1`（常驻至破隐）；手动按钮 `duration = 2`。
- 破隐邻近阈值：`STEALTH_PROXIMITY_RANGE = 2`（含）。
- 破隐白名单（不破隐的主动技能）：`['stealth_initiate', 'enter_stealth', 'stealth_ambush']`。
- 奇袭伤害加成默认倍率：`multiplier = 1.5`（去骰化 `Math.floor(base*(m-1))`）。
- 伪装闪避默认概率：`evasionChance = 0.5`。
- 手动重新隐匿代价：`cost.move = 1`。

---

## 三、前端展示方式

### 3.1 自然语言描述

前端在 `NewBattleView.vue` 的战场 Canvas 渲染循环中，按观察者视角对隐匿单位分三档处理：

1. **敌方隐匿单位（绝对隐形）**：若单位是敌方（`ownerId` 不同且战术角色不同）且 `stealth=true`，则**根本不在 Canvas 上绘制其机体模型与血条**，确保玩家完全看不到它。
2. **我方/友军隐匿单位（半透明幽灵态）**：自己能看到的隐匿单位（同 ownerId 或同战术角色），画成**派系色 15% 透明填充 + 30% 透明虚线描边圆 + 60% 透明首字母**的半透明幽灵标，不做完整机体/七视图绘制。
3. **隐匿指示器**：在幽灵标上方画一个**青蓝色小圆点**（`rgba(0,180,220,0.7)`）作为"处于隐匿"的标记；同时仍绘制 HP 血条。
4. **上帝视角豁免**：房主（hostId 匹配）或 `dominator`/`referee` 管理员可看到全部隐匿单位的完整形态（不降级为幽灵态）。

行动面板（`ambush` 偷袭方）提供「👻 隐匿」按钮，点击后弹确认框并调用后端进入隐匿；技能 tooltip 若词条带 `requires_stealth` 会显示「需潜行」。

### 3.2 程序展示（关键代码摘录）

**判定与迷雾过滤：**
> ```2383:2401:mecha-universe-engine/frontend/src/views/NewBattleView.vue
> // 敌方隐匿单位：绝不在 Canvas 上绘制其机体模型与血条，确保绝对隐形。
> const isStealth = unit.stealth === true
> const myRole = getFactionRole(myFaction.value)
> const unitRole = unit.role || getFactionRole(unit.faction)
> const isMyUnit = unit.ownerId === myId || (myRole && unitRole === myRole)
> // 上帝视角豁免：房主或 DOMINATOR/REFEREE 管理员可见全部 stealth 单位
> const isGod = !!(
>   (battleState.value?.hostId && myId && String(battleState.value.hostId) === String(myId)) ||
>   ['dominator', 'referee'].includes((user.value?.role || '').toString().toLowerCase())
> )
> if (isStealth && !isMyUnit && !isGod) return
> const isConcealed = isStealth && !isGod
> ```

**半透明幽灵圆标 + 首字母：**
> ```2532:2551:mecha-universe-engine/frontend/src/views/NewBattleView.vue
> } else if (!_hasSeven) {
>   // ---- 隐蔽状态: 低透明度圆形 + 首字母 ----
>   const r = HEX_RADIUS * 0.4
>   ctx.beginPath(); ctx.arc(0, 0, r, 0, Math.PI * 2)
>   ctx.fillStyle = hexToRGBA(fc, 0.15)        // 派系色 15% 透明度
>   ctx.fill()
>   ctx.strokeStyle = isSelected ? hexToRGBA('#ffffff', 0.5) : hexToRGBA(fc, 0.3)
>   ctx.lineWidth = isSelected ? 3.5 : 2.5
>   if (!isSelected) ctx.setLineDash([3, 4])   // 未选中用虚线描边
>   ctx.stroke()
>   const letter = (unit.name || 'U')[0]        // 首字母
>   ctx.fillStyle = hexToRGBA(fc, 0.6)
>   ctx.fillText(letter, 0, 0)
> }
> ```

**青蓝隐匿指示器小点：**
> ```2565:2571:mecha-universe-engine/frontend/src/views/NewBattleView.vue
> // Concealment indicator
> if (isConcealed) {
>   ctx.beginPath()
>   ctx.arc(0, -HEX_RADIUS * 0.33, 2.5, 0, Math.PI * 2)
>   ctx.fillStyle = 'rgba(0,180,220,0.7)'      // 青蓝色小点
>   ctx.fill()
> }
> ```

**行动面板按钮与调用：**
> ```1688:1690:mecha-universe-engine/frontend/src/views/NewBattleView.vue
> ambush: [
>   { key: 'conceal', icon: '👻', label: '隐匿', desc: '单位进入隐匿状态，持续3轮，全员可用' },
> ```
> ```1803:1809:mecha-universe-engine/frontend/src/views/NewBattleView.vue
> } else if (skillKey === 'conceal') {
>   if (!confirm('使用隐匿？该单位将进入隐匿状态（持续3轮）。')) return
>   await combatAPI.action(route.params.id, { actionType: 'conceal', unitId: ... })
>   addLog('action', `👻 ${selectedUnit.value.name} 进入隐匿状态`)
> }
> ```

---

## 四、对照四份「核心」协同终态，仍缺失的功能

> 四份「核心」报告（回写链路 / 数据契约三端错位 / 战场实体真相源七模块 / 词条库与战斗引擎六段式）在对话历史中已各自补入「实机核查」与「终态功能蓝图」章。本报告对其全文检索 `stealth / 隐匿 / 可见性 / enter_stealth / visibility`，结论如下。

### 4.1 现状：隐匿在「核心」协同终态中基本是盲区

- **A《回写链路断点》**：核查全文，**未提及** `stealth`/`visibility` 字段的回写。隐匿状态写回走 `applySpawnStealth` / `breakStealth` 直接改 `unit.stealth`，属"旁路写回"——不经过 A 章主张的 `applyPatch` 统一出口与 `_meta` 透传，在 A 的终态蓝图（统一回写闸门）里是**未登记通道**。
- **B《数据契约三端错位》**：全文**未覆盖** `stealth`/`stealthData`/`_meta.flags.visibility` 字段。前端读 `unit.stealth`，引擎写 `unit.stealth` + `unit._meta.flags.visibility`，但 **`stealthData` 与 `_meta.flags.visibility` 两处状态存在双写、无 Zod 契约**（BattleState 仍无 schema，B 章 P0 项未把隐匿/可见性纳入）。
- **C《战场实体真相源七模块》**：全文**未把 `stealth`/`_meta.flags.visibility` 列为战场实体真相源字段**，faction×role 红线也只论敌我判定，未把"可见性真相"纳入实体模型。
- **D《词条库与战斗引擎六段式》**：67 原子表（A/B/C/D/E/F/K 组）**未收口 `enter_stealth / exit_stealth / stealth_attack_bonus / stealth_evasion`**；六段式编辑器未暴露这些原子，词条作者无法在编辑器里可视化拼装"隐匿/奇袭/伪装"链路。

### 4.2 具体缺口清单（需另行设计/补完）

| # | 缺口 | 缺口性质 | 建议归属报告 |
|---|---|---|---|
| **G1** | `EFFECT_TYPE` 枚举（shared-kernel 真相源）**缺 `EXIT_STEALTH` / `STEALTH_ATTACK_BONUS` / `STEALTH_EVASION`**，仅 `ENTER_STEALTH`/`REVEAL`/`SCAN`/`VISIBILITY` 在册 | 枚举真相源不收口（三头分化） | D（六段式）+ A（契约） |
| **G2** | 前端读 `unit.stealth`，引擎另写 `unit._meta.flags.visibility`——**两处可见性状态并存、无单一真相**，破隐/扫描只改其一会导致前后端不一致 | 实体真相源分裂 | C + B |
| **G3** | 隐匿写回走旁路（`applySpawnStealth`/`breakStealth` 直接改 `unit`），**未纳 A 章统一 `applyPatch` 出口与 `_meta` 透传** | 回写闸门未统一 | A |
| **G4** | `stealthData` 字段（type/duration/appliedAt/auto）**无 Zod 契约、无 DB 落库字段** | 契约缺失 | B |
| **G5** | 67 原子表未收口 4 个隐匿原子，**编辑器无法可视化拼装**隐匿/奇袭/伪装链路 | 原子未登记 | D |
| **G6** | 「登场即隐 vs 手动按钮」两套入口（`/stealth` 独立路由 + `actionType:'conceal'`），**未统一到六段式 `enter_stealth` 原子驱动的单一路径** | 入口双轨 | D + A |
| **G7** | 前端"需潜行"门控（`requires_stealth`）在 DetailPanel/编辑器**无对应 UI 维度**，作者无法配置 | UI 缺维度 | D（L1 主语） |
| **G8** | 奇袭 QTE（`pendingSurprise`）+ 暗绿滤镜为反应方 C 专属，**隐匿方主动奇袭的 QTE/滤镜交互未与隐匿状态联动展示** | 交互未联动 | C（展示） |

### 4.3 结论与建议

隐匿功能在**引擎层与前端层已可运行**（四处理器非空壳、三档渲染已落地、破隐四规则已生效），但在**四份「核心」的协同终态蓝图里是实质盲区**：枚举真相源、实体真相源、统一回写闸门、Zod 契约、67 原子编辑器维度五处均未把它纳入。

其中 **G1 / G2 / G3 / G5** 是阻断"隐匿成为标准可配置技能"的硬缺口，建议在四份核心报告各自补一章「可见性/隐匿协同终态」，把：
- `EXIT_STEALTH` / `STEALTH_ATTACK_BONUS` / `STEALTH_EVASION` 补进 `EFFECT_TYPE` 枚举（G1）；
- 可见性真相单一化为 `unit.stealth` 或 `_meta.flags.visibility` 二选一并 Zod 契约化（G2/G4）；
- `applySpawnStealth`/`breakStealth` 改经 A 章 `applyPatch` 出口（G3）；
- 4 个隐匿原子登记进 67 原子表并开放编辑器拼装（G5）。

若你需要，我可以针对 G1–G8 中任一项直接产出**独立的设计方案**（含枚举补丁、Zod schema、六段式词条 JSON 样例），以便推进施工。

---

## 五、前端专项规划（针对 G1/G2/G5/G6/G7/G8 的前端侧）

> 依据第四章缺口清单，对前端 8 个相关落点（`NewBattleView.vue` / `EffectStackBuilder.vue` / `enums.mirror.js` / `skillContract.js` / `skillContract.d.ts` / `client.js` / `GlossaryView.vue` / `UnitReviewView.vue`）逐一规划。后端枚举与契约（G1/G2/G4）须先由 shared-kernel 落地，前端规划与之对齐。

### 5.1 现状盘点（前端已有什么）

| 落点 | 现状 | 缺口 |
|---|---|---|
| `enums.mirror.js` `EFFECT_TYPE` | 含 `ENTER_STEALTH`/`REVEAL`/`SCAN`/`VISIBILITY` | **缺 `EXIT_STEALTH`/`STEALTH_ATTACK_BONUS`/`STEALTH_EVASION`**（G1 前端侧） |
| `EffectStackBuilder.vue` `EFFECT_FIELD_SCHEMA` | `ENTER_STEALTH` 有 `{duration}` 字段 | 另 3 个隐匿原子**未登记**，编辑器无法拼装（G5 前端侧） |
| `EffectStackBuilder.vue` `STATUS_KEY_OPTIONS` | 含 `stealth_initiate/ambush/camouflage/break` | 仅作 status_key 标签，未与 67 原子联动（G5） |
| `skillContract.js` | 有 `requires_stealth` 字段 + UI「要求隐身」 | 无"赋予隐匿资格 / 登场即隐"维度（G7 部分缺失） |
| `NewBattleView.vue` 渲染 | 三档渲染 + 青蓝指示点 + 上帝视角 | 未消费 `_meta.flags.visibility`（G2 前端侧） |
| `NewBattleView.vue` 按钮 | `ambush` 面板「👻 隐匿」按钮（`actionType:'conceal'`） | 与 `/stealth` 路由双轨（G6） |
| `client.js` | `action()` / `/stealth` 双通道 | 入口未统一（G6） |

### 5.2 规划项 F1–F6

**F1 — 镜像枚举补齐（G1 前端侧）**
- 在 `frontend/src/contracts/enums.mirror.js` 的 `EFFECT_TYPE` 增加 `EXIT_STEALTH` / `STEALTH_ATTACK_BONUS` / `STEALTH_EVASION`，与 shared-kernel `enums.ts` 严格逐字对齐（先后端补、前端镜像）。
- `EFFECT_TYPE_KEYS` 自动随 `Object.keys` 扩展，下拉即出现。

**F2 — 编辑器原子登记（G5 前端侧）**
- 在 `EffectStackBuilder.vue` 的 `EFFECT_FIELD_SCHEMA` 增加三个隐匿原子的子字段：
  - `EXIT_STEALTH`: `{ reason: text, silent: bool }`
  - `STEALTH_ATTACK_BONUS`: `{ multiplier: number(默认1.5), cap: number }`
  - `STEALTH_EVASION`: `{ chance: number(默认0.5), vs_type: select[all,ranged,melee] }`
- 在 `EFFECT_LABELS` / `STATUS_KEY_LABELS` 补充中文标签（奇袭加成 / 伪装闪避 / 破隐）。
- 效果：词条作者可在六段式编辑器可视化拼出「进入隐匿→奇袭加成→伪装闪避→破隐」完整链路。

**F3 — 可见性真相单源（G2 前端侧）**
- 统一读 `unit.stealth` 作为唯一前端可见性来源；`_meta.flags.visibility` 仅作为后端内部标记，前端不再直接依赖其二值。
- `NewBattleView.vue` 渲染三档逻辑保持，但判定表达式由 `unit.stealth`（已是事实）闭环，移除对 `_meta.flags.visibility` 的任何隐式分支（如果存在）。

**F4 — 隐匿资格与登场即隐维度（G7 前端侧）**
- `skillContract.js` 增加词条级字段：`grants_stealth: bool`（该技能/词条赋予隐匿资格）、`spawn_stealth: bool`（登场即隐，对应 `applySpawnStealth`）。
- 在编辑器「compat / 自动化」段补对应 UI 开关（与 `requires_stealth` 并列），作者可配置"本词条是否让单位具备隐匿资格 / 是否登场即隐"。

**F5 — 入口统一（G6 前端侧）**
- `client.js` 收敛为单一 `enterStealth(battleId, unitId)` 封装；`NewBattleView` 的「👻 隐匿」按钮改调该封装，统一经 `actionType:'conceal'`（或后端统一后的 `enter_stealth` 原子），废弃独立 `/stealth` 路由双轨（后端 G6 同步废弃）。

**F6 — 奇袭交互联动（G8 前端侧）**
- 当发起攻击的单位 `unit.stealth===true` 且命中敌方时，`NewBattleView` 在战报与命中瞬间叠加一次**暗绿闪屏滤镜 + 「奇袭!」浮字**（复用 `pendingSurprise` 已实现的滤镜样式），使"主动奇袭"与"被动反应 QTE"视觉语言一致。
- `UnitReviewView.vue` / `GlossaryView.vue` 在展示带 stealth 系词条的单位时，补充「匿」角标。

### 5.3 实施顺序建议

1. **后端先行**：shared-kernel `enums.ts` 补 3 键（G1）→ rebuild gateway。
2. **F1+F2**：镜像枚举 + 编辑器原子登记（纯前端，无后端依赖可先落地，等后端契约）。
3. **F4**：技能契约补 `grants_stealth`/`spawn_stealth` + 编辑器 UI。
4. **F3**：可见性单源清理（低风险渲染改造）。
5. **F5**：入口统一（需后端 G6 路由收敛同批）。
6. **F6**：奇袭视觉联动（体验增强，可后置）。

### 5.4 验收口径

- 编辑器下拉出现 4 个隐匿原子且均可配子字段；
- 带 `spawn_stealth` 词条的单位载入即隐（前端三档渲染生效）；
- 前端不再读取 `_meta.flags.visibility` 作为可见性依据；
- 「👻 隐匿」走单一通道，战报出现「奇袭!」滤镜。

---

## 附：引用文件速查

- `mecha-universe-engine/backend-gateway/src/routes/combat.ts` — 资格判定 / 破隐四规则 / `/stealth` 路由
- `mecha-universe-engine/services/combat-service/src/services/combatCore/effectExecutor.cjs` — 4 个 stealth handler
- `mecha-universe-engine/frontend/src/views/NewBattleView.vue` — 三档渲染 / 按钮 / tooltip
- `mecha-universe-engine/shared-kernel/src/enums.ts` — `EFFECT_TYPE`（缺 3 项）
- `mecha-universe-engine/docs/游戏功能需求纪要.md` — 隐匿拨乱反正（faction→role）
- `67原子引擎落地与编辑器词库实施计划-2026-08-08.md` — 六段式递归模型 / 67 原子表（未收口隐匿）
