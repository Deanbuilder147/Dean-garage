# 数据契约错位与非对称读写 — 全盘代码审计报告

日期：2026-08-06
范围：`mecha-universe-engine` 全仓（frontend / backend-gateway / services/combat-service / shared-kernel）
审计对象：units、skills、glossary、equipment、maps 五类业务实体的「前端录入 → DB 落库 → 后端消费」三端字段契约

---

## 一、结论摘要

共确认 **13 处**数据契约错位隐患，按严重度分级：

| 级别 | 数量 | 说明 |
|---|---|---|
| P0 致命（功能完全失效，静默降级） | 4 | 消费端永远读不到数据，逻辑静默走兜底 |
| P1 严重（部分失效 / 数据丢失） | 5 | 特定路径下丢字段或退化 |
| P2 隐患（当前无害但架构脆弱） | 4 | 命名不一致 / 写多读少 / 缺约束 |

本次会话已**当场修复 2 处**（详见第六节）：
1. `SkillsEditor.vue` 的 `onEffectChange` 破坏性擦除 `skill_key`（P0）
2. `rooms.ts` 增加 `effectiveKey` 中文 Label→英文 Key 回填兜底（P0）

---

## 二、核心隐患总表

> 列义：**写入端字段** = 前端表单/normalizer 实际落库的字段；**消费端期望字段** = 引擎/聚合层实际读取的字段。

| # | 涉及实体 | 写入端字段 | 消费端期望字段 | 错位类型 | 建议修复方案 |
|---|---|---|---|---|---|
| 1 | units · 部件技能 | `attributes.skills_by_owner`（snake_case，3 处写入端一致） | `attributes.skillsByOwner`（camelCase，`rooms.ts` L586） | **命名法错位（硬断链）** | 消费端改读 `attributes.skills_by_owner ?? attributes.skillsByOwner`，双写兼容一个版本后统一为 snake_case（与 DB 列命名法一致） |
| 2 | units · 装备 | **无任何写入端**（`sqlite.ts` 建表无 `equipment` 列） | `rooms.ts` `JSON.parse(u.equipment)`；`equipmentDurability.cjs` 读 `unit.equipment[].durability/slot` | **幽灵外键（消费端凭空期望）** | ✅ **已采纳方案②并落地**：摘除 `rooms.ts` 幽灵读取，`getEquipmentDurability()` 改显式抛错。⚠️ 注意 `equipState`（由 `battleStateFactory` 从 parts 构建）是另一条**有效**路径，已保留 |
| 3 | units · 护甲 | 前端计算 `stats.armor` 并入库 | `damagePipe.cjs` 读 `unit.armor` / `unit.stats.armor`，但 `combat.ts` 的 `toExecutorUnit` **未透传** | **非对称读写（中间层截断）** | `toExecutorUnit` 白名单补 `armor`，并补 `defense`/`evasion` 等同类衍生属性 |
| 4 | skills | `effect`（中文展示文本，12 选项） | `skill_key`（英文 Key）用于查词条 | **中文 Label 与英文 Key 错位** | 已加回填兜底；根治方案见第五节「反查字典单一真相源」 |
| 5 | skills | `skill_key` 允许写 `null`/`''` | `skillExecutor.cjs` 查不到词条 → 走 default 分支 → **静默 0 效果** | **硬外键空缺 + 降级失效** | ①保存接口校验 `skill_key` 非空否则 400；②`skillExecutor` default 分支改为记录显式 `warning` 到战报，禁止静默 |
| 6 | skills | 无人写入 `typeLabel` | `resolveSkillCategory` 三处读 `typeLabel` | **写零读多（死字段）** | 删除 `typeLabel` 读取分支，或在 normalizer 落库时补写 |
| 7 | skills | `category` / `type` 并存 | `resolveSkillCategory` 兼容 `category/type/typeLabel/attack_type[]/action_type[]` **五路兜底** | **弱类型退化（真相源分裂）** | 落库时统一归一为 `category` 单字段，兜底逻辑仅保留读旧数据的迁移期 |
| 8 | glossary | Hub 写存储A（`configLoader`） | 部分旧代码仍可能读存储B 路径 | **双库脑裂残余** | 已于 2026-08-04 收拢为单一写入口；建议加启动自检断言存储B 文件不存在 |
| 9 | glossary | `effects[]` 新维度已迁移入库 | `skillExecutor` **尚未消费** `effects[]`，仍读旧扁平字段 | **写多读少（迁移未闭环）** | 排期让 `skillExecutor` 切换到 `effects[]`，旧扁平字段标记 deprecated |
| 10 | maps · 地形 | 地形 key | 战斗引擎地形加权查表 | **key 语言一致性需锁定** | 强制英文 key，中文仅作 `label` 展示字段，加 CI 校验 |
| 11 | 通用 REST | `const { a,b,c } = req.body` 白名单解构 | 前端提交的其余字段被**静默丢弃** | **非对称读写白名单** | 保存接口对未知字段返回 400 或至少 `console.warn`，禁止静默吞字段 |
| 12 | units | `max_hp` / `hp` / `hpMax` 三端命名不一 | — | **同语义多命名** | 统一为 `max_hp`（DB 命名法），入口处一次性归一 |
| 13 | 全实体 | 无 schema 校验层 | — | **契约无强制力** | 引入 zod/JSON Schema，前后端共享 `shared-kernel` 中的实体 schema |

---

## 三、P0 级隐患详解

### P0-1 部件技能全链路断裂（`skills_by_owner` vs `skillsByOwner`）

三处写入端**全部**使用 snake_case：
- Excel 导入 normalizer（L312）
- 手动单位编辑器（L1052）
- hangar-service

消费端 `rooms.ts` L586 读 `attributes.skillsByOwner` —— **永远为 undefined**。

后果：开战聚合时，机体的「左臂/右臂/腿部」等部件所携带的技能**全部丢失**，单位进入战场时只剩通用技能列表。由于 Phase 32 已删除普通攻击，这意味着部分单位可能**完全无法行动**。

修复：`rooms.ts` 改为
```js
const byOwner = u.attributes?.skills_by_owner ?? u.attributes?.skillsByOwner ?? {}
```

### P0-2 装备幽灵外键

`sqlite.ts` 建表语句中 `units` 表**不存在 `equipment` 列**，但 `rooms.ts` 执行 `JSON.parse(u.equipment)`。

`u.equipment` 恒为 `undefined` → `JSON.parse(undefined)` 抛异常或被 try/catch 吞掉 → 装备数组恒为空 → `equipmentDurability.cjs` 的整套耐久扣减、装备减伤逻辑**全程空转**。

历史溯源：装备系统属于「设计已完成、写入端从未实现」的半成品，引擎侧代码先行落地。

### P0-3 护甲被中间层截断

前端计算并落库 `stats.armor`，`damagePipe.cjs` 也确实读 `armor` 参与减伤计算，但 `combat.ts` 的 `toExecutorUnit()` 转换函数的字段白名单里**没有 armor**。

后果：所有单位在引擎内 `armor` 恒为 `undefined` → 减伤按 0 计算 → **护甲属性完全无效**，玩家投入的护甲数值毫无作用。

这是典型的「两端都对，中间层漏字段」——最难排查的一类非对称读写。

### P0-4 skill_key 空值静默降级

`skill_key` 无非空约束，前端 12 个 `effect` 选项中仅 3 个（格挡/扫射/投掷）在词条库有对应 key。

原 `onEffectChange` 逻辑更糟：切换 effect 时，若反查未命中就把 `skill_key` **写成空串**，会主动擦除用户已通过词条下拉正确绑定的外键。

下游 `skillExecutor.cjs` 查不到词条时落入 `default` 分支，**静默返回无效果**，战报不留痕迹 —— 玩家只会看到「技能放了但什么都没发生」。

---

## 四、三种反模式的系统性归因

### 反模式 A：中文 Label 与英文 Key 错位
根因是**展示层字段与业务层字段共用同一存储位**。`effect` 既当下拉展示值又当业务标识，导致换语言/换文案即断链。

### 反模式 B：非对称读写白名单
根因是**缺少共享 schema**。前端 payload 构造、后端解构白名单、引擎转换函数三处各自维护字段列表，任一处遗漏即断链，且**编译期零报错**（全是弱类型 JS/any）。

### 反模式 C：硬外键空缺与降级失效
根因是**降级路径设计为静默**。所有兜底分支（`default:`、`?? {}`、`|| 0`）都选择「安静地返回空」而非「大声报错」，使得断链在生产环境完全不可观测。

---

## 五、根治方案建议（按优先级）

1. **建立 `shared-kernel/src/contracts/` 实体 schema**（zod），units/skills/glossary/equipment 各一份，前端提交前校验、后端接收后校验、引擎消费前校验。三端引用同一份，字段增删自动全链路可见。

2. **反查字典单一真相源**：中文 Label → 英文 Key 的映射只允许存在于词条库本身，禁止在组件内硬编码 `EFFECT_TO_KEY_FALLBACK` 之类的静态表（当前作为过渡兜底保留，需排期删除）。

3. **降级必须发声**：所有 `default` / `??` 兜底分支加 `logger.warn` 并写入战报的 `diagnostics[]` 数组，前端战报面板显示黄色警告条。

4. **保存接口收紧**：`skill_key` 等业务外键改为 NOT NULL；未知字段提交返回 400 而非静默丢弃。

5. **CI 契约测试**：加一个测试用例，遍历所有实体 schema，断言「写入端字段集合 ⊇ 消费端读取字段集合」。

---

## 六、本次会话已完成的修复

| 文件 | 修改内容 | 状态 |
|---|---|---|
| `frontend/src/components/SkillsEditor.vue` | ①移除 TS 类型注解（普通 JS `<script setup>` 中 `Record<string,string>` 导致 Babel 解析失败、前端构建中断）；②`onEffectChange` 改为**仅在反查命中时才写入** `skill_key`，未命中时保留已有值，杜绝破坏性擦除 | ✅ 已上线 |
| `backend-gateway/src/routes/rooms.ts` | 开战聚合增加 `effectiveKey` 回填：`skill_key` 为空时用中文 `effect`/`name` 反查词条库补齐 | ✅ 已上线 |
| `backend-gateway/src/routes/rooms.ts` L586 | **修复 P0-1**：`attributes.skills_by_owner ?? attributes.skillsByOwner`，恢复部件（左臂/右臂/背包）技能聚合 | ✅ 已上线 |
| `backend-gateway/src/routes/combat.ts` `toExecutorUnit` | **修复 P0-3**：补透传 `armor: s.armor ?? 0`，恢复护甲减伤生效 | ✅ 已上线 |

部署验证：
- 服务器 `backend-gateway` tsc 编译通过，`frontend` build 通过
- `docker compose build --no-cache mecha-gateway mecha-frontend` + `up -d --no-deps` 完成
- 4 容器全部 healthy
- 容器内 `index.html` 资产哈希 == 线上 8081 返回哈希
- 容器内 bundle 含新逻辑；`gateway /app/dist/routes/rooms.js` 含 `effectiveKey`

---

## 七、待办优先级建议

| 优先级 | 事项 | 预估影响面 |
|---|---|---|
| ~~立即~~ ✅已完成 | 修复 P0-1 `skills_by_owner` 读取 | 部件技能全量恢复 |
| ~~立即~~ ✅已完成 | 修复 P0-3 `toExecutorUnit` 补 `armor` | 护甲属性恢复生效 |
| ~~本周~~ ✅已完成 | P0-2 装备系统：**决策为摘除消费端**，已停用耐久计算（详见 `强契约Zod-Schema现状核查与完整契约定义-2026-08-06.md` 第二部分） | 装备/耐久系统 |
| 本周 | `skillExecutor` default 分支改为显式告警 | 可观测性 |
| 排期 | shared-kernel 实体 schema + CI 契约测试 | 根治全部三类反模式 |

---

## 八、核心分歧点落地批示与完整调整清单（2026-08-06 批示）

> 本批示对应《强契约Zod-Schema现状核查与完整契约定义-2026-08-06.md》中定义的 **分歧 A / B / C / D**。
> 总目标：存储 A（`/hub-config`）与读取 API 全面以 `skill_key` 为唯一英文主键；`trigger` 以对象原样落库；靶场接入真实结算引擎；靶向字段统一收敛到 `TARGET_TYPES`。

### 8.0 落地总览

| 分歧 | 级别 | 批示要点 | 涉及主文件 | 落地状态 |
|---|---|---|---|---|
| B | P0 | 拔除 `normalizeSkillForSave` 的 `String(trigger)` 死逻辑，改用 `TriggerContract.safeParse` 原样落库 | `backend-gateway/src/routes/glossary.ts` | 待实施 |
| C | P0 | `id`→`skill_key` 全链路重命名；前端废弃 `id`，灰度期 `rooms.ts/effectiveKey` 兜底 | `glossary.ts`、`GlossaryHub.vue`、`DetailPanel.vue` | 待实施 |
| A | P1 | `DetailPanel.target_scope` 收敛复用 `EffectStackBuilder.TARGET_TYPES`；明确定义 L3/L6 语义 | `DetailPanel.vue`、`combat.ts` | 待实施 |
| D | P1 | `/test-skill` 废弃 `atk+flat-armor` 伪代码，直调 `executeUniversalSkill` | `glossary.ts` | 待实施 |
| — | P3 | 语法层级 L0~L6 分区表单（视效预览，未来 DetailPanel 重构） | `DetailPanel.vue` | 规划中 |

### 8.1 分歧 B / P0-1：拔除 trigger 字符串化死逻辑

**现状（致命）** — `glossary.ts` `normalizeSkillForSave`（178–181 行）：
```ts
// trigger：死字段，强制为字符串，非空则告警（非分派键）
if (skill.trigger != null && skill.trigger !== '') {
  skill.trigger = String(skill.trigger);   // ← 把 {type,condition} 强转成 "[object Object]"
  logger.warn({ msg: `[Glossary][A4] 词条 "${key}" 携带 trigger=...该字段为死字段...` });
}
```
前端 `DetailPanel.vue` 已正确构造对象式 trigger（`trigger.type` + `trigger.condition.distance_less_than` 等），但落库瞬间被 `String()` 阉割，导致 `evaluatePassives` 永远读不到 `trigger.type`（`combat.ts:256` 判 `s.trigger?.type === 'on_turn_start'` 恒为 false），条件被动技能调度机制整体失效。

**根因**：存储层把 `trigger` 当成"死字段"强制扁平化，与 `TriggerContract`（shared-kernel 已定义，`zod@^4.3.6` 已安装）完全脱节。

**修复方案**：
1. `glossary.ts` 顶部新增导入：
   ```ts
   import { TriggerContract } from '@mecha/shared-kernel';
   ```
2. 替换死逻辑为防御式 Parse，原样落库：
   ```ts
   // trigger：命中 TriggerContract 则原样对象落库（分歧 B 修复）
   if (skill.trigger != null && skill.trigger !== '') {
     const parsed = TriggerContract.safeParse(skill.trigger);
     if (parsed.success) {
       skill.trigger = parsed.data;            // 对象原样存储
     } else {
       logger.warn({ msg: `[Glossary][B] 词条 "${key}" trigger 不符合 TriggerContract，已忽略`,
                     issues: parsed.error.issues });
       delete skill.trigger;
     }
   }
   ```
3. 同步清理 178 行上方"trigger 为死字段"注释。

**调整点清单**：
| 文件:行 | 当前 | 改为 |
|---|---|---|
| `backend-gateway/src/routes/glossary.ts:178-181` | `skill.trigger = String(skill.trigger)` | `TriggerContract.safeParse` 对象落库 |
| `backend-gateway/src/routes/glossary.ts` 顶部 | 无 TriggerContract 导入 | 加 `import { TriggerContract } from '@mecha/shared-kernel'` |
| `backend-gateway/src/routes/glossary.ts:178` 上方注释 | "trigger 为死字段" | 更新为"trigger 经 TriggerContract 校验后原样存储" |

**消费端就绪性核验（已确认无需改动）**：
- `skillExecutor.cjs:_getUniversalFields`（~122 行）：`trigger: (cfg.trigger && typeof cfg.trigger === 'object') ? cfg.trigger : ''` —— 已正确消费对象 trigger。
- `combat.ts:evaluatePassives`（250/256）：仅读 `s.trigger?.type === 'on_turn_start'` 以触发被动；`trigger.condition.{hp_below_pct,distance_less_than,distance_greater_than}` 的阈值评估在 `skillExecutor.cjs` 的 `_checkTrigger`（约 254–289 行）进行。`TriggerContract` 原样落库后，两端均直接生效，无需改动。

**风险 / 注意**：
- 引入 `TriggerContract` 要求 `shared-kernel` 已 `npm run build`（dist 含 contracts，且含 zod 依赖）。当前 shared-kernel `package.json` 已含 `zod@^4.3.6`，`build:cjs` 已覆盖 `src/contracts/index.ts`，**无需新增依赖**，但部署时必须重建 shared-kernel 再重建 gateway。
- `TriggerContract` 用 `.passthrough()`，允许 `skill_key`/`name` 等额外字段透传，不会误删其它数据。

**验证**：构造 `trigger={type:'on_turn_start',condition:{distance_less_than:3}}` → PUT `/hub-config/:key` → GET 回读确认 `trigger` 为对象（非字符串）→ 战局内该被动在回合开始被 `evaluatePassives` 命中。

### 8.2 分歧 C / P0-2：id → skill_key 重命名对齐

**现状**：存储 A 已以 `skill_key` 为物理存储键（`stored.skills[key]`，key 即 skill_key），但落库对象内仍冗余写入 `id: key`，且前端读写混用 `id`：
- 后端 `glossary.ts:451`：`const nextSkill = { ...(stored.skills[key] || {}), ...normalized, id: key };`
- 后端 `glossary.ts:396`（GET 兜底）：`skills[k] = (CORE_SKILLS as any)[k] || { id: k, name: k };`
- 前端 `GlossaryHub.vue:165`：`copy.id = key`（进入编辑态）
- 前端 `GlossaryHub.vue:200`：`delete payload.id`（保存前剔除）
- 前端 `DetailPanel.vue:4`：`{{ draft.name || draft.id || '未选择词条' }}`
- 前端 `DetailPanel.vue:11`：`{{ draft.id || '-' }}`（"key" 行展示）

`id` 字段与 `skill_key` 并存，违背强契约 P0-4（唯一英文主键），且 `id` 来源歧义易引发回写错位。

**修复方案**：全链路以 `skill_key` 为唯一英文主键，废弃 `id`。

**调整点清单**：
| 文件:行 | 当前 | 改为 |
|---|---|---|
| `backend-gateway/src/routes/glossary.ts:451` | `...normalized, id: key }` | `...normalized, skill_key: key }` |
| `backend-gateway/src/routes/glossary.ts:396` | `{ id: k, name: k }` | `{ skill_key: k, name: k }` |
| `frontend/src/views/GlossaryHub.vue:165` | `copy.id = key` | `copy.skill_key = key` |
| `frontend/src/views/GlossaryHub.vue:200` | `delete payload.id` | `delete payload.skill_key` |
| `frontend/src/components/glossary-hub/DetailPanel.vue:4` | `draft.id` | `draft.skill_key` |
| `frontend/src/components/glossary-hub/DetailPanel.vue:11` | `draft.id` | `draft.skill_key` |

**灰度兜底（保留，不改动）**：
- `backend-gateway/src/routes/rooms.ts:70/93` 的 `effectiveKey` 反查逻辑在 `skill_key` 为空时用中文 `effect`/`name` 兜底，确保旧局/旧客户端平滑过渡。本批示不触碰该逻辑。

**风险 / 注意**：
- `combat.ts:1625/1850` 技能匹配为 `s.id === skillKey || s.key === skillKey || s.skill_key === skillKey || s.id === skill_id`，**含 `skill_key` 回退**，故去除 `id` 后战局内技能路由仍命中。注意：**单位入战携带的 `skills[]` 必须带上 `skill_key` 字段**（由开战聚合从存储 A 读取，key 即 skill_key，天然带值），否则仅 `id` 匹配会失效。
- `CORE_SKILLS`（`glossary.ts:62-96`）当前用 `id` 定义（如 `id:'melee_strike'`）。建议同步加 `skill_key` 字段（如 `skill_key:'melee_strike'`）保持一致性；前端只读 `s.name||s.label||selectedKey`，不依赖 `id`，故此项为**一致性优化、非强制**。
- 旧战局 JSON（已落库带 `id`）读取时 `id` 仍存在，新流程忽略即可，无破坏性。

**验证**：编辑任意词条保存 → 抓包确认 PUT body 不再含 `id`、`skill_key` 与 URL 主键一致 → GET 回读 `skills[key]` 含 `skill_key` 且**无 `id`** → 开战验证技能仍按 `skill_key` 命中。

**灰度兼容（P0-4 软着陆数据清洗）**：存量数据兼容与回填逻辑**不放在 8.4（8.4 是 P1-D 靶场）**，而归属本 P0-2 段——详见 **§8.9 之③**。要点：读取存量 JSON 时若 `skill_key` 缺失，自动回填 `id` 或 `name` 的 Slug 化字符串；写回时统一持久化为 `skill_key`。这样阶段 3（软着陆）向阶段 4（硬拦截 `skill_key` 非空）过渡时 100% 平滑，不会因旧局/旧客户端缺主键触发契约告警。

### 8.3 分歧 A / P1：消除靶向双轨（target_scope 统一）

**现状（双轨并存）**：
- `DetailPanel.vue:58-64` 的 `target_scope` 选项为旧词表：`enemy / ally / self / enemy_equipment / ally_equipment / both`（含 `all`）。
- `EffectStackBuilder.vue`（TARGET_TYPES，~188-195）为六值枚举：`SELF / SINGLE_ENEMY / AREA_ENEMY / SINGLE_ALLY / AREA_ALLY / ALL_UNITS`。
- 后端 `combat.ts:1629-1631` 双阵营路由特判：`scope === 'both' || scope === 'all' || !order` 才走 `routeEffectStackToTargets`（按 `effects[].target_type` 分头命中）。
- 关联旧词表：`skillContract.js:29` `TARGET_SCOPES`、`:7/:45` `TargetScope` 类型、`GlossaryView.vue:1043` 新词条默认 `target_scope:'enemy'`。

**语义定义（批示明确）**：
- `target_scope` = L3「主目标意图 / 施法范围」—— 描述技能对"哪一方的哪些单位"生效意图。
- `effects[].target_type` = L6「次级路由依据」—— 每个子效果在目标群内再筛选的维度。
- `action_type`（L2 谓语）= 「主动作类型 / 主 UI 路由」—— 仅决定技能顶层谓语分类（attack/heal/buff/debuff/passive）与编辑器主分区，**不参与子效果的目标路由**。
- **边界说明（L2↔L6 谓语归属，详见 §8.9 之①）**：复合技能（如"击中敌人同时治疗自己"）中，L2 `action_type` 只描述主谓语；`effects[]` 内每个子效果 `ef` 携独立 `ef.type`（damage/heal/buff/debuff/status）与 `ef.target_type`，**各自按 `ef.target_type` 执行二次子路由**，不随 L2 主谓语走敌方判定。即"主攻击里夹带 heal/buff 子效果"时，heal/buff 子效果按其 `target_type`（如 `SINGLE_ALLY`/`SELF`）走友方/自身路由，与攻击主效果的敌方路由互不干扰，逻辑上无歧义。

**修复方案（含关键语义冲突处理）**：
- 前端 `DetailPanel.vue` `target_scope` 选项收敛为 TARGET_TYPES，并建立旧→新映射：`enemy→SINGLE_ENEMY`、`ally→SINGLE_ALLY`、`self→SELF`、`both→ALL_UNITS`、`all→ALL_UNITS`；`enemy_equipment`/`ally_equipment` 为装备挂载专用，**保留为 deprecated 别名**（映射 `SINGLE_ENEMY`/`SINGLE_ALLY`），避免装备定向技能失配。
- 后端 `combat.ts:1630` scope 判定扩展，使 TARGET_TYPES 与旧 `both`/`all` 双语义等价：
  ```ts
  const scope = ownedSkillForRouting.target_scope;
  const dualScope = scope === 'both' || scope === 'all' || scope === 'ALL_UNITS';
  if (dualScope || !ownedSkillForRouting.order) {
    const rl = routeEffectStackToTargets(ownedSkillForRouting, casterBattleUnit, writeBackTarget, battle);
    routingLogs.push(...rl);
  }
  ```
- 单目标类（`SINGLE_ENEMY`/`SINGLE_ALLY`/`SELF`/`AREA_ENEMY`/`AREA_ALLY`）不匹配 `dualScope`，自然落到标准单/范围目标路径，无需改动。

**调整点清单**：
| 文件:行 | 当前 | 改为 |
|---|---|---|
| `frontend/src/components/glossary-hub/DetailPanel.vue:58-64` | 旧词表 7 项 | TARGET_TYPES 六值 + equipment 别名 |
| `backend-gateway/src/routes/combat.ts:1630` | `scope==='both'||scope==='all'` | 加 `|| scope==='ALL_UNITS'` |
| （一致性，可选）`skillContract.js:29/7`、`skillContract.d.ts:7/45`、`GlossaryView.vue:1043` | 旧 `TargetScope` 词表 | 收敛为 TARGET_TYPES 取值 |

**风险 / 注意**：
- 必须保留 `both`/`all` 作为 deprecated 别名至少到灰度结束，否则旧词条（已存 `target_scope:'both'`）的 AOE/双阵营路由会失效。
- `DetailPanel.vue:172-174` 已用 `draft.target.type` 渲染 TARGET_TYPES 标签（SELF/SINGLE_ENEMY/AREA_ENEMY/SINGLE_ALLY/ALL_UNITS），**无需改动**，证明 TARGET_TYPES 消费端已就绪。

**验证**：存一个 `target_scope=ALL_UNITS` 且带 `effects[]` 的词条 → 战局释放 → `effect_routes` 返回按 `target_type` 分头命中的敌友结果（与旧 `both` 行为一致）。

### 8.4 分歧 D / P1：靶场接入真实引擎（/test-skill 直调 executeUniversalSkill）

**现状（伪代码）** — `glossary.ts:490-548` `/test-skill`：
```ts
const final = atk + flat - armor;  // 直接算术，绕过 real engine
```
只返回单一 `final` 数字，无法体现 AOE 多目标、地形/高度差/防具差异，与"方案 AOE Fork"验证目标脱节。

**修复方案**：调用真实结算单例 `executeUniversalSkill`，复用与 `/attack`、`/skill` 完全相同的结算链路。

**调整点清单**：
| 文件:行 | 当前 | 改为 |
|---|---|---|
| `backend-gateway/src/routes/glossary.ts` 顶部 | 无 `getSkillExecutor` 导入 | 加 `import { getSkillExecutor } from '../combatBridge.js';` |
| `backend-gateway/src/routes/glossary.ts:490-548` | `atk+flat-armor` 伪代码 | 保持 `POST /test-skill`（body 取 `key`，与现有前端 BattleTestPanel 调用约定一致），内部构造 dummy caster/target + context 直调 `executeUniversalSkill` |

> ⚠️ **路由签名对齐**：现有端点为 `router.post('/test-skill', …)`（**无 `:skill_key` path 参数**，技能键取自 `req.body.key`），且**当前免鉴权公开**。本修复沿用该签名（不改为 path 参数、不新增 `authenticate`），以免破坏前端 BattleTestPanel 现有调用。若后续要加鉴权，须同步改前端调用并带 token。

**实现要点（与 /attack 同构）**：
```ts
router.post('/test-skill', (req, res) => {        // ← 沿用现有路由签名，body 取 key（免鉴权，与前端一致）
  try {
    const { getGlossaryConfig } = getConfigLoader();
    const stored = getGlossaryConfig();
    const key = (req.body && req.body.key) || '';
    const skill = (stored.skills || {})[key];
    if (!skill) return res.status(404).json({ error: 'SKILL_NOT_FOUND', skill_key: key });

    const executor = getSkillExecutor();
    const base = { q:0, r:0, faction:'earth', role:'attack', maxHp:100, hp:100, armor:0, attack:10, defense:0, level:1, skills:[], statusEffects:[] };
    const caster = { ...base, q:0, r:0 };
    // 多目标（AOE 验证）：按 body.targets 批量造 dummy
    const targets = Array.isArray(req.body.targets) && req.body.targets.length
      ? req.body.targets.map((t,i)=>({ ...base, q:i+1, r:0, armor:t.armor??0, hp:t.hp??100 }))
      : [{ ...base, q:1, r:0, armor: req.body.armor ?? 0, hp: req.body.hp ?? 100 }];
    const context = {
      allUnits: [caster, ...targets],
      battleState: { units: [caster, ...targets], round: 1, currentTurn: 0 },
      terrainMap: req.body.terrainMap ?? {},
      weaponMobility: 0,
      skillKey: key,
    };
    const result = executor.executeUniversalSkill(key, caster, targets, context);
    res.json({ success:true, skill_key:key, engine:'combat-service/.cjs', result });
  } catch (e:any) {
    res.status(500).json({ error: String(e?.message || e) });
  }
});
```
- `result.targets[]` 中每个目标含独立 `finalDamage`（受位置/防具/高度差影响），`result.final_damage` 为单目标汇总，完美验证「AOE Fork」。

**风险 / 注意**：
- `executeUniversalSkill` 内部可能读取 `caster.skills`（被动/连携），dummy `skills:[]` 足够（靶场不测被动链）。
- 真实引擎会写回 `currentStats`/statusEffects；靶场为只读验证，dummy 为局部变量不会被持久化，安全。
- `getSkillExecutor` 在 gateway 运行时可用：`combat.ts:40` 已 `import { getSkillExecutor } from '../combatBridge.js'`，路径可达（`combatBridge.ts:32` 导出，`loadCjs('skillExecutor.cjs')` 懒加载）。
- `context` 字段结构须与 `executeUniversalSkill(skillKey, caster, targets, context)` 当前签名对齐（以 `skillExecutor.cjs` 为准）；实施时务必比对 `context.allUnits/battleState/terrainMap` 是否被引擎实际消费，避免传冗余/缺失字段。

**验证**：BattleTestPanel 选「地图炮 / 扇形」词条 → 提交不同 armor/q,r 的多个 target → 回读 `result.targets[].finalDamage` 各目标互不相同，且与 manual-input 数值入口一致。

### 8.5 P3 视效预览（未来 DetailPanel 重构，不在本批示实施范围）

语法层级 L0~L6 分区表单（Identity / Subject / Predicate / Object / Attribute / Adverbial / Complement）为未来重构方案，详见用户提供的 Plaintext 原型。本批示仅落地 B/C/A/D 四项硬修复，UI 分区为后续独立 PR。

### 8.6 完整调整点总表（file:line 级）

| # | 分歧/级别 | 文件:行 | 当前 | 目标 | 风险档 |
|---|---|---|---|---|---|
| 1 | B/P0 | `glossary.ts:178-181` | `String(trigger)` | `TriggerContract.safeParse` 对象落库 | 中（需重建 shared-kernel） |
| 2 | B/P0 | `glossary.ts` 顶部 | — | 导入 `TriggerContract` | 低 |
| 3 | B/P0 | `glossary.ts:178` 注释 | 死字段注释 | 更新注释 | 低 |
| 4 | C/P0 | `glossary.ts:451` | `id:key` | `skill_key:key` | 低 |
| 5 | C/P0 | `glossary.ts:396` | `{id:k}` | `{skill_key:k}` | 低 |
| 6 | C/P0 | `GlossaryHub.vue:165` | `copy.id=key` | `copy.skill_key=key` | 低 |
| 7 | C/P0 | `GlossaryHub.vue:200` | `delete payload.id` | `delete payload.skill_key` | 低 |
| 8 | C/P0 | `DetailPanel.vue:4` | `draft.id` | `draft.skill_key` | 低 |
| 9 | C/P0 | `DetailPanel.vue:11` | `draft.id` | `draft.skill_key` | 低 |
| 10 | A/P1 | `DetailPanel.vue:58-64` | 旧词表 | TARGET_TYPES 六值+别名 | 中（语义映射） |
| 11 | A/P1 | `combat.ts:1630` | `both/all` | 加 `ALL_UNITS` | 中（保留别名） |
| 12 | D/P1 | `glossary.ts` 顶部 | — | 导入 `getSkillExecutor` | 低 |
| 13 | D/P1 | `glossary.ts:490-548` | 伪代码 | 直调 `executeUniversalSkill` | 中（context 构造） |

> 保留不改动：`rooms.ts` `effectiveKey`（灰度兜底，#4–9 兼容）、`DetailPanel.vue:172-174`（TARGET_TYPES 标签已就绪）、`skillExecutor.cjs:122`（对象 trigger 已消费）、`evaluatePassives`（对象 trigger 已消费）、`combat.ts:1625/1850`（含 `skill_key` 匹配回退）。

### 8.7 部署验证清单（确保改动上线后正常运作）

按本仓库既有纪律（见工作记忆「部署」节）：

1. **shared-kernel 重建**：`cd /root/mecha-universe-engine/shared-kernel && npm run build`（ESM+CJS 双产物，含 contracts/zod）→ 确认 `dist/contracts/index.js` 存在且导出 `TriggerContract`。
2. **gateway 编译**：`cd /root/mecha-universe-engine/backend-gateway && npm run build`（tsc 必须 0 error，禁止隐式 any）→ 确认 `dist/routes/glossary.js` 含 `TriggerContract` / `getSkillExecutor` 引用。
3. **前端构建**：`cd /root/mecha-universe-engine/frontend && npm run build`（确认 `draft.id`→`draft.skill_key` 改动被编译进 bundle）。
4. **rsync + 重建容器**：
   ```
   rsync -av --exclude node_modules --exclude .git --exclude dist --exclude '*.db' --exclude 'data/*.log' \
     ./ root@106.54.197.69:/root/mecha-universe-engine/
   ssh -i ~/Desktop/watson.pem root@106.54.197.69 \
     "cd /root/mecha-universe-engine/backend-gateway && npm run build && \
      cd /root/mecha-universe-engine && docker compose build --no-cache mecha-gateway mecha-frontend && \
      docker compose up -d --no-deps mecha-gateway mecha-frontend"
   ```
   > `mecha-comm`/`mecha-battle-db` 不动；dist 由宿主机 rsync 同步，必须先本地 `npm run build` 出 dist 再重建。
5. **磁盘预检**：`ssh ... "df -h /"` 确认非 ENOSPC（历史事故）。
6. **健康与哈希验收**：
   - 4 容器 healthy；
   - `curl 106.54.197.69:8081` 的 `index.html` 资产哈希 == 刚 build 产物哈希；
   - `curl -k -H "Authorization: Bearer <token>" 106.54.197.69:3006/api/hub-config` 返回的 skills 中 `trigger` 为对象、`skill_key` 存在且无 `id`。

### 8.8 端到端正确性校验总览

| 验收项 | 验证手段 | 通过标准 |
|---|---|---|
| B：trigger 对象化 | PUT 带 trigger 词条 → GET 回读 | `trigger` 为 `{type,condition}`，非字符串 |
| B：被动调度通 | 战局内 on_turn_start + distance<3 | `evaluatePassives` 命中，施加效果 |
| C：主键统一 | 编辑保存 → 抓包 | body 无 `id`，`skill_key`==URL key；GET 无 `id` |
| C：战局不回归 | 旧局开战 | 技能按 `skill_key` 命中（#11 回退 + effectiveKey 兜底） |
| A：靶双轨收敛 | 存 ALL_UNITS+effects | 释放走 `routeEffectStackToTargets`，`effect_routes` 敌友分头 |
| D：真引擎 | 地图炮/扇形多 target | `result.targets[].finalDamage` 各目标独立且受 armor/位置影响 |

### 8.9 编码落地阶段 3 个隐蔽工程细节（设计预说明）

> 本节对应"4 处真实分歧（A 靶向双轨 / B trigger 字符串化 / C id↔skill_key 错位 / D 靶场假引擎）"之外，在真正编码落地时容易踩坑的 **3 个边界问题**。提前在设计层显式说明，避免后续返工。

#### ① 多 Effect 叠加时的"动作谓语（L2）归属"混乱

**隐患**：`EffectStackBuilder` 中一个词条可挂载多个 `effects[]`（如"造成伤害 + 给队友护盾 + 给敌方 Debuff"）。若顶级 `action_type`（L2 谓语）为 `attack`，那么 `effects[]` 内的 `heal`/`buff` 子效果是随主攻击走敌方判定，还是按子 effect 自己的 `target_type` 走友方？

**设计显式说明（定稿规则）**：
- **L2 `action_type` 仅决定主动作类型与主 UI 路由**，不参与子效果的目标路由。
- **L6 的每个 `ef` 携独立 `ef.type` 与 `ef.target_type`，各自执行二次子路由**。复合技能中，主攻击的敌方路由与 `heal`/`buff` 子效果的友方/自身路由互不干扰。
- 例："击中敌人同时治疗自己" → 主效果 `ef.type:'damage', ef.target_type:'SINGLE_ENEMY'` 走敌方；子效果 `ef.type:'heal', ef.target_type:'SELF'` 走自身；二者按各自 `target_type` 命中，逻辑无歧义。

**代码级落地落点**：
- 后端路由已在 `combat.ts:1629-1631` 用 `routeEffectStackToTargets(ownedSkillForRouting, …)` 对每个 `effects[].target_type` 分头命中（双轨修复 A 后即完整支持），**无需新逻辑**，但须在文档/注释中明确"L2 不约束子效果路由"以免后续误改。
- 前端 `DetailPanel.vue` 的 `effects[]` 编辑器（`EffectStackBuilder.vue` 的 `TARGET_TYPES`）须保证每个子效果可独立选 `target_type`，且 UI 上区分"主谓语 action_type"与"子效果 target_type"两列，避免混淆。

**验收**：构造 `action_type:'attack'` + `effects:[{type:'damage',target_type:'SINGLE_ENEMY'},{type:'heal',target_type:'SELF'}]` 的词条 → 战局释放 → `effect_routes` 同时出现敌方伤害与自身治疗，且治疗不被误判为敌方。

#### ② 状态效果（Status Effects）的"持续时间（Duration）结算时机"

**隐患**：L6 补语含 `status_effects`（挂接状态），但草案未定义状态层数/回合数的衰减相位。若被动技能在 `on_turn_start`（回合开始）触发并挂载 `duration: 1` 的 Buff，它会在**当前回合结束时立即过期**，还是**下一回合开始时过期**？当前代码存在真实歧义：
- 结构化 `statusEffects`（`skillExecutor.cjs` 经 `BuffManager.buildStatusInstance` 生成）仅携带 `duration`（`skillExecutor.cjs:653` `duration: e.duration || 3`），**全局无 `expiry_phase` 字段**。
- 旧式 `buffs` 衰减在 `combatIntegrator.cjs:412` `cleanupExpiredBuffs`：每个单位回合结束时 `duration--`，`<=0` 移除——若在某回合中途挂载 `duration:1`，到该单位本回合结束即被 tick 到 0，出现"刚挂上就秒失效"。

**设计显式说明（定稿规则）**：为 L6 状态挂接补充 `expiry_phase` 字段：
- `expiry_phase ∈ {'turn_end', 'turn_start'}`，**默认 `'turn_end'`**。
- 语义（`effect` 在相位 P 施加，`duration = N`）：
  - `'turn_end'`：每次**施加者回合结束**时 `duration--`；`duration:1` → 持续至施加者本回合结束（本回合内有效，回合结束移除）。适用"本回合内生效的主动增益"。
  - `'turn_start'`：每次**施加者下一回合开始**时 `duration--`；`duration:1` → 持续过本回合 + 下一回合开始，下一回合开始时移除。适用 `on_turn_start` 被动挂载的 Buff，**避免"刚挂上就秒失效"**。
- 约定：**被动技能（`trigger.type==='on_turn_start'`）施加的 Buff 默认 `expiry_phase:'turn_start'`；主动技能默认 `'turn_end'`**。

**代码级落地落点**：
- `shared-kernel` 契约：在 `StatusEffectContract`（`skill.contract.ts` 内）与 `EffectContract` 的 status 子定义中新增 `expiry_phase: z.enum(['turn_end','turn_start']).optional().default('turn_end')`（需重建 shared-kernel → 见 8.7 部署纪律）。
- 引擎消费：`BuffManager.buildStatusInstance`（`skillExecutor.cjs` 调用）须把 `expiry_phase` 写入 status 实例；衰减逻辑（`combatIntegrator.cjs:412` 或新结构化 tick）须按 `expiry_phase` 选择递减时机（当前 `cleanupExpiredBuffs` 是 `turn_end` 语义，新增 `turn_start` 分支）。

**验收**：`on_turn_start` 被动挂 `duration:1, expiry_phase:'turn_start'` 的 Buff → 本回合全程有效、下一回合开始时恰好移除；主动技挂 `duration:1`（默认 `turn_end`）→ 本回合结束移除。两者均不出现"挂载当回合即消失"。

#### ③ 灰度平滑过渡期的旧数据兼容（P0-4 软着陆）

**隐患**：存储 A（`/hub-config`，磁盘 `config.json`）已存量数十条 JSON，部分为旧格式——只有 `id` / `name`，**没有 `skill_key`**。若直接将 `skill_key` 设为非空硬约束，前端加载旧数据或后端 `safeParse` 时会触发契约告警。

**设计显式说明（定稿规则）**：分两阶段软着陆——
- **阶段 3（软着陆，本批示实施期）**：读取层做**自动回填**，不强制非空；
- **阶段 4（硬拦截）**：待存量数据全部回填（或跑一次迁移脚本）后，再将 `skill_key` 设为非空硬约束（`shared-kernel` `SkillContract` 加 `.min(1)`）；旧战局仍由 `rooms.ts` 的 `effectiveKey`（70/93）兜底。

**代码级落地落点（回填逻辑）**：
- **读路径**：`getGlossaryConfig()`（`configLoader.cjs:48`，当前仅 `JSON.parse`，**无任何归一化**）是存储 A 读取权威源。在两个位置之一注入回填：
  - 推荐在 `GET /hub-config` handler（`glossary.ts:389-407`）遍历 `stored.skills` 时对每个 entry 执行 `entry.skill_key = entry.skill_key || entry.id || slugify(entry.name)`，并就地写回内存对象（前端拿到的即为主键齐备的数据）；
  - 或在 `configLoader.cjs` 的 `getGlossaryConfig` 返回前做同样的归一化（更彻底，战斗引擎读到的也是回填后的值）。
- **写路径**：`PUT /hub-config` → `normalizeSkillForSave`（`glossary.ts:160-202`）增加：`if (!normalized.skill_key && (body.id || body.name)) normalized.skill_key = normalized.skill_key || body.id || slugify(body.name)`。
- **slugify 约定**：转小写、空格/符号替换为 `_`、限长（如 48 字符）、去重（冲突时加数字后缀），确保英文主键合法。
- **迁移脚本（可选，阶段 3→4 桥接）**：一次性遍历 `config.json`，对所有缺 `skill_key` 的 entry 回填并 `saveGlossaryConfig` 写回，使阶段 4 硬拦截前数据已 100% 就绪。

**验收**：直接读取一份仅含 `id:'sweep'` / `name:'顺劈斩'` 的旧词条 → GET 回读其 `skill_key === 'sweep'`（或 `'shunpikezhan'` 若仅 name）→ 前端不告警；保存该词条后写回磁盘的 JSON 含 `skill_key` 且无 `id`（阶段 4 硬拦截亦不报错）。

#### 8.9 验收要点汇总（追加至 8.8）

| 验收项 | 验证手段 | 通过标准 |
|---|---|---|
| ① L2/L6 谓语归属 | 复合技（攻+自疗）释放 | `effect_routes` 同时出现敌方伤害与自身治疗，治疗不被误判敌方 |
| ② expiry_phase | `on_turn_start` 被动挂 `duration:1,expiry_phase:'turn_start'` | 本回合有效、下回合开始移除，无"秒失效" |
| ③ 旧数据兼容 | 读取仅含 `id`/`name` 的旧词条 | 回读 `skill_key` 已回填；保存后磁盘含 `skill_key` 且无 `id` |
