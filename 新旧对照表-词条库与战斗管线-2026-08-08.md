# 新旧对照表 —— 词条库 / 数据契约 / 战斗管线 与 2026-08-08 递归六段树定调的冲突核查

> 生成日期：2026-08-08
> 新设计真相源：`67原子引擎落地与编辑器词库实施计划-2026-08-08.md`（递归六段树模型）
> 配套执行约束：`.codebuddy/memory/MEMORY.md`（部署纪律 / 战棋宪法 v2.0 / faction×role / 射程核定 / Phase A 枚举真相源）

## 〇、背景

用户 2026-08-08 拍板 **递归六段树模型**，推翻了此前几份文档里的若干旧假设。本表核查「词条库改造」「数据契约/防三头分化」「战斗/伤害管线/Segment」三类文档中，与新定调冲突、或已过时失效的论述，供后续开发与文档清理参考。

新模型核心定调（摘要）：
- **六段 = `WHEN/IF/WHO/ROLL/DO/COST` 六类要件，作为有序列表（顺序即执行序），作者可自由排列**。
- **递归树**：每层 ≤6 段、每段最多挂 1 棵子树、最多 3 层（root/child/grandchild），层间独立不继承，仅通过只读全局事件上下文连通。
- **WHEN 不再是"第①段"**，而是"每个节点都有的可选触发闸门"。
- **COST 排在哪就在哪扣**（不再固定发动即扣）。
- **存储 A 单一写入口**（configLoader 读写，消除双库脑裂）；旧扁平字段保留仅为兼容 skillExecutor 读取。

---

## 一、已归档文档（词条库 / 编辑器改造类，不再作为执行依据）

以下 3 份已加「过时·已归档」标记并移至 `历史开发报告/归档-词条库编辑器改造/`：

| 原位置 | 归档位置 | 主要过时点 |
|---|---|---|
| `词条库改造落地实施总结报告-2026-08-06.md` | `历史开发报告/归档-词条库编辑器改造/词条库改造落地实施总结报告-2026-08-06.md` | 基于旧词条卡结构；未体现递归六段树与 `effects[]` 富集维度 |
| `词条库UI补充计划-PhaseF-2026-08-07.md` | `历史开发报告/归档-词条库编辑器改造/词条库UI补充计划-PhaseF-2026-08-07.md` | 编辑器维度按旧扁平字段设计，未对齐新六段树 schema |
| `mecha-universe-engine/docs/词条库中枢-数据交换整改实施报告-20260805.md` | `历史开发报告/归档-词条库编辑器改造/词条库中枢-数据交换整改实施报告-20260805.md` | 含 L0–L6 七层/主谓宾定状补模型，与递归六段树冲突（见下表 §2-1、§3-1） |

> 注：`mecha-universe-engine/docs/skill-hierarchy-主谓宾定状补.md`（L0–L6 草案）**尚未归档**，仍残留仓库，语义与新政冲突，建议后续一并加废弃标注（见下表 §2-1、§3-1）。

---

## 二、数据契约 / 防三头分化类 —— 冲突对照

| # | 主题 | 旧文档说法 | 新设计（2026-08-08） | 冲突等级 | 处理建议 |
|---|---|---|---|---|---|
| D1 | 六段层级归属 | `skill-hierarchy`（L0–L6）：六段被摊平分配到 7 个固定槽，结算严格 L1→L6 硬序，COST 固定 L1 先扣 | 六段为有序列表，顺序即执行序，COST 排在哪就在哪扣；无 L1→L6 硬序 | **高** | 以新模型为准，旧 L0–L6 文档加废弃标注（待办） |
| D2 | 枚举真相源 | 多份报告讨论"三头分化"（编辑器枚举 ≡ shared-kernel 枚举 ≡ handler key 不一） | Phase A 已落地：`shared-kernel/src/enums.ts`（`TIMING/EFFECT_TYPE/ROLL_MODE/LIMIT_SCOPE/VALUE_METHOD/CONDITION_TYPE/ENTRY_TYPE`，`as const`+`XxxKey`）为唯一真相源，全子包统一导入 | **已解决·低** | 文档补一行"已于 Phase A 收敛"，勿再按旧三头分化修改代码 |
| D3 | `effects[]` 字段 | 旧报告/旧 schema 以扁平字段为主，未定义 `effects[]` 维度 | 新编辑器产出 `effects[]`（富集维度）；但 **skillExecutor 尚未消费 `effects[]`**，旧扁平字段仍保留供其读取 | **中（过渡态）** | 引擎侧需排期消费 `effects[]`；文档应注明"双轨兼容期" |
| D4 | 存储读写架构 | 旧整改报告暗示"双库/双写入口"风险 | 已落地存储 A 单一写入口（configLoader 经 glossary.ts 读写），双库脑裂已消除 | **已解决·低** | 旧报告加"已落地"标注，避免后人误以为仍是双库 |
| D5 | 67 项原子来源模型 | 韦诺报告 §11 称 67 项原子从"31 条词条卡经 **L0–L6 七层模型**拆出" | 新词条模型是"递归六段树"，原子应从六段树（WHEN/IF/WHO/ROLL/DO/COST + 子树）拆出，而非 L0–L6 七层 | **中** | 原子清单（A–K 67 项）可保留，但其"来源模型"描述需改为六段树 |
| D6 | `EFFECT_TYPE` 与 67 原子混淆 | 韦诺报告 §11 已自我更正：曾误用 `EFFECT_TYPE`（27 种引擎内部标签）冒充 67 项原子 | 维持更正结论：`EFFECT_TYPE` 是执行器内部标签，67 项是业务能力原子，二者不可混用 | **已更正·低** | 仅需在归档/引用处保留该更正说明 |

---

## 三、战斗 / 伤害管线 / Segment 类 —— 冲突对照

| # | 主题 | 旧文档说法 | 新设计（2026-08-08） | 冲突等级 | 处理建议 |
|---|---|---|---|---|---|
| C1 | 六段结构形态 | 韦诺报告「〇、我们的机制」：词条 = **两段式**（WHEN 触发闸门 + 五段内容 WHEN/IF/WHO/ROLL/DO/COST 中 WHEN 单列） | 统一六段有序列表，**WHEN 不是单独闸门段**，而是每个节点都可带的可选触发闸门；六段平权可重排 | **高** | 以新模型重写"机制"描述；WHEN 处理改成"节点级闸门" |
| C2 | 结算顺序模型 | 韦诺报告 / C 段方案：五插槽 `PRE_CHECK→TARGET_RESOLVE→MAIN_CALCULATION→EFFECT_MUTATION→POST_EVENT_TRIGGER` 硬序；Slot 内对原子做确定序排序 | 新模型"顺序即执行序"，六段按列表序执行；嵌套子树在父段 `nest` 处展开 | **中** | 五插槽可作"引擎内部实现细节"保留，但对外契约改为"六段有序 + 节点闸门" |
| C3 | Branch 双引擎 | C 段方案 §4「历史词条兼容：旧 Branch 模型词条不受影响，仅 Segment 路径走新汇流，Branch 保持原样」 | 新设计鼓励**纯六段/Segment**，Branch 为过渡兼容层；长期应迁移至六段树 | **中** | 标注 Branch 为"遗留兼容层，新词条勿用" |
| C4 | Segment 伤害结算 | C 段方案：Segment 内 `type:damage` 走 **direct_damage 旁路**，绕开 `damagePipe`（武器惩罚/减伤/暴击）；`override` 仅战报标注 | 新模型要求 Segment 的 `damage` **接入主伤害管线**（`damagePipe` 汇流），含真实减伤与暴击 | **高（未落地）** | 该方案仍为草稿，落地时须按新模型汇流；文档标注"待实施" |
| C5 | 嵌套 / 子树 | C 段方案 / 韦诺报告未提及词条内嵌子树 | 新模型明确：每层 ≤6 段、每段最多 1 子树、最多 3 层；层间独立不继承，仅只读全局上下文连通 | **高（新增能力）** | 战斗引擎需补"子树展开"执行逻辑；文档补子树语义 |
| C6 | 层间数据传递 | 旧模型（L0–L6）隐含层级继承/流水线传递 target/roll | 新模型：层间**不继承**，共享载体是只读全局事件上下文（`attacker/primary_target/self/trigger_event` 等） | **中** | 引擎禁止父层向子层透传可变状态；文档明示"只读上下文" |
| C7 | ATOM_REGISTRY / 原子扩展 | 韦诺报告 §七：原子自描述（slot/applyTo/priority），主干不变 | 方向一致（注册表自描述）✅；但原子"来源"应按六段树而非 L0–L6 | **低（方向一致）** | 保留实现手法，仅修正来源模型描述 |
| C8 | 谓语承载 | skillExecutor 谓语 switch 为 attack/heal/buff/debuff/passive 5 谓语；DO 段默认承载单谓语效果 | 新模型 DO 段为有序效果列表，可承载多谓语/多效果组合，不限于单谓语 | **低** | 引擎 DO 段应支持多效果聚合；文档补此语义 |

---

## 四、待办清理项（建议后续执行）

1. ✅ **`mecha-universe-engine/docs/skill-hierarchy-主谓宾定状补.md`（L0–L6 草案）已于 2026-08-08 加「过时·已废弃」标注**，注明被递归六段树取代（与已归档的 词条库中枢报告同命运）。文件保留原位未移动。
2. **`韦诺之战战斗引擎对比分析-2026-08-07.md`「〇、我们的机制」段**改写：两段式 → 统一六段有序列表 + 节点级 WHEN 闸门；§11 原子"来源模型"由 L0–L6 改为六段树（A–K 清单保留）。
3. **`阶段二C-Segment伤害管线重接方案-2026-08-07.md`** 头部标注"草稿/待实施"，并把 §3 汇流目标对齐新模型的"Segment damage 必须过 damagePipe"。
4. **引擎侧补两件事**（非文档）：① 消费 `effects[]` 富集约；② 子树（最多 3 层）展开执行 + 只读全局上下文传递。
5. 数据契约类 D2/D4 已落地，仅在相关报告补"已收敛/已落地"标注即可，无需改代码。

---

## 五、结论

- **词条库 / 编辑器改造 3 份文档**：已归档 + 过时标记，不再作为执行依据。
- **数据契约类**：D2/D4 已在新模型下解决，D1/D3/D5 为表述或过渡态冲突，需标注或排期。
- **战斗 / Segment 类**：C1/C4/C5 为高冲突（六段形态、伤害汇流、子树能力），其中 C4 仍是未落地的草稿；C2/C3/C6 为需对齐的语义差异；C7/C8 方向一致仅微调。
- **唯一不被推翻的真相源**：`67原子引擎落地与编辑器词库实施计划-2026-08-08.md` + `shared-kernel/src/enums.ts` + `MEMORY.md` 部署纪律。
