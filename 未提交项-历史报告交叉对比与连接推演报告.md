# 43 个未提交项 × 老旧报告交叉对比与功能推演报告

> 生成时间：2026-08-02
> 范围：git 工作区中 **未提交** 的项目核心项（去除 `.codebuddy/`、`memory`、`skills-lock`、纯重构噪声后约 43 个，含 39 修改 + 4 已删 + 30 未追踪项目文件；本报告聚焦其中 43 个"功能/架构相关"项）。
> 对比基线：`./历史开发报告/`（约 188 篇 md，多为 2026-07-23 及更早的"桌面散落"报告）+ `./历史备份/`（约 122 篇 md）。
> 关键信息：通过 `grep` 精确匹配 `victoryChecker|conditionTrigger|reactionHandlers|damageModifiers.cjs|hexKey|diceService.cjs|unitSize|SizeConfigView|AdminPermissionsView|AttackReportModal|AssetGenPanel|battleSocket|animationQueue|manual-input-audit|data-contract-tests|syncMeowaTerrains|sprite-compare` **等核心模块名在全部历史报告与备份中 0 命中**。仅 `apStore` 词串在 2 篇报告中近似出现，实为 `battleStore/mapStore` 误匹配，非真实文件。

---

## 一、总体结论

| 维度 | 结论 |
|---|---|
| 在老旧报告中被提及 | **几乎全部未提及**（命中率 <3%）。老报告描述的是 Phase 1~29 及 7/23 前的架构，而本批未提交项是 **Phase 30（DiceUnify）→ 8/1 七视图尺寸体型工坊 → 8/2 方向语义校准** 这条更晚的演进线，时间上在老报告之后。 |
| 这批项是"垃圾"吗 | **否**。每个文件头部均带 Phase 自标注与清晰的职责注释（如 `Phase 30-DiceUnify`、`H7 七视图`、`A3 资产管理`、`P8 权限`），且与记忆库红线（七视图尺寸/方向/骰子工坊/faction×role 双语义）高度吻合，属于**已成型但未 commit 的功能增量**。 |
| 风险点 | 未提交即代表"云端裸奔"（服务器若已 rsync 则线上有、本地无版本控制；若未 rsync 则本地有、线上无）。按用户纪律须尽快按语义分原子 commit 并入。 |

---

## 二、按功能域分组与逐项推演

### 域 A：战斗结算核心（combat-service .cjs）—— 后端真实结算层

| 文件 | 历史报告提及 | 假设有用 → 应连接何处 / 作用 |
|---|---|---|
| `combatCore/diceService.cjs`（新增） | 未提及 | **唯一掷骰真相源**（Phase 30-DiceUnify）。连接：所有 `rollDice` 调用点（`damagePipe/branchEvaluator/skillExecutor/hookChain/tagProcessor/factionSkillRegistry`）统一收口到此单例；全局参数 `glossary.dice` 段实时读取。经网关 `GET/PUT /api/combat/dice-config` 暴露给前端 `DiceConfigView`。 |
| `combatCore/damageModifiers.cjs`（修改 +56） | 未提及 | 伤害修正累加层。连接：`damagePipe._calcDefense/_calcWeaponPenalty` 与装备/技能/地形减伤；新增 `applyFocusedFire`（集火）等分支，被 `skillExecutor` 在结算时调用。 |
| `combatCore/victoryChecker.cjs`（新增） | 未提及 | 胜负判定器。连接：`/end-turn` 与房间 `victory_conditions` JSON；被 `combat.ts` 在回合结束/单位阵亡后调用，写回 `battleState.status=win/lose/draw`。 |
| `combatCore/conditionTrigger.cjs`（新增） | 未提及 | 条件触发（持续效果/回合事件）。连接：`buffManager` 与 `statusEffects`，按 `onTurnStart/onAttack/afterDamage` 钩子触发，由 `hookChain` 编排。 |
| `combatCore/reactionHandlers/` 目录（新增 6 文件） | 未提及 | **反应系统**（反击/援护/抢先）。连接：`skillExecutor` 结算出"被攻击/被治疗"事件后，向反应方推送可选 reaction；前端 `components/reactions/` 渲染选择 UI，经 socket 回传。 |
| `combatCore/hexKey.cjs`（新增） | 未提及 | 六角格坐标键工具（`hexKey(r,q)` 归一化）。被 `move`(Dijkstra)、`terrainMovement`、ZOC 计算共用，去重幽灵字符串拼接。 |
| `combatCore/aiStrategies.cjs`（修改 +16） | 未提及 | AI 决策策略。连接：`/end-turn` 敌方回合接管，调用 `reactionHandlers` 与 `skillExecutor` 选技能。 |
| `combatCore/buffManager.cjs`（修改 +105） | 未提及 | buff/debuff 生命周期。连接：`skillExecutor action_type=buff/debuff` 落点；与 `conditionTrigger` 双向。 |
| `combatCore/skillExecutor.cjs`（修改 +386） | 未提及（老报告提过早期版） | v5.0 万能语法。新增 reaction/condition 谓语分支，写回 `statusEffects`。 |
| `combatCore/factionSkillRegistry.cjs`（修改 +78） | 未提及 | 按 `role`（非 faction）发技能。连接：红线 `unitRoleOf(u)` 兜底；前端 `ROLE_SKILLS[attack/defense/ambush]`。 |
| `combatCore/branchEvaluator/hookChain/tagProcessor/skillRegistry/tagCompatibilityAdapter/tags/stealth-tags/terrainMovement`（小改） | 未提及 | 全部改造成调用 `diceService` 单例 + 透传 `role`，无独立新职责。 |

### 域 B：前端战斗视图与交互（Vue）

| 文件 | 历史报告提及 | 假设有用 → 连接 / 作用 |
|---|---|---|
| `views/NewBattleView.vue`（+1223） | 未提及 | 主战斗视图。连接：渲染 `reactionHandlers` 选择弹层、`AttackReportModal`、`animationQueue` 播放、`apStore/logStore/moveStore` 三个 Pinia store；`getFactionSkills(role)` 按角色发技能。 |
| `views/NewPreparationRoom.vue`（+1338） | 未提及 | 战前房间。连接：`pushRoomUpdate`→comm→`prep-<id>`；房主开战权限校验（`host_id`）；`victory_conditions` 编辑。 |
| `views/NewUnitEditorView.vue`（+557） | 未提及 | 单位编辑器。连接：七视图 `directionViews` 方向箭头（按 hexUtils `DIRECTIONS` 真相源，`arrowRotate=朝向角+90`）；`unitSize` 七视图盒子。 |
| `views/NewBattlefieldSelector.vue`（+461） | 未提及 | 战场选择/续接。连接：`/me` 返回 `lastRoomId`→"返回上次战局"；`/api/map/battlefields`。 |
| `views/GlossaryView.vue`（+1206） | 未提及 | 词条库中枢。连接：`glossary.dice` / `glossary.size` 段可视化编辑，热更新到 `diceService` / `unitSize`。 |
| `views/NewBattlefieldView.vue` / `NewHomeView.vue` / `NewLoginView.vue` / `NewRegisterView.vue`（小改） | 未提及 | 路由/入口微调，接入新 sidebar 与权限门禁。 |
| `components/HexGridCanvasEngine.vue`（+224） | 老报告提过早期版（固定画布方案） | 渲染引擎。连接：宪法红线——单向数据管道终点，禁读 ref；`hexDraw`/`hexUtils` 唯一数学真理；画 `reactionHandlers` 高亮格、`AssetGen` 预览精灵。 |
| `components/layout/TheSidebar.vue`（+448） | 未提及新入口 | 侧边栏。新增"骰子工坊/体型工坊/权限/资产管理"入口 → 路由 `/dice-config` `/size-config` `/admin-permissions` `/asset-gen`。 |
| `components/AssetGenPanel.vue`（新增） | 未提及 | **资产管理面板（A3）**。连接：`image_gen` 能力生成精灵 → 写入 hangar `units` 贴图；`sprite-compare.html` 是其实机对照工具。 |
| `components/AttackReportModal.vue`（新增） | 未提及 | **战报弹窗**。连接：`docs/攻击结算与命中反馈_视觉设计报告与方案.md`（视觉方案来源）；展示 `victoryChecker`+`damagePipe` 结算明细；`战报生成器-数值模板演示.html` 是其模板原型。 |
| `components/reactions/`（新增 3 文件） | 未提及 | 反应 UI（反击/援护/抢先选择）。连接：域 A 的 `reactionHandlers` 经 socket 推送。 |
| `stores/apStore.js` / `logStore.js` / `moveStore.js`（新增） | 老报告近似提过（误匹配） | Pinia 状态。连接：`apStore`=AP 三动作(attack/move/defend)派生 `has_moved/has_acted/has_defended`；`logStore`=战斗日志流；`moveStore`=Dijkstra 移动预览。`NewBattleView` 是主消费者。 |
| `utils/unitSize.js`（新增） | 未提及 | 七视图尺寸真相源（`SIZE_SEVEN_BOX`/`SIZE_HIT_FACTOR`）。与后端 `unitSize.ts` 必须一致；`SizeConfigView` 热更新。 |
| `utils/diceUtil.js`（新增） | 未提及 | 前端骰子表达式解析，镜像 `diceService` 表达式语法。 |
| `utils/battleSocket.js`（新增） | 未提及 | 封装 socket.io 客户端，对接 comm `mecha-comm:3005`；`reactions`/`room-update`/`attack-report` 事件订阅。 |
| `utils/animationQueue.js`（新增） | 未提及 | 战斗动画队列（命中/受击/移动补间），被 `NewBattleView` 驱动 `HexGridCanvasEngine` 绘制。 |
| `utils/hexDraw.js`（+42）/ `hexUtils.js`（+142） | 老报告提过早期版 | 数学真理升级：新增 `DIRECTIONS` 6 扇区中心角、`computeDirection`、`SIZE_SEVEN_BOX` 适配；方向语义按记忆红线校准。 |
| `contracts/skillContract.js`（+55） | 未提及 | 前端技能契约。连接：`skillExecutor` v5.0 谓语白名单，前端校验技能 JSON 合法性。 |
| `api/client.js`（+102） | 未提及 | axios 封装。新增 `/api/combat/dice-config`、`/api/combat/size-config`、`/api/auth/*` 权限接口、`FormData` 删 Content-Type 处理。 |
| `App.vue`（+30）/ `main.js`（+58）/ `styles/variables.css`（+112）/ `nginx.conf`（+224）/ `vite.config.js`（+41） | 老报告提过主题 | 接入新路由、amber 主题真相源、nginx 反代 `/api`→3006 `/socket.io`→3005、vite 代理与 chunk 拆包。 |

### 域 C：后端服务（gateway / comm / hangar / shared-kernel）

| 文件 | 历史报告提及 | 假设有用 → 连接 / 作用 |
|---|---|---|
| `backend-gateway/.../combat.ts`（修改，未列具体行） | 老报告提过早期战斗路由 | 战斗路由聚合：新增 `GET/PUT /api/combat/dice-config`、`/size-config`、reaction 接收端点；`victoryChecker` 接入 `/end-turn`；`resolveUnitRole` 透传。 |
| `comm-service/src/index.js`（+48）/ `socketService.js`（+90） | 老报告提过 comm | socket 实时层：新增 `reaction` 事件转发、`attack-report` 推送、`room-update` 内部 token 同步（`x-internal-token`）；`battleSocket.js` 的对接端。 |
| `hangar-service/src/routes/units.js`（+2） | 未提及 | 单位路由微调，对接 `AssetGenPanel` 贴图写入字段。 |
| `shared-kernel/src/types.ts`（+44） | 未提及 | 类型扩展：`BattleUnit.role?`、`SizeConfig`、`DiceConfig`、`VictoryCondition` 等类型，前后端共用真相。 |
| `shared-kernel/tsconfig.tsbuildinfo`（+2） | — | 构建缓存，无语义，应加 `.gitignore` 或随 tsbuild 忽略。 |

### 域 D：独立脚本 / 工具 / 报告（项目支撑）

| 文件 | 历史报告提及 | 假设有用 → 连接 / 作用 |
|---|---|---|
| `scripts/data-contract-tests.cjs`（新增） | 未提及 | **数据契约测试**。连接：`contracts/skillContract.js` 与 `shared-kernel/types.ts`，CI 校验前后端 DTO 一致。 |
| `scripts/manual-input-audit.cjs`（新增） | 未提及 | **手动输入审计**。连接：`手动输入数值入口与输入框样式核查报告`（同源产出），校验所有手动数值入口符合规格。 |
| `backend-gateway/scripts/syncMeowaTerrains.mjs`（新增） | 未提及 | **地形同步脚本**。连接：Meowa 资产管理 → `glossary` 地形段 / `hangar` 地形表，批量同步。 |
| `docs/攻击结算与命中反馈_视觉设计报告与方案.md`（新增） | 未提及（新产出） | `AttackReportModal` 视觉方案来源文档。 |
| `全栈API与通信节点全局清查报告.md`（新增） | 未提及（新产出，根目录） | API 节点清查，对应 `api/client.js` 与 comm/gateway 路由清单。 |
| `手动输入数值入口与输入框样式核查报告-2026-07-30.md`（新增） | 未提及（新产出，根目录） | 对应 `manual-input-audit.cjs` 的设计依据。 |
| `情况调查报告-端口连接与词条库排版-2026-07-30.md`（新增） | 未提及（新产出，根目录） | 端口/词条库排版的排查结论。 |
| `词条库中枢-数值场景词典.html`（新增） | 未提及 | `GlossaryView` 的离线演示/词典原型。 |
| `战报生成器-数值模板演示.html`（新增） | 未提及 | `AttackReportModal` 数值模板原型。 |
| `sprite-compare.html`（新增） | 未提及 | `AssetGenPanel` 精灵实机对照工具。 |
| `attack-report-demo.html`（新增） | 未提及 | `AttackReportModal` 的早期 demo。 |
| 4 个已删 `.md`（WeirdNova 世界观 ×1、异星/地球技能对照 ×2、未连接缺口建议 ×1） | 属老报告本身 | 被删除说明已被后续实现取代或并入新报告体系，属**清理动作**，非功能项。 |

---

## 三、连接关系总图（推演）

```
                         ┌─ diceService.cjs ──┐ (唯一掷骰真相)
                         │   victoryChecker   │
                         │ conditionTrigger   │
后端 combat-core ────────┤ reactionHandlers ──┼──→ combat.ts 路由
                         │ damageModifiers    │      │
                         │ hexKey / buffMgr   │      ├─ GET/PUT /api/combat/dice-config ─→ DiceConfigView
                         │ skillExecutor v5   │      ├─ /api/combat/size-config ──────────→ SizeConfigView
                         └────────────────────┘      └─ reaction 端点 ───────────────────→ components/reactions/
                                  │                            │
                          shared-kernel/types.ts ◀──DTO──▶ contracts/skillContract.js
                                  │                            │
        comm-service (socket) ────┴── attack-report/room-update/reaction ──▶ battleSocket.js
                                                                              │
前端 NewBattleView ──── apStore / logStore / moveStore ── animationQueue ──▶ HexGridCanvasEngine
        │                      │                                            (宪法红线单向管道)
        ├─ AttackReportModal ◀── docs/攻击结算视觉方案 + 战报demo.html
        ├─ AssetGenPanel ─────── syncMeowaTerrains + sprite-compare.html ──▶ hangar units
        └─ TheSidebar 入口：/dice-config /size-config /admin-permissions /asset-gen
```

---

## 四、风险与建议

1. **全部未提交 = 版本失控**。按用户"云端裸奔"纪律，须立即按语义分原子 commit（建议分组：① `feat(combat)`: 域 A 战斗核心 ② `feat(frontend)`: 域 B 视图/store/组件 ③ `feat(api)`: 域 C 服务/共享类型 ④ `chore(tools)`: 域 D 脚本/报告）。
2. **shared-kernel/tsconfig.tsbuildinfo** 是构建缓存，建议加入 `.gitignore`，勿随功能提交。
3. **前后端一致性红线**：`unitSize.js`↔`unitSize.ts`、`diceService`↔`diceUtil`、`types.ts`↔`skillContract` 三对必须同步提交，否则出现 DTO 漂移。
4. **老报告已过时**：这批项证明项目已演进到 Phase 30+，老报告（≤7/23）仅作历史参考，不应作为当前架构依据；建议以记忆库红线 + 本报告为新的"现状真相"。
5. **删除的 4 个 .md** 属旧报告清理，确认无外部引用后可随清理 commit 一并提交。

---

## 五、逐项提及速查表（✓=老报告提及 / ✗=未提及）

| # | 项 | 提及 |
|---|---|---|
| 1 | combatCore/diceService.cjs | ✗ |
| 2 | combatCore/damageModifiers.cjs | ✗ |
| 3 | combatCore/victoryChecker.cjs | ✗ |
| 4 | combatCore/conditionTrigger.cjs | ✗ |
| 5 | combatCore/reactionHandlers/* (6) | ✗ |
| 6 | combatCore/hexKey.cjs | ✗ |
| 7 | combatCore/aiStrategies.cjs | ✗ |
| 8 | combatCore/buffManager.cjs | ✗ |
| 9 | combatCore/skillExecutor.cjs | ✗(早期版提过) |
| 10 | combatCore/factionSkillRegistry.cjs | ✗ |
| 11 | combatCore 其余小改 (8) | ✗ |
| 12 | views/NewBattleView.vue | ✗ |
| 13 | views/NewPreparationRoom.vue | ✗ |
| 14 | views/NewUnitEditorView.vue | ✗ |
| 15 | views/NewBattlefieldSelector.vue | ✗ |
| 16 | views/GlossaryView.vue | ✗ |
| 17 | views 其余 4 个小改 | ✗ |
| 18 | HexGridCanvasEngine.vue | ✓(早期固定画布方案) |
| 19 | TheSidebar.vue | ✗(新入口) |
| 20 | AssetGenPanel.vue | ✗ |
| 21 | AttackReportModal.vue | ✗ |
| 22 | components/reactions/* (3) | ✗ |
| 23 | stores/apStore.js | ✗(误匹配) |
| 24 | stores/logStore.js | ✗ |
| 25 | stores/moveStore.js | ✗ |
| 26 | utils/unitSize.js | ✗ |
| 27 | utils/diceUtil.js | ✗ |
| 28 | utils/battleSocket.js | ✗ |
| 29 | utils/animationQueue.js | ✗ |
| 30 | utils/hexDraw.js / hexUtils.js | ✓(早期版) |
| 31 | contracts/skillContract.js | ✗ |
| 32 | api/client.js | ✗ |
| 33 | App.vue/main.js/variables.css/nginx.conf/vite.config.js | ✓(主题/反代早期) |
| 34 | combat.ts (gateway) | ✓(早期路由) |
| 35 | comm-service index.js/socketService.js | ✓(早期 comm) |
| 36 | hangar units.js | ✗ |
| 37 | shared-kernel/types.ts | ✗ |
| 38 | shared-kernel/tsconfig.tsbuildinfo | — |
| 39 | scripts/data-contract-tests.cjs | ✗ |
| 40 | scripts/manual-input-audit.cjs | ✗ |
| 41 | syncMeowaTerrains.mjs | ✗ |
| 42 | docs+根目录报告/html (7) | ✗(多数新产出) |
| 43 | 已删 4 个 .md | ✓(即老报告本身) |
