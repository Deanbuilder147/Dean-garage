# 机甲战棋 (Mecha Battle) - 使用说明

**版本**: BETA 1.0  
**访问地址**: http://106.54.197.69:8081  
**技术栈**: Vue 3 + PixiJS + Node.js + Express + SQLite

---

## 一、系统概述

「机甲战棋」是一款战术模拟系统，你可以在其中创建机甲棋子、编辑战场地图、编组阵容并进行回合制对战。当前版本支持多用户注册登录，每位用户拥有独立的数据空间。

---

## 二、首次访问

### 2.1 入口页面（终端界面）

打开 `http://106.54.197.69:8081` 后，你会看到一个**类 Unix 终端界面**（TerminalView）。这是系统的统一入口。

终端启动后会自动执行以下流程：

1. **Boot Sequence** - 显示系统启动日志（KINETIC_OS v2.4.1 启动动画）
2. **登录/注册** - 终端提示输入命令：

   | 命令 | 功能 |
   |------|------|
   | `login` | 登录已有账号 |
   | `register` | 注册新账号 |

3. **输入凭证** - 按提示依次输入用户名和密码
4. **登录成功** - 自动跳转到主页

> 💡 终端界面支持 **Tab 键自动补全**命令。

### 2.2 注册新账号

在终端输入 `register` 后：
1. 输入用户名
2. 输入密码（密码输入时字符会被隐藏）
3. 选择所属阵营：**地球联合 / 拜隆 / 马克西翁**
4. 注册成功后自动登录

---

## 三、主页（HomeView）

登录成功后进入主页，顶部显示：
- **MECHA BATTLE** 标题
- **用户名**和**所属阵营标签**
- **退出登录**按钮

主页提供三个核心功能入口：

| 入口 | 功能 | 说明 |
|------|------|------|
| 🗡️ **机甲棋库** | 浏览、创建、编辑机甲棋子 | 跳转到棋子编辑器 |
| 🛡️ **整备室** | 编组出战阵容 | 跳转到整备室（需先选择战场） |
| ⚔️ **开始对战** | 选择战场开始对战 | 弹出战场选择弹窗 |

---

## 四、机甲棋库（UnitEditorView）

这是棋子管理的核心页面，路由为 `/units`。

### 4.1 棋子列表
- 以网格卡片形式展示你创建的所有棋子
- 每张卡片显示：棋子图片、名称、代号、所属阵营
- 点击卡片进入编辑模式
- 点击 🗑️ 按钮删除棋子

### 4.2 创建/编辑棋子

点击「➕ 新建棋子」或点击已有棋子进入编辑表单，包含以下配置：

#### 基础信息
- **机体番号** *（必填）* - 例如 `RX-78-2`
- **行动代号** - 例如 `高达`
- **所属阵营** - 地球联合 / 拜隆军 / 马克西翁（选择后需确认，不可随意更改）
- **主机体图片** - 支持上传图片文件

#### 主机体属性（上限 40 点）
- **格斗** - 近战攻击力
- **射击** - 远程攻击力
- **防御** - 物理防御
- **速度** - 移动速度
- **EN** - 能量值
- **HP** - 生命值

#### 子机体
可配置多个子机体（浮游炮、支援机等），每个子机体有独立属性。

#### 技能
为棋子配置主动/被动技能，支持自定义技能效果。

#### Excel 批量导入
支持通过 Excel 文件批量导入棋子数据。

---

## 五、战场编辑（BattlefieldView）

路由为 `/battlefields`，用于创建和编辑六边形战场地图。

### 5.1 战场列表
- 显示你创建的所有战场
- 每张卡片显示：战场名称、地图尺寸（宽×高）

### 5.2 创建战场
1. 点击「+ 新建战场」
2. 输入战场名称
3. 设置地图尺寸（宽度 × 高度，以六边形格子为单位）
4. 确认创建

### 5.3 编辑战场
选中战场后进入编辑模式：
- **格子间距调整**：水平间距、垂直间距、行偏移量，以 2% 步进微调
- **地形编辑**：在六边形格子上放置地形元素
- **保存**：编辑完成后点击 💾 保存

---

## 六、整备室（PreparationRoom）

路由为 `/preparation/:roomId`，用于战前准备。

### 6.1 房间信息
- 顶部显示战场名称和地图尺寸
- 显示房间编号

### 6.2 阵营选择
- 玩家选择自己的出战阵营（地球联合 / 拜隆 / 马克西翁）
- 不同玩家不能选择同一阵营

### 6.3 AI 对战设置（房主专属）
房主可配置 AI 对手：
- **选择 AI 阵营** - 从未被玩家占用的阵营中选择
- **选择 AI 难度** - 设置 AI 的智能程度
- 点击「+ 添加AI对手」展开设置面板

### 6.4 阵容编组
- 从你的棋子库中选择出战棋子
- 在战场上布置初始位置
- 准备就绪后开始对战

---

## 七、对战（BattleView）

路由为 `/battle/:id`，实际的对战界面。

- 六边形网格战场，采用 PixiJS 渲染
- 回合制对战逻辑
- 棋子移动、攻击、技能释放
- 支持 PvP（玩家对战）和 PvE（人机对战）

---

## 八、数据隔离

- 每位用户只能看到和操作**自己创建**的棋子和战场
- 不同用户的数据完全隔离
- 阵营选择与用户账号绑定

---

## 九、页面路由一览

| 路由 | 页面 | 是否需要登录 |
|------|------|:---:|
| `/` | 终端界面（登录/注册） | ❌ |
| `/home` | 主页 | ✅ |
| `/units` | 机甲棋库（棋子列表） | ✅ |
| `/units/new` | 新建棋子 | ✅ |
| `/units/:id` | 编辑棋子 | ✅ |
| `/battlefields` | 战场编辑 | ✅ |
| `/battle/:id` | 对战界面 | ✅ |
| `/preparation/:roomId` | 整备室 | ✅ |

---

## 十、注意事项

1. **登录限流**：15 分钟内最多登录 10 次，频繁尝试会被暂时限制
2. **阵营不可更改**：棋子创建时确认阵营后不可更换（需联系管理员）
3. **属性上限**：主机体总属性点不超过 40 点
4. **浏览器要求**：建议使用 Chrome / Edge 最新版本，支持 WebGL
5. **CRT 效果**：页面有 CRT 扫描线视觉效果，终端页面无此效果
