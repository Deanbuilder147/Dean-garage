# 触控方案 A — 现有前端叠加触控实施文档

> 版本：v1.0（基于 2026-08 评估结论 + 用户补充 3 项技术细节）
> 日期：2026-08-18
> 决策结论：**方案 A（在现有 PC 前端叠加触控层）优于独立小程序端**。PC 端零回归是硬性红线。

---

## 0. 目标与红线

### 0.1 目标
让战棋前端在触摸屏（手机/平板）上可用：单指平移、双指捏合缩放、点按选中/触发技能，且**完全不破坏现有 PC 鼠标交互**。

### 0.2 红线（不可违反）
1. **PC 零回归**：原有 `mousedown/mousemove/mouseup/wheel/click` 逻辑**原样保留**，绝不删除或改写。
2. **宪法 v2.0 不变**：Canvas 仍是单向数据管道终点，坐标数学仍只走 `hexUtils.js`；本方案只在**事件输入层**做抽象，不碰渲染管线。
3. **单一入口 8081**：改动构建进 `mecha-frontend`，不新增端口/子应用。
4. **路由隔离**：触控逻辑只在 `/battle-mobile`（MobileBattleView）链路生效；PC 路由 `/battle-pc`（NewBattleView）完全不引入指针抽象。

---

## 1. 技术选型（采纳用户 3 项细节）

### 1.1 Pointer Events 统一抽象（取代 mouse + touch 双套）
- 使用 `pointerdown / pointermove / pointerup / pointercancel`，不混用原生 `touchstart` 等。
- 每根手指用 `event.pointerId` 登记到 `activePointers: Map<number, {x,y}>`。
- `canvas.setPointerCapture(pointerId)` 确保移出画布仍收到 move。
- **收益**：消除 Ghost Clicks（鼠标 click 与触摸合成的重复触发），单套代码覆盖鼠标/触控笔/手指。

### 1.2 Slop 阈值 8px（Tap / Pan 区分）
- 手指/指针按下后，若位移 < `SLOP_PX = 8` 且未达时间阈值，判定为 **Tap**（触发 cell-clicked）。
- 位移 ≥ 8px 即升级为 **Pan**（平移），并取消本次 Tap。
- 沿用现有 `isDragging` 语义，但阈值统一为 8（原 mouse 用 5，保持 PC 原值不动，触控新路径用 8）。

### 1.3 Pivot Zoom 双指锚点锁定
- 双指时，缩放锚点 = 两指**中点**对应的世界坐标，缩放过程中该世界点钉死在屏幕中点不动。
- 公式复用现有 `getWorldPos` + `offsetX/offsetY/scale` 补偿逻辑（与 `wheel` 缩放同源）。
- 双指间距比 `currentDist / startDist` 作为缩放倍率，夹在 `[computeFitScale(), 3]`。

---

## 2. 现有可复用资产（勿重写）

文件：`mecha-universe-engine/frontend/src/components/HexGridCanvasEngine.vue`

| 函数 | 行号 | 用途 |
|------|------|------|
| `setupEvents()` | 1068 | 现有 mouse/wheel 绑定，**保留不动** |
| `getHexAtEvent(e)` | 326 | 屏幕事件→hex 坐标 |
| `getWorldPos(e)` | 339 | 屏幕事件→世界坐标（缩放锚点用） |
| `updateVisibleRange()` | 354 | 缩放/平移后刷新可见格 |
| `computeFitScale()` | 598 | 最小缩放下限 |
| `draw()` | — | 重绘（平移/缩放末尾调用） |
| `zoomIn/zoomOut/zoomReset` | 1163/1175/1187 | 按钮缩放，触控层可调用 |

相机状态（ref）：`offsetX / offsetY / scale`；标志位 `isDragging`。

---

## 3. 实施步骤

### 阶段 0 — 抽离纯函数（PC 零回归验证点）
在 `HexGridCanvasEngine.vue` 的 `<script setup>` 内，**新增**（不改动现有）以下纯函数，把相机操作从事件里解耦出来，供 Pointer 路径复用：

```js
// ---- 触控/指针共享的相机原语（新增，不影响现有 mouse 事件）----
const SLOP_PX = 8

function startPan(clientX, clientY) {
  dragStartX = clientX; dragStartY = clientY
  dragStartOX = offsetX.value; dragStartOY = offsetY.value
  isDragging = false
}

function doPan(clientX, clientY) {
  const rect = mainCanvas.value.getBoundingClientRect()
  const sx = mainCanvas.value.width / rect.width
  const sy = mainCanvas.value.height / rect.height
  const dx = clientX - dragStartX
  const dy = clientY - dragStartY
  if (Math.abs(dx) > SLOP_PX || Math.abs(dy) > SLOP_PX) isDragging = true
  if (isDragging) {
    offsetX.value = dragStartOX + dx * sx
    offsetY.value = dragStartOY + dy * sy
    draw()
  }
}

function endPan() {
  if (isDragging) updateVisibleRange()
  isDragging = false
}

function doZoomAt(clientX, clientY, factor) {
  const ns = Math.max(computeFitScale(), Math.min(3, scale.value * factor))
  const worldPos = canvasPosToWorld(clientX, clientY) // 或 getWorldPos 等价
  offsetX.value += (scale.value - ns) * worldPos.wx
  offsetY.value += (scale.value - ns) * worldPos.wy
  scale.value = ns
  updateVisibleRange()
  draw()
}

function doTap(clientX, clientY) {
  // 复用现有 click 逻辑：emit cell-clicked
  const hex = getHexAtEvent({ clientX, clientY })
  const data = props.gridData
  if (hex.q >= 0 && hex.q < (data.width || 100) && hex.r >= 0 && hex.r < (data.height || 100)) {
    const rect = mainCanvas.value.getBoundingClientRect()
    emit('cell-clicked', {
      q: hex.q, r: hex.r,
      screen: { x: clientX - rect.left, y: clientY - rect.top }
    })
  }
}
```

> 验证：此阶段仅抽离，不绑定任何新事件。PC 鼠标交互与改造前逐字节一致 → 部署后 curl 8081 验收 200，人工确认 PC 拖拽/缩放/点击无变化。

### 阶段 1 — Pointer Events 绑定（仅 mobile 链路）
新增 `setupPointerEvents()`，在 `MobileBattleView` 加载的引擎实例上调用（通过 `mode` 或 props 开关，避免 PC 触发）。

```js
const activePointers = new Map()
let pinchStartDist = 0
let pinchStartScale = 1

function setupPointerEvents() {
  const canvas = mainCanvas.value
  if (!canvas || !props.enableTouch) return

  canvas.addEventListener('pointerdown', (e) => {
    canvas.setPointerCapture(e.pointerId)
    activePointers.set(e.pointerId, { x: e.clientX, y: e.clientY })
    if (activePointers.size === 1) startPan(e.clientX, e.clientY)
    else if (activePointers.size === 2) {
      const pts = [...activePointers.values()]
      pinchStartDist = Math.hypot(pts[0].x - pts[1].x, pts[0].y - pts[1].y)
      pinchStartScale = scale.value
    }
  })

  canvas.addEventListener('pointermove', (e) => {
    if (!activePointers.has(e.pointerId)) return
    activePointers.set(e.pointerId, { x: e.clientX, y: e.clientY })
    if (activePointers.size === 1) {
      doPan(e.clientX, e.clientY)
    } else if (activePointers.size === 2) {
      const pts = [...activePointers.values()]
      const dist = Math.hypot(pts[0].x - pts[1].x, pts[0].y - pts[1].y)
      const mid = { x: (pts[0].x + pts[1].x) / 2, y: (pts[0].y + pts[1].y) / 2 }
      if (pinchStartDist > 0) doZoomAt(mid.x, mid.y, (dist / pinchStartDist))
    }
  })

  const up = (e) => {
    const wasSingle = activePointers.size === 1
    activePointers.delete(e.pointerId)
    canvas.releasePointerCapture?.(e.pointerId)
    if (wasSingle && !isDragging) doTap(e.clientX, e.clientY) // Slop 内=Tap
    if (activePointers.size < 2) pinchStartDist = 0
    if (activePointers.size === 0) endPan()
  }
  canvas.addEventListener('pointerup', up)
  canvas.addEventListener('pointercancel', up)
}
```

- 开关：`HexGridCanvasEngine` 新增 prop `enableTouch: Boolean`（默认 `false`）。`MobileBattleView` 传 `:enable-touch="true"`；`NewBattleView` 不传（保持 `false`）。
- `setupPointerEvents()` 在 `onMounted` 内 `if (props.enableTouch) setupPointerEvents()`。

### 阶段 2 — 防 Ghost Click
- 触控路径**不触发原生 `click`**：`doTap` 直接 emit `cell-clicked`，绕过 `canvas` 的 `click` 监听。
- 若发现移动端浏览器仍合成 click，在 `setupEvents` 的 click 监听里加守卫：`if (props.enableTouch && e.pointerType !== 'mouse') return`（仅移动端屏蔽合成 click，PC mouse 不受影响）。

### 阶段 3 — MobileBattleView 外壳接线
- `MobileBattleView.vue`：实例化 `HexGridCanvasEngine` 并传 `:enable-touch="true"`，监听 `cell-clicked` 走与 PC 相同的选中/技能逻辑（复用现有 `NewBattleView` 的业务方法，但**不**直接 import 该组件，遵循红线注释）。
- 触控不需要右侧/底部折叠把手（移动端布局另述，不在本阶段范围）。

### 阶段 4 — 构建与部署验收
1. 本地改完 → rsync 到 `106.54.197.69:/root/mecha-universe-engine/`（exclude node_modules/.git/dist）。
2. 服务器 `cd /root/mecha-universe-engine/frontend && npm run build`。
3. `docker compose build --no-cache mecha-frontend` → `up -d --no-deps mecha-frontend`。
4. 验收：`curl -s 106.54.197.69:8081` 资产哈希 == 刚 build 产物哈希，且 bundle 含 `enableTouch` 文案。
5. **回归测试**：PC 访问 `/battle-pc`，确认拖拽/滚轮缩放/点击选中与改造前一致（红线 0.2.1）。
6. **触控测试**：用浏览器 DevTools 设备模拟（或真机）访问 `/battle-mobile`，验证单指平移、双指捏合、点按选中。

---

## 4. 验收清单（勾选制）

- [ ] 阶段 0 纯函数抽离完成，PC 鼠标交互逐字节不变
- [ ] 阶段 1 `setupPointerEvents` 仅在 `enableTouch=true` 时绑定
- [ ] 阶段 2 Ghost Click 已消除（移动端不触发重复 click）
- [ ] 阶段 3 MobileBattleView 接 `cell-clicked` 并复用业务方法
- [ ] 阶段 4 部署 8081 验收 200，资产哈希匹配
- [ ] PC 回归：拖拽 / 滚轮缩放 / 点击选中全部正常
- [ ] 触控：单指平移 / 双指捏合锚点锁定 / 点按 Tap（Slop 8px）全部正常

---

## 5. 风险与回退

| 风险 | 缓解 |
|------|------|
| Pointer 事件与 mouse 事件在混合设备（触控笔+鼠标）冲突 | `enableTouch` 按路由隔离；PC 路由永不开启 |
| 双指锚点漂移 | 复用 `getWorldPos` + `offset/scale` 补偿（与 wheel 同源），不另写数学 |
| 部署后 PC 表现变化 | 阶段 0 即做 PC 零回归验收，任何变化立即回退该阶段 |
| 磁盘 ENOSPC | 构建前 `df -h /`；失败先 `docker builder prune -f` |

回退：本方案为**纯增量**（新增函数 + 新 prop + 新事件绑定），回退只需删除 `setupPointerEvents` 调用与 `enableTouch` prop，现有 mouse 逻辑原样保留，可一键 revert。

---

## 6. 不在本阶段范围（后续迭代）
- 移动端专属 UI 布局（折叠把手、浮动技能栏适配）。
- 惯性滑动（momentum）、双击放大等增强手势。
- `MobileBattleView` 与 `NewBattleView` 业务方法进一步抽公共 composable（当前仅复用逻辑、不共享组件实例）。
