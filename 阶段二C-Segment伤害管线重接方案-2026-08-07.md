# 阶段二 C：Segment 伤害管线重接方案（草稿）

> 日期：2026-08-07
> 前置：阶段一 A/B/B5/D/E/F 已上线（纯 Segment 模型扫射靶场验证通过）

## 0. 现状回顾

纯 Segment 模型已能让「扫射」词条脱离 Branch 旁路、用 `roll.segments[].effects[]`
表达离散点命中 / 多分支同中 / 全失效 / AOE 重定向 / 均摊 / 伤害模式。

但阶段一有个明确边界（effectExecutor.cjs L208-209 注释已标注）：

- Segment 内 `type:damage` 走 **direct_damage 旁路**，与谓语主伤害线（`damagePipe.cjs`）
  **完全独立**；
- `damage_mode:'override'` 在旁路下只是战报标注，**数值行为 = bonus**，即：
  它不会真正「覆盖并回主伤害线」，也不经过武器惩罚 / 装备减伤 / 地形减免 / 暴击倍率；
- 因此当前「扫射」的伤害是裸 `flat_value` 经均摊，绕过了全部真实减伤与暴击。

这意味着：纯 Segment 模型**只解决了「表达层」统一**，尚未解决「结算层」统一。
阶段二 C 要做的，就是让 Segment 的 `damage` 效果**接入主伤害管线**。

## 1. 目标（验收口径）

1. Segment 内的 `damage` 效果经 `damagePipe.cjs` 结算（含武器惩罚/装备/技能/地形减伤、暴击倍率）。
2. `damage_mode:'override'` 语义落地：用 Segment 伤害**替代**谓语默认伤害（而非叠加）；
   `damage_mode:'bonus'`（默认）语义落地：在谓语伤害外**追加**。
3. 阶段一已有的 A/B/B5/D/E/F 行为**不退化**（靶场两条路径仍正确）。
4. 单一写入口、共享枚举、靶场可复验，约束不变。

## 2. 当前结算链路（红线梳理）

```
skillExecutor.executeUniversalSkill
  └─ 谓语 switch (attack/heal/buff/debuff/passive)
       └─ attack 谓语 → damagePipe.cjs 主伤害线（含暴击/减伤）
  └─ _resolveRollSegments（阶段一）
       └─ effectExecutor.executeSync(seg.effects)
            └─ handleDirectDamage → 旁路裸伤害（不含 damagePipe）
```

问题根因：**两股伤害线在 skillExecutor 内并行，从未汇流**。

## 3. 方案草案：Segment 伤害经 damagePipe 汇流

### 3.1 handleDirectDamage 改造（effectExecutor.cjs）
- 新增 `damagePipe` 引用（经 configLoader / require 注入，避免幽灵依赖）。
- 当 `params.type==='damage'` 且上下文标记 `useMainPipe:true` 时：
  - 构造 `damagePipe` 入参（attacker=unit, defender=target, baseDamage=raw,
    damageKind=params.damage_kind, weaponMobility, battleState）；
  - 用 `damagePipe.applyDamage(...)` 的**返回值**（含 finalDamage / crit / blocked）
    覆盖 `totalDealt`，而非 `Math.round(raw*(1+pen*0.05))` 占位公式。
- `split_mode:'equal'` 仍在外层对 `finalDamage` 均摊（damagePipe 先算单份，再按目标数除）。
- `damage_mode` 在此只记录意图，真正 override/bonus 汇流由 skillExecutor 控制（见 3.2）。

### 3.2 skillExecutor 谓语后的汇流点（关键）
- 在谓语 switch 之后、`_resolveRollSegments` 之前，记录谓语主伤害
  `predicateDamage = result.finalDamage`（attack 谓语才有，其余 0）。
- `_resolveRollSegments` 返回 `segResult` 后：
  - 若 Segment 含 `damage_mode:'override'` 的效果 →
    最终伤害 = `segTotal`（Segment 伤害），**丢弃** predicateDamage；
  - 若 `damage_mode:'bonus'` → 最终伤害 = `predicateDamage + segTotal`；
  - 非 damage 类效果（heal/buff/debuff）按既有逻辑独立应用，不受 override 影响。
- 需在 `result` 上新增 `segmentDamage` / `predicateDamage` / `finalDamage` 三字段，
  供靶场 snapshot 与前端战报展示。

### 3.3 靶场复验
- `test-skill` 已支持 `dice_result` 覆盖 + 唯一 id 桩位（本次已修 AREA_ENEMY 误判）；
- 新增断言：开启 `damage_mode:'override'` 且配置减伤/暴击时，
  Segment 伤害应**反映** damagePipe 结果（而非裸 flat_value）。

## 4. 风险与待决

| 项 | 风险 | 缓解 |
|----|------|------|
| damagePipe 入参契约 | 需 attacker/defender 完整字段，Segment 桩位可能缺字段 | 沿用 test-skill 桩位扩展，补齐 defense/currentStats |
| 双槽 dualSlot | 阶段一 routes=2 会让 damagePipe 跑两遍 | 汇流时按 routes 乘或显式控制，需明确语义 |
| 暴击倍率归属 | override 路径下暴击是否生效 | 先与 predicate 同规则，靶场验证 |
| 历史词条兼容 | 旧 Branch 模型词条不受影响 | 仅 Segment 路径走新汇流，Branch 保持原样 |

## 5. 下一步

1. 与用户确认 `damage_mode:'override'|'bonus'` 在「非 attack 谓语（如 passive 扫射）」下
   的 predicateDamage 基准（passive 谓语 predicateDamage=0，override 即纯 Segment 伤害）。
2. 实现 3.1 + 3.2，本地补 verify 脚本（含减伤/暴击断言）。
3. 部署 SOP：rsync → backend-gateway `npm run build` → `docker compose build --no-cache mecha-gateway` → up。
4. 线上靶场复验两条路径 + override/bonus 区分。
