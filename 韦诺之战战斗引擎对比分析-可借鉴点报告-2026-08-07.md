# 开源战棋引擎《韦诺之战》（Wesnoth）代码实现手法借鉴 —— 让我们的「词条-战斗管道」机制在执行层更稳

> 日期：2026-08-07
> **定位澄清**：本报告的机制（六段式 / 五插槽 / ATOM_REGISTRY / PipelineContext / 结构化日志）**已经定稿、不改动设计**。这里只问一件事：**Wesnoth 在代码实现层面有哪些现成手法，能让我们这套机制在代码里执行得更稳、更省事、更少 bug**。
> 所以每条都是：**① 我们机制在代码执行上的痛点 → ② Wesnoth 用什么代码手法解 → ③ 我们怎么套用**。不模仿它的机制，只借它的实现技巧。
> 源码基线：`src/actions/attack.cpp`、`src/units/attack_type.cpp`、`src/units/abilities.cpp`、WML/WFL。

---

## 〇、我们的机制（已定稿，仅作索引）

- 词条 = 六段式 `WHEN/IF/WHO/ROLL/COST/DO` + `ATOM_REGISTRY` 原子真相源
- 管道 = 五插槽 `PRE_CHECK → TARGET_RESOLVE → MAIN_CALCULATION → EFFECT_MUTATION → POST_EVENT_TRIGGER`
- 沙盒 = `PipelineContext{caster, targets[], rollValue, skillMeta, battleState, mutationQueue}`
- 日志 = `PipelineResult{triggered, executedAtoms, details[]}`
- 入口真相（已核查）：`skillExecutor.executeUniversalSkill → effectExecutor.executeSync → damagePipe.calculate`

---

## 一、痛点：双路径分发不一致（冲突核查 C1）

**① 痛点**：`effectExecutor.executeSingle` 里有 `status→modify_stat / damage→direct_damage / recovery→apply_buff` 的重定向，但 `executeSync`（roll segment 真实入口）**没有这段**。结果同一份原子组合，走 skill 路径被正常重定向，走 segment 路径直接落 `unknown_effect_type`——两条路径行为不一致，Fuzz 测试必然出现"静默忽略"。

**② Wesnoth 的代码手法**：它**只有一个**求值入口 `attack_type::modified_damage() / modified_chance_to_hit()`，所有修正（特技、能力、环境）都通过这个唯一入口的 `specials_context_t` 链式消费。`battle_context` 不管是真实战斗还是 `simulate()` 概率模拟，都调**同一个** `battle_context_unit_stats` 构造函数——**不存在"两条路径两套逻辑"**。

**③ 我们怎么套用**：把重定向从 `executeSingle` 里的手写 if **抽成一个唯一的 `dispatchEffect(effect, ctx)` 函数**，`executeSingle` 和 `executeSync` 都只调它。这正是第九章"统一解释器"在代码层的最直接落地——用一个分发函数消灭双路径分歧。

---

## 二、痛点：原子效果"中途崩溃污染状态"

**① 痛点**：我们第九章 `PipelineContext` 设计了 `mutationQueue`（延迟统一写回），但当前 `effectExecutor` 是**内联直接改写** `target.current_hp`。一旦一个原子执行到一半抛异常，已经内联改掉的血量/状态就留在单位上，后续原子读到脏数据，整场战斗状态不一致。

**② Wesnoth 的代码手法**：它在 `battle_context_unit_stats` 构造时把**全部统计量算成不可变的快照值**（CTH、damage、strikes 都是构造结果，不是边跑边改的活对象）。真正改动单位发生在 `perform_hit` 里，且是**顺序确定的单方向写回**；模拟用的 `combatant` 是**独立副本**，绝不碰真实单位对象。即"先算清、后落地、模拟与真实隔离"。

**③ 我们怎么套用**：不必等 `mutationQueue` 完整实现，先套用两个 Wesnoth 手法：(a) **沙盒构造时把读到的数值快照进 `PipelineContext`**（而非 handler 里反复读活对象）；(b) **每个原子的"计算"与"写回"分离**——handler 先把要改的值算好返回给管道，管道在 Slot 收尾时统一写回单位。这样单个原子抛错也不会留下半截状态。

---

## 三、痛点：多原子组合结算顺序未定义（Fuzz 500 验收风险）

**① 痛点**：Slot 4 `EFFECT_MUTATION` 里若同时挂了 `DISPLACEMENT` + `ENTER_STEALTH` + `PLUNDER`，当前没有规定谁先谁后。Fuzz 随机组合时，不同顺序可能产出不同结果甚至冲突（如先偷装备再隐身 vs 先隐身再偷），属于"未定义行为"。

**② Wesnoth 的代码手法**：它的特技/能力生效有**硬编码但确定的四阶段顺序**——`自身武器特技 → 敌方武器特技 → 自身单位能力 → 敌方单位能力`，且每个带 `priority_` 数值。所有修正按这个顺序**确定性地**依次施加，没有"看情况"。

**③ 我们怎么套用**：在管道执行 Slot 时，对 `executedAtoms` 做一个**确定序排序**再依次执行：`(来源: caster效果 → target效果) × priority`。排序逻辑是纯函数，不依赖运行时机。这样 Fuzz 500 次每次同一输入必得同一输出，验收可复现。

---

## 四、痛点：效果是否"被免疫/被阻挡"散落在各 handler 特判

**① 痛点**：我们 `statusEffects` 是裸数组。某个原子要判断"目标会不会被这个效果影响"，现在只能在 handler 里自己写 `if (target.statusEffects.includes('xxx'))` 特判。机制一旦要加新的免疫类型，得改 N 个 handler。

**② Wesnoth 的代码手法**：它把免疫做成**数据化的布尔查询** `get_state("unslowable" / "undrainable" / ...)`，每个影响性特技**统一先查这个开关**再决定是否生效。免疫是单位身上的状态标志，不是特技逻辑里的硬编码分支。

**③ 我们怎么套用**：加一个**统一的 `queryState(unit, key): boolean`**（薄封装，内部仍读 `statusEffects` 数组）。原子的注册表声明里写 `requiresImmunityCheck: ['undrainable']`，管道在调用 handler 前先查 `queryState`，被免疫就直接记 `triggered:false` 进日志，**handler 内部零特判**。新增免疫类型只改注册表 + `queryState`，不动 handler。

---

## 五、痛点：结构化日志每原子缺推导链（冲突核查 C4）

**① 痛点**：`skillExecutor` 对 attack 已返回 `result.formula` 推导链，但 `effectExecutor` 对每个原子只回 `{type, triggered}`，没有 `rawPipe`（如 DAMAGE 的 base/暴击/高地拆项）。靶场要显示"真实执行了什么"做不到。

**② Wesnoth 的代码手法**：`battle_context_unit_stats` 在构造时就把**本场该单位的完整推导结论**（CTH、damage、各特技布尔）一次性固化下来，这份快照本身就是"日志"。它不需要事后反推，因为推导过程在算清时就被记录。

**③ 我们怎么套用**：套用"算清即记录"手法——handler 在返回结果时，**把本次计算用到的输入值与各修正因子一并塞进返回值**（`{type, triggered, rawPipe:{base, multiplierBreakdown:[...], final}}`）。管道把每个原子的 `rawPipe` 原样收进 `PipelineResult.details[]`。等于 Wesnoth 的"构造快照"思路，落在我们的结构化日志上。

---

## 六、痛点：effectExecutor 与源数据耦合，难做确定化测试

**① 痛点**：`effectExecutor` 的 handler 直接依赖活的单位对象、全局 `battleState`、甚至直接调用其他模块。单测一个原子要搭一整套活战场，Fuzz 也不好注入受控输入。

**② Wesnoth 的代码手法**：`specials_context_t` 把战斗双方单位 + 武器变量**全部显式装进一个上下文对象**传给求值函数；`modified_damage(context)` 是纯函数，给定同样的 context 必得同样结果，不偷偷读全局。

**③ 我们怎么套用**：这正是我们 `PipelineContext` 沙盒的设计初衷，但代码落地时要保证**所有 handler 只从 `ctx` 取数、不读全局、不互相调用**。测试用例只需构造一个 `PipelineContext` 字面量即可跑任意原子——Fuzz 500 也能靠"随机生成合法 ctx + 原子"灌入，不依赖真实战场。

---

## 七、痛点：新增原子类型要改管道多处（扩展性脆弱）

**① 痛点**：现在每加一种新原子，可能要改 `effectExecutor` 的 switch、改 `executeSync`、改日志、改编辑器枚举、改 shared-kernel 的 EFFECT_TYPE——四处分化（冲突核查 C5 的四头分化）。

**② Wesnoth 的代码手法**：特技/能力全部走 `unit_ability_t` **一个注册结构** + 统一的 `apply_to` / `active_on` / `priority` 字段。引擎靠这些字段**自动路由**生效时机与对象，新增一种特技只需在 WML 里加一段声明 + 一个 handler，引擎主流程不动。

**③ 我们怎么套用**：把 `ATOM_REGISTRY` 的每个原子做成**自描述结构**（含 `slot`、`applyTo`、`activeOn`、`priority`、`handler` 引用）。管道主流程只做"按 slot 分组 → 按序排序 → 逐个调 `atom.handler(ctx)`"，**新增原子只往注册表加一条，完全不改管道主干**。这把第九章"插槽自动挂载原子"从设计落到代码。

---

## 八、暂不套用的代码手法（标注原因）

| Wesnoth 代码手法 | 为何不套用 |
|---|---|
| `simulate()` 概率蒙特卡洛（`combatant.fight()`） | 我们是确定性结算 + 结构化日志，靶场不需概率模拟；引入会增加无谓复杂度 |
| WML 预处理器 / WFL 解释器 | 我们机制是 JSON 原子，不引入 DSL 解释器；沿用 JSON schema 即可 |
| C++ 模板/类型系统技巧 | 技术栈不同（TS/CJS），仅借思路不借语法 |
| 反击回合制 `firststrike`/`berserk` 的顺序反转实现 | 我们是单技能结算模型，无面对面对轰，暂无对应执行场景 |

---

## 九、代码层借鉴总表（机制已定，只借实现手法）

| 我们的机制（已定） | 代码执行痛点 | 借用的 Wesnoth 代码手法 | 套用方式 |
|---|---|---|---|
| 统一解释器 / 双路径 | C1 两路径重定向不一致 | 唯一求值入口 `modified_*` | 抽 `dispatchEffect()` 单函数，executeSingle/executeSync 共用 |
| mutationQueue / 防崩溃 | 内联改写留脏状态 | 先算清快照、后单向往回、模拟副本隔离 | 计算与写回分离；沙盒构造时快照数值 |
| EFFECT_MUTATION 多原子 | 结算顺序未定义 | 四阶段确定序 + `priority_` | 执行前对 atoms 做确定序排序 |
| statusEffects 免疫 | handler 散落特判 | `get_state()` 数据化布尔开关 | 加 `queryState()`，注册表声明免疫检查 |
| 结构化日志 9.2.3 | 每原子缺推导链（C4） | 构造时固化全量快照 | handler 返回 `rawPipe`，管道收进 details |
| PipelineContext 沙盒 | handler 耦合全局难测 | `specials_context_t` 显式上下文纯函数 | handler 只从 ctx 取数，可独立单测/Fuzz |
| ATOM_REGISTRY 扩展 | 加原子改四处 | `unit_ability_t` 自描述 + 自动路由 | 原子自描述(slot/applyTo/priority)，主干不变 |

---

## 十、一句话结论

> **机制是咱们的，Wesnoth 给的是"怎么把它写得更稳"的代码手艺**：唯一分发入口灭双路径分歧、先算清后写回防脏状态、确定序排序消未定义行为、`get_state` 把免疫从特判变查询、构造快照即日志、显式上下文纯函数让 Fuzz 可注入。七条全是**实现层技巧**，落地到 Phase 0 自检 / Phase 1-A / Phase 1-C，不改任何已定设计。

---

## 十一、原子中英对照翻译列（对齐桌面「67 项原子能力 A–K」）

> **重要更正**：此前本节误用 `EFFECT_TYPE`（effectExecutor 的 27 种"效果类型枚举"）冒充原子清单，与桌面 2026-08-07《词条库完善分析报告》真正在做、且已与用户确认的**67 项原子能力（A–K 十一组）**完全不相干。`EFFECT_TYPE` 是引擎"效果执行器"的内部类型标签，而 67 项原子是从 31 条词条卡经 L0–L6 七层模型拆出的**业务能力原子**，二者不可混为一谈。
> **本章对齐真相源**：桌面 `机甲战棋/词条库完善分析报告-2026-08-07.md` 的 §2.8（67 项汇总）与 §3.x（A/B/C/D… 带英文 key 的原子清单，key 已与用户确认）。英文 key 以桌面 3.x 节为准；桌面未给 key 的标「待定 key」。

### A. 触发时机（12）
| # | 中文原子能力 | 英文 key（对齐桌面 3.x） |
|---|---|---|
| A1 | 受击时 | `on_attacked` |
| A2 | 造成伤害后 | `on_damage_dealt` |
| A3 | 近战伤害结算后 | `post_melee_damage` |
| A4 | 击杀时 | `on_kill` |
| A5 | 攻击开始时 | `on_attack_start` |
| A6 | 目标选定时 | `on_target_selected` |
| A7 | 回合开始 | `on_turn_start` |
| A8 | 轮次开始 | `on_round_start` |
| A9 | 友军受击时 | `on_ally_attacked` |
| A10 | 移动路径计算时 | `on_move_path` |
| A11 | 获得空投时 | `on_airdrop_received`（已重定义，改用 `on_round_start`+坐标） |
| A12 | 踩点时 | `on_step_point` |

### B. 条件门槛（10）
| # | 中文原子能力 | 英文 key（对齐桌面 3.x B 组） |
|---|---|---|
| B1 | 射程内 | 待定 key（`conditions[].type:"in_range"`） |
| B2 | 互在射程 | 待定 key（`conditions[].type:"mutual_in_range"`） |
| B3 | HP 阈值 | 待定 key（`conditions[].type:"hp_threshold"`） |
| B4 | 属性对比 | `conditions[].type:"stat_compare"` |
| B5 | 共线并列 | `conditions[].type:"collinear_adjacent"` |
| B6 | 地形匹配 | `conditions[].type:"terrain_match"` |
| B7 | 伤害种类匹配 | `conditions[].type:"damage_kind_match"` |
| B8 | 未攻击过 | `conditions[].type:"has_not_acted"` |
| B9 | 不在扫描范围 | `conditions[].type:"not_in_scan"` |
| B10 | 限次（每局/阵营/个人/每回合） | `limit_scope` + `limit_count` |

### C. 数值生成与判定（7）
| # | 中文原子能力 | 英文 key（对齐桌面 3.x C 组） |
|---|---|---|
| C1 | 无判定 | `roll.type:"none"` |
| C2 | 单数值阈值二分 | `roll.check{op,threshold,then,else}` |
| C3 | 单数值多段 | `roll.segments[{range,do}]` |
| C4 | 对抗数值 | `roll.contest{a,b,compare}` |
| C5 | 数值参与运算 | `effects[].flat_value = base + roll.value` |
| C6 | 数值分色 | `roll.segments[{range,color}]` |
| C7 | 数值 vs 动态值（剩余血量） | `roll.check{op,compare_to:"target.hp"}` |

### D. 效果·伤害（7）
| # | 中文原子能力 | 英文 key（对齐桌面 3.x D 组） |
|---|---|---|
| D1 | 伤害加减值 | `effects[].type:"damage", flat_value` |
| D2 | 按 kind 减免 | `effects[].kind_modifiers{kinetic,beam}` |
| D3 | 伤害均摊 | `effects[].split:"equally"` |
| D4 | 伤害分担转移 | `effects[].transfer{from,to}` |
| D5 | 即死 | `INSTANT_KILL` |
| D6 | 同归于尽 | `MUTUAL_DESTRUCTION` |
| D7 | 延迟易伤标记 | `DELAYED_VULNERABILITY` |

### E. 效果·资源（3）
| # | 中文原子能力 | 英文 key |
|---|---|---|
| E1 | HP 回复（按属性系数/按地形） | `recovery` / `HEAL_BY_STAT` / `TERRAIN_CONDITIONED_REGEN` |
| E2 | 装备耐久回复 | `durability_change{supply}` |
| E3 | 耐久扣减 | `DURABILITY_CHARGE` / `durability_change` |

### F. 效果·状态（6）
| # | 中文原子能力 | 英文 key |
|---|---|---|
| F1 | 属性增减益 | `MODIFY_STAT` / `status` |
| F2 | 机动差项修正 | `MOBILITY_DIFF_MODIFY` |
| F3 | 跳过机动判定 | `SKIP_MOBILITY_CHECK` |
| F4 | 充能层（N 次） | `CHARGE_STACK_N` |
| F5 | 互斥组 | `EXCLUSIVE_GROUP` / `MUTEX_STATUS` |
| F6 | 永久失效 | `PERMANENT_DISABLE` |

### G. 效果·行动（8）
| # | 中文原子能力 | 英文 key |
|---|---|---|
| G1 | 额外回合 | `GRANT_EXTRA_TURN` / `GRANT_TURN` |
| G2 | 放弃移动 | `FORFEIT_MOVE` |
| G3 | 预支行动 | `ACTION_DEBT` / `PREPAY` |
| G4 | 预支回合 | `TURN_DEBT` / `PREPAY_TURN` |
| G5 | 抢占顶替 | `ACTION_PREEMPTION` / `TAKEOVER_TURN` |
| G6 | 行动序改写 | `ACTION_ORDER_REWRITE` / `REWRITE_ORDER` |
| G7 | 授予或剥夺行动 | `ACTION_GRANT/DENY` / `GRANT_ACTION`/`REVOKE_ACTION` |
| G8 | 防连锁 | `NO_CONSECUTIVE` |

### H. 效果·空间（6）
| # | 中文原子能力 | 英文 key |
|---|---|---|
| H1 | 射程形状扩展 | `RANGE_SHAPE_EXTEND` |
| H2 | 扇形 AOE | `AOE_SECTOR` |
| H3 | 强制位移 | `FORCED_REPOSITION` / `DISPLACEMENT` |
| H4 | 共线阻挡 | `COLLINEAR_BLOCKADE` / `BLOCKADE` |
| H5 | 区域揭示 | `REVEAL_AREA` / `REVEAL` |
| H6 | 生成地图实体 | `SPAWN_ENTITY` / `SPAWN_ITEMS` |

### I. 效果·信息（3）
| # | 中文原子能力 | 英文 key |
|---|---|---|
| I1 | 可见性 | `set_visibility` / `VISIBILITY` |
| I2 | 可选中性 | `TARGETABILITY` |
| I3 | 扫描范围 | `SCAN` / `not_in_scan`(B9) |

### J. 效果·所有权（1）
| # | 中文原子能力 | 英文 key |
|---|---|---|
| J1 | 装备抢夺转移 | `ITEM_OWNERSHIP_TRANSFER` / `PLUNDER` |

### K. 元能力（4）
| # | 中文原子能力 | 英文 key |
|---|---|---|
| K1 | 词条改写词条（解锁前置） | `PREREQUISITE_UNLOCK` / `meta_ops.unlock` |
| K2 | 多技能槽占用 | `SLOT_COST_N` / `meta_ops.dual_slot` |
| K3 | 玩家可选发动 | `OPTIONAL_ACTIVATION` |
| K4 | 打断响应窗口 | `INTERRUPT_WINDOW` |

> **统计**：A12 + B10 + C7 + D7 + E3 + F6 + G8 + H6 + I3 + J1 + K4 = **67 项**，与桌面 §2.8 去重后 67 项一致。
> **key 来源说明**：带反引号的为桌面 3.x 节已确认英文 key（或正文抽取的业务 key）；标「待定 key」的 B1–B3 桌面仅给中文语义，key 命名待用户在 Phase A 收口（见桌面报告 C5/C6 待确认项）。`EFFECT_TYPE` 的 27 项枚举（DAMAGE/RECOVERY/STATUS…）是**引擎执行器内部标签**，可视为本 67 项在 L4 效果层的"落地别名"，二者不是同一张表，请勿混用。

## 附录：信息来源
- `wesnoth/wesnoth@master`：`src/actions/attack.cpp`、`src/units/attack_type.cpp`、`src/units/abilities.cpp`
- DeepWiki：`wesnoth/wesnoth / 5.2 Abilities and Combat Mechanics`
- Wesnoth Wiki：《Wesnoth Formula Language》《ReferenceWML》
