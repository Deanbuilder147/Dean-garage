# 机甲战棋项目 · 项目总结

> 更新时间：2026-07-23
> 维护：AI 代理（依据对话历史、git 状态、服务器部署状态自动生成）

## 一、项目概览
机甲战棋（Mecha Universe）是一个基于 Vue3 + Node/TS + Docker 的六边形网格战棋游戏。核心由网关（`mecha-gateway`）、前端（`mecha-frontend`）、通信服务（`mecha-comm`）、数据库（`mecha-battle-db`）四容器组成；战斗逻辑以 `.cjs` 核心引擎打包进网关镜像。

- **代码仓库**：当前工作空间 `mecha-universe-engine/`（git，origin/main）
- **本地桌面归档**：`/Users/dingxuyang/Desktop/机甲战棋/`（历史报告 + 资产报告 + 本总结）
- **服务器**：`106.54.197.69:/root/mecha-universe-engine/`（rsync 同步，非 git）
- **在线访问**：前端 `:8081`（nginx 反代 `/api/*` → gateway `:3006`）

## 二、当前模块状态
| 模块 | 状态 | 说明 |
|---|---|---|
| backend-gateway | 活跃/健康 | 路由层 + Excel 导入 + `/attack`/`/skill` 距离校验 + 地形素材 + 地形代价表 |
| frontend | 活跃/健康 | 平顶六边形 Canvas 引擎、战场编辑器、整备室 Royroy、单位编辑器 |
| combat-service(.cjs) | 已部署 | skillExecutor/damagePipe/terrainMovement/equipmentDurability/battleState |
| hangar-service | 已部署 | 单位库 DB + Excel 导入 |
| map-service | 已部署 | 地图 hex 工具 |
| shared-kernel | 已部署 | 跨服务类型（Royroy/装备耐久） |

## 三、近期关键变更（2026-07）
1. **距离射程审计报告 #1~#4**（P0/P1，已全部部署）：新增 `/attack` 路由（修复攻击 404）、`/skill` 距离+最小距离校验、前端 `skillRangeHexes` 排除 `min_range`。
2. **平顶六边形 / Canvas 架构重构**（宪法 v2.0）：`hexUtils.js` 纯数学化，绘制函数迁入 `hexDraw.js`，`HexGridCanvas.vue` 退役由 `HexGridCanvasEngine.vue` 取代；planar 编辑器修复整张空白 + 拾取错位两坑。
3. **移动范围公式修正**：移动值=移动力总预算（不再 `/10`），后端改地形加权 Dijkstra，前端加权 BFS。
4. **Royroy 与阵营轮转**：属性模型 + `/action` 路由 + 阵营轮转 AP 规则（2026-07-22 部署）。
5. **地形素材上传** + **单位编辑器 codename/七视图丢失修复** + **主图上传 400/404 系列修复**。

## 四、服务器清理（2026-07-23 执行）
- 删除旧容器 `nginx-ssl`（Exited 4 周，不在 compose）。
- 删除旧命名/时间戳镜像：`mecha-gateway:latest`、`mecha-universe-engine-frontend:latest`、`mecha-universe-engine_20260623*`。
- 仅保留运行所需：`mecha-universe-engine-mecha-{gateway,frontend,comm}` + `postgres:14-alpine` + 基础镜像（alpine/nginx/node）。
- **未触碰**：`mecha-battle-db`（用户硬性要求）；`mecha-comm`（compose 服务，Created 未运行，保留）。

## 五、用户反馈处理
- **已找到反馈记录**（非"未找到反馈"）：见 `桌面/机甲战棋/机甲战棋资产整理与改动总报告_2026-07-23.md` 第五节。
- 已闭环：行动代号/七视图丢失、主图上传 400/404、攻击路由 404、距离校验。
- 待闭环：①"依然没解决"（疑似浏览器缓存/未硬刷）②战场 token 主图化（独立设计，待主图稳定后另议）。

## 六、已知缺口（运行态，待开发）
1. 胜利条件 `checkVictoryConditions` 未接入网关实时结算（仅配置可存取）。
2. 掩体系统仅标签未实装。
3. 装备独立耐久类型已定义但结算未全面接入。

## 七、资产归档结构
- `历史备份/历史报告/` ← 桌面 `机甲战棋/历史报告` 完整归档（含 2026-07-23 桌面散落报告）。
- `历史备份/WorkBuddy备份/` ← 按日期备份 WorkBuddy/brain/Claw 独有历史报告（20260412~20260421 + Excel导入 + GitHub包）。
- `桌面/机甲战棋/机甲战棋资产整理与改动总报告_2026-07-23.md` ← 全部改动 + 用户反馈汇总。

## 八、部署与同步命令
```bash
# 本地 → 服务器
rsync -az --delete --exclude node_modules --exclude .git --exclude dist \
  --exclude '*.db' --exclude /data --exclude '*.log' \
  -e "ssh -i ~/Desktop/watson.pem" \
  /本地/mecha-universe-engine/ root@106.54.197.69:/root/mecha-universe-engine/

# 前端改动后重建
ssh -i ~/Desktop/watson.pem root@106.54.197.69 \
  "cd /root/mecha-universe-engine/frontend && npm run build"
docker compose build --no-cache mecha-gateway mecha-frontend
docker compose up -d --no-deps mecha-gateway mecha-frontend
```

---
*本总结随每次重大变更更新。详细改动见 `机甲战棋资产整理与改动总报告_2026-07-23.md`。*
