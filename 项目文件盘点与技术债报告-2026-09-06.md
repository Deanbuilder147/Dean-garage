# 项目文件盘点与技术债报告

> 生成日期：2026-09-06
> 盘点范围：本机工作区 `/Users/dingxuyang/CodeBuddy/20260604120036`、桌面 `机甲战棋` 与 `alpha3.0 系统` 文件夹、桌面其它报告/方案文件，以及 `.codebuddy` 下的 soul/memory 等持久化文件。
> 说明：报告文件以 `.md` 为主，兼及少量 `.docx/.pdf/.html/.vue/.js` 等方案/原型文件；`mecha-universe-engine` 内 2.3 万份代码文件不计入“报告”，仅统计其根目录报告与代码级 TODO/FIXME 标记。

---

## 一、文件盘点总览（数量口径）

| 位置 | 文件总数 | 其中报告(.md) | 说明 |
|---|---|---|---|
| 工作区根（报告层） | 32（.md） | 32 | 六段式/原子/词条库/前端/战斗架构/审计等顶层报告 |
| 工作区 `历史备份/` | 139 | 119 | 历史备份报告堆叠 |
| 工作区 `历史开发报告/` | 217 | 194 | 历史开发报告堆叠 |
| 工作区 `mecha-universe-engine/`（项目根报告） | 8（.md） | 8 | README/code-index/六角格测距/词条编辑器核查等 |
| 工作区 `mecha-universe-engine/`（代码） | 23451 | — | 8561 js / 5593 ts / 2542 map / 1567 md / 1409 json（代码库，非报告） |
| 桌面 `机甲战棋/` | 699 | 267 | 见下方子目录细分 |
| 桌面 `alpha3.0 系统/` | 38 | 38 | 核心方案 + 参考文档 |
| `.codebuddy/memory/` | 31 | 31 | MEMORY.md + 30 份日志（2026-08-02 ~ 2026-09-05） |
| 桌面其它技术报告 | 若干 | 若干 | Godot-MCP 适用性分析、正六边形地图制作方法等 |
| 持久化配置（agents/rules/plans/skills/teams） | 5 | 4(.md/.mdc) | 见第六节 |

### 桌面 `机甲战棋/` 子目录细分（267 .md 分布）
- `历史报告/`：207 文件（187 .md）—— 审计报告、Phase 系列、更新报告、诊断报告等历史堆积
- `项目资产/`：35 .md —— 项目总结/资产总览/术语标准化/全栈 API 清查/战斗管道审计/避坑报告等
- `过期报告/`：11 文件（10 .md）—— 已标记过期，含技能射程排查、前端改造等
- `经验库/`：9（8 .md）—— 开发经验纪要
- `设计资料/`：79（19 js / 17 vue / 10 html / 8 png / 6 css）—— 设计原型代码
- `角色视图/`：47（37 png / 6 jpg / 4 json）、`选手卡/`：13（xlsx）、`docs/`、根目录 ~13 .md
- `backup0729/`：279 文件（74 cjs / 71 js / 34 ts / 32 vue / 24 json）—— **旧代码快照，疑似过期备份**
- `待完善内容/`：存在但未展开统计（命名即表明未完成内容）

### 桌面 `alpha3.0 系统/` 细分（38 .md）
- 根目录 13 份核心方案：六段式编排与 67 原子全景、数据契约三端错位与 Zod 强契约、引擎到战场回写链路断点、可见性与隐匿协同缺漏、六边形地图绘制、词条库与战斗引擎补完计划等
- `参考文档/`：25 .md

---

## 二、关键报告清单（按主题归类）

### 2.1 六段式 / 原子 / 词条库（真相源主线）
- 核心-并联分支六段式改造与扫射功能落地方案（alpha3.0）
- 核心-词条库与战斗引擎-六段式编排与67原子全景报告（alpha3.0）
- 原子清单-可编辑版、核心-数据契约三端错位与Zod强契约落地方案（alpha3.0）
- 词条库中枢改造设计文档-v1.0、词条编辑器-数值字段与系统联动核查报告（mecha 项目根）
- GlossaryForgePrismView/BlueprintView 相关：glossary-studio-架构重构文档、glossary-studio-五视图功能定位与待讨论清单（工作区根）
- 词条库分镜.svg / 词条库.svg（桌面，原型权威输入）

### 2.2 战斗架构 / 引擎
- 核心-战场实体真相源七模块现状与P0P1P2改动清单、引擎到战场作用回写链路断点与契约收敛顺序（alpha3.0）
- 战棋战斗引擎替换评估与适用性检查报告、韦诺之战战斗引擎对比分析（工作区根）
- 阶段二C-Segment伤害管线重接方案、扫射Segment归一化方案、ROLL分段取代式改造方案、Segment超集方案（工作区根）
- 技能-隐匿功能专项报告（工作区根 + alpha3.0）

### 2.3 前端 / UI / 样式
- 前端改造方案-2026-08-23、前端全局布局塌缩问题-根因分析报告、前端尺寸与按钮规格说明（工作区根）
- Godot-UI设计指南-锁定版、Godot六边形UI样式规范-v01、Godot全功能UI设计方案-C档（工作区根）
- 触控方案A-实施文档、按钮样式设计规范-RetroFuturism.html、8081功能搬移Godot-待勾选清单（工作区根）
- 正六边形地图制作方法总结.md、Godot-MCP-Native-适用性分析报告.md（桌面）

### 2.4 部署 / 运维 / 审计
- 全栈API与通信节点全局清查报告（工作区根 + 机甲战棋/项目资产）
- 数据契约错位与非对称读写-全盘代码审计报告、情况调查报告-端口连接与词条库排版（工作区根）
- 未提交项-历史报告交叉对比与连接推演报告、新旧对照表-词条库与战斗管线（工作区根）
- 机甲战棋-文档版本符合性排查报告、服务器地址.mdc（rules）、前端改造.md（agents）

### 2.5 诊断 / 缺陷 / 版本
- 战场技能丢失问题诊断报告-2026-08-05、项目缺陷-网络资源调研（机甲战棋根）
- 手动输入数值入口与输入框样式核查报告、前端全局布局塌缩问题（工作区根）
- 机甲战棋-文档版本符合性排查报告（机甲战棋根，提示版本已混乱）

---

## 三、现阶段项目情况梳理

**产品形态**：机甲战棋（mecha-universe-engine），Docker 多服务架构：
- `mecha-gateway`(3006) / `mecha-frontend`(8081→80) / `mecha-comm`(3005) / `mecha-battle-db`(5432)
- 战斗逻辑打进 gateway，无独立 combat 容器；服务器 `106.54.197.69`（非 git，rsync 同步）

**词条库两条线并存**：
1. 真实编辑器：`GlossaryForgePrismView.vue`（/glossary-forge-prism）、`GlossaryForgeBlueprintView.vue`、旧单体 `GlossaryStudio.vue`/`GlossaryHubNew.vue`（各内嵌 atomGroups 副本）。
2. 分镜原型：`prototype/glossary-prototype.html` + `词条库分镜.svg`，三镜头（类型轮播→词条网格→词条编辑），已部署到 `/glossary2`，菜单「词条展示」已外链挂接；**镜头3 仍为兜底说明，未实现**。

**当前活跃工作（据 2026-09-05 日志）**：glossary 分镜原型在端口 80 的 `/glossary2` 独立静态站，最终应统一进 mecha-frontend（单一入口 8081 约束）。下一步为镜头3 词条编辑实现。

**持久化记忆体系**：`.codebuddy/memory/MEMORY.md`（长期精简记忆，含部署强约束/战棋宪法/六段式真相源/样式偏好等） + 30 份每日日志，是跨会话的事实真相源。

---

## 四、技术债系统识别（按优先级）

### P0 — 约束违反 / 一致性硬风险
| 编号 | 问题 | 证据位置 | 建议 |
|---|---|---|---|
| TD-01 | glossary 原型以独立静态站占用端口 80（`/glossary2`），违反“单一入口 8081、严禁新建子端口”强约束 | MEMORY.md 部署与运维/词条库分镜原型节；2026-09-05 日志 | 按 A/B 方案统一进 mecha-frontend 词条库页面，撤掉独立站 |
| TD-02 | atomGroups **三副本须字节一致**（GlossaryStudio/GlossaryHubNew/battle/glossary/atomGroups.js），改原子须三处同步，极易错位 | MEMORY.md 六段式与原子节；代码内 3 处内嵌副本 | 抽离为单一共享模块，删除内嵌副本，CI 加 diff 校验 |
| TD-03 | `user.id` 才是主键，凡取 `userId` 是潜在 bug（已多次踩坑） | MEMORY.md 部署与运维 | 全局 grep `userId` 替换为 `user.id`，加 lint 规则 |
| TD-04 | 数据契约三端错位 / 非对称读写（已有全盘审计，但属结构性债） | 数据契约错位与非对称读写-全盘代码审计报告.md | 以 Zod 强契约为唯一真相源，前端禁用手编常量 |

### P1 — 部署 / 运维脆弱点
| 编号 | 问题 | 证据位置 | 建议 |
|---|---|---|---|
| TD-05 | ~~Lighthouse API 集成令牌失效（Token verification failed）~~ **已解决（2026-09-06 用户重新授权后集成 API 恢复可用）**；部署仍走 SSH/SCP+systemd（`deploy_project_preparation` 上传超时未变） | MEMORY.md 部署通道节 | 已恢复；部署通道保持 SSH/SCP+systemd |
| TD-06 | ~~本地产物 `dist-dev/` 会污染 dist~~ **误报（2026-09-06 核查）：全仓 grep `dist-dev` 0 命中，构建脚本为 `node scripts/check-mirror.mjs && vite build`，无 dist-dev 引用、目录亦不存在** | MEMORY.md 前端构建节（陈旧） | 关闭：不实删，保留原脚本 |
| TD-07 | ~~docker-compose `version` 属性已废弃（部署时告警）~~ **已解决（2026-09-06 删除 `version: '3.8'`）** | 部署日志 `version is obsolete` | 已删除 version 字段 |
| TD-08 | ~~orphan containers：`mecha-battle-db` 因 `--no-deps` 变 orphan~~ **误报/误导（2026-09-06 核查）：`docker-compose.yml` 中 `mecha-battle-db` 已明确定义；`docs/思想纪要-开发设计哲学.md` L88 明确 `--no-deps` 是「避孤儿冲突」的正确做法，非 bug** | 部署日志 `Found orphan containers` | 关闭：除非服务器 `docker compose ps` 实测 battle-db 真 orphan，否则不实改 |
| TD-09 | ~~前端 JS chunk >500kB（实测 index 623KB）未做代码分割~~ **已解决（2026-09-06）：`vite.config.js` 加 manualChunks 拆 vue/net/pixi vendor，重建后 `index` 410KB<500KB，全量无 >500KB 告警** | 构建日志 chunk size 警告 / 实测 | 已修复；vendor 拆为 vue(104KB)/net(85KB)/pixi 独立可缓存块 |
| TD-10 | ~~非 git 项目~~ **误报（2026-09-06 核查）：`git rev-parse`=true，项目是 git 仓库（287 文件未提交）；rsync 仅用于「服务器非 git」的部署，属合理选择** | MEMORY.md 部署与运维（陈旧） | 关闭/并入 N1：源已是 git，漂移问题归 N1 |

### P2 — 代码级标记（TODO/FIXME/deprecated 扫描命中 12 文件，2 为非代码）
- combat-service：`damagePipe.cjs`、`combatIntegrator.cjs`、`skillExecutor.cjs`、`effectExecutor.cjs`、`vendor/dicescript.js`/`.cjs`（第三方 vendor，含 deprecated）
- backend-gateway：`src/routes/maps.ts`
- frontend：`HexGridCanvasEngine.vue`、`hexUtils.js`（`hexUtils.js` 的 `@deprecated` 为**故意委托**给 pointyTopCenter/pointyTopToHex，良性，保留）
- shared-kernel：`enums.ts`（自动生成但仍命中）
- 根：`docker-compose.yml`（version 已于 TD-07 删除）、词条编辑器数值字段核查报告（2 份，其中 _副本 已于 TD-13 归档）
> 建议：对以上 13 文件逐条核实标记含义，vendor 第三方文件单独隔离、不混入业务修复。

### P3 — 历史 / 过期 / 重复文件堆积（报告债）
| 编号 | 问题 | 位置 |
|---|---|---|
| TD-11 | 历史报告大量堆积未归档：机甲战棋/历史报告 187 .md、工作区 历史备份 119 .md、历史开发报告 194 .md | 多处 |
| TD-12 | 过期备份快照 `backup0729/`（279 文件）疑似长期未清理 | 桌面/机甲战棋 |
| TD-13 | 重复/副本文件：`词条编辑器-数值字段与系统联动核查报告-2026-08-04_副本.md` | mecha 项目根 |
| TD-14 | `待完善内容/`、`过期报告/`（10 .md）、`待确认技术资产清单.md` 表明存在大量未闭环项 | 机甲战棋 |
| TD-15 | 文档版本已混乱：`机甲战棋-文档版本符合性排查报告.md` 存在即说明版本管理失效 | 机甲战棋根 |

### P4 — 设计 / 视觉死代码与未落地功能
| 编号 | 问题 | 位置 |
|---|---|---|
| TD-16 | ~~前端绿色 `.tactical-*` 死代码~~ **误报（2026-09-06 复核）：`.tactical-*` 是活跃 UI 组件库样式（ui/Button·Card·Tag + NewHomeView `.tactical-header` + NewBattleView `.ap-tactical`），删除会破坏样式，非死代码** | glossary-theme.css / variables.css（MEMORY 样式节） |
| TD-17 | 镜头3 词条编辑**生产 Vue 组件未实现**（仅 `prototype/glossary-prototype.html` 的 `buildLens3()` 有可用原型） | prototype/glossary-prototype.html；GlossaryForgePrismView.vue |
| TD-18 | 文档查找红线：权威设计文档散在两处（工作区根 `docs/` 与 `mecha-universe-engine/docs/`），**实测两处均存在且内容不同** | MEMORY.md 六段式节 |

### P5 — 工作树未提交漂移（N1，本次新增·高危）
| 编号 | 问题 | 证据 |
|---|---|---|
| N1 | ~~工作树 **287 个文件未提交**~~ **已解决（2026-09-06）：`git add -A && git commit` 固化基线，commit `40d1631`，工作树 0 未提交**；按 MEMORY 约定 backend 改动须 rsync 上服务器并重建容器使线上=最新 | `git status --short | wc -l` = 0（2026-09-06 提交后） |

> **N1 已解决（2026-09-06）**：commit `40d1631` 已固化源码基线（998 文件）。**后续**：backend/combat 改动需 rsync 上服务器并重建对应容器，使线上=最新（部署动作待用户触发）。

---

## 五、行动建议（优先级排序）

1. **立即（P0）**：消 glossary 独立站、统一进 mecha-frontend（TD-01）；抽离 atomGroups 单一真相源并删除三处内嵌副本（TD-02）；全量替换 `userId`→`user.id`（TD-03）。
2. **近期（P1）**：修复 Lighthouse 凭据或固化 SSH 部署脚本（TD-05）；构建前置清理 dist-dev（TD-06）；清理 docker-compose version 与 orphan（TD-07/08）；前端代码分割（TD-09）。
3. **计划（P2/P3）**：逐条清理 13 处 TODO/FIXME（TD-02 代码扫描）；归档/清理历史报告与 backup0729（TD-11/12）；删除副本与过期目录（TD-13/14）；建立文档单一真相源与版本规范（TD-15/18）。
4. **功能闭环（P4）**：实现镜头3 词条编辑（TD-17）；删除 `.tactical-*` 死代码（TD-16）。

---

## 六、soul / memory 等持久化文件清单

- **memory（已定位）**：`.codebuddy/memory/MEMORY.md`（长期记忆，含部署强约束/战棋宪法/六段式真相源/样式偏好/战斗架构）+ 30 份每日日志（2026-08-02 ~ 2026-09-05）。← 跨会话事实真相源。
- **agents**：`.codebuddy/agents/前端改造.md`
- **rules**：`.codebuddy/rules/服务器地址.mdc`
- **plans**：`.codebuddy/plans/` 下 3 份（部署404与单位编辑器Excel修复、词条Excel读取上传功能、combat-service-bridge-gateway(未完成)）
- **skills / teams**：目录存在但为空
- **soul**：**本机工作区、桌面、mecha 项目中均未定位到 `soul` 文件**（以 `*soul*` 递归搜索返回 0）。若该文件存在，可能命名不同或位于工作区外全局配置；建议确认其实际路径后补录。

---
*本报告由文件盘点 + 关键词扫描（技术债/不一致/错位/TODO/FIXME/deprecated）+ MEMORY.md 权威约束交叉核对生成。代码级标记与历史报告的具体修复需后续逐条核实。*
